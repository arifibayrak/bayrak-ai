---
phase: 15-chainage-as-built-view-approval-snapshot
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - src/lib/bot-audit.ts
  - src/lib/bot-messages.ts
  - src/db/migrations/0013_v4_chainage_backfill.sql
  - src/db/migrations/meta/_journal.json
autonomous: true
requirements: [CHN-01, CHN-02, CHN-03]
must_haves:
  truths:
    - "Approving a submission writes submissions.chainage_m = ROUND(segment_fraction × total_length_m, 2) and route_geometry_version inside the SAME approval transaction (D-03 immutability, Pitfall 1)"
    - "The worker approval Telegram message includes a calibrated chainage line 'km X+YYY' using chainage_m + chainage_offset_m (Open Question 1 resolved: yes)"
    - "Migration 0013 backfills chainage_m for existing approved submissions from current route geometry, marked as estimated"
    - "bot-audit.ts never calls auth(), logOfficeActivity, or after() (Pitfall 5)"
  artifacts:
    - path: "src/db/migrations/0013_v4_chainage_backfill.sql"
      provides: "Additive UPDATE backfill of chainage_m + route_geometry_version for approved rows"
      contains: "UPDATE submissions"
    - path: "src/lib/bot-audit.ts"
      provides: "chainage snapshot write in approval transaction + calibrated worker notification"
      contains: "chainage_m"
  key_links:
    - from: "src/lib/bot-audit.ts"
      to: "submissions.chainage_m"
      via: "second tx.update inside txDb.transaction, Postgres ROUND"
      pattern: "chainageM.*ROUND|ROUND.*segment_fraction"
    - from: "src/lib/bot-audit.ts"
      to: "src/lib/format-chainage.ts"
      via: "import formatChainage for worker approval line"
      pattern: "formatChainage"
---

<objective>
The chainage WRITE this phase owns: snapshot `chainage_m` + `route_geometry_version` inside `handleAuditDecision`'s approval transaction, add a calibrated chainage line to the worker approval Telegram message, and author the one-time backfill migration 0013 for historical approvals.

Purpose: CHN-03 immutability — there must be no window where `status='approved'` but `chainage_m IS NULL` (Pitfall 1). The Telegram line and dashboard/export must all read the same calibrated value (Pitfall 13). Backfill gives the strip view real data for pre-Phase-15 approvals.
Output: modified `bot-audit.ts` + `bot-messages.ts`, new `0013_v4_chainage_backfill.sql` (authored, NOT yet applied — plan 04 applies it), updated journal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15-chainage-as-built-view-approval-snapshot/15-RESEARCH.md
@.planning/phases/15-chainage-as-built-view-approval-snapshot/15-PATTERNS.md

<interfaces>
<!-- Extracted from codebase — executor uses these directly, no exploration needed. -->

src/db/schema/submissions.ts (columns):
  personId uuid NOT NULL → people.id
  projectId uuid NOT NULL → projects.id
  segmentFraction numeric(10,8)        // 0.0–1.0, written at submission (Phase 4)
  chainageM numeric('chainage_m', { precision: 10, scale: 2 })   // currently NULL for all rows
  routeGeometryVersion integer('route_geometry_version')          // currently NULL

src/db/schema/routes.ts: totalLengthM, geometryVersion, chainageOffsetM

bot-audit.ts approve transaction (lines ~415-449) — EXISTING shape:
  await txDb.transaction(async (tx) => {
    const affected = await tx.update(sub2)
      .set({ status:'approved', decidedBy: auditorPerson.id, decidedAt: new Date() })
      .where(and2(eq2(sub2.id, submissionId), eq2(sub2.status,'pending_audit')))
      .returning({ id, quantity, boqItemId });   // ← extend .returning here
    if (affected.length === 0) throw new AlreadyResolvedError();
    await tx.update(boq2).set({ approvedQty: sql2`approved_qty + ${affected[0].quantity}` })...
  });

bot-messages.ts: `workerApproved` is a FIXED string at line 172 (must become a function accepting an optional chainage label).

src/db/migrations/meta/_journal.json: last entry idx=12, tag "0012_v4_route_source_documents", when=1780088633500 (a 2026 epoch). New entry MUST have when > 1780088633500.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chainage snapshot write inside the approval transaction</name>
  <files>src/lib/bot-audit.ts</files>
  <read_first>
    - `src/lib/bot-audit.ts` lines 404-536 (the approve block — RE-READ in full before editing)
    - `.planning/phases/15-chainage-as-built-view-approval-snapshot/15-PATTERNS.md` §`src/lib/bot-audit.ts` (insertion point lines 447-462; getTxDb cleanup; two-update-in-one-tx precedent at line 444-449; Pitfall 5 constraints)
    - `.planning/phases/15-chainage-as-built-view-approval-snapshot/15-RESEARCH.md` §Pattern 1 (recommended approach: extend .returning() with segmentFraction + projectId, fetch route totalLengthM + geometryVersion in same tx, then second UPDATE with Postgres ROUND), §Pitfall 1
  </read_first>
  <action>Inside the existing `txDb.transaction` approve block: (1) extend the first UPDATE's `.returning({...})` to also return `segmentFraction: sub2.segmentFraction` and `projectId: sub2.projectId`. (2) After the existing BOQ `approved_qty` increment, fetch the route snapshot in the same tx: `tx.select({ totalLengthM: routes.totalLengthM, geometryVersion: routes.geometryVersion, chainageOffsetM: routes.chainageOffsetM }).from(routes).where(eq2(routes.projectId, affected[0].projectId)).limit(1)` (import routes schema via the existing dynamic-import style). (3) Guard `if (route?.totalLengthM && affected[0].segmentFraction)` then issue a SECOND `tx.update(sub2)` setting `chainageM: sql2\`ROUND(${affected[0].segmentFraction}::numeric * ${route.totalLengthM}::numeric, 2)\`` and `routeGeometryVersion: route.geometryVersion`, WHERE `eq2(sub2.id, submissionId)`. ROUND happens in Postgres — NEVER multiply the numeric strings in JS (money-math discipline). Capture the resulting chainage (re-read it from the row, or recompute via Postgres in a returning) and `chainageOffsetM` into outer-scope vars for Task 2's worker notification. Do NOT add auth(), logOfficeActivity, or after() (Pitfall 5). All DB access inside the tx uses the `tx` handle (txDb pool), not `@/db`.</action>
  <verify>
    <automated>grep -v '^//' src/lib/bot-audit.ts | grep -c "ROUND" ; grep -cE "logOfficeActivity|after\(|@/lib/auth" src/lib/bot-audit.ts</automated>
  </verify>
  <done>chainage_m + route_geometry_version written in the same tx via Postgres ROUND; grep for ROUND ≥ 1; grep for logOfficeActivity/after()/@/lib/auth returns 0 (Pitfall 5 intact). `npx tsc --noEmit` passes.</done>
</task>

<task type="auto">
  <name>Task 2: Calibrated chainage line in worker approval message (Open Question 1)</name>
  <files>src/lib/bot-messages.ts, src/lib/bot-audit.ts</files>
  <read_first>
    - `src/lib/bot-messages.ts` line 172 (`workerApproved` current fixed-string form)
    - `src/lib/bot-audit.ts` lines 510-523 (worker notification `bot.api.sendMessage(..., MESSAGES.workerApproved)`)
    - `.planning/phases/15-chainage-as-built-view-approval-snapshot/15-RESEARCH.md` §Open Questions #1 (recommendation: add chainage line; format as formatChainage(chainageM + offsetM)), §Pattern 5
    - `src/lib/format-chainage.ts` (from plan 01)
  </read_first>
  <action>Convert `MESSAGES.workerApproved` from a fixed string to a function `workerApproved(chainageLabel?: string)` that appends a Turkish chainage line (e.g. "📍 Konum: {chainageLabel}") when `chainageLabel` is provided, otherwise returns the original text unchanged (so other callers, if any, stay valid). In `bot-audit.ts` post-commit worker-notification block, compute the calibrated label only when chainage is known: `formatChainage(Number(chainageM) + Number(chainageOffsetM ?? 0))` using the values captured in Task 1, and pass it to `MESSAGES.workerApproved(label)`. Import `formatChainage` from `@/lib/format-chainage`. This is a post-commit best-effort path already wrapped in try/catch — keep that. The calibrated value here MUST equal the dashboard bucketStart and Excel first column (Pitfall 13 consistency).</action>
  <verify>
    <automated>grep -c "formatChainage" src/lib/bot-audit.ts</automated>
  </verify>
  <done>workerApproved is parameterized; bot-audit imports formatChainage and passes the calibrated label; `npx tsc --noEmit` passes; all existing bot-audit tests still green via `npx vitest run -t "audit"`.</done>
</task>

<task type="auto">
  <name>Task 3: Backfill migration 0013 (authored, registered — NOT applied)</name>
  <files>src/db/migrations/0013_v4_chainage_backfill.sql, src/db/migrations/meta/_journal.json</files>
  <read_first>
    - `src/db/migrations/0010_v4_routes_ext.sql` lines 1-16 (HAND-WRITTEN header comment discipline; D-49 apply-via-migrate.ts note; statement-breakpoint rule)
    - `src/db/migrations/meta/_journal.json` last entries (idx=12 tag 0012, when=1780088633500 — new entry needs idx=13, when strictly greater, a 2026 timestamp)
    - `.planning/phases/15-chainage-as-built-view-approval-snapshot/15-RESEARCH.md` §Pattern 2 (full backfill SQL), §Pitfall 7, §Open Questions #3 (total_length_m NULL handling)
    - `.planning/phases/15-chainage-as-built-view-approval-snapshot/15-PATTERNS.md` §`0013_v4_chainage_backfill.sql`
  </read_first>
  <action>Create `src/db/migrations/0013_v4_chainage_backfill.sql` with the 0010-style HAND-WRITTEN header (apply only via `npx tsx src/db/migrate.ts` per D-49; note this is DML with a single statement so no `-- > statement-breakpoint` needed but document it for future editors; note values are ESTIMATED snapshots using current geometry, not true historical snapshots). The statement: `UPDATE submissions s SET chainage_m = ROUND(s.segment_fraction::numeric * r.total_length_m::numeric, 2), route_geometry_version = r.geometry_version FROM routes r WHERE r.project_id = s.project_id AND s.status = 'approved' AND s.chainage_m IS NULL AND s.segment_fraction IS NOT NULL AND r.total_length_m IS NOT NULL;`. Register the migration in `_journal.json` as idx=13, version "7", tag "0013_v4_chainage_backfill", breakpoints true, with a `when` value strictly greater than 1780088633500 (a 2026 timestamp — a pre-0012 or 2025 epoch causes Drizzle to silently skip the migration, per the Phase 14 lesson). Do NOT edit 0010-0012 (immutable, hash-locked). Do NOT apply the migration in this plan — plan 04 owns the [BLOCKING] apply.</action>
  <verify>
    <automated>test -f src/db/migrations/0013_v4_chainage_backfill.sql && grep -c "UPDATE submissions" src/db/migrations/0013_v4_chainage_backfill.sql && node -e "const j=require('./src/db/migrations/meta/_journal.json'); const e=j.entries.find(x=>x.tag==='0013_v4_chainage_backfill'); if(!e) throw new Error('not registered'); if(e.when<=1780088633500) throw new Error('when must be > 0012 epoch'); console.log('journal ok', e.when)"</automated>
  </verify>
  <done>0013 SQL exists with the additive UPDATE; journal has idx=13 entry with when > 1780088633500; 0010-0012 untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram webhook → Neon DB | Auditor callback (already authz-checked upstream in handleAuditDecision) writes chainage inside the approval tx |
| Migration runner → Neon DB | 0013 mutates existing approved rows (additive, NULL→value only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02-WINDOW | Tampering | chainage write outside approval tx | mitigate | Second UPDATE is inside the SAME txDb.transaction as the status flip (Pitfall 1) — no approved-but-NULL window |
| T-15-02-FLOAT | Tampering | chainage numeric arithmetic | mitigate | ROUND(...,2) executed in Postgres via sql2 template; zero JS float multiplication of numeric strings |
| T-15-02-BOTAUTH | Elevation of Privilege | bot path calling auth()/logOfficeActivity | mitigate | Pitfall 5 — grep gate confirms 0 occurrences of logOfficeActivity/after()/@/lib/auth in bot-audit.ts |
| T-15-02-MIGSKIP | Tampering | journal when < 0012 epoch | mitigate | journal entry validated when > 1780088633500 so Drizzle does not silently skip 0013 (Phase 14 lesson) |
| T-15-SC | Tampering | npm installs | accept | Zero new packages this phase |
</threat_model>

<verification>
- `grep -cE "logOfficeActivity|after\(|@/lib/auth" src/lib/bot-audit.ts` = 0 (Pitfall 5).
- `npx tsc --noEmit` passes.
- `npx vitest run -t "audit"` — existing approval-path tests still green after the snapshot write addition.
- Migration 0013 registered with a 2026 `when` strictly after 0012.
</verification>

<success_criteria>
chainage_m + route_geometry_version are written immutably at approval; the worker Telegram message carries the calibrated chainage; backfill migration is authored and journal-registered but not yet applied. Pitfall 1, 5, 7, 13 all honored.
</success_criteria>

<output>
Create `.planning/phases/15-chainage-as-built-view-approval-snapshot/15-02-SUMMARY.md` when done.
</output>
