---
phase: 15-chainage-as-built-view-approval-snapshot
plan: 06
type: execute
wave: 5
depends_on: ["15-05"]
files_modified:
  - src/lib/chainage-excel.ts
  - src/lib/pdf/chainage-pdf.tsx
  - src/app/api/exports/chainage/route.ts
  - src/db/schema/office-activity-log.ts
  - tests/chainage.test.ts
autonomous: true
requirements: [CHN-01, CHN-07]
must_haves:
  truths:
    - "GET /api/exports/chainage with no session returns HTTP 401 JSON (auth() first, not redirect) — same gate as all Phase 11 export handlers (CHN-07 SC)"
    - "Excel output has exactly 8 columns in order: Km Başlangıç, Km Bitiş, İş Adedi, Malzeme, Miktar, Birim, İşçi, Denetçi"
    - "Chainage values in Excel + PDF use formatChainage (calibrated km X+YYY) — same value as dashboard + Telegram (Pitfall 13)"
    - "PDF uses @react-pdf/renderer + DejaVu Sans (Phase 11 hakkediş aesthetic); user-content cells wrapped in sanitizeExcelCell (formula-injection)"
    - "Route Handler calls fetchChainageBucketsRaw (shared helper), NOT the use-server action (Pitfall 6)"
  artifacts:
    - path: "src/app/api/exports/chainage/route.ts"
      provides: "auth-guarded binary export route (xlsx + pdf)"
      contains: "fetchChainageBucketsRaw"
    - path: "src/lib/chainage-excel.ts"
      provides: "buildChainageLedger 8-column workbook"
      exports: ["buildChainageLedger"]
    - path: "src/lib/pdf/chainage-pdf.tsx"
      provides: "renderChainagePdf (DejaVu, hakkediş aesthetic)"
      exports: ["renderChainagePdf"]
  key_links:
    - from: "src/app/api/exports/chainage/route.ts"
      to: "src/lib/chainage-data.ts"
      via: "import fetchChainageBucketsRaw (not the Server Action)"
      pattern: "fetchChainageBucketsRaw"
---

<objective>
The chainage export (CHN-07): an auth-guarded `GET /api/exports/chainage` Route Handler producing an 8-column Excel and a hakkediş-aesthetic PDF, reusing the Phase 11 export skeleton verbatim. Calls the shared `fetchChainageBucketsRaw` helper (Pitfall 6).

Purpose: CHN-07 — exportable as-built breakdown, same auth gate (401 on no session) as every other export handler. Calibrated chainages in the export must match the dashboard strip and Telegram line (Pitfall 13).
Output: `chainage-excel.ts`, `chainage-pdf.tsx`, the export `route.ts`, plus `chainage_exported` action type.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-chainage-as-built-view-approval-snapshot/15-RESEARCH.md
@.planning/phases/15-chainage-as-built-view-approval-snapshot/15-PATTERNS.md
@.planning/phases/15-chainage-as-built-view-approval-snapshot/15-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: buildChainageLedger (8-column Excel) + chainage_exported action type</name>
  <files>src/lib/chainage-excel.ts, src/db/schema/office-activity-log.ts, tests/chainage.test.ts</files>
  <read_first>
    - `src/lib/excel.ts` lines 133-208 (sanitizeExcelCell CVE-2014-3524 mitigation; buildSubmissionLedger column setup; numeric strings direct to cells, no parseFloat; ExcelJS XLSX does NOT persist column keys — read-back tests use 1-based numeric index)
    - `.planning/phases/15-chainage-as-built-view-approval-snapshot/15-UI-SPEC.md` §Copywriting (column header source) + CONTEXT.md fixed columns
    - `src/db/schema/office-activity-log.ts` lines 8-33 (OFFICE_ACTION_TYPES as-const tuple + OfficeActionType union)
    - `src/lib/format-chainage.ts` (from plan 01)
    - `src/lib/chainage-data.ts` ChainageBucket type (from plan 05)
  </read_first>
  <action>Create `src/lib/chainage-excel.ts` exporting `async buildChainageLedger(data: { buckets: ChainageBucket[]; ... }): Promise<Buffer>` building a single-sheet ExcelJS workbook with exactly 8 columns in this order: "Km Başlangıç", "Km Bitiş", "İş Adedi", "Malzeme", "Miktar", "Birim", "İşçi", "Denetçi". One row per bucket: Km Başlangıç = formatChainage(bucket.bucketStart), Km Bitiş = formatChainage(bucket.bucketEnd) (calibrated values — Pitfall 13), İş Adedi = approvedCount, Malzeme/Miktar/Birim from boqBreakdown (one combined row per bucket — join material/qty/unit consistently with the strip-table representation), İşçi = workers joined, Denetçi = auditors joined. Wrap every user-content string cell (material, unit, worker names, auditor names) in `sanitizeExcelCell` (formula-injection, T-15-06-FORMULA). Numeric quantities flow as strings direct to cells (no parseFloat). Add `'chainage_exported'` to the `OFFICE_ACTION_TYPES` as-const tuple in office-activity-log.ts (keeps OfficeActionType union valid for plan 06 route + plan 05 setChainageOffset if it reused it). Fill the "chainage excel columns" test (parse the workbook back, assert 8 columns in order by 1-based index — keys are not persisted).</action>
  <verify>
    <automated>npx vitest run tests/chainage.test.ts -t "chainage excel columns" && grep -c "chainage_exported" src/db/schema/office-activity-log.ts && grep -c "sanitizeExcelCell" src/lib/chainage-excel.ts</automated>
  </verify>
  <done>buildChainageLedger produces an 8-column-in-order workbook; sanitizeExcelCell on all user-content cells; chainage_exported in the action-type union; "chainage excel columns" test green.</done>
</task>

<task type="auto">
  <name>Task 2: renderChainagePdf (DejaVu, hakkediş aesthetic)</name>
  <files>src/lib/pdf/chainage-pdf.tsx</files>
  <read_first>
    - `src/lib/pdf/hakedis-pdf.tsx` (READ IN FULL — copy byte-for-byte structure: NO 'use client', imports from @react-pdf/renderer, StyleSheet, Document>Page>View, renderToBuffer wrapper, table header + body rows)
    - `src/lib/pdf/fonts.ts` line 27 (`registerFonts()` — DejaVu Sans registration; call it before render)
    - `src/app/api/exports/hakedis/[periodId]/pdf/route.ts` (where registerFonts() is invoked in the Phase 11 flow)
    - `.planning/phases/15-chainage-as-built-view-approval-snapshot/15-UI-SPEC.md` §Phase-Specific Note #10 (PDF aesthetic parity — same header/cell padding/footer as hakkediş certificate)
    - `src/lib/format-chainage.ts` + `src/lib/chainage-data.ts` ChainageBucket
  </read_first>
  <action>Create `src/lib/pdf/chainage-pdf.tsx` (NO 'use client') mirroring `hakedis-pdf.tsx`: a `ChainagePdf` component (Document title + author bayrak.ai + creationDate; A4 Page with fontFamily 'DejaVuSans'; header View with title "As-Built / Chainage" + project + generatedAt meta; a table header row + one body row per bucket showing the same 8 columns as the Excel) and an exported `async renderChainagePdf(data): Promise<Buffer>` calling `renderToBuffer(<ChainagePdf data={data} />)`. Ensure `registerFonts()` runs before render (call it in the route handler in Task 3, matching the Phase 11 placement). Chainage cells use formatChainage(calibrated). Same StyleSheet padding/border tokens as hakedis-pdf for aesthetic parity (SC-6).</action>
  <verify>
    <automated>grep -c "renderChainagePdf\|DejaVu" src/lib/pdf/chainage-pdf.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>renderChainagePdf exported; uses DejaVu Sans + @react-pdf/renderer; structurally mirrors hakedis-pdf; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 3: GET /api/exports/chainage route handler (auth-first, binary, Pitfall 6)</name>
  <files>src/app/api/exports/chainage/route.ts, tests/chainage.test.ts</files>
  <read_first>
    - `src/app/api/exports/submissions/route.ts` lines 38-139 (COPY VERBATIM: runtime='nodejs', dynamic='force-dynamic', auth() first → 401 JSON, searchParams parse, NextResponse Uint8Array body + Content-Disposition + Content-Length, logOfficeActivity fire-and-forget never awaited)
    - `.planning/phases/15-chainage-as-built-view-approval-snapshot/15-RESEARCH.md` §Pattern 6 + §Pitfall 6 (call fetchChainageBucketsRaw, NOT the Server Action), §Security Domain (bucketSizeM whitelist; 401 not redirect)
    - `.planning/phases/15-chainage-as-built-view-approval-snapshot/15-PATTERNS.md` §src/app/api/exports/chainage/route.ts (full skeleton) + Binary Response + runtime/dynamic shared patterns
    - `src/lib/pdf/fonts.ts` registerFonts() placement
  </read_first>
  <action>Create `src/app/api/exports/chainage/route.ts` mirroring submissions/route.ts: `export const runtime = 'nodejs'; export const dynamic = 'force-dynamic';`. `GET`: `auth()` first → `NextResponse.json({error:'Unauthorized'}, {status:401})` on null (NOT redirect — binary endpoint). Parse `projectId`, `format` (xlsx|pdf), `bucketSizeM` (Number, whitelist-coerce to {100,500,1000}, default 1000). Call `fetchChainageBucketsRaw(projectId, bucketSizeM, getDefaultTenantId())` — the shared helper, NOT getChainageBuckets the Server Action (Pitfall 6). If format==='pdf': `registerFonts()` then `renderChainagePdf(data)`, contentType application/pdf, filename `chainage-asbuilt-${projectId}.pdf`; else `buildChainageLedger(data)`, the spreadsheetml contentType, `.xlsx` filename. Return `new NextResponse(new Uint8Array(buffer), { status:200, headers:{ 'Content-Type', 'Content-Disposition': attachment, 'Content-Length' } })`. Fire-and-forget (never await) `logOfficeActivity({ actorUserId: session.user!.id!, actionType:'chainage_exported', entityType:'chainage_export', projectId, metadata:{ format, bucketSizeM } })`. Add a route-shape inspection assertion to the "chainage excel columns" area or a separate static test confirming auth() is the first statement and 401 is returned (file-byte/static check, since live route is manual-only per VALIDATION).</action>
  <verify>
    <automated>grep -nE "await auth\(\)|fetchChainageBucketsRaw|401|runtime = 'nodejs'|force-dynamic" src/app/api/exports/chainage/route.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Route handler: auth() first → 401 JSON, runtime/dynamic set, calls shared helper (not Server Action), binary response for xlsx+pdf, chainage_exported logged fire-and-forget; tsc passes; `npx next build` succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/exports/chainage | unauthenticated request possible; projectId + bucketSizeM + format are untrusted |
| Route Handler → Neon | reads via shared helper |
| user-content → Excel cell | material/worker/auditor names are user-supplied |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-06-AUTH | Elevation of Privilege | export without session | mitigate | `auth()` first statement → 401 JSON (not redirect); does not inherit layout auth (CHN-07 SC) |
| T-15-06-FORMULA | Tampering | Excel formula injection via names | mitigate | `sanitizeExcelCell()` on all user-content string cells (CVE-2014-3524) |
| T-15-06-IDOR | Information Disclosure | projectId cross-tenant export | mitigate | fetchChainageBucketsRaw tenant-scopes via getDefaultTenantId(); same scope as the Server Action |
| T-15-06-SQLI | Tampering | bucketSizeM into FLOOR | mitigate | Whitelist-coerce to {100,500,1000} before passing to the helper; bound param downstream |
| T-15-06-SA | Tampering | Route Handler calling Server Action | mitigate | Calls fetchChainageBucketsRaw (plain async helper), never the 'use server' action (Pitfall 6) |
| T-15-SC | Tampering | npm installs | accept | Zero new packages this phase |
</threat_model>

<verification>
- `npx vitest run tests/chainage.test.ts -t "chainage excel columns"` green (8 cols in order).
- Route file: auth() first, 401 JSON, runtime nodejs, force-dynamic, fetchChainageBucketsRaw (grep gate).
- `npx tsc --noEmit` + `npx next build` pass.
- Manual (VALIDATION): trigger export signed-in → 8-col xlsx + PDF download; signed-out → 401.
</verification>

<success_criteria>
The chainage export ships an 8-column Excel + hakkediş-aesthetic PDF behind the same 401 auth gate as all exports; calibrated chainages match dashboard + Telegram; Route Handler uses the shared helper (no Server-Action-from-handler). CHN-07 complete.
</success_criteria>

<output>
Create `.planning/phases/15-chainage-as-built-view-approval-snapshot/15-06-SUMMARY.md` when done.
</output>
