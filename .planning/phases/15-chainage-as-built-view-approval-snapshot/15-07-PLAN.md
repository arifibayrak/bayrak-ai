---
phase: 15-chainage-as-built-view-approval-snapshot
plan: 07
type: execute
wave: 5
depends_on: ["15-05"]
files_modified:
  - src/components/dashboard/ChainageTab.tsx
  - src/components/dashboard/ChainageTable.tsx
  - src/components/dashboard/ChainageOffsetForm.tsx
  - src/app/dashboard/projects/[id]/page.tsx
  - messages/tr.json
  - messages/en.json
autonomous: false
requirements: [CHN-02, CHN-04, CHN-05, CHN-06]
must_haves:
  truths:
    - "An As-Built tab on the project page renders a colour-coded chainage bar (CSS flex, no charting lib), a per-bucket BrandTable, a completion-% KpiCard, and a granularity toggle (1km/500m/100m default 1km) — D-01, D-02, D-03, D-04"
    - "Each table row drills to /dashboard/records/[firstSubmissionId]?from=asbuilt (CHN-05); not-started buckets render with em-dash cells and no detail link"
    - "Calibration offset form writes via setChainageOffset then router.refresh() — strip recomputes with calibrated chainage (CHN-02, Pitfall 13)"
    - "All as-built copy is bilingual TR/EN under dashboard.asbuilt.* + dashboard.projects.tab_asbuilt"
  artifacts:
    - path: "src/components/dashboard/ChainageTab.tsx"
      provides: "RSC data shell (Promise.all getChainageBuckets + getRoute) + KpiCard row; force-dynamic"
      contains: "force-dynamic"
    - path: "src/components/dashboard/ChainageTable.tsx"
      provides: "colour bar + granularity toggle + BrandTable + export buttons (client)"
      contains: "bg-emerald-400"
    - path: "src/components/dashboard/ChainageOffsetForm.tsx"
      provides: "calibration offset client form"
      contains: "setChainageOffset"
  key_links:
    - from: "src/app/dashboard/projects/[id]/page.tsx"
      to: "src/components/dashboard/ChainageTab.tsx"
      via: "TabsTrigger/TabsContent value=asbuilt"
      pattern: "asbuilt"
    - from: "src/components/dashboard/ChainageTable.tsx"
      to: "/dashboard/records/[id]?from=asbuilt"
      via: "Detay link per row"
      pattern: "from=asbuilt"
---

<objective>
The As-Built tab UI (CHN-04/05/06/02 surfaces): RSC data shell, client colour bar + granularity toggle + per-bucket table + export buttons, calibration offset form, and the additive project-page tab wiring — all on brand primitives per 15-UI-SPEC.

Purpose: the headline surface — the office sees route completion %, a per-km colour bar, a per-bucket attribution table with drill-down, and can calibrate chainage. End-of-phase UAT confirms the strip renders bilingual, the drill-down/back-link round-trips, and calibration is consistent across all three surfaces.
Output: ChainageTab/Table/OffsetForm, tab wire in page.tsx, dashboard.asbuilt.* i18n.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-chainage-as-built-view-approval-snapshot/15-UI-SPEC.md
@.planning/phases/15-chainage-as-built-view-approval-snapshot/15-PATTERNS.md
@.planning/phases/15-chainage-as-built-view-approval-snapshot/15-RESEARCH.md

<interfaces>
<!-- from src/actions/chainage.ts (plan 05) -->
getChainageBuckets(projectId, bucketSizeM=1000) → { buckets: ChainageBucket[]; totalLengthM; chainageOffsetM; completionPct }
setChainageOffset(projectId, offsetM) → void
<!-- KpiCard (src/components/admin/KpiCard.tsx) — UNCHANGED, use as-is -->
interface KpiCardProps { label; subLabel; value: number|string; icon: ReactNode; drillHref?; valueColor?: 'default'|'success'|'destructive'|'warning'; alertBadge?: ReactNode }
<!-- brand primitives: BrandTable, BrandCard, BrandBadge, BrandButton, BrandHeading, BrandEmpty from @/components/brand -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ChainageTab (RSC) + ChainageOffsetForm (client) + i18n</name>
  <files>src/components/dashboard/ChainageTab.tsx, src/components/dashboard/ChainageOffsetForm.tsx, messages/tr.json, messages/en.json</files>
  <read_first>
    - `src/components/dashboard/RouteTab.tsx` lines 30-83 (RSC shell: getTranslations, Promise.all, Date→ISO serialization, pass to client)
    - `src/components/admin/KpiCard.tsx` lines 22-86 (KpiCardProps + usage)
    - `.planning/phases/15-chainage-as-built-view-approval-snapshot/15-UI-SPEC.md` §Surface Spec §1 (layout top→bottom), §5 (KPI card props), §ChainageOffsetForm spec (placement, w-32 input, "Ofset Kaydet" button), Copywriting Contract (ALL dashboard.asbuilt.* keys)
    - `.planning/phases/15-chainage-as-built-view-approval-snapshot/15-PATTERNS.md` §ChainageTab.tsx + §ChainageOffsetForm.tsx (useTransition + setChainageOffset + router.refresh)
  </read_first>
  <action>Create `src/components/dashboard/ChainageTab.tsx` as an RSC: `export const dynamic = 'force-dynamic'`; `getTranslations('dashboard.asbuilt')`; `Promise.all([getChainageBuckets(projectId, 1000), getRoute(projectId)])`; render a `grid grid-cols-1 sm:grid-cols-3 gap-3` KPI row with three KpiCards — Route Completion % (value `${completionPct}%`, valueColor success when ≥80 / warning 1-79 / default 0, Route icon), Onaylı Güzergah (covered km), Toplam Güzergah (totalLengthM/1000 "X.X km"); render `ChainageOffsetForm` (BrandCard below KPI row) and `ChainageTable` (pass buckets/totalLengthM/chainageOffsetM/projectId). When buckets empty render `BrandEmpty` (MapPin icon, empty_heading/empty_body). Serialize any Date from getRoute to ISO. Create `src/components/dashboard/ChainageOffsetForm.tsx` (`'use client'`): w-32 number input (step 0.01) initialised from currentOffsetM, "Ofset Kaydet" BrandButton variant=default size=sm (key dashboard.asbuilt.calibration_save), useTransition → setChainageOffset(projectId, parseFloat) → router.refresh() on success + toast 'common.saved'; inline error text on failure. Add ALL dashboard.asbuilt.* keys (kpi_*, calibration_*, granularity_label, col_*, detail_link, status_*, export_excel, export_pdf, empty_*, error_body) AND dashboard.projects.tab_asbuilt to BOTH tr.json and en.json per the Copywriting Contract.</action>
  <verify>
    <automated>grep -c "force-dynamic" src/components/dashboard/ChainageTab.tsx && grep -c "setChainageOffset" src/components/dashboard/ChainageOffsetForm.tsx && node -e "const tr=require('./messages/tr.json'),en=require('./messages/en.json'); const g=(o,p)=>p.split('.').reduce((a,k)=>a&&a[k],o); for(const k of ['dashboard.asbuilt.calibration_save','dashboard.asbuilt.kpi_completion_label','dashboard.asbuilt.status_approved','dashboard.asbuilt.export_excel','dashboard.projects.tab_asbuilt']){ if(!g(tr,k))throw new Error('tr missing '+k); if(!g(en,k))throw new Error('en missing '+k);} console.log('i18n ok')"</automated>
  </verify>
  <done>ChainageTab RSC (force-dynamic) renders KPI row + offset form + table; ChainageOffsetForm calls setChainageOffset + router.refresh; all asbuilt i18n keys present in tr+en; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 2: ChainageTable (colour bar + granularity toggle + table + export buttons)</name>
  <files>src/components/dashboard/ChainageTable.tsx</files>
  <read_first>
    - `.planning/phases/15-chainage-as-built-view-approval-snapshot/15-UI-SPEC.md` §Surface Spec §2 (colour bar markup), §3 (table columns + row anatomy + not-started em-dash), §4 (granularity toggle three BrandButtons), §6 (export buttons window.open), §Interaction Contracts, §Phase-Specific Notes #1/#3/#4/#7
    - `.planning/phases/15-chainage-as-built-view-approval-snapshot/15-PATTERNS.md` §ChainageTable.tsx (colour bar verbatim; BrandBadge variant mapping; export button window.open pattern; useTransition re-query)
    - `src/lib/format-chainage.ts` (from plan 01)
    - an existing client tab component (e.g. RouteTabClient.tsx) for the 'use client' + useTransition + BrandTable conventions
  </read_first>
  <action>Create `src/components/dashboard/ChainageTable.tsx` (`'use client'`). State `bucketSizeM: 100|500|1000` default 1000. Granularity toggle: three BrandButtons in `inline-flex gap-0` (left rounded-l-md, middle rounded-none, right rounded-r-md), active=variant default (amber) / inactive=variant outline, labels "1 km"/"500 m"/"100 m"; on change call `getChainageBuckets(projectId, newSize)` inside useTransition and update local buckets (LoadingSpinner overlay on the table only). Colour bar: `div.w-full.h-2.rounded-full.overflow-hidden.border.border-slate-200.my-3` role=img aria-label "Güzergah tamamlanma durumu", one child div per bucket width `${(1/totalBuckets)*100}%`, class bg-emerald-400 (approved) / bg-amber-300 (in_progress) / bg-slate-200 (not_started); NOT clickable; NO charting library (Note #1). BrandTable columns in order: Km Aralığı (formatChainage(start) – formatChainage(end), font-mono), Durum (BrandBadge variant success/primary/neutral → status_approved/in_progress/not_started, Note #3), İş Adedi (tabular-nums right), Malzeme/Miktar (boqBreakdown joined "PE Boru: 45 m, ..." or text-muted "—"), İşçi (workers joined), Denetçi (auditors joined), Detay (text-xs text-primary underline link to `/dashboard/records/${firstSubmissionId}?from=asbuilt`, empty for not-started). Export row: two BrandButton outline sm at table header right — Excel İndir (FileSpreadsheet) window.open(`/api/exports/chainage?projectId=${projectId}&format=xlsx&bucketSizeM=${bucketSizeM}`,'_blank') and PDF İndir (FileText) format=pdf.</action>
  <verify>
    <automated>grep -c "bg-emerald-400" src/components/dashboard/ChainageTable.tsx && grep -c "from=asbuilt" src/components/dashboard/ChainageTable.tsx && grep -ciE "recharts|d3|visx" src/components/dashboard/ChainageTable.tsx</automated>
  </verify>
  <done>ChainageTable: colour bar (3 status classes), granularity toggle re-queries via useTransition, table with Detay drill-down (from=asbuilt), export buttons; zero charting-lib imports (grep returns 0); tsc passes.</done>
</task>

<task type="auto">
  <name>Task 3: Wire As-Built tab into the project page (additive)</name>
  <files>src/app/dashboard/projects/[id]/page.tsx</files>
  <read_first>
    - `src/app/dashboard/projects/[id]/page.tsx` lines 16 (force-dynamic), 27-31 (getTranslations), 44-47 (activeTab ternary), 72-92 (TabsTrigger pattern), 95-130 (TabsContent pattern)
    - `.planning/phases/15-chainage-as-built-view-approval-snapshot/15-PATTERNS.md` §page.tsx MODIFY (exact activeTab extension, TabsTrigger, TabsContent insertion points)
    - `.planning/phases/15-chainage-as-built-view-approval-snapshot/15-UI-SPEC.md` §Phase-Specific Note #6 (do not restructure the tab bar)
  </read_first>
  <action>Extend the existing `activeTab` ternary with `tab === 'asbuilt' ? 'asbuilt' :` before the `'boq'` fallback. Add a `TabsTrigger value="asbuilt"` wrapping a `Link href={\`/dashboard/projects/${id}?tab=asbuilt\`}` with label `t('tab_asbuilt')` (dashboard.projects.tab_asbuilt) following the existing trigger pattern. Add a `TabsContent value="asbuilt"` rendering `<ChainageTab projectId={id} />` inside a `BrandCard`/`BrandCard.Body` following the existing tab content pattern. Purely additive — do not move or rename the existing four tabs (Note #6).</action>
  <verify>
    <automated>grep -c "asbuilt" src/app/dashboard/projects/[id]/page.tsx && grep -c "ChainageTab" src/app/dashboard/projects/[id]/page.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>asbuilt tab trigger + content present, ChainageTab mounted, existing tabs untouched; tsc passes; `npx next build` succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: End-of-phase UAT — As-Built strip, drill-down, calibration consistency</name>
  <what-built>The full As-Built tab: completion KPI, colour bar, per-bucket table, granularity toggle, calibration form, drill-down, and (from plan 06) export buttons.</what-built>
  <how-to-verify>
    1. Open a project with approved submissions → As-Built tab. Confirm: completion % KpiCard, colour bar (emerald/amber/slate segments full width incl. not-started grey), per-bucket table with "km X+YYY – km X+YYY", status badges, work count, materials, worker + auditor names.
    2. Toggle granularity 1km → 500m → 100m: bar + table re-segment without page reload.
    3. Click a row "Detay →": lands on the canonical submission detail; the "← As-Built'e dön" back-link returns to the strip (CHN-05).
    4. Set a calibration offset (e.g. 50) → Ofset Kaydet → "Kaydedildi"; confirm the strip km values shift by the offset. Then export Excel (plan 06) and approve a new submission via Telegram (plan 02): confirm the SAME calibrated km appears on the strip, in the Excel Km Başlangıç column, and in the Telegram approval line (Pitfall 13 — three-surface consistency).
    5. Switch dashboard locale TR ↔ EN: all as-built labels translate.
    6. Sign out, hit `/api/exports/chainage?...` → 401.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (per surface).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Server Actions (getChainageBuckets, setChainageOffset) | tab re-query + offset write (auth+tenant in plan 05) |
| client → /api/exports/chainage | export trigger via window.open (auth in plan 06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-07-XSS | Tampering | bucket material/worker names in table | mitigate | React escapes text children by default; names rendered as text, not dangerouslySetInnerHTML |
| T-15-07-CONSIST | Tampering | offset shown differently per surface | mitigate | All surfaces read calibrated value via formatChainage(chainage_m + offset) from the same Postgres-applied offset (Pitfall 13); UAT step 4 verifies |
| T-15-07-AUTHZ | Elevation of Privilege | tab data without session | mitigate | ChainageTab calls auth()-guarded Server Actions (plan 05); page is under the authenticated dashboard layout |
| T-15-SC | Tampering | npm installs | accept | Zero new packages this phase |
</threat_model>

<verification>
- `grep -ciE "recharts|d3|visx" src/components/dashboard/ChainageTable.tsx` = 0 (no charting lib, Note #1).
- asbuilt + tab_asbuilt i18n keys resolve in tr + en.
- `npx tsc --noEmit` + `npx next build` pass.
- UAT Task 4 approved (manual rows in 15-VALIDATION.md: strip render/drill-down/back-link, calibration consistency, locale).
</verification>

<success_criteria>
The As-Built tab renders the completion KPI, colour bar, per-bucket table, granularity toggle, and calibration form on brand primitives; drill-down + back-link round-trip; calibration is consistent across strip, Excel, and Telegram; bilingual. CHN-02/04/05/06 surfaced and UAT-approved.
</success_criteria>

<output>
Create `.planning/phases/15-chainage-as-built-view-approval-snapshot/15-07-SUMMARY.md` when done.
</output>
