---
phase: 15-chainage-as-built-view-approval-snapshot
plan: 05
type: execute
wave: 4
depends_on: ["15-01", "15-04"]
files_modified:
  - src/lib/chainage-data.ts
  - src/actions/chainage.ts
  - tests/chainage.test.ts
autonomous: true
requirements: [CHN-01, CHN-02, CHN-04, CHN-06]
must_haves:
  truths:
    - "getChainageBuckets enumerates ALL buckets via generate_series (not-started buckets present) — colour bar gets correct proportions (Pitfall 8)"
    - "Three-state bucket status: ≥1 approved→approved; 0 approved+≥1 pending→in_progress; none→not_started (D-04)"
    - "Approved rows read immutable chainage_m; pending rows derive COALESCE(chainage_m, segment_fraction×total_length_m); offset applied INSIDE Postgres FLOOR (Pitfall 2, 4)"
    - "Completion % = covered buckets ÷ total buckets ×100, clamped LEAST(...,100) (D-02, CHN-06)"
    - "setChainageOffset writes routes.chainage_offset_m (auth + tenant-scoped, CR-02); bucketSizeM whitelist-validated to {100,500,1000}"
    - "fetchChainageBucketsRaw(projectId, bucketSizeM, tenantId) is a non-server-action helper the export route imports (Pitfall 6)"
  artifacts:
    - path: "src/lib/chainage-data.ts"
      provides: "fetchChainageBucketsRaw shared helper + ChainageBucket type"
      exports: ["fetchChainageBucketsRaw", "ChainageBucket"]
    - path: "src/actions/chainage.ts"
      provides: "getChainageBuckets + setChainageOffset Server Actions"
      exports: ["getChainageBuckets", "setChainageOffset"]
  key_links:
    - from: "src/actions/chainage.ts"
      to: "src/lib/chainage-data.ts"
      via: "getChainageBuckets wraps fetchChainageBucketsRaw with auth+tenant"
      pattern: "fetchChainageBucketsRaw"
    - from: "src/lib/chainage-data.ts"
      to: "submissions + routes (Postgres GROUP BY)"
      via: "db.execute(sql) generate_series + FLOOR + JSON_AGG"
      pattern: "generate_series"
---

<objective>
The chainage aggregation backend: a shared `fetchChainageBucketsRaw` helper (callable from both the Server Action and the export Route Handler per Pitfall 6), the `getChainageBuckets` + `setChainageOffset` Server Actions, and the completion-% computation. This is the data spine the UI and export plans consume.

Purpose: CHN-04 (per-bucket attribution + three-state status), CHN-06 (completion clamp), CHN-02 (calibration offset write + consistent application), CHN-01 (chainage derivation). The query is the hardest piece — generate_series for empty buckets, COALESCE for pending, offset inside FLOOR.
Output: `src/lib/chainage-data.ts` + `src/actions/chainage.ts`; the named test scaffolds (getChainageBuckets / bucket status / completion / completion clamp / bucket boundary / chainage offset) turn green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-chainage-as-built-view-approval-snapshot/15-RESEARCH.md
@.planning/phases/15-chainage-as-built-view-approval-snapshot/15-PATTERNS.md

<interfaces>
<!-- ChainageBucket shape (15-RESEARCH §Pattern 3) — export from chainage-data.ts -->
type ChainageBucket = {
  bucketIndex: number;
  bucketStart: number;   // calibrated metres
  bucketEnd: number;     // calibrated metres (cap last at totalLengthM — Pitfall 3)
  status: 'approved' | 'in_progress' | 'not_started';
  approvedCount: number;
  pendingCount: number;
  boqBreakdown: Array<{ material: string; unit: string; quantity: string }>;
  workers: string[];
  auditors: string[];
  firstSubmissionId: string | null;
};
// getChainageBuckets returns { buckets: ChainageBucket[]; totalLengthM: number; chainageOffsetM: number; completionPct: number }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: fetchChainageBucketsRaw shared helper (generate_series + three-state + COALESCE)</name>
  <files>src/lib/chainage-data.ts, tests/chainage.test.ts</files>
  <read_first>
    - `.planning/phases/15-chainage-as-built-view-approval-snapshot/15-RESEARCH.md` §Pattern 3 (FULL query: all_buckets CTE via generate_series, sub_agg with COUNT FILTER, JSON_AGG FILTER for boq, ARRAY_AGG DISTINCT for workers/auditors, MIN(id) FILTER for first_submission_id, COALESCE(chainage_m, segment_fraction×total_length_m) for pending, offset inside FLOOR), §Pitfall 2/3/4/8, §Completion % computation
    - `.planning/phases/15-chainage-as-built-view-approval-snapshot/15-PATTERNS.md` §src/actions/chainage.ts (db.execute(sql) with bound params; result.rows.map; shared-helper note — takes tenantId as explicit param, no getDefaultTenantId/auth inside)
    - `src/actions/analytics.ts` lines 227-298 (db.execute(sql`...`) bound-param pattern + row mapping convention: numeric→String, nullable→null)
  </read_first>
  <behavior>
    - 3000m route, bucketSizeM=1000 → exactly 3 buckets (indices 0,1,2); not-started buckets present.
    - 3 approved submissions at fractions 0.166/0.5/0.833 → buckets 0 (chainage 498), 1 (1500), 2 (2499) each approved.
    - Bucket with 1 approved + 1 pending → status 'approved'; 0 approved + 1 pending → 'in_progress'; none → 'not_started' (D-04).
    - Bucket boundary: chainage exactly 1000.0 m → bucket index 1, not 0 (FLOOR; Pitfall 2).
    - completionPct: 2 approved both in km 0-1 on a 1000m route → 100 (not 200), via LEAST(...,100).
  </behavior>
  <action>Create `src/lib/chainage-data.ts` exporting the `ChainageBucket` type and an async `fetchChainageBucketsRaw(projectId: string, bucketSizeM: number, tenantId: string)` that takes tenantId as an explicit param (NO auth(), NO getDefaultTenantId() inside — this is the Pitfall-6 shared helper the Route Handler imports). Build the query from 15-RESEARCH §Pattern 3 verbatim: an `all_buckets` CTE using `generate_series(0, CEIL(r.total_length_m / ${bucketSizeM})::int - 1)` scoped to the project+tenant route; a `sub_agg` CTE that buckets via `FLOOR((COALESCE(s.chainage_m, s.segment_fraction * r.total_length_m) + r.chainage_offset_m) / ${bucketSizeM})::int` with WHERE `s.status IN ('approved','pending_audit') AND (s.chainage_m IS NOT NULL OR (s.status='pending_audit' AND s.segment_fraction IS NOT NULL))`, using COUNT FILTER for approved/pending counts, JSON_AGG FILTER (approved only) for boq breakdown (material/unit/quantity), ARRAY_AGG DISTINCT FILTER for worker and auditor display_names, MIN(s.id) FILTER for first_submission_id (approved first, fall back to pending); LEFT JOIN all_buckets to sub_agg ORDER BY bucket_idx. Tenant scope (`r.tenant_id = ${tenantId}`, `s.tenant_id = ${tenantId}`) on every table. Offset applied INSIDE the Postgres FLOOR (Pitfall 4 — never in JS). Map rows: derive `status` three-state from counts (D-04), cap last bucketEnd at totalLengthM (Pitfall 3), quantities/numeric as String. Compute `completionPct = Math.min(100, Math.round((coveredBuckets / totalBuckets) * 100))` where coveredBuckets = rows with approvedCount>0 and totalBuckets = Math.ceil(totalLengthM/bucketSizeM). Return `{ buckets, totalLengthM, chainageOffsetM, completionPct }`. Return empty buckets + completionPct 0 when no route exists (Pitfall A6). Fill the "getChainageBuckets", "bucket status", "completion", "completion clamp", "bucket boundary" test scaffolds (describeIfDb, using seedChainageFixture from plan 01).</action>
  <verify>
    <automated>npx vitest run tests/chainage.test.ts -t "getChainageBuckets" -t "completion" -t "bucket"</automated>
  </verify>
  <done>fetchChainageBucketsRaw exists; all-bucket enumeration, three-state status, COALESCE pending derivation, offset-in-FLOOR, and LEAST/clamp completion verified; the named DB tests are green (or skipped cleanly when no TEST_DATABASE_URL but pass when present).</done>
</task>

<task type="auto">
  <name>Task 2: getChainageBuckets + setChainageOffset Server Actions</name>
  <files>src/actions/chainage.ts, tests/chainage.test.ts</files>
  <read_first>
    - `.planning/phases/15-chainage-as-built-view-approval-snapshot/15-RESEARCH.md` §Pattern 4 (setChainageOffset auth + ownership + chainageOffsetM stored as String + revalidatePath), §Security Domain (V5 bucketSizeM whitelist {100,500,1000}; projectId UUID validate; IDOR tenant scope)
    - `.planning/phases/15-chainage-as-built-view-approval-snapshot/15-PATTERNS.md` §src/actions/chainage.ts ('use server' header; auth-guard; setChainageOffset from routes.ts uploadRoute lines 46-99 ownership pattern; logOfficeActivity + revalidatePath lines 86-98)
    - `src/actions/routes.ts` lines 46-99 (uploadRoute: auth() first, CR-02 ownership check, logOfficeActivity, revalidatePath)
    - `src/db/schema/office-activity-log.ts` lines 8-31 (OFFICE_ACTION_TYPES — add `chainage_exported` is owned by plan 06's export; this plan's setChainageOffset uses an appropriate existing action type or a new one — coordinate so the union stays valid)
  </read_first>
  <action>Create `src/actions/chainage.ts` with `'use server'`. `getChainageBuckets(projectId, bucketSizeM = 1000)`: `auth()` first → throw 'Unauthorized' if null; validate `bucketSizeM` against the whitelist `[100,500,1000]` (default 1000 on invalid); validate `projectId` is a UUID; then `return fetchChainageBucketsRaw(projectId, bucketSizeM, getDefaultTenantId())`. `setChainageOffset(projectId, offsetM)`: `auth()` first; CR-02 ownership check (project belongs to active tenant — mirror uploadRoute); `db.update(routes).set({ chainageOffsetM: String(offsetM) }).where(and(eq(routes.projectId, projectId), eq(routes.tenantId, getDefaultTenantId())))`; store offset as STRING (numeric precision); do NOT recompute stored chainage_m (offset applied at query time only — Don't-Hand-Roll key insight); `logOfficeActivity` (a new action type for the offset set, added to OFFICE_ACTION_TYPES if needed — keep the union valid) and `revalidatePath(\`/dashboard/projects/${projectId}\`)`. Fill the "chainage offset" test scaffold (setChainageOffset writes routes.chainage_offset_m; getChainageBuckets bucketStart reflects the offset).</action>
  <verify>
    <automated>npx vitest run tests/chainage.test.ts -t "chainage offset" && grep -c "fetchChainageBucketsRaw" src/actions/chainage.ts && grep -cE "100.*500.*1000|\\[100, 500, 1000\\]" src/actions/chainage.ts</automated>
  </verify>
  <done>Both Server Actions exist; getChainageBuckets delegates to the shared helper with auth+tenant+whitelist; setChainageOffset writes the offset as string + revalidates; "chainage offset" test green; `npx tsc --noEmit` passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Server Action (getChainageBuckets, setChainageOffset) | projectId + bucketSizeM are untrusted client input |
| Server Action / Route Handler → Neon | aggregation + offset write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05-IDOR | Information Disclosure | projectId param crosses tenant | mitigate | `r.tenant_id = ${tenantId}` / `s.tenant_id = ${tenantId}` on every query; setChainageOffset CR-02 ownership check before write |
| T-15-05-SQLI | Tampering | bucketSizeM injected into FLOOR divisor | mitigate | Whitelist validate to {100,500,1000} in getChainageBuckets; Drizzle `${bucketSizeM}` is a bound param, not string concat |
| T-15-05-AUTHZ | Elevation of Privilege | unauthenticated aggregation | mitigate | `auth()` first statement in both Server Actions; throw Unauthorized on null |
| T-15-05-FLOAT | Tampering | offset/chainage math in JS | mitigate | Offset applied inside Postgres FLOOR; ROUND/arithmetic Postgres-side; completionPct integer-only |
| T-15-SC | Tampering | npm installs | accept | Zero new packages this phase |
</threat_model>

<verification>
- `npx vitest run tests/chainage.test.ts` — getChainageBuckets, bucket status, completion, completion clamp, bucket boundary, chainage offset all green (or skip cleanly without TEST_DATABASE_URL).
- `grep -c "fetchChainageBucketsRaw" src/actions/chainage.ts` ≥ 1 (Server Action delegates to shared helper).
- bucketSizeM whitelist present.
- `npx tsc --noEmit` passes.
</verification>

<success_criteria>
The aggregation backend computes all buckets (incl. not-started), three-state status, calibrated offset in Postgres, and clamped completion %. The shared raw helper is callable from the export Route Handler. CHN-01/02/04/06 backend complete.
</success_criteria>

<output>
Create `.planning/phases/15-chainage-as-built-view-approval-snapshot/15-05-SUMMARY.md` when done.
</output>
