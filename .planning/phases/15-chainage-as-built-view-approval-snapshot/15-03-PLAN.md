---
phase: 15-chainage-as-built-view-approval-snapshot
plan: 03
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - src/lib/types/canonical-submission.ts
  - src/actions/analytics.ts
  - src/components/admin/SubmissionDetailView.tsx
  - messages/tr.json
  - messages/en.json
  - tests/chainage.test.ts
autonomous: true
requirements: [CHN-05]
must_haves:
  truths:
    - "getCanonicalSubmissions returns snappedLat (ST_Y = latitude) and snappedLon (ST_X = longitude) — no axis swap (Pitfall 5)"
    - "SubmissionDetailView renders a Google Maps link 'Haritada Gör / View on Map' with rel=noopener noreferrer when both coords are non-null"
    - "SubmissionDetailView renders a '← As-Built'e dön' back-link via router.back() when searchParams.from === 'asbuilt' (CHN-05)"
  artifacts:
    - path: "src/lib/types/canonical-submission.ts"
      provides: "snappedLat + snappedLon nullable fields on CanonicalSubmission"
      contains: "snappedLat"
    - path: "src/components/admin/SubmissionDetailView.tsx"
      provides: "Google Maps link + As-Built back-link"
      contains: "google.com/maps"
  key_links:
    - from: "src/actions/analytics.ts"
      to: "submissions.snapped_point"
      via: "ST_Y AS snapped_lat, ST_X AS snapped_lon in SELECT"
      pattern: "snapped_lat"
    - from: "src/components/admin/SubmissionDetailView.tsx"
      to: "https://www.google.com/maps"
      via: "anchor with ?q=lat,lon"
      pattern: "google.com/maps"
---

<objective>
Folded todo `submission-detail-map-link` + the CHN-05 drill-down target: extend `CanonicalSubmission` with snapped lat/lon, render a Google Maps link on the submission detail view, and add the As-Built back-link.

Purpose: CHN-05 sends the as-built strip row into this exact detail view; this plan prepares both the back-link (so the strip drill-down has a return path) and the map-link the folded todo requested. Runs in parallel with plan 02 — zero file overlap with the bot path.
Output: modified `canonical-submission.ts`, `analytics.ts`, `SubmissionDetailView.tsx`, both i18n files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-chainage-as-built-view-approval-snapshot/15-RESEARCH.md
@.planning/phases/15-chainage-as-built-view-approval-snapshot/15-PATTERNS.md
@.planning/phases/15-chainage-as-built-view-approval-snapshot/15-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend CanonicalSubmission + getCanonicalSubmissions with snapped coords</name>
  <files>src/lib/types/canonical-submission.ts, src/actions/analytics.ts, tests/chainage.test.ts</files>
  <read_first>
    - `src/lib/types/canonical-submission.ts` lines 21-44 (existing type shape — add two fields at the end)
    - `src/actions/analytics.ts` lines 227-298 (getCanonicalSubmissions SELECT block + result row mapping; sql template; tenant scope at line 193)
    - `.planning/phases/15-chainage-as-built-view-approval-snapshot/15-RESEARCH.md` §Pattern 8 + §Pitfall 5 (ST_Y = latitude, ST_X = longitude; Google Maps needs ?q=lat,lon so snapped_lat=ST_Y, snapped_lon=ST_X)
    - `.planning/phases/15-chainage-as-built-view-approval-snapshot/15-PATTERNS.md` §getCanonicalSubmissions SELECT extension + row-mapping
  </read_first>
  <action>Add `snappedLat: number | null` and `snappedLon: number | null` to the `CanonicalSubmission` type with comments noting snappedLat = ST_Y = latitude, snappedLon = ST_X = longitude. In `getCanonicalSubmissions`, add to the SELECT (before FROM): `ST_Y(s.snapped_point) AS snapped_lat, ST_X(s.snapped_point) AS snapped_lon` — these return NULL when snapped_point IS NULL (no_route submissions). In the result row mapping add `snappedLat: r.snapped_lat != null ? Number(r.snapped_lat) : null` and `snappedLon: r.snapped_lon != null ? Number(r.snapped_lon) : null`. Coordinates are floats, NOT money — `Number()` is correct (no decimal.js). Tenant scope and auth on getCanonicalSubmissions already exist; do not change them. Fill the "maps link" test scaffold in tests/chainage.test.ts asserting the axis order (snapped_lat derives from ST_Y/latitude, snapped_lon from ST_X/longitude), per the existing test's reach.</action>
  <verify>
    <automated>grep -c "snappedLat" src/lib/types/canonical-submission.ts && grep -E "snapped_lat" src/actions/analytics.ts</automated>
  </verify>
  <done>Type carries snappedLat/snappedLon; SELECT uses ST_Y AS snapped_lat + ST_X AS snapped_lon; `npx tsc --noEmit` passes; existing analytics/exports callers unaffected (additive nullable fields).</done>
</task>

<task type="auto">
  <name>Task 2: Google Maps link + As-Built back-link in SubmissionDetailView + i18n</name>
  <files>src/components/admin/SubmissionDetailView.tsx, messages/tr.json, messages/en.json</files>
  <read_first>
    - `src/components/admin/SubmissionDetailView.tsx` lines 34 (MapPin import), 122-127 (existing anchor + rel=noopener noreferrer pattern), 204-210 (distance/warning badge in Location dd)
    - `.planning/phases/15-chainage-as-built-view-approval-snapshot/15-UI-SPEC.md` §Surface Spec §7 (map link markup) + §Interaction Contracts (back-link: BrandButton variant=ghost size=sm, ChevronLeft, router.back(), only when from===asbuilt) + Copywriting Contract (key dashboard.records.view_on_map; back-link copy "← As-Built'e dön" / "← Back to As-Built")
    - `.planning/phases/15-chainage-as-built-view-approval-snapshot/15-PATTERNS.md` §SubmissionDetailView.tsx map link
  </read_first>
  <action>In `SubmissionDetailView.tsx`, after the existing distance/warning badge inside the Location `<dd>`, render (only when `submission.snappedLat != null && submission.snappedLon != null`) an anchor href `https://www.google.com/maps?q={snappedLat},{snappedLon}` with `target="_blank" rel="noopener noreferrer"`, MapPin size-3 icon (aria-hidden), class `inline-flex items-center gap-1 text-xs text-primary underline`, label `t('view_on_map')`. CRITICAL: latitude first, longitude second — snappedLat (ST_Y) before snappedLon (ST_X), no swap (Pitfall 5). Add the As-Built back-link: when the detail page passes `from === 'asbuilt'` (thread the `from` value into this component as a prop), render a back-link styled per UI-SPEC (ghost button, ChevronLeft size-4) that calls `router.back()`; if SubmissionDetailView is an RSC, add a small `'use client'` back-link subcomponent so router.back() works. Add i18n keys `dashboard.records.view_on_map` (TR "Haritada Gör" / EN "View on Map") and `dashboard.records.back_to_asbuilt` (TR "← As-Built'e dön" / EN "← Back to As-Built") to BOTH messages/tr.json and messages/en.json.</action>
  <verify>
    <automated>grep -c "google.com/maps" src/components/admin/SubmissionDetailView.tsx && grep -c "noopener noreferrer" src/components/admin/SubmissionDetailView.tsx && node -e "const tr=require('./messages/tr.json'),en=require('./messages/en.json'); const g=(o,p)=>p.split('.').reduce((a,k)=>a&&a[k],o); for(const k of ['dashboard.records.view_on_map','dashboard.records.back_to_asbuilt']){ if(!g(tr,k))throw new Error('tr missing '+k); if(!g(en,k))throw new Error('en missing '+k);} console.log('i18n ok')"</automated>
  </verify>
  <done>Maps link renders with lat,lon (no swap) and noopener noreferrer; back-link present gated on from=asbuilt; both i18n keys exist in tr+en; `npx tsc --noEmit` + `npx next build` (or tsc) pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → external (Google Maps) | target=_blank anchor to an external site |
| client → Server Action (getCanonicalSubmissions) | already auth()+tenant-scoped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03-TABNAB | Repudiation | Google Maps target=_blank anchor | mitigate | `rel="noopener noreferrer"` on the anchor (existing SubmissionDetailView pattern at line 122-127) |
| T-15-03-AXIS | Information Disclosure | swapped lat/lon → wrong pin location | mitigate | snapped_lat = ST_Y, snapped_lon = ST_X; ?q=lat,lon order; named columns assert intent (Pitfall 5) |
| T-15-03-IDOR | Information Disclosure | getCanonicalSubmissions row access | mitigate | Existing auth() + tenant scope unchanged; additive columns only |
| T-15-SC | Tampering | npm installs | accept | Zero new packages this phase |
</threat_model>

<verification>
- `grep -c "google.com/maps" src/components/admin/SubmissionDetailView.tsx` ≥ 1 and `noopener noreferrer` present.
- Both i18n keys resolve in tr.json and en.json (node assertion above).
- `npx tsc --noEmit` passes.
</verification>

<success_criteria>
CanonicalSubmission carries correctly-ordered snapped coords; the detail view shows a Google Maps link with no axis swap and the As-Built back-link; i18n bilingual. CHN-05 drill-down return path is ready.
</success_criteria>

<output>
Create `.planning/phases/15-chainage-as-built-view-approval-snapshot/15-03-SUMMARY.md` when done.
</output>
