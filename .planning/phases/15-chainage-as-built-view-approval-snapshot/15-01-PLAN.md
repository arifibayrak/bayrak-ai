---
phase: 15-chainage-as-built-view-approval-snapshot
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/format-chainage.ts
  - tests/chainage.test.ts
  - tests/fixtures/chainage.ts
autonomous: true
requirements: [CHN-01, CHN-03, CHN-04, CHN-06]
must_haves:
  truths:
    - "formatChainage(2347) returns 'km 2+347' (D-01 Turkish stationing)"
    - "tests/chainage.test.ts exists and the pure-unit formatChainage block is green"
    - "Integration tests for snapshot, buckets, three-state, completion clamp exist as it.todo or describeIfDb scaffolds with byte-identical -t names from 15-VALIDATION.md"
    - "Fixture seeds a 3000m route + 3 approved submissions at segment_fraction 0.166/0.5/0.833"
  artifacts:
    - path: "src/lib/format-chainage.ts"
      provides: "formatChainage(m) pure utility shared across RSC/client/PDF/Telegram"
      exports: ["formatChainage"]
    - path: "tests/chainage.test.ts"
      provides: "Nyquist test scaffold for CHN-01/03/04/06 + Pitfall 2"
      contains: "formatChainage"
    - path: "tests/fixtures/chainage.ts"
      provides: "3000m route + 3-submission seed + exported CHAINAGE_FIXTURE_IDS"
      exports: ["seedChainageFixture", "CHAINAGE_FIXTURE_IDS"]
  key_links:
    - from: "tests/chainage.test.ts"
      to: "src/lib/format-chainage.ts"
      via: "import { formatChainage }"
      pattern: "import.*formatChainage.*format-chainage"
---

<objective>
Wave 0 foundation: the shared `formatChainage` utility and the full Nyquist test scaffold for Phase 15. Every later plan binds its automated verification to the `-t` names created here.

Purpose: Establish the pure `formatChainage` util (used by strip table, Telegram approval line, Excel and PDF exports) and a single `tests/chainage.test.ts` whose test names match 15-VALIDATION.md verbatim so downstream plans turn red scaffolds green without renaming.
Output: `src/lib/format-chainage.ts`, `tests/chainage.test.ts` (pure-unit green + integration scaffolds), `tests/fixtures/chainage.ts` (3000m route fixture).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-chainage-as-built-view-approval-snapshot/15-VALIDATION.md
@.planning/phases/15-chainage-as-built-view-approval-snapshot/15-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: formatChainage utility + green unit tests</name>
  <files>src/lib/format-chainage.ts, tests/chainage.test.ts</files>
  <read_first>
    - `.planning/phases/15-chainage-as-built-view-approval-snapshot/15-UI-SPEC.md` §Copywriting Contract (verbatim formatChainage implementation, lines 280-290)
    - `.planning/phases/15-chainage-as-built-view-approval-snapshot/15-PATTERNS.md` §`src/lib/format-chainage.ts` (analog: `src/lib/spatial.ts` → `formatDistance`, pure-function pattern)
    - `tests/spatial.test.ts` lines 1-50 (pure-unit `describe` block pattern; vitest imports)
  </read_first>
  <behavior>
    - formatChainage(0) === "km 0+000"
    - formatChainage(500) === "km 0+500"
    - formatChainage(1000) === "km 1+000"
    - formatChainage(2347) === "km 2+347"
    - formatChainage(12480) === "km 12+480"
  </behavior>
  <action>Create `src/lib/format-chainage.ts` exporting a pure `formatChainage(m: number): string` with zero imports: `km = Math.floor(m / 1000)`, `remainder = Math.round(m % 1000).toString().padStart(3, '0')`, return `` `km ${km}+${remainder}` ``. It must be importable from RSC, client components, the PDF helper, and the Telegram bot path without circular deps (verified by zero imports). In `tests/chainage.test.ts` add a top-level `describe('formatChainage (CHN-01)', ...)` block (always runs, no DB) with the five `it` cases above, importing from `../src/lib/format-chainage`. Test name must contain the literal token `formatChainage` so `-t "formatChainage"` selects it.</action>
  <verify>
    <automated>npx vitest run tests/chainage.test.ts -t "formatChainage"</automated>
  </verify>
  <done>formatChainage exported; all five formatChainage unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Chainage fixture (3000m route + 3 approved submissions)</name>
  <files>tests/fixtures/chainage.ts, tests/chainage.test.ts</files>
  <read_first>
    - `.planning/phases/15-chainage-as-built-view-approval-snapshot/15-PATTERNS.md` §`tests/chainage.test.ts` (fixture seed pattern; `describeIfDb`, `getTestDb`, `truncateAllTables` imports from `./fixtures/db`)
    - `tests/postgis.test.ts` lines 19-45 (describeIfDb + INSERT tenant/project seed via `db.execute(sql.raw(...))`)
    - `tests/fixtures/exports.ts` (existing fixture file shape: exported `*_FIXTURE_IDS` const + `seed*` async function; UUID-range collision-avoidance convention)
    - `src/db/schema/submissions.ts` lines 15-66 (columns: personId, projectId, segmentFraction numeric(10,8), chainageM numeric(10,2), routeGeometryVersion integer, status)
    - `src/db/schema/routes.ts` (total_length_m, geometry_version, chainage_offset_m columns)
  </read_first>
  <action>Create `tests/fixtures/chainage.ts` exporting `CHAINAGE_FIXTURE_IDS` (deterministic UUIDs in a fresh 0f00-range to avoid collision with existing exports/hakedis fixtures) and an async `seedChainageFixture(db)` that inserts: one tenant, one project, one route with `total_length_m = 3000`, `geometry_version = 1`, `chainage_offset_m = 0`; one BOQ item; one worker person and one auditor person; three submissions with `status='approved'`, `segment_fraction` 0.166 / 0.5 / 0.833 and `decided_by` = auditor person id. Leave `chainage_m` NULL on seed so the snapshot/backfill integration tests assert the write. Use `db.execute(sql.raw(...))` INSERT pattern with ON CONFLICT DO NOTHING for tenant/project. Wire a `describeIfDb('chainage snapshot + bucket aggregation (CHN-03, CHN-04)', ...)` block in tests/chainage.test.ts that calls `seedChainageFixture` in `beforeEach` after `truncateAllTables`.</action>
  <verify>
    <automated>npx vitest run tests/chainage.test.ts -t "formatChainage"</automated>
  </verify>
  <done>Fixture file exists with `seedChainageFixture` + `CHAINAGE_FIXTURE_IDS`; importing it does not break the green unit suite; the describeIfDb block compiles.</done>
</task>

<task type="auto">
  <name>Task 3: Integration + unit test scaffolds matching 15-VALIDATION -t names</name>
  <files>tests/chainage.test.ts</files>
  <read_first>
    - `.planning/phases/15-chainage-as-built-view-approval-snapshot/15-VALIDATION.md` §Per-Requirement Verification Map (the exact `-t` strings: "chainage snapshot", "getChainageBuckets", "bucket status", "completion", "completion clamp", "bucket boundary", "chainage offset", "chainage excel columns", "maps link")
    - `.planning/phases/15-chainage-as-built-view-approval-snapshot/15-RESEARCH.md` §Validation Architecture (behaviors per test), §Common Pitfalls (Pitfall 2 boundary: exactly 1000.0 m → bucket index 1)
    - `tests/hakedis-live.test.ts` (it.todo contract-scaffold convention — names byte-identical to verify-command -t filters)
  </read_first>
  <action>Add scaffold tests (as `it.todo` or `describeIfDb` with skeleton assertions) whose names contain these exact tokens so downstream plans bind by `-t`: "chainage snapshot" (CHN-03 — chainage_m non-NULL = ROUND(frac×len,2), route_geometry_version matches), "getChainageBuckets" (CHN-04 — generate_series enumerates ALL buckets; 3000m/1000m → 3 buckets), "bucket status" (CHN-04/D-04 three-state: ≥1 approved→approved, 0 approved+≥1 pending→in_progress, none→not_started), "completion" + "completion clamp" (CHN-06 — covered ÷ total ×100; 2 approved in km 0-1 on 1km route → 100 not 200), "bucket boundary" (Pitfall 2 — exactly 1000.0 m → index 1 not 0), "chainage offset" (CHN-02 — setChainageOffset writes routes.chainage_offset_m; buckets apply offset), "chainage excel columns" (CHN-07 — 8 cols in order), "maps link" (folded todo — ST_Y=lat ST_X=lon no axis swap). Each scaffold either `it.todo(...)` or a `describeIfDb` block that is skipped cleanly when no TEST_DATABASE_URL. Do NOT implement the production code here — only the named scaffolds.</action>
  <verify>
    <automated>npx vitest run tests/chainage.test.ts 2>&1 | grep -E "formatChainage|todo|chainage" | head</automated>
  </verify>
  <done>tests/chainage.test.ts contains named scaffolds for all 15-VALIDATION.md rows; suite runs (todos reported, unit green); zero failing tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none (test/util only) | This plan ships a pure utility and test scaffolds; no untrusted input crosses a boundary at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01-FLOAT | Tampering | formatChainage rounding | accept | Display-only formatting; not a money/quantity value. Postgres-side ROUND owns the stored numeric (plan 02). JS Math.round here only formats the integer-metre remainder for display. |
| T-15-SC | Tampering | npm installs | accept | Phase 15 installs zero new packages (15-RESEARCH §No New Dependencies); no package-legitimacy gate needed. |
</threat_model>

<verification>
- `npx vitest run tests/chainage.test.ts -t "formatChainage"` green (5/5).
- Full file `npx vitest run tests/chainage.test.ts` reports todos/skips but zero failures.
- `grep -c "import" src/lib/format-chainage.ts` returns 0 (zero-import purity).
</verification>

<success_criteria>
formatChainage util exists and is correct; test scaffold names match 15-VALIDATION.md exactly; fixture seeds a deterministic 3000m route. Downstream plans can turn each named test green without renaming.
</success_criteria>

<output>
Create `.planning/phases/15-chainage-as-built-view-approval-snapshot/15-01-SUMMARY.md` when done.
</output>
