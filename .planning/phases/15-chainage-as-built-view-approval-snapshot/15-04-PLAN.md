---
phase: 15-chainage-as-built-view-approval-snapshot
plan: 04
type: execute
wave: 3
depends_on: ["15-02"]
files_modified: []
autonomous: false
requirements: [CHN-03]
must_haves:
  truths:
    - "Migration 0013 is applied to BOTH Neon branches (dev DATABASE_URL + test TEST_DATABASE_URL) via npx tsx src/db/migrate.ts (never drizzle-kit push, D-49)"
    - "Post-apply DB check: COUNT(*) FROM submissions WHERE status='approved' AND chainage_m IS NULL AND segment_fraction IS NOT NULL returns 0 (or flagged non-zero with a route.total_length_m NULL diagnosis)"
  artifacts: []
  key_links:
    - from: "src/db/migrations/0013_v4_chainage_backfill.sql"
      to: "Neon dev + test branches"
      via: "npx tsx src/db/migrate.ts on both DATABASE_URL and TEST_DATABASE_URL"
      pattern: "migrate"
---

<objective>
[BLOCKING] Apply the backfill migration 0013 to both Neon branches and verify the backfill landed. This closes the false-positive verification trap — build and type checks pass without the live migration, but the strip view and tests need real chainage data in the DB.

Purpose: CHN-03 backfill of historical approved submissions; without this, the as-built strip is empty for pre-Phase-15 approvals and the snapshot integration tests (plan 02/05) run against an empty column.
Output: applied migration on dev + test; verification COUNT result recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15-chainage-as-built-view-approval-snapshot/15-RESEARCH.md
@.planning/phases/15-chainage-as-built-view-approval-snapshot/15-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Apply migration 0013 to dev + test, then verify backfill</name>
  <what-built>Migration 0013_v4_chainage_backfill.sql (authored + journal-registered in plan 02) is ready to apply. This task runs the project migration runner against both Neon branches and runs the post-apply verification SQL.</what-built>
  <how-to-verify>
    1. Apply to DEV branch: `npx tsx src/db/migrate.ts` (uses DATABASE_URL).
    2. Apply to TEST branch: `DATABASE_URL=$TEST_DATABASE_URL npx tsx src/db/migrate.ts` (the test Neon branch — same env var name the project uses for the test DB; confirm against vitest/fixtures/db setup).
    3. NEVER use `drizzle-kit push` (D-49 — spatial_ref_sys permission error).
    4. Post-apply verification on DEV: run `SELECT COUNT(*) FROM submissions WHERE status='approved' AND chainage_m IS NULL AND segment_fraction IS NOT NULL;` — expect 0.
    5. If the COUNT is non-zero: the likely cause (RESEARCH Open Question 3) is `routes.total_length_m IS NULL` because the route predates Phase 14. FLAG this — those rows will be populated going forward by the plan 02 approval snapshot path; do not block the phase, but record the diagnosis in the summary.
    6. Confirm 0013 appears applied in Drizzle's `__drizzle_migrations` table (journal `when` > 0012 epoch was validated in plan 02).
  </how-to-verify>
  <resume-signal>Type "applied" with the dev COUNT result (and test branch confirmation), or describe any migration error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration runner → Neon (dev + test) | DDL/DML applied with elevated DB credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04-DRIFT | Tampering | dev and test branches out of sync | mitigate | Apply 0013 to BOTH branches in the same task (Phase 7/12 precedent: test branch needs its own migrate.ts run) |
| T-15-04-SKIP | Tampering | Drizzle silently skips 0013 | mitigate | journal `when` > 1780088633500 validated in plan 02; verify 0013 in __drizzle_migrations post-apply |
| T-15-04-PUSH | Tampering | accidental drizzle-kit push | mitigate | D-49 — runner is `npx tsx src/db/migrate.ts` only; push errors on spatial_ref_sys |
| T-15-SC | Tampering | npm installs | accept | Zero new packages this phase |
</threat_model>

<verification>
- 0013 present in `__drizzle_migrations` on both branches.
- Dev `SELECT COUNT(*) ... chainage_m IS NULL AND segment_fraction IS NOT NULL` = 0 (or flagged with total_length_m NULL diagnosis).
</verification>

<success_criteria>
Migration 0013 applied to dev + test; backfill verified (0 unbackfilled approved rows, or a clearly-recorded total_length_m-NULL exception). The as-built strip now has real data to render.
</success_criteria>

<output>
Create `.planning/phases/15-chainage-as-built-view-approval-snapshot/15-04-SUMMARY.md` when done (record the COUNT result).
</output>
