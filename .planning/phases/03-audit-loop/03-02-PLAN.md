---
phase: 03-audit-loop
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/db/migrations/
autonomous: true
requirements: [AUDIT-04, AUDIT-05, AUDIT-06]
must_haves:
  truths:
    - "A generated Drizzle migration adds decided_by/decided_at/rejection_reason to submissions and creates the audit_notifications table"
    - "The migration is applied to the live database via the project migrate runner (not drizzle-kit push)"
    - "After apply, the live DB has the audit_notifications table and the three new submissions columns"
  artifacts:
    - path: "src/db/migrations/"
      provides: "Generated SQL migration for Phase 3 schema additions"
      contains: "audit_notifications"
  key_links:
    - from: "src/db/migrate.ts"
      to: "src/db/migrations/"
      via: "migrate(db, { migrationsFolder: 'src/db/migrations' })"
      pattern: "migrationsFolder"
---

<objective>
Generate and APPLY the Phase 3 database migration. Without this, every type and build check passes (types come from schema files, not the live DB) while the live database lacks the audit_notifications table and the three new submissions columns — a false-positive verification state. This [BLOCKING] plan closes that trap, exactly as Phase 2 Plan 02-03 did for its three tables.

Purpose: The atomic decision transaction (Plan 05) and fan-out persistence (Plan 04) write to columns/tables that must physically exist in Postgres.
Output: New SQL files in src/db/migrations/ and a migrated live database.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-audit-loop/03-CONTEXT.md
@.planning/phases/03-audit-loop/03-RESEARCH.md
@.planning/phases/03-audit-loop/03-PATTERNS.md

<interfaces>
<!-- Migration tooling — extracted from codebase -->

From drizzle.config.ts:
- dialect 'postgresql', schema './src/db/schema/*.ts', out './src/db/migrations', dbCredentials.url = DATABASE_URL.

From src/db/migrate.ts (the project migrate runner — Phase 1/2 convention):
- Step 1: runs src/db/migrations/0000_enable_postgis.sql via neon-http `sql.query()`.
- Step 2: `migrate(db, { migrationsFolder: 'src/db/migrations' })` (drizzle-orm/neon-http/migrator).
- Invoked with `npx tsx src/db/migrate.ts` (tsx is in devDependencies; there is NO npm script for it).

Existing migrations present: 0000_enable_postgis.sql, 0000_lame_silver_sable.sql, 0001_fixed_sunspot.sql, meta/.
Project convention is `drizzle-kit generate` + `migrate()` runner — NOT `drizzle-kit push` (despite some research/pattern wording). Match how Phases 1/2 applied migrations.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate the Phase 3 migration and inspect the SQL</name>
  <files>src/db/migrations/</files>
  <read_first>
    - drizzle.config.ts (out dir + schema glob)
    - src/db/migrations/0001_fixed_sunspot.sql (most recent generated migration — format reference)
    - src/db/schema/audit-notifications.ts (the new table the migration must contain — created in Plan 01)
    - src/db/schema/submissions.ts (the three new columns the migration must contain)
    - 03-PATTERNS.md §"src/db/migrations/ (Drizzle migration files)"
  </read_first>
  <action>
    Run `npx drizzle-kit generate` to produce a new migration in `src/db/migrations/`. This reads the schema glob and diffs against `meta/` snapshots, emitting SQL for: `ALTER TABLE submissions ADD COLUMN decided_by uuid` (+ FK to people), `ADD COLUMN decided_at timestamptz`, `ADD COLUMN rejection_reason text`; and `CREATE TABLE audit_notifications (...)` with its two indexes and FKs (submission_id cascade, auditor_person_id, tenant_id).

    Open and inspect the generated SQL file. This phase adds NO geometry/custom-type columns, so the routes.ts LineString hand-edit caveat does NOT apply here — but still confirm: no unexpected DROP statements, the audit_notifications.submission_id FK has `ON DELETE CASCADE`, chat_id is bigint, message_id is integer, send_failed boolean default false. Do not hand-edit unless the generated SQL is wrong; if a manual fix is needed, document it in the SUMMARY.
  </action>
  <verify>
    <automated>ls -t src/db/migrations/*.sql | head -1 | xargs grep -l "audit_notifications" && grep -rl "rejection_reason" src/db/migrations/*.sql</automated>
  </verify>
  <acceptance_criteria>
    - A new `.sql` file exists in src/db/migrations/ newer than 0001_fixed_sunspot.sql.
    - The new migration SQL contains `CREATE TABLE` (or `create table`) for `audit_notifications` with a `submission_id` FK and `ON DELETE CASCADE`.
    - The new migration SQL contains `ALTER TABLE` adding `decided_by`, `decided_at`, and `rejection_reason` to submissions.
    - The SQL contains NO `DROP TABLE` for any existing Phase 1/2 table and NO `CHECK` constraint on approved_qty.
    - `meta/_journal.json` references the new migration tag.
  </acceptance_criteria>
  <done>New migration SQL generated, inspected, and confirmed to add the three columns + the new table without destructive statements.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 2: [BLOCKING] Apply the migration to the live database</name>
  <files>src/db/migrations/</files>
  <read_first>
    - src/db/migrate.ts (the runner — PostGIS pre-step then migrate())
    - The migration file generated in Task 1
  </read_first>
  <action>
    Apply the generated migration to the live database using the project runner: `npx tsx src/db/migrate.ts` (matches Phase 1/2 — NOT `drizzle-kit push`). The runner enables PostGIS first (idempotent), then applies all pending migrations in src/db/migrations/, including the new Phase 3 file. Requires DATABASE_URL in the environment (.env.local). If the runner needs env loading, invoke it the same way Phase 1/2 did (the runner reads process.env.DATABASE_URL directly).

    After apply, verify the live DB physically has the new table and columns by querying the live schema (a quick `psql`-style check via the runner or a one-off tsx script querying information_schema is acceptable — or re-run the migrate runner and confirm it reports no pending migrations).

    This task is MANDATORY and BLOCKING: the phase CANNOT pass verification without the live DB reflecting these additions. Build and type checks pass without it (false positive), so do not skip on a green typecheck.
  </action>
  <verify>
    <automated>npx tsx src/db/migrate.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx src/db/migrate.ts` runs to completion and prints "Migrations complete" with no error.
    - Re-running the runner is idempotent (reports no pending migrations / completes cleanly a second time).
    - A live-DB introspection confirms `audit_notifications` table exists and `submissions` has `decided_by`, `decided_at`, `rejection_reason` columns. (E.g. a one-off tsx query against `information_schema.columns` / `information_schema.tables`, or the DB-gated tests in Plan 04/05 begin passing where they previously errored on a missing table.)
    - DATABASE_URL is NOT printed to logs.
  </acceptance_criteria>
  <done>Live database has the audit_notifications table and the three submissions audit-trail columns; migrate runner is idempotent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration runner → Postgres | Schema-changing DDL crosses here; a bad migration could drop data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-MIG-01 | Tampering | generated migration SQL | mitigate | Inspect generated SQL before apply (Task 1 acceptance forbids DROP of existing tables); additions are nullable columns + a new table — non-destructive to Phase 1/2 data. |
| T-3-MIG-02 | Denial of Service | false-positive verification | mitigate | [BLOCKING] apply (Task 2) is mandatory; verification gate requires live-DB introspection, not just typecheck. |
| T-3-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (RESEARCH.md Package Legitimacy Audit: none). drizzle-kit/tsx already installed. |
</threat_model>

<verification>
- New migration SQL present and inspected (no destructive statements, no approved_qty CHECK).
- `npx tsx src/db/migrate.ts` completes; live DB has audit_notifications + the three submissions columns.
- Idempotent re-run completes cleanly.
</verification>

<success_criteria>
- Phase 3 schema additions exist in the LIVE database (not just in schema files).
- Migration generated via drizzle-kit generate and applied via the project migrate runner (Phase 1/2 convention).
- The false-positive verification trap (green types without a live migration) is closed.
</success_criteria>

<output>
Create `.planning/phases/03-audit-loop/03-02-SUMMARY.md` when done.
</output>
