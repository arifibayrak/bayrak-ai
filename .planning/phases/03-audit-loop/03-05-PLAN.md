---
phase: 03-audit-loop
plan: 05
type: execute
wave: 4
depends_on: ["03-04"]
files_modified:
  - src/lib/bot-audit.ts
  - src/lib/telegram.ts
autonomous: true
requirements: [AUDIT-03, AUDIT-04, AUDIT-05, AUDIT-06]
must_haves:
  truths:
    - "Only an auditor assigned to the submission's project can decide; a non-assigned tap changes no DB row and returns an ephemeral rejection"
    - "Approve atomically sets status='approved' and increments boq_items.approved_qty by exactly the submitted quantity; a second/concurrent tap is a no-op returning 'already resolved' with NO double-increment"
    - "Reject prompts for a MANDATORY reason (canned or free-text via the DB-row FSM); abandoning before a reason leaves the submission pending_audit"
    - "On both outcomes the worker is notified via Telegram; on decision all sibling auditor messages lose their buttons and show the outcome"
    - "A replayed callback_query is de-duped by the processed_updates fence"
  artifacts:
    - path: "src/lib/bot-audit.ts"
      provides: "handleAuditDecision + handleAuditReasonSelect + handleAuditRejectFreeText + commitRejection (D-29,30,31,32,36,37)"
      contains: "handleAuditDecision"
    - path: "src/lib/telegram.ts"
      provides: "audit: callback dispatch branches + AWAITING_REJECT_REASON message-switch case"
      contains: "audit:approve:"
  key_links:
    - from: "src/lib/telegram.ts dispatchCallbackQuery"
      to: "handleAuditDecision / handleAuditReasonSelect"
      via: "audit:approve / audit:reject / audit:reason prefix branches"
      pattern: "audit:approve:"
    - from: "src/lib/bot-audit.ts handleAuditDecision"
      to: "assignments"
      via: "re-query role_on_project='auditor' on every tap (D-36)"
      pattern: "roleOnProject"
    - from: "src/lib/bot-audit.ts approve transaction"
      to: "boq_items.approved_qty"
      via: "UPDATE-RETURNING WHERE status='pending_audit' then sql`approved_qty + qty`"
      pattern: "approved_qty \\+"
    - from: "src/lib/bot-audit.ts commitRejection"
      to: "worker notification"
      via: "bot.api.sendMessage to submission.personId → people.telegram_user_id"
      pattern: "workerRejected"
---

<objective>
Implement the decision side of the audit loop: authorize every tap server-side against assignments (D-36), commit Approve in a single atomic first-wins transaction that increments approved_qty (D-27, D-29), and capture a MANDATORY reject reason via the two-tier canned/free-text DB-row FSM (D-30, D-31, D-32) before committing a rejection. On every decision, edit all sibling auditor messages (Plan 04's editAllSiblingMessages) and notify the worker (D-37). Wire the `audit:` callback branches into the existing dispatcher and the `AWAITING_REJECT_REASON` message-switch case. Idempotency relies on the existing processed_updates fence (D-13) plus the atomic guard.

Purpose: Delivers AUDIT-03, AUDIT-04, AUDIT-05, AUDIT-06 — the security- and race-critical core. This is where the trust loop closes and where the two MANDATORY race/atomic-increment tests must pass.
Output: Decision handlers in `src/lib/bot-audit.ts`; dispatcher + message-switch wiring in `src/lib/telegram.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-audit-loop/03-CONTEXT.md
@.planning/phases/03-audit-loop/03-RESEARCH.md
@.planning/phases/03-audit-loop/03-PATTERNS.md
@.planning/phases/03-audit-loop/03-VALIDATION.md
@.planning/phases/03-audit-loop/03-01-SUMMARY.md
@.planning/phases/03-audit-loop/03-03-SUMMARY.md
@.planning/phases/03-audit-loop/03-04-SUMMARY.md

<interfaces>
<!-- Extracted from codebase + Plan 01/03/04 outputs — executor uses these directly. -->

From src/lib/telegram.ts (current):
- `bot.on('callback_query:data')` (lines 314-376): calls `await ctx.answerCallbackQuery()` FIRST (line 316), then routes flow:resume / flow:restart, else delegates to `dispatchCallbackQuery(ctx, data, db, telegramUserId)` (line 375). NOTE: the generic answerCallbackQuery at line 316 already fires for all callback data; audit handlers may still call answerCallbackQuery with a TOAST (text) which is a separate, allowed call.
- `dispatchCallbackQuery(ctx, data, db, telegramUserId)` (lines 462-558): an if-chain on `data.startsWith(...)`. It loads conversation_state and bails with `noActiveFlow` if absent/stale (lines 475-485) — this is a PROBLEM for audit callbacks because auditors have NO conversation_state row. Add the `audit:` branches BEFORE the conversation_state load (right at the top of the function), so audit taps never require a worker FSM row. The existing fallthrough is `repromptStep` at line 557.
- `bot.on('message')` (lines 567-620): loads conversation_state, switches on `state.currentStep` (cases at 595-619). Add a `case STEPS.AWAITING_REJECT_REASON:` here for the free-text reason entry.
- `getTxDb()` (lines 1156-1175): neon-serverless Pool — MANDATORY for `.transaction()`. The default `@/db` (neon-http) throws on transactions. Copy/reuse this exact helper (it is module-private; either export it or replicate the same block in bot-audit.ts per PATTERNS §getTxDb).
- `saveState(db, telegramUserId: bigint, step, data, personId, flowId?)` (exported, lines 158-195): upserts conversation_state (one row per telegram_user_id). Reuse for the auditor reject FSM state.

From src/lib/bot-audit.ts (Plan 04 output): `editAllSiblingMessages(submissionId, outcomeCaption)`, `AlreadyResolvedError`.
From src/lib/bot-messages.ts (Plan 03): auditUnauthorized, auditAlreadyResolved, auditRejectPrompt, auditRejectFreeTextPrompt, auditApprovedOutcome(name), auditRejectedOutcome(name, reason), workerApproved, workerRejected(reason), genericError.
From src/lib/bot-keyboards.ts (Plan 03): buildRejectReasonKeyboard().
From src/lib/bot-fsm.ts (Plan 01): STEPS.AWAITING_REJECT_REASON, STEPS.AWAITING_REJECT_REASON_FREE.

From src/db/schema: submissions (id, status, decidedBy, decidedAt, rejectionReason, quantity, boqItemId, projectId, personId), boqItems (id, approvedQty), assignments (personId, projectId, roleOnProject), people (id, telegramUserId, displayName).

Atomic approve guard (RESEARCH Pattern 1 + Code Examples, VERIFIED against drizzle-orm 0.45.2): UPDATE-RETURNING is preferred —
`tx.update(submissions).set({status:'approved', decidedBy, decidedAt}).where(and(eq(id,submissionId), eq(status,'pending_audit'))).returning({id, quantity, boqItemId})`; if `affected.length===0` → AlreadyResolvedError (already decided). Then `tx.update(boqItems).set({approvedQty: sql\`approved_qty + ${qty}\`}).where(eq(boqItems.id, boqItemId))`. Postgres serializes concurrent UPDATEs on the same row so the second tap's WHERE matches 0 rows (RESEARCH Pitfall 7). NEVER a subtractive op (D-27 is increment-only).
answerCallbackQuery must be called on EVERY code path (RESEARCH Pattern 3 / Pitfall 2), including unauthorized, already-resolved, and error.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: handleAuditDecision — authz + atomic approve + reject-reason entry</name>
  <files>src/lib/bot-audit.ts</files>
  <read_first>
    - src/lib/bot-audit.ts (Plan 04 — editAllSiblingMessages, AlreadyResolvedError; the file being extended)
    - src/lib/telegram.ts handleConfirmSubmit transaction (1246-1331) + getTxDb (1156-1175) + saveState (158-195)
    - 03-PATTERNS.md §"src/lib/bot-audit.ts" (decision transaction, authorization re-check, error-handling, saveState reuse patterns)
    - 03-RESEARCH.md Pattern 1 (atomic transaction), Pattern 3 (answerCallbackQuery on every path), Pitfall 1 (neon-serverless only), Pitfall 2 (answerCallbackQuery), Pitfall 3 (reject not committed before reason), Pitfall 7 (concurrent UPDATE)
    - 03-VALIDATION.md (MANDATORY race + atomic-increment rows)
  </read_first>
  <behavior>
    - Unauthorized tap (no auditor assignment on the submission's project for this telegram_user_id) → answerCallbackQuery({text: auditUnauthorized, show_alert:true}); NO submissions/boq_items/conversation_state write.
    - Approve by an authorized auditor on a pending submission → status='approved', decided_by/decided_at set, approved_qty += quantity (atomic, one transaction); editAllSiblingMessages + worker notified workerApproved.
    - Approve on an already-decided submission (second sequential tap OR concurrent tap) → answerCallbackQuery({text: auditAlreadyResolved}); approved_qty unchanged (increments exactly once across two taps).
    - Concurrent: two simultaneous approve handler invocations on one pending submission → exactly one commits, approved_qty increments once, the other takes the already-resolved path.
    - Reject by an authorized auditor → does NOT touch submissions; writes conversation_state (AWAITING_REJECT_REASON, data={submissionId, auditorPersonId, workerPersonId}); replies with buildRejectReasonKeyboard + auditRejectPrompt (D-31: submission stays pending_audit until a reason arrives).
  </behavior>
  <action>
    In `src/lib/bot-audit.ts`, add `export async function handleAuditDecision(ctx, action: 'approve'|'reject', submissionId: string, db): Promise<void>` with lazy imports.

    Step 0 — answerCallbackQuery first is already done by the generic handler; still ensure each terminal path issues a toast where required (Pattern 3). Step 1 — AUTHORIZATION (D-36): load the submission's projectId + personId (`Kayıt bulunamadı` toast + return if missing); resolve the tapping auditor's person row by `people.telegramUserId = BigInt(ctx.from.id)` (auditUnauthorized toast + return if missing); query `assignments WHERE personId = auditor.id AND projectId = submission.projectId AND roleOnProject = 'auditor'` — if empty, `answerCallbackQuery({text: MESSAGES.auditUnauthorized, show_alert:true})` and return with NO DB change. Never trust callback_data for authorization.

    Step 2a — action === 'approve': use `getTxDb()` (neon-serverless Pool; the default @/db throws on .transaction()). In the transaction run the UPDATE-RETURNING guard `WHERE id=submissionId AND status='pending_audit'` setting status='approved', decidedBy=auditor.id, decidedAt=now, returning {id, quantity, boqItemId}; if returning is empty throw `AlreadyResolvedError`; else `tx.update(boqItems).set({ approvedQty: sql\`approved_qty + ${affected[0].quantity}\` }).where(eq(boqItems.id, affected[0].boqItemId))` (D-27 increment-only). Wrap in try/catch: on AlreadyResolvedError → `answerCallbackQuery({text: MESSAGES.auditAlreadyResolved})` + return; on other error → log + `answerCallbackQuery({text: MESSAGES.genericError, show_alert:true})` + return. After commit: call `editAllSiblingMessages(submissionId, MESSAGES.auditApprovedOutcome(auditorDisplayName))` and notify the worker via `bot.api.sendMessage(Number(workerTelegramUserId), MESSAGES.workerApproved)` (resolve worker chat id from submission.personId → people.telegramUserId) (D-37).

    Step 2b — action === 'reject' (D-30/D-31): do NOT modify submissions. Call `saveState(db, BigInt(ctx.from.id), STEPS.AWAITING_REJECT_REASON, { submissionId, auditorPersonId: auditor.id, workerPersonId: submission.personId }, auditor.id)` then `ctx.reply(MESSAGES.auditRejectPrompt, { reply_markup: buildRejectReasonKeyboard() })`. The submission stays pending_audit and remains actionable by other auditors until a reason is confirmed.
  </action>
  <verify>
    <automated>npx vitest run tests/telegram-audit.test.ts -t "AUDIT-03|AUDIT-04|AUDIT-06" 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - AUDIT-03 unit test green: non-assigned tap → answerCallbackQuery called with the unauthorized toast, zero DB writes.
    - AUDIT-04 (describeIfDb) green: approve sets status='approved' + decided_by/decided_at and approved_qty increases by exactly the submitted quantity.
    - AUDIT-04 SC3 (describeIfDb, MANDATORY) green: applying the same approve twice increments approved_qty exactly once; second tap returns auditAlreadyResolved.
    - AUDIT-06 SC5 (describeIfDb, MANDATORY) green: two concurrent approve invocations → exactly one commit, approved_qty increments once, other gets auditAlreadyResolved.
    - The approve path uses `getTxDb()` and UPDATE-RETURNING with `status='pending_audit'` guard: `grep -c "pending_audit'" src/lib/bot-audit.ts` ≥1 and `grep -c "approved_qty +" src/lib/bot-audit.ts` ≥1.
    - Reject path performs NO submissions UPDATE before a reason — only saveState(AWAITING_REJECT_REASON) + reply: `grep -c "AWAITING_REJECT_REASON" src/lib/bot-audit.ts` ≥1; no `status: 'rejected'` set inside handleAuditDecision's reject branch.
    - answerCallbackQuery is invoked on the unauthorized, already-resolved, and error paths.
  </acceptance_criteria>
  <done>Authorization, atomic first-wins approve with single-increment, and reject-reason entry implemented; both MANDATORY race/atomic tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: handleAuditReasonSelect + handleAuditRejectFreeText + commitRejection</name>
  <files>src/lib/bot-audit.ts</files>
  <read_first>
    - src/lib/bot-audit.ts (Task 1 — handleAuditDecision, editAllSiblingMessages, AlreadyResolvedError, getTxDb usage)
    - src/lib/telegram.ts saveState (158-195) + conversation_state load/delete pattern in handleConfirmSubmit (1256-1316)
    - 03-PATTERNS.md §"src/lib/bot-audit.ts" (decision transaction reused for rejection) + §Shared Patterns (saveState, error handling)
    - 03-RESEARCH.md Pattern 7 (reject-reason FSM, free-text path), Pitfall 3 (reason mandatory before commit), §Security Domain V5 (length-cap free text)
  </read_first>
  <behavior>
    - Canned reason tap (`audit:reason:<reason>`) → commit rejection with that reason: status='rejected', rejection_reason set, decided_by/decided_at set, atomically; clear the auditor's conversation_state; editAllSiblingMessages + worker notified workerRejected(reason).
    - Free-text tap (`audit:reason:free`) → update FSM to AWAITING_REJECT_REASON (or _FREE) and reply auditRejectFreeTextPrompt; do NOT commit yet.
    - Free-text message arrives (AWAITING_REJECT_REASON case) → use the typed text (length-capped) as the reason and commit exactly as the canned path; clear conversation_state.
    - Committing a rejection on an already-decided submission → already-resolved path, no double-write.
    - The auditor's submissionId/workerPersonId come from the conversation_state data written in Task 1 (never from callback_data, which only carries the reason).
  </behavior>
  <action>
    In `src/lib/bot-audit.ts` add:

    `export async function commitRejection(ctx, submissionId, auditorPersonId, reason, db)`: in a `getTxDb()` transaction run UPDATE-RETURNING `WHERE id=submissionId AND status='pending_audit'` setting status='rejected', decidedBy=auditorPersonId, decidedAt=now, rejectionReason=reason, returning {id, personId}; empty returning → throw AlreadyResolvedError (handled with auditAlreadyResolved toast). After commit: delete the auditor's conversation_state row (by their telegram_user_id), `editAllSiblingMessages(submissionId, MESSAGES.auditRejectedOutcome(auditorDisplayName, reason))`, and notify the worker `bot.api.sendMessage(Number(workerTelegramUserId), MESSAGES.workerRejected(reason))` (D-37). This is the single rejection commit point (Pitfall 3: only fires AFTER a reason exists).

    `export async function handleAuditReasonSelect(ctx, reasonOrFree: string, db)`: read the auditor's conversation_state by `BigInt(ctx.from.id)`; if absent/stale → auditAlreadyResolved or noActiveFlow toast + return. If `reasonOrFree === 'free'`: `saveState(...AWAITING_REJECT_REASON...)` keeping the same data, then `ctx.reply(MESSAGES.auditRejectFreeTextPrompt)`; return. Else treat reasonOrFree as the canned reason and call `commitRejection(ctx, data.submissionId, data.auditorPersonId, reasonOrFree, db)`.

    `export async function handleAuditRejectFreeText(ctx, stateData, db)`: take `ctx.message.text`, trim, cap at 500 chars (V5 input validation, same as worker notes); if empty/whitespace → reprompt auditRejectFreeTextPrompt and keep state (mandatory reason, D-31); else `commitRejection(ctx, stateData.submissionId, stateData.auditorPersonId, cappedText, db)`.
  </action>
  <verify>
    <automated>npx vitest run tests/telegram-audit.test.ts -t "AUDIT-05" 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - AUDIT-05 (describeIfDb) green: canned-reason reject sets status='rejected' + rejection_reason and the worker is notified with the reason (workerRejected).
    - AUDIT-05 free-text (unit) green: tapping Başka (`audit:reason:free`) sets conversation_state to AWAITING_REJECT_REASON and replies the free-text prompt without committing.
    - AUDIT-05 abandon (describeIfDb) green: tapping ❌ then giving no reason leaves submission status='pending_audit' (D-31) — `commitRejection` is never reached without a reason.
    - There is exactly ONE rejection commit point (`commitRejection`): `grep -c "status: 'rejected'\|status:'rejected'" src/lib/bot-audit.ts` returns 1 (only inside commitRejection).
    - Free-text reason is length-capped (≤500 chars) before commit (V5).
    - submissionId + auditorPersonId for the commit are read from conversation_state data, not callback_data.
  </acceptance_criteria>
  <done>Two-tier mandatory reject reason (canned + free-text FSM) committed atomically with worker notify; abandon leaves submission pending_audit; single commit point.</done>
</task>

<task type="auto">
  <name>Task 3: Wire audit: dispatch branches + AWAITING_REJECT_REASON message case into telegram.ts</name>
  <files>src/lib/telegram.ts</files>
  <read_first>
    - src/lib/telegram.ts dispatchCallbackQuery (462-558) — add audit branches BEFORE the conversation_state load at line 475; bot.on('message') switch (595-619) — add the AWAITING_REJECT_REASON case
    - src/lib/bot-audit.ts (Tasks 1-2 — handleAuditDecision, handleAuditReasonSelect, handleAuditRejectFreeText)
    - 03-PATTERNS.md §"src/lib/telegram.ts" (dispatch extension + message dispatcher extension + callback parsing) — branches added BEFORE the fallthrough repromptStep
    - 03-RESEARCH.md Pattern 2 (callback handler registration — Option A extend existing dispatcher), Pattern 6 (callback_data parse)
  </read_first>
  <action>
    In `src/lib/telegram.ts` `dispatchCallbackQuery`, add at the VERY TOP of the function (before the conversation_state select at line 475, because auditors have no worker FSM row) lazy-imported branches:
    - `if (data.startsWith('audit:approve:') || data.startsWith('audit:reject:'))` → parse `submissionId = data.split(':')[2]`, call `handleAuditDecision(ctx, data.startsWith('audit:approve:') ? 'approve' : 'reject', submissionId, db)`, return.
    - `if (data.startsWith('audit:reason:'))` → parse `reason = data.slice('audit:reason:'.length)` (may equal the `free` sentinel and may contain `/`), call `handleAuditReasonSelect(ctx, reason, db)`, return.
    These must precede the existing `noActiveFlow` bail and the worker `project:/boq:/edit:/confirm:/flow:` branches; they must NOT run repromptStep.

    In `src/lib/telegram.ts` `bot.on('message')` switch (595-619), add `case STEPS.AWAITING_REJECT_REASON:` → `await handleAuditRejectFreeText(ctx, conversationData, db); break;` (the existing handler already loads state + enforces TTL via isStaleState, so the auditor free-text reason is routed here just like a worker step).

    Import the three audit handlers lazily from `'@/lib/bot-audit'` inside the relevant scopes (consistent with the file's lazy-import discipline). Do not alter the generic `answerCallbackQuery()` at line 316 (it correctly fires first for all callbacks).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | tail -10 && npx vitest run tests/telegram-audit.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `dispatchCallbackQuery` routes `audit:approve:`, `audit:reject:`, and `audit:reason:` BEFORE the conversation_state load: `grep -n "audit:approve:\|audit:reason:" src/lib/telegram.ts` shows the branches at a line number lower than the conversation_state select inside dispatchCallbackQuery.
    - `bot.on('message')` switch has a `case STEPS.AWAITING_REJECT_REASON:` calling handleAuditRejectFreeText: `grep -c "AWAITING_REJECT_REASON" src/lib/telegram.ts` ≥1.
    - End-to-end via `bot.handleUpdate(makeCallbackUpdate(...))`: an `audit:approve:<id>` callback from an assigned auditor drives the approve path; the AUDIT-06 replay (duplicate update_id) is de-duped by the existing processed_updates fence (no second decision) — AUDIT-06/T-3-DUP test green.
    - Full audit suite: `npx vitest run tests/telegram-audit.test.ts` is green (all AUDIT-* including both MANDATORY DB-gated race/atomic tests when TEST_DATABASE_URL is set).
    - `npx tsc --noEmit` exits 0 and `npx vitest run tests/telegram-bot.test.ts` (Phase 2 regression) stays green.
  </acceptance_criteria>
  <done>Audit callbacks and the free-text reason message are routed through the existing dispatchers without requiring a worker FSM row; full audit suite green; Phase 2 regression intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram client → callback handler | callback_data (from.id, submissionId, reason) is attacker-controllable |
| Concurrent auditor taps → submission row | two clients can race the same submission |
| Replayed update → webhook | a retried/replayed callback_query may re-enter the handler |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-AUTHZ | Elevation of Privilege | handleAuditDecision authorization | mitigate | (HIGH) Re-query `assignments role_on_project='auditor'` on the submission's project for the tapping telegram_user_id on EVERY tap (D-36). callback_data is never trusted for authorization. Non-assigned tap → unauthorized toast, zero DB writes. Verified by AUDIT-03 test. |
| T-3-RACE | Tampering | atomic approve / reject transaction | mitigate | (HIGH) Single getTxDb() transaction; UPDATE-RETURNING `WHERE status='pending_audit'` is the atomic first-wins guard. Postgres serializes concurrent same-row UPDATEs → second tap matches 0 rows → already-resolved. No double-increment. approved_qty is increment-only (D-27, no subtractive op). Verified by AUDIT-04 SC3 + AUDIT-06 SC5 (MANDATORY tests). |
| T-3-DUP | Tampering | replayed callback_query | mitigate | (MEDIUM/HIGH) The existing processed_updates idempotency fence (D-13 Guard 1) de-dupes a replayed update_id at the webhook middleware before the handler runs; the atomic guard is the backstop. Verified by AUDIT-06/T-3-DUP test. |
| T-3-CBDATA | Tampering | crafted submissionId / reason in callback_data | mitigate | (HIGH) submissionId is validated by DB lookup (missing → `Kayıt bulunamadı`) and re-authorized; reject reason/auditor identity for the commit come from conversation_state, not callback_data. A forged submissionId returns 0 rows + fails authorization. |
| T-3-REJ-NOREASON | Tampering/Integrity | rejection without reason | mitigate | D-31/Pitfall 3: rejection committed ONLY in `commitRejection`, which always carries a reason; tapping ❌ writes conversation_state only and leaves status='pending_audit'. Verified by AUDIT-05 abandon test. |
| T-3-INPUT | Input Validation | free-text reject reason | mitigate | (V5) Free-text reason trimmed + capped at 500 chars; stored via Drizzle parameterized insert (no raw SQL interpolation of user text). |
| T-3-CBQ | Denial of Service | unanswered callback spinner → Telegram retry | mitigate | answerCallbackQuery is called on every code path (success/unauthorized/already-resolved/error) — generic first-call at telegram.ts:316 + explicit toasts in handlers (Pattern 3 / Pitfall 2). |
| T-3-SECRET | Spoofing | webhook secret-token | mitigate | route.ts webhookCallback secretToken is untouched (T-04-01 not regressed); confirmed by grep in Plan 04. |
| T-3-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (RESEARCH.md Package Legitimacy Audit: none). |
</threat_model>

<verification>
- `npx vitest run tests/telegram-audit.test.ts` — ALL AUDIT-* green, including the two MANDATORY DB-gated tests (AUDIT-04 SC3 atomic single-increment, AUDIT-06 SC5 concurrent first-wins) when TEST_DATABASE_URL is set.
- `npx vitest run` full suite — Phase 1/2 regression + Phase 3 all green.
- `npx tsc --noEmit` exits 0.
- All HIGH threats (T-3-AUTHZ, T-3-RACE, T-3-CBDATA) have passing tests proving the mitigation.
</verification>

<success_criteria>
- AUDIT-03: only assigned auditors can decide; non-assigned tap = no DB change + rejection toast.
- AUDIT-04: approve atomically sets approved + increments approved_qty by the submitted qty; no double-deduction on duplicate/concurrent callbacks.
- AUDIT-05: reject prompts a MANDATORY reason (canned + free-text FSM), sets rejected + rejection_reason, notifies the worker; abandon leaves pending_audit.
- AUDIT-06: first action wins; later/concurrent action gets "Bu kayıt zaten çözüldü"; replay de-duped.
- D-37 worker notified on both outcomes; D-34 all sibling messages edited to the resolved view on first decision.
</success_criteria>

<output>
Create `.planning/phases/03-audit-loop/03-05-SUMMARY.md` when done.
</output>
