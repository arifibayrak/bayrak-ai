---
phase: 03-audit-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema/submissions.ts
  - src/db/schema/audit-notifications.ts
  - src/db/schema/index.ts
  - src/lib/bot-fsm.ts
  - tests/fixtures/db.ts
  - tests/telegram-audit.test.ts
autonomous: true
requirements: [AUDIT-04, AUDIT-05, AUDIT-06]
must_haves:
  truths:
    - "submissions table carries decided_by, decided_at, rejection_reason columns (audit trail)"
    - "audit_notifications table exists keyed to submission with chat_id + message_id + send_failed"
    - "FSM exposes awaiting_reject_reason and awaiting_reject_reason_free steps"
    - "All five Wave 0 audit test files exist and run (red, gated by describeIfDb where DB-bound)"
  artifacts:
    - path: "src/db/schema/audit-notifications.ts"
      provides: "audit_notifications Drizzle table (D-34 fan-out message refs)"
      contains: "auditNotifications"
    - path: "src/db/schema/submissions.ts"
      provides: "decided_by / decided_at / rejection_reason columns (D-38)"
      contains: "rejection_reason"
    - path: "src/lib/bot-fsm.ts"
      provides: "AWAITING_REJECT_REASON + AWAITING_REJECT_REASON_FREE step constants (D-32)"
      contains: "awaiting_reject_reason"
    - path: "tests/telegram-audit.test.ts"
      provides: "Wave 0 audit test scaffold for all AUDIT-* behaviors"
      contains: "audit:approve"
  key_links:
    - from: "src/db/schema/index.ts"
      to: "src/db/schema/audit-notifications.ts"
      via: "barrel export"
      pattern: "audit-notifications"
    - from: "src/db/schema/audit-notifications.ts"
      to: "src/db/schema/submissions.ts"
      via: "submissionId FK references submissions.id onDelete cascade"
      pattern: "submissions.id"
---

<objective>
Lay the Phase 3 data + state foundation: extend `submissions` with the audit decision trail (D-38), create the `audit_notifications` table for fan-out message refs (D-34), add the two auditor reject-reason FSM step constants (D-32), register the new table in the barrel + test truncation order, and scaffold the Wave 0 audit test file so every AUDIT-* behavior has a red test target before any handler is written.

Purpose: Every downstream plan (migration, fan-out, decision handlers) depends on these schema shapes and the FSM steps. Writing the test scaffold here satisfies the Nyquist contract (03-VALIDATION.md) — no handler is implemented without a pre-existing failing test.
Output: Two modified schema files, one new schema file, one barrel edit, one FSM edit, one fixtures edit, one new test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-audit-loop/03-CONTEXT.md
@.planning/phases/03-audit-loop/03-RESEARCH.md
@.planning/phases/03-audit-loop/03-PATTERNS.md
@.planning/phases/03-audit-loop/03-VALIDATION.md

<interfaces>
<!-- Extracted from codebase — executor uses these directly, no exploration needed. -->

From src/db/schema/submissions.ts (current — status enum already includes approved/rejected):
- pgTable 'submissions', PK uuid id, tenantId (nullable, references tenants.id), flowId, personId→people.id, projectId→projects.id, boqItemId→boqItems.id, photoUrl, photoFileId, location geometry, locationLat/Lon numeric, quantity numeric notNull, notes text, status text enum ['pending_audit','approved','rejected'] default 'pending_audit', submittedAt timestamptz.
- `people` is ALREADY imported at top of this file. No new import needed for decidedBy FK.
- `text` and `timestamp` and `uuid` are already imported from 'drizzle-orm/pg-core'.

From src/db/schema/conversation-state.ts (analog for new table — bigint Telegram id + uuid FK + nullable tenantId + index):
- bigint('telegram_user_id', { mode: 'bigint' }), uuid FK pattern, tenantId nullable references tenants.id, index() in the (t)=>[] array.

From src/db/schema/boq-items.ts (cascade FK analog):
- `.references(() => projects.id, { onDelete: 'cascade' })` is the cascade pattern.

From src/db/schema/index.ts (current barrel — last line is the submissions export):
```
export * from './conversation-state';  // references tenants, people
export * from './processed-updates';   // no FK references
export * from './submissions';         // references tenants, people, projects, boq-items
```

From src/lib/bot-fsm.ts (current STEPS const, lines 23-31):
```
export const STEPS = {
  PROJECT: 'project', BOQ: 'boq', PHOTO: 'photo', LOCATION: 'location',
  QUANTITY: 'quantity', NOTES: 'notes', CONFIRM: 'confirm',
} as const;
```

From tests/fixtures/db.ts (truncateAllTables tables array, most-dependent-first; line 54+):
First entry is currently 'submissions'. New table references submissions, so it must appear BEFORE 'submissions'.

From tests/telegram-audit.test.ts test harness target (PATTERNS.md §tests/telegram-audit.test.ts):
- setupBotForTest(): spy bot.init, set bot.botInfo, intercept via bot.api.config.use(transformer).
- makeCallbackUpdate(userId, data, updateId?): builds a callback_query update with .data and .message.chat.id.
- describeIfDb / getTestDb / truncateAllTables from './fixtures/db'.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend submissions schema + create audit_notifications table + register in barrel</name>
  <files>src/db/schema/submissions.ts, src/db/schema/audit-notifications.ts, src/db/schema/index.ts</files>
  <read_first>
    - src/db/schema/submissions.ts (the file being modified; `people` already imported)
    - src/db/schema/conversation-state.ts (analog: bigint Telegram id, uuid FK, nullable tenantId, index array)
    - src/db/schema/boq-items.ts (analog: onDelete cascade FK)
    - src/db/schema/index.ts (barrel — add export after submissions line)
    - 03-PATTERNS.md §"src/db/schema/audit-notifications.ts (NEW table, D-34)" and §"src/db/schema/submissions.ts (extend)"
  </read_first>
  <action>
    In `src/db/schema/submissions.ts`, add three nullable columns inside the column map after `submittedAt` (D-38): `decidedBy: uuid('decided_by').references(() => people.id)`, `decidedAt: timestamp('decided_at', { withTimezone: true })`, `rejectionReason: text('rejection_reason')`. All nullable — no DEFAULT, no backfill. Do NOT add any CHECK constraint on approved_qty (D-28 explicitly forbids `CHECK (approved_qty <= planned_qty)`; over-delivery is allowed).

    Create `src/db/schema/audit-notifications.ts` (D-34) modeling conversation-state.ts: pgTable `'audit_notifications'` with `id` uuid PK defaultRandom; `tenantId` uuid nullable references tenants.id (D-09); `submissionId` uuid notNull references submissions.id with `{ onDelete: 'cascade' }`; `auditorPersonId` uuid notNull references people.id; `chatId` bigint mode 'bigint' notNull (matches people.telegram_user_id type); `messageId` integer notNull; `sendFailed` boolean notNull default false (D-40 records failed sends); `sentAt` timestamptz defaultNow notNull. Add two indexes in the `(t)=>[]` array: `audit_notifications_submission_idx` on submissionId and `audit_notifications_auditor_idx` on auditorPersonId. Import pgTable/uuid/bigint/integer/boolean/timestamp/index from 'drizzle-orm/pg-core' and tenants/submissions/people from the sibling schema files.

    In `src/db/schema/index.ts`, append `export * from './audit-notifications';` AFTER the `./submissions` export (it references submissions — must come after).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/schema/audit-notifications.ts` exports `auditNotifications` with a `submission_id` column FK to submissions.id `onDelete: 'cascade'`, a `bigint` `chat_id`, an `integer` `message_id`, and a `boolean` `send_failed` default false.
    - `grep -c "rejection_reason\|decided_by\|decided_at" src/db/schema/submissions.ts` returns 3 (the three new column literals).
    - `grep -c "audit-notifications" src/db/schema/index.ts` returns 1 and the export line is positioned after the submissions export.
    - No `CHECK` / `approved_qty <= planned_qty` text appears in submissions.ts or boq-items.ts: `grep -c "approved_qty <= planned_qty" src/db/schema/*.ts` returns 0.
    - `npx tsc --noEmit` exits 0 (schema typechecks; types derive from schema files, NOT the live DB — this passes BEFORE the migration, which is the false-positive trap Plan 02 closes).
  </acceptance_criteria>
  <done>Three audit-trail columns added to submissions; audit_notifications table created and exported in the barrel; typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: Add reject-reason FSM steps + audit_notifications to test truncation order</name>
  <files>src/lib/bot-fsm.ts, tests/fixtures/db.ts</files>
  <read_first>
    - src/lib/bot-fsm.ts (STEPS const at lines 23-31 — pure module, no DB imports)
    - tests/fixtures/db.ts (truncateAllTables tables array at line 54+ — most-dependent-first ordering)
    - 03-PATTERNS.md §"src/lib/bot-fsm.ts (extend)" and §"tests/fixtures/db.ts (extend)"
  </read_first>
  <action>
    In `src/lib/bot-fsm.ts`, add two values to the `STEPS` const object (D-32 auditor reject-reason FSM): `AWAITING_REJECT_REASON: 'awaiting_reject_reason'` and `AWAITING_REJECT_REASON_FREE: 'awaiting_reject_reason_free'`. Keep the existing worker steps unchanged. Do NOT modify `CONVERSATION_TTL_MS` or `isStaleState` — the auditor FSM reuses the D-22 TTL identically. The `Step` type derives automatically from `keyof typeof STEPS`. Do not add the auditor data fields to the `ConversationData` interface (that interface describes worker-flow data); the auditor state `data` shape `{ submissionId, auditorPersonId, workerPersonId }` is documented in a code comment only.

    In `tests/fixtures/db.ts`, add `'audit_notifications'` as the FIRST entry of the `tables` array in `truncateAllTables` (before `'submissions'`) — it references submissions, so it must be truncated first in the most-dependent-first order.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | tail -10 && grep -c "awaiting_reject_reason" src/lib/bot-fsm.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "awaiting_reject_reason'" src/lib/bot-fsm.ts` returns 2 (both new step literal values).
    - `STEPS.AWAITING_REJECT_REASON` and `STEPS.AWAITING_REJECT_REASON_FREE` are usable as typed members (typecheck green).
    - `audit_notifications` appears in tests/fixtures/db.ts truncation list positioned before `submissions`: `grep -n "audit_notifications\|\"submissions\"" tests/fixtures/db.ts` shows audit_notifications on an earlier line than submissions.
    - `CONVERSATION_TTL_MS` and `isStaleState` are unchanged.
  </acceptance_criteria>
  <done>Two auditor FSM steps added; truncation list includes audit_notifications before submissions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wave 0 — scaffold tests/telegram-audit.test.ts covering all AUDIT-* behaviors (red)</name>
  <files>tests/telegram-audit.test.ts</files>
  <read_first>
    - tests/telegram-bot.test.ts (THE harness analog — setupBotForTest lines ~64-93, makeTextUpdate ~98-109, beforeEach/afterEach ~245-279, buildDbMock ~707-729, api transformer ~544-549)
    - tests/fixtures/db.ts (describeIfDb / getTestDb / truncateAllTables — DB-gated suites)
    - 03-PATTERNS.md §"tests/telegram-audit.test.ts (NEW test file)" (full harness patterns + makeCallbackUpdate + buildAuditDbMock)
    - 03-VALIDATION.md (Per-Task Verification Map — the MANDATORY race + atomic-increment rows)
    - 03-RESEARCH.md §"Validation Architecture" (Phase Requirements → Test Map table)
  </read_first>
  <behavior>
    - AUDIT-02 (unit, pure): a `buildAuditKeyboard(submissionId)` callback_data is `audit:approve:<id>` / `audit:reject:<id>`, both ≤64 bytes. (Implementation lands in Plan 03; test asserts the contract.)
    - AUDIT-03 (unit, mock DB): a callback from a telegram_user_id with NO auditor assignment on the submission's project triggers `answerCallbackQuery` and performs NO submissions/boq_items UPDATE.
    - AUDIT-04 (describeIfDb, live DB): approve sets submissions.status='approved', decided_by/decided_at set, and boq_items.approved_qty increases by exactly the submitted quantity.
    - AUDIT-04 SC3 / T-3-RACE (describeIfDb, live DB) MANDATORY: the SAME decision applied twice (sequential re-tap) increments approved_qty exactly once; second tap returns "already resolved".
    - AUDIT-06 SC5 / T-3-RACE (describeIfDb, live DB) MANDATORY: two concurrent approve taps (Promise.all on two handler invocations against one pending submission) → exactly one succeeds, approved_qty increments once, the other gets the "Bu kayıt zaten çözüldü" path.
    - AUDIT-06 / T-3-DUP (describeIfDb or mock): a replayed callback_query with a duplicate update_id is de-duped by the processed_updates fence (no second decision).
    - AUDIT-05 (describeIfDb, live DB): reject WITH a canned reason sets status='rejected' + rejection_reason; reject WITHOUT a reason (abandon after ❌) leaves status='pending_audit' (D-31).
  </behavior>
  <action>
    Create `tests/telegram-audit.test.ts`. Copy `setupBotForTest()` and the beforeEach/afterEach env-setup pattern from tests/telegram-bot.test.ts EXACTLY (spy `bot.init`, set `bot.botInfo`, intercept outbound calls via `bot.api.config.use(transformer)` — `vi.spyOn(api.sendMessage)` does NOT work on the grammY Proxy). Add a local `makeCallbackUpdate(userId, data, updateId?)` helper per PATTERNS.md (callback_query with `.data`, `.from.id`, `.message.chat.id`). Add a `buildAuditDbMock(opts)` helper for the mock-DB unit cases.

    Write one test per behavior above. The DB-bound behaviors (AUDIT-04, AUDIT-04 SC3, AUDIT-06 SC5, AUDIT-05) MUST be wrapped in `describeIfDb(...)` so they skip cleanly without TEST_DATABASE_URL but run red against a live DB. Each test references the handlers/keyboards that DO NOT EXIST YET (`buildAuditKeyboard` from '@/lib/bot-keyboards', `handleAuditDecision`/`fanOutToAuditors` from '@/lib/bot-audit'); the tests are expected to FAIL or error on missing exports — that is the Wave 0 red state. Use `bot.handleUpdate(makeCallbackUpdate(...))` to drive callback dispatch where the handler is wired in later plans. Use `vitest run` (NEVER watch mode). Name the race test describe blocks clearly ("AUDIT-06 SC5 — double-tap race" / "AUDIT-04 SC3 — duplicate decision").

    Map every test to the 03-VALIDATION.md Per-Task Verification Map row (race + atomic-increment are the two MANDATORY rows). Do NOT implement any handler in this task — this file is the failing target only.
  </action>
  <verify>
    <automated>npx vitest run tests/telegram-audit.test.ts 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/telegram-audit.test.ts` exists and is picked up by vitest (appears in the run output).
    - `grep -c "describeIfDb" tests/telegram-audit.test.ts` returns ≥1 (DB-bound behaviors are gated).
    - `grep -c "makeCallbackUpdate\|audit:approve\|audit:reject" tests/telegram-audit.test.ts` returns ≥3.
    - The run output contains test names for: AUDIT-02 callback_data ≤64 bytes, AUDIT-03 unauthorized no-op, AUDIT-04 approve increments approved_qty, AUDIT-04 SC3 duplicate-once, AUDIT-06 SC5 concurrent-race, AUDIT-05 reject-with/without-reason.
    - Without TEST_DATABASE_URL: DB suites SKIP (not error); unit suites RUN and are RED (referencing not-yet-existing exports). The command exits non-zero (red is expected at Wave 0) OR shows skipped+failing — it MUST NOT show all-green (an all-green Wave 0 means the tests assert nothing).
    - No `vitest` watch flag is used (only `vitest run`).
  </acceptance_criteria>
  <done>Audit test scaffold exists; all AUDIT-* behaviors have named red/skipped targets; the two MANDATORY race/atomic tests are present and DB-gated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram client → webhook | Attacker-controllable update payloads (callback_data, from.id) cross here |
| App code → Postgres | Schema shapes here define the atomic-decision and audit-trail surface used downstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-SCHEMA-01 | Tampering | submissions over-deduction column design | mitigate | No subtractive column; approved_qty is increment-only (D-27). decided_by/decided_at/rejection_reason are nullable audit-trail fields written only inside the atomic decision transaction (Plan 05). No CHECK constraint per D-28 (over-delivery is a warning, not a DB error). |
| T-3-SCHEMA-02 | Repudiation | who-decided audit trail | mitigate | `decided_by` (FK people.id) + `decided_at` (timestamptz) + `rejection_reason` columns added so every decision is attributable (feeds DASH-03). |
| T-3-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed this plan (RESEARCH.md Package Legitimacy Audit: none). |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (schema + FSM typecheck without the live DB — the false-positive trap; Plan 02 closes it with the real migration).
- `npx vitest run tests/telegram-audit.test.ts` shows the scaffold red/skipped (NOT all-green).
- `npx vitest run` full suite: Phase 1/2 regression stays green; new audit file red/skipped as expected at Wave 0.
</verification>

<success_criteria>
- submissions has decided_by, decided_at, rejection_reason (D-38).
- audit_notifications table exists with submission FK cascade, bigint chat_id, integer message_id, boolean send_failed (D-34/D-40), registered in barrel.
- bot-fsm STEPS includes AWAITING_REJECT_REASON + AWAITING_REJECT_REASON_FREE (D-32).
- tests/fixtures/db.ts truncates audit_notifications before submissions.
- tests/telegram-audit.test.ts scaffolds all AUDIT-* behaviors including the two MANDATORY race/atomic-increment tests, DB-gated via describeIfDb.
</success_criteria>

<output>
Create `.planning/phases/03-audit-loop/03-01-SUMMARY.md` when done.
</output>
