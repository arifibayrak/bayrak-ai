---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - components.json
  - drizzle.config.ts
  - vitest.config.ts
  - vercel.json
  - .env.example
  - .gitignore
  - src/app/layout.tsx
  - src/app/globals.css
  - src/db/index.ts
  - tests/setup.ts
  - tests/fixtures/geojson.ts
  - tests/fixtures/boq.ts
  - tests/fixtures/db.ts
autonomous: true
requirements: [SETUP-01, SETUP-02, SETUP-03, SETUP-04, AUTH-01, AUTH-02, AUTH-03, AUTH-04, I18N-02]
user_setup:
  - service: neon
    why: "Primary Postgres + PostGIS database; all migrations and DB tests run against it"
    env_vars:
      - name: DATABASE_URL
        source: "Neon Dashboard -> Project -> Connection Details (pooled connection string)"
      - name: TEST_DATABASE_URL
        source: "Neon Dashboard -> create a branch 'test' -> Connection Details (DB integration tests run here)"
  - service: resend
    why: "Magic-link email transport for Auth.js (wired in plan 01-03)"
    env_vars:
      - name: AUTH_RESEND_KEY
        source: "Resend Dashboard -> API Keys -> Create API Key"
    dashboard_config:
      - task: "Verify the bayrak.ai sending domain (or use onboarding@resend.dev for dev)"
        location: "Resend Dashboard -> Domains"
  - service: telegram
    why: "Bot token for the /start webhook (wired in plan 01-04)"
    env_vars:
      - name: TELEGRAM_BOT_TOKEN
        source: "Telegram @BotFather -> /newbot -> copy token"
      - name: TELEGRAM_WEBHOOK_SECRET
        source: "Generate locally: openssl rand -hex 32"

must_haves:
  truths:
    - "A Next.js 15 App Router project builds with `npm run build` and serves at localhost:3000"
    - "shadcn/ui is initialized (components.json present, neutral base color) and all Phase 1 components are installed"
    - "`npx vitest run` executes and reports (zero or more) tests without a config error"
    - "The Drizzle client and drizzle-kit config resolve DATABASE_URL without throwing"
    - "next.config.ts wraps the config with the next-intl plugin and declares grammy + pg as serverExternalPackages"
  artifacts:
    - path: "package.json"
      provides: "All Phase 1 dependencies at the versions pinned in RESEARCH.md Standard Stack"
      contains: "next-auth"
    - path: "vitest.config.ts"
      provides: "Vitest test runner config (ESM + TS + path alias)"
    - path: "next.config.ts"
      provides: "next-intl plugin wrapper + serverExternalPackages [grammy, pg]"
      contains: "serverExternalPackages"
    - path: "drizzle.config.ts"
      provides: "drizzle-kit schema discovery + migrations output dir"
      contains: "src/db/schema"
    - path: "src/db/index.ts"
      provides: "Drizzle client bound to Neon"
    - path: "tests/fixtures/db.ts"
      provides: "DATABASE_URL-gated test DB helper + describeIfDb guard"
  key_links:
    - from: "next.config.ts"
      to: "src/i18n/request.ts"
      via: "createNextIntlPlugin path argument"
      pattern: "createNextIntlPlugin"
    - from: "vitest.config.ts"
      to: "tests/setup.ts"
      via: "setupFiles"
      pattern: "setupFiles"
---

<objective>
Scaffold the greenfield bayrak.ai Next.js 15 App Router project, install and pin every Phase 1 dependency, initialize shadcn/ui (neutral), wire the build-level integration points (next-intl plugin, grammY external packages, Drizzle config), and stand up the Vitest test harness with shared fixtures (Wave 0 of the validation strategy).

Purpose: Every other Phase 1 plan depends on this project existing, the dependencies being installed at the verified versions, and the test harness being runnable. This is the foundation-of-the-foundation; nothing else can run in parallel with it.
Output: A buildable Next.js project with all deps, shadcn components, a resolving Drizzle client, a runnable Vitest harness, fixtures, and an `.env.example` documenting every Phase 1 secret.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/phases/01-foundation/01-UI-SPEC.md

Greenfield note: the working directory `/Users/arifismailbayrak/bayrak-ai` already contains `.planning/` and `CLAUDE.md`. `create-next-app` must scaffold INTO this existing directory (do not create a nested `bayrak-ai/bayrak-ai`). Use the current-directory form (`.`) and keep the existing `.planning/`, `CLAUDE.md`, and `.git` if present.

File-count note: this plan touches 14 files, above the usual per-plan target. This is acceptable and intentional - the bulk are greenfield-scaffold config files (package.json, tsconfig.json, next.config.ts, components.json, drizzle.config.ts, vitest.config.ts, vercel.json, .env.example, .gitignore, globals.css) plus the harness/fixture files that every downstream plan consumes. None carry meaningful per-file implementation complexity; the build verify proves them as a set.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 15 + install all Phase 1 dependencies + init shadcn/ui</name>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md § "Standard Stack" (exact versions) and § "Installation commands"
    - .planning/phases/01-foundation/01-UI-SPEC.md § "Design System" and § "Component Inventory (Phase 1)" (shadcn init flags + component list)
    - CLAUDE.md § Constraints (Next.js 15 App Router, Tailwind 4, Drizzle, single Vercel deploy)
  </read_first>
  <files>package.json, tsconfig.json, src/app/layout.tsx, src/app/globals.css, components.json, .gitignore</files>
  <action>
    Scaffold NON-INTERACTIVELY into the CURRENT directory with `npx --yes create-next-app@latest . --yes --typescript --tailwind --app --src-dir --import-alias "@/*"` (the `--yes` flag accepts all defaults and the in-place overwrite prompt without interaction; the trailing `.` is the current-dir form). Pin Next.js to the 15.x line per RESEARCH (not 16.x) - if `create-next-app@latest` resolves to 16.x, instead run it from a temp dir and copy the generated files in, then `npm install next@15` to pin. CRITICAL: do NOT create a nested `bayrak-ai/bayrak-ai`; do NOT delete or overwrite the existing `.planning/`, `CLAUDE.md`, or `.git` - if create-next-app refuses the non-empty dir even with `--yes`, scaffold into a temp dir and copy the app files (src/, config files) over the existing tree, preserving planning artifacts.
    Install runtime deps at the RESEARCH-verified versions: drizzle-orm@0.45.x, @neondatabase/serverless@1.1.x, pg, next-auth@beta (5.0.0-beta.31), @auth/drizzle-adapter@1.11.x, resend@6.12.x, grammy@1.43.x, next-intl@4.12.x, zod@4.x, exceljs@4.4.x, react-hook-form@7.76.x, @hookform/resolvers, wkx@0.5.x, @vercel/blob@2.4.x.
    Install dev deps: drizzle-kit@0.31.x, @types/pg, vitest, @vitest/coverage-v8.
    Initialize shadcn/ui non-interactively: `npx shadcn@latest init --defaults` (style=default, base color=neutral, CSS variables=yes), then add every Phase 1 component listed in UI-SPEC Component Inventory: button input label table card badge dialog tabs form separator dropdown-menu select alert progress skeleton sonner.
    Do NOT add packages beyond the RESEARCH Standard Stack list (no mapbox, no ai SDK, no @grammyjs/conversations — those are later phases).
  </action>
  <verify>
    <automated>test -d .planning && test -f CLAUDE.md && test -d .git && npx next build 2>&1 | tail -20 && test -f components.json && node -e "const p=require('./package.json'); ['next-auth','drizzle-orm','grammy','next-intl','exceljs','zod'].forEach(d=>{if(!p.dependencies[d])throw new Error('missing '+d)})"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` lists all runtime + dev deps named in the action at the pinned major/minor lines.
    - `components.json` exists with base color neutral; `src/components/ui/` contains button, input, table, card, dialog, tabs, form, select, alert, progress, skeleton, sonner, badge, label, separator, dropdown-menu, skeleton.
    - `npx next build` completes without error.
    - No mapbox-gl, react-map-gl, ai, @grammyjs/conversations, or @grammyjs/storage-psql in package.json (deferred phases).
    - Planning artifacts survived scaffolding: `.planning/`, `CLAUDE.md`, and `.git` still present (in-place scaffold did not clobber them).
  </acceptance_criteria>
  <done>Project builds; all Phase 1 deps installed at verified versions; shadcn neutral initialized with the full Phase 1 component set.</done>
</task>

<task type="auto">
  <name>Task 2: Wire build-level integration points + Drizzle client + drizzle-kit config + env layout</name>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md § "next-intl 4.x Setup" (createNextIntlPlugin wrapping), § "Minimal grammY /start Webhook" (serverExternalPackages + vercel.json maxDuration), § "Schema" (Drizzle client / migrate.ts), and § "Required env vars" blocks across Auth/Telegram/Database
    - .planning/phases/01-foundation/01-RESEARCH.md § "Recommended Project Structure" (file layout)
  </read_first>
  <files>next.config.ts, drizzle.config.ts, vercel.json, src/db/index.ts, .env.example</files>
  <action>
    next.config.ts: set `serverExternalPackages: ['grammy', 'pg']`, then wrap the exported config with `createNextIntlPlugin('./src/i18n/request.ts')` from next-intl/plugin. (The `src/i18n/request.ts` file itself is created in plan 01-03; the plugin path is declared here so it is ready.)
    drizzle.config.ts: dialect 'postgresql', schema glob `./src/db/schema/*.ts`, out `./src/db/migrations`, dbCredentials from `DATABASE_URL`.
    vercel.json: declare the telegram webhook function override `{ "functions": { "src/app/api/telegram/webhook/route.ts": { "memory": 512, "maxDuration": 55 } } }` (the route file is created in plan 01-04; the override is harmless until then).
    src/db/index.ts: create the Drizzle client using the Neon serverless driver (`drizzle(neon(process.env.DATABASE_URL!))`); export `db`. This is the canonical import `@/db` used by all later plans.
    .env.example: document every Phase 1 env var with placeholder values and a one-line comment for each — DATABASE_URL, TEST_DATABASE_URL, AUTH_SECRET, AUTH_RESEND_KEY, AUTH_URL, AUTH_ALLOWED_EMAILS, TELEGRAM_BOT_TOKEN, TELEGRAM_WEBHOOK_SECRET, BAYRAK_TENANT_ID (fixed UUID 00000000-0000-0000-0000-000000000001). Ensure `.env.local` is gitignored. NEVER commit real secret values.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');['next.config.ts','drizzle.config.ts','vercel.json','src/db/index.ts','.env.example'].forEach(f=>{if(!fs.existsSync(f))throw new Error('missing '+f)});const nc=fs.readFileSync('next.config.ts','utf8');if(!/createNextIntlPlugin/.test(nc))throw new Error('no next-intl plugin');if(!/serverExternalPackages/.test(nc))throw new Error('no serverExternalPackages');const env=fs.readFileSync('.env.example','utf8');['DATABASE_URL','AUTH_SECRET','AUTH_ALLOWED_EMAILS','TELEGRAM_BOT_TOKEN','TELEGRAM_WEBHOOK_SECRET','BAYRAK_TENANT_ID'].forEach(k=>{if(!env.includes(k))throw new Error('env missing '+k)})"</automated>
  </verify>
  <acceptance_criteria>
    - next.config.ts: `serverExternalPackages` includes 'grammy' and 'pg'; config is wrapped by `createNextIntlPlugin('./src/i18n/request.ts')`.
    - drizzle.config.ts references `./src/db/schema/*.ts` and `./src/db/migrations`.
    - src/db/index.ts exports a `db` client bound to `DATABASE_URL` via the Neon driver.
    - .env.example documents all 9 named env vars; `.gitignore` ignores `.env.local`; no real secret values are committed.
  </acceptance_criteria>
  <done>Build-level integration points wired; Drizzle client + config in place; env layout documented and secrets gitignored.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Vitest harness + shared fixtures (Validation Wave 0)</name>
  <read_first>
    - .planning/phases/01-foundation/01-VALIDATION.md § "Test Infrastructure", § "Wave 0 Requirements", § "Per-Task Verification Map"
    - .planning/phases/01-foundation/01-RESEARCH.md § "Validation Architecture" (Vitest rationale, test file list, DB-gated tests)
    - .planning/phases/01-foundation/01-RESEARCH.md § "GeoJSON Upload" (valid/invalid GeoJSON shapes) and § "ExcelJS BOQ Import" (BoqRow shape)
  </read_first>
  <behavior>
    - `vitest.config.ts` loads with ESM + TS + the `@/*` path alias; `npx vitest run` exits 0 even with no test files matched.
    - `tests/fixtures/db.ts` exports a `describeIfDb` helper that SKIPS DB integration tests when `TEST_DATABASE_URL` is unset (so unit tests stay runnable without a DB), and runs them when it is set.
    - `tests/fixtures/geojson.ts` exports a valid LineString Feature (Istanbul coords lng=28.9, lat=41.0 first), a FeatureCollection-with-Polygon (invalid), and a lat-first-out-of-range fixture (lat > 90 caught).
    - `tests/fixtures/boq.ts` exports a function generating a sample `.xlsx` Buffer (3 valid rows incl. a Turkish-decimal "123,5") and a buffer with missing-material/unit/qty rows.
  </behavior>
  <files>vitest.config.ts, tests/setup.ts, tests/fixtures/geojson.ts, tests/fixtures/boq.ts, tests/fixtures/db.ts</files>
  <action>
    Create `vitest.config.ts` with the vitejs react/node test environment, `test.setupFiles: ['./tests/setup.ts']`, globals true, and the `@/*` alias resolving to `./src`. Use `node` test environment for server/DB/lib tests.
    Create `tests/setup.ts` (load env, no watch mode).
    Create `tests/fixtures/db.ts`: export `hasTestDb` (boolean from `process.env.TEST_DATABASE_URL`), `describeIfDb` (= hasTestDb ? describe : describe.skip), and a helper to obtain a Drizzle test client + a truncate-all-tables reset between tests.
    Create `tests/fixtures/geojson.ts` and `tests/fixtures/boq.ts` with the fixtures described in <behavior>, using the ExcelJS template generator shape from RESEARCH (Malzeme/Birim/Sözleşme Miktarı columns).
    These fixtures are consumed by tests written in plans 01-02 through 01-06; this plan only creates the harness + fixtures (no feature tests yet).
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -15 && node -e "const fs=require('fs');['vitest.config.ts','tests/setup.ts','tests/fixtures/db.ts','tests/fixtures/geojson.ts','tests/fixtures/boq.ts'].forEach(f=>{if(!fs.existsSync(f))throw new Error('missing '+f)})"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0 (no config error) with no feature tests yet present.
    - `tests/fixtures/db.ts` exports `describeIfDb` that resolves to `describe.skip` when `TEST_DATABASE_URL` is unset.
    - `tests/fixtures/geojson.ts` exports a valid LineString Feature, an invalid Polygon FeatureCollection, and a lat-out-of-range fixture.
    - `tests/fixtures/boq.ts` exports a sample valid `.xlsx` Buffer (including a "123,5" Turkish-decimal cell) and an invalid-rows buffer.
  </acceptance_criteria>
  <done>Vitest runs cleanly; shared fixtures + DB-gated test helper exist for downstream plans to consume.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine → git repo | Secrets must never enter version control |
| npm registry → local install | Supply-chain risk on dependency install |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `.env.local` / secrets | mitigate | `.env.example` carries placeholders only; `.env.local` is gitignored; verify no real DATABASE_URL / token committed |
| T-01-02 | Tampering | next.config / Drizzle config | mitigate | serverExternalPackages set so grammY/pg are not bundled (prevents Edge-runtime breakage); Drizzle config reads DATABASE_URL from env, never hardcoded |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | RESEARCH Package Legitimacy Audit reviewed all packages; all `[ASSUMED]` packages are CLAUDE.md-mandated and verified against official source repos; no postinstall scripts found. slopcheck was unavailable at research time — see note below |
</threat_model>

<verification>
- `npx next build` completes without error.
- `npx vitest run` exits 0 with no config error.
- `node -e` guard confirms all named deps present and no deferred-phase packages installed.
- Manual: `.env.local` is absent from `git status` (gitignored).
</verification>

<success_criteria>
- Next.js 15 App Router project builds and serves.
- All Phase 1 deps installed at RESEARCH-verified versions; no deferred-phase packages present.
- shadcn/ui neutral initialized with the full Phase 1 component set.
- Drizzle client (`@/db`) + drizzle-kit config resolve DATABASE_URL.
- next.config wraps next-intl plugin + declares grammy/pg external.
- Vitest harness runnable; fixtures + DB-gated helper exist.
- `.env.example` documents all 9 Phase 1 secrets; `.env.local` gitignored.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done.
</output>
