---
phase: 01-foundation
plan: 04
type: execute
wave: 3
depends_on: ["01-02a"]
files_modified:
  - src/lib/telegram.ts
  - src/app/api/telegram/webhook/route.ts
  - tests/telegram-webhook.test.ts
autonomous: true
requirements: [AUTH-02, AUTH-03]

must_haves:
  truths:
    - "A /start update from an unknown Telegram user upserts exactly one pending_people row and replies with a Turkish pending-approval message"
    - "Sending the same /start update twice results in exactly one pending_people row (idempotent via ON CONFLICT DO NOTHING)"
    - "The webhook is constructed with webhookCallback(bot, 'std/http', { secretToken: process.env.TELEGRAM_WEBHOOK_SECRET! }) so grammY validates the X-Telegram-Bot-Api-Secret-Token header; a request with a wrong/missing secret header is rejected (401-class) and the /start handler does not run"
    - "The webhook route runs on the Node.js runtime (not Edge) — grammY requires Node"
  artifacts:
    - path: "src/lib/telegram.ts"
      provides: "Minimal grammY Bot instance + /start command handler (no conversations plugin)"
      contains: "bot.command('start'"
    - path: "src/app/api/telegram/webhook/route.ts"
      provides: "POST webhook via webhookCallback(bot, 'std/http', { secretToken: TELEGRAM_WEBHOOK_SECRET }) - grammY validates the X-Telegram-Bot-Api-Secret-Token header"
      contains: "secretToken"
  key_links:
    - from: "src/app/api/telegram/webhook/route.ts"
      to: "src/db/schema/pending-people.ts"
      via: "insert pendingPeople ... onConflictDoNothing"
      pattern: "pendingPeople"
    - from: "src/app/api/telegram/webhook/route.ts"
      to: "src/lib/telegram.ts"
      via: "import bot + webhookCallback"
      pattern: "webhookCallback"
---

<objective>
Ship the minimal Telegram `/start` webhook (D-02): a grammY bot with a single `/start` handler that upserts the caller's Telegram ID + name into `pending_people` (idempotently) and replies with a Turkish pending-approval acknowledgement, exposed as a Node-runtime Next.js route handler with webhook secret-token verification.

Purpose: This is the self-start half of the onboarding flow (D-01) — it feeds the pending-approval queue that the office promotes to active people in plan 01-05. It is the same route entry point Phase 2 extends into the full conversational state machine, so the boundary must be correct now. No conversations plugin, no session middleware (RESEARCH A4).
Output: A live `/start` webhook that registers pending people idempotently, verifies the Telegram secret token, and a passing idempotency integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-VALIDATION.md

<interfaces>
<!-- From src/db/schema/pending-people.ts (plan 01-02a): pendingPeople { id, tenantId, telegramUserId bigint unique, telegramName, startedAt } -->
<!-- From src/db/index.ts (plan 01-01): export const db -->
<!-- From src/lib/tenant.ts (plan 01-02a): getDefaultTenantId() — supply tenant_id on the pending_people insert (Pitfall 3) -->
<!-- grammY webhook contract (RESEARCH § Minimal grammY /start Webhook): bot.command('start') upserts pendingPeople via onConflictDoNothing; POST = webhookCallback(bot, 'std/http'); secret token verified -->
<!-- vercel.json function override for this route was created in plan 01-01 (memory 512, maxDuration 55) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: grammY bot instance + /start handler + Node-runtime webhook route with secret verification</name>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md § "Minimal grammY /start Webhook" (bot instance, /start handler, webhookCallback, secret verification, env vars) and § "Common Pitfalls" Pitfall 6 (std/http adapter name) + § "State of the Art" (Node runtime, NOT edge)
    - .planning/phases/01-foundation/01-CONTEXT.md D-01, D-02 (self-start onboarding, minimal Phase 1 webhook) + § "Specific Ideas" (friendly TR pending message)
    - src/db/schema/pending-people.ts, src/lib/tenant.ts, src/db/index.ts (from plans 01-02a / 01-01)
  </read_first>
  <behavior>
    - A /start update from telegram user id N (not previously seen) inserts one pending_people row with telegramUserId=N, telegramName from ctx.from, tenant_id = getDefaultTenantId().
    - Replaying the same /start update (same user id) a second time leaves exactly one pending_people row (ON CONFLICT DO NOTHING on the unique telegram_user_id).
    - The reply text is the Turkish pending-approval acknowledgement greeting the user by name.
    - A webhook POST carrying a wrong or missing X-Telegram-Bot-Api-Secret-Token header is rejected (grammY's secretToken check returns a 401-class error) and NO pending_people row is written for that request.
  </behavior>
  <files>src/lib/telegram.ts, src/app/api/telegram/webhook/route.ts, tests/telegram-webhook.test.ts</files>
  <action>
    src/lib/telegram.ts: create the minimal grammY `Bot` from `TELEGRAM_BOT_TOKEN` (throw if unset). Register `bot.command('start')`: read `ctx.from.id` + `ctx.from.first_name ?? ctx.from.username`, insert into pendingPeople `{ telegramUserId: BigInt(id), telegramName, tenantId: getDefaultTenantId() }` with `.onConflictDoNothing()`, then `ctx.reply(...)` the Turkish pending-approval message (per CONTEXT Specific Ideas). NO conversations plugin, NO session middleware (RESEARCH A4).
    src/app/api/telegram/webhook/route.ts: `export const runtime = 'nodejs'`, `export const dynamic = 'force-dynamic'`, `export const fetchCache = 'force-no-store'`. Export the POST handler as `webhookCallback(bot, 'std/http', { secretToken: process.env.TELEGRAM_WEBHOOK_SECRET! })` - grammY compares the incoming `X-Telegram-Bot-Api-Secret-Token` header to that value and rejects a wrong/missing secret (401-class) WITHOUT running the bot handlers (do NOT rely on grammY auto-reading the env var; the secretToken option is required). Also export a lightweight `GET` returning `{ ok: true, phase: 1 }`. Throw at module load if `TELEGRAM_WEBHOOK_SECRET` is unset so a misconfigured deploy fails fast rather than serving an unauthenticated webhook. Do NOT set `runtime = 'edge'` (grammY needs Node).
    Write tests/telegram-webhook.test.ts: (a) under describeIfDb, build the /start handler logic against the test DB; assert one insert, then replay → still one row (idempotency); (b) a secret-token unit test (no DB needed) - POST to the route handler with a wrong/missing `X-Telegram-Bot-Api-Secret-Token` header and assert the response is a 401-class error AND no pending_people insert is attempted (spy the db insert / assert status). This proves the secretToken option is wired, not assumed.
  </action>
  <verify>
    <automated>npx vitest run tests/telegram-webhook.test.ts --reporter=verbose 2>&1 | tail -15 && node -e "const fs=require('fs');const r=fs.readFileSync('src/app/api/telegram/webhook/route.ts','utf8');if(!/webhookCallback/.test(r))throw new Error('no webhookCallback');if(!/secretToken/.test(r))throw new Error('no secretToken option');if(!/TELEGRAM_WEBHOOK_SECRET/.test(r))throw new Error('secretToken not bound to TELEGRAM_WEBHOOK_SECRET');if(!/runtime\s*=\s*['\"]nodejs['\"]/.test(r))throw new Error('not node runtime');if(/runtime\s*=\s*['\"]edge['\"]/.test(r))throw new Error('edge runtime forbidden');const t=fs.readFileSync('src/lib/telegram.ts','utf8');if(!/onConflictDoNothing/.test(t))throw new Error('no idempotent upsert')"</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/telegram.ts: minimal Bot + bot.command('start') upserting pendingPeople via onConflictDoNothing with tenant_id supplied; Turkish pending-approval reply.
    - route.ts: `runtime = 'nodejs'`, POST = `webhookCallback(bot, 'std/http', { secretToken: process.env.TELEGRAM_WEBHOOK_SECRET! })`; a request with a wrong/missing secret header is rejected (401-class) and the handler does not run; no edge runtime; GET health returns phase 1.
    - tests/telegram-webhook.test.ts: first /start → one row; replay → still exactly one row (idempotency) under describeIfDb; AND a bad/missing-secret request is rejected (401-class) with no insert attempted.
    - No @grammyjs/conversations or session middleware imported (Phase 2 scope).
  </acceptance_criteria>
  <done>Minimal /start webhook registers pending people idempotently with secret verification on the Node runtime; idempotency test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet → /api/telegram/webhook | Any caller can POST; only genuine Telegram requests (correct secret token) must be processed |
| webhook handler → pending_people | Untrusted Telegram payload must not inject arbitrary or duplicate rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Spoofing | webhook caller authenticity | mitigate | `webhookCallback(bot, 'std/http', { secretToken: process.env.TELEGRAM_WEBHOOK_SECRET! })` makes grammY validate the `X-Telegram-Bot-Api-Secret-Token` header; a wrong/missing secret yields a 401-class rejection unprocessed (unit-tested). Module load throws if the secret env var is unset (fail-fast) |
| T-04-02 | Tampering / DoS | duplicate pending_people injection | mitigate | telegram_user_id is UNIQUE; insert uses ON CONFLICT DO NOTHING — repeated /start cannot create duplicate or unbounded rows per user |
| T-04-03 | Information Disclosure | bot token leakage | mitigate | TELEGRAM_BOT_TOKEN + TELEGRAM_WEBHOOK_SECRET read from env only (gitignored); never logged; handler throws if token unset |
| T-04-04 | Elevation of Privilege | pending != active | mitigate | /start only writes pending_people (no role, no project, no session); access is granted only after office approval in plan 01-05 |
| T-04-SC | Tampering | grammy install | mitigate | grammy is CLAUDE.md-mandated, verified in RESEARCH Package Legitimacy Audit against github.com/grammyjs/grammY; installed in plan 01-01 |
</threat_model>

<verification>
- `npx vitest run tests/telegram-webhook.test.ts` passes (idempotency under describeIfDb).
- grep guards: webhookCallback present, runtime nodejs, no edge runtime, onConflictDoNothing present.
- Manual (deferred to phase verify): set webhook via setWebhook with secret_token after deploy; send /start from a phone; confirm one pending_people row + the TR reply.
</verification>

<success_criteria>
- /start upserts pending people idempotently with tenant_id supplied.
- Webhook verifies the Telegram secret token; rejects spoofed callers.
- Node runtime; no conversations plugin (Phase 2 boundary preserved).
- Idempotency integration test green.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-04-SUMMARY.md` when done.
</output>
