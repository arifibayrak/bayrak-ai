---
phase: 12-submission-driven-hakkedi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema/hakedis-line-submissions.ts
  - src/db/schema/hakedis-period-lines.ts
  - src/db/schema/index.ts
  - messages/en.json
  - messages/tr.json
  - tests/hakedis-live.test.ts
  - tests/fixtures/hakedis.ts
autonomous: true
requirements: [SDH-01, SDH-02, SDH-03]
must_haves:
  truths:
    - "hakedis_line_submissions schema file exists with the D-119 column/FK/PK shape and exports hakedisLineSubmissions"
    - "hakedis_period_lines schema declares UNIQUE (period_id, boq_item_id) (Open Question 4 — required for D-117 UPSERT)"
    - "Barrel src/db/schema/index.ts re-exports the new module"
    - "Bilingual TR + EN i18n keys for the SDH-02 traceability UI exist under dashboard.admin.hakedis.line_submissions.*"
    - "tests/hakedis-live.test.ts scaffolded with describeIfDb and ≥9 it.todo entries mapping to the SDH-01/02/03 + Pitfall 4 + Pitfall 5 contract"
    - "seedDraftPeriod helper exported from tests/fixtures/hakedis.ts (reuses existing seed pattern; idempotent)"
  artifacts:
    - path: "src/db/schema/hakedis-line-submissions.ts"
      provides: "D-119 join-table schema"
      contains: "hakedisLineSubmissions"
    - path: "src/db/schema/hakedis-period-lines.ts"
      provides: "UNIQUE (period_id, boq_item_id) declaration"
      contains: "unique"
    - path: "src/db/schema/index.ts"
      provides: "Re-export of hakedis-line-submissions"
      contains: "hakedis-line-submissions"
    - path: "tests/hakedis-live.test.ts"
      provides: "Phase 12 contract test scaffold"
      contains: "describeIfDb"
    - path: "tests/fixtures/hakedis.ts"
      provides: "seedDraftPeriod helper"
      contains: "seedDraftPeriod"
    - path: "messages/en.json"
      provides: "EN traceability UI keys"
      contains: "line_submissions"
    - path: "messages/tr.json"
      provides: "TR traceability UI keys"
      contains: "line_submissions"
  key_links:
    - from: "src/db/schema/index.ts"
      to: "src/db/schema/hakedis-line-submissions.ts"
      via: "barrel re-export"
      pattern: "hakedis-line-submissions"
    - from: "tests/hakedis-live.test.ts"
      to: "tests/fixtures/hakedis.ts"
      via: "seedDraftPeriod import"
      pattern: "seedDraftPeriod"
---

<objective>
Wave 0 + schema-author wave for Phase 12: stand up the D-119 join-table Drizzle schema, add the UNIQUE constraint on `hakedis_period_lines` that the D-117 UPSERT requires (Open Question 4 RESOLVED — UPSERT path chosen because DELETE-then-INSERT would orphan join rows via CASCADE), wire the barrel export, scaffold the contract test file with `describeIfDb` + 9 `it.todo` entries (the Nyquist gates that downstream waves bind verify commands to), commit the `seedDraftPeriod` fixture helper, and ship the bilingual TR + EN i18n keys for the SDH-02 traceability UI (so Plan 04 only consumes — never edits — `messages/*.json`).

Purpose: Lock the data contract and verification surface before any code that calls or renders against them lands. Per D-117 + D-119 + Open Question 4, the join-table schema, the parent UNIQUE constraint, and the test scaffold are all interdependent — they ship together so Wave 2's [BLOCKING] migration generation has a coherent diff to emit.

Output: One new schema file, one edited schema file (UNIQUE addition), one edited barrel, one new test scaffold file, one new fixture helper, two edited i18n files. No live-DB writes in this wave — that is Wave 2's job.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-submission-driven-hakkedi/12-CONTEXT.md
@.planning/phases/12-submission-driven-hakkedi/12-RESEARCH.md
@.planning/phases/12-submission-driven-hakkedi/12-VALIDATION.md
@src/db/schema/hakedis-period-lines.ts
@src/db/schema/hakedis-periods.ts
@src/db/schema/submissions.ts
@src/db/schema/audit-notifications.ts
@src/db/schema/index.ts
@tests/fixtures/db.ts
@tests/fixtures/exports.ts
@tests/hakedis.test.ts
@messages/en.json
@messages/tr.json

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/db/schema/hakedis-period-lines.ts (Phase 7 + Phase 10):
- export const hakedisPeriodLines = pgTable('hakedis_period_lines', { ... })
- Columns: id (uuid PK), tenantId (uuid nullable), periodId (uuid notNull FK → hakedisPeriods, ON DELETE CASCADE), boqItemId (uuid notNull FK → boqItems, ON DELETE RESTRICT), materialSnapshot/unitSnapshot/currencyCodeSnapshot (text notNull), unitPriceSnapshot (numeric notNull), cumulativeQtyApproved/previousCumulativeQty (numeric notNull), periodQty (numeric, GENERATED ALWAYS AS cumulative_qty_approved - previous_cumulative_qty), periodValue/cumulativeValue (numeric notNull), createdAt (timestamptz notNull)
- Existing indexes: hakedis_period_lines_period_idx (periodId), hakedis_period_lines_boq_idx (boqItemId)
- NO existing UNIQUE constraint on (period_id, boq_item_id) — Open Question 4 requires adding it

From src/db/schema/submissions.ts:
- export const submissions = pgTable('submissions', { ... })
- Relevant columns: id (uuid PK), tenantId (uuid nullable), status (enum: pending_audit | approved | rejected), boqItemId (uuid notNull FK), quantity (numeric notNull, precision 12 scale 3), decidedAt (timestamptz nullable), personId (uuid notNull FK → people)

From src/db/schema/audit-notifications.ts pattern (D-34 reference for FK shape):
- Uses references(() => parent.id, { onDelete: 'cascade' | 'restrict' })
- Composite PRIMARY KEY via primaryKey({ columns: [t.col1, t.col2] }) in the third pgTable arg
- index() declarations in the third pgTable arg

From tests/fixtures/db.ts:
- export const hasTestDb = Boolean(process.env.TEST_DATABASE_URL)
- export const describeIfDb = hasTestDb ? describe : describe.skip
- export async function getTestDb(): Promise<DrizzleClient>
- export async function truncateAllTables(db): Promise<void>

From tests/hakedis.test.ts header (pattern to mirror):
- import { it, expect, beforeEach, afterEach, vi } from 'vitest'
- import { describeIfDb, getTestDb, truncateAllTables } from './fixtures/db'
- vi.mock('next/cache', () => ({ revalidatePath: vi.fn() }))
- vi.mock('next/server', () => ({ after: (fn: () => Promise<void>) => { fn().catch(() => {}); } }))
- vi.mock('@/lib/auth', () => ({ auth: vi.fn().mockResolvedValue({ user: { id: 'test-user-auth-id', email: 'test@example.com' } }) }))
- vi.mock('@/lib/tenant', () => ({ getDefaultTenantId: vi.fn().mockReturnValue('00000000-0000-0000-0000-000000000001') }))

From messages/en.json existing key shape (lines 387-420):
- "dashboard"."admin"."hakedis"."detail"."*" — Phase 10/11 keys (back, lines_heading, recompute, finalize, mark_submitted, export_excel, download_pdf, etc.)
- Sibling block to add: "dashboard"."admin"."hakedis"."line_submissions"."*"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add D-119 join-table schema + UNIQUE on hakedis_period_lines + barrel export</name>
  <files>src/db/schema/hakedis-line-submissions.ts, src/db/schema/hakedis-period-lines.ts, src/db/schema/index.ts</files>
  <read_first>
    - .planning/phases/12-submission-driven-hakkedi/12-CONTEXT.md (D-119 spec)
    - .planning/phases/12-submission-driven-hakkedi/12-RESEARCH.md (§Pattern Code Example 1, §Open Question 2 RESOLVED, §Open Question 4 RESOLVED)
    - src/db/schema/hakedis-period-lines.ts (parent table — must add UNIQUE here, per Open Question 4)
    - src/db/schema/audit-notifications.ts (FK shape + composite primaryKey() + index() pattern reference)
    - src/db/schema/index.ts (barrel re-export pattern — alphabetical-ish; new entry slots after hakedis-period-lines)
  </read_first>
  <action>
    Create `src/db/schema/hakedis-line-submissions.ts` per D-119: pgTable 'hakedis_line_submissions' with columns tenantId (uuid, references tenants.id, nullable per D-09 + Open Question 2 RESOLVED), periodLineId (uuid, notNull, references hakedisPeriodLines.id with onDelete: 'cascade' — D-119 cascade-on-draft-delete), submissionId (uuid, notNull, references submissions.id with onDelete: 'restrict' — D-119 snapshot durability), qtyContributed (numeric precision 12 scale 3, notNull — name choice per Claude's Discretion in CONTEXT.md, matching submissions.quantity precision), createdAt (timestamp withTimezone, defaultNow, notNull). Third pgTable arg: primaryKey({ columns: [t.periodLineId, t.submissionId] }) (D-119 idempotency key — also serves as ON CONFLICT target for D-117 UPSERT), index('hakedis_line_submissions_submission_idx').on(t.submissionId) (Pitfall 8 — needed for any future "find all periods this submission contributed to" reverse-lookup). Re-use imports tenants, hakedisPeriodLines, submissions; do NOT import hakedisPeriods (only joins through period_line_id).

    Edit `src/db/schema/hakedis-period-lines.ts` to add UNIQUE (period_id, boq_item_id) — per Open Question 4 RESOLVED. Add `unique` to the drizzle-orm/pg-core import line (alongside pgTable/uuid/text/numeric/timestamp/index/sql). Add a new entry to the third pgTable arg array: `unique('hakedis_period_lines_period_boq_unique').on(t.periodId, t.boqItemId)`. Comment above explains: D-117 UPSERT requires this constraint — DELETE-then-INSERT would briefly orphan hakedis_line_submissions rows via the period_line_id CASCADE. Do NOT touch the existing periodQty GENERATED column declaration or the existing CHECK constraints (Pitfall 7 — generated migration must not re-emit them).

    Edit `src/db/schema/index.ts` to add `export * from './hakedis-line-submissions';  // references tenants, hakedis-period-lines, submissions (D-119)` slotted after the `hakedis-period-lines` line.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "hakedisLineSubmissions" src/db/schema/hakedis-line-submissions.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `src/db/schema/hakedis-line-submissions.ts` exists
    - `grep -c "export const hakedisLineSubmissions" src/db/schema/hakedis-line-submissions.ts` returns 1
    - `grep -c "primaryKey({ columns: \[t.periodLineId, t.submissionId\] })" src/db/schema/hakedis-line-submissions.ts` returns 1
    - `grep -c "onDelete: 'cascade'" src/db/schema/hakedis-line-submissions.ts` returns 1 (periodLineId)
    - `grep -c "onDelete: 'restrict'" src/db/schema/hakedis-line-submissions.ts` returns 1 (submissionId)
    - `grep -c "qty_contributed" src/db/schema/hakedis-line-submissions.ts` returns 1
    - `grep -c "hakedis_line_submissions_submission_idx" src/db/schema/hakedis-line-submissions.ts` returns 1
    - `grep -c "hakedis_period_lines_period_boq_unique" src/db/schema/hakedis-period-lines.ts` returns 1
    - `grep -c "hakedis-line-submissions" src/db/schema/index.ts` returns 1
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The new schema file compiles, exports `hakedisLineSubmissions`, declares the composite PK + cascade/restrict FKs + reverse-lookup index per D-119, the parent table has `UNIQUE (period_id, boq_item_id)` for the D-117 UPSERT target, and the barrel re-exports the new module.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold tests/hakedis-live.test.ts contract + seedDraftPeriod fixture</name>
  <files>tests/hakedis-live.test.ts, tests/fixtures/hakedis.ts</files>
  <read_first>
    - .planning/phases/12-submission-driven-hakkedi/12-VALIDATION.md (§Per-Task Verification Map + §Wave 0 Requirements — 9 contract rows the scaffold MUST cover)
    - .planning/phases/12-submission-driven-hakkedi/12-RESEARCH.md (§Validation Architecture, §Common Pitfalls 4 + 5)
    - tests/hakedis.test.ts (mirror header: vi.mock('next/cache'), vi.mock('next/server'), vi.mock('@/lib/auth'), vi.mock('@/lib/tenant'); describeIfDb usage)
    - tests/fixtures/db.ts (truncateAllTables + getTestDb signature)
    - tests/fixtures/exports.ts (seedFinalizedHakedisFixture pattern reference for the new seedDraftPeriod helper)
  </read_first>
  <action>
    Create `tests/hakedis-live.test.ts` mirroring `tests/hakedis.test.ts` head (same 4 vi.mock blocks for next/cache, next/server, @/lib/auth, @/lib/tenant). Wrap in `describeIfDb('Phase 12 submission-driven hakkediş', () => { ... })`. Inside, add nine `it.todo` entries with EXACT names matching the verify commands in 12-VALIDATION.md (so Plan 03's `npx vitest run tests/hakedis-live.test.ts -t "<name>"` can target them deterministically):
      1. `it.todo('D-117 scoped recompute fires on approve for matching (project_id, boq_item_id, currency_code) triplet')`
      2. `it.todo('D-118 no-open-period: scoped recompute is a no-op when no draft period exists for (project_id, currency_code)')`
      3. `it.todo('D-119 join row: hakedis_line_submissions row inserted with qty_contributed = submissions.quantity')`
      4. `it.todo('D-119 idempotency: re-running scoped recompute for the same submission yields exactly one join row (ON CONFLICT DO UPDATE)')`
      5. `it.todo('getLineSubmissions returns rows shape { workerName, decidedAt, qtyContributed, photoUrl, notes } ordered by decided_at DESC')`
      6. `it.todo('finalize race: approve commit + finalize commit do not produce a join row for the just-finalized period (Pitfall 4)')`
      7. `it.todo('Pitfall 5: bot path never writes office_activity_log (no FK violation on actor_user_id)')`
      8. `it.todo('SDH-03 regression: manual recomputePeriodLines on a draft produces identical line totals before/after the helper extraction')`
      9. `it.todo('LivePeriodPoller mount gate: component renders nothing and returns no DOM when enabled=false')`

    Create `tests/fixtures/hakedis.ts` exporting:
      - const HAKEDIS_LIVE_FIXTURE_IDS with deterministic UUIDs for tenant, project, boqItem, worker, auditor, period (use the same pattern as tests/fixtures/exports.ts HAKEDIS_FIXTURE_IDS).
      - `export async function seedDraftPeriod(db, opts?: { unitPrice?: string; currencyCode?: string; periodEndDate?: string })`: inserts tenant (idempotent ON CONFLICT DO NOTHING), project, BOQ item (with unit_price), worker + auditor people rows, an auditor assignment row, and a single hakedis_periods row with status='draft' (period_end_date default today+1, currency_code default 'TRY'). Returns { tenantId, projectId, boqItemId, workerPersonId, auditorPersonId, periodId }. Use the same Drizzle insert idiom as tests/fixtures/exports.ts seedFinalizedHakedisFixture — lazy-imports schemas, calls db.insert(...).values(...).onConflictDoNothing() for tenant.
  </action>
  <verify>
    <automated>npx vitest run tests/hakedis-live.test.ts 2>&1 | grep -E "(todo|skipped)" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/hakedis-live.test.ts` exists
    - `grep -c "describeIfDb" tests/hakedis-live.test.ts` returns ≥ 1
    - `grep -c "it.todo" tests/hakedis-live.test.ts` returns 9
    - `grep -c "vi.mock('next/cache'" tests/hakedis-live.test.ts` returns 1
    - `grep -c "vi.mock('next/server'" tests/hakedis-live.test.ts` returns 1
    - `grep -c "vi.mock('@/lib/auth'" tests/hakedis-live.test.ts` returns 1
    - `grep -c "vi.mock('@/lib/tenant'" tests/hakedis-live.test.ts` returns 1
    - File `tests/fixtures/hakedis.ts` exists
    - `grep -c "export async function seedDraftPeriod" tests/fixtures/hakedis.ts` returns 1
    - `grep -c "HAKEDIS_LIVE_FIXTURE_IDS" tests/fixtures/hakedis.ts` returns ≥ 1
    - `npx vitest run tests/hakedis-live.test.ts` exits 0 with at least 9 todo entries listed
  </acceptance_criteria>
  <done>The Phase 12 test file exists with 9 it.todo entries that map 1:1 to the rows in 12-VALIDATION.md §Per-Task Verification Map; the seedDraftPeriod helper is exported and importable from tests/fixtures/hakedis.ts; vitest run is green (all todos pass).</done>
</task>

<task type="auto">
  <name>Task 3: Add bilingual SDH-02 traceability i18n keys (EN + TR)</name>
  <files>messages/en.json, messages/tr.json</files>
  <read_first>
    - messages/en.json (existing `dashboard.admin.hakedis.detail.*` namespace — lines 387-420 — to slot the new sub-block adjacent in the same parent)
    - messages/tr.json (mirror file — every new EN key must have a TR sibling at the byte-identical JSON path, per D-111 bilingual convention)
    - .planning/phases/12-submission-driven-hakkedi/12-RESEARCH.md (§Pattern 4 — recommended UI shape is inline expand-row; key set must support that shape)
  </read_first>
  <action>
    In `messages/en.json`, inside `dashboard.admin.hakedis` (sibling to the existing `detail` block), add a new `line_submissions` object with EN copy keys: heading ("Contributing Submissions"), trigger_label ("Show contributing submissions ({count})"), empty ("No submissions contributed to this line yet."), col_worker ("Worker"), col_decided_at ("Approved At"), col_qty ("Quantity"), col_notes ("Notes"), col_photo ("Photo"), photo_alt ("Submission photo"), photo_view ("View"), polling_indicator ("Auto-refreshing every 30 seconds"), polling_aria ("Live updates enabled — page refreshes automatically while this period is draft").

    In `messages/tr.json`, mirror the same JSON path `dashboard.admin.hakedis.line_submissions` with Turkish copy: heading ("Katkı Sağlayan Kayıtlar"), trigger_label ("Katkı sağlayan kayıtları göster ({count})"), empty ("Bu kaleme henüz katkı sağlayan kayıt yok."), col_worker ("İşçi"), col_decided_at ("Onaylanma Zamanı"), col_qty ("Miktar"), col_notes ("Notlar"), col_photo ("Fotoğraf"), photo_alt ("Kayıt fotoğrafı"), photo_view ("Görüntüle"), polling_indicator ("Her 30 saniyede bir otomatik yenileniyor"), polling_aria ("Canlı güncellemeler etkin — bu dönem taslak halindeyken sayfa otomatik yenileniyor"). Use Turkish-tone "siz" register established in Phase 2 D-26 / Phase 11 D-111.

    Keep all keys at exactly the same JSON path in both files (byte-identical path tree). Do NOT touch any existing keys in either file — additive-only edit.
  </action>
  <verify>
    <automated>node -e "const en=require('./messages/en.json'); const tr=require('./messages/tr.json'); const enKeys=Object.keys(en.dashboard.admin.hakedis.line_submissions); const trKeys=Object.keys(tr.dashboard.admin.hakedis.line_submissions); if (enKeys.length!==12 || trKeys.length!==12) {process.exit(1);} for (const k of enKeys) { if (!trKeys.includes(k)) { console.error('Missing TR key:', k); process.exit(2); } } console.log('OK', enKeys.length);"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "JSON.parse(require('fs').readFileSync('messages/en.json'))"` exits 0 (valid JSON)
    - `node -e "JSON.parse(require('fs').readFileSync('messages/tr.json'))"` exits 0 (valid JSON)
    - The verify command above prints `OK 12` (exact count of new keys)
    - `grep -c "line_submissions" messages/en.json` returns 1
    - `grep -c "line_submissions" messages/tr.json` returns 1
    - `grep -c "Contributing Submissions" messages/en.json` returns 1
    - `grep -c "Katkı Sağlayan Kayıtlar" messages/tr.json` returns 1
  </acceptance_criteria>
  <done>Both messages files parse as valid JSON; the new `dashboard.admin.hakedis.line_submissions` block exists at the byte-identical path in both files with all 12 keys present; no existing keys are mutated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Drizzle schema → DB | Schema authoring is dev-time only; no runtime trust boundary crossed in this wave. Wave 2's migration apply is the live-DB boundary. |
| Test fixture seed → test DB | Only TEST_DATABASE_URL is touched, gated by describeIfDb. No prod DB credentials in scope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01-SCH | Tampering | hakedis-line-submissions.ts schema declaration | mitigate | onDelete: 'restrict' on submission_id FK preserves snapshot durability (D-119 + Pitfall 8); onDelete: 'cascade' on period_line_id FK matches D-97 draft-period delete semantics. Both encoded as Drizzle column options — DB enforces. |
| T-12-01-UPS | Tampering | hakedis-period-lines.ts UNIQUE constraint addition | mitigate | UNIQUE(period_id, boq_item_id) is the correct-by-design business invariant (one period cannot have two lines for the same BOQ item) — adding it prevents the D-117 UPSERT from violating snapshot semantics under concurrent approval (Pitfall 4). |
| T-12-01-I18N | Information Disclosure | messages/en.json + messages/tr.json | accept | New keys carry no PII or sensitive data; bilingual labels are routine UI copy. |
| T-12-01-FIX | Tampering | tests/fixtures/hakedis.ts seedDraftPeriod | accept | Test-only path, never imported by production code; idempotent inserts use ON CONFLICT DO NOTHING to remain safe under repeat runs. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npx vitest run tests/hakedis-live.test.ts` exits 0 (9 todos listed/passing)
- `grep -c "hakedis_period_lines_period_boq_unique" src/db/schema/hakedis-period-lines.ts` returns 1
- Both messages/*.json files parse as valid JSON and the 12 new keys exist at the byte-identical path
- `grep -c "hakedis-line-submissions" src/db/schema/index.ts` returns 1
</verification>

<success_criteria>
- Schema: `hakedisLineSubmissions` table declared with composite PK + cascade/restrict FKs + reverse index; UNIQUE constraint on parent `hakedis_period_lines`
- Barrel: new module re-exported from `src/db/schema/index.ts`
- Test scaffold: `tests/hakedis-live.test.ts` contains 9 it.todo entries with names that match 12-VALIDATION.md verify commands; vitest run is green
- Fixture: `seedDraftPeriod` helper exported and discoverable from `tests/fixtures/hakedis.ts`
- i18n: `dashboard.admin.hakedis.line_submissions` block exists at the byte-identical JSON path in both messages/en.json and messages/tr.json with all 12 keys
- Type check passes — no broken imports anywhere in the schema graph
</success_criteria>

<output>
Create `.planning/phases/12-submission-driven-hakkedi/12-01-SUMMARY.md` when done.
</output>
