---
phase: 12-submission-driven-hakkedi
plan: 03
type: execute
wave: 3
depends_on: [12-01, 12-02]
files_modified:
  - src/actions/hakedis.ts
  - src/lib/bot-audit.ts
  - tests/hakedis-live.test.ts
autonomous: true
requirements: [SDH-01, SDH-02, SDH-03]
must_haves:
  truths:
    - "recomputeHakedisLine(projectId, boqItemId, currencyCode) helper exists as an internal (non-'use server') exported function in src/actions/hakedis.ts"
    - "recomputeHakedisLine is idempotent: re-running for the same (project, boq item, currency) yields one period_lines row and one join row per contributing submission (ON CONFLICT DO UPDATE on both)"
    - "recomputeHakedisLine no-ops cleanly when no draft period exists for the (project_id, currency_code) (D-118)"
    - "recomputeHakedisLine skips cleanly when the matched period is non-draft (Pitfall 4 finalize-during-approve race)"
    - "recomputeHakedisLine writes hakedis_line_submissions rows via INSERT…SELECT FROM submissions with the period's Istanbul-tz cutoff (D-119)"
    - "Existing recomputePeriodLines body internally calls recomputeHakedisLine for each priced BOQ item (math drift impossible — single body, two callers)"
    - "getLineSubmissions(periodLineId) Server Action exists, is auth-guarded, tenant-scoped, returns rows ordered by decided_at DESC with shape { submissionId, workerName, decidedAt, qtyContributed, photoUrl, notes }"
    - "handleAuditDecision approve branch in src/lib/bot-audit.ts calls recomputeHakedisLine inside a try/catch AFTER editAllSiblingMessages and BEFORE the worker notification (post-commit best-effort per CR-02/D-40)"
    - "Bot path NEVER calls logOfficeActivity (Pitfall 5 — no Auth.js session → FK violation)"
    - "tests/hakedis-live.test.ts has 9 concrete it() entries (the it.todo placeholders from Plan 01 are replaced 1:1) and all pass against the test DB"
    - "Existing tests/hakedis.test.ts (28 tests) and tests/telegram-audit.test.ts (52 tests) and tests/exports.test.ts (16+ tests) all still pass — SDH-03 no-regression guarantee"
  artifacts:
    - path: "src/actions/hakedis.ts"
      provides: "recomputeHakedisLine + getLineSubmissions"
      contains: "recomputeHakedisLine"
    - path: "src/lib/bot-audit.ts"
      provides: "D-117 post-commit hook in handleAuditDecision approve branch"
      contains: "recomputeHakedisLine"
    - path: "tests/hakedis-live.test.ts"
      provides: "9 concrete Phase 12 contract tests"
      contains: "describeIfDb"
  key_links:
    - from: "src/lib/bot-audit.ts (handleAuditDecision approve branch)"
      to: "src/actions/hakedis.ts (recomputeHakedisLine)"
      via: "dynamic import('@/actions/hakedis')"
      pattern: "recomputeHakedisLine\\("
    - from: "src/actions/hakedis.ts (recomputeHakedisLine)"
      to: "DB table hakedis_line_submissions"
      via: "INSERT … SELECT … FROM submissions … ON CONFLICT DO UPDATE"
      pattern: "INSERT INTO hakedis_line_submissions"
    - from: "src/actions/hakedis.ts (getLineSubmissions)"
      to: "DB tables hakedis_line_submissions + submissions + people"
      via: "JOIN query"
      pattern: "FROM hakedis_line_submissions"
---

<objective>
Land the structural core of Phase 12: extract a shared, idempotent `recomputeHakedisLine` helper from the existing `recomputePeriodLines` body (one math implementation, two callers — manual Recompute button + new D-117 post-commit hook), populate the D-119 join table from the same INSERT…SELECT it drives, add the `getLineSubmissions` Server Action for SDH-02 traceability, wire the D-117 hook into the bot's approve branch with the CR-02 best-effort try/catch shape, and replace all 9 `it.todo` entries in `tests/hakedis-live.test.ts` with passing assertions.

Purpose: This is where SDH-01 + SDH-02 actually ship. By the end of this plan, an auditor tapping ✅ Onayla on a Telegram message updates the open draft period's matching line + qty (D-117) and writes the contributing-submission join row (D-119). Office engineers can call `getLineSubmissions(periodLineId)` to enumerate every submission that built that line.

Output: Edits to `src/actions/hakedis.ts` (helper extract + new getter), `src/lib/bot-audit.ts` (best-effort post-commit hook), and `tests/hakedis-live.test.ts` (9 it.todo → 9 it.); existing test suites still pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-submission-driven-hakkedi/12-CONTEXT.md
@.planning/phases/12-submission-driven-hakkedi/12-RESEARCH.md
@.planning/phases/12-submission-driven-hakkedi/12-VALIDATION.md
@.planning/phases/12-submission-driven-hakkedi/12-01-SUMMARY.md
@.planning/phases/12-submission-driven-hakkedi/12-02-SUMMARY.md
@.planning/phases/10-hakkedi-billing/10-CONTEXT.md
@.planning/phases/03-audit-loop/03-CONTEXT.md
@src/actions/hakedis.ts
@src/lib/bot-audit.ts
@src/db/schema/hakedis-line-submissions.ts
@src/db/schema/hakedis-period-lines.ts
@src/db/schema/hakedis-periods.ts
@src/db/schema/submissions.ts
@src/db/schema/boq-items.ts
@src/db/schema/people.ts
@src/lib/log-office-activity.ts
@src/lib/auth.ts
@src/lib/tenant.ts
@tests/hakedis.test.ts
@tests/hakedis-live.test.ts
@tests/fixtures/hakedis.ts
@tests/fixtures/db.ts
@tests/telegram-audit.test.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from the live tree. -->

From src/actions/hakedis.ts (existing — body to be refactored):
- `export async function recomputePeriodLines(periodId: string): Promise<{ ok: true }>` — currently does DELETE-then-INSERT-all over hakedis_period_lines for the period. Lines 184-218 are the cumulative SELECT; lines 220-235 are the previous-cumulative SELECT; lines 246-282 are the per-item INSERT loop. After refactor: keeps its 'use server' export + auth guard + status='draft' check + DELETE clause, then loops calling `recomputeHakedisLine(projectId, boqItemId, currencyCode)` for each priced BOQ item with cumulative > 0.
- `export async function getPeriodDetail(periodId: string): Promise<{ period, lines, deductions, unpricedItems }>` — UNCHANGED by Phase 12.
- `export async function createPeriod(...)` — UNCHANGED (still calls recomputePeriodLines after INSERT).
- `export async function finalizePeriod(periodId: string): Promise<{ ok: true }>` — UNCHANGED.

From src/lib/bot-audit.ts handleAuditDecision approve branch (lines 402-498):
- After the txDb.transaction(async (tx) => { ... }) block COMMITs, `affected[0]` carries id + quantity + boqItemId.
- Line 462 closes the finally with txCleanup() (Pool .end()).
- Line 468: `await editAllSiblingMessages(submissionId, MESSAGES.auditApprovedOutcome(auditorDisplayName))` — D-34 fan-out edit
- Lines 470-495: worker notification try/catch (CR-02 best-effort shape — exact pattern Phase 12's D-117 hook mirrors)
- The D-117 hook is inserted AFTER line 468 and BEFORE line 470 (between sibling-edit and worker-notify), so a failed recompute does not leave stale auditor keyboards.
- `boqItemId` from `affected[0]` is the input to the BOQ-item lookup (needed because recomputeHakedisLine needs projectId + currencyCode, derived from boqItems).

From src/db/schema/boq-items.ts (verify by grep before coding):
- export const boqItems = pgTable('boq_items', { id, tenantId, projectId, material, unit, plannedQty, approvedQty, unitPrice (nullable per COST-01), currencyCode, ... })

From src/lib/log-office-activity.ts (DO NOT call from bot path — Pitfall 5):
- `import { after } from 'next/server'` at top — REQUIRES Next.js request scope
- `logOfficeActivity({ actorUserId, actionType, entityType, entityId, projectId?, metadata? })` — actorUserId is text.notNull().references(() => users.id) in the office_activity_log table
- Bot path has neither Auth.js session NOR Next.js request scope; calling this from recomputeHakedisLine would throw OR violate the FK

From src/lib/auth.ts:
- export const auth: () => Promise<Session | null> — first statement of every 'use server' function in hakedis.ts

From src/lib/tenant.ts:
- export function getDefaultTenantId(): string — returns the single-tenant MVP UUID

From tests/hakedis-live.test.ts (Plan 01 scaffold):
- 9 it.todo entries with the exact names listed in Plan 01 Task 2 acceptance criteria — replaced 1:1 by concrete it() in this plan

From tests/fixtures/hakedis.ts (Plan 01 fixture):
- export const HAKEDIS_LIVE_FIXTURE_IDS = { tenantId, projectId, boqItemId, workerPersonId, auditorPersonId, periodId }
- export async function seedDraftPeriod(db, opts?): Promise<{ tenantId, projectId, boqItemId, workerPersonId, auditorPersonId, periodId }>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract recomputeHakedisLine helper + refactor recomputePeriodLines to call it; add getLineSubmissions</name>
  <files>src/actions/hakedis.ts, tests/hakedis-live.test.ts</files>
  <read_first>
    - src/actions/hakedis.ts (current recomputePeriodLines body — lines 138-285 — the math to extract)
    - .planning/phases/12-submission-driven-hakkedi/12-RESEARCH.md (§Pattern 1, §Pattern 4, §Example 3 INSERT…SELECT SQL, §Example 4 getLineSubmissions sketch, §Open Question 1 RESOLVED — delta-only semantic, §Open Question 4 RESOLVED — UPSERT path with the UNIQUE constraint from Plan 01/02)
    - .planning/phases/12-submission-driven-hakkedi/12-VALIDATION.md (verify commands the new it() entries bind to)
    - src/db/schema/hakedis-line-submissions.ts (the join-table contract — Plan 01)
    - src/db/schema/hakedis-period-lines.ts (UPSERT target with the new UNIQUE constraint — Plan 01)
    - tests/hakedis.test.ts (existing 28 Phase 10 tests — none must regress)
  </read_first>
  <behavior>
    - Test 1 (D-117 trigger contract): calling `recomputeHakedisLine(projectId, boqItemId, currencyCode)` after seeding 1 approved submission with quantity '5.000' into a draft period yields a single hakedis_period_lines row with cumulative_qty_approved = '5.000' and period_value = '5.000' × unitPriceSnapshot. The matching hakedis_line_submissions row exists with qty_contributed = '5.000'.
    - Test 2 (D-118 no-open-period): calling `recomputeHakedisLine` for a (project, boqItem, currency) where no draft period exists returns { updated: false, periodLineId: null } without throwing. No hakedis_period_lines or hakedis_line_submissions rows are written.
    - Test 3 (D-119 join row): after approving 2 submissions for the same BOQ item, recomputeHakedisLine writes exactly 2 hakedis_line_submissions rows; each row has qty_contributed = the source submission's quantity (Postgres numeric round-trip preserved as the same decimal string).
    - Test 4 (D-119 idempotency): calling recomputeHakedisLine twice in a row for the same triplet produces the same final state — exactly 1 line row and N join rows for N approved submissions; the second call performs ON CONFLICT DO UPDATE for both target tables.
    - Test 5 (getLineSubmissions shape): seed a draft period with 2 approved submissions, fire recomputeHakedisLine, then call getLineSubmissions(periodLineId). Result is an array of length 2, ordered by decided_at DESC; each row has { submissionId, workerName, decidedAt (ISO string), qtyContributed (decimal string), photoUrl, notes }.
    - Test 6 (Pitfall 4 finalize race): seed a draft period, approve submission A, finalize the period, then call recomputeHakedisLine for the same triplet — function must skip cleanly (return { updated: false, ... }) without throwing and without writing any join row to the now-finalized period.
    - Test 7 (Pitfall 5 no office_activity_log): call recomputeHakedisLine from a no-session context (simulate bot path by spying on logOfficeActivity); assert it was NEVER called. Confirm office_activity_log row count is unchanged across the function call.
    - Test 8 (SDH-03 manual recompute no-regression): the existing tests/hakedis.test.ts suite must still pass after the refactor. Assert by running it in this plan's verify step.
    - Test 9 (LivePeriodPoller mount gate): UNIT TEST — render <LivePeriodPoller enabled={false} /> with @testing-library/react (already a transitive dep via shadcn vitest precedent — confirm available; if not present, defer to Plan 04's component plan where it actually renders). If react testing library is not available, replace this it() with a pure-import assertion: `expect(typeof LivePeriodPoller).toBe('function')` AND `expect(LivePeriodPoller({ enabled: false }) === null)` (the component should return null when disabled — testable without DOM). [Note: Plan 04 owns the actual component file; this test imports it only after Plan 04 ships. If Plan 04 hasn't shipped yet (it depends on this plan), this it() must STAY as it.todo until Plan 04. Update VALIDATION.md row 3 to note "covered in Plan 04 wave" if the executor cannot test before the component exists.]
  </behavior>
  <action>
    Inside `src/actions/hakedis.ts`, add (NOT as 'use server' — internal helper; the file's top-of-file 'use server' directive will still apply, so name-export the helper but keep its body callable from non-request-scoped code by NOT using auth() inside it — per Architectural Responsibility Map row 1 + Open Question 6 finding):

    Add `export async function recomputeHakedisLine(projectId: string, boqItemId: string, currencyCode: string): Promise<{ updated: boolean; periodLineId: string | null }>` with body:
      1. `const tenantId = getDefaultTenantId();`
      2. Find the open draft period for `(projectId, tenantId, currencyCode, status='draft')` ORDER BY period_end_date DESC LIMIT 1. If none → return `{ updated: false, periodLineId: null }` (D-118).
      3. Re-check `status = 'draft'` defensively (Pitfall 4 — Postgres MVCC) on the loaded row; if not draft → return same no-op (cannot happen for the path above but is defense-in-depth for shared callers).
      4. Run the v2.0 cumulative aggregation scoped to this single BOQ item — paste the SQL pattern from `recomputePeriodLines` lines 184-218 but add `AND b.id = ${boqItemId}` to the WHERE. If the result is empty (no approved submissions ≤ cutoff OR unit_price NULL → D-103) → return `{ updated: false, periodLineId: null }`.
      5. Run the previous-cumulative SELECT scoped to this boqItemId — paste pattern from lines 220-235 with `AND hpl.boq_item_id = ${boqItemId}`. previousCumulative defaults to '0' if no finalized period exists (D-99).
      6. UPSERT into `hakedis_period_lines` using `INSERT … ON CONFLICT ON CONSTRAINT "hakedis_period_lines_period_boq_unique" DO UPDATE SET cumulative_qty_approved = EXCLUDED.cumulative_qty_approved, previous_cumulative_qty = EXCLUDED.previous_cumulative_qty, period_value = EXCLUDED.period_value, cumulative_value = EXCLUDED.cumulative_value, unit_price_snapshot = EXCLUDED.unit_price_snapshot, material_snapshot = EXCLUDED.material_snapshot, unit_snapshot = EXCLUDED.unit_snapshot, currency_code_snapshot = EXCLUDED.currency_code_snapshot RETURNING id` — the UNIQUE constraint from Plan 01 is the ON CONFLICT target. Do NOT include period_qty in INSERT (GENERATED column per D-104 / Pitfall 1).
      7. Capture `periodLineId` from RETURNING.
      8. INSERT…SELECT into `hakedis_line_submissions` per 12-RESEARCH §Example 3, with the delta-only NOT EXISTS clause per Open Question 1 RESOLVED (the join row represents period_qty contributors, not cumulative). Use `ON CONFLICT (period_line_id, submission_id) DO UPDATE SET qty_contributed = EXCLUDED.qty_contributed` so D-13 audit-handler replay is idempotent.
      9. Return `{ updated: true, periodLineId }`.

    Refactor `recomputePeriodLines` body (lines 153-285):
      1. Keep the 'use server' export + auth guard + tenantId + period fetch + status='draft' check + DELETE clause as-is (lines 153-178).
      2. Replace the cumulativeResult loop (lines 184-282) with: run the same cumulative SELECT to enumerate (boqItemId, currencyCode) pairs with cumulative > 0, then loop calling `await recomputeHakedisLine(projectId, boqItemId, currencyCode)` for each.
      3. Math is identical (single body, two callers). Existing 28 hakedis.test.ts tests MUST still pass.

    Add `export async function getLineSubmissions(periodLineId: string): Promise<LineSubmission[]>` (with `'use server'` semantics inherited from the file's top-of-file directive). Body per 12-RESEARCH §Example 4: auth() first; tenantId from getDefaultTenantId(); SELECT joining hakedis_line_submissions hls → submissions s → people p with `WHERE hls.period_line_id = ${periodLineId} AND hpl.tenant_id = ${tenantId}` ORDER BY s.decided_at DESC. Return shape `{ submissionId, workerName, decidedAt, qtyContributed, photoUrl, notes }`. Export the `LineSubmission` type from this file too.

    In `tests/hakedis-live.test.ts`, replace the 9 it.todo entries with concrete it() implementations per the <behavior> block above. Each test uses seedDraftPeriod from tests/fixtures/hakedis.ts. Test 9 (LivePeriodPoller mount gate) STAYS as it.todo if the component file does not yet exist (Plan 04 ownership) — the dependency graph already covers it; the verify command in 12-VALIDATION.md just remains "pending" until Plan 04 lands.
  </action>
  <verify>
    <automated>npx vitest run tests/hakedis-live.test.ts tests/hakedis.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export async function recomputeHakedisLine" src/actions/hakedis.ts` returns 1
    - `grep -c "export async function getLineSubmissions" src/actions/hakedis.ts` returns 1
    - `grep -c "ON CONSTRAINT \"hakedis_period_lines_period_boq_unique\"" src/actions/hakedis.ts` returns 1
    - `grep -c "INSERT INTO hakedis_line_submissions" src/actions/hakedis.ts` returns 1
    - `grep -c "ON CONFLICT (period_line_id, submission_id)" src/actions/hakedis.ts` returns 1
    - `grep -c "recomputeHakedisLine(" src/actions/hakedis.ts` returns ≥ 2 (definition + at least one caller inside recomputePeriodLines)
    - `grep -c "logOfficeActivity" src/actions/hakedis.ts` count is UNCHANGED vs Phase 10 (no new call; existing calls in createPeriod/finalize/delete remain)
    - `grep -cE "it\\(" tests/hakedis-live.test.ts` returns ≥ 8 (8 concrete + 1 may stay it.todo for LivePeriodPoller)
    - `grep -cE "it\\.todo" tests/hakedis-live.test.ts` returns ≤ 1 (only LivePeriodPoller gate if component missing)
    - `npx vitest run tests/hakedis-live.test.ts -x` exits 0
    - `npx vitest run tests/hakedis.test.ts -x` exits 0 (Phase 10 no-regression)
    - `grep -c "AND b.id = " src/actions/hakedis.ts` returns ≥ 1 (scoped cumulative SELECT)
    - `grep -c "AND hpl.boq_item_id = " src/actions/hakedis.ts` returns ≥ 1 (scoped previous SELECT)
  </acceptance_criteria>
  <done>recomputeHakedisLine is the single shared helper; recomputePeriodLines calls it in a loop (zero math drift); getLineSubmissions is exported as a 'use server' Server Action; 8 of 9 hakedis-live tests pass against the test DB; the 28 Phase 10 hakedis tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire D-117 post-commit hook into handleAuditDecision approve branch</name>
  <files>src/lib/bot-audit.ts, tests/hakedis-live.test.ts</files>
  <read_first>
    - src/lib/bot-audit.ts (full file — esp. handleAuditDecision approve branch lines 402-498)
    - .planning/phases/12-submission-driven-hakkedi/12-RESEARCH.md (§Pattern 2 — post-commit hook shape; §Pitfall 1 — never inside the approve TX; §Pitfall 5 — never call logOfficeActivity)
    - .planning/phases/03-audit-loop/03-CONTEXT.md (D-13 idempotency, D-29 atomic first-wins, D-40 best-effort post-commit)
    - tests/telegram-audit.test.ts (existing 52 audit tests — must still pass after the post-commit hook is added)
  </read_first>
  <action>
    In `src/lib/bot-audit.ts handleAuditDecision`, inside the `if (action === 'approve') { ... }` block, AFTER the `await editAllSiblingMessages(submissionId, MESSAGES.auditApprovedOutcome(auditorDisplayName))` call (current line 468) and BEFORE the `try { const workerRows = await db.select(...).from(people)... }` worker-notification block (current lines 473-495), insert a try/catch'd D-117 post-commit hook with the following shape:

      1. Dynamic-imports per the file's lazy-import discipline:
         - `const { recomputeHakedisLine } = await import('@/actions/hakedis');`
         - `const { boqItems } = await import('@/db/schema/boq-items');`
         - `const { eq } = await import('drizzle-orm');`
      2. Use the already-imported top-level `db` (from `@/db`) — neon-http is fine here (no transaction) and matches the surrounding lookup style on lines 474-477.
      3. Lookup the BOQ item to obtain projectId + currencyCode:
         - `const boqRows = await db.select({ currencyCode: boqItems.currencyCode, projectId: boqItems.projectId }).from(boqItems).where(eq(boqItems.id, boqItemId));`
         - boqItemId is already in scope from `affected[0].boqItemId` captured inside the transaction (line 441).
      4. If boqRows.length === 0 → log and skip (defensive — boq item could have been deleted between approve and hook; recovery on next manual Recompute).
      5. Otherwise call `await recomputeHakedisLine(boqRows[0].projectId, boqItemId, boqRows[0].currencyCode);`
      6. Wrap the entire block in `try { ... } catch (hakErr) { console.error('[handleAuditDecision] hakkediş recompute failed for submission', submissionId, ':', hakErr); }` — D-40 best-effort + CR-02 pattern: a transient hakkediş failure must NOT propagate back to the auditor (the approval is already committed and atomically successful).

    DO NOT call logOfficeActivity from this hook (Pitfall 5 — no Auth.js session in the bot path). DO NOT extend the txDb.transaction(...) block to include the recompute (Pitfall 1 — would extend lock-window past the Telegram webhook 60s retry budget).

    Update `tests/hakedis-live.test.ts` to flesh out test 1 (D-117 trigger) so it actually invokes the audit flow rather than calling the helper directly. Two viable shapes:
      (a) Call `handleAuditDecision` directly with a mocked grammY ctx — pattern already used by tests/telegram-audit.test.ts (mock ctx via grammY transformer). Confirm hakedis_period_lines + hakedis_line_submissions both updated.
      (b) Call `recomputeHakedisLine` directly (faster, less integration surface) AND add a separate smaller test that asserts the bot-audit module imports the helper (`grep -c` style assertion via a fs.readFileSync inside vitest if needed).
    Use shape (a) if the existing tests/telegram-audit.test.ts harness pattern is reusable; otherwise fall back to (b) and add a 10th test asserting the static-import edge.
  </action>
  <verify>
    <automated>npx vitest run tests/hakedis-live.test.ts tests/telegram-audit.test.ts -x && grep -c "recomputeHakedisLine" src/lib/bot-audit.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "recomputeHakedisLine" src/lib/bot-audit.ts` returns 1
    - `grep -c "logOfficeActivity" src/lib/bot-audit.ts` returns 0 (Pitfall 5 — no new call)
    - The recompute call appears AFTER `editAllSiblingMessages` and BEFORE the worker notification block — verify via `awk '/editAllSiblingMessages/{a=NR} /recomputeHakedisLine/{b=NR} /workerRows = await db/{c=NR} END {if (a<b && b<c) print "OK"; else print "ORDER_VIOLATION"}' src/lib/bot-audit.ts` prints "OK"
    - The recompute call is inside a try/catch — verify via `awk '/try {/{flag=1} /recomputeHakedisLine/{if(flag) print "in_try"} /catch \\(hakErr\\)/{flag=0}' src/lib/bot-audit.ts` prints "in_try" at least once
    - `npx vitest run tests/hakedis-live.test.ts -x` exits 0
    - `npx vitest run tests/telegram-audit.test.ts -x` exits 0 (52 Phase 3 audit tests no-regression)
    - `npx vitest run tests/exports.test.ts -x` exits 0 (16+ Phase 11 exports no-regression — SDH-03 byte-identical export guarantee)
  </acceptance_criteria>
  <done>Auditor approval via Telegram now contributes to the matching draft hakkediş period within seconds (SDH-01); the join row exists for traceability (SDH-02); Phase 3 audit tests + Phase 10 hakedis tests + Phase 11 exports tests all still pass (SDH-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram webhook → recomputeHakedisLine | Bot path enters Server Action territory without an Auth.js session. The helper is intentionally NOT auth-guarded because its inputs (projectId, boqItemId, currencyCode) come from rows the auditor approval transaction has already authorized via the assignments/auditor check earlier in handleAuditDecision. |
| Dashboard → getLineSubmissions | Office-engineer Server Action call — must be auth() + tenant-scoped (V4 ASVS access control). |
| recomputeHakedisLine → DB | INSERT…SELECT must bind all values via Drizzle sql`` parameters (V5 ASVS input validation). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03-EOP | Elevation of Privilege | recomputeHakedisLine called without auth | mitigate | Helper is internal; the only TWO callers are (a) recomputePeriodLines which IS auth-guarded at its 'use server' entry, and (b) handleAuditDecision which has already auth'd the auditor via the assignments check earlier in the function. Helper accepts only typed string args bound via sql`` — no SQL injection surface. |
| T-12-03-IDOR | Information Disclosure | recomputeHakedisLine writes to wrong tenant's hakkediş | mitigate | tenantId comes from getDefaultTenantId() inside the helper (single-tenant MVP D-09); all SELECTs and the UPSERT include `AND tenant_id = ${tenantId}` filters. Cross-tenant write impossible. |
| T-12-03-IMM | Tampering | Finalize-during-approve race writes to finalized snapshot | mitigate | Pitfall 4: helper re-checks period.status = 'draft' before any write; SKIPs cleanly if non-draft. Postgres MVCC guarantees serialisable read-then-write within the helper's single statement scope. Verified by test 6 (Pitfall 4 finalize race). |
| T-12-03-FLOAT | Tampering | JS-side multiplication of money | mitigate | qty_contributed comes straight from submissions.quantity via INSERT…SELECT — never multiplied in JS. period_value computation stays in Postgres numeric (existing v2.0 pattern preserved). |
| T-12-03-LATENCY | Denial of Service | Recompute inside audit TX extends webhook to >60s → Telegram retries | mitigate | Hook is POST-COMMIT, not in-TX (Pitfall 1). Approve commits in <1s; the recompute fires after editAllSiblingMessages. Telegram retry fires on the original webhook which is already 200 OK by then. |
| T-12-03-LOG-FK | Tampering | Bot path calling logOfficeActivity | mitigate | Pitfall 5: explicit decision NOT to log. Verified by `grep -c "logOfficeActivity" src/lib/bot-audit.ts` returning 0. Test 7 asserts no office_activity_log row is written by recomputeHakedisLine. |
| T-12-03-SQLi | Tampering | Helper accepts arbitrary projectId/boqItemId/currencyCode strings | mitigate | All three bound via Drizzle sql`${...}` parameters — verified existing pattern across hakedis.ts and analytics.ts. No sql.raw() usage. |
| T-12-03-CR02 | Denial of Service | Hakkediş recompute failure cascades back to auditor toast | mitigate | try/catch wraps the whole hook; console.error on failure; auditor flow continues. CR-02 best-effort precedent preserved. Next approval OR manual Recompute self-heals via helper's idempotency (ON CONFLICT DO UPDATE). |
</threat_model>

<verification>
- `npx vitest run tests/hakedis-live.test.ts -x` — 8+ tests pass, 0 fail (test 9 LivePeriodPoller may stay it.todo until Plan 04)
- `npx vitest run tests/hakedis.test.ts -x` — 28 Phase 10 tests still pass (no math regression)
- `npx vitest run tests/telegram-audit.test.ts -x` — 52 Phase 3 audit tests still pass (no audit-flow regression)
- `npx vitest run tests/exports.test.ts -x` — 16+ Phase 11 export tests still pass (SDH-03 byte-identical export guarantee)
- `npx tsc --noEmit` exits 0
- `grep` audits per acceptance_criteria — call ordering, no-logOfficeActivity, sql binding hygiene
</verification>

<success_criteria>
- D-117: a Telegram approval calls recomputeHakedisLine via the post-commit hook for the matching (projectId, boqItemId, currencyCode) triplet
- D-118: no draft period → graceful no-op (no error visible to auditor or office)
- D-119: scoped recompute writes both the hakedis_period_lines UPSERT and the hakedis_line_submissions INSERT…SELECT rows
- Idempotency: re-running with the same submission produces the same final state (ON CONFLICT DO UPDATE on both target tables)
- Pitfall 4: finalize race never writes to a finalized period
- Pitfall 5: bot path never calls logOfficeActivity
- SDH-03 no-regression: Phase 10 hakedis tests + Phase 3 audit tests + Phase 11 export tests all still pass
- SDH-02: getLineSubmissions returns the expected shape for the Plan 04 UI
</success_criteria>

<output>
Create `.planning/phases/12-submission-driven-hakkedi/12-03-SUMMARY.md` when done.
</output>
