---
phase: 12-submission-driven-hakkedi
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - src/db/migrations/0009_v3_line_submissions.sql
  - src/db/migrations/meta/_journal.json
  - src/db/migrations/meta/0009_snapshot.json
autonomous: false
requirements: [SDH-01, SDH-02, SDH-03]
must_haves:
  truths:
    - "0009 migration SQL exists and exclusively CREATEs the hakedis_line_submissions table + the new UNIQUE constraint on hakedis_period_lines"
    - "Migration does NOT re-emit any DROP/CREATE statement touching hakedis_period_lines.period_qty (Pitfall 7 — the GENERATED column hand-edit must survive)"
    - "Migration applied successfully to DATABASE_URL (dev/preview Neon branch)"
    - "Migration applied successfully to TEST_DATABASE_URL (per Phase 7-02 precedent — integration tests need the same schema)"
    - "Drizzle meta journal carries the 0009 entry with the project's tag convention"
    - "Live DB query confirms hakedis_line_submissions table exists with the D-119 PK + FKs + index"
    - "Live DB query confirms hakedis_period_lines has the new UNIQUE (period_id, boq_item_id) constraint"
  artifacts:
    - path: "src/db/migrations/0009_v3_line_submissions.sql"
      provides: "Phase 12 schema migration (join table + parent UNIQUE)"
      contains: "CREATE TABLE \"hakedis_line_submissions\""
    - path: "src/db/migrations/meta/_journal.json"
      provides: "Drizzle journal entry for 0009"
      contains: "0009"
  key_links:
    - from: "src/db/migrations/0009_v3_line_submissions.sql"
      to: "live DB (hakedis_line_submissions table)"
      via: "npx tsx src/db/migrate.ts"
      pattern: "CREATE TABLE \"hakedis_line_submissions\""
    - from: "src/db/migrations/0009_v3_line_submissions.sql"
      to: "live DB (UNIQUE on hakedis_period_lines)"
      via: "npx tsx src/db/migrate.ts"
      pattern: "ADD CONSTRAINT \"hakedis_period_lines_period_boq_unique\" UNIQUE"
---

<objective>
[BLOCKING] Generate the Drizzle migration scaffold for the schema changes Plan 01 authored, hand-verify the diff matches the spec (no surprise re-emits on `hakedis_period_lines.period_qty` per Pitfall 7), rename the file to the project's v-tagged convention, and apply it to BOTH the dev DB and the test DB. This is the live-DB gate — the phase cannot pass verification without it because Wave 3's helper and integration tests both write into the new table.

Purpose: Close the well-documented Phase 7/9/10 false-positive trap where TypeScript/Drizzle types compile fine without the live schema existing. The integration tests in Wave 3 will fail at INSERT time if the table is missing — and the failure will look like a code bug, not a missing migration. We push the migration FIRST.

Output: A single SQL migration file at `src/db/migrations/0009_v3_line_submissions.sql`, the updated `meta/_journal.json`, the updated `meta/0009_snapshot.json`, and a live DB confirmation (psql or `db.execute` SELECT) that the table + UNIQUE constraint exist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-submission-driven-hakkedi/12-CONTEXT.md
@.planning/phases/12-submission-driven-hakkedi/12-RESEARCH.md
@.planning/phases/12-01-SUMMARY.md
@src/db/schema/hakedis-line-submissions.ts
@src/db/schema/hakedis-period-lines.ts
@src/db/migrations/0008_v2_hakedis_deductions.sql
@src/db/migrate.ts
@drizzle.config.ts

<interfaces>
<!-- Key contracts the executor needs. -->

From .planning/STATE.md (accumulated decisions):
- D-49: drizzle-kit push is UNUSABLE for this project (spatial_ref_sys permission error on Neon) — `npx tsx src/db/migrate.ts` is the ONLY working migration runner
- Phase 7-02 finding: TEST_DATABASE_URL requires its own migrate.ts apply (it's a separate Neon branch)
- Phase 9-03 finding: applied migrations must not be edited post-apply — drizzle migration-hash integrity is broken by post-apply edits
- Phase 10-01: 0008 was a [BLOCKING] live apply gate due to live DB changes — Phase 12-02 follows the same gate pattern

From .planning/phases/12-submission-driven-hakkedi/12-RESEARCH.md Pitfall 7:
- `drizzle-kit generate` MUST NOT re-emit DROP/ADD on `hakedis_period_lines.period_qty` (the GENERATED column declared via .generatedAlwaysAs() in Plan 01's parent — but the GENERATED column was already hand-confirmed in 0008's WR-07 precedent; if 0009 touches period_qty, hand-strip those lines before apply)
- WR-07 precedent in 0008: hand-edits applied; rename to `0009_v3_line_submissions.sql` and update meta/_journal.json `tag` field

From src/db/migrate.ts:
- Existing runner: applies postgis extension first (0000_enable_postgis.sql), then runs through drizzle-kit migrations table
- Invoked as: `npx tsx src/db/migrate.ts`
- DATABASE_URL env var is read at module top
- For test DB: prefix invocation with `DATABASE_URL=$TEST_DATABASE_URL npx tsx src/db/migrate.ts` (Phase 7-02 pattern)

From src/db/migrations/0008_v2_hakedis_deductions.sql (precedent for v-tagged rename + hand-edit):
- File name pattern: `0008_v2_hakedis_deductions.sql`
- Phase 12 equivalent: `0009_v3_line_submissions.sql` (v3 prefix = milestone v3.0)
- meta/_journal.json entry updated to match the new tag

CRITICAL — SQL comment syntax:
- Postgres SQL line comments use `--`, NOT `#`. Any grep filter intended to skip comment lines in SQL migrations MUST use `grep -vE '^--'`. A `grep -v '^#'` filter is a no-op against SQL files (false negative — comments are NOT stripped, so a header comment mentioning "period_qty" would slip past the gate).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate migration scaffold and hand-verify Pitfall 7 (no period_qty re-emit)</name>
  <files>src/db/migrations/0009_v3_line_submissions.sql, src/db/migrations/meta/_journal.json, src/db/migrations/meta/0009_snapshot.json</files>
  <read_first>
    - .planning/phases/12-submission-driven-hakkedi/12-RESEARCH.md (§Pitfall 7 — diff inspection before apply; §Example 2 — exact bash sequence)
    - src/db/migrations/0008_v2_hakedis_deductions.sql (precedent for v-tagged rename + WR-07 hand-edit conventions)
    - src/db/migrations/meta/_journal.json (existing journal entries — examine the `tag` field shape for 0007/0008 to know what to set for 0009)
    - src/db/schema/hakedis-line-submissions.ts (target schema — Plan 01 output)
    - src/db/schema/hakedis-period-lines.ts (parent table — added UNIQUE per Plan 01)
  </read_first>
  <action>
    Run `npx drizzle-kit generate` from the repo root. Drizzle will emit a new file `src/db/migrations/0009_<adjective>_<noun>.sql` and update `meta/_journal.json` + create `meta/0009_snapshot.json`.

    [HALT CONDITION — explicit] Drizzle assigns the next sequential index based on the contents of `meta/_journal.json`. Because Plan 01 + Plan 02 add no other migration files between 0008 and the new one, the generated index MUST be 9. If `drizzle-kit generate` writes a file numbered something other than 0009 (e.g., it sees a stray dev migration and emits 0010), HALT immediately, surface the situation to the user, and do NOT silently renumber the file. Renumbering breaks meta/_journal.json hash integrity (Phase 9-03 finding). Recovery: revert generated artefacts (`git checkout src/db/migrations/`), reconcile any stray migration with the user, then re-run generate.

    Open the generated SQL and verify Pitfall 7 explicitly: search for any occurrence of `period_qty`, `hakedis_period_lines`, `DROP`, `GENERATED ALWAYS AS`, or `cumulative_qty_approved - previous_cumulative_qty`. The ONLY legitimate references to `hakedis_period_lines` in this migration are:
      (1) `ALTER TABLE "hakedis_period_lines" ADD CONSTRAINT "hakedis_period_lines_period_boq_unique" UNIQUE("period_id", "boq_item_id");` (the Open Question 4 UNIQUE)
      (2) Possibly nothing else.
    Any DROP/RECREATE of `period_qty` or the GENERATED expression MUST be deleted from the SQL (Pitfall 7 — the 0008 hand-edits are LIVE and cannot be lost). If found, strip those statements from the file.

    Confirm the join-table CREATE block matches the D-119 spec exactly:
      - CREATE TABLE "hakedis_line_submissions" ( "tenant_id" uuid, "period_line_id" uuid NOT NULL, "submission_id" uuid NOT NULL, "qty_contributed" numeric(12, 3) NOT NULL, "created_at" timestamp with time zone DEFAULT now() NOT NULL, CONSTRAINT "hakedis_line_submissions_period_line_id_submission_id_pk" PRIMARY KEY("period_line_id","submission_id") );
      - The three FK ALTERs: tenant_id → tenants(id), period_line_id → hakedis_period_lines(id) ON DELETE cascade, submission_id → submissions(id) ON DELETE restrict.
      - CREATE INDEX "hakedis_line_submissions_submission_idx" ON "hakedis_line_submissions" USING btree ("submission_id");

    Rename the file to `0009_v3_line_submissions.sql` (per Phase 10/11 v-tagged precedent). Update `meta/_journal.json`: in the entries array, the entry for index 9 must have `"tag": "0009_v3_line_submissions"`. Do not touch other journal entries.

    Do NOT apply yet — Task 2 (a blocking-human checkpoint) reviews before any live DB change.
  </action>
  <verify>
    <automated>test -f src/db/migrations/0009_v3_line_submissions.sql && grep -c "CREATE TABLE \"hakedis_line_submissions\"" src/db/migrations/0009_v3_line_submissions.sql && grep -c "hakedis_period_lines_period_boq_unique" src/db/migrations/0009_v3_line_submissions.sql && [ "$(grep -vE '^--' src/db/migrations/0009_v3_line_submissions.sql | grep -cE 'DROP.*period_qty|GENERATED ALWAYS AS')" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - File `src/db/migrations/0009_v3_line_submissions.sql` exists (file was generated as 0009 — if drizzle picked a different index, executor halted per HALT CONDITION above)
    - `grep -c 'CREATE TABLE "hakedis_line_submissions"' src/db/migrations/0009_v3_line_submissions.sql` returns 1
    - `grep -c 'PRIMARY KEY("period_line_id","submission_id")' src/db/migrations/0009_v3_line_submissions.sql` returns 1
    - `grep -c 'ON DELETE cascade' src/db/migrations/0009_v3_line_submissions.sql` returns ≥ 1 (period_line_id FK)
    - `grep -c 'ON DELETE restrict' src/db/migrations/0009_v3_line_submissions.sql` returns ≥ 1 (submission_id FK)
    - `grep -c 'hakedis_period_lines_period_boq_unique' src/db/migrations/0009_v3_line_submissions.sql` returns 1
    - `grep -c 'hakedis_line_submissions_submission_idx' src/db/migrations/0009_v3_line_submissions.sql` returns 1
    - `grep -vE '^--' src/db/migrations/0009_v3_line_submissions.sql | grep -cE 'DROP.*period_qty|GENERATED ALWAYS AS'` returns 0 (Pitfall 7 verification — SQL comment-aware filter; `--` is the correct comment prefix for Postgres SQL)
    - `grep -c '"tag": "0009_v3_line_submissions"' src/db/migrations/meta/_journal.json` returns 1
  </acceptance_criteria>
  <done>The 0009 migration scaffold exists at index 9 (executor halted if not), contains exactly the spec'd D-119 join-table CREATE + the Open Question 4 UNIQUE ALTER on hakedis_period_lines, has been renamed to the v3 convention, the meta journal carries the matching tag entry, and the diff is FREE of any stray period_qty DROP/RECREATE statements.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Human review of 0009 migration diff before live apply</name>
  <what-built>
    Drizzle scaffold for 0009: CREATE TABLE hakedis_line_submissions (D-119) + ALTER hakedis_period_lines ADD UNIQUE (period_id, boq_item_id) (Open Question 4). Hand-renamed to 0009_v3_line_submissions.sql; meta journal updated. NOT applied to any DB yet.
  </what-built>
  <how-to-verify>
    1. Open `src/db/migrations/0009_v3_line_submissions.sql` in an editor.
    2. Confirm visually that the file contains:
       (a) A single CREATE TABLE "hakedis_line_submissions" statement with 5 columns + composite PRIMARY KEY (period_line_id, submission_id).
       (b) Three ALTER TABLE "hakedis_line_submissions" ADD CONSTRAINT statements for the FKs (tenants, hakedis_period_lines ON DELETE cascade, submissions ON DELETE restrict).
       (c) One CREATE INDEX "hakedis_line_submissions_submission_idx".
       (d) One ALTER TABLE "hakedis_period_lines" ADD CONSTRAINT "hakedis_period_lines_period_boq_unique" UNIQUE("period_id", "boq_item_id").
       (e) NOTHING ELSE — in particular NO references to period_qty, no DROP/RECREATE of the GENERATED column (Pitfall 7).
    3. Open `src/db/migrations/meta/_journal.json` and confirm the entry for index 9 has `"tag": "0009_v3_line_submissions"`.
    4. Confirm DATABASE_URL points at the expected dev/preview Neon branch (run `echo $DATABASE_URL | sed 's/postgres.*@//'` to see only the host — no password leak).
    5. Type "approved" to proceed with `npx tsx src/db/migrate.ts` against DATABASE_URL.
    6. After dev DB apply: confirm TEST_DATABASE_URL also points at a dedicated Neon branch (Phase 7-02 pattern); type "approved-test" to proceed with the second apply against TEST_DATABASE_URL.
    7. If anything looks wrong: type the specific problem (e.g., "period_qty DROP present on line 24" / "TEST_DATABASE_URL not set" / "wrong tag in journal" / "generated index was 10 not 9 — Task 1 halt").
  </how-to-verify>
  <resume-signal>Type "approved" (dev apply) AND "approved-test" (test DB apply) after both checks pass, OR describe the issue to halt and fix.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Apply 0009 migration to dev DB and test DB; live-DB verification</name>
  <files>src/db/migrations/0009_v3_line_submissions.sql, src/db/migrations/meta/_journal.json</files>
  <read_first>
    - .planning/STATE.md (Phase 7-02 finding: TEST_DATABASE_URL requires its own migrate.ts apply)
    - src/db/migrate.ts (runner — reads DATABASE_URL at module load)
    - .planning/phases/12-submission-driven-hakkedi/12-RESEARCH.md (§Example 2 — exact apply command sequence)
  </read_first>
  <action>
    PRECONDITION: Task 1 has produced the file at the expected `0009_v3_line_submissions.sql` path. If the executor reached Task 3 with the file at a different index (e.g., 0010), HALT immediately and surface to the user — Task 3 must NOT silently apply a misnumbered migration. Recovery: revert generated artefacts and restart Task 1.

    Run `npx tsx src/db/migrate.ts` to apply against DATABASE_URL. Confirm stdout shows the 0009 file applied and no errors.

    Run `DATABASE_URL=$TEST_DATABASE_URL npx tsx src/db/migrate.ts` (Phase 7-02 pattern) to apply against the test branch. If TEST_DATABASE_URL is unset, halt with a clear error message — Wave 3's integration tests cannot run without it.

    Run a live-DB verification query against DATABASE_URL using `npx tsx -e` with a small Neon HTTP client snippet (or `node -e` if the project ships `pg` directly). The query MUST confirm BOTH:
      (a) The hakedis_line_submissions table exists and the PK + the two FKs report correctly via information_schema.table_constraints joined with information_schema.constraint_column_usage.
      (b) The hakedis_period_lines table now has a UNIQUE constraint named hakedis_period_lines_period_boq_unique on (period_id, boq_item_id).
    Capture the query output to a transient file and grep-assert both findings.

    Do NOT touch the meta journal or the SQL after apply — Phase 9-03 finding: post-apply edits break drizzle migration-hash integrity.
  </action>
  <verify>
    <automated>npx tsx -e "import('@neondatabase/serverless').then(async ({neon}) => { const sql = neon(process.env.DATABASE_URL); const r = await sql\`SELECT table_name FROM information_schema.tables WHERE table_name='hakedis_line_submissions'\`; const u = await sql\`SELECT constraint_name FROM information_schema.table_constraints WHERE table_name='hakedis_period_lines' AND constraint_name='hakedis_period_lines_period_boq_unique'\`; if (r.length!==1 || u.length!==1) { console.error('MISSING', {tables: r, unique: u}); process.exit(1); } console.log('OK'); });"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx src/db/migrate.ts` exits 0 against DATABASE_URL
    - `DATABASE_URL=$TEST_DATABASE_URL npx tsx src/db/migrate.ts` exits 0 against TEST_DATABASE_URL
    - The verify command above prints "OK" exactly once (table + UNIQUE both confirmed in the live dev DB)
    - The SAME verification command, re-run with `DATABASE_URL=$TEST_DATABASE_URL`, also prints "OK"
    - No edits to `0009_v3_line_submissions.sql` or `meta/_journal.json` after the apply (post-apply edits break migration hash — Phase 9-03 finding)
  </acceptance_criteria>
  <done>Both dev DB and test DB have the 0009 migration applied; live information_schema confirms the join table exists with the correct PK + FKs and the parent table has the new UNIQUE constraint; Wave 3 can now write into and query the join table without false-positive type-check passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration SQL → live DB | This is the live-DB schema-change boundary. Once applied, irreversible without a follow-up migration. |
| Local CLI → Neon branch | DATABASE_URL credentials authenticate the schema change; treated as a privileged operation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02-PIT7 | Tampering | 0009 SQL touching hakedis_period_lines.period_qty | mitigate | Pitfall 7: hand-strip any DROP/RECREATE of period_qty BEFORE the blocking-human checkpoint applies. Verified via `grep -vE '^--' file | grep -cE 'DROP.*period_qty|GENERATED ALWAYS AS'` returning 0 — uses the correct Postgres SQL `--` comment prefix so header comments cannot self-invalidate the gate. |
| T-12-02-IDX | Tampering | drizzle-kit picks an unexpected migration index | mitigate | Task 1 + Task 3 both enforce an explicit HALT CONDITION if the generated index ≠ 9. Silent renumbering would break meta/_journal.json hash integrity (Phase 9-03 finding). Recovery is `git checkout` of generated artefacts and re-run after reconciliation. |
| T-12-02-FK | Tampering | onDelete clauses in 0009 | mitigate | Spec'd in the migration text per D-119 — period_line_id cascade (D-97 draft delete) + submission_id restrict (snapshot durability + Pitfall 8). Inspector confirms during Task 2 checkpoint. |
| T-12-02-CRED | Information Disclosure | DATABASE_URL / TEST_DATABASE_URL in shell | mitigate | Task 2 checkpoint requires the human to confirm the host (echo with sed-masked password) rather than print the URL. CI/automation runs never log full URLs. |
| T-12-02-INT | Integrity | drizzle migration-hash check | mitigate | Phase 9-03 finding: NEVER edit a SQL migration after apply. The 0009 file is read-only post-Task-3. |
| T-12-02-SC | Tampering | (no new packages installed in Phase 12) | accept | Phase 12 installs zero new dependencies (12-RESEARCH §Package Legitimacy Audit). No supply-chain surface introduced. |
</threat_model>

<verification>
- 0009 SQL exists with the four expected statements (CREATE TABLE + 3 ALTER FK + CREATE INDEX + parent UNIQUE ALTER) and nothing else
- `npx tsx src/db/migrate.ts` exits 0 on dev DB
- `DATABASE_URL=$TEST_DATABASE_URL npx tsx src/db/migrate.ts` exits 0 on test DB
- information_schema query confirms both the new table and the new UNIQUE constraint on both DBs
- Migration file untouched after apply
</verification>

<success_criteria>
- Drizzle-generated, hand-renamed, Pitfall-7-clean migration file shipped
- Live DB (dev branch): hakedis_line_submissions table exists with composite PK + cascade/restrict FKs + reverse index
- Live DB (dev branch): hakedis_period_lines now has UNIQUE (period_id, boq_item_id) — D-117 UPSERT target ready
- Test DB: same schema applied (Phase 7-02 pattern)
- meta/_journal.json carries the 0009_v3_line_submissions tag
</success_criteria>

<output>
Create `.planning/phases/12-submission-driven-hakkedi/12-02-SUMMARY.md` when done.
</output>
