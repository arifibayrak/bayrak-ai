---
phase: 10-hakkedi-billing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema/hakedis-periods.ts
  - src/db/schema/hakedis-period-lines.ts
  - src/db/schema/office-activity-log.ts
  - src/db/migrations/0008_v2_hakedis_deductions.sql
  - src/db/migrations/meta/_journal.json
  - tests/schema.test.ts
  - tests/hakedis.test.ts
  - messages/en.json
  - messages/tr.json
  - src/components/ui/switch.tsx
autonomous: false
requirements: [HAK-01, HAK-02, HAK-03, HAK-05]
tags: [hakedis, billing, migration, schema, postgis, drizzle]

must_haves:
  truths:
    - "hakedis_periods has tevkifat_fraction, stopaj_enabled, stopaj_rate, avans_kesintisi_rate columns in the live DB"
    - "hakedis_period_lines.period_qty is a GENERATED ALWAYS AS (cumulative_qty_approved - previous_cumulative_qty) STORED column in the live DB"
    - "An INSERT into hakedis_period_lines that supplies period_qty explicitly is rejected by Postgres"
    - "OFFICE_ACTION_TYPES includes 'hakedis_period_deleted'"
    - "tests/hakedis.test.ts exists as a Wave 0 scaffold that the suite can run"
    - "dashboard.admin.hakedis.* keys exist in both messages/en.json and messages/tr.json"
  artifacts:
    - path: "src/db/migrations/0008_v2_hakedis_deductions.sql"
      provides: "Deduction columns + GENERATED period_qty migration"
      contains: "GENERATED ALWAYS AS"
    - path: "src/db/schema/hakedis-periods.ts"
      provides: "Four new deduction columns in Drizzle schema"
      contains: "tevkifat_fraction"
    - path: "src/db/schema/hakedis-period-lines.ts"
      provides: "periodQty as generatedAlwaysAs"
      contains: "generatedAlwaysAs"
    - path: "src/components/ui/switch.tsx"
      provides: "shadcn Switch component for stopaj toggle"
    - path: "tests/hakedis.test.ts"
      provides: "Wave 0 test scaffold for HAK-01..HAK-05"
  key_links:
    - from: "src/db/schema/hakedis-period-lines.ts"
      to: "0008_v2_hakedis_deductions.sql"
      via: "generatedAlwaysAs schema definition matches GENERATED ALWAYS AS STORED DDL"
      pattern: "cumulative_qty_approved - previous_cumulative_qty"
    - from: "src/db/migrations/meta/_journal.json"
      to: "0008_v2_hakedis_deductions"
      via: "journal entry idx 8 references the new migration tag"
      pattern: "0008_v2_hakedis_deductions"
---

<objective>
Extend the existing (Phase 7) hakkediş schema with the four missing per-period deduction
columns (D-91) and convert `period_qty` to a database-GENERATED column (D-104), apply the
migration to the live database the project's established way (D-49), and lay the Wave 0 test
scaffold so all downstream waves have automated verification.

Purpose: The tables exist but are missing the deduction columns and the DB-level arithmetic
guarantee. Without the applied migration, build/type checks pass against schema config (not the
live DB) — a false-positive verification trap. This plan closes that trap.

Output: Updated schema files, migration 0008, applied live DB changes, the new `Switch`
component, the full `dashboard.admin.hakedis.*` i18n namespace, and `tests/hakedis.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-hakkedi-billing/10-CONTEXT.md
@.planning/phases/10-hakkedi-billing/10-RESEARCH.md
@.planning/phases/10-hakkedi-billing/10-UI-SPEC.md

<interfaces>
<!-- Existing schema the executor extends. Do NOT recreate these tables. -->

From src/db/schema/hakedis-periods.ts (existing — ADD 4 columns):
- HAKEDIS_STATUSES = ['draft','finalized','submitted','paid'] (exported const + HakedisStatus type)
- existing rate columns: kdvRate numeric(5,4) default '0.2000', retentionRate numeric(5,4) default '0.0500'
- the file already has a COMMENTED placeholder line for tevkifatFraction (lines 27-28) — replace it with the real column.

From src/db/schema/hakedis-period-lines.ts (existing — CONVERT periodQty):
- periodQty: numeric('period_qty', { precision: 12, scale: 3 }).notNull()  ← becomes GENERATED
- WR-03 comment (lines 35-46) documents this exact change; remove/replace the comment to reflect it is now DONE.
- existing CHECK in DB: hakedis_period_lines_cumulative_check (cumulative >= previous, from 0004) — REMAINS.
- existing CHECK in DB: hakedis_period_lines_period_qty_nonneg (period_qty >= 0, from 0006) — auto-dropped with the column.

From src/db/schema/office-activity-log.ts (existing — ADD one literal):
- OFFICE_ACTION_TYPES const array already has 'hakedis_period_created' and 'hakedis_period_finalized', NOT 'hakedis_period_deleted'.

From src/db/migrate.ts (the migration runner — do NOT modify):
- runs 0000_enable_postgis.sql first, then migrate(db, { migrationsFolder: 'src/db/migrations' }).
- apply command: `npx tsx src/db/migrate.ts` (reads .env.local DATABASE_URL).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Edit schema files + add Switch + i18n namespace + Wave 0 scaffold</name>
  <files>src/db/schema/hakedis-periods.ts, src/db/schema/hakedis-period-lines.ts, src/db/schema/office-activity-log.ts, src/components/ui/switch.tsx, messages/en.json, messages/tr.json, tests/hakedis.test.ts</files>
  <read_first>
    - src/db/schema/hakedis-periods.ts (the file being edited; note the commented tevkifatFraction placeholder at lines 27-28 and existing kdvRate/retentionRate columns)
    - src/db/schema/hakedis-period-lines.ts (the file being edited; note periodQty line 47 + WR-03 comment lines 35-46)
    - src/db/schema/office-activity-log.ts (OFFICE_ACTION_TYPES array — append one literal)
    - src/db/schema/tenant-settings.ts (precedent for numeric(5,4) Drizzle column with default string literal)
    - .planning/phases/10-hakkedi-billing/10-UI-SPEC.md §"i18n Namespace Contract" + §"Copywriting Contract" (every TR/EN key + value)
    - messages/en.json + messages/tr.json (find the existing dashboard.admin.settings.* namespace as the format precedent; the existing dashboard.admin.stubs.hakedis_* keys stay)
    - tests/fixtures/db.ts (describeIfDb + getTestDb + truncateAllTables exports)
    - tests/schema.test.ts (Money-Math Test 5 import + fixture style — reuse the same imports for the scaffold)
  </read_first>
  <action>
    In hakedis-periods.ts: add columns `tevkifatFraction: numeric('tevkifat_fraction', { precision: 5, scale: 4 })` (nullable), `stopajEnabled: boolean('stopaj_enabled').notNull().default(false)` (import `boolean` from drizzle-orm/pg-core), `stopajRate: numeric('stopaj_rate', { precision: 5, scale: 4 })` (nullable), `avansKesintisiRate: numeric('avans_kesintisi_rate', { precision: 5, scale: 4 }).notNull().default('0.0000')`. Replace the commented tevkifatFraction placeholder with the real column. Keep column ordering near kdvRate/retentionRate.
    In hakedis-period-lines.ts: import `sql` from 'drizzle-orm'; change periodQty to `numeric('period_qty', { precision: 12, scale: 3 }).generatedAlwaysAs(sql\`cumulative_qty_approved - previous_cumulative_qty\`)` (drop `.notNull()` — GENERATED columns are not insertable). Replace the WR-03 deferral comment with a note that the identity is now DB-enforced via the GENERATED column (D-104).
    In office-activity-log.ts: append `'hakedis_period_deleted'` to the OFFICE_ACTION_TYPES array (TS-only, no migration — column is text).
    Add the Switch component via `node_modules/.bin/shadcn add switch` (NOT `npx shadcn@latest` — D-Phase8 finding). Creates src/components/ui/switch.tsx.
    In messages/en.json and messages/tr.json: add the full `dashboard.admin.hakedis` namespace with every key from the UI-SPEC i18n contract: heading, subtitle, empty_heading, empty_body, col_period, col_end_date, col_currency, col_status, col_net_payment, col_actions, create_cta, open_link, delete_link, status_draft, status_finalized, status_submitted, status_paid, a `form` object (period_label, start_date, end_date, currency, kdv_rate, tevkifat_rate, stopaj_label, stopaj_rate, teminat_rate, avans_rate, deduction_rates_heading, create_compute, discard, err_missing_end_date, err_missing_label, err_general), a `detail` object (back, end_date_label, finalized_notice, unpriced_warning, unpriced_cta, recompute, finalize, delete, mark_submitted, mark_paid, lines_heading, lines_empty, gross_total, summary_heading, summary_period, row_gross, row_kdv, row_tevkifat, row_stopaj, row_teminat, row_avans, row_net, err_finalize_blocked), a `finalize_dialog` object (title, body, cancel, confirm), and a `delete_dialog` object (title, body, cancel, confirm). Use the exact TR and EN strings from the UI-SPEC Copywriting Contract table.
    Create tests/hakedis.test.ts as a Wave 0 scaffold: import describeIfDb/getTestDb/truncateAllTables from fixtures; declare `describeIfDb('Phase 10 hakedis billing', ...)` with placeholder `it.todo()` entries naming each HAK behavior (createPeriod inserts draft + computes lines; cumulative aggregation with Istanbul cutoff; previous_cumulative from latest finalized; deduction chain values; updatePaymentStatus transitions; finalizePeriod immutability; recompute throws on non-draft). The scaffold must compile and run (todos are fine) so downstream tasks fill them in.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "hakedis\|switch\|office-activity" || echo "TYPECHECK CLEAN"; node -e "const en=require('./messages/en.json'),tr=require('./messages/tr.json'); const ek=Object.keys(en.dashboard.admin.hakedis), tk=Object.keys(tr.dashboard.admin.hakedis); if(JSON.stringify(ek.sort())!==JSON.stringify(tk.sort())){console.error('TR/EN key mismatch');process.exit(1)} console.log('i18n keys parity OK:',ek.length)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^[[:space:]]*//' src/db/schema/hakedis-periods.ts | grep -c "tevkifat_fraction"` returns >= 1 (uncommented column present)
    - src/db/schema/hakedis-periods.ts contains `stopaj_enabled`, `stopaj_rate`, and `avans_kesintisi_rate` as real (uncommented) columns
    - src/db/schema/hakedis-period-lines.ts contains `generatedAlwaysAs` and the expression `cumulative_qty_approved - previous_cumulative_qty`
    - src/db/schema/hakedis-period-lines.ts periodQty definition does NOT contain `.notNull()`
    - src/db/schema/office-activity-log.ts OFFICE_ACTION_TYPES contains `'hakedis_period_deleted'`
    - src/components/ui/switch.tsx exists and exports `Switch`
    - `node -e` parity check above exits 0 (TR and EN hakedis namespaces have identical key sets)
    - tests/hakedis.test.ts exists and `npx vitest run tests/hakedis.test.ts` exits 0 (todos count as passing)
    - `npx tsc --noEmit` reports no errors in the four edited TS files
  </acceptance_criteria>
  <done>Schema files carry the four deduction columns + GENERATED periodQty; OFFICE_ACTION_TYPES has the delete literal; Switch is installed; the full hakedis i18n namespace exists in both locales with key parity; the Wave 0 test scaffold runs green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author migration 0008 + update Money-Math Test 5 for GENERATED column</name>
  <files>src/db/migrations/0008_v2_hakedis_deductions.sql, src/db/migrations/meta/_journal.json, tests/schema.test.ts</files>
  <read_first>
    - src/db/migrations/0006_v2_period_qty_check.sql (precedent: hand-written CHECK migration + the period_qty_nonneg constraint that auto-drops with the column + the --> statement-breakpoint marker)
    - src/db/migrations/0007_v2_tenant_settings.sql (precedent: hand-edited migration header comment + WARNING-do-not-regenerate note + statement-breakpoint usage)
    - src/db/migrations/0004_v2_data_foundation.sql (the cumulative_check that MUST remain; lines 1-20 show the table DDL + hand-verified CHECK note)
    - src/db/migrations/meta/_journal.json (the entries array — last entry is idx 7, tag 0007_v2_tenant_settings; version "7" dialect postgresql)
    - .planning/phases/10-hakkedi-billing/10-RESEARCH.md §"Migration 0008 — Required DDL" (exact Part A + Part B SQL) and §"Common Pitfalls" 1+2 (GENERATED INSERT + drizzle-kit DDL emission)
    - tests/schema.test.ts lines 235-387 (Money-Math Test 5 — three its currently INSERT periodQty explicitly)
  </read_first>
  <behavior>
    - DB integration: after migration applied, an INSERT into hakedis_period_lines supplying period_qty explicitly throws (column can only be updated to DEFAULT).
    - DB integration: an INSERT supplying only cumulative_qty_approved + previous_cumulative_qty auto-computes period_qty = cumulative - previous (read back via RETURNING).
    - DB integration: the cumulative_check (cumulative >= previous) still rejects an INSERT where cumulative < previous (the existing reject test stays green WITHOUT supplying periodQty).
  </behavior>
  <action>
    Run `npx drizzle-kit generate` to produce a baseline SQL for the schema changes from Task 1. Inspect the generated file for the period_qty DDL: the correct form is `"period_qty" numeric(12, 3) GENERATED ALWAYS AS (cumulative_qty_approved - previous_cumulative_qty) STORED`. drizzle-kit cannot ALTER a column to GENERATED in place — it must `DROP COLUMN period_qty` then `ADD COLUMN period_qty ... GENERATED ALWAYS AS (...) STORED`. Hand-edit the file to: Part A — `ALTER TABLE "hakedis_periods" ADD COLUMN "tevkifat_fraction" numeric(5,4), ADD COLUMN "stopaj_enabled" boolean NOT NULL DEFAULT false, ADD COLUMN "stopaj_rate" numeric(5,4), ADD COLUMN "avans_kesintisi_rate" numeric(5,4) NOT NULL DEFAULT '0.0000'`; Part B — `ALTER TABLE "hakedis_period_lines" DROP COLUMN "period_qty"` then `ALTER TABLE "hakedis_period_lines" ADD COLUMN "period_qty" numeric(12,3) GENERATED ALWAYS AS (cumulative_qty_approved - previous_cumulative_qty) STORED`. Separate every statement with `--> statement-breakpoint` (neon-http cannot run multiple commands per prepared statement — Phase 7-02 finding). Add a header comment block matching 0006/0007 precedent (HAND-WRITTEN note + D-91/D-104 reference + "the period_qty_nonneg CHECK auto-drops with the column; cumulative_check remains" + the WARNING-do-not-regenerate line). Rename the file to `0008_v2_hakedis_deductions.sql`. Add a journal entry to meta/_journal.json: idx 8, version "7", a `when` timestamp greater than 1779891756000, tag "0008_v2_hakedis_deductions", breakpoints true.
    Update tests/schema.test.ts Money-Math Test 5: in the reject test, REMOVE `periodQty: '-50.000'` from the INSERT values object (the GENERATED column cannot accept it; the cumulative_check rejects the bad row regardless). In the two allow tests, REMOVE `periodQty` from the INSERT values, and add an assertion that the returned `line.periodQty` equals the auto-computed value (`'0.000'` for the boundary case where cumulative==previous, `'100.000'` for the normal case where 200-100). Update the describe label/comment to note period_qty is now GENERATED (Phase 10 / D-104).
  </action>
  <verify>
    <automated>grep -c "GENERATED ALWAYS AS" src/db/migrations/0008_v2_hakedis_deductions.sql; node -e "const j=require('./src/db/migrations/meta/_journal.json'); const last=j.entries[j.entries.length-1]; if(last.tag!=='0008_v2_hakedis_deductions'){console.error('journal not updated');process.exit(1)} console.log('journal OK idx',last.idx)"; grep -c "periodQty" tests/schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "GENERATED ALWAYS AS" src/db/migrations/0008_v2_hakedis_deductions.sql` returns >= 1
    - 0008 SQL contains `DROP COLUMN "period_qty"` AND `ADD COLUMN "period_qty"` with `STORED`
    - 0008 SQL contains all four: tevkifat_fraction, stopaj_enabled, stopaj_rate, avans_kesintisi_rate
    - 0008 SQL contains at least one `--> statement-breakpoint` marker
    - 0008 SQL header contains a WARNING line about not re-running drizzle-kit generate (matches 0006/0007 precedent)
    - meta/_journal.json last entry tag == "0008_v2_hakedis_deductions" and idx == 8
    - tests/schema.test.ts Money-Math Test 5 INSERTs no longer supply `periodQty` (the reject test's INSERT object has no periodQty key)
    - `npx tsc --noEmit` reports no errors in tests/schema.test.ts
  </acceptance_criteria>
  <done>Migration 0008 exists with hand-verified GENERATED DDL + deduction columns + statement-breakpoints + journal entry; Money-Math Test 5 updated to never INSERT period_qty and asserts the GENERATED value.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Apply migration 0008 to the live database</name>
  <action>Apply migration 0008 to the live Neon database via `npx tsx src/db/migrate.ts` (D-49 — NOT drizzle-kit push), and to the separate TEST_DATABASE_URL Neon branch the same way, then confirm the schema landed via information_schema and the GENERATED-column tests pass. See how-to-verify for the exact steps and acceptance checks.</action>
  <what-built>
    Migration 0008 (Task 2) is authored but NOT yet applied to the live Neon database. Build and
    type checks pass without it (types come from schema config, not the live DB) — applying it is
    mandatory before any verification can be trusted. The apply uses the project's established path
    (D-49: NOT drizzle-kit push).
  </what-built>
  <how-to-verify>
    1. Apply the migration: run `npx tsx src/db/migrate.ts` from the repo root. Expected output ends with "Migrations complete".
    2. Confirm the deduction columns exist on the live DB. Run a one-off query (via `npx tsx` or psql) selecting from information_schema.columns WHERE table_name='hakedis_periods' AND column_name IN ('tevkifat_fraction','stopaj_enabled','stopaj_rate','avans_kesintisi_rate') — expect 4 rows.
    3. Confirm period_qty is GENERATED. Query information_schema.columns WHERE table_name='hakedis_period_lines' AND column_name='period_qty' — expect is_generated='ALWAYS' and generation_expression referencing cumulative_qty_approved - previous_cumulative_qty.
    4. Run the GENERATED-column tests against the live test DB: `npx vitest run tests/schema.test.ts` — Money-Math Test 5 must be green (no period_qty INSERT; auto-computed value asserted). Note: TEST_DATABASE_URL is a separate Neon branch — apply 0008 there too (set DATABASE_URL=$TEST_DATABASE_URL and re-run `npx tsx src/db/migrate.ts`) before running the integration tests (Phase 7-02 precedent: the test branch needs its own apply).
    5. Confirm an explicit period_qty INSERT is rejected: the updated Money-Math Test 5 covers this; if running ad-hoc, an INSERT supplying period_qty must error with "column \"period_qty\" can only be updated to DEFAULT".
  </how-to-verify>
  <acceptance_criteria>
    - `npx tsx src/db/migrate.ts` prints "Migrations complete" and exits 0
    - information_schema shows 4 new deduction columns on hakedis_periods
    - information_schema shows hakedis_period_lines.period_qty with is_generated = 'ALWAYS'
    - `npx vitest run tests/schema.test.ts` passes (Money-Math Test 5 green) against the test DB with 0008 applied
    - An INSERT supplying period_qty explicitly is rejected by Postgres
  </acceptance_criteria>
  <resume-signal>Type "applied" after the migration runs clean on both the primary and test Neon branches and the schema tests pass, or describe the failure (e.g. drizzle-kit emitted VIRTUAL not STORED — hand-edit and re-run).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration script → live DB | DDL changes to a table holding legally-significant financial snapshots |
| schema config → live DB | TS schema types can drift from applied DB structure (false-positive trap) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-T | Tampering | hakedis_period_lines.period_qty | mitigate | GENERATED ALWAYS AS STORED makes period_qty non-writable — no code path can write a quantity that disagrees with cumulative−previous (D-104, WR-03) |
| T-10-01-I | Information Disclosure | schema/live-DB drift | mitigate | [BLOCKING] Task 3 applies + verifies via information_schema before any downstream verification trusts the build |
| T-10-01-DI | Denial of integrity | cumulative_check vs dropped period_qty_nonneg | accept | period_qty_nonneg auto-drops with the column; the retained cumulative_check (cumulative ≥ previous) mathematically guarantees the GENERATED period_qty ≥ 0 |
| T-10-01-SC | Tampering | npm/shadcn installs | mitigate | Switch sourced from official shadcn registry via node_modules/.bin/shadcn (RESEARCH Package Legitimacy Audit: no new npm packages; no [ASSUMED]/[SUS]/[SLOP]) |
</threat_model>

<verification>
- `npx vitest run tests/schema.test.ts` green (GENERATED column identity, no explicit period_qty INSERT)
- `npx vitest run tests/hakedis.test.ts` green (Wave 0 scaffold runs)
- `npx tsc --noEmit` clean across the edited TS files
- information_schema confirms 4 new columns on hakedis_periods + GENERATED period_qty
- i18n key parity between en.json and tr.json hakedis namespaces
</verification>

<success_criteria>
- hakedis_periods carries tevkifat_fraction, stopaj_enabled, stopaj_rate, avans_kesintisi_rate in the live DB
- hakedis_period_lines.period_qty is GENERATED ALWAYS AS (cumulative_qty_approved - previous_cumulative_qty) STORED in the live DB
- An explicit period_qty INSERT is rejected; auto-computed value is correct
- OFFICE_ACTION_TYPES includes 'hakedis_period_deleted'
- Switch component installed; full hakedis i18n namespace present in both locales
- tests/hakedis.test.ts scaffold exists and runs green
</success_criteria>

<output>
Create `.planning/phases/10-hakkedi-billing/10-01-SUMMARY.md` when done
</output>
