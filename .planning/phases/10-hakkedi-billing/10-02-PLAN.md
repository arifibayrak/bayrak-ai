---
phase: 10-hakkedi-billing
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/actions/hakedis.ts
  - tests/hakedis.test.ts
autonomous: true
requirements: [HAK-01, HAK-02, HAK-03, HAK-04, HAK-05]
tags: [hakedis, billing, server-actions, money-math, postgres]

must_haves:
  truths:
    - "D-90: net = gross + (KDV − KDV tevkifat) − stopaj − teminat − avans; tevkifat is a fraction OF the KDV; all arithmetic in Postgres numeric"
    - "D-94: avans kesintisi = flat avans_kesintisi_rate × period gross, computed independently each period (no running advance balance in v1)"
    - "D-95: linear lifecycle draft→finalized→submitted→paid; finalize is the mandatory gate; only draft is editable/recomputable"
    - "D-96: finalize is irreversible (no un-finalize); recompute/rate-edit/line-edit on a finalized period returns an error; corrections flow into the next period"
    - "D-97: draft periods (and their lines) are deletable; finalized/submitted/paid are never deletable; create/finalize/delete write office_activity_log"
    - "D-98: lines compute on period create with a draft-only manual recompute; recompute is blocked once finalized; line rows are stored"
    - "D-99: previous_cumulative_qty = cumulative_qty_approved from the most recent FINALIZED period (same project+currency, earlier period_end_date); 0 if none"
    - "D-100: cumulative-approved-qty = sum of approved submission quantities where status='approved' and decided_at ≤ period_end_date (Istanbul tz); period_qty = cumulative − previous"
    - "D-101: one period = one currency; a period only aggregates BOQ items whose currency_code matches; never cross-currency sum"
    - "D-102: include all priced BOQ items (matching currency) with any cumulative approved qty > 0 up to the cutoff; period_qty may be 0 for fully-prior items"
    - "D-103: unpriced BOQ items (NULL unit_price) are excluded; the compute/UI surfaces a warning listing the excluded items"
    - "D-104: INSERT writes cumulative + previous only — never period_qty (DB-GENERATED); the read-back period_qty is the generated value"
    - "createPeriod() inserts a draft hakedis_period and computes its lines on create"
    - "computePeriodLines() aggregates approved-qty per BOQ item with an Istanbul-tz inclusive cutoff"
    - "previous_cumulative_qty is sourced only from the most recent FINALIZED period"
    - "getPeriodDetail() returns gross/KDV/tevkifat/stopaj/teminat/avans/net computed in Postgres numeric"
    - "getPeriodsByProject() returns netByDisplay (net payable as a string) for each finalized/submitted/paid period and null for drafts with no lines"
    - "finalizePeriod() sets status='finalized' + finalizedAt and is blocked on non-draft"
    - "recomputePeriodLines() and deletePeriod() throw on a non-draft period"
    - "updatePaymentStatus() enforces finalized→submitted→paid and rejects invalid transitions"
  artifacts:
    - path: "src/actions/hakedis.ts"
      provides: "All hakkediş server actions + computePeriodLines + getPeriodDetail"
      exports: ["createPeriod", "recomputePeriodLines", "finalizePeriod", "updatePaymentStatus", "deletePeriod", "getPeriodsByProject", "getPeriodDetail"]
    - path: "tests/hakedis.test.ts"
      provides: "Filled-in HAK-01..HAK-05 integration + unit tests"
  key_links:
    - from: "src/actions/hakedis.ts"
      to: "hakedis_period_lines"
      via: "INSERT writes cumulative + previous (never period_qty — GENERATED)"
      pattern: "cumulative_qty_approved"
    - from: "src/actions/hakedis.ts"
      to: "logOfficeActivity"
      via: "create/finalize/delete fire-and-forget activity log"
      pattern: "logOfficeActivity"
    - from: "src/actions/hakedis.ts"
      to: "submissions"
      via: "approved-qty aggregation with decided_at Istanbul cutoff"
      pattern: "decided_at <="
---

<objective>
Build the `src/actions/hakedis.ts` server-action module: the yeşil-defter `computePeriodLines()`
aggregation (HAK-02), the deduction chain in `getPeriodDetail()` (HAK-03), and the period lifecycle
actions create/recompute/finalize/advance-status/delete (HAK-01/04/05) — all auth-guarded,
tenant-scoped, money-in-Postgres, and activity-logged. Fill in the Wave 0 tests.

Purpose: This is the financial core of the phase. Every number here is legally significant and must
be computed in Postgres `numeric` (never JS float), chained off locked finalized snapshots, and
protected by status guards so a finalized period is immutable.

Decisions covered: D-90 (net formula), D-94 (flat avans rate), D-95 (linear lifecycle), D-96
(irreversible finalize), D-97 (draft-only delete + activity log), D-98 (compute-on-create +
draft-only recompute), D-99 (previous-cumulative from latest finalized), D-100 (Istanbul-cutoff
approved-qty aggregation), D-101 (one period = one currency), D-102 (priced-item inclusion),
D-103 (unpriced-item exclusion + warning), D-104 (never INSERT the GENERATED period_qty).

Output: `src/actions/hakedis.ts` with seven exported functions + a complete `tests/hakedis.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-hakkedi-billing/10-CONTEXT.md
@.planning/phases/10-hakkedi-billing/10-RESEARCH.md
@.planning/phases/10-hakkedi-billing/10-01-SUMMARY.md

<interfaces>
<!-- Established idioms the executor reuses. No codebase exploration needed beyond read_first. -->

From src/actions/settings.ts (canonical Server Action shape — copy this skeleton):
- `const session = await auth(); if (!session) throw new Error('Unauthorized'); const tenantId = getDefaultTenantId();`
- zod schema.parse(input) before any DB write
- `await db.execute(sql\`...\`)` returns `{ rows: Record<string,unknown>[] }`
- `revalidatePath('/dashboard/...')` at the end

From src/actions/boq.ts:
- `export const ALLOWED_CURRENCIES = ['TRY','USD','EUR'] as const;` — import + reuse for currency validation
- IDOR guard (CR-01): verify `projects.id = projectId AND projects.tenant_id = tenantId` BEFORE any write
- `if (session.user?.id) logOfficeActivity({...})` — guard empty actorUserId (CR-04)

From src/lib/log-office-activity.ts:
- `logOfficeActivity({ actorUserId, actionType, entityType, entityId?, projectId?, metadata? }): void` — fire-and-forget via after(); NEVER await; call AFTER the primary write. actionType must be an OFFICE_ACTION_TYPES literal ('hakedis_period_created' | 'hakedis_period_finalized' | 'hakedis_period_deleted').

From src/db/schema/hakedis-periods.ts:
- HAKEDIS_STATUSES = ['draft','finalized','submitted','paid']; columns: projectId, periodNumber, periodStartDate (nullable), periodEndDate (NOT NULL), currencyCode (default 'TRY'), status (default 'draft'), kdvRate, retentionRate, tevkifatFraction, stopajEnabled, stopajRate, avansKesintisiRate, createdByUserId, finalizedAt.

From src/db/schema/hakedis-period-lines.ts:
- INSERT writes: tenantId, periodId, boqItemId, materialSnapshot, unitSnapshot, currencyCodeSnapshot, unitPriceSnapshot, cumulativeQtyApproved, previousCumulativeQty, periodValue, cumulativeValue. NEVER periodQty (GENERATED — Postgres computes it; INSERT supplying it errors).

From src/actions/analytics.ts (money-math idioms):
- All multiplication/sum in Postgres: SUM(x::numeric); numeric columns return as strings; never parseFloat in a loop.
- Istanbul-tz cutoff (RESEARCH Code Examples): `(${periodEndDate}::date + interval '1 day') AT TIME ZONE 'Europe/Istanbul'` — inclusive of the cutoff calendar day.

From tests/fixtures/db.ts:
- describeIfDb (skips without TEST_DATABASE_URL), getTestDb(), truncateAllTables(db) — fileParallelism:false is already set.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: computePeriodLines + createPeriod + getPeriodsByProject + getPeriodDetail</name>
  <files>src/actions/hakedis.ts, tests/hakedis.test.ts</files>
  <read_first>
    - src/actions/settings.ts (auth+tenant+zod+revalidate skeleton — entire file is short; note the EXACT auth idiom `const session = await auth()` — reuse it verbatim so the per-function auth-guard grep gate is unambiguous)
    - src/actions/boq.ts lines 57-145 (ALLOWED_CURRENCIES export; addBoqItem IDOR guard CR-01; logOfficeActivity guard CR-04)
    - src/actions/analytics.ts lines 1-130 + the Istanbul-tz aggregation idioms (the getPortfolioTrends AT TIME ZONE pattern)
    - src/lib/log-office-activity.ts (logOfficeActivity signature + OfficeActionType)
    - src/db/schema/hakedis-periods.ts + src/db/schema/hakedis-period-lines.ts (post-Wave-1 columns)
    - src/db/schema/boq-items.ts + src/db/schema/submissions.ts (boq unit_price nullable + currency_code; submission status/quantity/decided_at — the cumulative source)
    - .planning/phases/10-hakkedi-billing/10-RESEARCH.md §"Cumulative-Approved-Qty Aggregation", §"Deduction Computation Chain", §"Code Examples" (the exact SQL), §"Common Pitfalls" 3/4/5/8
    - tests/hakedis.test.ts (the Wave 0 scaffold from 10-01 — fill the relevant todos)
    - tests/schema.test.ts lines 235-387 (fixture insert style for tenant/project/boq/period/submission)
  </read_first>
  <behavior>
    - createPeriod inserts a draft period (status='draft') with the supplied rates and immediately calls recomputePeriodLines — detail then shows computed lines.
    - computePeriodLines includes only priced BOQ items (unit_price IS NOT NULL) matching the period currency_code with cumulative approved qty > 0 up to the Istanbul-tz inclusive cutoff (D-102/D-103).
    - cumulative_qty_approved sums approved submission quantities where decided_at <= (period_end_date + 1 day) AT TIME ZONE Europe/Istanbul (D-100, Pitfall 3 inclusive).
    - previous_cumulative_qty for each BOQ item comes from the most recent FINALIZED period (status != 'draft', same project+currency, earlier period_end_date) via DISTINCT ON; 0 if none (D-99, Pitfall 4).
    - INSERT writes only cumulative + previous (+ snapshots + period_value + cumulative_value); period_qty is read back from RETURNING as the GENERATED value.
    - getPeriodDetail returns period header + lines + deduction object {gross,kdv,tevkifat,stopaj,teminat,avans,net} as strings, computed in one Postgres query using COALESCE on nullable tevkifat_fraction/stopaj_rate (Pitfall 5), CASE on stopaj_enabled, and the D-90 net formula; plus the list of unpriced excluded BOQ items (D-103).
    - getPeriodsByProject returns each period row WITH a netByDisplay string (the period's computed net payable) when the period has lines, and null when there are none (e.g. a freshly created draft with no priced items / cumulative qty); the net is computed in Postgres numeric per period using the same deduction formula over SUM(period_value).
    - getPeriodDetail and getPeriodsByProject are tenant-scoped (Pitfall 8) and cross-currency-safe (D-101).
  </behavior>
  <action>
    Create `src/actions/hakedis.ts` with `'use server'`. Import sql from drizzle-orm, db from @/db, auth from @/lib/auth, getDefaultTenantId from @/lib/tenant, logOfficeActivity from @/lib/log-office-activity, ALLOWED_CURRENCIES from @/actions/boq, z from zod, revalidatePath from next/cache, Decimal NOT needed here (display-only in pages). Every exported function MUST open with the exact guard idiom `const session = await auth(); if (!session) throw new Error('Unauthorized');` as its first two statements (D-89, T-10-02-EoP) — use the verbatim `const session = await auth()` form so the per-function auth-guard gate counts cleanly.
    Define `createPeriodSchema` (zod): projectId uuid, periodNumber string 1-50 chars, periodStartDate optional date string, periodEndDate date string (required), currencyCode in ALLOWED_CURRENCIES, kdvRate/tevkifatFraction/retentionRate/avansKesintisiRate as 0-1 fraction strings, stopajEnabled boolean, stopajRate optional 0-1 fraction string. Rates arrive as fractions (form converts % to fraction before calling).
    Implement `recomputePeriodLines(periodId: string)`: auth+tenant; SELECT the period (id, status, project_id, currency_code, period_end_date) WHERE id AND tenant_id; throw 'Period not found' if absent; throw 'Period is not in draft status' if status !== 'draft' (Pitfall 7); DELETE existing hakedis_period_lines WHERE period_id AND tenant_id; run the cumulative aggregation query (RESEARCH Code Examples — Istanbul cutoff, HAVING cumulative>0, unit_price NOT NULL, currency match); run the previous-cumulative DISTINCT ON query (status != 'draft'); for each aggregated item INSERT a line writing cumulative_qty_approved, previous_cumulative_qty (from the prev map or 0), snapshots from the boq row, period_value = period_qty*unit_price computed in Postgres (use a SQL expression `(cumulative - previous) * unit_price` so multiplication stays in Postgres), cumulative_value = cumulative*unit_price; NEVER write period_qty. Return { ok: true }.
    Implement `createPeriod(input)`: auth+tenant; parse with createPeriodSchema; IDOR-verify projectId belongs to tenant (CR-01); if periodNumber not supplied default to `HK-{YYYY}-{NN}` where NN = COUNT(*) of periods for project in the year + 1, zero-padded (Open Question 3 recommendation); INSERT hakedis_periods (status defaults 'draft', createdByUserId = session.user.id); call `recomputePeriodLines(period.id)` synchronously (D-98); logOfficeActivity actionType 'hakedis_period_created' (guard session.user?.id, CR-04); revalidatePath('/dashboard/hakedis'); return { ok: true, periodId }.
    Implement `getPeriodsByProject(projectId)`: auth+tenant; SELECT periods WHERE project_id AND tenant_id ORDER BY period_end_date DESC; for each period compute its net payable in Postgres numeric via a correlated subquery (or a LEFT JOIN + GROUP BY) over hakedis_period_lines using the same D-90 deduction formula (gross = SUM(period_value); net = gross + (KDV − tevkifat) − stopaj − teminat − avans, COALESCE nullable rates, CASE stopaj_enabled) and alias it `net_by_display`; when the period has no lines the subquery yields NULL → return netByDisplay = null. Return typed rows including periodNumber, periodEndDate, currencyCode, status, and netByDisplay string|null.
    Implement `getPeriodDetail(periodId)`: auth+tenant; SELECT period header WHERE id AND tenant_id; SELECT lines ORDER BY material; run the deduction SQL (RESEARCH §Deduction Chain — six values + net, COALESCE nullable rates, CASE stopaj_enabled); SELECT unpriced BOQ items (unit_price IS NULL, project+currency match) for the warning. Return { period, lines, deductions, unpricedItems }.
    Fill the corresponding todos in tests/hakedis.test.ts: createPeriod-inserts-draft-and-computes; cumulative-aggregation-Istanbul-cutoff (insert two approved submissions, one before and one after the cutoff date, assert cumulative); previous-cumulative-from-latest-finalized (insert a finalized period then a draft, assert delta); deduction-chain-values (known qty*price, assert gross/kdv/tevkifat/stopaj/teminat/avans/net strings to 2 decimals); unpriced-item-excluded-and-warned; getPeriodsByProject-net (create a period with priced items + approved submissions, run computePeriodLines, then assert getPeriodsByProject returns a NON-NULL netByDisplay for that period equal to the expected net string). Use the schema.test.ts fixture pattern (tenant/project/boq/period/submission inserts).
  </action>
  <verify>
    <automated>npx vitest run tests/hakedis.test.ts 2>&1 | tail -20; npx tsc --noEmit 2>&1 | grep -i "hakedis.ts" || echo "TYPECHECK CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - src/actions/hakedis.ts exports createPeriod, recomputePeriodLines, getPeriodsByProject, getPeriodDetail
    - `grep -c "const session = await auth()" src/actions/hakedis.ts` returns >= 7 — one auth guard for every exported function (createPeriod, recomputePeriodLines, finalizePeriod, updatePaymentStatus, deletePeriod, getPeriodsByProject, getPeriodDetail); the first statement of each exported fn is `const session = await auth()` (a `>= 4` count is INSUFFICIENT — every exported fn that touches financial data must guard)
    - recomputePeriodLines throws when period.status !== 'draft' (covered by a test asserting .rejects.toThrow on a finalized period)
    - The hakedis_period_lines INSERT in hakedis.ts does NOT reference periodQty / period_qty (grep: `grep -i "period_qty\|periodQty" src/actions/hakedis.ts` returns only read/select references, never an INSERT value key)
    - The deduction SQL uses `COALESCE(` for tevkifat_fraction and stopaj_rate and `CASE WHEN` for stopaj_enabled
    - The cumulative query uses `AT TIME ZONE 'Europe/Istanbul'` with `+ interval '1 day'` (inclusive cutoff)
    - The previous-cumulative query contains `status` `!= 'draft'` (or `finalized_at IS NOT NULL`)
    - getPeriodsByProject returns a `netByDisplay` field per row; tests/hakedis.test.ts asserts a NON-NULL netByDisplay (equal to the expected net string) for a period AFTER computePeriodLines has populated its lines, and null for a period with no lines
    - Every query touching hakedis_periods / hakedis_period_lines / submissions / boq_items includes `tenant_id = ${tenantId}`
    - `npx vitest run tests/hakedis.test.ts` passes (the filled-in compute + deduction + chaining + getPeriodsByProject-net tests are green)
  </acceptance_criteria>
  <done>computePeriodLines/createPeriod/getPeriodsByProject/getPeriodDetail implemented with money-in-Postgres, Istanbul cutoff, finalized-only chaining, never inserting period_qty; deduction chain correct; getPeriodsByProject returns a per-period netByDisplay (non-null once lines exist); tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: finalizePeriod + updatePaymentStatus + deletePeriod (lifecycle + immutability)</name>
  <files>src/actions/hakedis.ts, tests/hakedis.test.ts</files>
  <read_first>
    - src/actions/hakedis.ts (the module from Task 1 — extend it; reuse the auth+tenant skeleton + the period SELECT-by-id pattern; reuse the verbatim `const session = await auth()` first-statement idiom)
    - .planning/phases/10-hakkedi-billing/10-RESEARCH.md §"Actions Required" table, §"updatePaymentStatus Lifecycle Guard", §"Finalization Lock Guard" (the exact guard code)
    - src/db/schema/hakedis-periods.ts (HAKEDIS_STATUSES + finalizedAt + status)
    - src/db/schema/office-activity-log.ts (OFFICE_ACTION_TYPES now includes 'hakedis_period_deleted')
    - tests/hakedis.test.ts (fill the lifecycle/immutability todos)
  </read_first>
  <behavior>
    - finalizePeriod on a draft sets status='finalized' + finalized_at=NOW(); on a non-draft throws 'Period is not in draft status' (D-95/D-96, HAK-05). Irreversible — no un-finalize action exists.
    - After finalize, recomputePeriodLines and deletePeriod both throw (immutability lock keys off status != 'draft').
    - updatePaymentStatus enforces VALID_TRANSITIONS: finalized→submitted, submitted→paid; draft and paid have no valid advance (draft must use finalize; paid is terminal) — invalid transitions throw (HAK-04).
    - deletePeriod deletes a draft period (cascade drops its lines via the period_id FK ON DELETE cascade); non-draft is never deletable (D-97).
    - finalize and delete write office_activity_log ('hakedis_period_finalized' / 'hakedis_period_deleted'); updatePaymentStatus does not log (not a PERF-03 action type). create logged in Task 1.
  </behavior>
  <action>
    Extend src/actions/hakedis.ts. Each new exported function opens with the verbatim `const session = await auth(); if (!session) throw new Error('Unauthorized');` first-statement guard (keeps the per-function auth-guard gate at >= 7 across the module).
    Implement `finalizePeriod(periodId)`: auth+tenant; SELECT period (id,status,project_id) WHERE id AND tenant_id; throw 'Period not found' if absent; throw 'Period is not in draft status' if status !== 'draft'; UPDATE status='finalized', finalized_at=NOW(), updated_at=NOW() WHERE id AND tenant_id; logOfficeActivity 'hakedis_period_finalized' (entityType 'hakedis_period', entityId periodId, projectId; guard session.user?.id); revalidatePath('/dashboard/hakedis') and revalidatePath(`/dashboard/hakedis/${periodId}`); return { ok: true }.
    Add `const VALID_TRANSITIONS: Record<HakedisStatus, HakedisStatus | null> = { draft: null, finalized: 'submitted', submitted: 'paid', paid: null }` (import HakedisStatus from the schema).
    Implement `updatePaymentStatus(periodId, target: HakedisStatus)`: auth+tenant; SELECT current status WHERE id AND tenant_id; throw 'Period not found' if absent; compute expected = VALID_TRANSITIONS[current]; throw 'Invalid status transition' if expected === null || target !== expected; UPDATE status=target, updated_at=NOW() WHERE id AND tenant_id; revalidatePath the list + detail; return { ok: true }. No activity log.
    Implement `deletePeriod(periodId)`: auth+tenant; SELECT period (id,status,project_id) WHERE id AND tenant_id; throw 'Period not found' if absent; throw 'Cannot delete a finalized period' if status !== 'draft' (D-97); DELETE FROM hakedis_periods WHERE id AND tenant_id (lines cascade); logOfficeActivity 'hakedis_period_deleted' (guard session.user?.id); revalidatePath('/dashboard/hakedis'); return { ok: true }.
    Fill tests/hakedis.test.ts lifecycle/immutability todos: finalize-sets-finalizedAt; finalize-blocks-recompute (finalize then expect recomputePeriodLines rejects); finalize-blocks-delete; updatePaymentStatus-valid-chain (finalized→submitted→paid green); updatePaymentStatus-invalid (draft→submitted rejects; paid→anything rejects); deletePeriod-draft-cascades-lines (assert lines gone); deletePeriod-finalized-rejects.
  </action>
  <verify>
    <automated>npx vitest run tests/hakedis.test.ts 2>&1 | tail -20; npx tsc --noEmit 2>&1 | grep -i "hakedis.ts" || echo "TYPECHECK CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - src/actions/hakedis.ts exports finalizePeriod, updatePaymentStatus, deletePeriod
    - finalizePeriod first statement is `const session = await auth()`; throws when status !== 'draft' (test: finalize on already-finalized period rejects)
    - After Task 2, `grep -c "const session = await auth()" src/actions/hakedis.ts` returns exactly 7 (one per exported function) — no exported financial action is left unguarded
    - recomputePeriodLines and deletePeriod both reject on a finalized period (tests green, exit 0)
    - updatePaymentStatus rejects draft→submitted and paid→anything; accepts finalized→submitted→paid (tests green)
    - deletePeriod on a draft removes the period AND its lines (test asserts lines count == 0 after delete)
    - deletePeriod calls logOfficeActivity with actionType 'hakedis_period_deleted'; finalizePeriod with 'hakedis_period_finalized'
    - `grep -c "tenant_id = \${tenantId}" src/actions/hakedis.ts` shows tenant scope in finalize/update/delete queries
    - `npx vitest run tests/hakedis.test.ts` passes all HAK-04/HAK-05 tests
  </acceptance_criteria>
  <done>Lifecycle + immutability actions implemented and tested: finalize is the mandatory irreversible gate, payment status follows the linear chain, draft-only delete cascades lines, all guarded server-side with one auth() per exported function (7 total).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (client form) → Server Action | caller-supplied periodId/projectId/rates cross into financial mutations |
| Server Action → Neon DB | parameterized SQL writes legally-significant billing rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-EoP | Elevation of Privilege | every exported action | mitigate | `const session = await auth(); if (!session) throw 'Unauthorized'` is the first statement of every exported fn (D-89); the acceptance gate asserts one guard per exported fn (>= 7), not a weaker count |
| T-10-02-IDOR-P | Information Disclosure | getPeriodDetail/getPeriodsByProject on periodId/projectId | mitigate | every query includes `WHERE ... tenant_id = ${tenantId}`; createPeriod verifies projectId belongs to tenant before INSERT (CR-01, Pitfall 8) |
| T-10-02-IMM | Tampering | finalized snapshot recompute/edit/delete | mitigate | finalizePeriod/recomputePeriodLines/deletePeriod check `status === 'draft'` and throw before any write (HAK-05, D-96) |
| T-10-02-FLOAT | Tampering / integrity | deduction + line value math | mitigate | all multiplication/sum stays in Postgres `numeric` (period_value computed as SQL `(cumulative-previous)*unit_price`); deduction chain + the getPeriodsByProject net are single Postgres queries; JS only formats for display (D-90) |
| T-10-02-SQLi | Tampering | period label / rates / ids | mitigate | all values bound via Drizzle `sql\`\`` params; never `sql.raw()` for user input |
| T-10-02-XCUR | Tampering / bad sums | computePeriodLines aggregation | mitigate | only BOQ items with `currency_code = period.currencyCode` included; previous-cumulative chains only off same-currency finalized periods (D-101) |
| T-10-02-NULL | Integrity | NULL tevkifat_fraction/stopaj_rate poisoning net | mitigate | `COALESCE(rate, 0)` in the deduction query (and the getPeriodsByProject net subquery) prevents NULL propagation (Pitfall 5) |
| T-10-02-SC | Tampering | npm installs | accept | no new npm packages this plan (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `npx vitest run tests/hakedis.test.ts` green across HAK-01..HAK-05
- `npx vitest run` full suite green (no regression in schema.test.ts)
- `npx tsc --noEmit` clean for src/actions/hakedis.ts
- grep confirms `const session = await auth()` appears 7 times (one per exported fn), tenant scope everywhere, no period_qty INSERT, COALESCE on nullable rates, getPeriodsByProject returns netByDisplay
</verification>

<success_criteria>
- All seven actions exported, auth-guarded (one auth() per exported function — 7 total), tenant-scoped, money-in-Postgres
- computePeriodLines: Istanbul-inclusive cutoff, finalized-only chaining, never inserts period_qty
- getPeriodDetail returns the full D-90 deduction chain in Postgres numeric
- getPeriodsByProject returns a per-period netByDisplay (non-null once lines exist; null otherwise), tested
- finalize/recompute/delete enforce draft-only immutability; payment status follows the linear chain
- create/finalize/delete write office_activity_log
</success_criteria>

<output>
Create `.planning/phases/10-hakkedi-billing/10-02-SUMMARY.md` when done
</output>
</content>
