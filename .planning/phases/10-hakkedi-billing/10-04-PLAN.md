---
phase: 10-hakkedi-billing
plan: 04
type: execute
wave: 4
depends_on: ["10-02", "10-03"]
files_modified:
  - src/app/dashboard/(admin)/hakedis/[periodId]/page.tsx
  - src/components/admin/PeriodDetailControls.tsx
  - src/components/admin/FinalizeDialog.tsx
  - src/components/admin/DeletePeriodDialog.tsx
autonomous: true
requirements: [HAK-02, HAK-03, HAK-04, HAK-05]
tags: [hakedis, billing, ui, detail, deduction-summary, finalize, i18n]

must_haves:
  truths:
    - "The /dashboard/hakedis/[periodId] page shows the yeşil-defter line-item table with snapshot quantities and values"
    - "The deduction summary card shows gross → KDV → tevkifat → stopaj → teminat → avans → net to 2 decimals"
    - "Net Ödeme is the visual focal point (text-2xl, separated by a heavy top border)"
    - "Draft periods show Recompute/Finalize/Delete controls; non-draft show only payment-status advance or none"
    - "A finalized period shows the immutability banner and removes edit controls entirely"
    - "An unpriced-item warning lists excluded BOQ items on a draft when applicable"
  artifacts:
    - path: "src/app/dashboard/(admin)/hakedis/[periodId]/page.tsx"
      provides: "Period detail page (lines table + deduction summary + state-gated controls)"
      contains: "force-dynamic"
    - path: "src/components/admin/PeriodDetailControls.tsx"
      provides: "State-gated control row (recompute/finalize/delete/advance)"
    - path: "src/components/admin/FinalizeDialog.tsx"
      provides: "Finalize confirmation dialog"
    - path: "src/components/admin/DeletePeriodDialog.tsx"
      provides: "Delete confirmation dialog"
  key_links:
    - from: "src/app/dashboard/(admin)/hakedis/[periodId]/page.tsx"
      to: "src/actions/hakedis.ts getPeriodDetail"
      via: "RSC fetch of period header + lines + deductions + unpriced items"
      pattern: "getPeriodDetail"
    - from: "src/components/admin/PeriodDetailControls.tsx"
      to: "src/actions/hakedis.ts"
      via: "recompute/finalize/advance/delete server-action calls"
      pattern: "recomputePeriodLines|finalizePeriod|updatePaymentStatus|deletePeriod"
---

<objective>
Build the period detail page (HAK-02 lines table, HAK-03 deduction summary, HAK-04 payment-status
controls, HAK-05 finalize/immutability) at `/dashboard/(admin)/hakedis/[periodId]`, plus the
state-gated control row and the Finalize/Delete confirmation dialogs. This is where the office
engineer confirms the net payable and finalizes the period into an immutable snapshot.

Purpose: The detail page is the decision surface — it renders the legally-significant numbers and
hosts the irreversible finalize action. Controls are state-gated per D-96 (removed, not just disabled).

Output: The detail RSC page + three client components (controls + two confirm dialogs).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-hakkedi-billing/10-CONTEXT.md
@.planning/phases/10-hakkedi-billing/10-UI-SPEC.md
@.planning/phases/10-hakkedi-billing/10-02-SUMMARY.md
@.planning/phases/10-hakkedi-billing/10-03-SUMMARY.md

<interfaces>
<!-- Page + component contracts the executor wires against. -->

From src/actions/hakedis.ts (Wave 2):
- getPeriodDetail(periodId): { period: {id, periodNumber, projectId, currencyCode, status, periodEndDate, finalizedAt, kdvRate, tevkifatFraction, retentionRate, stopajEnabled, stopajRate, avansKesintisiRate}, lines: [{materialSnapshot, unitSnapshot, unitPriceSnapshot, previousCumulativeQty, cumulativeQtyApproved, periodQty, periodValue}], deductions: {gross, kdv, tevkifat, stopaj, teminat, avans, net} (all strings), unpricedItems: [{material}] }
- recomputePeriodLines(periodId): { ok: true } (throws on non-draft)
- finalizePeriod(periodId): { ok: true } (throws on non-draft)
- updatePaymentStatus(periodId, target): { ok: true } (finalized→submitted→paid only)
- deletePeriod(periodId): { ok: true } (throws on non-draft; cascades lines)

From src/components/admin/HakedisStatusBadge.tsx (Wave 3): status badge.

From src/components/ui/ (installed): Table, TableFooter, Card, CardHeader, CardTitle, CardDescription, Dialog, Badge, Alert, AlertDescription, Button, Separator.

Money display (UI-SPEC §"Money Display Rules"): values are strings → `new Decimal(value).toFixed(2)` → toLocaleString('tr-TR'|'en-US', {minimumFractionDigits:2, maximumFractionDigits:2}); currency ISO suffix; "—" when null/not computed; tabular-nums on every money cell. NEVER parseFloat.

i18n: dashboard.admin.hakedis.detail.*, .finalize_dialog.*, .delete_dialog.* (Wave 1). getTranslations RSC; useTranslations clients.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize + Delete dialogs + state-gated control row</name>
  <files>src/components/admin/FinalizeDialog.tsx, src/components/admin/DeletePeriodDialog.tsx, src/components/admin/PeriodDetailControls.tsx</files>
  <read_first>
    - .planning/phases/10-hakkedi-billing/10-UI-SPEC.md §"Surface 3d: Period Controls", §"Surface 4: Finalize Confirmation Dialog", §"Surface 5: Delete Confirmation Dialog", §"State Machine: Interaction Flow", §"Copywriting Contract"
    - src/components/admin/HakedisCreateDialog.tsx (Wave 3 — client-dialog-calling-server-action pattern + error Alert + useRouter)
    - src/components/ui/dialog.tsx (DialogTitle/DialogDescription/DialogFooter APIs — both required for a11y wiring)
    - src/actions/hakedis.ts (recomputePeriodLines/finalizePeriod/updatePaymentStatus/deletePeriod signatures + thrown errors)
  </read_first>
  <action>
    Create FinalizeDialog.tsx ('use client'): props { periodId: string }. shadcn `<Dialog>` (max-w-sm) triggered by the Finalize button. `<DialogTitle>` t('finalize_dialog.title'), `<DialogDescription>` t('finalize_dialog.body'). Footer: ghost "Hayır, Geri Dön/No, Go Back" (close) + `variant="default"` (primary, NOT destructive — finalizing is positive/irreversible) "Kesinleştir/Finalize Period" that calls finalizePeriod(periodId); on ok close dialog + router.refresh(); on error show inline `<Alert variant="destructive">` t('detail.err_finalize_blocked'). Both DialogTitle + DialogDescription always present (a11y).
    Create DeletePeriodDialog.tsx ('use client'): props { periodId: string, periodNumber: string }. `<Dialog>` (max-w-sm). DialogTitle t('delete_dialog.title'); DialogDescription t('delete_dialog.body', { periodNumber }). Footer: ghost "Hayır, Koru/No, Keep It" + `variant="destructive"` "Evet, Sil/Yes, Delete" that calls deletePeriod(periodId); on ok router.push('/dashboard/hakedis'); on error inline destructive Alert t('form.err_general'). The trigger Delete button carries `aria-label` "Delete period {periodNumber}".
    Create PeriodDetailControls.tsx ('use client'): props { periodId: string, periodNumber: string, status: 'draft'|'finalized'|'submitted'|'paid' }. Render the control row per state (D-96 — controls absent when not applicable, never just disabled):
      - draft: a "Yeniden Hesapla/Recompute Lines" `<Button variant="outline" size="sm">` (RefreshCw icon) calling recomputePeriodLines(periodId) then router.refresh() (inline destructive Alert on error); the Finalize button is the `<FinalizeDialog>` trigger (primary, Lock icon); the Delete button is the `<DeletePeriodDialog>` trigger (destructive).
      - finalized: a "Tahakkuk Et/Mark Submitted" outline button calling updatePaymentStatus(periodId,'submitted') then router.refresh().
      - submitted: a "Ödendi/Mark Paid" outline button calling updatePaymentStatus(periodId,'paid') then router.refresh().
      - paid: no controls.
    All labels from dashboard.admin.hakedis.detail.*. Decorative lucide icons aria-hidden. Wrap state buttons in a `flex items-center gap-2` row.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -iE "FinalizeDialog|DeletePeriodDialog|PeriodDetailControls" || echo "TYPECHECK CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - FinalizeDialog.tsx confirm button uses `variant="default"` (NOT destructive) and calls finalizePeriod
    - DeletePeriodDialog.tsx confirm button uses `variant="destructive"`, calls deletePeriod, and on success router.push('/dashboard/hakedis')
    - DeletePeriodDialog trigger button has an `aria-label` containing the period number
    - PeriodDetailControls renders DIFFERENT controls per status (grep shows branches on status === 'draft' / 'finalized' / 'submitted'); 'paid' renders no action buttons
    - PeriodDetailControls draft branch references recomputePeriodLines and embeds FinalizeDialog + DeletePeriodDialog
    - finalized branch calls updatePaymentStatus with 'submitted'; submitted branch with 'paid'
    - Both dialogs include DialogTitle AND DialogDescription
    - All visible strings use t() from the hakedis namespace
    - `npx tsc --noEmit` clean for all three files
  </acceptance_criteria>
  <done>Finalize + Delete confirmation dialogs and the state-gated control row built per UI-SPEC: draft has recompute/finalize/delete; non-draft has only the payment-status advance (or none for paid); finalize confirm is primary, delete is destructive.</done>
</task>

<task type="auto">
  <name>Task 2: Period detail page — header, banners, lines table, deduction summary</name>
  <files>src/app/dashboard/(admin)/hakedis/[periodId]/page.tsx</files>
  <read_first>
    - .planning/phases/10-hakkedi-billing/10-UI-SPEC.md §"Surface 3" entirety (3a header + back link, 3b finalized banner, 3c unpriced warning, 3d controls placement, 3e lines table columns + row styling + footer, 3f deduction summary chain rows + Net Ödeme focal point), §"Money Display Rules", §"Typography" (text-2xl tabular-nums net), §"Accessibility Contract", §"Spacing Scale" (compact py-3 / p-3)
    - src/app/dashboard/(admin)/people/[personId]/page.tsx (force-dynamic + auth() redirect + notFound() + back Link + getTranslations + KPI/Card layout)
    - src/actions/hakedis.ts (getPeriodDetail return shape)
    - src/components/admin/PeriodDetailControls.tsx + HakedisStatusBadge.tsx (Wave 3/Task 1)
    - src/components/ui/table.tsx + card.tsx + alert.tsx + separator.tsx
    - any existing decimal.js display usage in src/app/dashboard (grep for `new Decimal(` to reuse the money formatting helper convention)
  </read_first>
  <action>
    Create the detail RSC page. `export const dynamic = 'force-dynamic'`. async; `const session = await auth(); if (!session) redirect('/auth/signin');` first. Await params for periodId (Next 15 async params). Call getPeriodDetail(periodId); `if (!result?.period) notFound();`. Determine locale for money formatting (getLocale() from next-intl/server). Write a local `formatMoney(value, currency)` using `new Decimal(value).toFixed(2)` + toLocaleString(locale==='tr'?'tr-TR':'en-US', {minimumFractionDigits:2, maximumFractionDigits:2}) + currency suffix; "—" when value null. NEVER parseFloat.
    Layout `<div className="space-y-6">`:
    - Back link to `/dashboard/hakedis?project={projectId}` (ChevronLeft, t('detail.back')).
    - 3a header: `flex items-start justify-between` — left: h1 periodNumber (text-xl font-semibold) + `<HakedisStatusBadge status>` inline + a muted line "{projectName} · {t('detail.end_date_label')}: {dd.MM.yyyy}"; right: `<PeriodDetailControls periodId periodNumber status>`.
    - 3b finalized banner: when status !== 'draft' render `<Alert role="status" className="p-3">` (Lock icon) with t('detail.finalized_notice', { date: formatted finalizedAt }).
    - 3c unpriced warning: when status === 'draft' AND unpricedItems.length > 0 render `<Alert role="alert" className="border-amber-200 bg-amber-50 p-3">` (TriangleAlert) with t('detail.unpriced_warning', { count }) + a Link to `/dashboard/projects/{projectId}?tab=boq` (t('detail.unpriced_cta')).
    - 3e lines table in a `<Card>` (CardTitle t('detail.lines_heading')): `<Table>` columns Malzeme, Birim, Birim Fiyat (right tabular-nums), Önceki Kümülatif (right), Kümülatif (right), Dönem Miktarı (right tabular-nums, bold when periodQty>0), Dönem Tutarı (right tabular-nums font-semibold). Rows with periodQty===0 get `text-muted-foreground` (D-102 full register); periodQty>0 normal. `<th scope="col">` headers. `<TableFooter>` row: colSpan 6 right-aligned t('detail.gross_total') + the gross value cell (formatMoney deductions.gross). Empty state: a single row colSpan 7 centered t('detail.lines_empty') when lines.length===0.
    - 3f deduction summary in a `<Card>` (CardTitle t('detail.summary_heading') + CardDescription t('detail.summary_period')). Chain rows in SC3 order, each `<div className="flex justify-between py-3 border-b last:border-b-0">` (py-3 = compact token, do not change): row1 gross t('detail.row_gross'); row2 KDV t('detail.row_kdv') (indented); row3 tevkifat t('detail.row_tevkifat') (indented, value negative); row4 stopaj t('detail.row_stopaj') ONLY when period.stopajEnabled (value negative); row5 teminat t('detail.row_teminat') (indented, negative); row6 avans t('detail.row_avans') ONLY when Number(avansKesintisiRate) > 0 (negative); row7 Net Ödeme t('detail.row_net') as the focal point: `font-semibold text-lg border-t-2 border-foreground pt-3 mt-1` with the value in `text-2xl font-semibold tabular-nums`. Value column right-aligned tabular-nums; rate % labels interpolate the period's rates ×100. Money cells carry `aria-label` per a11y contract.
    All labels via getTranslations('dashboard.admin.hakedis').
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "hakedis/\[periodId\]" || echo "TYPECHECK CLEAN"; npx next build 2>&1 | grep -iE "periodId|hakedis|error|failed" | head -20 || echo "BUILD OK"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/dashboard/(admin)/hakedis/[periodId]/page.tsx contains `export const dynamic = 'force-dynamic'`
    - First executable statement awaits auth() and redirects when null; calls notFound() when period missing
    - The page imports getPeriodDetail from '@/actions/hakedis' and renders PeriodDetailControls + HakedisStatusBadge
    - Money is formatted via `new Decimal(` (grep shows it); no `parseFloat` anywhere in the file
    - The finalized banner renders only when status !== 'draft'; the unpriced warning only when status === 'draft' && unpricedItems.length > 0
    - The deduction summary renders all 7 rows; stopaj row gated on period.stopajEnabled; avans row gated on avansKesintisiRate > 0
    - The Net Ödeme row uses `text-2xl` and `border-t-2 border-foreground` (focal point)
    - Lines with periodQty===0 use `text-muted-foreground`; the gross total appears in a `<TableFooter>` row
    - `npx next build` succeeds with the [periodId] route present
  </acceptance_criteria>
  <done>Detail page renders the yeşil-defter line table, the full D-90 deduction chain to 2 decimals with Net Ödeme as the focal point, state-gated controls, the immutability banner for finalized periods, and the unpriced-item warning for drafts — all money via decimal.js, fully i18n'd.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → RSC page | periodId path param is caller-controlled |
| client controls → Server Actions | recompute/finalize/advance/delete cross into financial mutations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-EoP | Elevation of Privilege | detail page RSC | mitigate | `auth()` redirect first statement; (admin) layout guards too |
| T-10-04-IDOR | Information Disclosure | periodId → getPeriodDetail | mitigate | getPeriodDetail (Wave 2) is tenant-scoped; a cross-tenant periodId returns no period → notFound() |
| T-10-04-IMM | Tampering | finalize/recompute/delete on a finalized period | mitigate | controls are REMOVED (not disabled) for non-draft (D-96); the Wave 2 actions also throw server-side (defense in depth) |
| T-10-04-FLOAT | Tampering / integrity | money display | mitigate | display via `new Decimal(str).toFixed(2)`; never parseFloat; all arithmetic already done in Postgres (Wave 2) |
| T-10-04-XSS | Information Disclosure | material/project/period strings | mitigate | React auto-escapes; no dangerouslySetInnerHTML |
| T-10-04-SC | Tampering | npm installs | accept | no new npm packages |
</threat_model>

<verification>
- `npx tsc --noEmit` clean for the page + three components
- `npx next build` succeeds with the [periodId] route
- Manual (end-of-phase): open a draft → recompute/finalize/delete visible + summary chain to 2 decimals in TR and EN; finalize → banner appears, edit controls vanish, status advances via Tahakkuk Et / Ödendi
</verification>

<success_criteria>
- Detail page shows the yeşil-defter line table + the D-90 deduction summary to 2 decimals (Postgres-computed), Net Ödeme as the focal point
- Controls are state-gated: draft = recompute/finalize/delete; finalized/submitted = payment advance; paid = none
- Finalized periods show the immutability banner and no edit controls
- Draft unpriced-item warning lists excluded items
- All surfaces render in both TR and EN
</success_criteria>

<output>
Create `.planning/phases/10-hakkedi-billing/10-04-SUMMARY.md` when done
</output>
