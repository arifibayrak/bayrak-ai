---
phase: 16-ai-vision-assist
plan: 05
type: execute
wave: 4
depends_on: ["16-02", "16-03", "16-04"]
files_modified:
  - src/actions/ai-flags.ts
  - src/components/brand/AiFlagCard.tsx
  - src/components/admin/SubmissionDetailView.tsx
  - src/lib/chainage-data.ts
  - src/components/dashboard/ChainageTable.tsx
  - messages/tr.json
  - messages/en.json
autonomous: false

requirements: [AI-03]

must_haves:
  truths:
    - "When a submission has an eval_passed=true flag, the detail page shows AiFlagCard with Turkish anomaly description, traffic-light confidence badge, and material suggestion (SC3)"
    - "D-03: AiFlagCard shows ALL eval-passed flags with a traffic-light confidence badge (green/amber/red)"
    - "When no eval-passed flag exists, AiFlagCard renders zero DOM (SC3)"
    - "getSubmissionAiFlag queries WHERE eval_passed = true and is the only read path to flag display (SC1)"
    - "ChainageTable shows an amber dot on buckets with an eval_passed flag; tooltip 'Bu segmentte AI tavsiyesi var'"
    - "AiFlagCard has no approve/reject affordance — advisory only (AI-03)"
  artifacts:
    - path: "src/actions/ai-flags.ts"
      provides: "getSubmissionAiFlag (eval_passed-gated read) + SubmissionAiFlag type"
      exports: ["getSubmissionAiFlag", "SubmissionAiFlag"]
    - path: "src/components/brand/AiFlagCard.tsx"
      provides: "advisory flag card; null render when no flag"
      exports: ["AiFlagCard"]
    - path: "messages/tr.json"
      provides: "dashboard.admin.ai_flags namespace (TR)"
      contains: "ai_flags"
  key_links:
    - from: "src/actions/ai-flags.ts"
      to: "submission_ai_flags WHERE eval_passed = true"
      via: "Drizzle select gated query"
      pattern: "eval_passed|evalPassed"
    - from: "src/components/admin/SubmissionDetailView.tsx"
      to: "src/components/brand/AiFlagCard.tsx"
      via: "AiFlagCard flag={aiFlag} mount (replaces inert Alert slot)"
      pattern: "AiFlagCard"
    - from: "src/lib/chainage-data.ts"
      to: "submission_ai_flags WHERE eval_passed = true"
      via: "LEFT JOIN setting bucket.hasAiFlag"
      pattern: "hasAiFlag"
---

<objective>
Surface the advisory flags. Build the `eval_passed`-gated `getSubmissionAiFlag` Server Action, the `AiFlagCard` brand component (Turkish anomaly description + traffic-light confidence badge + material suggestion; zero DOM when absent), mount it in `SubmissionDetailView` (replacing the inert Phase-6 Alert slot), add the amber-dot indicator to `ChainageTable` (driven by a new `hasAiFlag` bucket field from an `eval_passed`-gated LEFT JOIN), and add the `dashboard.admin.ai_flags` i18n namespace (TR + EN).

Purpose: SC3 (AiFlagCard display + absent state), SC1 (eval_passed is the single read gate), AI-03 (advisory-only — no approve/reject affordance in the card). This is the visible payoff, gated entirely on Plan 04's eval gate being open.
Output: getSubmissionAiFlag, AiFlagCard, the two component modifications, the chainage-data query change, and i18n keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-ai-vision-assist/16-UI-SPEC.md
@.planning/phases/16-ai-vision-assist/16-PATTERNS.md

<interfaces>
<!-- submission_ai_flags columns (Plan 01/02): status, photoAnomalyScore numeric(4,3), workClassification,
     anomalyDescription, evalPassed boolean, rawResponse jsonb, phashHex. -->

<!-- getSubmissionAiFlag contract (this plan creates it):
       export type SubmissionAiFlag = { ...row fields used by the card }
       export async function getSubmissionAiFlag(submissionId: string): Promise<SubmissionAiFlag | null>
       // SELECT * FROM submission_ai_flags WHERE submission_id = $1 AND eval_passed = true; returns null otherwise.
       // Errors caught → returns null (UI-SPEC: AI failure never surfaces an error UI). -->

<!-- AiFlagCard: import { BrandBadge, BrandCard, BrandHeading } from '@/components/brand';
     interface AiFlagCardProps { flag: SubmissionAiFlag | null }  → if (!flag) return null.
     confidenceBadgeVariant(score): >=0.75 'success' / >=0.50 'warning' / else 'destructive'.
     Signal icons (lucide): ImageOff (mismatch), Eye (quality), MapPin (location), FileText (material), Copy (duplicate). -->

<!-- SubmissionDetailView is 'use client' (lightbox useState). Keep 'use client'; the page RSC fetches
     the flag and passes it down as the aiFlag prop. Replace inert Alert slot at lines ~283–292
     (Sparkles/ai_slot_label/ai_slot_body) with <AiFlagCard flag={aiFlag ?? null} />. -->

<!-- ChainageBucket (src/lib/chainage-data.ts line 29): add `hasAiFlag: boolean`. Existing query has a
     sub_agg CTE LEFT JOINed by bucket_idx; add a LEFT JOIN / FILTER on submission_ai_flags
     WHERE eval_passed = true to set hasAiFlag = count > 0. ChainageTable cols (lines 133–138):
     col_km_range, col_status, col_work_count, col_material_qty, col_worker, col_auditor.
     Add the amber-dot column consistent with existing column order. -->

<!-- i18n namespace dashboard.admin.ai_flags keys (UI-SPEC): card_heading, advisory_badge,
     suggested_classification_label, confidence_high, confidence_medium, confidence_low,
     strip_indicator_tooltip, col_ai_flag (sr-only header). TR + EN byte-parallel. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: getSubmissionAiFlag action + AiFlagCard + i18n namespace</name>
  <read_first>
    - src/actions/ai-flags.ts (does not yet exist — create it)
    - src/components/brand/AiFlagCard.tsx (does not yet exist — create it)
    - src/actions/chainage.ts (auth+tenant wrapper analog for a gated read action)
    - src/components/admin/SubmissionDetailView.tsx lines 27–41, 117–175, 181–292 (brand imports, BrandCard compound, dl/dt/dd, the Alert slot)
    - src/components/brand/BrandBadge.tsx lines 19–36 (variant mapping) + src/components/brand/index.ts (barrel)
    - messages/tr.json + messages/en.json line ~344 (existing ai_slot keys → new ai_flags namespace sibling)
    - .planning/phases/16-ai-vision-assist/16-UI-SPEC.md (full — component inventory, color, copy, a11y)
    - .planning/phases/16-ai-vision-assist/16-PATTERNS.md "AiFlagCard" section
  </read_first>
  <action>
    Create src/actions/ai-flags.ts: export `type SubmissionAiFlag` (fields the card needs: photoAnomalyScore, workClassification, anomalyDescription, plus any signal flags surfaced from rawResponse) and `async function getSubmissionAiFlag(submissionId: string): Promise<SubmissionAiFlag | null>` — Drizzle select on submissionAiFlags `WHERE eq(submissionId) AND eq(evalPassed, true)`, return first row or null. Wrap in try/catch → return null on error (UI-SPEC: AI failure never surfaces an error UI). This is the ONLY read path that opens the gate (SC1).
    Create src/components/brand/AiFlagCard.tsx (pure presentational — receives `flag: SubmissionAiFlag | null` as prop; renders inside the client SubmissionDetailView, no 'use client' fetch). `if (!flag) return null` (hard gate — zero DOM, no skeleton). Render `<section aria-label={t('card_heading')}>` → BrandCard (border-slate-200, shadow-none, rounded-md) → BrandCard.Header (p-3): BrandHeading size="h3" t('card_heading') + Bot icon size16 text-slate-400 + BrandBadge variant="neutral" t('advisory_badge'); BrandCard.Body (p-4): `<dl>` per active signal with the matching lucide icon (ImageOff/Eye/MapPin/FileText/Copy, size16 text-slate-500 aria-hidden), `<dt>` signal label, `<dd>` anomalyDescription (14px), BrandBadge variant=confidenceBadgeVariant(score) with aria-label="Güven skoru: {score} — {level}", mono score; then material suggestion row (t('suggested_classification_label') + workClassification) when present. confidenceBadgeVariant: >=0.75 'success', >=0.50 'warning', else 'destructive'. NO button / approve / reject affordance (AI-03). NO amber on the card (amber reserved for the strip).
    Add the `dashboard.admin.ai_flags` namespace to BOTH messages/tr.json and messages/en.json with keys: card_heading, advisory_badge, suggested_classification_label, confidence_high, confidence_medium, confidence_low, strip_indicator_tooltip, col_ai_flag — TR and EN copy exactly per UI-SPEC Copywriting Contract.
  </action>
  <verify>
    <automated>grep -qE "evalPassed.*true|eval_passed = true" src/actions/ai-flags.ts && grep -q "if (!flag) return null" src/components/brand/AiFlagCard.tsx && ! grep -qiE "approve|reject|onClick" src/components/brand/AiFlagCard.tsx && node -e "const tr=require('./messages/tr.json'),en=require('./messages/en.json'); const k=['card_heading','advisory_badge','suggested_classification_label','confidence_high','confidence_medium','confidence_low','strip_indicator_tooltip']; for(const x of k){if(!tr.dashboard.admin.ai_flags[x]||!en.dashboard.admin.ai_flags[x])process.exit(1)}" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - getSubmissionAiFlag query filters `evalPassed = true` (SC1 single gate)
    - AiFlagCard contains `if (!flag) return null` (zero-DOM absent state, SC3)
    - AiFlagCard has no approve/reject/onClick affordance (`grep -iE "approve|reject|onClick" src/components/brand/AiFlagCard.tsx` returns nothing) (AI-03)
    - all 7 ai_flags copy keys present in BOTH tr.json and en.json
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Gated read action + advisory card + bilingual copy shipped; card is zero-DOM when no eval-passed flag.</done>
</task>

<task type="auto">
  <name>Task 2: Mount AiFlagCard in SubmissionDetailView + add amber dot to ChainageTable</name>
  <read_first>
    - src/components/admin/SubmissionDetailView.tsx lines 1, 27–41, 283–292 (use client; imports; the inert Alert slot to replace)
    - the page RSC that renders SubmissionDetailView (find via grep "SubmissionDetailView" in src/app) — it must call getSubmissionAiFlag and pass aiFlag prop
    - src/lib/chainage-data.ts lines 29–48 (ChainageBucket type), 107–199 (CTE query), 207–230 (row→bucket map)
    - src/components/dashboard/ChainageTable.tsx lines 131–215 (BrandTable.Head + Row/Cell)
    - src/components/ui/tooltip.tsx (Tooltip primitive)
    - .planning/phases/16-ai-vision-assist/16-PATTERNS.md "SubmissionDetailView.tsx" + "ChainageTable.tsx" sections
    - .planning/phases/16-ai-vision-assist/16-UI-SPEC.md "Amber strip indicator"
  </read_first>
  <action>
    SubmissionDetailView: add `aiFlag?: SubmissionAiFlag | null` to SubmissionDetailViewProps; import AiFlagCard from '@/components/brand/AiFlagCard'; replace the inert Alert block (lines ~283–292, the Sparkles/ai_slot_label/ai_slot_body placeholder) with `<AiFlagCard flag={aiFlag ?? null} />`. Update the page RSC that renders SubmissionDetailView to `const aiFlag = await getSubmissionAiFlag(submission.id)` and pass `aiFlag={aiFlag}`. Remove the now-unused ai_slot_label/ai_slot_body usages (leave the i18n keys in place to avoid churn unless trivially removable).
    chainage-data.ts: add `hasAiFlag: boolean` to the ChainageBucket type; extend the aggregation query with a LEFT JOIN / FILTER on submission_ai_flags (joined via submission id within the bucket) `WHERE eval_passed = true`, producing a per-bucket count; set `hasAiFlag: count > 0` in the row→bucket map. Keep the GROUP BY clamped-expression invariant intact (CR-01 — use the identical clamped expression).
    ChainageTable.tsx: add a far-right BrandTable.Head with `<span className="sr-only">{t('col_ai_flag')}</span>`; in the row map add a BrandTable.Cell rendering, when `bucket.hasAiFlag`, a shadcn Tooltip whose trigger is `<span className="inline-block size-2 rounded-full bg-amber-500" aria-hidden="true" />` and whose content is `t('strip_indicator_tooltip')`; render nothing (empty cell) when false. No animation, no pulse.
  </action>
  <verify>
    <automated>grep -q "AiFlagCard" src/components/admin/SubmissionDetailView.tsx && ! grep -q "ai_slot_body" src/components/admin/SubmissionDetailView.tsx && grep -q "hasAiFlag" src/lib/chainage-data.ts && grep -q "bg-amber-500" src/components/dashboard/ChainageTable.tsx && DETAIL_PAGE=$(grep -rl 'SubmissionDetailView' src/app --include='*.tsx' | head -1) && [ -n "$DETAIL_PAGE" ] && grep -q 'getSubmissionAiFlag' "$DETAIL_PAGE" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - SubmissionDetailView renders `<AiFlagCard ...>` and no longer renders the inert ai_slot_body placeholder
    - the page RSC calls getSubmissionAiFlag and passes the aiFlag prop
    - ChainageBucket has `hasAiFlag`; the query filters `eval_passed = true`
    - ChainageTable renders the amber `size-2 bg-amber-500` dot inside a Tooltip only when hasAiFlag is true
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>AiFlagCard mounted (inert slot removed); amber dot wired to eval_passed-gated bucket data.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual + bilingual UAT of AiFlagCard and amber strip</name>
  <read_first>
    - .planning/phases/16-ai-vision-assist/16-UI-SPEC.md (the full visual/interaction/a11y contract)
  </read_first>
  <action>
    This is a verification checkpoint — no code is written. Claude has already shipped AiFlagCard, the SubmissionDetailView mount, and the ChainageTable amber dot in Tasks 1–2; the human now confirms the rendered result matches the UI-SPEC in both locales. If issues are reported, Claude fixes them in a follow-up edit to Task 1/2 files before re-requesting approval.
  </action>
  <what-built>
    With the eval gate open (Plan 04), a submission with an eval_passed flag now renders AiFlagCard on its detail page and an amber dot on its chainage bucket. This checkpoint verifies the visual contract in BOTH locales.
  </what-built>
  <how-to-verify>
    1. Visit the submission detail page for a submission that has an eval_passed=true flag. Confirm: AiFlagCard renders with the "AI Değerlendirmesi" heading, a "Tavsiye Niteliğinde" neutral badge, per-signal rows with traffic-light confidence badges (green/orange/red) + mono score, and the "Önerilen Sınıflandırma:" material suggestion. No approve/reject button inside the card.
    2. Visit a submission with NO eval_passed flag — confirm zero AiFlagCard DOM (no empty card, no placeholder).
    3. Open the chainage as-built view — confirm an amber dot appears on buckets with a flagged submission; hover shows "Bu segmentte AI tavsiyesi var".
    4. Toggle EN locale — confirm "AI Assessment" / "Advisory" / "Suggested Classification:" / "AI advisory on this segment" render correctly.
  </how-to-verify>
  <resume-signal>Type "approved" once both locales render the card + amber dot per UI-SPEC, or describe visual issues.</resume-signal>
  <acceptance_criteria>
    - AiFlagCard renders for an eval_passed flag with heading, advisory badge, confidence badges, material suggestion — no decision affordance
    - absent state = zero DOM
    - amber dot + tooltip present on flagged chainage buckets
    - TR and EN copy both correct
  </acceptance_criteria>
  <done>Visual + bilingual UAT approved against UI-SPEC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB flag row → rendered UI | Only eval_passed=true rows may reach the UI; the card must never imply a required action |
| AI advisory → auditor mental model | Card must read as non-blocking hint, not an authorization control (AI-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-UG | Spoofing / Tampering | getSubmissionAiFlag returning unvalidated flags | mitigate | query hard-filters `evalPassed = true`; null on error; the single read gate (SC1) |
| T-16-AD2 | Tampering | AiFlagCard offering an approve/reject control | mitigate | card has zero decision affordance (grep gate); neutral "Tavsiye Niteliğinde" badge enforces advisory framing (AI-03) |
| T-16-ID | Information Disclosure | AI error surfacing on detail page | mitigate | getSubmissionAiFlag catches errors → returns null; card simply does not render (UI-SPEC) |
</threat_model>

<verification>
- `npx tsc --noEmit` passes
- grep gates: evalPassed=true gate, if(!flag) return null, no approve/reject in card, hasAiFlag + bg-amber-500 wired
- all 7 ai_flags i18n keys present in TR + EN
- visual + bilingual UAT approved
</verification>

<success_criteria>
- SC3: AiFlagCard shows for eval-passed flags, absent otherwise
- SC1: eval_passed is the single read gate
- AI-03: advisory-only card, no decision control; amber strip indicator live
</success_criteria>

<output>
Create `.planning/phases/16-ai-vision-assist/16-05-SUMMARY.md` when done
</output>
