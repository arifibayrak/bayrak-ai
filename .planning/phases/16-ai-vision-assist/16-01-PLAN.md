---
phase: 16-ai-vision-assist
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema/ai-flags.ts
  - src/db/migrations/0014_v4_ai_flags_phash.sql
  - src/db/migrations/meta/_journal.json
  - .env.example
autonomous: false
requirements: [AI-06]
user_setup:
  - service: vercel-ai-gateway
    why: "Claude vision API calls via AI Gateway"
    env_vars:
      - name: AI_GATEWAY_API_KEY
        source: "Vercel Dashboard -> Project -> Settings -> AI Gateway (or `vercel env pull` via OIDC)"
  - service: vercel-cron
    why: "Authorize the /api/cron/ai-flags retry endpoint"
    env_vars:
      - name: CRON_SECRET
        source: "Generate a random secret (openssl rand -hex 32); add to Vercel project env + .env.local"

must_haves:
  truths:
    - "submission_ai_flags has a phash_hex column for near-duplicate detection"
    - "AI SDK v6 + Zod v4 Output.object compiles and returns a typed object"
    - "CRON_SECRET and AI_GATEWAY_API_KEY are documented in .env.example"
  artifacts:
    - path: "src/db/schema/ai-flags.ts"
      provides: "phashHex column in the Drizzle schema"
      contains: "phashHex"
    - path: "src/db/migrations/0014_v4_ai_flags_phash.sql"
      provides: "ALTER TABLE adding phash_hex + partial index"
      contains: "phash_hex"
  key_links:
    - from: "src/db/migrations/0014_v4_ai_flags_phash.sql"
      to: "submission_ai_flags table (both Neon branches)"
      via: "npx tsx src/db/migrate.ts"
      pattern: "ALTER TABLE .*submission_ai_flags.* ADD COLUMN .*phash_hex"
---

<objective>
Lay the schema + environment foundation for Phase 16. Add the `phash_hex` column to `submission_ai_flags` (RESEARCH finding #4 / Pitfall 7 — the column is missing from the Phase 14 schema and is mandatory for pHash near-duplicate detection, AI-06), apply the migration to BOTH Neon branches via the project's established runner, document the two new env vars, and prove the AI SDK v6 + Zod v4 stack compiles before any AI code is written.

Purpose: Every downstream plan depends on this. The pHash pre-filter writes/reads `phash_hex`; build & typecheck pass without the live column (types come from the schema file), so the migration MUST be applied here or Phase 16 silently false-positives verification.
Output: `phash_hex` column live on both branches; env vars documented; Zod v4 / Output.object compatibility confirmed (RESEARCH Open Question 4).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-ai-vision-assist/16-RESEARCH.md
@.planning/phases/16-ai-vision-assist/16-PATTERNS.md

<interfaces>
<!-- Existing submission_ai_flags schema — add phashHex; do NOT remove any existing column/index. -->
From src/db/schema/ai-flags.ts (existing columns, do not re-read):
  submissionAiFlags = pgTable('submission_ai_flags', {
    id, tenantId, submissionId, status, photoAnomalyScore (numeric 4,3),
    workClassification (text), anomalyDescription (text), evalPassed (boolean),
    rawResponse (jsonb), createdAt, updatedAt
  })
  indexes: submission_ai_flags_submission_id_unique (UNIQUE on submissionId),
           submission_ai_flags_submission_idx, submission_ai_flags_status_idx

Migration runner (D-49 — drizzle-kit push is UNUSABLE on this project, spatial_ref_sys permission error):
  - Hand-written SQL in src/db/migrations/NNNN_*.sql
  - Apply via: npx tsx src/db/migrate.ts
  - Latest applied tag: 0013_v4_chainage_backfill (idx 13 in meta/_journal.json)
  - Next tag: 0014_v4_ai_flags_phash
  - Migration files use `--> statement-breakpoint` between statements (neon-http cannot run multiple commands per prepared statement)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add phashHex to schema + write migration 0014</name>
  <read_first>
    - src/db/schema/ai-flags.ts (the file being modified — full content already in plan interfaces)
    - src/db/migrations/0013_v4_chainage_backfill.sql (most recent migration — statement-breakpoint + header-comment convention)
    - src/db/migrations/0011_v4_ai_flags.sql (the original ai_flags migration — index naming convention)
    - src/db/migrations/meta/_journal.json (journal entry shape; latest idx = 13)
    - .planning/phases/16-ai-vision-assist/16-RESEARCH.md "Pattern 6: Schema addition" and "Pitfall 7"
    - .planning/phases/16-ai-vision-assist/16-PATTERNS.md "src/db/schema/ai-flags.ts" section
  </read_first>
  <action>
    Add `phashHex: text('phash_hex')` (nullable; 64-char binary string from sharp-phash) to the `submissionAiFlags` columns in src/db/schema/ai-flags.ts, placed after `rawResponse` and before `createdAt`. Do NOT add the partial index via the Drizzle index API (it is a partial index, not expressible cleanly) — keep the existing three indexes unchanged.
    Hand-write src/db/migrations/0014_v4_ai_flags_phash.sql following the 0013 header-comment + `--> statement-breakpoint` convention. Two statements: `ALTER TABLE "submission_ai_flags" ADD COLUMN "phash_hex" text;` then `CREATE INDEX "submission_ai_flags_phash_idx" ON "submission_ai_flags" USING btree ("phash_hex") WHERE "phash_hex" IS NOT NULL AND "status" = 'done';`. Add a header comment noting "Apply only via npx tsx src/db/migrate.ts (D-49); do NOT re-run drizzle-kit generate."
    Append a journal entry to src/db/migrations/meta/_journal.json with idx 14, version "7", tag "0014_v4_ai_flags_phash", breakpoints true, and a `when` timestamp greater than the idx-13 value.
  </action>
  <verify>
    <automated>grep -q "phashHex" src/db/schema/ai-flags.ts && grep -q "phash_hex" src/db/migrations/0014_v4_ai_flags_phash.sql && grep -q "0014_v4_ai_flags_phash" src/db/migrations/meta/_journal.json && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c phashHex src/db/schema/ai-flags.ts` returns >= 1
    - 0014 migration contains both `ADD COLUMN "phash_hex" text` and the partial index `WHERE "phash_hex" IS NOT NULL AND "status" = 'done'`
    - `_journal.json` has an entry with `tag: "0014_v4_ai_flags_phash"` and idx 14
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>phash_hex is in both the Drizzle schema and migration 0014; journal updated; typecheck clean.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Apply migration 0014 to BOTH Neon branches</name>
  <read_first>
    - src/db/migrate.ts (the runner — loads .env.local, enables PostGIS, runs migrate())
    - .planning/STATE.md "Migration protocol" decision (apply to dev + test branches)
  </read_first>
  <what-built>
    Migration 0014 adds `phash_hex` + partial index to `submission_ai_flags`. This is the BLOCKING schema-push gate: build/typecheck pass WITHOUT the live column (types come from the schema file), so the column must be applied to the live DBs here before any pHash code runs against it.
  </what-built>
  <action>
    Apply to the dev branch: `npx tsx src/db/migrate.ts` (uses DATABASE_URL from .env.local).
    Apply to the test branch: export the test DB URL then run the same runner — `DATABASE_URL="$TEST_DATABASE_URL" npx tsx src/db/migrate.ts` (the runner reads DATABASE_URL with override:false, so an explicitly-exported value wins per src/db/migrate.ts comment). Confirm `TEST_DATABASE_URL` is set in .env.local first.
    NOTE: drizzle-kit push is forbidden (D-49 — spatial_ref_sys permission error). Only the migrate.ts runner is valid. This task is flagged non-autonomous because the executor must confirm both branch URLs are present and the apply succeeded before proceeding.
  </action>
  <how-to-verify>
    After both applies print "Migrations complete", confirm the column exists. The downstream proof is Plan 02's pHash write; for this checkpoint, verify the migrate runner exited 0 on both branches and no "column already exists" or permission error appeared.
  </how-to-verify>
  <resume-signal>Type "applied" once 0014 is live on both dev and test branches, or describe the error.</resume-signal>
  <acceptance_criteria>
    - `npx tsx src/db/migrate.ts` prints "Migrations complete" against the dev branch
    - The same runner with TEST_DATABASE_URL prints "Migrations complete" against the test branch
    - No "permission denied" / "spatial_ref_sys" / "column already exists" errors
  </acceptance_criteria>
  <done>phash_hex column + partial index live on BOTH Neon branches.</done>
</task>

<task type="auto">
  <name>Task 3: Document env vars + smoke-test AI SDK v6 / Zod v4 compatibility</name>
  <read_first>
    - .env.example (the file being modified — confirm current keys; AI_GATEWAY_API_KEY / CRON_SECRET absent per RESEARCH Environment Availability)
    - .planning/phases/16-ai-vision-assist/16-RESEARCH.md "Open Questions #4" (Zod v4 + Output.object) and "Pattern 1"
    - .planning/phases/16-ai-vision-assist/16-PATTERNS.md "src/lib/ai-vision.ts" AI SDK v6 core pattern
  </read_first>
  <action>
    Add `AI_GATEWAY_API_KEY=` and `CRON_SECRET=` to .env.example with one-line comments (AI Gateway key from Vercel dashboard or OIDC; CRON_SECRET = random hex for cron auth).
    Write a throwaway smoke verification in a scratch test file `tests/ai-sdk-smoke.test.ts`: import `{ generateText, Output } from 'ai'` and `{ z } from 'zod'`, construct `Output.object({ schema: z.object({ ok: z.boolean() }) })`, and assert the call constructs without throwing (do NOT make a real API call — assert the Output.object factory returns a defined value). This resolves RESEARCH Open Question 4 (Zod v4 + Output.object spelling) before the full schema is built in Plan 02. Gate any real-call assertion behind `process.env.AI_EVAL_ENABLED === 'true'` so CI never spends tokens.
    Confirm `ls node_modules/ai node_modules/@ai-sdk/gateway node_modules/sharp-phash node_modules/sharp` all resolve (packages are in package.json; verify installed).
  </action>
  <verify>
    <automated>grep -q "AI_GATEWAY_API_KEY" .env.example && grep -q "CRON_SECRET" .env.example && ls node_modules/ai node_modules/@ai-sdk/gateway node_modules/sharp-phash node_modules/sharp >/dev/null && npx vitest run tests/ai-sdk-smoke.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `.env.example` contains both `AI_GATEWAY_API_KEY` and `CRON_SECRET`
    - All four package dirs resolve under node_modules
    - `tests/ai-sdk-smoke.test.ts` asserts `Output.object({ schema: z.object(...) })` is defined and exits 0
    - Smoke test makes NO real API call unless `AI_EVAL_ENABLED=true`
  </acceptance_criteria>
  <done>Env vars documented; AI SDK v6 import + Output.object + Zod v4 compatibility confirmed (Open Question 4 resolved); packages confirmed installed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local/CI → Neon DB | Migration runner writes DDL to both branches; ambiguous DDL could be applied to the wrong branch |
| repo → external registry | Four AI/image packages already installed (legitimacy audit cleared in RESEARCH) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | Migration applied to only one branch | mitigate | BLOCKING checkpoint requires explicit confirmation of BOTH dev + test applies before proceeding |
| T-16-SC | Tampering | npm packages (ai, @ai-sdk/gateway, sharp-phash, sharp) | accept | RESEARCH Package Legitimacy Audit rated all four [OK]; already in package.json + installed; no postinstall scripts |
| T-16-02 | Information Disclosure | CRON_SECRET / AI_GATEWAY_API_KEY in .env.example | mitigate | .env.example carries empty placeholders only; real values live in .env.local + Vercel env (never committed) |
</threat_model>

<verification>
- `npx tsc --noEmit` passes with phashHex in schema
- Migration 0014 applied to both Neon branches (BLOCKING checkpoint)
- `npx vitest run tests/ai-sdk-smoke.test.ts` exits 0
</verification>

<success_criteria>
- phash_hex column live on both branches (unblocks pHash in Plan 02)
- AI SDK v6 + Zod v4 Output.object confirmed compatible
- AI_GATEWAY_API_KEY + CRON_SECRET documented
</success_criteria>

<output>
Create `.planning/phases/16-ai-vision-assist/16-01-SUMMARY.md` when done
</output>
