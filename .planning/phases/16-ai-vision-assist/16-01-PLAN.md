---
phase: 16-ai-vision-assist
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema/ai-flags.ts
  - src/db/migrations/0014_v4_ai_flags_phash.sql
  - src/db/migrations/meta/_journal.json
  - .env.example
  - package.json
  - package-lock.json
autonomous: false
requirements: [AI-06]
user_setup:
  - service: vercel-ai-gateway
    why: "Claude vision API calls via AI Gateway"
    env_vars:
      - name: AI_GATEWAY_API_KEY
        source: "Vercel Dashboard -> Project -> Settings -> AI Gateway (or `vercel env pull` via OIDC)"
  - service: vercel-cron
    why: "Authorize the /api/cron/ai-flags retry endpoint"
    env_vars:
      - name: CRON_SECRET
        source: "Generate a random secret (openssl rand -hex 32); add to Vercel project env + .env.local"

must_haves:
  truths:
    - "submission_ai_flags has a phash_hex column for near-duplicate detection"
    - "submission_ai_flags has a queryable anomaly_detected boolean so the gate keys on all five signals, not just mismatch confidence"
    - "AI SDK v6 + Zod v4 Output.object compiles AND a real gated vision call returns the typed `output` field (Open Question 4 + v6 runtime contract resolved)"
    - "CRON_SECRET and AI_GATEWAY_API_KEY are documented in .env.example"
    - "sharp is a declared direct dependency so a clean `npm ci` / Vercel build resolves the pHash stack"
    - "@vercel/functions is a declared dependency so Plan 03's waitUntil dispatch resolves at build time"
  artifacts:
    - path: "src/db/schema/ai-flags.ts"
      provides: "phashHex + anomalyDetected columns in the Drizzle schema"
      contains: "phashHex"
    - path: "src/db/migrations/0014_v4_ai_flags_phash.sql"
      provides: "ALTER TABLE adding phash_hex + anomaly_detected + partial index"
      contains: "phash_hex"
    - path: "package.json"
      provides: "sharp + @vercel/functions declared as direct dependencies"
      contains: "sharp"
  key_links:
    - from: "src/db/migrations/0014_v4_ai_flags_phash.sql"
      to: "submission_ai_flags table (both Neon branches)"
      via: "npx tsx src/db/migrate.ts"
      pattern: "ALTER TABLE .*submission_ai_flags.* ADD COLUMN .*phash_hex"
---

<objective>
Lay the schema + environment foundation for Phase 16. Add the `phash_hex` column AND a queryable `anomaly_detected` boolean to `submission_ai_flags` (RESEARCH finding #4 / Pitfall 7 — phash_hex is missing from the Phase 14 schema and is mandatory for pHash near-duplicate detection, AI-06; `anomaly_detected` is the multi-signal gate column added per REVIEWS HIGH-3 so the eval gate / read query / chainage join key on all five D-01 signals, not just mismatch confidence). Apply the migration to BOTH Neon branches via the project's established runner, document the two new env vars, declare `sharp` AND `@vercel/functions` as direct dependencies (sharp is currently only a transitive peer of `sharp-phash`; `@vercel/functions` is required by Plan 03's `waitUntil` dispatch and is NOT yet installed), and prove the AI SDK v6 + Zod v4 stack compiles AND its v6 runtime contract holds before any AI code is written.

Purpose: Every downstream plan depends on this. The pHash pre-filter writes/reads `phash_hex`; the gate (Plan 04 opener + Plan 05 read/join) keys on `anomaly_detected`; build & typecheck pass without the live columns (types come from the schema file), so the migration MUST be applied here or Phase 16 silently false-positives verification. `@vercel/functions` must be a declared dep here so Plan 03's `import { waitUntil } from '@vercel/functions'` resolves at Vercel build time (per the CONTEXT.md OVERRIDE 2026-05-31 adopting waitUntil over a bare detached promise).
Output: `phash_hex` + `anomaly_detected` columns live on both branches; env vars documented; `sharp` + `@vercel/functions` declared in package.json + lockfile; AI SDK v6 / Zod v4 / `Output.object` compatibility confirmed AND the v6 runtime contract (`generateText` returns a typed `output` field) validated by one gated real call (RESEARCH Open Question 4 + the v6-API-shape risk).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-ai-vision-assist/16-RESEARCH.md
@.planning/phases/16-ai-vision-assist/16-PATTERNS.md

<interfaces>
<!-- Existing submission_ai_flags schema — add phashHex + anomalyDetected; do NOT remove any existing column/index. -->
From src/db/schema/ai-flags.ts (existing columns, do not re-read):
  submissionAiFlags = pgTable('submission_ai_flags', {
    id, tenantId, submissionId, status, photoAnomalyScore (numeric 4,3),
    workClassification (text), anomalyDescription (text), evalPassed (boolean),
    rawResponse (jsonb), createdAt, updatedAt
  })
  indexes: submission_ai_flags_submission_id_unique (UNIQUE on submissionId),
           submission_ai_flags_submission_idx, submission_ai_flags_status_idx

NEW columns this plan adds (after rawResponse, before createdAt):
  phashHex (text, nullable)        — sharp-phash 64-char binary string (AI-06)
  anomalyDetected (boolean, null)  — TRUE if ANY of the five D-01 signals fired (the multi-signal gate;
                                     downstream gate/read/join key on THIS, not photoAnomalyScore; REVIEWS HIGH-3)

Migration runner (D-49 — drizzle-kit push is UNUSABLE on this project, spatial_ref_sys permission error):
  - Hand-written SQL in src/db/migrations/NNNN_*.sql
  - Apply via: npx tsx src/db/migrate.ts  (also exposed as `npm run migrate`)
  - Latest applied tag: 0013_v4_chainage_backfill (idx 13 in meta/_journal.json)
  - Next tag: 0014_v4_ai_flags_phash
  - Migration files use `--> statement-breakpoint` between statements (neon-http cannot run multiple commands per prepared statement)

Package manager (CONFIRMED): npm — the lockfile is package-lock.json (NOT pnpm). Use `npm install`, not `pnpm add`.
Package status (CONFIRMED against package.json + package-lock.json):
  - `ai@^6.0.193`, `@ai-sdk/gateway@^3.0.121`, `sharp-phash@^2.2.0` ARE declared dependencies (RESEARCH's "not installed" note is stale).
  - `sharp` is NOT a declared dependency — it resolves at install time only as a peer of sharp-phash. A clean `npm ci` could hoist differently or fail; it must be declared explicitly.
  - `@vercel/functions` is NOT installed at all (confirmed absent from package.json AND node_modules). Plan 03 dispatches `runAiAnalysis` via `import { waitUntil } from '@vercel/functions'` (the CONTEXT.md OVERRIDE 2026-05-31 async primitive), so this package MUST be added here. `waitUntil` is request-scoped and needs no Auth.js session (Pitfall 5 still holds — see Plan 03).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add phashHex + anomalyDetected to schema + write migration 0014</name>
  <read_first>
    - src/db/schema/ai-flags.ts (the file being modified — full content already in plan interfaces)
    - src/db/migrations/0013_v4_chainage_backfill.sql (most recent migration — statement-breakpoint + header-comment convention)
    - src/db/migrations/0011_v4_ai_flags.sql (the original ai_flags migration — index naming convention)
    - src/db/migrations/meta/_journal.json (journal entry shape; latest idx = 13)
    - .planning/phases/16-ai-vision-assist/16-RESEARCH.md "Pattern 6: Schema addition" and "Pitfall 7"
    - .planning/phases/16-ai-vision-assist/16-PATTERNS.md "src/db/schema/ai-flags.ts" section
  </read_first>
  <action>
    Add TWO nullable columns to the `submissionAiFlags` columns in src/db/schema/ai-flags.ts, placed after `rawResponse` and before `createdAt`:
      (a) `phashHex: text('phash_hex')` — 64-char binary string from sharp-phash (AI-06 near-duplicate detection).
      (b) `anomalyDetected: boolean('anomaly_detected')` — the QUERYABLE multi-signal gate. TRUE when ANY of the D-01 advisory anomaly signals fired (photo≠work OR photo-quality OR location-inconsistent OR duplicate), NOT just photo-mismatch. It exists so the `eval_passed` opener (Plan 04), `getSubmissionAiFlag` (Plan 05), and the chainage LEFT JOIN (Plan 05) key the gate on the real multi-signal set without re-parsing `rawResponse` jsonb (REVIEWS HIGH-3 — `photo_anomaly_score` alone conflates five signals into mismatch-confidence and silently hides quality/location/duplicate-only flags). `runAiAnalysis` (Plan 02) writes it; the eval harness (Plan 02/04) reads the same field as `predicted`. (`materialSuggestion` / AI-02 is advisory-display only and is NOT part of `anomalyDetected` — see Plan 04 MEDIUM-5 scoping note.) Import `boolean` from `drizzle-orm/pg-core` if not already imported.
    Do NOT add the partial index for phash via the Drizzle index API (it is a partial index, not expressible cleanly) — keep the existing three indexes unchanged.
    Hand-write src/db/migrations/0014_v4_ai_flags_phash.sql following the 0013 header-comment + `--> statement-breakpoint` convention. Three statements: `ALTER TABLE "submission_ai_flags" ADD COLUMN "phash_hex" text;` then `ALTER TABLE "submission_ai_flags" ADD COLUMN "anomaly_detected" boolean;` then `CREATE INDEX "submission_ai_flags_phash_idx" ON "submission_ai_flags" USING btree ("phash_hex") WHERE "phash_hex" IS NOT NULL AND "status" = 'done';`. Add a header comment noting "Apply only via npx tsx src/db/migrate.ts (D-49); do NOT re-run drizzle-kit generate."
    Append a journal entry to src/db/migrations/meta/_journal.json with idx 14, version "7", tag "0014_v4_ai_flags_phash", breakpoints true, and a `when` timestamp greater than the idx-13 value.
  </action>
  <verify>
    <automated>grep -q "phashHex" src/db/schema/ai-flags.ts && grep -q "anomalyDetected" src/db/schema/ai-flags.ts && grep -q "phash_hex" src/db/migrations/0014_v4_ai_flags_phash.sql && grep -q "anomaly_detected" src/db/migrations/0014_v4_ai_flags_phash.sql && grep -q "0014_v4_ai_flags_phash" src/db/migrations/meta/_journal.json && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c phashHex src/db/schema/ai-flags.ts` returns >= 1 and `grep -c anomalyDetected src/db/schema/ai-flags.ts` returns >= 1
    - 0014 migration contains `ADD COLUMN "phash_hex" text`, `ADD COLUMN "anomaly_detected" boolean`, and the partial phash index `WHERE "phash_hex" IS NOT NULL AND "status" = 'done'`
    - `_journal.json` has an entry with `tag: "0014_v4_ai_flags_phash"` and idx 14
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>phash_hex + anomaly_detected are in both the Drizzle schema and migration 0014; journal updated; typecheck clean.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Apply migration 0014 to BOTH Neon branches</name>
  <read_first>
    - src/db/migrate.ts (the runner — loads .env.local, enables PostGIS, runs migrate())
    - .planning/STATE.md "Migration protocol" decision (apply to dev + test branches)
  </read_first>
  <what-built>
    Migration 0014 adds `phash_hex` + `anomaly_detected` + a partial index to `submission_ai_flags`. This is the BLOCKING schema-push gate: build/typecheck pass WITHOUT the live columns (types come from the schema file), so the columns must be applied to the live DBs here before any pHash / gate code runs against them.
  </what-built>
  <action>
    Apply to the dev branch: `npm run migrate` (alias for `tsx src/db/migrate.ts`; uses DATABASE_URL from .env.local).
    Apply to the test branch: `npm run migrate:test` (the package.json script already wires `DATABASE_URL="$TEST_DATABASE_URL"` via dotenv; the runner reads DATABASE_URL with override:false so the explicit value wins). Confirm `TEST_DATABASE_URL` is set in .env.local first.
    NOTE: drizzle-kit push is forbidden (D-49 — spatial_ref_sys permission error). Only the migrate.ts runner is valid. This task is flagged non-autonomous because the executor must confirm both branch URLs are present and the apply succeeded before proceeding.
  </action>
  <how-to-verify>
    After both applies print "Migrations complete", confirm the columns exist. The downstream proof is Plan 02's pHash + anomaly_detected write; for this checkpoint, verify the migrate runner exited 0 on both branches and no "column already exists" or permission error appeared.
  </how-to-verify>
  <resume-signal>Type "applied" once 0014 is live on both dev and test branches, or describe the error.</resume-signal>
  <acceptance_criteria>
    - `npm run migrate` prints "Migrations complete" against the dev branch
    - `npm run migrate:test` prints "Migrations complete" against the test branch
    - No "permission denied" / "spatial_ref_sys" / "column already exists" errors
  </acceptance_criteria>
  <done>phash_hex + anomaly_detected columns + partial index live on BOTH Neon branches.</done>
</task>

<task type="auto">
  <name>Task 3: Declare sharp + @vercel/functions + document env vars + smoke-test the AI SDK v6 runtime contract</name>
  <read_first>
    - package.json dependencies (confirm `ai`, `@ai-sdk/gateway`, `sharp-phash` already present; `sharp` and `@vercel/functions` absent)
    - .env.example (the file being modified — confirm current keys; AI_GATEWAY_API_KEY / CRON_SECRET absent per RESEARCH Environment Availability)
    - .planning/phases/16-ai-vision-assist/16-RESEARCH.md "Open Questions #4" (Zod v4 + Output.object), "Pattern 1" (generateText returns `{ output }`), "Key API notes" (model string, image-URL content part)
    - .planning/phases/16-ai-vision-assist/16-PATTERNS.md "src/lib/ai-vision.ts" AI SDK v6 core pattern
  </read_first>
  <action>
    Declare `sharp` and `@vercel/functions` as direct dependencies. `ai`, `@ai-sdk/gateway`, and `sharp-phash` are already in package.json + package-lock.json. `sharp` is only a transitive peer of `sharp-phash` and MUST be declared so a clean `npm ci` / Vercel build resolves it deterministically; `@vercel/functions` is not installed at all and is required by Plan 03's `import { waitUntil } from '@vercel/functions'`. Run `npm install sharp @vercel/functions` (this project uses npm — NOT pnpm; the lockfile is package-lock.json). Pin `sharp` to the version already resolved in the lockfile (sharp ^0.34.x as installed under node_modules) so the install does not bump unrelated deps; take the latest stable `@vercel/functions` (it has no postinstall scripts — confirm in the legitimacy note). If the combined install would change `ai`/`@ai-sdk/gateway`/`sharp-phash` versions, abort and run `npm install sharp@<installed-version> @vercel/functions` instead to keep the change scoped.
    Add `AI_GATEWAY_API_KEY=` and `CRON_SECRET=` to .env.example with one-line comments (AI Gateway key from Vercel dashboard or OIDC; CRON_SECRET = random hex for cron auth).
    Write a smoke verification in `tests/ai-sdk-smoke.test.ts` with TWO tiers:
      (1) ALWAYS-ON (no API): import `{ generateText, Output } from 'ai'` and `{ z } from 'zod'`, construct `Output.object({ schema: z.object({ ok: z.boolean() }) })`, and assert the factory returns a defined value (resolves RESEARCH Open Question 4 — the Zod v4 + `Output.object` spelling — at zero token cost).
      (2) GATED REAL CALL behind `process.env.AI_EVAL_ENABLED === 'true'` (use `it.skipIf(process.env.AI_EVAL_ENABLED !== 'true')` or a describeIf guard so default CI burns NO tokens): make ONE minimal real `generateText({ model: 'anthropic/claude-sonnet-4.6', output: Output.object({ schema: z.object({ ok: z.boolean() }) }), messages: [{ role:'user', content:'Reply with ok=true.' }] })` call and assert the RUNTIME v6 contract: the returned object has a typed `output` field (not just `text`) AND `output.ok` is a boolean — proving `generateText` actually returns `{ output }` under the experimental v6 Output API, and that `AI_GATEWAY_API_KEY` resolves (the call throwing an auth error = key not resolved → fail with a clear message). Keep the timeout at 30_000ms.
    This validates BOTH the v6 API SHAPE and the gateway key BEFORE the full schema lands in Plan 02, but only when explicitly enabled.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.dependencies.sharp)process.exit(1); if(!p.dependencies['@vercel/functions'])process.exit(1); for(const d of ['ai','@ai-sdk/gateway','sharp-phash']) if(!p.dependencies[d])process.exit(1)" && grep -q "AI_GATEWAY_API_KEY" .env.example && grep -q "CRON_SECRET" .env.example && grep -q "AI_EVAL_ENABLED" tests/ai-sdk-smoke.test.ts && npx vitest run tests/ai-sdk-smoke.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - package.json dependencies now include `sharp` AND `@vercel/functions` (alongside the pre-existing `ai`, `@ai-sdk/gateway`, `sharp-phash`); package-lock.json updated
    - `.env.example` contains both `AI_GATEWAY_API_KEY` and `CRON_SECRET`
    - `tests/ai-sdk-smoke.test.ts` has an always-on assertion that `Output.object({ schema: z.object(...) })` is defined AND a gated real call asserting `generateText` returns a typed `output` field (`output.ok` is boolean) behind `AI_EVAL_ENABLED`
    - default `npx vitest run tests/ai-sdk-smoke.test.ts` (no AI_EVAL_ENABLED) makes NO real API call and exits 0
    - the install did NOT change `ai`/`@ai-sdk/gateway`/`sharp-phash` versions (scoped to adding sharp + @vercel/functions)
  </acceptance_criteria>
  <done>sharp + @vercel/functions declared as direct dependencies (lockfile updated); env vars documented; AI SDK v6 import + Output.object + Zod v4 compatibility confirmed AND the v6 runtime contract (`generateText` → typed `output`) + gateway-key resolution validated via one gated call (Open Question 4 + v6-shape risk resolved).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local/CI → Neon DB | Migration runner writes DDL to both branches; ambiguous DDL could be applied to the wrong branch |
| repo → external registry | Five AI/image/runtime packages (legitimacy audit cleared in RESEARCH); sharp newly promoted from peer to direct dep; @vercel/functions newly added |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | Migration applied to only one branch | mitigate | BLOCKING checkpoint requires explicit confirmation of BOTH dev + test applies before proceeding |
| T-16-SC | Tampering | npm packages (ai, @ai-sdk/gateway, sharp-phash, sharp, @vercel/functions) | accept | RESEARCH Package Legitimacy Audit rated ai/@ai-sdk/gateway/sharp-phash/sharp [OK]; @vercel/functions is a first-party Vercel package (official scope `@vercel`, used by the deploy target) with no postinstall scripts — confirm absence of postinstall during install; ai/@ai-sdk/gateway/sharp-phash already declared; sharp promoted from an existing peer install |
| T-16-02 | Information Disclosure | CRON_SECRET / AI_GATEWAY_API_KEY in .env.example | mitigate | .env.example carries empty placeholders only; real values live in .env.local + Vercel env (never committed) |
</threat_model>

<verification>
- `npx tsc --noEmit` passes with phashHex + anomalyDetected in schema
- Migration 0014 applied to both Neon branches (BLOCKING checkpoint)
- package.json declares sharp + @vercel/functions + the three pre-existing AI packages; package-lock.json updated
- `npx vitest run tests/ai-sdk-smoke.test.ts` exits 0 (always-on tier; gated real call skipped without AI_EVAL_ENABLED)
</verification>

<success_criteria>
- phash_hex + anomaly_detected columns live on both branches (unblocks pHash + the multi-signal gate)
- AI SDK v6 + Zod v4 Output.object confirmed compatible AND the v6 runtime `{ output }` contract + gateway key validated by one gated call
- sharp + @vercel/functions declared as direct dependencies (clean-install safe; waitUntil resolves in Plan 03)
- AI_GATEWAY_API_KEY + CRON_SECRET documented
</success_criteria>

<output>
Create `.planning/phases/16-ai-vision-assist/16-01-SUMMARY.md` when done
</output>
