---
phase: 16-ai-vision-assist
plan: 03
type: execute
wave: 3
depends_on: ["16-02"]
files_modified:
  - src/lib/bot-audit.ts
  - src/app/api/cron/ai-flags/route.ts
  - vercel.json
autonomous: true
requirements: [AI-04]

must_haves:
  truths:
    - "After approval, handleAuditDecision calls enqueueAiFlag (which dispatches the analysis via waitUntil) AFTER the approval transaction commits and does NOT await it — webhook returns before AI work (AI-04 / SC2)"
    - "GET /api/cron/ai-flags rejects requests without Authorization: Bearer $CRON_SECRET (SC6)"
    - "The cron reclaims BOTH stale pending AND orphaned processing rows and retries runAiAnalysis (safety net for a mid-call function death)"
    - "vercel.json registers a crons entry for /api/cron/ai-flags (SC6)"
  artifacts:
    - path: "src/app/api/cron/ai-flags/route.ts"
      provides: "CRON_SECRET-guarded GET retry handler reclaiming pending + stale processing rows"
      exports: ["GET"]
    - path: "vercel.json"
      provides: "crons entry for /api/cron/ai-flags"
      contains: "/api/cron/ai-flags"
  key_links:
    - from: "src/lib/bot-audit.ts"
      to: "src/lib/ai-flag-queue.ts"
      via: "enqueueAiFlag post-commit (not awaited; enqueueAiFlag dispatches via waitUntil internally)"
      pattern: "enqueueAiFlag\\("
    - from: "src/app/api/cron/ai-flags/route.ts"
      to: "src/lib/ai-vision.ts"
      via: "runAiAnalysis on stale pending + processing rows"
      pattern: "runAiAnalysis"
---

<objective>
Wire the async path. Add the 4th post-commit best-effort block to `handleAuditDecision` that calls `enqueueAiFlag(submissionId, photoUrl)` (after adding `photoUrl` to the approval UPDATE `.returning()` clause — RESEARCH Pitfall 3 / Open Question 1). `enqueueAiFlag` (Plan 02) inserts the pending row and dispatches `runAiAnalysis` via Vercel `waitUntil` internally (CONTEXT OVERRIDE 2026-05-31 — a bare detached promise may never run once the webhook response flushes); handleAuditDecision must NOT await the enqueue so the webhook responds first (SC2). Build the `CRON_SECRET`-guarded `/api/cron/ai-flags` retry route that reclaims BOTH stale `pending` rows AND orphaned `processing` rows, registered in `vercel.json`.

Purpose: Implements AI-04 (async, off the Telegram critical path — webhook responds before AI work) and SC6 (cron retry, protected by CRON_SECRET). With the waitUntil dispatch in Plan 02 doing the inline work, the cron is the EXPLICIT safety net: it recovers both never-started (`pending`) rows and rows orphaned mid-call (`processing`) when the Fluid-Compute function dies before completing (REVIEWS HIGH-1b / MEDIUM-4).
Output: bot-audit enqueue wiring, cron route reclaiming pending+processing, vercel.json crons block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-ai-vision-assist/16-RESEARCH.md
@.planning/phases/16-ai-vision-assist/16-PATTERNS.md

<interfaces>
<!-- Consumed from Plan 02: -->
  enqueueAiFlag(submissionId: string, photoUrl: string): Promise<void>   // src/lib/ai-flag-queue.ts
                                                                         // inserts pending row + dispatches
                                                                         // runAiAnalysis via waitUntil internally
  runAiAnalysis(submissionId: string, photoUrl: string): Promise<void>   // src/lib/ai-vision.ts (NEVER throws)

<!-- bot-audit.ts approval UPDATE .returning() currently (lines 432–438) returns:
     { id, quantity, boqItemId, segmentFraction, projectId } as affected[].
     ADD photoUrl: sub2.photoUrl as the 6th field. Insertion point for the new block:
     after the hakkediş try/catch (~line 552), before the worker-lookup block (~line 554). -->

<!-- Cron route analog: src/app/api/exports/chainage/route.ts (runtime='nodejs', dynamic='force-dynamic'). -->
<!-- Cron query (REVIEWS HIGH-1b / MEDIUM-4 — reclaim BOTH never-started and orphaned-mid-call rows):
     SELECT submissionId + submissions.photoUrl WHERE
       (status='pending'    AND createdAt < now() - interval '5 minutes')
       OR (status='processing' AND updatedAt < now() - interval '15 minutes');
     for each: runAiAnalysis(...).catch(console.error). The 'processing' staleness window MUST exceed the
     max realistic analysis duration so an in-flight call is not double-dispatched; runAiAnalysis sets
     status='processing' with a fresh updatedAt at step 0, so a row still 'processing' after 15 min is
     orphaned (the function died after the webhook flushed) and is safe to retry. -->

<!-- vercel.json existing: { "functions": { "src/app/api/telegram/webhook/route.ts": { memory:512, maxDuration:55 } } }.
     ADD a sibling "crons": [{ "path": "/api/cron/ai-flags", "schedule": "0 * * * *" }]. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add photoUrl to .returning() + call enqueueAiFlag post-commit (not awaited) in handleAuditDecision</name>
  <read_first>
    - src/lib/bot-audit.ts lines 426–438 (.returning clause) + 516–552 (hakkediş post-commit block — exact shape to copy) + 529–533 (Pitfall 5 discipline comment)
    - .planning/phases/16-ai-vision-assist/16-PATTERNS.md "src/lib/bot-audit.ts" section (photoUrl addition + 4th block)
    - .planning/phases/16-ai-vision-assist/16-RESEARCH.md "Pattern 2" + "Pitfall 3"
    - .planning/phases/16-ai-vision-assist/16-CONTEXT.md D-04 "OVERRIDE (2026-05-31)" + Pitfall-5 note
  </read_first>
  <action>
    In the approval UPDATE `.returning()` (lines 432–438), add `photoUrl: sub2.photoUrl` as a 6th returned field so `affected[0].photoUrl` is available at the call site (Pitfall 3 fix).
    After the hakkediş try/catch (~line 552) and before the worker-lookup block (~line 554), insert a new best-effort block: lazy `const { enqueueAiFlag } = await import('@/lib/ai-flag-queue');` then call `enqueueAiFlag(submissionId, affected[0].photoUrl).catch((err) => console.error('[handleAuditDecision] AI flag enqueue error:', err));` — NOT awaited. enqueueAiFlag itself dispatches runAiAnalysis via waitUntil internally (Plan 02 — the CONTEXT OVERRIDE async primitive); bot-audit must NOT await it so the webhook response goes out first (SC2). Wrap in try/catch logging "AI flag enqueue setup failed". Copy the D-40 best-effort comment + Pitfall-5 discipline comment shape from the hakkediş block. This block runs OUTSIDE/after the approval transaction (post-commit) — never inside the TX. Do NOT call auth(), logOfficeActivity(), or after() (from next/server) here — the file already enforces this for hakkediş, and the waitUntil OVERRIDE lives inside enqueueAiFlag (Plan 02), NOT in bot-audit.ts; this file must stay session-free (Pitfall 5).
    Guard against a null photoUrl (`affected[0].photoUrl`) — if null, skip enqueue silently (some submissions may lack a photoUrl; only enqueue when a URL exists).
  </action>
  <verify>
    <automated>grep -qE "enqueueAiFlag\(.*\)\.catch" src/lib/bot-audit.ts && grep -q "photoUrl: sub2.photoUrl" src/lib/bot-audit.ts && [ "$(grep -c 'await enqueueAiFlag' src/lib/bot-audit.ts)" = "0" ] && [ "$(grep -c 'logOfficeActivity' src/lib/bot-audit.ts)" = "0" ] && [ "$(grep -c "from 'next/server'" src/lib/bot-audit.ts)" = "0" ] && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `.returning()` includes `photoUrl: sub2.photoUrl`
    - enqueueAiFlag is called and NOT awaited (`grep "await enqueueAiFlag" src/lib/bot-audit.ts` returns nothing) — the webhook response flushes before AI work (SC2); the waitUntil dispatch is internal to enqueueAiFlag (Plan 02)
    - `grep -c logOfficeActivity src/lib/bot-audit.ts` == 0 AND no `from 'next/server'` import (Pitfall 5 preserved — waitUntil lives in ai-flag-queue.ts, not here)
    - The enqueue block is positioned after the hakkediş block (post-commit), not inside the approval transaction
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>handleAuditDecision returns photoUrl and calls enqueueAiFlag (not awaited) post-commit; bot-path discipline intact; SC2 proven against the un-awaited enqueue.</done>
</task>

<task type="auto">
  <name>Task 2: CRON_SECRET-guarded /api/cron/ai-flags retry route (reclaims pending + stale processing)</name>
  <read_first>
    - src/app/api/cron/ai-flags/route.ts (does not yet exist — create it)
    - src/app/api/exports/chainage/route.ts lines 1–53 (runtime/dynamic directives + auth-first guard analog)
    - src/db/schema/ai-flags.ts + src/db/schema/submissions.ts (cron query join)
    - .planning/phases/16-ai-vision-assist/16-RESEARCH.md "Pattern 4" + "Cron route handler" code example
    - .planning/phases/16-ai-vision-assist/16-PATTERNS.md "src/app/api/cron/ai-flags/route.ts" section
  </read_first>
  <action>
    Create src/app/api/cron/ai-flags/route.ts with `export const runtime = 'nodejs';` and `export const dynamic = 'force-dynamic';`. Export `async function GET(request: Request)`. FIRST statement: read `request.headers.get('authorization')`; if it !== `` `Bearer ${process.env.CRON_SECRET}` `` return `new Response('Unauthorized', { status: 401 })`. Behind the guard, lazy-import db/schema/drizzle-orm/runAiAnalysis. Query STALE rows reclaiming BOTH classes (REVIEWS HIGH-1b / MEDIUM-4): select `{ id: submissionAiFlags.submissionId, photoUrl: submissions.photoUrl }` innerJoin submissions, `where(or(and(eq(status,'pending'), lt(createdAt, sql\`now() - interval '5 minutes'\`)), and(eq(status,'processing'), lt(updatedAt, sql\`now() - interval '15 minutes'\`))))`. The 'processing' branch reclaims rows orphaned when the waitUntil function died mid-call (status set to 'processing' at step 0 of runAiAnalysis but never advanced to 'done'/'error'); the 15-minute window safely exceeds a normal analysis so an in-flight call is not double-dispatched. For each row `await runAiAnalysis(row.id, row.photoUrl).catch(console.error)`, count processed. Return `Response.json({ processed })`. Do NOT import auth() — CRON_SECRET replaces it.
  </action>
  <verify>
    <automated>grep -q "Bearer \${process.env.CRON_SECRET}" src/app/api/cron/ai-flags/route.ts && grep -q "runtime = 'nodejs'" src/app/api/cron/ai-flags/route.ts && grep -q "interval '5 minutes'" src/app/api/cron/ai-flags/route.ts && grep -q "interval '15 minutes'" src/app/api/cron/ai-flags/route.ts && grep -q "'processing'" src/app/api/cron/ai-flags/route.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - GET's first behavior rejects a missing/wrong Authorization header with 401 (SC6)
    - route exports `runtime = 'nodejs'` and `dynamic = 'force-dynamic'`
    - query reclaims BOTH `status='pending' AND createdAt < now() - interval '5 minutes'` AND `status='processing' AND updatedAt < now() - interval '15 minutes'` (REVIEWS HIGH-1b / MEDIUM-4 — orphaned-processing recovery)
    - calls `runAiAnalysis(...).catch(console.error)` per stale row
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Cron route retries stale pending AND orphaned processing rows behind a CRON_SECRET bearer check.</done>
</task>

<task type="auto">
  <name>Task 3: Register cron in vercel.json</name>
  <read_first>
    - vercel.json (the file being modified — existing functions block)
    - .planning/phases/16-ai-vision-assist/16-PATTERNS.md "vercel.json" section (merge crons alongside functions)
    - .planning/phases/16-ai-vision-assist/16-RESEARCH.md "Pattern 4" + "Pitfall 5" (Hobby min 1-day; Pro min 1-min)
  </read_first>
  <action>
    Add a `"crons"` array as a sibling of the existing `"functions"` block: one entry `{ "path": "/api/cron/ai-flags", "schedule": "0 * * * *" }` (hourly — the conservative Hobby-safe default per RESEARCH Pitfall 5; the executor confirms the Vercel plan tier and may tighten to `*/5 * * * *` only if the project is on Pro). With the waitUntil dispatch (Plan 02) doing inline delivery, this hourly cron is the safety net that reclaims orphaned rows. Do not remove or alter the existing functions block.
  </action>
  <verify>
    <automated>node -e "const v=require('./vercel.json'); if(!Array.isArray(v.crons)||!v.crons.some(c=>c.path==='/api/cron/ai-flags'))process.exit(1); if(!v.functions)process.exit(1);" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - vercel.json parses as valid JSON
    - `crons` is an array containing an entry with `path === '/api/cron/ai-flags'`
    - the existing `functions` block is preserved unchanged
  </acceptance_criteria>
  <done>Cron registered in vercel.json; functions block intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → /api/cron/ai-flags | Anyone can hit the cron URL; must be authenticated |
| Telegram webhook → AI work | AI must not delay or block the webhook response (latency / duplicate-delivery risk) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-CR | Elevation of Privilege | GET /api/cron/ai-flags | mitigate | First statement checks `Authorization: Bearer $CRON_SECRET`; returns 401 before any DB access (SC6) — blocks unauthenticated analysis-trigger / cost-amplification |
| T-16-CP | Denial of Service | enqueueAiFlag awaited in webhook path | mitigate | enqueue called un-awaited; the waitUntil dispatch (Plan 02) keeps the function alive AFTER the webhook response flushes (AI-04 / SC2 — webhook responds before AI work); verified by grep gate (no `await enqueueAiFlag`) |
| T-16-OR | Denial of Service / Availability | row orphaned in 'processing' after a mid-call function death | mitigate | cron reclaims `processing` rows older than 15 min (REVIEWS HIGH-1b / MEDIUM-4) so no submission is permanently stuck unanalyzed |
| T-16-P5 | Elevation of Privilege | bot-audit enqueue block calling auth/after | mitigate | grep gate asserts logOfficeActivity count stays 0 and no `next/server` import; enqueue runs post-commit, outside TX; waitUntil lives in ai-flag-queue.ts (request-scoped, no session) not here |
</threat_model>

<verification>
- `npx tsc --noEmit` passes
- grep gates: un-awaited enqueueAiFlag, CRON_SECRET bearer check, cron reclaims pending+processing, cron registered in vercel.json
- `grep -c logOfficeActivity src/lib/bot-audit.ts` == 0; no `from 'next/server'` in bot-audit.ts
</verification>

<success_criteria>
- AI-04: enqueue called un-awaited post-commit; waitUntil (Plan 02) runs analysis after the response; webhook unaffected (SC2)
- Cron reclaims pending AND orphaned processing rows (REVIEWS HIGH-1b / MEDIUM-4 — explicit safety net)
- SC6: cron route guarded by CRON_SECRET + registered in vercel.json
</success_criteria>

<output>
Create `.planning/phases/16-ai-vision-assist/16-03-SUMMARY.md` when done
</output>
