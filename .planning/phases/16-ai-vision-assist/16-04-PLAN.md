---
phase: 16-ai-vision-assist
plan: 04
type: execute
wave: 3
depends_on: ["16-02"]
files_modified:
  - tests/fixtures/ai-vision/fixtures.json
  - scripts/set-eval-passed.sql
  - tests/ai-flags-advisory.test.ts
autonomous: false
requirements: [AI-03, AI-05]

must_haves:
  truths:
    - "The eval harness runs against a labeled dataset of real submission photos via the shared analyzePhoto and reports precision >= 0.80 on the anomaly class (AI-05 / REVIEWS HIGH-2)"
    - "The eval measures all four anomaly signals via isAnomalous (mismatch, quality, location, duplicate); material-suggestion (AI-02) is explicitly advisory-display-only and out of the gate's anomaly metric (REVIEWS MEDIUM-5)"
    - "D-02: the precision >= 0.80 eval gate runs on a real labeled approved-photo dataset and the eval result is the single switch that opens flag display"
    - "eval_passed = true is the single switch controlling flag display; it is keyed on the multi-signal anomaly_detected column (not the conflated photo_anomaly_score) so quality/location/duplicate-only flags can open (AI-05 / SC1 / REVIEWS HIGH-3)"
    - "No code path connects submission_ai_flags to submissions.status — grep-proven (AI-03 / SC5)"
  artifacts:
    - path: "tests/fixtures/ai-vision/fixtures.json"
      provides: "30-50 labeled real-photo fixtures (photoUrl, groundTruth, workType, notes)"
      contains: "groundTruth"
    - path: "scripts/set-eval-passed.sql"
      provides: "one-time SQL to open the eval gate after precision passes, keyed on anomaly_detected"
      contains: "eval_passed"
    - path: "tests/ai-flags-advisory.test.ts"
      provides: "static grep-style assertion proving no ai_flags → submissions.status coupling (SC5)"
      contains: "submissions.status"
  key_links:
    - from: "tests/ai-vision.test.ts"
      to: "precision >= 0.80 gate (via shared analyzePhoto + isAnomalous)"
      via: "AI_EVAL_ENABLED eval run over fixtures.json"
      pattern: "toBeGreaterThanOrEqual\\(0.8"
    - from: "scripts/set-eval-passed.sql"
      to: "submission_ai_flags.eval_passed"
      via: "UPDATE ... SET eval_passed = true WHERE anomaly_detected ..."
      pattern: "eval_passed = true"
---

<objective>
Close the AI-05 eval gate and prove the AI-03 advisory-only invariant. Build the labeled fixture dataset from real approved-submission photos (D-02: worker-declared work type = weak label; office engineer confirms ground truth on ~30–50 samples), run the eval harness (built in Plan 02, which calls the SHARED `analyzePhoto` + `isAnomalous` so the gate provably measures the production prompt across all four anomaly signals — REVIEWS HIGH-2/MEDIUM-5), confirm precision ≥ 0.80, ship the one-time `eval_passed = true` opener SQL keyed on the multi-signal `anomaly_detected` column (REVIEWS HIGH-3), and add a static test that grep-proves no `submission_ai_flags` → `submissions.status` coupling exists.

Purpose: AI-05 (validated against reference dataset before any flag is shown — the SINGLE gate controlling flag display) and AI-03 (advisory-only, never blocks/auto-decides). This plan is the build-order gate: no UI plan (Plan 05) renders a flag until this eval passes. The fixture labeling is a human checkpoint (the engineer is ground-truth per D-02).
Output: labeled fixtures, eval-gate opener SQL (anomaly_detected-keyed), advisory-invariant static test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-ai-vision-assist/16-RESEARCH.md
@.planning/phases/16-ai-vision-assist/16-PATTERNS.md

<interfaces>
<!-- Eval harness scaffold from Plan 02 lives in tests/ai-vision.test.ts (describeIfAiEval-gated,
     reads tests/fixtures/ai-vision/fixtures.json, calls the SHARED analyzePhoto, classifies
     predicted = isAnomalous(output), asserts precision >= 0.80). This plan POPULATES fixtures.json
     with real labeled data; the harness itself is unchanged. -->

<!-- fixture shape: { photoUrl: string, groundTruth: 'anomaly' | 'normal', workType: string, notes?: string } -->

<!-- EVAL SCOPE (REVIEWS MEDIUM-5 — make this explicit):
     The precision metric is scoped to ANOMALY DETECTION across the four anomaly signals that
     isAnomalous() reads: photoMismatch (AI-01 core), photoQualityFlag, locationOpinion==='inconsistent',
     isDuplicate (AI-06). The fixture groundTruth label ('anomaly'/'normal') is the union of these.
     materialSuggestion (AI-02) is a CLASSIFICATION SUGGESTION, not an anomaly — it is displayed
     advisory-only on AiFlagCard and is intentionally NOT measured by the 0.80 precision gate. AI-05
     ("outputs validated against reference dataset before shown") is satisfied for the anomaly signals
     by this gate; material-suggestion ships as an explicitly-scoped advisory aid (auditor judges it,
     it never blocks). Document this scope in the SUMMARY so the unvalidated-AI-02 decision is on record. -->

<!-- eval gate query (Plan 05 getSubmissionAiFlag): WHERE eval_passed = true. The opener SQL sets it. -->
<!-- eval_passed opener (REVIEWS HIGH-3 — key on the multi-signal anomaly_detected column, NOT the
     conflated photo_anomaly_score): UPDATE submission_ai_flags SET eval_passed = true
     WHERE status = 'done' AND anomaly_detected = true. This opens display for ANY flag whose real
     signal is quality/location/duplicate even when mismatch confidence is low — D-01 (five signals)
     + D-03 (show ALL eval-passed flags) honored. The D-02 precision-≥0.80 gate semantics are
     unchanged — this changes WHICH rows open, not the threshold. -->

<!-- SC5 grep proof: grep -r "submission_ai_flags" src --include="*.ts" --include="*.tsx"
     must show only getSubmissionAiFlag (gated read), enqueueAiFlag/runAiAnalysis (writes),
     cron route — NONE writing submissions.status from an ai_flags read. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Build + label the real-photo fixture dataset (D-02)</name>
  <read_first>
    - tests/fixtures/ai-vision/fixtures.json (empty-array placeholder from Plan 02 — populate it)
    - .planning/phases/16-ai-vision-assist/16-RESEARCH.md "Pattern 5: Eval harness — Fixture labeling workflow"
    - .planning/phases/16-ai-vision-assist/16-CONTEXT.md D-02 (weak label = declared work type; engineer confirms ground truth)
  </read_first>
  <what-built>
    The eval harness (Plan 02) needs ~30–50 labeled real-photo fixtures. Claude can export the candidate rows automatically; the office engineer must confirm the ground-truth `anomaly`/`normal` label per photo (D-02 — the engineer IS the ground truth). This is the only step a human must perform.
    PRE-FLIGHT (REVIEWS MEDIUM-8): before labeling, COUNT the approved submissions with a non-null photoUrl in the dev DB. If fewer than 30 exist, STOP and report the count — the gate cannot open on too small a set; the developer decides whether to proceed with a smaller set (documented) or defer until more field data accrues.
  </what-built>
  <action>
    AUTOMATE FIRST: query the dev Neon DB for the count of approved submissions with a photoUrl; if < 30, surface the count and pause for a developer decision (do not silently proceed). Otherwise query ~30–50 such submissions returning `{ photoUrl, workType (declared BOQ work type), notes }`. Write these into tests/fixtures/ai-vision/fixtures.json with a provisional `groundTruth` set to the weak label (`'normal'` default, or `'anomaly'` heuristically where the declared work type is absent/uncertain). Present the photo URLs + provisional labels to the engineer for confirmation.
    HUMAN STEP: the office engineer opens each photo URL and confirms/corrects the `groundTruth` field (`'anomaly'` = photo clearly does not match work type OR quality prevents verification OR location obviously wrong OR obvious duplicate; `'normal'` otherwise — the union of the four anomaly signals, matching the eval scope). Aim for a mix (not all-normal) so precision is measurable.
  </action>
  <how-to-verify>
    fixtures.json contains 30–50 entries (or the developer-approved smaller set), each with photoUrl + groundTruth + workType; at least a few entries labeled 'anomaly' (so the precision denominator is non-trivial). Photo URLs are real Vercel Blob URLs (no binary images committed).
  </how-to-verify>
  <resume-signal>Type "labeled" once the engineer has confirmed ground-truth labels for the fixtures, or describe blockers (e.g. fewer than 30 approved photos exist — report the count).</resume-signal>
  <acceptance_criteria>
    - `node -e "const f=require('./tests/fixtures/ai-vision/fixtures.json'); if(f.length<30)process.exit(1); if(!f.every(x=>x.photoUrl&&x.groundTruth&&x.workType))process.exit(1); if(!f.some(x=>x.groundTruth==='anomaly'))process.exit(1);"` exits 0 (or, if the developer approved a smaller set, the count floor is the agreed number and is recorded in the SUMMARY)
    - No binary image files committed under tests/fixtures/ai-vision/
  </acceptance_criteria>
  <done>30–50 (or developer-approved count of) engineer-confirmed labeled fixtures exist; mix of anomaly/normal; pre-flight count recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run eval harness → confirm precision >= 0.80 → open the gate (keyed on anomaly_detected)</name>
  <read_first>
    - tests/ai-vision.test.ts (eval block from Plan 02 — calls shared analyzePhoto, predicted = isAnomalous(output))
    - scripts/set-eval-passed.sql (does not yet exist — create it)
    - src/lib/ai-vision.ts (the SHARED analyzePhoto + the system prompt to iterate — the eval and production share it, so prompt edits move the eval — REVIEWS HIGH-2)
    - .planning/phases/16-ai-vision-assist/16-RESEARCH.md "Pattern 5" + "Open Questions #3" (eval_passed bulk-set workflow) + "Assumptions A7" (precision may need prompt iteration)
  </read_first>
  <what-built>
    Claude runs the eval harness against the labeled fixtures (real API calls via the shared analyzePhoto, costs tokens — gated by AI_EVAL_ENABLED). If precision >= 0.80, the gate opens via a one-time SQL script keyed on anomaly_detected. If < 0.80, the system prompt INSIDE analyzePhoto in ai-vision.ts is iterated (no schema/code change — only system-prompt wording) and re-run (A7); because the eval calls the same analyzePhoto, the iteration provably moves the eval (REVIEWS HIGH-2).
  </what-built>
  <action>
    AUTOMATE: create scripts/set-eval-passed.sql containing `UPDATE submission_ai_flags SET eval_passed = true WHERE status = 'done' AND anomaly_detected = true;` (REVIEWS HIGH-3 — keyed on the multi-signal anomaly_detected column so quality/location/duplicate-only advisories open, not just high mismatch confidence; aligns with D-03 "show ALL eval-passed flags" with the badge colored by score). Run the eval harness: `AI_EVAL_ENABLED=true AI_GATEWAY_API_KEY=<key> npx vitest run tests/ai-vision.test.ts -t "precision"`.
    If precision >= 0.80: apply scripts/set-eval-passed.sql to the dev branch (and test branch if the UI integration test reads from it) so AiFlagCard begins rendering. If precision < 0.80: iterate the analyzePhoto system-prompt wording ONLY (Claude's discretion per CONTEXT) and re-run until >= 0.80 — do NOT lower the 0.80 threshold (it is the AI-05 acceptance criterion) and do NOT change isAnomalous / the schema.
  </action>
  <how-to-verify>
    The eval test prints precision and the assertion `expect(precision).toBeGreaterThanOrEqual(0.80)` passes. After the SQL opener runs, a spot-check query confirms at least one row with anomaly_detected=true now has `eval_passed = true`. The human confirms the precision number is real (not a degenerate 1.0 from an all-normal fixture set — there must be true positives).
  </how-to-verify>
  <resume-signal>Type "gate open" once precision >= 0.80 and eval_passed=true is set on qualifying (anomaly_detected) rows, or report the precision value if it cannot reach 0.80 after prompt iteration.</resume-signal>
  <acceptance_criteria>
    - `AI_EVAL_ENABLED=true ... npx vitest run tests/ai-vision.test.ts -t "precision"` exits 0 (assertion precision >= 0.80 holds on a fixture set with real anomalies; the eval calls the shared analyzePhoto + isAnomalous)
    - scripts/set-eval-passed.sql contains `eval_passed = true` and the `anomaly_detected = true` key (NOT `photo_anomaly_score >= 0.50`) — REVIEWS HIGH-3
    - after applying the opener, at least one submission_ai_flags row has eval_passed = true
    - the 0.80 threshold in tests/ai-vision.test.ts is unchanged (never lowered); isAnomalous / the schema unchanged
  </acceptance_criteria>
  <done>Precision >= 0.80 confirmed on real labeled data via the shared analyzePhoto; eval gate opened for qualifying anomaly_detected rows.</done>
</task>

<task type="auto">
  <name>Task 3: Static advisory-only invariant proof (AI-03 / SC5)</name>
  <read_first>
    - tests/ai-flags-advisory.test.ts (does not yet exist — create it)
    - tests/hakedis-live.test.ts (static-edge / fs.readFileSync assertion pattern, per STATE.md Phase 12-03 "static-edge test pattern")
    - src/lib/bot-audit.ts (the only file that writes submissions.status — confirm it never reads submission_ai_flags to do so)
    - .planning/phases/16-ai-vision-assist/16-RESEARCH.md "Anti-Patterns" (no ai_flags → submissions.status) + Security Domain
  </read_first>
  <action>
    Create tests/ai-flags-advisory.test.ts — a fast static test (no DB, no API) that asserts the AI-03 advisory-only invariant. Read the relevant source files (src/lib/bot-audit.ts, src/lib/ai-vision.ts, src/lib/ai-flag-queue.ts, src/actions/ai-flags.ts when present) via fs.readFileSync and assert that no statement writing `submissions.status` (or `status: 'approved'/'rejected'`) appears within a scope that reads from `submissionAiFlags`. Concretely: assert that ai-vision.ts and ai-flag-queue.ts contain zero references to `submissions.status` / `.set({ status:` on submissions; and that any read of submissionAiFlags (getSubmissionAiFlag) does not import or call the approval mutation. Mirror the Phase 12-03 static-edge pattern (fs.readFileSync + string assertions, <100ms). Name a test "no code path connects submission_ai_flags to submissions.status (SC5)".
  </action>
  <verify>
    <automated>npx vitest run tests/ai-flags-advisory.test.ts && grep -rl "submission_ai_flags\|submissionAiFlags" src --include="*.ts" --include="*.tsx" | xargs grep -l "submissions.status\|status: 'approved'" 2>/dev/null; test $? -ne 0 && echo "SC5 grep clean"</automated>
  </verify>
  <acceptance_criteria>
    - tests/ai-flags-advisory.test.ts asserts no submissionAiFlags-reading file writes submissions.status and exits 0
    - the grep `grep -rl submissionAiFlags src | xargs grep -l "submissions.status"` finds no file coupling the two (SC5 grep-clean)
    - test runs in < 1s (static, no DB/API)
  </acceptance_criteria>
  <done>Advisory-only invariant codified as a fast static test; SC5 grep-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI model output → auditor decision | Model output must never influence approval; advisory-only is a security invariant |
| eval gate (eval_passed) → flag visibility | Only validated flags may be shown; unvalidated/failed flags must stay hidden |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-AD | Tampering | ai_flags coupled to submissions.status | mitigate | Static test (Task 3) + SC5 grep prove no coupling — AI cannot escalate into an authorization control (AI-03) |
| T-16-EG | Spoofing / Tampering | unvalidated flags shown before eval | mitigate | eval_passed gate is the single switch (Task 2 opener only sets it after precision >= 0.80, keyed on anomaly_detected); null/false rows invisible (AI-05 / SC1) |
| T-16-FX | Information Disclosure | fixtures.json containing photo URLs | accept | URLs are already-public Vercel Blob links used in the Telegram bot; no new exposure; no binary images committed |
</threat_model>

<verification>
- `AI_EVAL_ENABLED=true ... npx vitest run tests/ai-vision.test.ts -t "precision"` passes (>= 0.80) via the shared analyzePhoto
- `npx vitest run tests/ai-flags-advisory.test.ts` passes
- SC5 grep: no file reading submissionAiFlags writes submissions.status
- fixtures.json has >= 30 labeled entries (or developer-approved count) with at least one anomaly
- scripts/set-eval-passed.sql keys on anomaly_detected (not photo_anomaly_score)
</verification>

<success_criteria>
- AI-05: precision >= 0.80 on real labeled data via the production analyzePhoto; eval_passed gate opened (keyed on the multi-signal anomaly_detected)
- AI-03 / SC5: advisory-only invariant proven (static test + grep)
- MEDIUM-5 scope recorded: eval covers the four anomaly signals; material-suggestion (AI-02) ships as explicitly-scoped advisory display
</success_criteria>

<output>
Create `.planning/phases/16-ai-vision-assist/16-04-SUMMARY.md` when done
</output>
