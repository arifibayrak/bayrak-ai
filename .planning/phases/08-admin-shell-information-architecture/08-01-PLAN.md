---
phase: 08-admin-shell-information-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/sidebar.tsx
  - src/components/ui/chart.tsx
  - messages/en.json
  - messages/tr.json
  - tests/i18n.test.ts
autonomous: false
requirements: [I18N-03]
user_setup: []

must_haves:
  truths:
    - "Running the dashboard locale toggle shows every new admin surface label in both Turkish and English"
    - "shadcn sidebar and chart primitives exist on disk and export the names the shell/chart plans import"
  artifacts:
    - path: "src/components/ui/sidebar.tsx"
      provides: "SidebarProvider, Sidebar, SidebarInset, SidebarTrigger, SidebarMenu, SidebarMenuButton, SidebarMenuItem"
      contains: "SidebarProvider"
    - path: "src/components/ui/chart.tsx"
      provides: "ChartContainer, ChartTooltip, ChartTooltipContent"
      contains: "ChartContainer"
    - path: "messages/en.json"
      provides: "dashboard.admin.* namespace (nav/overview/filters/currency/people/records/detail/stubs)"
      contains: "\"admin\""
    - path: "messages/tr.json"
      provides: "dashboard.admin.* namespace mirror"
      contains: "\"admin\""
    - path: "tests/i18n.test.ts"
      provides: "key-existence assertions for dashboard.admin.* in both message files"
      contains: "dashboard.admin.nav"
  key_links:
    - from: "tests/i18n.test.ts"
      to: "messages/en.json + messages/tr.json"
      via: "flattenKeys parity + required-key assertions"
      pattern: "dashboard\\.admin\\."
---

<objective>
Install the two new shadcn primitives (sidebar, chart) and lay down the COMPLETE `dashboard.admin.*` i18n namespace in both message files so every downstream page/component consumes existing keys instead of inventing them. Extend `tests/i18n.test.ts` to assert the new namespace exists in both catalogs.

Purpose: Every other Phase 8 plan imports from `@/components/ui/sidebar` / `@/components/ui/chart` and reads `dashboard.admin.*` translation keys. Centralizing the install + full key set here means downstream plans never touch `messages/*.json` (no shared-file write conflict) and never block on a missing component export.
Output: `sidebar.tsx`, `chart.tsx`, fully-populated `dashboard.admin.*` keys in `en.json` + `tr.json`, and i18n parity/coverage tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-admin-shell-information-architecture/08-UI-SPEC.md
@.planning/phases/08-admin-shell-information-architecture/08-PATTERNS.md
@.planning/phases/08-admin-shell-information-architecture/08-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: [SECURITY GATE] Verify recharts + shadcn registry before install</name>
  <what-built>Nothing yet — this is a pre-install legitimacy gate. RESEARCH.md tagged recharts (installed transitively by `npx shadcn@latest add chart`) and nuqs as [ASSUMED] because slopcheck was unavailable. nuqs is NOT being installed (existing router.push pattern is used instead). Only `recharts` (via the shadcn chart CLI) and the official shadcn registry are in scope.</what-built>
  <how-to-verify>
    1. Confirm `recharts` is a real, widely-used package: visit https://www.npmjs.com/package/recharts — confirm latest is in the 3.x line, weekly downloads in the millions, repo github.com/recharts/recharts.
    2. Confirm the shadcn `chart` and `sidebar` blocks come from the official registry only — `components.json` `registries` must be `{}` (no third-party registry).
    3. Confirm NO `nuqs` install is planned (this phase uses the existing `router.push` + `searchParams` pattern per RESEARCH.md §URL State Management).
  </how-to-verify>
  <read_first>
    - .planning/phases/08-admin-shell-information-architecture/08-RESEARCH.md (§Package Legitimacy Audit, §URL State Management)
    - components.json (confirm registries is {})
  </read_first>
  <acceptance_criteria>
    - Human types "approved" only after confirming recharts on npmjs.com is legitimate (3.x, high download count, recharts/recharts repo).
    - `components.json` `registries` key reads `{}` (no third-party registries).
    - No `nuqs` dependency is added in any Phase 8 task.
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed with the shadcn installs, or describe a concern.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install shadcn sidebar + chart and verify exports</name>
  <files>src/components/ui/sidebar.tsx, src/components/ui/chart.tsx</files>
  <read_first>
    - .planning/phases/08-admin-shell-information-architecture/08-PATTERNS.md (§Install prerequisites; §No Analog Found — exact export names to verify)
    - components.json (style base-nova, iconLibrary lucide — confirms CLI target)
    - src/app/globals.css (confirm `--sidebar-*` and `--chart-1..5` tokens already present so no theme rebuild is needed)
  </read_first>
  <action>Run `npx shadcn@latest add sidebar` then `npx shadcn@latest add chart` from the repo root. These create `src/components/ui/sidebar.tsx` and `src/components/ui/chart.tsx` and add `recharts` as a peer dependency (do NOT install recharts separately). After install, open both files and confirm the exact export identifiers downstream plans import: from sidebar — `SidebarProvider`, `Sidebar`, `SidebarInset`, `SidebarTrigger`, `SidebarMenu`, `SidebarMenuButton`, `SidebarMenuItem` (and `SidebarHeader`/`SidebarContent` if present); from chart — `ChartContainer`, `ChartTooltip`, `ChartTooltipContent`. If any export name differs from these (shadcn CLI drift), record the actual names in the SUMMARY so plans 03/04 import correctly. Do NOT modify globals.css — tokens already exist per RESEARCH.md.</action>
  <verify>
    <automated>test -f src/components/ui/sidebar.tsx && test -f src/components/ui/chart.tsx && grep -q "SidebarProvider" src/components/ui/sidebar.tsx && grep -q "SidebarInset" src/components/ui/sidebar.tsx && grep -q "SidebarTrigger" src/components/ui/sidebar.tsx && grep -q "ChartContainer" src/components/ui/chart.tsx && echo OK</automated>
  </verify>
  <done>Both UI primitive files exist; sidebar.tsx exports SidebarProvider/Sidebar/SidebarInset/SidebarTrigger/SidebarMenu*; chart.tsx exports ChartContainer/ChartTooltip/ChartTooltipContent; recharts present in package.json or node_modules; globals.css unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add complete dashboard.admin.* namespace to both message files + i18n tests</name>
  <files>messages/en.json, messages/tr.json, tests/i18n.test.ts</files>
  <behavior>
    - Test: every key path under `dashboard.admin.*` listed in the UI-SPEC Copywriting Contract exists in BOTH en.json and tr.json (use the existing required-key loop pattern from tests/i18n.test.ts).
    - Test: the existing flattenKeys parity test (`tr.json ⟺ en.json identical key sets`) still passes — no orphan key on either side.
    - Test: assert presence of representative keys per sub-namespace: `dashboard.admin.nav.overview`, `dashboard.admin.overview.kpi_pending_label`, `dashboard.admin.filters.clear`, `dashboard.admin.currency.label`, `dashboard.admin.people.tab_workers`, `dashboard.admin.records.details`, `dashboard.admin.detail.ai_slot_body`, `dashboard.admin.stubs.coming_soon`.
  </behavior>
  <read_first>
    - messages/en.json and messages/tr.json (existing structure + existing `dashboard.*` namespaces — do NOT modify existing keys)
    - tests/i18n.test.ts (existing flattenKeys helper, parity test, and required-key loop pattern at the `requiredAuthKeys`/`requiredNavKeys`/`requiredDashboardKeys` blocks)
    - .planning/phases/08-admin-shell-information-architecture/08-UI-SPEC.md (§Copywriting Contract — EXACT EN/TR strings for every label, heading, KPI label, column header, empty/error state; §i18n Implementation Contract — the 8 sub-namespaces)
    - .planning/phases/08-admin-shell-information-architecture/08-PATTERNS.md (§messages/en.json — the namespace skeleton to follow)
  </read_first>
  <action>Add a single new `admin` object under `dashboard` in BOTH `messages/en.json` and `messages/tr.json`, mirroring the PATTERNS.md namespace skeleton and using the EXACT copy from the UI-SPEC §Copywriting Contract. Required sub-namespaces and their keys: `nav` (overview, projects, people, analytics, hakedis, exports, main_nav_aria, open_nav, close_nav, expand_sidebar, collapse_sidebar); `overview` (heading, subtitle_all_time, subtitle_filtered, kpi_pending_label, kpi_pending_sub, kpi_approvals_label, kpi_rejections_label, kpi_workers_label, kpi_sub_all_time, kpi_sub_filtered, ev_heading, ev_col_project, ev_col_bac, ev_col_ev, ev_col_complete, chart_throughput, chart_earned_value, chart_rejection_rate, empty_no_projects, chart_no_data, error_load); `filters` (from, to, all_projects, all_people, clear); `currency` (label); `people` (heading, subtitle, tab_workers, tab_auditors, empty_state, col_name, col_submissions, col_approved, col_rejected, col_pending, col_value, col_decisions, col_turnaround, col_backlog, back_to_people, role_worker, role_auditor, kpi_worker_metrics, kpi_auditor_metrics, no_records_alert, error_load); `timeline` (heading, load_more, empty, view_detail, decided_in); `records` (heading, col_status, col_worker, col_project, col_boq, col_quantity, col_submitted, col_auditor, details_header, details, prev, next, pagination, empty, error_load); `detail` (heading, back, field_worker, field_project, field_boq, field_quantity, field_submitted, field_location, field_location_warning, field_auditor, field_decided, field_rejection_reason, ai_slot_label, ai_slot_body, view_on_maps, photo_alt, no_photo); `stubs` (coming_soon, analytics_heading, analytics_body, hakedis_heading, hakedis_body, exports_heading, exports_body). Use the UI-SPEC TR strings verbatim (e.g. nav.overview TR="Genel Bakış", stubs.coming_soon TR="Yakında", records.details TR="Detay", detail.ai_slot_body TR="AI analizi ileride eklenecektir."). The TR Exports nav label is "Dışa Aktar"; the Exports stub heading is "Dışa Aktarma" — keep these distinct per UI-SPEC. Then extend tests/i18n.test.ts with a new `describe('dashboard.admin.* namespace coverage (I18N-03)')` block asserting the representative keys above exist in both catalogs, following the existing required-key loop pattern. Do NOT touch any existing key.</action>
  <verify>
    <automated>npx vitest run tests/i18n.test.ts</automated>
  </verify>
  <done>Both message files contain a complete `dashboard.admin.*` namespace with byte-identical key sets (parity test green); tests/i18n.test.ts asserts the new namespace keys; existing i18n tests still pass; no existing key modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm/shadcn registry → local node_modules | Third-party package code enters the build (recharts via shadcn chart) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-SC | Tampering | `npx shadcn@latest add chart` (pulls recharts) | mitigate | Task 1 blocking-human gate verifies recharts on npmjs.com (3.x, high downloads, recharts/recharts repo) before install; official shadcn registry only (components.json registries = {}) |
| T-08-01 | Tampering | message JSON files | accept | Static localized strings only; no executable content; JSX auto-escapes on render |
</threat_model>

<verification>
- `npx vitest run tests/i18n.test.ts` — green (parity + new namespace coverage)
- `test -f src/components/ui/sidebar.tsx && test -f src/components/ui/chart.tsx`
- `grep -q "SidebarProvider" src/components/ui/sidebar.tsx && grep -q "ChartContainer" src/components/ui/chart.tsx`
</verification>

<success_criteria>
- shadcn sidebar + chart primitives installed with the expected exports (or actual names recorded in SUMMARY).
- `dashboard.admin.*` complete in both en.json and tr.json with identical key sets.
- I18N-03 key coverage asserted by an automated test.
</success_criteria>

<output>
Create `.planning/phases/08-admin-shell-information-architecture/08-01-SUMMARY.md` when done. Record the EXACT export names from sidebar.tsx and chart.tsx (in case the shadcn CLI emitted different identifiers than expected) so plans 03/04 import correctly.
</output>
