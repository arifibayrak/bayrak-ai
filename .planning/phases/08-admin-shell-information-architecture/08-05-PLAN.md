---
phase: 08-admin-shell-information-architecture
plan: 05
type: execute
wave: 4
depends_on: [08-01, 08-02, 08-03, 08-04]
files_modified:
  - src/actions/analytics.ts
  - src/components/admin/ActivityTimeline.tsx
  - src/app/dashboard/(admin)/people/page.tsx
  - src/app/dashboard/(admin)/people/[personId]/page.tsx
autonomous: false
requirements: [PERF-04, UX-03]
user_setup: []

must_haves:
  truths:
    - "The People directory lists approved people across all projects with Workers/Auditors tabs"
    - "A person who is both worker and auditor appears in both tabs"
    - "Each person row links to a profile page with KPI cards and a grouped activity timeline"
    - "Worker timeline shows submissions; auditor timeline shows decisions; each entry drills to the submission detail page"
    - "Date-range and project filters scope the directory and profile metrics"
  artifacts:
    - path: "src/app/dashboard/(admin)/people/page.tsx"
      provides: "force-dynamic RSC directory with Workers/Auditors tabs + metrics tables"
      contains: "force-dynamic"
    - path: "src/app/dashboard/(admin)/people/[personId]/page.tsx"
      provides: "force-dynamic profile: KPI cards + ActivityTimeline; notFound on missing"
      contains: "notFound"
    - path: "src/components/admin/ActivityTimeline.tsx"
      provides: "grouped-by-month timeline with status dots + drill links"
      contains: "dashboard/records/"
    - path: "src/actions/analytics.ts"
      provides: "getPortfolioPeople bulk aggregation (one query, no N+1)"
      contains: "getPortfolioPeople"
  key_links:
    - from: "people/page.tsx"
      to: "getPortfolioPeople"
      via: "single bulk query (avoid N× getPersonMetrics)"
      pattern: "getPortfolioPeople"
    - from: "ActivityTimeline entry"
      to: "/dashboard/records/[id]"
      via: "drill-through link"
      pattern: "dashboard/records/"
    - from: "people/[personId]/page.tsx"
      to: "getPersonMetrics + getCanonicalSubmissions + getAuditorDecisions"
      via: "parallel fetch with dateRange/projectIds"
      pattern: "getAuditorDecisions"
---

<objective>
Build the cross-project People directory (`/dashboard/(admin)/people`) and the per-person profile (`/dashboard/(admin)/people/[personId]`) with a grouped activity timeline. Add ONE bulk aggregation query `getPortfolioPeople` to `analytics.ts` so the directory does not call `getPersonMetrics` N times (avoids N+1). The profile reuses `getPersonMetrics` (worker + auditor, with dateRange), `getCanonicalSubmissions` (worker timeline) and `getAuditorDecisions` (auditor timeline). Filters (date-range + project) scope both surfaces (D-75).

Purpose: PERF-04 (profile + timeline + value contribution) and UX-03 (filters extend to people).
Output: `getPortfolioPeople` query, ActivityTimeline component, directory page, profile page.

Note on file ownership: this plan and plan 06 both touch `src/actions/analytics.ts`. This plan runs in Wave 4 after plan 04 (Wave 3) and before/independent of plan 06 — `analytics.ts` is owned sequentially. Plan 06 does NOT add new analytics functions (it reuses the plan-02 `getCanonicalSubmissions` extensions), so the only writer of new analytics functions after plan 02 is THIS plan. See wave assignment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-admin-shell-information-architecture/08-UI-SPEC.md
@.planning/phases/08-admin-shell-information-architecture/08-PATTERNS.md
@.planning/phases/08-admin-shell-information-architecture/08-RESEARCH.md
@.planning/phases/08-admin-shell-information-architecture/08-02-SUMMARY.md
@.planning/phases/08-admin-shell-information-architecture/08-04-SUMMARY.md

<interfaces>
<!-- From plan 02 (see 08-02-SUMMARY for exact field names): -->
- getPersonMetrics(personId, { projectIds?, asAuditor?, dateRange? }): Promise<PersonMetrics>
- getCanonicalSubmissions({ personId, projectIds?, from?, to?, limit?, offset? }): Promise<CanonicalSubmission[]>
- getAuditorDecisions({ personId, dateRange?, projectIds?, limit?, offset? }): Promise<AuditorDecision[]>

<!-- From plan 04 (see 08-04-SUMMARY): FilterBar prop shape -->
- FilterBar({ projectOptions, personOptions?, showStatus? }) — reuse; person filter omitted on these pages
- KpiCard({ label, subLabel, value, icon, drillHref?, valueColor? }) — reuse

<!-- Schema facts (read-only): assignments.role_on_project ∈ {worker, auditor}; people = approved; pending_people excluded; office engineers are auth users (excluded) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: getPortfolioPeople bulk aggregation query</name>
  <files>src/actions/analytics.ts, tests/analytics.test.ts</files>
  <behavior>
    - getPortfolioPeople({ role: 'worker'|'auditor', dateRange?, projectIds? }): returns ONE row per approved person who has the given role on at least one assignment, with aggregated metrics in a single SQL query (NOT N× getPersonMetrics).
    - Workers: { personId, displayName, submissionsApproved, submissionsRejected, submissionsPending, valueContributedByCurrency: Record<string,string> }.
    - Auditors: { personId, displayName, decisionsCount, avgDecisionLatencyHours: number|null, pendingBacklogCount }.
    - A person assigned as worker on one project and auditor on another appears in BOTH role result sets (D-69).
    - pending_people and office-engineer users are excluded; auth-guarded; tenant-scoped; dateRange scopes counts (auditor latency via split FILTER on decided_at IS NOT NULL).
  </behavior>
  <read_first>
    - src/actions/analytics.ts (getPersonMetrics — the worker counts query, the currency-grouped value_contributed query, the auditor split-query latency pattern; getPortfolioOverview — the all-rows-in-one-query bulk idiom)
    - .planning/phases/08-admin-shell-information-architecture/08-RESEARCH.md (Open Question 3 — write a bulk getPortfolioPeople, NOT N× getPersonMetrics; Pitfall 7 auditor source; D-69 role-on-assignments)
    - .planning/phases/08-admin-shell-information-architecture/08-PATTERNS.md (§analytics.ts — currency-map merge, split-query, numeric coercion)
    - tests/analytics.test.ts (describeIfDb + sql.raw deterministic-UUID fixture pattern)
  </read_first>
  <action>RED: add `describeIfDb('D-69/PERF-04: getPortfolioPeople() bulk')` to tests/analytics.test.ts. Seed an approved person assigned as worker on project A and as auditor on project B, plus submissions for both roles. Assert: the person appears in BOTH the worker result (with submission counts) and the auditor result (with decision count); a pending_people row is NOT returned; auth-null throws 'Unauthorized'. Run — fails.
GREEN: implement `getPortfolioPeople(options: { role: 'worker' | 'auditor'; dateRange?: { from: Date; to: Date }; projectIds?: string[] })` returning a new exported `PortfolioWorker[]` or `PortfolioAuditor[]` (discriminate on role via overloads, or two narrowly-typed functions if cleaner — Claude's discretion, document the final shape in SUMMARY). auth() guard + `getDefaultTenantId()` + `WHERE ... tenant_id = ${tenantId}`. Workers: JOIN people → assignments (role_on_project='worker') → submissions; GROUP BY person; counts via FILTER(WHERE status=...); value_contributed currency-grouped into a Record map (never cross-currency summed). Auditors: people who hold an auditor assignment; decisionsCount on `decided_by = person AND status IN ('approved','rejected')`; avg latency via `... FILTER (WHERE decided_at IS NOT NULL)` (split rule); pendingBacklogCount as a separate sub-aggregate over the projects they audit. Apply optional dateRange + projectIds as parameterized `sql\`\`` fragments (CR-03). Coerce via String()/Number(). One query (one round trip) per role — never loop getPersonMetrics. Run — green.</action>
  <verify>
    <automated>npx vitest run tests/analytics.test.ts && grep -q "getPortfolioPeople" src/actions/analytics.ts && echo OK</automated>
  </verify>
  <done>getPortfolioPeople exported; one bulk query per role; dual-role person appears in both result sets; pending_people/office users excluded; auth + tenant scoped; dateRange/projectIds parameterized; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: ActivityTimeline component (grouped-by-month, drill-through)</name>
  <files>src/components/admin/ActivityTimeline.tsx</files>
  <read_first>
    - src/components/dashboard/KayitlarTabClient.tsx (StatusBadge color mapping lines ~90-104 → adapt to StatusDot; pagination prev/next lines ~273-296 for the "Load more" idiom; date formatting toLocaleDateString('tr-TR'))
    - .planning/phases/08-admin-shell-information-architecture/08-PATTERNS.md (§ActivityTimeline.tsx — StatusDot, ordered-list `<ol>` semantic pattern, drill link to /dashboard/records/[id])
    - .planning/phases/08-admin-shell-information-architecture/08-UI-SPEC.md (§Activity timeline — worker vs auditor entry layout, month group `<h3>`, drill-through `>` arrow, empty state, Load more first-50 pagination)
  </read_first>
  <action>Create `src/components/admin/ActivityTimeline.tsx`. Accept a normalized entry list — props `{ entries: { id, status, material, unit, quantity, dateStr, workerName?, latencyLabel? }[]; mode: 'worker' | 'auditor' }` — so the parent can feed either getCanonicalSubmissions (worker) or getAuditorDecisions (auditor) after mapping. Group entries by calendar month (newest first) and render the UI-SPEC `<ol>`/`<li>` semantic structure: month group `<h3 className="text-sm font-semibold text-muted-foreground">` (e.g. "Mayıs 2026" via `toLocaleDateString('tr-TR', { month:'long', year:'numeric' })`), nested `<ol className="divide-y divide-border">`, each entry `<li className="flex items-center gap-3 py-3 min-h-[44px]">` with a `StatusDot` (emerald=approved, destructive=rejected, amber=pending), center text — worker: "[material] — [quantity] [unit]"; auditor: "[material] — [quantity] [unit] ([workerName])" plus latencyLabel — right: `dateStr` (muted, tabular-nums) and a `<Link href={\`/dashboard/records/${entry.id}\`} aria-label={t('view_detail')}>` containing a `ChevronRight` icon. `StatusDot` adapts the KayitlarTabClient StatusBadge color logic. Localize via `useTranslations('dashboard.admin.timeline')`. This is a `'use client'` component because it owns the Load-more slice state. Empty state: `t('empty')`. "Load more" `<Button>` (`t('load_more')`) reveals beyond the first 50 (client-side slice — no infinite scroll).</action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "ActivityTimeline.tsx" || echo "no type errors"; grep -q "dashboard/records/" src/components/admin/ActivityTimeline.tsx && grep -qE "load_more|empty" src/components/admin/ActivityTimeline.tsx && echo OK</automated>
  </verify>
  <done>ActivityTimeline groups entries by month in a semantic `<ol>`, renders status dots, worker/auditor entry variants, drill links to /dashboard/records/[id], a Load-more (first 50), and a localized empty state.</done>
</task>

<task type="auto">
  <name>Task 3: People directory page + person profile page</name>
  <files>src/app/dashboard/(admin)/people/page.tsx, src/app/dashboard/(admin)/people/[personId]/page.tsx</files>
  <read_first>
    - .planning/phases/08-admin-shell-information-architecture/08-PATTERNS.md (§people/page.tsx — force-dynamic + searchParams + Tabs + empty state; §people/[personId]/page.tsx — await params + notFound + breadcrumb)
    - src/app/dashboard/projects/[id]/page.tsx (analog: await params/searchParams, Tabs usage, notFound, Promise.all)
    - src/app/dashboard/projects/page.tsx (analog: directory list + empty state)
    - .planning/phases/08-admin-shell-information-architecture/08-UI-SPEC.md (§People Directory — tabs, table columns Workers vs Auditors, "—" value rule, empty state, no pagination unless >50; §Person Profile — back link, role badges, KPI cards Worker vs Auditor, dual-role 8-card layout with Separator, no-records Alert, Activity Timeline)
    - .planning/phases/08-admin-shell-information-architecture/08-RESEARCH.md (Pitfall 6 dateRange must be passed; Pitfall 3 Suspense around FilterBar)
    - .planning/phases/08-admin-shell-information-architecture/08-04-SUMMARY.md (FilterBar + KpiCard prop shapes)
  </read_first>
  <action>Create `src/app/dashboard/(admin)/people/page.tsx`: `export const dynamic = 'force-dynamic'`. Props `{ searchParams: Promise<{ from?: string; to?: string; project?: string; role?: string }> }`. `await searchParams`; validate dates (`!isNaN(Date.parse(str))` guard) → build `dateRange` only when both from+to present; `projectIds` from `project`. `const t = await getTranslations('dashboard.admin.people')`. Fetch `const [workers, auditors] = await Promise.all([getPortfolioPeople({ role:'worker', dateRange, projectIds }), getPortfolioPeople({ role:'auditor', dateRange, projectIds })])`. Heading `t('heading')` + subtitle `t('subtitle')`. Suspense-wrap `<FilterBar projectOptions={...} />` (no person filter on this page). shadcn `<Tabs>` with URL state `?role=worker|auditor` (default worker): Workers tab table columns — Name (`<Link href={\`/dashboard/people/${w.personId}\`}>`), Submissions (approved+rejected+pending), Approved (success badge), Rejected (destructive badge), Pending (secondary badge), Value (selected-currency value or "—"). Auditors tab — Name (link), Decisions, Avg Turnaround ("X.X sa"/"X.X h" or "—"), Pending Backlog (secondary badge if ≤5, destructive if >5). Empty state: Users icon 48px + `t('empty_state')`. Wrap each table in `overflow-x-auto`. Use `tabular-nums` + `Intl.NumberFormat('tr-TR')`. If a result exceeds 50 rows, note in SUMMARY that pagination should be added (per UI-SPEC).
Create `src/app/dashboard/(admin)/people/[personId]/page.tsx`: `export const dynamic = 'force-dynamic'`. Props `{ params: Promise<{ personId: string }>; searchParams: Promise<{ from?: string; to?: string; project?: string }> }`. `await params`/`await searchParams`; build dateRange/projectIds with the isNaN guard. Determine the person's role(s) from their assignments. Fetch worker metrics `getPersonMetrics(personId, { asAuditor:false, dateRange, projectIds })` and, when the person has auditor assignments, `getPersonMetrics(personId, { asAuditor:true, dateRange, projectIds })`. If the person does not exist → `notFound()`. Back link `t('back_to_people')` → `/dashboard/people`. Person name (Display 20px/600) + role `<Badge variant="secondary">` (`t('role_worker')` and/or `t('role_auditor')`). Suspense-wrap FilterBar (date + project; no person filter). KPI cards via the plan-04 `KpiCard` — Worker: approved count, rejection rate %, location compliance %, value contributed; Auditor: decisions count, approval rate %, avg turnaround, pending backlog. Dual-role: 8 cards in a 4-col grid with a `<Separator>` and headings `t('kpi_worker_metrics')`/`t('kpi_auditor_metrics')`. Neither role with data: `<Alert>` `t('no_records_alert')`. Activity section: fetch worker timeline `getCanonicalSubmissions({ personId, from, to, projectIds, limit: 100 })` and auditor timeline `getAuditorDecisions({ personId, dateRange, projectIds, limit: 100 })`, map each into the ActivityTimeline entry shape (id, status, material, unit, quantity, dateStr via toLocaleDateString('tr-TR'), workerName + latencyLabel for auditor mode), and render `<ActivityTimeline mode="worker" entries={...} />` and/or `<ActivityTimeline mode="auditor" entries={...} />`.</action>
  <verify>
    <automated>test -f "src/app/dashboard/(admin)/people/page.tsx" && test -f "src/app/dashboard/(admin)/people/[personId]/page.tsx" && grep -q "getPortfolioPeople" "src/app/dashboard/(admin)/people/page.tsx" && grep -q "notFound" "src/app/dashboard/(admin)/people/[personId]/page.tsx" && grep -q "getAuditorDecisions" "src/app/dashboard/(admin)/people/[personId]/page.tsx" && (npx tsc --noEmit 2>&1 | grep -E "people/page.tsx|\[personId\]/page.tsx" || echo "no type errors in people pages")</automated>
  </verify>
  <done>Directory lists workers + auditors in tabs with dual-role appearing in both; each row links to the profile; profile shows role-correct KPI cards (dual-role = 8 cards), notFound on missing person, and worker/auditor activity timelines drilling to detail; date+project filters scope both surfaces.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify People directory + profile + timeline (browser-only)</name>
  <what-built>The People directory (Workers/Auditors tabs), the per-person profile with KPI cards, and the grouped activity timeline with drill-through. Rendering + filter behaviors are manual-only per VALIDATION.md.</what-built>
  <how-to-verify>
    1. `npm run dev`, sign in, click the People sidebar item → `/dashboard/people`.
    2. Confirm Workers tab lists approved workers with submission counts + value; switch to Auditors tab — lists auditors with decisions/turnaround/backlog. A person who is both should appear in both tabs.
    3. Set a date range / project filter → table metrics re-scope; URL shows the params.
    4. Click a person's name → profile page: KPI cards render (8 cards with a Worker/Auditor separator if dual-role); activity timeline grouped by month, newest first.
    5. Click a timeline entry's `>` → lands on the submission detail page `/dashboard/records/[id]` (plan 06).
    6. Click "Load more" if >50 entries → more entries appear.
    7. Visit a non-existent personId → Next.js 404. Toggle TR/EN → labels switch.
  </how-to-verify>
  <read_first>
    - .planning/phases/08-admin-shell-information-architecture/08-UI-SPEC.md (§People Directory; §Person Profile; §Activity timeline)
  </read_first>
  <acceptance_criteria>
    - Directory shows Workers + Auditors tabs; dual-role person in both.
    - Filters re-scope metrics and persist in URL.
    - Profile shows role-correct KPI cards (8 with separator when dual-role) and a month-grouped timeline.
    - Timeline `>` drills to /dashboard/records/[id]; Load-more works; missing person → 404.
    - TR/EN toggle switches all copy.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL params (personId, from/to/project) → analytics SQL | UUID + date params reach queries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-IV | Tampering | personId / project UUID params, date params | mitigate | Drizzle `sql\`\`` binds personId/projectIds/dates as parameters (CR-03); page validates dates with isNaN before constructing Date objects |
| T-08-05-ID | Information Disclosure | cross-tenant person leak; pending_people exposure | mitigate | getPortfolioPeople + getPersonMetrics + getAuditorDecisions are tenant-scoped; directory queries approved `people` only (pending_people excluded by JOIN; office users are auth users, not people) |
| T-08-05-XSS | Tampering | worker/material/rejection text in timeline | mitigate | React JSX auto-escapes; no dangerouslySetInnerHTML |
</threat_model>

<verification>
- `npx vitest run tests/analytics.test.ts` green (getPortfolioPeople)
- `npx tsc --noEmit` clean for the four files
- people pages contain getPortfolioPeople / notFound / getAuditorDecisions
- Manual checkpoint (Task 4)
</verification>

<success_criteria>
- PERF-04: cross-project directory + per-person profile + activity timeline + value contribution.
- UX-03: date+project filters scope directory and profile.
- Dual-role correctness (D-69); auditor timeline from decisions not office_activity_log; bulk query (no N+1).
</success_criteria>

<output>
Create `.planning/phases/08-admin-shell-information-architecture/08-05-SUMMARY.md` when done. Record the final getPortfolioPeople return shape(s) and whether directory pagination was needed.
</output>
