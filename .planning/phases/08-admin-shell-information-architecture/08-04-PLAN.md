---
phase: 08-admin-shell-information-architecture
plan: 04
type: execute
wave: 3
depends_on: [08-01, 08-02, 08-03]
files_modified:
  - src/components/admin/FilterBar.tsx
  - src/components/admin/CurrencySelector.tsx
  - src/components/admin/KpiCard.tsx
  - src/components/admin/TrendChartsClient.tsx
  - src/app/dashboard/(admin)/overview/page.tsx
autonomous: false
requirements: [UX-02, UX-03, UX-04]
user_setup: []

must_haves:
  truths:
    - "The Overview page shows 4 portfolio KPI cards (pending backlog, approvals, rejections, active workers) with range-aware sub-labels"
    - "A global filter bar (date range · project · person) re-scopes the page via URL query params"
    - "Three trend charts (throughput, earned value, rejection rate) render from server-prefetched data"
    - "A currency selector governs money displays; pending-backlog drill-down omits the date filter"
    - "Each KPI stat links to the underlying filtered records (or people for active workers)"
  artifacts:
    - path: "src/app/dashboard/(admin)/overview/page.tsx"
      provides: "force-dynamic RSC reading searchParams, parallel data fetch, Suspense-wrapped FilterBar"
      contains: "force-dynamic"
    - path: "src/components/admin/FilterBar.tsx"
      provides: "'use client' URL-param filter controls (date/project/person/status)"
      contains: "useSearchParams"
    - path: "src/components/admin/TrendChartsClient.tsx"
      provides: "'use client' Recharts LineCharts via ChartContainer"
      contains: "ChartContainer"
    - path: "src/components/admin/KpiCard.tsx"
      provides: "stat card with optional drill-down link"
      contains: "KpiCard"
    - path: "src/components/admin/CurrencySelector.tsx"
      provides: "'use client' page-local currency select (default TRY)"
      contains: "CurrencySelector"
  key_links:
    - from: "overview/page.tsx"
      to: "getPortfolioKPIs + getPortfolioTrends + getPortfolioOverview"
      via: "Promise.all server fetch"
      pattern: "getPortfolioKPIs"
    - from: "FilterBar.tsx"
      to: "URL query params"
      via: "router.push(`${pathname}?${params}`)"
      pattern: "router\\.push"
    - from: "KpiCard pending backlog"
      to: "/dashboard/records?status=pending_audit"
      via: "drill link WITHOUT date filter (D-66/Pitfall 5)"
      pattern: "status=pending_audit"
---

<objective>
Build the Overview command-center page (`/dashboard/(admin)/overview/page.tsx`) and its supporting client components: a range-aware KPI card row, a global URL-param filter bar, a page-local currency selector, and three trend charts. The page is a `force-dynamic` RSC that reads `searchParams`, validates dates, fetches `getPortfolioKPIs` + `getPortfolioTrends` + `getPortfolioOverview` in parallel, and passes serialized data to client components.

Purpose: UX-02 (command center), UX-03 (global filters), UX-04 (trend charts). This is the landing page after the `/dashboard` redirect.
Output: 4 admin components + the overview page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-admin-shell-information-architecture/08-UI-SPEC.md
@.planning/phases/08-admin-shell-information-architecture/08-PATTERNS.md
@.planning/phases/08-admin-shell-information-architecture/08-RESEARCH.md
@.planning/phases/08-admin-shell-information-architecture/08-02-SUMMARY.md
@.planning/phases/08-admin-shell-information-architecture/08-01-SUMMARY.md

<interfaces>
<!-- From plan 02 (confirm exact field names in 08-02-SUMMARY): -->
- getPortfolioKPIs(filters): Promise<PortfolioKPIs> — { pendingBacklog, approvalsInRange, rejectionsInRange, activeWorkers }
- getPortfolioTrends(filters): Promise<TrendPoint[]> — { bucket, currencyCode, approvedCount, rejectedCount, totalCount, earnedValue: string|null }
- getPortfolioOverview(): Promise<ProjectSummary[]> — existing; per-project EV/BAC/currency
- SubmissionFilters: { projectIds?, from?: Date, to?: Date, status?, personId? }

<!-- chart.tsx exports (plan 01): ChartContainer, ChartTooltip, ChartTooltipContent + recharts (LineChart, Line, XAxis, YAxis, CartesianGrid, Area) -->
<!-- i18n keys (plan 01): dashboard.admin.overview.*, dashboard.admin.filters.*, dashboard.admin.currency.* -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FilterBar + CurrencySelector + KpiCard client/server components</name>
  <files>src/components/admin/FilterBar.tsx, src/components/admin/CurrencySelector.tsx, src/components/admin/KpiCard.tsx</files>
  <read_first>
    - src/components/dashboard/KayitlarTabClient.tsx (EXACT analog: useRouter/usePathname/useSearchParams + the `router.push(`${pathname}?${params}`)` URL-state idiom at lines ~114-129; the FilterChips local-state sub-component pattern ~lines 322-351 for CurrencySelector)
    - .planning/phases/08-admin-shell-information-architecture/08-PATTERNS.md (§FilterBar.tsx — applyFilter/clearFilters; §CurrencySelector.tsx — local useState, NOT a URL param; §KpiCard.tsx — Card + optional drill Link + valueColor)
    - .planning/phases/08-admin-shell-information-architecture/08-UI-SPEC.md (§Global Filter Bar — controls/widths/labels; §Currency Selector — default TRY, page-local, "—" for no data; §KPI card anatomy + drill-down targets)
    - src/components/ui/select.tsx + src/components/ui/input.tsx + src/components/ui/button.tsx + src/components/ui/card.tsx (existing shadcn primitives — confirm Select onValueChange null-coalesce per STATE.md Base UI note)
  </read_first>
  <action>Create `src/components/admin/FilterBar.tsx` (`'use client'`). Props: `{ projectOptions: {id,name}[]; personOptions?: {id,name}[]; showStatus?: boolean }`. Use `useRouter`/`usePathname`/`useSearchParams` and `useTranslations('dashboard.admin.filters')`. Render From/To `<Input type="date">` (160px), Project `<Select>` (200px, placeholder = `t('all_projects')`), Person `<Select>` when personOptions provided (placeholder = `t('all_people')`), and a `<Button variant="ghost" size="sm">{t('clear')}</Button>`. `applyFilter(key, value)` clones `new URLSearchParams(searchParams.toString())`, sets/deletes the key, then `router.push(`${pathname}?${params.toString()}`)`. `clearFilters()` → `router.push(pathname)`. Base UI Select onValueChange is `(value: string | null) => ...` — null-coalesce with `?? ''` (STATE.md 01-05 note). Each input has an explicit `<label>` or `aria-label` (accessibility). Mobile: stacked 2×2 grid (`grid grid-cols-2 gap-2 md:flex`).
Create `src/components/admin/CurrencySelector.tsx` (`'use client'`). Props: `{ availableCurrencies: string[]; onCurrencyChange: (c: string) => void }`. Local `useState` defaulting to 'TRY' (NOT a URL param — D-67). `<Select>` 100px wide; label from `useTranslations('dashboard.admin.currency')` `t('label')`.
Create `src/components/admin/KpiCard.tsx` (server-compatible — no hooks). Props: `{ label, subLabel, value, icon, drillHref?, valueColor? }`. Render shadcn `<Card>` with `<CardHeader>` (icon + label, muted) and `<CardContent>` (28px `text-3xl font-semibold tabular-nums` stat in a `<dd>` with the label as `<dt>` for definition-list semantics per UI-SPEC accessibility). When `drillHref` is set, wrap the stat in `<Link href={drillHref} className="hover:underline">`. valueColor maps success→emerald, destructive→`text-destructive`, default→`text-foreground`.</action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "FilterBar.tsx|CurrencySelector.tsx|KpiCard.tsx" || echo "no type errors"; grep -q "useSearchParams" src/components/admin/FilterBar.tsx && grep -q "router.push" src/components/admin/FilterBar.tsx && grep -q "useState" src/components/admin/CurrencySelector.tsx && grep -q "drillHref" src/components/admin/KpiCard.tsx && echo OK</automated>
  </verify>
  <done>FilterBar updates URL params via router.push and clears via pathname; CurrencySelector holds local state defaulting TRY; KpiCard renders Card with optional drill Link and dl/dt/dd semantics; all type-check clean.</done>
</task>

<task type="auto">
  <name>Task 2: TrendChartsClient — three Recharts line charts</name>
  <files>src/components/admin/TrendChartsClient.tsx</files>
  <read_first>
    - src/components/ui/chart.tsx (installed plan 01 — confirm ChartContainer/ChartTooltip/ChartTooltipContent exports + the chartConfig shape)
    - .planning/phases/08-admin-shell-information-architecture/08-PATTERNS.md (§TrendChartsClient.tsx — ChartContainer + LineChart pattern, empty state, chartConfig with --chart-* tokens)
    - .planning/phases/08-admin-shell-information-architecture/08-UI-SPEC.md (§Trend charts — three charts, LineChart type, 240px/180px heights, axis colors --chart-1/2/3, titles, empty "Veri yok"/"No data", weekly/monthly bucket rule, tooltip currency)
    - .planning/phases/08-admin-shell-information-architecture/08-RESEARCH.md (Pitfall 8 — chart components MUST be 'use client'; data passed as serialized props, never re-fetched)
  </read_first>
  <action>Create `src/components/admin/TrendChartsClient.tsx` (`'use client'`). Props: `{ data: TrendPoint[]; currencyCode: string }`. Filter `data` to the selected `currencyCode` for the Earned Value series. Export three chart components (or one component rendering three in a grid): Throughput (`approvedCount`/`totalCount`, `var(--chart-1)`), Earned Value (`earnedValue` parsed for the selected currency, `var(--chart-2)`), Rejection Rate (`rejectedCount / totalCount × 100`, `var(--chart-3)`). Each uses `<ChartContainer config={chartConfig} className="h-[240px]">` (mobile 180px via responsive class) wrapping a Recharts `<LineChart>` with `<CartesianGrid strokeDasharray="3 3">`, `<XAxis dataKey="bucket" tick={{fontSize:14}}>`, `<YAxis tick={{fontSize:14}}>`, `<ChartTooltip content={<ChartTooltipContent />}>`, and `<Line type="monotone" dot={false}>`. Titles via `useTranslations('dashboard.admin.overview')` (chart_throughput/chart_earned_value/chart_rejection_rate). Each `<ChartContainer>` gets an `aria-label` describing its subject. Empty state: when data is empty render a 240px centered `t('chart_no_data')` block. Currency in the EV tooltip obeys the passed `currencyCode`. Use `tabular-nums` for any inline numbers. Do NOT import recharts in any server component.</action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "TrendChartsClient.tsx" || echo "no type errors"; head -1 src/components/admin/TrendChartsClient.tsx | grep -q "use client" && grep -q "ChartContainer" src/components/admin/TrendChartsClient.tsx && grep -q "LineChart" src/components/admin/TrendChartsClient.tsx && echo OK</automated>
  </verify>
  <done>TrendChartsClient is 'use client', renders three Recharts LineCharts via ChartContainer with --chart-1/2/3 tokens, range-aware empty states, aria-labels, and selected-currency EV; no recharts import escapes to a server component.</done>
</task>

<task type="auto">
  <name>Task 3: Overview page — RSC data fetch + KPI row + charts + EV table</name>
  <files>src/app/dashboard/(admin)/overview/page.tsx</files>
  <read_first>
    - .planning/phases/08-admin-shell-information-architecture/08-PATTERNS.md (§overview/page.tsx — force-dynamic, async searchParams with isNaN date validation, Promise.all, Suspense-wrapped FilterBar)
    - .planning/phases/08-admin-shell-information-architecture/08-UI-SPEC.md (§Overview Page — page layout, KPI cards table with icons/labels/drill targets, range-aware subtitle, EV table columns + Progress bar + "—" rule, empty states)
    - .planning/phases/08-admin-shell-information-architecture/08-RESEARCH.md (Pattern 3 RSC searchParams; Pitfall 3 Suspense around FilterBar; Pitfall 4/5 pending-backlog drill WITHOUT date filter)
    - .planning/phases/08-admin-shell-information-architecture/08-02-SUMMARY.md (exact PortfolioKPIs / TrendPoint field names)
    - src/components/dashboard/KayitlarTabClient.tsx (date/number locale formatting: toLocaleDateString('tr-TR'), Intl.NumberFormat('tr-TR'))
  </read_first>
  <action>Create `src/app/dashboard/(admin)/overview/page.tsx`: `export const dynamic = 'force-dynamic'`. Props `{ searchParams: Promise<{ from?, to?, project?, person?, status? }> }`. `await searchParams`; build a validated `filters` object — dates via `from && !isNaN(Date.parse(from)) ? new Date(from) : undefined` (Tampering mitigation — never pass a raw bad string to SQL); `projectIds: project ? [project] : undefined`; `personId: person || undefined`. `const t = await getTranslations('dashboard.admin.overview')`. Fetch `const [kpis, trends, overview] = await Promise.all([getPortfolioKPIs(filters), getPortfolioTrends(filters), getPortfolioOverview()])`. Heading `t('heading')`; subtitle = `(from||to) ? t('subtitle_filtered') : t('subtitle_all_time')`. Wrap `<FilterBar projectOptions={...} personOptions={...} />` in `<Suspense fallback={<div className="h-12 animate-pulse bg-muted rounded" />}>` (Pitfall 3). Place CurrencySelector adjacent (right-aligned). Render the 4 KpiCards in a `grid grid-cols-2 md:grid-cols-4 gap-8` row: Pending (Clock icon, `t('kpi_pending_label')`, sub `t('kpi_pending_sub')`, valueColor destructive if >20, drillHref `/dashboard/records?status=pending_audit` — NO date params per Pitfall 5); Approvals (CheckCircle2, success, drill `/dashboard/records?status=approved` + current from/to); Rejections (XCircle, destructive, drill `/dashboard/records?status=rejected` + from/to); Active Workers (HardHat, drill `/dashboard/people` + from/to). Sub-labels are range-aware: `(from||to) ? t('kpi_sub_filtered') : t('kpi_sub_all_time')` (Pending always uses kpi_pending_sub). Render `<TrendChartsClient data={trends} currencyCode={...} />` in a `grid md:grid-cols-3` row. Render the EV table from `overview` (shadcn Table): Project (link to `/dashboard/projects/[id]`), Contracted (BAC), Earned Value, % Complete with inline `<Progress>` — money via `new Decimal(...).toNumber()` + `Intl.NumberFormat('tr-TR', {minimumFractionDigits:2, maximumFractionDigits:2})`; rows with no value in the selected currency show "—" and no progress bar. Empty state when overview has no priced projects: LayoutDashboard icon 48px + `t('empty_no_projects')`. Since the currency selector is a client control and the page is RSC, render money for the default currency (TRY) server-side and let the client selector swap among the currency-keyed maps the data provides (or, simplest acceptable: build the EV table inside a small client wrapper that receives the currency-keyed maps and re-renders on selection — Claude's discretion, keep it simple and avoid cross-currency sums).</action>
  <verify>
    <automated>test -f "src/app/dashboard/(admin)/overview/page.tsx" && grep -q "force-dynamic" "src/app/dashboard/(admin)/overview/page.tsx" && grep -q "getPortfolioKPIs" "src/app/dashboard/(admin)/overview/page.tsx" && grep -q "Suspense" "src/app/dashboard/(admin)/overview/page.tsx" && grep -q "status=pending_audit" "src/app/dashboard/(admin)/overview/page.tsx" && npx tsc --noEmit 2>&1 | grep -E "overview/page.tsx" || echo "no type errors in overview page"</automated>
  </verify>
  <done>Overview page is force-dynamic, validates dates before SQL, fetches all three data sources in parallel, renders 4 range-aware KPI cards with correct drill targets (pending without date filter), three trend charts, and an EV table with "—" for missing-currency rows; FilterBar is Suspense-wrapped.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify Overview renders, filters re-scope, charts + currency work (browser-only)</name>
  <what-built>The full Overview command center: KPI cards, filter bar, currency selector, three trend charts, EV table. Rendering + URL-filter re-scoping are browser behaviors (manual-only per VALIDATION.md).</what-built>
  <how-to-verify>
    1. `npm run dev`, sign in, land on `/dashboard/overview`.
    2. Confirm 4 KPI cards show numbers; sub-labels read "All time"/"Tüm zamanlarda" with no filter.
    3. Set a From/To date range → page re-renders; approvals/rejections/active-workers sub-labels switch to "In selected period"; pending backlog stays its all-time count (D-66).
    4. Pick a project in the filter → KPIs + charts + EV table re-scope; URL shows `?project=...`. Navigate away and back via the link — params persist in URL.
    5. Click the Pending KPI → lands on `/dashboard/records?status=pending_audit` with NO date param. Click Approvals → `/dashboard/records?status=approved&from=...&to=...`.
    6. Confirm three charts render (or show "No data"/"Veri yok" if empty). Change the currency selector → EV table + EV chart numbers switch currency; missing-currency rows show "—".
    7. Toggle locale TR↔EN → all labels switch.
  </how-to-verify>
  <read_first>
    - .planning/phases/08-admin-shell-information-architecture/08-UI-SPEC.md (§Overview Page; §Currency Selector; §Trend charts)
  </read_first>
  <acceptance_criteria>
    - 4 KPI cards render with range-aware sub-labels; pending backlog is not date-scoped.
    - Filter changes update the URL and re-scope KPIs/charts/EV table; params persist across navigation.
    - Pending drill omits date filter; approvals/rejections drill includes from/to.
    - Charts render; currency selector swaps money displays; "—" for missing-currency rows.
    - TR/EN toggle switches all copy.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL searchParams → overview RSC → analytics SQL | from/to/project/person reach query functions |
| Server-fetched trend/EV strings → client charts | money strings serialized to the browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-DATE | Tampering | `?from=`/`?to=` params | mitigate | Validate with `!isNaN(Date.parse(str))` in page.tsx; invalid → undefined; only `new Date(...)` objects reach analytics (which binds them as params) |
| T-08-04-ID | Information Disclosure | project/person filter params | mitigate | analytics functions are tenant-scoped (plan 02); page passes IDs as filter values bound by Drizzle |
| T-08-04-MONEY | Tampering | EV display | mitigate | `new Decimal(str)` for display; never cross-currency sum; currency selector picks one currency-keyed map |
</threat_model>

<verification>
- `npx tsc --noEmit` clean for the five files
- overview page contains force-dynamic, getPortfolioKPIs, Suspense, status=pending_audit
- FilterBar uses useSearchParams + router.push
- Manual checkpoint (Task 4) confirms render + filter behaviors
</verification>

<success_criteria>
- UX-02: portfolio KPIs + EV table on the command center.
- UX-03: URL-persisted date/project/person filters re-scope the page.
- UX-04: three trend charts from prefetched data.
- Pending backlog point-in-time; currency selector default TRY; metric drill-downs correct.
</success_criteria>

<output>
Create `.planning/phases/08-admin-shell-information-architecture/08-04-SUMMARY.md` when done. Record the final FilterBar prop shape and whether the EV table was made a client wrapper (so plans 05/06 reuse FilterBar consistently).
</output>
