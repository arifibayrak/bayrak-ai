---
phase: 08-admin-shell-information-architecture
plan: 03
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - src/app/dashboard/layout.tsx
  - src/components/layout/TopNav.tsx
  - src/components/admin/AppSidebar.tsx
  - src/components/admin/SidebarNav.tsx
  - src/app/dashboard/page.tsx
  - src/app/dashboard/(admin)/layout.tsx
  - src/app/dashboard/(admin)/analytics/page.tsx
  - src/app/dashboard/(admin)/hakedis/page.tsx
  - src/app/dashboard/(admin)/exports/page.tsx
autonomous: false
requirements: [UX-01]
user_setup: []

must_haves:
  truths:
    - "A persistent sidebar (Overview · Projects · People · Analytics · Hakkediş · Exports) appears on every dashboard page including /dashboard/projects/*"
    - "The active nav item is highlighted based on the current route"
    - "Visiting /dashboard redirects to /dashboard/overview"
    - "Analytics/Hakkediş/Exports routes render a Coming-soon stub instead of 404"
    - "Existing /dashboard/projects/* page files are unmodified"
  artifacts:
    - path: "src/app/dashboard/layout.tsx"
      provides: "SidebarProvider + AppSidebar + SidebarInset wrapping TopNav and children; auth guard preserved"
      contains: "SidebarProvider"
    - path: "src/components/admin/AppSidebar.tsx"
      provides: "Sidebar shell composing SidebarNav + wordmark"
      contains: "AppSidebar"
    - path: "src/components/admin/SidebarNav.tsx"
      provides: "client nav with usePathname active detection and 6 NAV_ITEMS"
      contains: "usePathname"
    - path: "src/app/dashboard/page.tsx"
      provides: "redirect('/dashboard/overview')"
      contains: "redirect"
    - path: "src/app/dashboard/(admin)/layout.tsx"
      provides: "passthrough layout (no second SidebarProvider)"
      contains: "children"
    - path: "src/app/dashboard/(admin)/analytics/page.tsx"
      provides: "Coming-soon stub"
      contains: "stubs"
  key_links:
    - from: "src/app/dashboard/layout.tsx"
      to: "src/components/admin/AppSidebar.tsx"
      via: "import + render inside SidebarProvider"
      pattern: "AppSidebar"
    - from: "src/components/admin/SidebarNav.tsx"
      to: "next/navigation usePathname"
      via: "active-item detection"
      pattern: "usePathname"
    - from: "src/components/layout/TopNav.tsx"
      to: "SidebarTrigger"
      via: "mobile hamburger inside SidebarInset"
      pattern: "SidebarTrigger"
---

<objective>
Install the persistent admin shell by modifying the ROOT `src/app/dashboard/layout.tsx` ONLY, so every dashboard route — including the existing `/dashboard/projects/*` — inherits the sidebar without any project page file being touched (D-64). Build `AppSidebar` + the `'use client'` `SidebarNav` (6 items, active detection), add the `SidebarTrigger` mobile hamburger to `TopNav`, create the `/dashboard → /dashboard/overview` redirect, the `(admin)` passthrough layout, and the three Coming-soon stub pages so Analytics/Hakkediş/Exports nav items do not 404.

Purpose: UX-01 — the navigation shell. This is the structural frame all later pages render inside.
Output: modified root layout + TopNav, new AppSidebar + SidebarNav, redirect page, passthrough layout, 3 stub pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-admin-shell-information-architecture/08-UI-SPEC.md
@.planning/phases/08-admin-shell-information-architecture/08-PATTERNS.md
@.planning/phases/08-admin-shell-information-architecture/08-RESEARCH.md
@.planning/phases/08-admin-shell-information-architecture/08-01-SUMMARY.md

<interfaces>
<!-- Current root layout (src/app/dashboard/layout.tsx) — auth guard MUST be preserved verbatim: -->
- `await auth()` → `if (!session) redirect('/auth/signin')`
- `const userEmail = session.user?.email ?? ''`
- TopNav currently dynamically imported; can become a static import once AppSidebar exists.
- Current return wraps `<TopNav userEmail={userEmail} />` + `<main className="max-w-5xl mx-auto px-6 py-8 sm:py-10">{children}</main>` in a `min-h-screen` div.

<!-- sidebar.tsx exports (confirm exact names from 08-01-SUMMARY before importing): -->
- SidebarProvider, Sidebar, SidebarInset, SidebarTrigger, SidebarMenu, SidebarMenuButton, SidebarMenuItem (+ SidebarHeader/SidebarContent if present)

<!-- i18n keys already added in plan 01 (consume, do not add): dashboard.admin.nav.* and dashboard.admin.stubs.* -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build AppSidebar + SidebarNav and wire the shell into the root layout + TopNav</name>
  <files>src/components/admin/SidebarNav.tsx, src/components/admin/AppSidebar.tsx, src/app/dashboard/layout.tsx, src/components/layout/TopNav.tsx</files>
  <read_first>
    - src/app/dashboard/layout.tsx (CURRENT FILE — preserve the auth guard + userEmail extraction verbatim; replace only the return JSX)
    - src/components/layout/TopNav.tsx (CURRENT FILE — preserve header/flex; add SidebarTrigger left of the wordmark)
    - src/components/ui/sidebar.tsx (installed in plan 01 — confirm exact export names)
    - src/components/dashboard/KayitlarTabClient.tsx (analog for a `'use client'` component using usePathname/useTranslations — lines 1-26 imports, the active-state detection idiom)
    - .planning/phases/08-admin-shell-information-architecture/08-PATTERNS.md (§src/app/dashboard/layout.tsx; §src/components/admin/SidebarNav.tsx — the NAV_ITEMS array + active-detection; §src/components/layout/TopNav.tsx — SidebarTrigger placement)
    - .planning/phases/08-admin-shell-information-architecture/08-UI-SPEC.md (§Sidebar navigation structure — the 6 items, icons, routes, active treatment; §Admin shell layout)
    - .planning/phases/08-admin-shell-information-architecture/08-01-SUMMARY.md (actual sidebar export names + any deviation)
  </read_first>
  <action>Create `src/components/admin/SidebarNav.tsx` as a `'use client'` component. Define `NAV_ITEMS` exactly: overview→`/dashboard/overview` (LayoutDashboard, exact match), projects→`/dashboard/projects` (FolderOpen, prefix match), people→`/dashboard/people` (Users, prefix match), analytics→`/dashboard/analytics` (BarChart2, exact), hakedis→`/dashboard/hakedis` (FileText, exact), exports→`/dashboard/exports` (Download, exact) — icons from lucide-react. Use `usePathname()` for active detection: exact items compare `pathname === href`; prefix items use `pathname.startsWith(href)`. Render via `SidebarMenu`/`SidebarMenuItem`/`SidebarMenuButton asChild isActive={isActive}` with an `<a href>` carrying `aria-current={isActive ? 'page' : undefined}`. Labels via `useTranslations('dashboard.admin.nav')` (keys: overview/projects/people/analytics/hakedis/exports); wrap in `<nav aria-label={t('main_nav_aria')}>`. Active visual treatment per UI-SPEC: `--sidebar-accent` background + 3px left `--primary` border + `--sidebar-accent-foreground` label/icon (apply via the sidebar component's isActive styling and a className for the left border). NOTE: `/dashboard/records` has NO nav item (D-74) — do not add it.
Create `src/components/admin/AppSidebar.tsx` composing `<Sidebar>` (with SidebarHeader showing the wordmark if that export exists, else a header div) + `<SidebarNav />` inside `<SidebarContent>` (or the closest available slot). AppSidebar may be a server component (it renders the client SidebarNav).
Modify `src/app/dashboard/layout.tsx`: KEEP `await auth()` + redirect + `userEmail` exactly. Convert the dynamic TopNav import to a static import, add `import { SidebarProvider, SidebarInset } from '@/components/ui/sidebar'` and `import { AppSidebar } from '@/components/admin/AppSidebar'`. Replace the return with `<SidebarProvider><AppSidebar /><SidebarInset><TopNav userEmail={userEmail} /><main className="max-w-5xl mx-auto px-6 py-8 sm:py-10">{children}</main></SidebarInset></SidebarProvider>`.
Modify `src/components/layout/TopNav.tsx`: add `import { SidebarTrigger } from '@/components/ui/sidebar'` and place `<SidebarTrigger className="md:hidden" aria-label={t('open_nav')} />` immediately left of the wordmark (wrap both in a `flex items-center gap-2` div). `open_nav` key lives in `dashboard.admin.nav` (added in plan 01) — read it via the appropriate translations call for TopNav's existing namespace usage; if TopNav uses `nav.*`, fetch the admin key with a second `getTranslations('dashboard.admin.nav')`/`useTranslations` as fits TopNav's server/client nature.</action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "layout.tsx|TopNav.tsx|SidebarNav.tsx|AppSidebar.tsx" || echo "no type errors in shell files"; grep -q "SidebarProvider" src/app/dashboard/layout.tsx && grep -q "AppSidebar" src/app/dashboard/layout.tsx && grep -q "usePathname" src/components/admin/SidebarNav.tsx && grep -q "SidebarTrigger" src/components/layout/TopNav.tsx && echo WIRED</automated>
  </verify>
  <done>Root layout wraps children in SidebarProvider + AppSidebar + SidebarInset with auth guard intact; SidebarNav renders 6 items with usePathname active detection and aria-current; TopNav has the mobile SidebarTrigger; no existing project page file was modified.</done>
</task>

<task type="auto">
  <name>Task 2: Redirect page, (admin) passthrough layout, and 3 Coming-soon stub pages</name>
  <files>src/app/dashboard/page.tsx, src/app/dashboard/(admin)/layout.tsx, src/app/dashboard/(admin)/analytics/page.tsx, src/app/dashboard/(admin)/hakedis/page.tsx, src/app/dashboard/(admin)/exports/page.tsx</files>
  <read_first>
    - .planning/phases/08-admin-shell-information-architecture/08-PATTERNS.md (§src/app/dashboard/page.tsx redirect; §(admin)/layout.tsx passthrough — anti-pattern: NO second SidebarProvider; §analytics/hakedis/exports stub pattern)
    - src/app/dashboard/projects/page.tsx (analog: minimal RSC with getTranslations + heading)
    - .planning/phases/08-admin-shell-information-architecture/08-UI-SPEC.md (§Stub pages; §Copywriting Contract — stub headings/badge/body)
    - .planning/phases/08-admin-shell-information-architecture/08-RESEARCH.md (Pitfall 1 double-sidebar)
  </read_first>
  <action>Create `src/app/dashboard/page.tsx`: `export const dynamic = 'force-dynamic'` and a default function that calls `redirect('/dashboard/overview')` from `next/navigation` — no JSX. Create `src/app/dashboard/(admin)/layout.tsx` as a pure passthrough: `export default function AdminLayout({ children }: { children: React.ReactNode }) { return <>{children}</>; }` — do NOT add a second SidebarProvider (Pitfall 1). Create the three stub pages `(admin)/analytics/page.tsx`, `(admin)/hakedis/page.tsx`, `(admin)/exports/page.tsx`, each `export const dynamic = 'force-dynamic'`, async RSC using `getTranslations('dashboard.admin.stubs')`: render an `<h1 className="text-xl font-semibold">{t('analytics_heading')}</h1>` (etc.), a `<Badge variant="secondary">{t('coming_soon')}</Badge>`, and a muted `<p className="text-sm text-muted-foreground">{t('analytics_body')}</p>` (use hakedis_heading/hakedis_body and exports_heading/exports_body for the others). All copy comes from the plan-01 keys — invent nothing.</action>
  <verify>
    <automated>test -f "src/app/dashboard/page.tsx" && test -f "src/app/dashboard/(admin)/layout.tsx" && test -f "src/app/dashboard/(admin)/analytics/page.tsx" && test -f "src/app/dashboard/(admin)/hakedis/page.tsx" && test -f "src/app/dashboard/(admin)/exports/page.tsx" && grep -q "redirect('/dashboard/overview')" "src/app/dashboard/page.tsx" && ! grep -q "SidebarProvider" "src/app/dashboard/(admin)/layout.tsx" && echo OK</automated>
  </verify>
  <done>/dashboard redirects to /dashboard/overview; (admin)/layout.tsx is a passthrough with no SidebarProvider; the three stub pages render heading + Coming-soon badge + body from dashboard.admin.stubs keys and carry force-dynamic.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify shell renders on all pages + redirect + active state (browser-only per VALIDATION.md)</name>
  <what-built>The persistent sidebar shell wired into the root dashboard layout, the /dashboard→/dashboard/overview redirect, the mobile hamburger, and three Coming-soon stub pages. These are browser-render behaviors that VALIDATION.md classifies as manual-only (no E2E framework in this project).</what-built>
  <how-to-verify>
    1. Run the dev server (non-Turbopack is required for map work but fine here): `npm run dev`. Sign in.
    2. Visit `/dashboard` → confirm it redirects to `/dashboard/overview`.
    3. Confirm the sidebar (Overview · Projects · People · Analytics · Hakkediş · Exports) is visible. Visit `/dashboard/projects` and `/dashboard/projects/[id]` → confirm the SAME sidebar appears and the "Projects" item is highlighted (active). On `/dashboard/overview` confirm "Overview" is highlighted.
    4. Click Analytics, Hakkediş, Exports → each shows a Coming-soon stub, not a 404.
    5. Toggle locale TR↔EN → nav labels switch (Genel Bakış ↔ Overview, etc.).
    6. Narrow the viewport (<768px) → sidebar collapses to a hamburger in TopNav; tapping it opens the drawer.
    7. Confirm no project page lost functionality (BOQ/Rota/Kayıtlar tabs still work).
  </how-to-verify>
  <read_first>
    - .planning/phases/08-admin-shell-information-architecture/08-UI-SPEC.md (§Responsiveness; §Sidebar active item visual treatment)
  </read_first>
  <acceptance_criteria>
    - /dashboard redirects to /dashboard/overview (observed in browser).
    - Sidebar with all 6 items renders on /dashboard/overview AND /dashboard/projects/* (D-64).
    - Active item highlight follows the route (Projects active on project routes; Overview active on overview).
    - Analytics/Hakkediş/Exports show Coming-soon, not 404.
    - Locale toggle switches nav labels; mobile hamburger opens the drawer.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe what rendered incorrectly.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → dashboard layout (auth) | Unauthenticated requests must not see the shell |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-AC | Elevation of Privilege | dashboard/layout.tsx auth guard | mitigate | Preserve `await auth()` + `redirect('/auth/signin')` BEFORE the SidebarProvider render — auth runs server-side before any client tree mounts |
| T-08-03-NAV | Information Disclosure | sidebar nav links | accept | Nav links are static internal routes; each target page enforces its own auth via the inherited layout guard |
</threat_model>

<verification>
- `npx tsc --noEmit` clean for the shell files
- `grep -q "SidebarProvider" src/app/dashboard/layout.tsx` and auth guard still present
- `! grep -q "SidebarProvider" "src/app/dashboard/(admin)/layout.tsx"` (no double sidebar)
- Manual checkpoint (Task 3) confirms render behaviors
</verification>

<success_criteria>
- Persistent sidebar on every dashboard page incl. /dashboard/projects/* (UX-01).
- /dashboard → /dashboard/overview redirect.
- Active item highlight; mobile hamburger; stub pages reachable.
- Existing project routes/files unmodified.
</success_criteria>

<output>
Create `.planning/phases/08-admin-shell-information-architecture/08-03-SUMMARY.md` when done.
</output>
