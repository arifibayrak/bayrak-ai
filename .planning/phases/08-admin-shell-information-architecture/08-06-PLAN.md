---
phase: 08-admin-shell-information-architecture
plan: 06
type: execute
wave: 4
depends_on: [08-01, 08-02, 08-03, 08-04]
files_modified:
  - src/components/admin/SubmissionDetailView.tsx
  - src/app/dashboard/records/page.tsx
  - src/app/dashboard/records/[id]/page.tsx
  - src/components/dashboard/KayitlarTabClient.tsx
autonomous: false
requirements: [UX-05]
user_setup: []

must_haves:
  truths:
    - "A cross-project records list at /dashboard/records is filterable and paginated (25/page)"
    - "Each record row links to a canonical submission detail page"
    - "The detail page shows the full record: photo, location, BOQ item, quantity, status, auditor decision, rejection reason, with an empty AI slot"
    - "The existing project Kayitlar tab rows gain an additive Details link, lightbox preserved"
    - "The records list is reachable via metric drill-downs but has NO sidebar item"
  artifacts:
    - path: "src/app/dashboard/records/page.tsx"
      provides: "force-dynamic filterable paginated records list"
      contains: "force-dynamic"
    - path: "src/app/dashboard/records/[id]/page.tsx"
      provides: "force-dynamic detail page; notFound on missing"
      contains: "notFound"
    - path: "src/components/admin/SubmissionDetailView.tsx"
      provides: "dl/dt/dd record view + photo lightbox + Google Maps link + AI slot"
      contains: "maps.google"
    - path: "src/components/dashboard/KayitlarTabClient.tsx"
      provides: "additive Details link column (lightbox unchanged)"
      contains: "dashboard/records/"
  key_links:
    - from: "records/page.tsx"
      to: "getCanonicalSubmissions with limit/offset"
      via: "paginated server fetch"
      pattern: "getCanonicalSubmissions"
    - from: "records/[id]/page.tsx"
      to: "getCanonicalSubmissions submissionId lookup"
      via: "single-record lookup + notFound"
      pattern: "submissionId"
    - from: "KayitlarTabClient row"
      to: "/dashboard/records/[id]"
      via: "additive Details link"
      pattern: "dashboard/records/"
---

<objective>
Build the cross-project records list (/dashboard/records) — the drill-down target for every metric (D-74, NO sidebar item) — and the canonical submission detail page (/dashboard/records/[id], D-71). Add the SubmissionDetailView component (full record via a definition list, photo lightbox reusing the D-61 pattern, Google Maps link, and an empty AI-flags slot). Make the minimal additive change to the existing Phase 5 KayitlarTabClient so each row gains a Details link to the new detail page (D-72) — lightbox behavior preserved.

Purpose: UX-05 — every metric drills to filtered records and a structured submission detail view.
Output: records list page, detail page, SubmissionDetailView component, additive Kayitlar Details link.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-admin-shell-information-architecture/08-UI-SPEC.md
@.planning/phases/08-admin-shell-information-architecture/08-PATTERNS.md
@.planning/phases/08-admin-shell-information-architecture/08-RESEARCH.md
@.planning/phases/08-admin-shell-information-architecture/08-02-SUMMARY.md
@.planning/phases/08-admin-shell-information-architecture/08-04-SUMMARY.md

<interfaces>
From plan 02 (see 08-02-SUMMARY for confirmed shapes):
- getCanonicalSubmissions(filters) where filters may include projectIds, from, to, status, personId, submissionId, limit, offset.
  - submissionId returns exactly the one row (or empty array); limit/offset return a page slice; ORDER BY submitted_at DESC.
- CanonicalSubmission fields (all money/qty are STRING): id, projectId, projectName, personId, workerName,
  auditorName, boqItemId, material, unit, unitPrice, currencyCode, quantity, earnedValue, status,
  submittedAt, decidedAt, auditLatencyHours, locationMatch, locationDistanceM, photoUrl, notes, rejectionReason.

From plan 04: FilterBar({ projectOptions, personOptions?, showStatus? }) — reuse with showStatus on the records page.

Existing KayitlarTabClient (src/components/dashboard/KayitlarTabClient.tsx, 351 lines):
- 7 LOCKED columns; row.id available; photo lightbox via shadcn Dialog + next/image (D-61, lines ~203-216, 298-317).
- Google Maps link pattern lines ~244-258; StatusBadge lines ~90-104; pagination prev/next lines ~273-296.

Pagination total count: confirm from 08-02-SUMMARY whether getCanonicalSubmissions returns a total count. If it does not, use the limit+1 lookahead approach described in Task 2.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SubmissionDetailView component (full record + photo lightbox + Maps link + AI slot)</name>
  <files>src/components/admin/SubmissionDetailView.tsx</files>
  <read_first>
    - src/components/dashboard/KayitlarTabClient.tsx (D-61 photo lightbox lines ~203-216 + Dialog lines ~298-317 — REUSE EXACTLY; Google Maps link lines ~244-258 with rel noopener noreferrer; StatusBadge lines ~90-104)
    - .planning/phases/08-admin-shell-information-architecture/08-PATTERNS.md (SubmissionDetailView.tsx section — dl/dt/dd semantic pairs, photo lightbox reuse, Google Maps link)
    - .planning/phases/08-admin-shell-information-architecture/08-UI-SPEC.md (Canonical Submission Detail Page section — two-column layout, photo block, detail field labels, location field with distance/warning, rejection-reason Alert, AI flags slot copy/icon)
    - src/lib/types/canonical-submission.ts (the exact CanonicalSubmission field names and types — confirm which coordinate fields exist for the Maps link)
  </read_first>
  <action>Create src/components/admin/SubmissionDetailView.tsx. Props are a single CanonicalSubmission. Because the photo lightbox needs client interactivity, make this a 'use client' component (simplest: one client view reusing the D-61 pattern). Localize via useTranslations('dashboard.admin.detail'). Layout: a status Badge using the StatusBadge color logic, then a two-column grid (desktop photo left / details right; mobile stacked). Photo block: a next/image thumbnail 200x200 object-cover that opens a shadcn Dialog lightbox with the full image — copy the D-61 pattern from KayitlarTabClient verbatim (do not reinvent); a no-photo state shows a muted ImageOff icon 48px. Details: a definition list (dl with dt Label 14px/600 muted and dd Body 14px/400) for: Worker (t('field_worker') → workerName), Project (projectName), BOQ Item (material), Quantity (quantity + unit via Intl.NumberFormat('tr-TR')), Submitted (toLocaleDateString('tr-TR') of submittedAt), Location, Auditor (auditorName or em dash), Decided (decidedAt formatted or em dash), Rejection reason (ONLY when status is 'rejected'). Location field: when location data is present render the Google Maps link (target _blank, rel noopener noreferrer) plus an "X m from route" distance from locationDistanceM; when locationMatch is 'far' (location warning) add a destructive Badge t('field_location_warning'); no location → em dash. Confirm from the CanonicalSubmission type which lat/lon or snapped-point fields are available for the Maps link; if raw coordinates are not on the canonical type, surface this in the SUMMARY and either link via the available snapped point or show distance only. Rejection reason renders inside a muted Alert with an AlertCircle icon — React auto-escapes the text (no dangerouslySetInnerHTML). AI flags slot: ALWAYS render an inert placeholder Alert variant secondary with a Sparkles icon, label t('ai_slot_label'), body t('ai_slot_body') — it MUST NOT be wired to any data (Phase 6 deferred).</action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "SubmissionDetailView.tsx" || echo "no type errors"; grep -q "maps.google" src/components/admin/SubmissionDetailView.tsx && grep -q "ai_slot_body" src/components/admin/SubmissionDetailView.tsx && grep -q "noopener noreferrer" src/components/admin/SubmissionDetailView.tsx && ! grep -q "dangerouslySetInnerHTML" src/components/admin/SubmissionDetailView.tsx && echo OK</automated>
  </verify>
  <done>SubmissionDetailView renders the full canonical record as a definition list, reuses the D-61 photo lightbox, shows a Google Maps link (rel noopener) with distance/warning, renders rejection reason only when rejected, and always shows an inert AI-flags placeholder; no dangerouslySetInnerHTML.</done>
</task>

<task type="auto">
  <name>Task 2: Records list page + submission detail page</name>
  <files>src/app/dashboard/records/page.tsx, src/app/dashboard/records/[id]/page.tsx</files>
  <read_first>
    - .planning/phases/08-admin-shell-information-architecture/08-PATTERNS.md (records/page.tsx section — searchParams incl. page, PAGE_SIZE 25, limit/offset; records/[id]/page.tsx section — await params + notFound single fetch; filter-propagation on navigation links)
    - src/app/dashboard/projects/[id]/page.tsx (analog: force-dynamic, await params/searchParams, notFound)
    - src/components/dashboard/KayitlarTabClient.tsx (table + StatusBadge + pagination prev/next idiom lines ~273-296)
    - .planning/phases/08-admin-shell-information-architecture/08-UI-SPEC.md (Cross-Project Records List section — columns, 25/page pagination, empty state; Submission detail back link preserving filters)
    - .planning/phases/08-admin-shell-information-architecture/08-RESEARCH.md (Pitfall 3 Suspense around FilterBar; Pitfall 5 drill-down filter behavior)
    - .planning/phases/08-admin-shell-information-architecture/08-02-SUMMARY.md (whether getCanonicalSubmissions returns a total count or only rows)
    - .planning/phases/08-admin-shell-information-architecture/08-04-SUMMARY.md (FilterBar prop shape incl. showStatus)
  </read_first>
  <action>Create src/app/dashboard/records/page.tsx (lives OUTSIDE the (admin) group per UI-SPEC, since it is also reachable from the project Kayitlar tab). export const dynamic = 'force-dynamic'. Props are searchParams as a Promise of { from, to, project, person, status, page }. Await searchParams; validate dates with the isNaN guard; pageNum = page ? Math.max(1, parseInt(page,10)) : 1; PAGE_SIZE = 25; build filters including status (cast to the union or undefined), limit PAGE_SIZE, offset (pageNum-1)*PAGE_SIZE. const t = await getTranslations('dashboard.admin.records'). Fetch the page rows via getCanonicalSubmissions(filters). Heading t('heading'). Suspense-wrap FilterBar with projectOptions, personOptions and showStatus (all five params active here). Render a shadcn Table inside overflow-x-auto with columns Status (color-coded Badge via the StatusBadge logic), Worker, Project, Item (material), Quantity (tabular-nums + unit), Submitted (toLocaleDateString tr-TR), Auditor (auditorName or em dash), Actions (a ghost Button asChild wrapping a Link to /dashboard/records/[id] with the current filter params appended, label t('details')). Use th scope col headers; the Actions header is sr-only. Build a currentParams query string from the active filter params so the detail page can offer a filter-preserving back link. Pagination: prev/next outline Buttons updating ?page=; display t('pagination', { page: pageNum, pages }). For total page count: if 08-02-SUMMARY exposes a total count, use it; otherwise request PAGE_SIZE+1 rows to detect a next page and disable Next when fewer than a full page is returned — record the chosen approach in the SUMMARY. Empty state: a FileSearch icon 48px + t('empty').
Create src/app/dashboard/records/[id]/page.tsx. export const dynamic = 'force-dynamic'. Props are params (Promise of { id }) and searchParams (Promise of the filter params). const { id } = await params. const rows = await getCanonicalSubmissions({ submissionId: id }); if (rows.length === 0) notFound(). Back link t('back') to /dashboard/records with the preserved filter params rebuilt from searchParams so the user returns to their filtered list. Heading t('heading'). Render SubmissionDetailView with rows[0].</action>
  <verify>
    <automated>test -f "src/app/dashboard/records/page.tsx" && test -f "src/app/dashboard/records/[id]/page.tsx" && grep -q "force-dynamic" "src/app/dashboard/records/page.tsx" && grep -q "getCanonicalSubmissions" "src/app/dashboard/records/page.tsx" && grep -q "submissionId" "src/app/dashboard/records/[id]/page.tsx" && grep -q "notFound" "src/app/dashboard/records/[id]/page.tsx" && grep -q "Suspense" "src/app/dashboard/records/page.tsx" && echo OK</automated>
  </verify>
  <done>Records list is force-dynamic, validates dates, paginates 25/page with prev/next, and links each row to the detail page preserving filters; detail page does a submissionId lookup, calls notFound on miss, and renders SubmissionDetailView with a filter-preserving back link.</done>
</task>

<task type="auto">
  <name>Task 3: Additive Details link on the existing Kayitlar tab (lightbox preserved)</name>
  <files>src/components/dashboard/KayitlarTabClient.tsx</files>
  <read_first>
    - src/components/dashboard/KayitlarTabClient.tsx (CURRENT FILE — the existing 7-column table head + body; the photo lightbox onClick that MUST stay unchanged; the existing useTranslations namespace)
    - .planning/phases/08-admin-shell-information-architecture/08-PATTERNS.md (Kayitlar tab additive Details link section — add ONE new TableHead + TableCell after existing columns)
    - .planning/phases/08-admin-shell-information-architecture/08-UI-SPEC.md (Kayitlar Tab Modification section — additive Details link, label Detay/Details, lightbox unchanged)
  </read_first>
  <action>Make the MINIMAL additive change to KayitlarTabClient: add ONE new TableHead as the last column (a Button-less header; use a localized "Details"/"Detay" header — pull from dashboard.admin.records details_header, or reuse the existing namespace if simpler — keep it visible or sr-only per the UI-SPEC; the UI-SPEC names the header "Details"/"Detay"). Add ONE new TableCell per row, as the last cell, containing a ghost Button asChild wrapping a Link to /dashboard/records/[row.id] with label from dashboard.admin.records.details ("Details"/"Detay"). Do NOT modify the existing photo-lightbox onClick, the existing 7 columns, the existing pagination, or any existing behavior — this is purely additive. If the component does not already import next/link, add the import.</action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "KayitlarTabClient.tsx" || echo "no type errors"; grep -q "dashboard/records/" src/components/dashboard/KayitlarTabClient.tsx && grep -q "Dialog" src/components/dashboard/KayitlarTabClient.tsx && echo OK</automated>
  </verify>
  <done>KayitlarTabClient gains a single additive Details column linking each row to /dashboard/records/[id]; the existing photo lightbox (Dialog) and all existing columns/behavior are unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify records list, detail page, and Kayitlar Details link (browser-only)</name>
  <what-built>The cross-project records list, the canonical submission detail page, and the additive Kayitlar Details link. Rendering, pagination, and drill-through are manual-only per VALIDATION.md.</what-built>
  <how-to-verify>
    1. npm run dev, sign in. From the Overview, click a KPI stat (e.g. Pending) → lands on /dashboard/records with the correct status filter.
    2. Confirm the records table shows Status/Worker/Project/Item/Quantity/Submitted/Auditor + a Details action. Apply filters (date/project/person/status) → list re-scopes; URL carries the params.
    3. Use prev/next pagination → 25 rows per page; page indicator updates; Next disabled on the last page.
    4. Click a row Details → detail page shows photo (click → lightbox), all fields, location (Google Maps link + distance/warning if any), rejection reason only when rejected, and an inert "AI analysis will be available..." slot.
    5. Click the back link → returns to /dashboard/records with the prior filters preserved.
    6. Open a project, go to the Kayitlar tab → each row now has a Details link to the same detail page; the existing photo lightbox still opens normally.
    7. Visit /dashboard/records/<bogus-id> → Next.js 404. Toggle TR/EN → labels switch. Confirm /dashboard/records has NO sidebar item.
  </how-to-verify>
  <read_first>
    - .planning/phases/08-admin-shell-information-architecture/08-UI-SPEC.md (Cross-Project Records List; Canonical Submission Detail Page; Kayitlar Tab Modification)
  </read_first>
  <acceptance_criteria>
    - Metric drill-downs land on /dashboard/records with the right status filter; filters re-scope and persist in URL.
    - Pagination shows 25/page with working prev/next and a page indicator.
    - Detail page shows the full record + working photo lightbox + Maps link + rejection-reason-only-when-rejected + inert AI slot.
    - Back link preserves filters; bogus id → 404; records has no sidebar item.
    - Kayitlar tab gains a Details link with the photo lightbox preserved.
    - TR/EN toggle switches all copy.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL params (id, from/to/project/person/status/page) → analytics SQL | UUID + date + enum + int params reach queries |
| Vercel Blob photo URL → next/image | external image source rendered |
| rejection_reason / notes text → JSX | user-influenced text displayed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06-IV | Tampering | submissionId / filter / page params reaching SQL | mitigate | Drizzle sql binds id/filters as parameters (CR-03); page is parsed with parseInt + Math.max; dates validated with isNaN before Date construction |
| T-08-06-XSS | Tampering | rejection_reason / notes display in detail + list | mitigate | React JSX auto-escapes; no dangerouslySetInnerHTML (asserted in Task 1 verify) |
| T-08-06-SSRF | Elevation of Privilege | photo Blob URL via next/image | mitigate | next/image remotePatterns already locked to *.public.blob.vercel-storage.com (next.config.ts) — reuse D-61 component, no new image source |
| T-08-06-TN | Tampering | external Google Maps link | mitigate | rel="noopener noreferrer" + target=_blank (T-05-TN precedent) — asserted in Task 1 verify |
</threat_model>

<verification>
- npx tsc --noEmit clean for the four files
- records/page.tsx contains force-dynamic, getCanonicalSubmissions, Suspense
- records/[id]/page.tsx contains submissionId, notFound
- SubmissionDetailView contains maps.google, ai_slot_body, noopener noreferrer, and NO dangerouslySetInnerHTML
- KayitlarTabClient contains dashboard/records/ and still contains Dialog (lightbox preserved)
- Manual checkpoint (Task 4)
</verification>

<success_criteria>
- UX-05: cross-project filterable+paginated records list reachable from every metric drill-down (no sidebar item, D-74); canonical submission detail page with full record (D-71); all submission references — incl. the project Kayitlar tab — link to the one detail page (D-72).
- Inert AI slot (Phase 6 deferred); security mitigations (escape, noopener, locked image source) in place.
</success_criteria>

<output>
Create `.planning/phases/08-admin-shell-information-architecture/08-06-SUMMARY.md` when done. Record the pagination total-count approach chosen and which coordinate fields were used for the detail-page Maps link.
</output>
