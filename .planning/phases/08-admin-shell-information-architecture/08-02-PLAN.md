---
phase: 08-admin-shell-information-architecture
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/actions/analytics.ts
  - tests/analytics.test.ts
autonomous: true
requirements: [UX-02, UX-03, UX-04, UX-05, PERF-04]
user_setup: []

must_haves:
  truths:
    - "getPortfolioKPIs returns a point-in-time pending backlog plus date-scoped approvals/rejections/active-workers"
    - "getPortfolioTrends returns Istanbul-tz time-bucketed series for throughput, earned value, and rejection counts"
    - "getAuditorDecisions returns a person's approve/reject decision history (NOT office_activity_log)"
    - "getPersonMetrics accepts a dateRange and scopes all sub-queries by it"
    - "getCanonicalSubmissions supports submissionId single-record lookup plus limit/offset pagination and a total count"
  artifacts:
    - path: "src/actions/analytics.ts"
      provides: "getPortfolioKPIs, getPortfolioTrends, getAuditorDecisions; extended getPersonMetrics + getCanonicalSubmissions"
      contains: "export async function getPortfolioKPIs"
    - path: "tests/analytics.test.ts"
      provides: "integration coverage for the new/extended functions"
      contains: "getPortfolioKPIs"
  key_links:
    - from: "getPortfolioKPIs / getPortfolioTrends / getPersonMetrics"
      to: "submissions + boq_items tables"
      via: "Drizzle sql`` parameterized query with tenant scope"
      pattern: "s\\.tenant_id = \\$\\{tenantId\\}"
    - from: "getPortfolioKPIs pending backlog"
      to: "status='pending_audit' count"
      via: "FILTER clause with NO date condition (D-66)"
      pattern: "pending_audit"
---

<objective>
Extend `src/actions/analytics.ts` with the cross-project aggregation queries every Phase 8 surface reads. Add three new functions — `getPortfolioKPIs`, `getPortfolioTrends`, `getAuditorDecisions` — and extend two existing ones — `getPersonMetrics` (add `dateRange`) and `getCanonicalSubmissions` (add `submissionId`, `limit`, `offset`, and a total count for pagination). All money math runs in Postgres; all date bucketing uses Istanbul tz; every function is auth-guarded and tenant-scoped.

Purpose: This is the single source of truth for the Overview, People directory, Person profile, Records list, and Submission detail pages (plans 04/05/06). Building and testing it independently keeps the data contract stable before any UI consumes it.
Output: 3 new + 2 extended functions in `analytics.ts`, new return types, and integration tests in `tests/analytics.test.ts`.

This is a TDD plan: write the failing integration test for each function (RED), implement the SQL (GREEN), then refactor shared fragments (REFACTOR). Commit per cycle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-admin-shell-information-architecture/08-RESEARCH.md
@.planning/phases/08-admin-shell-information-architecture/08-PATTERNS.md

<interfaces>
<!-- Existing analytics.ts contracts the new functions follow. Source: src/actions/analytics.ts -->

Existing exported types (do NOT redefine — extend or add alongside):
- `ProjectMetrics`, `PersonMetrics`, `ProjectSummary`, `ActivityLogEntry`

Existing SubmissionFilters (module-private, ~line 113):
```
type SubmissionFilters = {
  projectIds?: string[];
  from?: Date;
  to?: Date;
  status?: 'pending_audit' | 'approved' | 'rejected';
  personId?: string;
};
```

Existing signatures:
- `getCanonicalSubmissions(filters: SubmissionFilters = {}): Promise<CanonicalSubmission[]>`
- `getPersonMetrics(personId: string, options?: { projectIds?: string[]; asAuditor?: boolean }): Promise<PersonMetrics>`
- `getPortfolioOverview(): Promise<ProjectSummary[]>`

CanonicalSubmission shape (src/lib/types/canonical-submission.ts) — all money/qty fields are STRING:
  id, projectId, projectName, personId, workerName, auditorName, boqItemId, material,
  unit, unitPrice(string|null), currencyCode, quantity(string), earnedValue(string|null),
  status, submittedAt, decidedAt, auditLatencyHours(number|null), locationMatch,
  locationDistanceM(string|null), photoUrl, notes, rejectionReason
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: getPortfolioKPIs + extend getCanonicalSubmissions (RED→GREEN→REFACTOR)</name>
  <files>src/actions/analytics.ts, tests/analytics.test.ts</files>
  <behavior>
    - getPortfolioKPIs(filters): pending backlog = `COUNT(*) FILTER (WHERE status='pending_audit')` with NO date condition (D-66 — point-in-time even when a narrow date range is active). approvals_in_range / rejections_in_range = counts WITH the date condition. active_workers = `COUNT(DISTINCT person_id)` of submitters within range (D-65; all-time when no range). projectIds/personId filters apply to all four where sensible.
    - getPortfolioKPIs throws 'Unauthorized' when auth() returns null.
    - getCanonicalSubmissions({ submissionId }) returns exactly the one matching row (length 1) or [] when not found.
    - getCanonicalSubmissions with { limit, offset } returns at most `limit` rows starting at `offset`, preserving the existing `ORDER BY s.submitted_at DESC`.
    - getCanonicalSubmissions still returns ALL existing fields and existing filters (projectIds/from/to/status/personId) keep working — existing tests stay green.
  </behavior>
  <read_first>
    - src/actions/analytics.ts (getCanonicalSubmissions lines ~109-225 — the SubmissionFilters type, the parameterized `conditions`/`sql.join` WHERE pattern, the SELECT column list, the String()/Number() return coercion; getProjectMetrics lines ~228-330 — the FILTER count pattern and currency-map merge)
    - .planning/phases/08-admin-shell-information-architecture/08-PATTERNS.md (§src/actions/analytics.ts — DATE CONDITION variant, split-query pattern, currency-map merge, numeric coercion, pagination extension)
    - .planning/phases/08-admin-shell-information-architecture/08-RESEARCH.md (Pattern 5, Pitfall 4 pending-backlog-not-date-filtered, Pitfall 5)
    - tests/analytics.test.ts (lines 1-90 — the vi.mock blocks for @/lib/auth, next/cache, next/server; describeIfDb + getTestDb + truncateAllTables usage; sql.raw fixture INSERT pattern with deterministic UUIDs)
    - tests/fixtures/db.ts (describeIfDb, getTestDb, truncateAllTables)
  </read_first>
  <action>RED: add a `describeIfDb('UX-02: getPortfolioKPIs()')` block to tests/analytics.test.ts. Seed (via `sql.raw` fixture inserts, deterministic UUIDs like the existing tests) one tenant, one project, one priced boq_item, then submissions: some pending_audit dated OUTSIDE a narrow date window, some approved/rejected INSIDE the window, and submissions from ≥2 distinct person_ids. Assert: pending backlog counts the out-of-window pending rows (NOT zero) even when passing a narrow `{from,to}` (D-66); approvals/rejections counts reflect only in-window rows; activeWorkers equals the distinct in-window submitter count. Add an auth-null test asserting `rejects.toThrow('Unauthorized')`. Add a `describeIfDb('UX-05: getCanonicalSubmissions pagination + single-record')` block asserting `{ submissionId }` returns the one row and `{ limit, offset }` slices correctly. Run — these fail (functions/params do not exist yet).
GREEN: implement `getPortfolioKPIs(filters: SubmissionFilters = {})` returning a new exported `PortfolioKPIs` type `{ pendingBacklog: number; approvalsInRange: number; rejectionsInRange: number; activeWorkers: number }`. auth() guard + `getDefaultTenantId()` + `WHERE s.tenant_id = ${tenantId}` first. Build a `dateCondition` sql fragment that is empty when from/to absent; apply it ONLY to approvals/rejections/active-workers — never to pending backlog. Coerce counts with `Number(row.x ?? 0)`. Extend `SubmissionFilters` with `submissionId?: string; limit?: number; offset?: number;`; in getCanonicalSubmissions add `if (filters.submissionId) conditions.push(sql\`s.id = ${filters.submissionId}\`)` and append `LIMIT ${filters.limit ?? 1000} OFFSET ${filters.offset ?? 0}` to the query (LIMIT/OFFSET are bound parameters, never string-concatenated). Run — green.
REFACTOR: extract the shared `dateCondition` builder if reused; keep existing tests green. Commit each phase: `test(08-02)…`, `feat(08-02)…`, optional `refactor(08-02)…`.</action>
  <verify>
    <automated>npx vitest run tests/analytics.test.ts</automated>
  </verify>
  <done>getPortfolioKPIs exported and returns pending backlog as point-in-time (D-66), date-scoped approvals/rejections, and D-65 active workers; getCanonicalSubmissions accepts submissionId/limit/offset; all new + existing analytics tests green.</done>
</task>

<task type="tdd">
  <name>Task 2: getPortfolioTrends (Istanbul-tz bucketing) + extend getPersonMetrics with dateRange (RED→GREEN→REFACTOR)</name>
  <files>src/actions/analytics.ts, tests/analytics.test.ts</files>
  <behavior>
    - getPortfolioTrends(filters): returns an array of buckets ordered ASC, each `{ bucket: string; currencyCode: string; approvedCount: number; rejectedCount: number; totalCount: number; earnedValue: string | null }`. Bucketing uses `date_trunc(..., s.submitted_at AT TIME ZONE 'Europe/Istanbul')`; weekly when the active range ≤ 60 days, else monthly. earnedValue = `SUM(quantity::numeric * unit_price::numeric) FILTER (WHERE status='approved' AND unit_price IS NOT NULL)` grouped by currency_code (never cross-currency summed), returned as STRING.
    - getPortfolioTrends throws 'Unauthorized' when auth() is null.
    - getPersonMetrics(personId, { dateRange }) scopes ALL sub-queries by `submitted_at >= from AND < to`; without dateRange it returns the same all-time numbers as before (existing tests stay green).
  </behavior>
  <read_first>
    - src/actions/analytics.ts (getPersonMetrics lines ~436-560 — the 4 sub-queries, the existing `projectFilter` sql fragment pattern, the currency-grouped value_contributed query, the NULL-decidedAt split-query rule)
    - .planning/phases/08-admin-shell-information-architecture/08-PATTERNS.md (§src/actions/analytics.ts — Istanbul timezone bucketing fragments, getPersonMetrics dateRange extension snippet, currency-map merge, numeric coercion)
    - .planning/phases/08-admin-shell-information-architecture/08-RESEARCH.md (Pattern 6 trend bucketing, Pattern 7 dateRange extension, Pitfall 6, Pitfall 8 recharts-server-bundle note)
    - tests/analytics.test.ts (existing getPersonMetrics test block — extend, do not replace)
  </read_first>
  <action>RED: add `describeIfDb('UX-04: getPortfolioTrends()')` — seed approved + rejected submissions across two different months (use submitted_at dates ~2 months apart, priced boq_item) and assert the function returns ≥2 monthly buckets ordered ASC with correct approvedCount/rejectedCount and a non-null earnedValue string for the bucket containing approved rows. Add a weekly-vs-monthly assertion: pass a `{from,to}` window ≤60 days apart and assert finer (weekly) buckets are produced. Add an auth-null test. Extend the existing getPersonMetrics describe with a dateRange case: seed two submissions in different months, assert metrics with a narrow `{ dateRange }` count only the in-range one, and that calling WITHOUT dateRange counts both (proves backward-compatibility). Run — fails.
GREEN: implement `getPortfolioTrends(filters: SubmissionFilters = {}): Promise<TrendPoint[]>` (export a `TrendPoint` type). auth() + tenant scope + parameterized WHERE (reuse the conditions/sql.join pattern). Choose bucket granularity in TS from the from/to delta (≤60 days → `date_trunc('week', ...)`, else `date_trunc('month', ...)`), both wrapped `AT TIME ZONE 'Europe/Istanbul'`. JOIN boq_items for currency_code + unit_price; GROUP BY bucket, currency_code; ORDER BY bucket ASC. Return bucket as ISO/`String()`, counts via `Number()`, earnedValue via `String()` or null. Extend getPersonMetrics signature options with `dateRange?: { from: Date; to: Date }`; build a `dateConditions` sql fragment (empty when absent) and append it to the WHERE clause of all four sub-queries (same approach as the existing `projectFilter`). Run — green; existing getPersonMetrics tests still pass.
REFACTOR: deduplicate the date-condition fragment with Task 1's if practical. Commit per RED/GREEN/REFACTOR phase with `08-02` scope.</action>
  <verify>
    <automated>npx vitest run tests/analytics.test.ts</automated>
  </verify>
  <done>getPortfolioTrends exported with Istanbul-tz weekly/monthly bucketing and currency-grouped earned value; getPersonMetrics accepts dateRange and scopes all sub-queries; backward-compatible all-time path unchanged; all analytics tests green.</done>
</task>

<task type="tdd">
  <name>Task 3: getAuditorDecisions slim query (auditor timeline source — NOT office_activity_log) (RED→GREEN)</name>
  <files>src/actions/analytics.ts, tests/analytics.test.ts</files>
  <behavior>
    - getAuditorDecisions({ personId, dateRange?, projectIds?, limit?, offset? }): returns decisions where `s.decided_by = personId AND s.status IN ('approved','rejected')`, joined to people (worker name), boq_items (material/unit), projects (name). Each row carries: submissionId, status, material, unit, quantity(string), workerName, projectName, decidedAt(string), auditLatencyHours(number|null). Ordered by decided_at DESC. Respects dateRange (on submitted_at), projectIds, limit/offset.
    - It MUST NOT read office_activity_log (that table is office-engineer-only — Pitfall 7).
    - Throws 'Unauthorized' when auth() is null.
  </behavior>
  <read_first>
    - src/actions/analytics.ts (getCanonicalSubmissions JOIN structure for worker/auditor/boq/project names; getPersonMetrics auditor sub-query for the `decided_by = personId` + status IN pattern + latency EXTRACT(EPOCH ...)/3600 expression)
    - .planning/phases/08-admin-shell-information-architecture/08-PATTERNS.md (§src/actions/analytics.ts — Pattern 8 getAuditorDecisions; split-query/latency pattern)
    - .planning/phases/08-admin-shell-information-architecture/08-RESEARCH.md (Pattern 8, Pitfall 7 auditor-timeline-not-office-log)
  </read_first>
  <action>RED: add `describeIfDb('PERF-04: getAuditorDecisions()')`. Seed two people (a worker submitter and an auditor), a project, a priced boq_item, and submissions where `decided_by` = the auditor's id with mixed approved/rejected statuses plus one pending_audit row (which must be EXCLUDED). Assert: returned rows are only the approved+rejected decisions by that auditor; each carries workerName/material/quantity/decidedAt; ordered decided_at DESC; the pending row is absent. Add an auth-null test. Grep-style assert in the test or SUMMARY that the function body contains no reference to `office_activity_log`. Run — fails.
GREEN: implement `getAuditorDecisions(options)` returning a new exported `AuditorDecision[]` type. auth() + tenant scope + `WHERE s.decided_by = ${personId} AND s.status IN ('approved','rejected') AND s.tenant_id = ${tenantId}`, plus optional parameterized dateRange (on submitted_at) and projectIds fragments, plus `LIMIT/OFFSET` bound params. JOIN people (worker via s.person_id), boq_items, projects. Compute auditLatencyHours with the EXTRACT(EPOCH ...)/3600 expression (null-safe). Coerce strings/numbers per the established pattern. Run — green.</action>
  <verify>
    <automated>npx vitest run tests/analytics.test.ts && ! grep -n "office_activity_log" src/actions/analytics.ts | grep -i "getAuditorDecisions" ; echo "auditor-timeline-source-check done"</automated>
  </verify>
  <done>getAuditorDecisions exported; returns approve/reject decisions by personId only (pending excluded), ordered DESC, with worker/material/quantity/decidedAt/latency; does not read office_activity_log; auth-guarded + tenant-scoped; all analytics tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Page searchParams → analytics function args | URL-supplied `from/to/project/person/status` (dates, UUIDs, enum) reach SQL |
| analytics function → Postgres (Neon) | Query execution against tenant-shared tables |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-IV | Tampering | UUID/enum/date params reaching SQL in all new functions | mitigate | Drizzle `sql\`\`` template literals bind every caller value as a parameter (CR-03); arrays via `= ANY(${arr})`; LIMIT/OFFSET as bound params — never string concat |
| T-08-02-ID | Information Disclosure | cross-tenant data leak via filters | mitigate | Every new function starts with `auth()` guard + `const tenantId = getDefaultTenantId()` and `WHERE s.tenant_id = ${tenantId}` as the first condition |
| T-08-02-AV | Tampering | NULL decidedAt poisoning latency averages | mitigate | Split-query rule: latency via `... FILTER (WHERE decided_at IS NOT NULL)`, pending counted separately; never mixed |
| T-08-02-MONEY | Tampering | money math drift | mitigate | All EV computed in Postgres `SUM(quantity::numeric * unit_price::numeric)`; returned as STRING; never parseFloat in JS; currency-grouped, never cross-currency summed |
</threat_model>

<verification>
- `npx vitest run tests/analytics.test.ts` — all new + existing analytics tests green
- `grep -c "auth()" src/actions/analytics.ts` increases (each new function has its guard)
- New functions each contain `WHERE s.tenant_id = ${tenantId}` (tenant scope)
</verification>

<success_criteria>
- getPortfolioKPIs, getPortfolioTrends, getAuditorDecisions exist, exported, typed, tested.
- getPersonMetrics accepts dateRange; getCanonicalSubmissions accepts submissionId/limit/offset.
- Pending backlog is point-in-time (D-66); active workers per D-65; money in Postgres; Istanbul-tz bucketing; auth + tenant scope on every function.
</success_criteria>

<output>
Create `.planning/phases/08-admin-shell-information-architecture/08-02-SUMMARY.md` when done. Record the EXACT names + field shapes of `PortfolioKPIs`, `TrendPoint`, and `AuditorDecision` (downstream plans 04/05 import these types).
</output>
