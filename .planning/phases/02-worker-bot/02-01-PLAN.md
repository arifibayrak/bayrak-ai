---
phase: 02-worker-bot
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema/conversation-state.ts
  - src/db/schema/processed-updates.ts
  - src/db/schema/submissions.ts
  - src/db/schema/index.ts
  - tests/fixtures/db.ts
  - tests/telegram-bot.test.ts
autonomous: true
requirements: [LOG-08, LOG-10]
must_haves:
  truths:
    - "Three new Drizzle tables (conversation_state, processed_updates, submissions) are defined in source with the constraints D-13 requires"
    - "The submissions table has a named UNIQUE constraint on flow_id (D-13 Guard 2)"
    - "processed_updates has update_id as PRIMARY KEY (D-13 Guard 1)"
    - "The test suite can run (Wave 0 scaffold exists) without a live DB and skips DB tests cleanly"
  artifacts:
    - path: "src/db/schema/conversation-state.ts"
      provides: "conversation_state table: telegramUserId unique, flowId, currentStep, data jsonb, updatedAt"
      contains: "pgTable('conversation_state'"
    - path: "src/db/schema/processed-updates.ts"
      provides: "processed_updates dedup table keyed on update_id"
      contains: "pgTable('processed_updates'"
    - path: "src/db/schema/submissions.ts"
      provides: "submissions table with status pending_audit default + flow_id unique"
      contains: "submissions_flow_id_unique"
    - path: "tests/telegram-bot.test.ts"
      provides: "Wave 0 test scaffold for all LOG-* requirements"
      min_lines: 30
  key_links:
    - from: "src/db/schema/index.ts"
      to: "conversation-state, processed-updates, submissions"
      via: "barrel re-export"
      pattern: "export \\* from './submissions'"
    - from: "tests/fixtures/db.ts"
      to: "truncateAllTables"
      via: "three new tables in FK-safe order"
      pattern: "submissions"
---

<objective>
Author the three new Drizzle schema files that Phase 2 requires — `conversation_state` (D-12 FSM state), `processed_updates` (D-13 Guard 1 dedup fence), and `submissions` (LOG-08 + D-13 Guard 2 flow_id unique) — register them in the schema barrel, extend the test truncation helper for FK-safe teardown, and lay down the Wave 0 test scaffold file `tests/telegram-bot.test.ts` so every later wave has an automated verification target.

Purpose: All Phase 2 handlers, the idempotency middleware, and the submission insert depend on these tables existing in source. This is the foundation plan — it produces the schema source but does NOT push it to the live DB (that is the [BLOCKING] task in Plan 03).
Output: 3 new schema files, extended barrel, extended truncateAllTables, and a runnable (mostly stubbed) test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-worker-bot/02-CONTEXT.md
@.planning/phases/02-worker-bot/02-RESEARCH.md
@.planning/phases/02-worker-bot/02-PATTERNS.md

<interfaces>
<!-- Schema conventions the executor must replicate. Extracted from live codebase. -->

From src/db/schema/pending-people.ts (analog for conversation_state + processed_updates):
- pgTable('pending_people', { id: uuid().primaryKey().defaultRandom(), tenantId: uuid('tenant_id').references(() => tenants.id), telegramUserId: bigint('telegram_user_id', { mode: 'bigint' }).notNull().unique(), telegramName: text(...), startedAt: timestamp(..., { withTimezone: true }).defaultNow().notNull() }, (t) => [ index('pending_people_telegram_idx').on(t.telegramUserId) ])

From src/db/schema/boq-items.ts (numeric column analog for submissions):
- plannedQty: numeric('planned_qty', { precision: 12, scale: 3 }).notNull()

From src/db/schema/routes.ts (geometry + GiST index analog for submissions.location):
- geom: geomLinestring('geom').notNull(); index('routes_geom_gist').using('gist', t.geom)

From src/db/schema/assignments.ts (named unique + text enum analog):
- roleOnProject: text('role_on_project', { enum: ['worker', 'auditor'] }).notNull()
- unique('unique_person_project_role').on(t.personId, t.projectId, t.roleOnProject)

From src/db/schema/index.ts (current barrel — append after assignments):
- export * from './tenants' ... './assignments'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author conversation_state and processed_updates schema files</name>
  <read_first>
    - src/db/schema/pending-people.ts (exact analog: uuid PK, tenantId FK, bigint telegramUserId unique, timestamps, indexes)
    - src/db/schema/people.ts (FK target for personId reference)
    - .planning/phases/02-worker-bot/02-PATTERNS.md lines 268-338 (conversation-state + processed-updates pattern assignments)
    - .planning/phases/02-worker-bot/02-RESEARCH.md lines 636-675 (schema design for both tables, D-22 TTL note)
  </read_first>
  <files>src/db/schema/conversation-state.ts, src/db/schema/processed-updates.ts</files>
  <action>
    Create src/db/schema/conversation-state.ts with pgTable('conversation_state') containing: id uuid PK defaultRandom; tenantId uuid('tenant_id') nullable references tenants.id (D-09 pattern); telegramUserId bigint('telegram_user_id', { mode: 'bigint' }).notNull().unique() (one active flow per worker); personId uuid('person_id').notNull() references people.id; flowId uuid('flow_id').notNull().defaultRandom() (this is the natural key carried to submissions for D-13 Guard 2); currentStep text('current_step').notNull(); data jsonb('data').notNull().default('{}'); updatedAt timestamp('updated_at', { withTimezone: true }).defaultNow().notNull(). Use updatedAt (NOT startedAt) because the D-22 TTL check reads this column and handlers must bump it on every state write. Add an index on telegram_user_id. Import jsonb from drizzle-orm/pg-core.

    Create src/db/schema/processed-updates.ts with pgTable('processed_updates') containing exactly: updateId bigint('update_id', { mode: 'bigint' }).primaryKey() and processedAt timestamp('processed_at', { withTimezone: true }).defaultNow().notNull(). This table intentionally has NO uuid PK and NO tenantId — update_id IS the natural primary key (the dedup fence applies before tenant context is resolved, D-13 Guard 1). Import only { pgTable, bigint, timestamp } from drizzle-orm/pg-core. No secondary indexes.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "conversation-state|processed-updates" | grep -v "^#" | grep -c error; test $? -eq 0 || true; npx tsc --noEmit 2>&1 | grep -cE "conversation-state\.ts|processed-updates\.ts"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `src/db/schema/conversation-state.ts` contains `pgTable('conversation_state'` and `telegramUserId` declared `.notNull().unique()`
    - Source: `src/db/schema/conversation-state.ts` contains `flowId` with `uuid('flow_id').notNull().defaultRandom()` and `data` declared as `jsonb('data')`
    - Source: `src/db/schema/conversation-state.ts` uses `updatedAt` column named `'updated_at'` (NOT `started_at`)
    - Source: `src/db/schema/processed-updates.ts` contains `updateId` declared `bigint('update_id', { mode: 'bigint' }).primaryKey()` and imports neither `uuid` nor `tenants`
    - Behavior: `npx tsc --noEmit` reports zero TypeScript errors originating in either new file
  </acceptance_criteria>
  <done>Both schema files exist, compile clean, and match the pending-people analog structure with the documented column set.</done>
</task>

<task type="auto">
  <name>Task 2: Author submissions schema file + register all three in barrel</name>
  <read_first>
    - src/db/schema/boq-items.ts (numeric column precision/scale pattern, tenantId nullable, project FK)
    - src/db/schema/routes.ts lines 22-33 (geometry column + GiST index .using('gist', ...) pattern)
    - src/db/schema/assignments.ts lines 13-19 (text enum column + named unique constraint pattern)
    - src/db/schema/index.ts (barrel — append new exports in FK dependency order)
    - .planning/phases/02-worker-bot/02-PATTERNS.md lines 342-427 (submissions + barrel pattern assignments)
    - .planning/phases/02-worker-bot/02-RESEARCH.md lines 677-717 (submissions schema design, A4 geometry note)
  </read_first>
  <files>src/db/schema/submissions.ts, src/db/schema/index.ts</files>
  <action>
    Create src/db/schema/submissions.ts with pgTable('submissions'): id uuid PK defaultRandom; tenantId uuid('tenant_id') nullable references tenants.id; flowId uuid('flow_id').notNull() (ties to conversation_state.flow_id, D-13 Guard 2); personId uuid('person_id').notNull() references people.id; projectId uuid('project_id').notNull() references projects.id; boqItemId uuid('boq_item_id').notNull() references boqItems.id; photoUrl text('photo_url').notNull() (Vercel Blob URL); photoFileId text('photo_file_id') nullable (Telegram file_id reference); location geometry('location', { type: 'point', mode: 'xy', srid: 4326 }) nullable (Phase 4 PostGIS compatibility per A4 — geometry column + ::geography cast later, no migration needed); locationLat numeric('location_lat', { precision: 10, scale: 7 }); locationLon numeric('location_lon', { precision: 10, scale: 7 }); quantity numeric('quantity', { precision: 12, scale: 3 }).notNull(); notes text('notes') nullable; status text('status', { enum: ['pending_audit', 'approved', 'rejected'] }).notNull().default('pending_audit') (LOG-08); submittedAt timestamp('submitted_at', { withTimezone: true }).defaultNow().notNull(). Table-level constraints array: unique('submissions_flow_id_unique').on(t.flowId) (D-13 Guard 2 — name it explicitly so the migration SQL is readable); index('submissions_project_idx').on(t.projectId); index('submissions_person_idx').on(t.personId); index('submissions_status_idx').on(t.status); index('submissions_location_gist').using('gist', t.location) (Phase 4 ready). Import geometry from drizzle-orm/pg-core.

    Then append to src/db/schema/index.ts these three exports in FK dependency order (referenced tables first): export * from './conversation-state'; export * from './processed-updates'; export * from './submissions'.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -cE "submissions\.ts|schema/index\.ts"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `src/db/schema/submissions.ts` contains `unique('submissions_flow_id_unique').on(t.flowId)`
    - Source: `src/db/schema/submissions.ts` contains `status` with `enum: ['pending_audit', 'approved', 'rejected']` and `.default('pending_audit')`
    - Source: `src/db/schema/submissions.ts` contains `geometry('location'` and `index('submissions_location_gist').using('gist', t.location)`
    - Source: `src/db/schema/index.ts` ends with `export * from './submissions'` and includes the conversation-state + processed-updates exports
    - Behavior: `npx tsc --noEmit` reports zero TypeScript errors originating in submissions.ts or schema/index.ts
  </acceptance_criteria>
  <done>submissions.ts exists with the flow_id unique constraint, status enum, and GiST-indexed geometry column; barrel re-exports all three new tables.</done>
</task>

<task type="auto">
  <name>Task 3: Extend truncateAllTables + create Wave 0 test scaffold</name>
  <read_first>
    - tests/fixtures/db.ts (truncateAllTables — append three new tables in FK-safe order)
    - tests/telegram-webhook.test.ts (exact analog for the new test file: imports, beforeEach env setup, bot.botInfo setter, api.config.use transformer, handleUpdate shape, describeIfDb)
    - .planning/phases/02-worker-bot/02-PATTERNS.md lines 431-545 (test file pattern assignments + truncateAllTables extension)
    - .planning/phases/02-worker-bot/02-RESEARCH.md lines 732-759 (Phase requirements to test map + Wave 0 gaps)
  </read_first>
  <files>tests/fixtures/db.ts, tests/telegram-bot.test.ts</files>
  <action>
    In tests/fixtures/db.ts, add the three new tables to the START of the `tables` array in truncateAllTables (most dependent first, FK-safe): "submissions", "conversation_state", "processed_updates" — placed before "assignments". submissions has FKs to people/projects/boq_items; conversation_state has FKs to people/tenants; processed_updates has none. The existing TRUNCATE ... CASCADE handles ordering, but list them dependent-first per the established convention.

    Create tests/telegram-bot.test.ts as the Wave 0 scaffold. Copy the harness pattern from tests/telegram-webhook.test.ts exactly: import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest'; import { describeIfDb, getTestDb, truncateAllTables } from './fixtures/db'. Set up the beforeEach env block (TELEGRAM_BOT_TOKEN, TELEGRAM_WEBHOOK_SECRET, vi.resetModules, vi.doMock('@/db')) and afterEach cleanup. Include the bot.botInfo setter block and the bot.api.config.use transformer pattern in a helper so later waves reuse them. Add three describe groups with at least one passing placeholder test each so the file runs green now: (a) a pure unit test group 'Turkish decimal normalization' with a real assertion expect(parseFloat('25,5'.replace(',', '.'))).toBe(25.5) (this is a genuine Pitfall-4 guard, keep it permanently); (b) a unit group 'step input enforcement' with it.todo() placeholders for LOG-04/LOG-05/LOG-06 reject cases (filled in Wave 4); (c) a describeIfDb group 'submission persistence & idempotency (SC4)' with it.todo() placeholders for LOG-08 and the duplicate-update test (filled in Wave 5). The file MUST exit 0 today.
  </action>
  <verify>
    <automated>npx vitest run tests/telegram-bot.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source: `tests/fixtures/db.ts` `tables` array contains the strings `"submissions"`, `"conversation_state"`, and `"processed_updates"` ahead of `"assignments"`
    - Source: `tests/telegram-bot.test.ts` contains a real (non-todo) assertion `expect(parseFloat('25,5'.replace(',', '.'))).toBe(25.5)`
    - Source: `tests/telegram-bot.test.ts` imports `describeIfDb` from `./fixtures/db` and contains a `describeIfDb(` block
    - Behavior: `npx vitest run tests/telegram-bot.test.ts` exits 0 (todo tests do not fail the run)
  </acceptance_criteria>
  <done>truncateAllTables covers all three new tables; the test scaffold runs green with the Turkish-decimal guard passing and todo placeholders staged for later waves.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram → webhook → DB | Untrusted update payloads ultimately drive inserts into the three new tables |
| Schema source → live Neon DB | Schema defined here is the contract; the live push (Plan 03) is where it becomes enforceable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | submissions row duplication on update replay | mitigate | DB-level `unique('submissions_flow_id_unique')` constraint defined in this plan (D-13 Guard 2); app-level checks are TOCTOU-unsafe (RESEARCH Pitfall 7) |
| T-02-02 | Tampering | duplicate update_id processed twice | mitigate | `processed_updates` PRIMARY KEY on update_id defined here (D-13 Guard 1); INSERT ON CONFLICT DO NOTHING enforced in Plan 04 |
| T-02-03 | Information disclosure | cross-worker conversation_state leakage | mitigate | `telegram_user_id` UNIQUE on conversation_state — one row per worker, keyed by authenticated Telegram identity; no shared state |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — all deps installed and audited in Phase 1 (RESEARCH Package Legitimacy Audit). No new supply-chain surface. |
</threat_model>

<verification>
- `npx tsc --noEmit` reports zero new errors
- `npx vitest run tests/telegram-bot.test.ts` exits 0
- `src/db/schema/index.ts` re-exports all three new tables
- The submissions UNIQUE(flow_id) constraint is named `submissions_flow_id_unique`
</verification>

<success_criteria>
- Three schema files compile and follow the verified codebase analogs
- D-13 Guard 1 (processed_updates PK) and Guard 2 (submissions flow_id unique) are expressed in source
- truncateAllTables is FK-safe for the new tables
- The Wave 0 test file is the durable automated verification target for all later waves
</success_criteria>

<output>
Create `.planning/phases/02-worker-bot/02-01-SUMMARY.md` when done
</output>
