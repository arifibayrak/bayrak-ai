---
phase: 02-worker-bot
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/db/migrations/meta/_journal.json
autonomous: false
requirements: [LOG-08, LOG-10]
must_haves:
  truths:
    - "The three new tables physically exist in the live Neon database after the push"
    - "The submissions_flow_id_unique constraint and processed_updates PK are enforced at the DB level, not just in source"
    - "A live-DB integration test can insert and read back a submissions row"
  artifacts:
    - path: "src/db/migrations/meta/_journal.json"
      provides: "updated migration journal reflecting the pushed schema state"
  key_links:
    - from: "src/db/schema/*.ts"
      to: "live Neon database"
      via: "drizzle-kit push"
      pattern: "conversation_state|processed_updates|submissions"
---

<objective>
[BLOCKING] Push the three new Drizzle tables (`conversation_state`, `processed_updates`, `submissions`) to the live Neon database via `drizzle-kit push`. This MUST run AFTER Plan 01's schema files are complete and BEFORE any verification that relies on the live DB (the SC4 idempotency test and SC3 persistence test in Plan 06).

Purpose: Build and `tsc` pass on the schema SOURCE alone (Drizzle types come from source, not the live DB) — so without this push the phase reaches a FALSE-POSITIVE verification state where code compiles but every live-DB insert fails. RESEARCH.md explicitly flags this. This plan closes that gap.
Output: Live DB schema synced; the three tables and their constraints exist in Neon.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-worker-bot/02-RESEARCH.md
@drizzle.config.ts

<interfaces>
<!-- drizzle.config.ts (verified): schema glob ./src/db/schema/*.ts, dialect postgresql,
     out ./src/db/migrations, credential process.env.DATABASE_URL.
     Project uses npm — no db:push script; call drizzle-kit directly via the local bin. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Push new schema to live Neon DB</name>
  <read_first>
    - drizzle.config.ts (schema glob, dialect, DATABASE_URL credential)
    - src/db/schema/conversation-state.ts (must exist from Plan 01 before pushing)
    - src/db/schema/processed-updates.ts (must exist from Plan 01)
    - src/db/schema/submissions.ts (must exist from Plan 01)
    - .planning/phases/02-worker-bot/02-RESEARCH.md lines 752-759 (Wave 0 gap: schema push task) + lines 632-717 (the exact schema being pushed)
  </read_first>
  <what-built>
    Plan 01 authored three new schema files. Drizzle types compile from source, so `npm run build` and `npx tsc --noEmit` are GREEN whether or not the live DB has these tables — a false-positive trap. This task syncs the live Neon DB to match the source.
  </what-built>
  <action>
    Confirm DATABASE_URL is set in the environment (.env.local for local execution). Run the push using the local drizzle-kit binary (this project uses npm and has NO db:push script — call drizzle-kit directly): `node_modules/.bin/drizzle-kit push` (equivalently the package runner form `npx drizzle-kit push` if the shell resolves it to the local binary). drizzle-kit reads ./src/db/schema/*.ts and applies the diff to DATABASE_URL. If drizzle-kit prompts an interactive confirmation (e.g. "Is X table created or renamed?") choose the CREATE option for each of conversation_state, processed_updates, submissions — these are brand-new tables, never renames. If the environment cannot answer interactive prompts, this is why the plan is flagged autonomous: false — the developer answers the prompts. Do NOT use `drizzle-kit generate` + manual migration edit here (no LineString hand-edit is needed; submissions.location is a POINT geometry which drizzle-kit emits correctly, unlike the routes LineString case). After push, the three tables must exist with: submissions.status default 'pending_audit', the named constraint submissions_flow_id_unique, processed_updates PRIMARY KEY on update_id, and the submissions_location_gist GiST index.
  </action>
  <how-to-verify>
    1. Run `node_modules/.bin/drizzle-kit push` and confirm it reports the three tables created (or "No changes" on a re-run after a successful first push).
    2. Verify the tables exist live — run a psql/Neon query or a one-off node script asserting the tables and the named unique constraint are present, e.g. query information_schema.table_constraints for `submissions_flow_id_unique` and information_schema.tables for the three table names.
    3. Confirm `submissions` has a column `status` whose default is `pending_audit`.
  </how-to-verify>
  <verify>
    <automated>node_modules/.bin/drizzle-kit push 2>&1 | grep -viE "warning" | grep -ciE "changes applied|No changes|conversation_state|processed_updates|submissions"</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `node_modules/.bin/drizzle-kit push` exits 0 and reports either the three tables created or "No changes detected" on a clean re-run
    - Behavior: a SQL query against the live DB returns rows for all three table names in `information_schema.tables`
    - Behavior: `information_schema.table_constraints` contains a constraint named `submissions_flow_id_unique` of type UNIQUE
    - Behavior: inserting a `submissions` row without specifying `status` yields `status = 'pending_audit'`
  </acceptance_criteria>
  <resume-signal>Type "pushed" once drizzle-kit reports the three tables created and the constraint check passes, or describe the prompt/error encountered.</resume-signal>
  <done>conversation_state, processed_updates, and submissions exist in the live Neon DB with their constraints; the false-positive verification trap is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local/dev environment → live Neon DB | drizzle-kit push applies a schema diff with the DATABASE_URL credential |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | destructive schema diff (accidental DROP) on push | mitigate | These are purely additive new tables; the human checkpoint (autonomous: false) requires the developer to read any interactive prompt and reject any rename/drop suggestion — only CREATE is correct |
| T-02-08 | Elevation of privilege | DATABASE_URL credential exposure | accept | Credential is read from env (.env.local / Vercel env), never hardcoded; same credential already used by Phase 1 migrations |
| T-02-SC | Tampering | npm installs | accept | No installs — drizzle-kit already a devDependency (0.31.10) from Phase 1 |
</threat_model>

<verification>
- `node_modules/.bin/drizzle-kit push` exits 0
- The three tables exist in the live DB with their constraints
- A subsequent `npx vitest run tests/telegram-bot.test.ts` describeIfDb block (once filled in Plan 06) can insert and read a submissions row
</verification>

<success_criteria>
- Live Neon schema matches the Phase 2 source schema
- D-13 Guard 2 (flow_id unique) and Guard 1 (update_id PK) are DB-enforced
- The false-positive verification trap (build green / DB empty) is eliminated
</success_criteria>

<output>
Create `.planning/phases/02-worker-bot/02-03-SUMMARY.md` when done
</output>
