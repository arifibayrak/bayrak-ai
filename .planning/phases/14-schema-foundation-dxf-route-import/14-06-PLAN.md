---
phase: 14-schema-foundation-dxf-route-import
plan: 06
type: execute
wave: 5
depends_on: ["14-05"]
files_modified:
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [RTE-01, RTE-02, RTE-03, RTE-04, RTE-05]

must_haves:
  truths:
    - "The 13 v1 core-loop capabilities in PROJECT.md move from Active to Validated with phase references"
    - "The AI assist capability stays in Active (Phase 16 owns it)"
    - "REQUIREMENTS.md RTE-01..05 status flips to Done/Complete reflecting Phase 14 delivery"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Reconciled Requirements ledger (Validated vs Active)"
      contains: "Validated"
  key_links:
    - from: ".planning/PROJECT.md"
      to: ".planning/ROADMAP.md"
      via: "phase references on each newly-validated capability"
      pattern: "Validated in Phase"
---

<objective>
Close the SC6 bookkeeping reconciliation: move the already-built v1 core-loop capabilities (AUTH-01..04, SETUP-01..04, LOG-01..10, AUDIT-01..06, GEO-01..02, DASH-01..05, I18N-01..02) from PROJECT.md "Active" to "Validated" with their phase references, leaving only the genuinely-pending AI assist capability in Active (Phase 16). Flip RTE-01..05 to Done in REQUIREMENTS.md. This is a documentation-integrity task with no code.

Purpose: PROJECT.md's Active list currently lists shipped v1 capabilities as if pending — a false signal for planners. SC6 of the phase goal requires this reconciliation. It also gates the phase: the Active/Validated split must be honest before `/gsd:verify-work`.

Output: reconciled PROJECT.md Requirements section + RTE status flips in REQUIREMENTS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-schema-foundation-dxf-route-import/14-CONTEXT.md
@.planning/phases/14-schema-foundation-dxf-route-import/14-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile PROJECT.md Active→Validated + flip RTE status in REQUIREMENTS.md (SC6)</name>
  <read_first>
    - .planning/PROJECT.md "## Requirements" — "### Validated" (lines ~45-54) + "### Active" (lines ~56-70). The 13 Active bullets to migrate are the worker-bot/identity/project-select/input-enforce/persist+ping/auditor-notify/approve/reject/office-CRUD/geojson-upload/magic-link/i18n lines (lines 58-68 + line 70). Line 69 (AI assist vision/LLM flags) STAYS Active.
    - ROADMAP.md Phase 1-5 entries (for the phase references each migrated capability cites: AUTH/SETUP/I18N-02 → Phase 1; LOG/I18N-01 → Phase 2; AUDIT → Phase 3; GEO → Phase 4; DASH → Phase 5)
    - .planning/REQUIREMENTS.md lines 139-143 (RTE-01..05 checkboxes) + lines 271-275 (RTE status table Pending→Done) + the Housekeeping line 166
    - STATE.md "Phase 14 Key Constraints" bullet listing the exact REQ-ID set to move
  </read_first>
  <action>
    In `.planning/PROJECT.md`: move the 13 shipped v1 core-loop capability bullets from "### Active" into "### Validated", each rewritten as `- [x] <capability> — *Validated in Phase N: <name> (<REQ-IDs>).*` using the correct phase reference (Phase 1 for AUTH-01..04/SETUP-01..04/I18N-02; Phase 2 for LOG-01..10/I18N-01; Phase 3 for AUDIT-01..06; Phase 4 for GEO-01..02; Phase 5 for DASH-01..05). Leave ONLY the AI assist bullet ("AI assist (vision/LLM) flags photo/location anomalies...") in "### Active" with a note that it is Phase 16 scope. Do NOT alter the existing v2/v3 Validated bullets (lines 47-54). In `.planning/REQUIREMENTS.md`: change the RTE-01..05 checkboxes (lines ~139-143) from `[ ]` to `[x]` and the RTE rows in the status table (lines ~271-275) from `Pending` to `Done` (or the table's existing done token); mark the Housekeeping "Bookkeeping reconciliation" item (line ~166) complete. Keep all wording factual — reference the phase that actually delivered each capability; do not invent new phase numbers.
  </action>
  <verify>
    <automated>grep -c "Validated in Phase" .planning/PROJECT.md && grep -c "AI assist" .planning/PROJECT.md</automated>
  </verify>
  <done>PROJECT.md Validated section gains the 13 v1 capabilities with phase refs; AI assist remains the sole pending v1 Active bullet; REQUIREMENTS.md RTE-01..05 marked Done + Housekeeping reconciliation marked complete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Reconciled the PROJECT.md requirements ledger (13 v1 capabilities moved Active→Validated with phase references, AI assist left in Active for Phase 16) and flipped RTE-01..05 to Done in REQUIREMENTS.md.</what-built>
  <how-to-verify>
    1. Open .planning/PROJECT.md "## Requirements" — confirm the Active list now contains only the AI assist (vision/LLM) capability, and every v1 core-loop capability appears under Validated with a "Validated in Phase N" reference.
    2. Open .planning/REQUIREMENTS.md — confirm RTE-01..05 are checked and the status table reads Done, and the Housekeeping reconciliation item is marked complete.
    3. Confirm no v2/v3 Validated bullets were altered.
  </how-to-verify>
  <resume-signal>Type "approved" if the ledger is honest and complete; otherwise describe what is mis-categorized.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning ledger → future planners | mis-categorized capabilities mislead scope decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-DOC | Repudiation | PROJECT.md requirements ledger | mitigate | Each migrated capability carries an explicit "Validated in Phase N" reference; human-verify checkpoint confirms the split is honest before phase verification |
</threat_model>

<verification>
- PROJECT.md Active contains only the AI assist capability
- All 13 v1 capabilities under Validated with phase references
- REQUIREMENTS.md RTE-01..05 = Done; Housekeeping reconciliation complete
- v2/v3 Validated bullets unchanged
</verification>

<success_criteria>
- SC6 reconciliation complete and human-approved
- No code changes; documentation integrity restored
</success_criteria>

<output>
Create `.planning/phases/14-schema-foundation-dxf-route-import/14-06-SUMMARY.md` when done
</output>
