---
phase: 14-schema-foundation-dxf-route-import
plan: 03
type: execute
wave: 2
depends_on: ["14-02"]
files_modified:
  - package.json
requirements: [RTE-03, RTE-04, RTE-05]

must_haves:
  truths:
    - "Migration 0010 + 0011 + 0012 are applied to the dev branch (neondb via DATABASE_URL)"
    - "Migration 0010 + 0011 + 0012 are applied to the test branch (neondb_test via TEST_DATABASE_URL)"
    - "A direct DB check confirms routes.geometry_version, submissions.chainage_m, submission_ai_flags, and route_source_documents exist on both branches"
    - "The full vitest suite (which truncates submission_ai_flags and route_source_documents) runs without 'relation does not exist' errors"
  artifacts:
    - path: "package.json"
      provides: "migrate:test convenience script (optional) for the test-branch apply"
      contains: "migrate"
  key_links:
    - from: "src/db/migrate.ts"
      to: "src/db/migrations/0010_v4_routes_ext.sql"
      via: "migrate() reads the migrations folder journal"
      pattern: "migrationsFolder"
---

<objective>
[BLOCKING] Apply migrations 0010 + 0011 + 0012 to BOTH Neon branches (dev `neondb` and test `neondb_test`) using `npx tsx src/db/migrate.ts` — the project migration runner (drizzle-kit push is unusable, D-49). This closes the false-positive verification trap: build/type checks pass without the live migration because types come from schema files, not the live DB.

Purpose: Integration tests, the dashboard, and every Phase 15/16 consumer query the live schema. Without applying to the test branch, vitest DB tests fail with "column does not exist"; without the dev branch, the dashboard crashes. Migration 0012 (`route_source_documents`, the D-05 version-history audit table) must land here so Plan 04's uploadDxf can insert into it. Migrations are immutable after this apply (Phase 9-03 hash integrity) — the SQL authored in Plan 02 must be correct the first time.

Output: Both Neon branches migrated; a verified DB-level column/table check; optional `migrate:test` script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-schema-foundation-dxf-route-import/14-RESEARCH.md
@.planning/phases/14-schema-foundation-dxf-route-import/14-02-SUMMARY.md

<interfaces>
<!-- Migration apply protocol — 14-RESEARCH.md Pattern 5 + Pitfall 4; D-49 + D-12-02 precedent. -->

Dev branch:  npx tsx src/db/migrate.ts                 # reads DATABASE_URL from .env.local
Test branch: DATABASE_URL="$TEST_DATABASE_URL" npx tsx src/db/migrate.ts
  # migrate.ts override:false lets an explicitly-exported DATABASE_URL win over .env.local

Both branches were migrated together for 0009 in Phase 12-02 (precedent to mirror).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Plan 02 authored migrations 0010 (routes/submissions columns + chainage index), 0011 (submission_ai_flags table), and 0012 (route_source_documents version-history table, D-05). They are NOT yet applied to any database.</what-built>
  <how-to-verify>
    This step needs the live database credentials, which only the developer can provide/confirm. Confirm that `.env.local` has a valid `DATABASE_URL` (neondb) and that `TEST_DATABASE_URL` (neondb_test) is available in the environment or `.env.local`. If the test-branch URL is stored under a different variable name than `TEST_DATABASE_URL`, tell the executor the correct variable name.
  </how-to-verify>
  <resume-signal>Confirm DATABASE_URL (dev) and the test-branch URL variable name, then type "proceed".</resume-signal>
</task>

<task type="auto">
  <name>Task 1: [BLOCKING] Apply 0010 + 0011 + 0012 to both Neon branches and verify (RTE-03/04/05)</name>
  <read_first>
    - src/db/migrate.ts (the runner — enables PostGIS first, then migrate() over the migrations folder; reads DATABASE_URL; override:false honors an exported DATABASE_URL)
    - 14-RESEARCH.md "Migration application protocol" + Pitfall 4 (both branches) + "## Environment Availability" (DATABASE_URL + TEST/DATABASE_URL_TEST)
    - STATE.md decision "[Phase 12-02]: 0009 applied to dev (neondb) + test (neondb_test) via npx tsx src/db/migrate.ts (D-49)"
  </read_first>
  <action>
    Apply to the dev branch: `npx tsx src/db/migrate.ts`. Then apply to the test branch by exporting the test-branch URL into DATABASE_URL for one invocation: `DATABASE_URL="$TEST_DATABASE_URL" npx tsx src/db/migrate.ts` (use the exact variable name confirmed in the checkpoint). Add a `migrate:test` script to package.json that runs the test-branch apply for repeatability. After both applies, run a direct column/table check against each branch (e.g. `psql "$DATABASE_URL" -c "SELECT column_name FROM information_schema.columns WHERE table_name='routes' AND column_name='geometry_version'"` and similarly for `submissions.chainage_m`, table `submission_ai_flags`, and table `route_source_documents`), OR a tsx one-off using the neon client, confirming the columns/tables exist on BOTH branches. Do NOT edit any migration SQL file during this plan (immutability — if a SQL error surfaces, stop and report; the fix must be a NEW forward migration, never an edit to an applied file).
  </action>
  <verify>
    <automated>npx tsx src/db/migrate.ts && npx vitest run tests/dxf-parser.test.ts -t "schema"</automated>
  </verify>
  <done>migrate.ts reports "Migrations complete" on both branches; direct DB check shows geometry_version + chainage_m + submission_ai_flags + route_source_documents on dev and test; the `tests/dxf-parser.test.ts -t "schema"` smoke case (which exercises the truncate of submission_ai_flags + route_source_documents) passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration runner → Neon | irreversible DDL applied to live branches |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-BRANCH | Denial of Service | preview deploy on neondb_test | mitigate | Apply ALL THREE migrations (0010/0011/0012) to BOTH branches in the same session (Pitfall 4); verify with information_schema check on each |
| T-14-IMMUTABLE2 | Tampering | already-applied 0010/0011/0012 | mitigate | No SQL edits during/after apply; any defect fixed via a new forward migration only (Phase 9-03 hash invariant) |
</threat_model>

<verification>
- `npx tsx src/db/migrate.ts` → "Migrations complete" (dev)
- test-branch apply → "Migrations complete" (test)
- information_schema confirms geometry_version + chainage_m + submission_ai_flags + route_source_documents on both branches
- `npx vitest run tests/dxf-parser.test.ts -t "schema"` passes (schema smoke; truncate touches submission_ai_flags + route_source_documents)
</verification>

<success_criteria>
- Both Neon branches carry the v4.0 schema (routes/submissions columns, submission_ai_flags, route_source_documents)
- No migration SQL file edited after apply
- DB-level smoke check passes
</success_criteria>

<output>
Create `.planning/phases/14-schema-foundation-dxf-route-import/14-03-SUMMARY.md` when done
</output>
</content>
</invoke>
