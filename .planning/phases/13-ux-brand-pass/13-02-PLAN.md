---
phase: 13-ux-brand-pass
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - src/app/dashboard/(admin)/hakedis/page.tsx
  - src/app/dashboard/(admin)/hakedis/[periodId]/page.tsx
  - src/app/dashboard/(admin)/exports/page.tsx
  - src/components/admin/HakedisStatusBadge.tsx
  - src/components/admin/HakedisCreateDialog.tsx
  - src/components/admin/HakedisProjectFilter.tsx
  - src/components/admin/PeriodDetailControls.tsx
  - src/components/admin/LivePeriodPoller.tsx
  - src/components/admin/LineSubmissionsPanel.tsx
  - src/components/admin/DeletePeriodDialog.tsx
  - src/components/admin/FinalizeDialog.tsx
autonomous: true
requirements:
  - BRAND-02
user_setup: []

must_haves:
  truths:
    - "Hakkediş hub (`/dashboard/hakedis`) renders the period table using BrandTable + BrandBadge with compact density and amber primary CTAs"
    - "Hakkediş period detail (`/dashboard/hakedis/[periodId]`) renders with BrandCard sections (line table + deduction summary), no shadows, rounded-md corners"
    - "HakedisStatusBadge uses BrandBadge variants (draft=warning, finalized=info, submitted=primary, paid=success) — not raw amber/blue/violet/emerald inline classes"
    - "PeriodDetailControls Excel + PDF + Recompute + Mark-* buttons use BrandButton (variant outline/secondary), keeping the `status !== 'draft'` gate frozen byte-identical"
    - "LivePeriodPoller renders the same sr-only span and short-circuit-null contract (BYTE-IDENTICAL gating) — only the visible affordance, if added, is restyled"
    - "LineSubmissionsPanel chevron + expand panel + nested table styled with brand primitives — column count + colSpan math (8 in body / 7 in footer) frozen byte-identical"
    - "Exports hub (`/dashboard/exports`) re-skinned: 3 trigger cards use BrandCard + BrandButton variant primary, period picker uses BrandTable"
    - "All Phase 12 contract tests (tests/hakedis-live.test.ts including LivePeriodPoller mount-gate) still pass"
    - "All Phase 11 export-byte regression tests (tests/exports.test.ts) still pass — restyle does not touch /api/exports/* route handlers"
  artifacts:
    - path: "src/app/dashboard/(admin)/hakedis/page.tsx"
      provides: "Re-skinned hakkediş hub using BrandTable + BrandBadge"
      contains: "BrandTable"
    - path: "src/app/dashboard/(admin)/hakedis/[periodId]/page.tsx"
      provides: "Re-skinned period detail; 8-column line table + 7-colspan footer preserved"
      contains: "colSpan={8}"
    - path: "src/app/dashboard/(admin)/exports/page.tsx"
      provides: "Re-skinned exports hub — 3 trigger cards + period picker"
      contains: "BrandCard"
    - path: "src/components/admin/HakedisStatusBadge.tsx"
      provides: "Status pill now wraps BrandBadge with semantic variants"
      contains: "BrandBadge"
    - path: "src/components/admin/PeriodDetailControls.tsx"
      provides: "Re-skinned controls; `status !== 'draft'` gate untouched"
      contains: "BrandButton"
    - path: "src/components/admin/LivePeriodPoller.tsx"
      provides: "null-on-disabled + sr-only span contracts preserved byte-identical"
      contains: "if (!enabled) return null"
    - path: "src/components/admin/LineSubmissionsPanel.tsx"
      provides: "Re-skinned chevron + inline expand-row + nested table"
      contains: "ChevronRight"
  key_links:
    - from: "src/app/dashboard/(admin)/hakedis/[periodId]/page.tsx"
      to: "src/components/admin/LineSubmissionsPanel.tsx"
      via: "8th column on draft period detail"
      pattern: "LineSubmissionsPanel"
    - from: "src/components/admin/HakedisStatusBadge.tsx"
      to: "src/components/brand/BrandBadge.tsx"
      via: "import + JSX render"
      pattern: "BrandBadge"
    - from: "src/components/admin/PeriodDetailControls.tsx"
      to: "src/components/brand/BrandButton.tsx"
      via: "import + JSX render"
      pattern: "BrandButton"
---

<objective>
Wave 2 — Hakkediş + Exports. Re-skin the highest-velocity daily surfaces for the office engineer: hakkediş hub, hakkediş period detail (Phase 10 + Phase 12 traceability column), exports hub. Replace raw shadcn primitives with brand primitives (BrandCard, BrandButton, BrandTable, BrandBadge) per D-127 wave 2 + D-125 layout primitives. Preserve every Phase 12 contract byte-identical (LivePeriodPoller null-on-disabled, LineSubmissionsPanel 8-column + colSpan math, PeriodDetailControls draft gate).

Purpose: Highest-leverage surfaces after the Wave 1 token cascade. The office engineer uses these daily; getting the brand language right here proves the primitives package and validates the cascade against the most complex data tables in the app.

Output: Hakkediş hub + period detail + exports hub render with slate canvas + amber accent + compact density + flat depth; Phase 12 contracts intact; Phase 11 export-route handlers untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-ux-brand-pass/13-CONTEXT.md
@.planning/phases/13-ux-brand-pass/13-RESEARCH.md
@.planning/phases/13-ux-brand-pass/13-VALIDATION.md
@.planning/phases/12-submission-driven-hakkedi/12-CONTEXT.md
@.planning/phases/11-exports/11-CONTEXT.md

<interfaces>
<!-- Brand primitives shipped in Wave 1 -->
From src/components/brand/index.ts:
  export { BrandButton }   // variants: primary | secondary | destructive | outline | ghost; sizes: sm | md | lg
  export { BrandCard }     // namespaced: BrandCard, BrandCard.Header, BrandCard.Body, BrandCard.Footer
  export { BrandBadge }    // variants: primary | success | info | warning | destructive | neutral
  export { BrandTable }    // namespaced: BrandTable.Root | .Header | .Body | .Row | .Head | .Cell | .Footer
  export { BrandHeading }  // sizes: display | h1 | h2 | h3; as prop polymorphic
  export { BrandEmpty }    // composed: icon | title | description | action

<!-- Locked Phase 12 contracts (DO NOT BREAK) -->
LivePeriodPoller:
  - First statement after function entry: `if (!enabled) return null;` (line 52 — frozen byte-identical)
  - Internal LivePeriodPollerEnabled renders <span className="sr-only" role="status" aria-live="polite">
  - Phase 13 may restyle a visible affordance (e.g. a dot, a pill) ONLY by adding new JSX alongside the sr-only span. The sr-only span itself stays untouched.

LineSubmissionsPanel:
  - Trigger: chevron-collapsed/expanded button (ChevronRight / ChevronDown at h-4 w-4)
  - Frozen logic: state model (expanded, rows, isPending), lazy first-fetch via getLineSubmissions, photo link target="_blank" rel="noopener noreferrer"
  - Phase 13 may restyle chevron size, panel border, inner row visuals; cannot change column count or fetch contract

[periodId]/page.tsx:
  - TableHead with sr-only label at column index 8 (line 227–231) — 8-column layout
  - Empty body row uses colSpan={8} (line 238)
  - Footer uses colSpan={7} for label + 1 value cell (line 297) — math FROZEN
  - LivePeriodPoller mounted at line 163: `{status === 'draft' && <LivePeriodPoller enabled={true} />}`

PeriodDetailControls:
  - `status !== 'draft'` gate at line 142 wraps Excel + PDF outline buttons (D-108 distributed trigger — UI-SPEC Surface 2)
  - Phase 13 may restyle button surface (BrandButton variant outline) but the `status !== 'draft'` conditional is frozen
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Re-skin hakkediş hub + status badge + hakkediş dialogs</name>
  <read_first>
    - src/app/dashboard/(admin)/hakedis/page.tsx (full file — 207 lines; period list + project filter + create-dialog mount + per-period delete-dialog mount)
    - src/components/admin/HakedisStatusBadge.tsx (current STATUS_CLASS_MAP at lines 22–27 — DRAFT amber / FINALIZED blue / SUBMITTED violet / PAID emerald)
    - src/components/admin/HakedisCreateDialog.tsx (13.6K — period-create form; deduction-rate inputs)
    - src/components/admin/HakedisProjectFilter.tsx (1.5K — project dropdown selector)
    - src/components/admin/DeletePeriodDialog.tsx (3.8K — destructive confirmation dialog)
    - src/components/admin/FinalizeDialog.tsx (3.4K — finalize confirmation)
    - .planning/phases/13-ux-brand-pass/13-CONTEXT.md §D-125 (compact density: `p-3` KPI tile / `p-4` page section; no shadow on cards; rounded-md)
    - .planning/phases/13-ux-brand-pass/13-RESEARCH.md §Per-Surface Inventory (W2 row) + §Brand Primitive Wrapping Pattern (BrandBadge semantic mapping)
  </read_first>
  <files>src/components/admin/HakedisStatusBadge.tsx, src/components/admin/HakedisCreateDialog.tsx, src/components/admin/HakedisProjectFilter.tsx, src/components/admin/DeletePeriodDialog.tsx, src/components/admin/FinalizeDialog.tsx, src/app/dashboard/(admin)/hakedis/page.tsx</files>
  <action>
    Step 1 — `src/components/admin/HakedisStatusBadge.tsx`:
      - Replace the `import { Badge } from '@/components/ui/badge'` line with `import { BrandBadge } from '@/components/brand';`.
      - Remove the `STATUS_CLASS_MAP` constant entirely.
      - Map status → BrandBadge variant: `draft` → `warning` (orange D-121), `finalized` → `info` (sky), `submitted` → `primary` (amber — in-progress payment), `paid` → `success` (emerald).
      - Replace the `<Badge variant="secondary" className={cn(STATUS_CLASS_MAP[status])} ...>` with `<BrandBadge variant={variantFromStatus} aria-label={\`Status: ${label}\`}>{label}</BrandBadge>`.
      - Keep `'use client'` directive, `useTranslations` call, and `aria-label` shape unchanged (UI-SPEC accessibility contract).
      - Remove the unused `cn` import.

    Step 2 — `src/app/dashboard/(admin)/hakedis/page.tsx`:
      - Replace `Table / TableHeader / TableBody / TableHead / TableRow / TableCell` imports from `@/components/ui/table` with the namespaced `BrandTable` from `@/components/brand`. Inline usage: change `<Table>` → `<BrandTable.Root>`, `<TableHeader>` → `<BrandTable.Header>`, `<TableBody>` → `<BrandTable.Body>`, `<TableRow>` → `<BrandTable.Row>`, `<TableHead>` → `<BrandTable.Head>`, `<TableCell>` → `<BrandTable.Cell>`.
      - Wrap the page-level layout in `<BrandCard>` shells where the existing code uses bespoke divs. Specifically the period-list area gets `<BrandCard><BrandCard.Header>...title...</BrandCard.Header><BrandCard.Body className="p-0">...table...</BrandCard.Body></BrandCard>`.
      - Replace any `rounded-lg` / `rounded-xl` / `shadow-sm` / `shadow-md` utility classes anywhere in this file with `rounded-md` and remove `shadow-*` (D-125 enforcement).
      - Replace any `p-6` / `p-8` page-section padding with `p-4` (D-125 compact density).
      - Replace primary CTA buttons (Create Period trigger inside HakedisCreateDialog mount, any "Yeni Dönem" / "New Period" button) with `<BrandButton variant="primary">` — BUT note: HakedisCreateDialog renders its own trigger; for that component, do step 4.
      - Add `import { BrandTable, BrandCard, BrandButton } from '@/components/brand';` at top.
      - Preserve every server-side data fetch, auth guard, getProjects/getPeriodsByProject call, project-filter wiring, FileX empty-state icon byte-identical.

    Step 3 — `src/components/admin/HakedisProjectFilter.tsx`:
      - This is a shadcn Select-based filter (~1.5K). Re-skin shells with BrandCard if there's a wrapping container; replace any direct `Button` with `BrandButton variant="outline" size="sm"` for the apply/clear control if present.
      - Verify the underlying Select component (`@/components/ui/select`) still owns the popover surface — DO NOT wrap Select with a brand primitive. Selects pull from the token cascade via Wave 1 automatically.

    Step 4 — `src/components/admin/HakedisCreateDialog.tsx` (13.6K — period-create form):
      - Replace the dialog trigger `<Button>` (whichever variant it currently uses) with `<BrandButton variant="primary" size="md">` — keep the icon and label byte-identical.
      - Replace the submit `<Button>` inside the dialog footer with `<BrandButton variant="primary">`.
      - Replace any cancel/secondary `<Button>` with `<BrandButton variant="outline">`.
      - The `<Dialog>` / `<DialogContent>` shadcn primitives stay — those pull `--popover` / `--popover-foreground` / `--radius` from the Wave 1 cascade automatically.
      - Any rounded-lg / shadow / p-6 utility classes inside the dialog: swap to rounded-md / remove shadow / p-4 per D-125.
      - DO NOT touch deduction-rate input wiring, zod schema, form-handler logic, or any state. Visual-only restyle.

    Step 5 — `src/components/admin/DeletePeriodDialog.tsx` + `FinalizeDialog.tsx`:
      - Replace destructive trigger and confirm buttons with `<BrandButton variant="destructive">`; replace cancel/secondary with `<BrandButton variant="outline">`.
      - Visual-only — confirmation dialog flow + server action calls untouched.
  </action>
  <verify>
    <automated>
      grep -c "BrandBadge" src/components/admin/HakedisStatusBadge.tsx; (>= 1 — status pill uses brand primitive)
      grep -c "STATUS_CLASS_MAP" src/components/admin/HakedisStatusBadge.tsx; (== 0 — removed)
      grep -c "BrandTable" "src/app/dashboard/(admin)/hakedis/page.tsx"; (>= 1 — hub uses brand table)
      grep -c "BrandCard" "src/app/dashboard/(admin)/hakedis/page.tsx"; (>= 1 — hub uses brand card)
      grep -v "^#" "src/app/dashboard/(admin)/hakedis/page.tsx" | grep -Ec "shadow-(sm|md|lg|xl|2xl)\\b"; (== 0 — D-125 flat depth)
      grep -v "^#" "src/app/dashboard/(admin)/hakedis/page.tsx" | grep -Ec "rounded-(lg|xl|2xl)\\b"; (== 0 — D-125 rounded-md only)
      grep -c "BrandButton" src/components/admin/HakedisCreateDialog.tsx; (>= 2 — trigger + submit)
      grep -c "BrandButton" src/components/admin/DeletePeriodDialog.tsx; (>= 1)
      grep -c "BrandButton" src/components/admin/FinalizeDialog.tsx; (>= 1)
      npx vitest run; (full suite still green — Phase 10/11/12 tests unaffected)
      npx next build 2>&1 | tail -5; (exit 0)
    </automated>
  </verify>
  <done>Hakkediş hub + status badge + create/delete/finalize dialogs render via brand primitives; D-125 shadow/rounded/padding constraints enforced; full test suite still green; next build exits 0.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Re-skin period detail page + Phase 12 trace components (frozen-contract preservation)</name>
  <read_first>
    - src/app/dashboard/(admin)/hakedis/[periodId]/page.tsx (full file — 431 lines; 8-column line table + deduction summary card + finalized-banner + unpriced-warning + LivePeriodPoller mount + LineSubmissionsPanel mount)
    - src/components/admin/LivePeriodPoller.tsx (null-on-disabled contract at line 52 + sr-only span at line 76 — BOTH FROZEN)
    - src/components/admin/LineSubmissionsPanel.tsx (chevron trigger + lazy fetch + inline expand-row + nested table)
    - src/components/admin/PeriodDetailControls.tsx (status-gated control row + `status !== 'draft'` Excel/PDF gate at line 142 — FROZEN)
    - .planning/phases/12-submission-driven-hakkedi/12-CONTEXT.md (Phase 12 UI shapes)
    - .planning/phases/13-ux-brand-pass/13-CONTEXT.md §D-125 + §D-126 (icon size-4 inline / size-5 button / size-6 header)
    - .planning/phases/13-ux-brand-pass/13-VALIDATION.md §Per-Task Verification Map (Phase 12 contract grep gates)
  </read_first>
  <files>src/app/dashboard/(admin)/hakedis/[periodId]/page.tsx, src/components/admin/PeriodDetailControls.tsx, src/components/admin/LivePeriodPoller.tsx, src/components/admin/LineSubmissionsPanel.tsx</files>
  <behavior>
    DO-NOT-BREAK contracts (every grep below MUST still pass after this task):
    - `if (!enabled) return null;` in LivePeriodPoller.tsx (frozen byte-identical at top of the function).
    - `colSpan={8}` exactly once in [periodId]/page.tsx (empty body row).
    - `colSpan={7}` exactly once in [periodId]/page.tsx (footer label cell).
    - `status !== 'draft'` in PeriodDetailControls.tsx (D-108 Excel/PDF gate).
    - `<span className="sr-only" role="status" aria-live="polite">` in LivePeriodPoller.tsx (sr-only accessibility shape).
    - `target="_blank"` and `rel="noopener noreferrer"` on the photo link in LineSubmissionsPanel.tsx (T-12-04-TAB mitigation).
  </behavior>
  <action>
    Step 1 — `src/app/dashboard/(admin)/hakedis/[periodId]/page.tsx`:
      - Add `import { BrandTable, BrandCard, BrandButton, BrandHeading } from '@/components/brand';` at the top alongside existing imports.
      - Replace `<Card>` → `<BrandCard>`, `<CardHeader>` → `<BrandCard.Header>`, `<CardContent>` → `<BrandCard.Body>` for the two cards (Yeşil Defter Line-Item Table card at lines 196–311; Deduction Summary Card at lines 313–428).
      - The deduction summary card body (`<CardContent>` at line 326) currently uses bespoke `<div className="space-y-0">` — keep that internal structure byte-identical (the Net Ödeme focal point styling at line 416 with `border-t-2 border-foreground` is intentional and stays).
      - Replace `<Table>` / `<TableHeader>` / `<TableBody>` / `<TableRow>` / `<TableHead>` / `<TableCell>` / `<TableFooter>` with the `BrandTable.*` namespaced equivalents. The header row's 8 `<TableHead>` cells become 8 `<BrandTable.Head>` cells (byte-identical column count — verify with grep gate).
      - The empty-body row at line 237 must keep `colSpan={8}` exactly (frozen — verify in <verify>).
      - The footer label cell at line 297 must keep `colSpan={7}` exactly.
      - The 8th `<BrandTable.Head>` (SDH-02 traceability column header at line 227) keeps the sr-only `<span>` accessibility label byte-identical.
      - Each `<TableRow key={line.id}>` in the body keeps `<LineSubmissionsPanel periodLineId={line.id} periodQty={line.periodQty} unitSnapshot={line.unitSnapshot} />` in its 8th cell — props unchanged.
      - LivePeriodPoller mount at line 163: `{status === 'draft' && <LivePeriodPoller enabled={true} />}` stays byte-identical (the conditional mount is the Rules-of-Hooks safety for the null-gate per the LivePeriodPoller file header).
      - Heading at line 142 (`<h1 className="text-xl font-semibold">{period.periodNumber}</h1>`): replace with `<BrandHeading as="h1" size="h1">{period.periodNumber}</BrandHeading>`.
      - Replace `<Alert>` finalized-banner at lines 167–173 and unpriced-warning at lines 177–193: keep shadcn `<Alert>` primitives (they pull from token cascade), but ensure no `shadow-*` or `rounded-lg`/`rounded-xl` overrides remain inline. The unpriced-warning's `border-amber-200 bg-amber-50` inline classes at line 179 stay (D-121 amber-50 IS the brand subtle bg — intentional).
      - Sweep the file for `shadow-sm` / `shadow-md` / `rounded-lg` / `rounded-xl` / `p-6` / `p-8` utility classes and remove/replace per D-125.

    Step 2 — `src/components/admin/PeriodDetailControls.tsx`:
      - Add `import { BrandButton } from '@/components/brand';` at top.
      - Replace each `<Button variant="outline" size="sm">` with `<BrandButton variant="outline" size="sm">`:
        - Recompute button (lines 93–101) → BrandButton outline with icon `<RefreshCw className="h-4 w-4 mr-1" />` (icon size unchanged — keep h-4 w-4 per D-126 inline icon).
        - Mark Submitted button (lines 113–121) → BrandButton outline.
        - Mark Paid button (lines 124–132) → BrandButton outline.
        - Excel + PDF download buttons (lines 149 + 159) inside the `{status !== 'draft' && ...}` gate (line 142) → BrandButton outline. The download-anchor `<a href={\`/api/exports/hakedis/${periodId}\`} download>` wrapper stays byte-identical.
      - DO NOT touch the `status === 'draft'` / `status === 'finalized'` / `status === 'submitted'` conditionals. DO NOT touch the `status !== 'draft'` Excel/PDF gate at line 142.
      - DO NOT touch the FinalizeDialog import or its rendered position; DO NOT touch the DeletePeriodDialog import or its rendered position. Those dialogs render their own triggers (already updated to BrandButton in Plan 13-02 Task 1).
      - Increase the file-type icon size in Excel + PDF buttons from `h-4 w-4` to `h-4 w-4` (no change — D-126 says size-5 in button but the existing pattern matches Recompute h-4 w-4 + mr-1, keep consistent — leave unchanged byte-identical).

    Step 3 — `src/components/admin/LivePeriodPoller.tsx`:
      - The visible-affordance pattern under Phase 13: this component is currently sr-only. The brand pass MAY add a visible dot indicator alongside the sr-only span — but optional. Recommended: leave the file fully untouched in Wave 2, since the only restyling vector is "visible affordance" and there isn't one to restyle yet (the sr-only is the only DOM output).
      - REQUIRED gate: do not change the `if (!enabled) return null;` short-circuit (line 52). Do not change the `<span className="sr-only" role="status" aria-live="polite">` shape at line 76. Do not change the useEffect interval id setup pattern.
      - If the implementer chooses to add a visible dot (purely additive — not required), it MUST be in a separate sibling element rendered ALONGSIDE the sr-only span by `LivePeriodPollerEnabled`, never replacing it. Recommendation: skip; revisit in a future phase if office engineers ask for a visible polling indicator.

    Step 4 — `src/components/admin/LineSubmissionsPanel.tsx`:
      - Add `import { BrandButton, BrandTable } from '@/components/brand';` at top.
      - Replace the chevron trigger `<Button variant="ghost" size="sm" ...>` (lines 110–121) with `<BrandButton variant="ghost" size="sm" type="button" onClick={handleToggle} aria-expanded={expanded} aria-label={triggerLabel} disabled={isPending}>` — same children (the lucide `<Icon className="h-4 w-4 mr-1" aria-hidden="true" />` and the `<span className="text-xs">{t('heading')}</span>` stay byte-identical).
      - The expanded panel `<div className="rounded-md border bg-muted/30 p-2">` (line 124) stays byte-identical — `rounded-md` already; `p-2` is the D-125 row density.
      - Replace `<Table>` / `<TableHeader>` / `<TableBody>` / `<TableRow>` / `<TableHead>` / `<TableCell>` (lines 132–177) with `BrandTable.*` namespaced equivalents. Column count + content + classes stay byte-identical.
      - The photo link `<a target="_blank" rel="noopener noreferrer">` (lines 164–171) MUST stay byte-identical (T-12-04-TAB mitigation — frozen security contract).
  </action>
  <verify>
    <automated>
      grep -c "BrandTable" "src/app/dashboard/(admin)/hakedis/[periodId]/page.tsx"; (>= 1)
      grep -c "BrandCard" "src/app/dashboard/(admin)/hakedis/[periodId]/page.tsx"; (>= 2 — yeşil defter + deduction summary)
      grep -c "colSpan={8}" "src/app/dashboard/(admin)/hakedis/[periodId]/page.tsx"; (== 1 — frozen Phase 12 contract)
      grep -c "colSpan={7}" "src/app/dashboard/(admin)/hakedis/[periodId]/page.tsx"; (== 1 — frozen Phase 12 contract)
      grep -c "<LivePeriodPoller enabled={true} />" "src/app/dashboard/(admin)/hakedis/[periodId]/page.tsx"; (== 1 — frozen mount-gate)
      grep -c "<LineSubmissionsPanel" "src/app/dashboard/(admin)/hakedis/[periodId]/page.tsx"; (== 1 — frozen mount on 8th column)
      grep -Ec "shadow-(sm|md|lg|xl|2xl)" "src/app/dashboard/(admin)/hakedis/[periodId]/page.tsx"; (== 0 — D-125 flat)
      grep -E "if \\(!enabled\\) return null;" src/components/admin/LivePeriodPoller.tsx; (frozen — present)
      grep -c "<span className=\"sr-only\" role=\"status\" aria-live=\"polite\">" src/components/admin/LivePeriodPoller.tsx; (== 1 — sr-only contract frozen)
      grep -c "BrandButton" src/components/admin/PeriodDetailControls.tsx; (>= 4 — recompute + mark-submitted + mark-paid + Excel/PDF buttons converted)
      grep -c "status !== 'draft'" src/components/admin/PeriodDetailControls.tsx; (>= 1 — D-108 gate frozen)
      grep -c "BrandButton" src/components/admin/LineSubmissionsPanel.tsx; (>= 1 — chevron trigger converted)
      grep -c "BrandTable" src/components/admin/LineSubmissionsPanel.tsx; (>= 1 — nested table converted)
      grep -c 'target="_blank"' src/components/admin/LineSubmissionsPanel.tsx; (>= 1 — T-12-04-TAB mitigation frozen)
      grep -c 'rel="noopener noreferrer"' src/components/admin/LineSubmissionsPanel.tsx; (>= 1 — T-12-04-TAB frozen)
      npx vitest run tests/hakedis-live.test.ts; (Phase 12 contract tests green — LivePeriodPoller mount-gate + LineSubmissionsPanel column math)
      npx vitest run tests/exports.test.ts; (Phase 11 export-byte regression green — no /api/exports/* touched)
      npx vitest run; (full suite green)
      npx next build 2>&1 | tail -5; (exit 0)
    </automated>
  </verify>
  <done>Period detail page + Phase 12 components re-skinned with brand primitives; ALL Phase 12 frozen contracts pass (null-gate, sr-only span, 8-column, 7-colspan footer, mount-gate, T-12-04-TAB); Phase 11 export tests green; full test suite green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Re-skin exports hub</name>
  <read_first>
    - src/app/dashboard/(admin)/exports/page.tsx (full file — 10.7K; 3 trigger surfaces per D-108 + period picker)
    - .planning/phases/11-exports/11-CONTEXT.md §D-108 (distributed trigger surface)
    - .planning/phases/13-ux-brand-pass/13-CONTEXT.md §D-125 + §D-127 wave 2
  </read_first>
  <files>src/app/dashboard/(admin)/exports/page.tsx</files>
  <action>
    Step 1 — `src/app/dashboard/(admin)/exports/page.tsx`:
      - Add `import { BrandCard, BrandButton, BrandTable, BrandHeading } from '@/components/brand';` at top.
      - Each of the 3 trigger surfaces (Submission Ledger card, Performance Summary card, Hakkediş Files period picker — D-108) gets wrapped in `<BrandCard>` shells:
        - Submission Ledger trigger card → `<BrandCard><BrandCard.Header>...</BrandCard.Header><BrandCard.Body>... <BrandButton variant="primary" asChild><a href="/api/exports/submissions?..." download>Excel İndir</a></BrandButton></BrandCard.Body></BrandCard>`.
        - Performance Summary trigger card → identical pattern with `/api/exports/performance?...` link.
        - Hakkediş Files period picker → `<BrandCard><BrandCard.Header>...</BrandCard.Header><BrandCard.Body className="p-0"><BrandTable>...periods...</BrandTable></BrandCard.Body></BrandCard>` — each row in the table has Excel + PDF download `<BrandButton variant="outline" size="sm">` links.
      - Replace the page-level headings with `<BrandHeading as="h1" size="h1">`.
      - Sweep this file for `shadow-*` / `rounded-lg` / `rounded-xl` / `p-6` / `p-8` and convert per D-125.
      - Keep the FilterBar `<Suspense>` wrapper byte-identical (it's a shared Phase 8 component re-skinned in Wave 3).
      - Keep the `auth()`-then-redirect server-component pattern and `export const dynamic = 'force-dynamic'` byte-identical.
      - DO NOT touch any `/api/exports/*` route handler. DO NOT change any URL string. DO NOT touch the `download` attribute or aria-labels.
  </action>
  <verify>
    <automated>
      grep -c "BrandCard" "src/app/dashboard/(admin)/exports/page.tsx"; (>= 3 — 3 trigger surfaces per D-108)
      grep -c "BrandButton" "src/app/dashboard/(admin)/exports/page.tsx"; (>= 3 — at least one CTA per surface)
      grep -c "BrandTable" "src/app/dashboard/(admin)/exports/page.tsx"; (>= 1 — period picker table)
      grep -v "^#" "src/app/dashboard/(admin)/exports/page.tsx" | grep -Ec "shadow-(sm|md|lg|xl|2xl)\\b"; (== 0)
      grep -v "^#" "src/app/dashboard/(admin)/exports/page.tsx" | grep -Ec "rounded-(lg|xl|2xl)\\b"; (== 0)
      grep -c '/api/exports/submissions' "src/app/dashboard/(admin)/exports/page.tsx"; (>= 1 — EXP-01 URL preserved)
      grep -c '/api/exports/performance' "src/app/dashboard/(admin)/exports/page.tsx"; (>= 1 — EXP-03 URL preserved)
      grep -c '/api/exports/hakedis' "src/app/dashboard/(admin)/exports/page.tsx"; (>= 1 — EXP-02/04 URLs preserved)
      grep -c "force-dynamic" "src/app/dashboard/(admin)/exports/page.tsx"; (>= 1 — v2.0 financial lock preserved)
      grep -c "auth()" "src/app/dashboard/(admin)/exports/page.tsx"; (>= 1 — D-114 first-statement auth guard preserved)
      npx vitest run tests/exports.test.ts; (Phase 11 export contract tests green)
      npx next build 2>&1 | tail -5; (exit 0)
    </automated>
  </verify>
  <done>Exports hub re-skinned via BrandCard + BrandButton + BrandTable; 3 D-108 trigger surfaces still link to the correct /api/exports/* URLs; auth + force-dynamic preserved; Phase 11 export tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → period detail page | Existing — no change; auth + tenant scope in getPeriodDetail unchanged |
| browser → /api/exports/* | Existing — no change; restyle does not touch route handlers (out of scope) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02-REG | Tampering (regression) | LivePeriodPoller / LineSubmissionsPanel / PeriodDetailControls frozen contracts | mitigate | Task 2 grep gates assert null-gate, sr-only span, 8/7 colSpan math, draft gate, T-12-04-TAB rel attributes all still present byte-identical post-restyle |
| T-13-02-FIN | Tampering | Period detail money display path (`formatMoney` calls + Net Ödeme focal point) | mitigate | Tasks 2 & 3 do NOT touch any `formatMoney` call site or its decimal.js path; T-10-04-FLOAT (UI-SPEC Money Display) preserved |
| T-13-02-XSS | XSS | `materialSnapshot` / `unitSnapshot` rendered in line table | accept | React auto-escapes; no `dangerouslySetInnerHTML` introduced; Phase 11 `sanitizeExcelCell` (T-11-02-FORMULA) only applies to Excel cells not screen renders |
| T-13-02-AUTH | Authorization | `/api/exports/*` route handlers | accept | Out of scope — Phase 13 does not touch route handlers; D-114 auth-first pattern preserved by definition |
| T-13-02-IDOR | IDOR | `getPeriodDetail(periodId)` server action call | accept | Tenant scope frozen — Task 2 does not touch the data-fetch path |
</threat_model>

<verification>
- Phase 12 contract grep gates in Task 2 verify (null-gate, sr-only span, 8-column, 7-colspan footer, mount-gate, T-12-04-TAB rel attributes).
- Phase 11 export-byte regression test in Task 3 verifies exports hub did not break export URLs.
- VALIDATION.md Manual UAT row "Wave 2 hakkediş + exports re-skin matches brand language" SATISFIED on Wave 2 merge (UAT at Wave 4 end-of-phase covers visual sign-off).
</verification>

<success_criteria>
- BRAND-02: Hakkediş hub + period detail + exports hub all render via BrandCard / BrandButton / BrandTable / BrandBadge; no shadow/lg-radius/p-6 utility classes remain in those files.
- Phase 12 contracts intact: LivePeriodPoller null-gate, sr-only span, 8-column LineSubmissionsPanel, draft-gated PeriodDetailControls — all grep-asserted.
- Phase 11 contracts intact: /api/exports/* URLs unchanged, auth + force-dynamic preserved, tests/exports.test.ts green.
</success_criteria>

<output>
Create `.planning/phases/13-ux-brand-pass/13-02-SUMMARY.md` when done. Record: count of files re-skinned (target ~11), grep-gate outputs for the 6 frozen contracts (Phase 12 null-gate, sr-only span, 8-column, 7-colspan, T-12-04-TAB rel, D-108 draft gate), and the export-test pass/fail line for Phase 11.
</output>
