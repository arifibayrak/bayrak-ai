---
phase: 13-ux-brand-pass
plan: 04
type: execute
wave: 4
depends_on: [13-01, 13-02, 13-03]
files_modified:
  - src/app/dashboard/projects/page.tsx
  - src/app/dashboard/projects/loading.tsx
  - src/app/dashboard/projects/new/page.tsx
  - src/app/dashboard/projects/[id]/page.tsx
  - src/app/dashboard/projects/[id]/edit/page.tsx
  - src/app/dashboard/projects/[id]/boq-template/page.tsx
  - src/app/auth/signin/page.tsx
  - src/app/auth/error/page.tsx
  - src/app/page.tsx
autonomous: false
requirements:
  - BRAND-02
user_setup: []

must_haves:
  truths:
    - "Projects list page renders project cards via BrandCard with compact density"
    - "Project detail page (edit + BOQ template + new) uses BrandCard sections, BrandTable for BOQ rows, BrandButton variants for primary/secondary/destructive actions"
    - "BOQ table column headers + rows use compact `text-sm` density per D-125; remaining-balance column intact (SETUP-04)"
    - "GeoJSON upload widget on edit page uses BrandButton; file-input wiring + zod validation untouched"
    - "People-assignment surface within project edit uses BrandTable + BrandButton variants"
    - "Auth signin page renders bayrak.ai wordmark via BrandLogo + email-magic-link form via BrandCard + BrandButton primary"
    - "Auth error page renders BrandEmpty with destructive icon"
    - "Marketing landing root page (`/`) uses BrandLogo, BrandHeading display size, BrandButton primary CTA"
    - "Mapbox map controls on project edit page remain readable on slate canvas (RESEARCH A5 — verify in UAT)"
    - "End-of-phase UAT (Manual UAT row 4 + 5 + 6 + 7) sign-off accepted"
    - "Phase 1 capabilities (sign in, create project, edit BOQ, upload GeoJSON, assign person) still work identically"
  artifacts:
    - path: "src/app/dashboard/projects/[id]/edit/page.tsx"
      provides: "Re-skinned project edit (BOQ table + GeoJSON upload + people assignment)"
      contains: "BrandTable"
    - path: "src/app/auth/signin/page.tsx"
      provides: "Re-skinned magic-link sign-in with BrandLogo"
      contains: "BrandLogo"
    - path: "src/app/auth/error/page.tsx"
      provides: "Re-skinned auth error with BrandEmpty"
      contains: "BrandEmpty"
    - path: "src/app/page.tsx"
      provides: "Re-skinned marketing landing page"
      contains: "BrandLogo"
  key_links:
    - from: "src/app/auth/signin/page.tsx"
      to: "src/components/brand/BrandLogo.tsx"
      via: "import + JSX render at top of sign-in card"
      pattern: "BrandLogo"
    - from: "src/app/page.tsx"
      to: "src/components/brand"
      via: "BrandLogo + BrandHeading + BrandButton imports"
      pattern: "BrandHeading"
---

<objective>
Wave 4 — Projects + Auth + Marketing. Re-skin the remaining surfaces: projects list, project detail (edit + new + BOQ template), auth sign-in + auth error, and the marketing root landing page. Close out Phase 13 with end-of-phase Manual UAT covering all four waves' visual sign-off + functional regression of the audit→hakkediş loop (Manual UAT row 7).

Purpose: Wave 4 covers the lower-velocity but identity-critical surfaces (sign-in is the first impression; landing page is the public face). End-of-phase UAT is the formal acceptance gate for BRAND-02 success criterion 4 ("user accepts each surface visually").

Output: Every Phase 1–12 surface re-skinned; end-of-phase UAT signed off; Phase 13 complete and ready for `/gsd:verify-work`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-ux-brand-pass/13-CONTEXT.md
@.planning/phases/13-ux-brand-pass/13-RESEARCH.md
@.planning/phases/13-ux-brand-pass/13-VALIDATION.md

<interfaces>
<!-- Brand primitives shipped in Wave 1 -->
From src/components/brand/index.ts:
  export { BrandButton, BrandCard, BrandBadge, BrandTable, BrandHeading, BrandEmpty, BrandLogo }

<!-- Phase 1 contracts (DO NOT BREAK) -->
- Auth.js magic-link signin via next-auth — form posts to /api/auth/signin/email
- Project create/edit Server Actions exist in src/actions/projects.ts
- BOQ create/edit/import dialogs in projects/[id]/edit
- GeoJSON LineString upload validates as zod schema; success path triggers spatial re-snap (Phase 4 chained)
- People assignment via projects/[id] edit page
- SETUP-04 remaining-balance column on BOQ table

<!-- Mapbox surface (RESEARCH A5 — MEDIUM risk) -->
- mapbox-gl 3.24.x in projects/[id]/edit may render the GeoJSON route preview
- Mapbox uses its own style tokens; the map controls (zoom buttons, attribution) may need contrast verification on slate-50 canvas
- UAT step in end-of-phase checkpoint covers this assertion
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Re-skin projects surfaces (list + detail + edit + new + BOQ template)</name>
  <read_first>
    - src/app/dashboard/projects/page.tsx (1.6K — project list; iterates getProjects())
    - src/app/dashboard/projects/loading.tsx (282B — skeleton row pattern)
    - src/app/dashboard/projects/new/page.tsx (379B — create-project form; redirect/server-action pattern)
    - src/app/dashboard/projects/[id]/page.tsx (4.4K — project tabbed shell + detail)
    - src/app/dashboard/projects/[id]/edit/page.tsx (9.0K — BOQ table + GeoJSON upload + people assignment)
    - src/app/dashboard/projects/[id]/boq-template/page.tsx (800B — Excel template download page)
    - .planning/phases/13-ux-brand-pass/13-CONTEXT.md §D-125 + §D-128 (Procore/Autodesk table density)
    - .planning/phases/13-ux-brand-pass/13-RESEARCH.md §Per-Surface Inventory (W4 row) + §Assumptions Log A5 (Mapbox)
  </read_first>
  <files>src/app/dashboard/projects/page.tsx, src/app/dashboard/projects/loading.tsx, src/app/dashboard/projects/new/page.tsx, src/app/dashboard/projects/[id]/page.tsx, src/app/dashboard/projects/[id]/edit/page.tsx, src/app/dashboard/projects/[id]/boq-template/page.tsx</files>
  <action>
    Step 1 — `src/app/dashboard/projects/page.tsx` (project list):
      - Add `import { BrandCard, BrandHeading, BrandButton } from '@/components/brand';`.
      - Replace project-row card containers with `<BrandCard>` shells (each project = one BrandCard with `<BrandCard.Header>` name + `<BrandCard.Body>` summary).
      - Page heading → `<BrandHeading as="h1" size="h1">`.
      - "Create new project" CTA → `<BrandButton variant="primary" asChild><Link href="/dashboard/projects/new">+ Yeni Proje</Link></BrandButton>` (preserve next-intl key).
      - Sweep file for D-125 violations.

    Step 2 — `src/app/dashboard/projects/loading.tsx`:
      - Skeleton rows → wrap in `<BrandCard>` shells; the shadcn `<Skeleton>` primitives inherit from `--muted` token (Wave 1 cascade).
      - Sweep for D-125 violations.

    Step 3 — `src/app/dashboard/projects/new/page.tsx`:
      - 379B — likely a minimal server-action wrapper. Replace any inline `<Button>` with `<BrandButton variant="primary">`.

    Step 4 — `src/app/dashboard/projects/[id]/page.tsx` (project detail tabbed shell, 4.4K):
      - Wrap tab content sections in `<BrandCard>`.
      - Tab triggers stay shadcn `<Tabs>` (token cascade).
      - Page heading → `<BrandHeading as="h1" size="h1">`.
      - Sweep for D-125 violations.

    Step 5 — `src/app/dashboard/projects/[id]/edit/page.tsx` (9.0K — BOQ table + GeoJSON upload + people assignment):
      - Add `import { BrandCard, BrandTable, BrandButton, BrandHeading } from '@/components/brand';`.
      - BOQ table → `<BrandTable.*>` namespace. Column count must remain unchanged; `text-sm` baked in by BrandTable.
      - The remaining-balance column (SETUP-04 — contracted - approved) must remain present. Add a grep assertion: file should still reference `boqBalance` or `balanceRemaining` or whatever column key the existing code uses; do not rename.
      - "Add BOQ item" / "Edit row" / "Delete" → `<BrandButton variant="primary" | "outline" | "destructive">`.
      - GeoJSON upload widget — wrap container in `<BrandCard>`; the `<input type="file">` keeps its existing handler; the submit/upload button becomes `<BrandButton variant="primary">`. DO NOT change the zod validation or the upload server-action call.
      - People-assignment surface → `<BrandTable.*>` for the assigned-list; "Assign worker / Assign auditor" → `<BrandButton variant="primary" size="sm">`.
      - Mapbox map preview container (if present) — keep the existing mapbox-gl mount byte-identical; only wrap the outer container in `<BrandCard>` if it isn't already.
      - Sweep file for D-125 violations.
      - DO NOT touch any server action import (createProject / updateProject / addBoqItem / updateBoqItem / uploadGeoJson / assignPerson / unassignPerson) or any data-fetch path.

    Step 6 — `src/app/dashboard/projects/[id]/boq-template/page.tsx` (800B):
      - Small file — likely an Excel template download CTA. `<BrandButton variant="primary" asChild>` with the download anchor.
  </action>
  <verify>
    <automated>
      grep -c "BrandCard" src/app/dashboard/projects/page.tsx; (>= 1)
      grep -c "BrandButton" src/app/dashboard/projects/page.tsx; (>= 1)
      grep -c "BrandTable" "src/app/dashboard/projects/[id]/edit/page.tsx"; (>= 1)
      grep -c "BrandButton" "src/app/dashboard/projects/[id]/edit/page.tsx"; (>= 3 — BOQ + GeoJSON + people)
      grep -c "BrandCard" "src/app/dashboard/projects/[id]/page.tsx"; (>= 1)
      grep -v "^#" src/app/dashboard/projects/page.tsx | grep -Ec "shadow-(sm|md|lg|xl|2xl)\\b"; (== 0)
      grep -v "^#" "src/app/dashboard/projects/[id]/edit/page.tsx" | grep -Ec "shadow-(sm|md|lg|xl|2xl)\\b"; (== 0)
      npx vitest run; (full suite green; Phase 1 + Phase 4 spatial integration tests intact)
      npx next build 2>&1 | tail -5; (exit 0)
    </automated>
  </verify>
  <done>All 6 projects surfaces re-skinned; SETUP-04 balance column intact; GeoJSON upload + Mapbox preview wiring untouched; full vitest green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Re-skin auth signin + auth error + marketing landing</name>
  <read_first>
    - src/app/auth/signin/page.tsx (1.6K — Auth.js magic-link form host)
    - src/app/auth/error/page.tsx (2.9K — Auth.js error page)
    - src/app/page.tsx (7.6K — public marketing landing — bilingual TR/EN tagline)
    - .planning/phases/13-ux-brand-pass/13-CONTEXT.md §D-124 (wordmark spec) + §D-127 wave 4
  </read_first>
  <files>src/app/auth/signin/page.tsx, src/app/auth/error/page.tsx, src/app/page.tsx</files>
  <action>
    Step 1 — `src/app/auth/signin/page.tsx`:
      - Add `import { BrandLogo, BrandCard, BrandButton, BrandHeading } from '@/components/brand';`.
      - Page structure: centered vertical layout, slate-50 canvas. Wrap form in `<BrandCard>` with `max-w-md mx-auto`.
      - Top of the BrandCard.Header: render `<BrandLogo size="lg" />` (large wordmark — first-impression surface).
      - Magic-link email input stays shadcn `<Input>` (token cascade).
      - Submit `<Button>` → `<BrandButton variant="primary" size="md">` with the existing next-intl label.
      - "Magic link sent" success state stays — only restyle the visual surface to BrandCard.Body content with green-toned success indicator.
      - DO NOT touch the Auth.js signIn() call or the form action URL.
      - Sweep for D-125 violations.

    Step 2 — `src/app/auth/error/page.tsx`:
      - Add `import { BrandEmpty, BrandButton } from '@/components/brand';` + `import { TriangleAlert } from 'lucide-react';`.
      - Replace error surface with `<BrandEmpty icon={<TriangleAlert className="size-12 text-destructive" />} title={...} description={...} action={<BrandButton variant="outline" asChild><Link href="/auth/signin">{t('back_to_signin')}</Link></BrandButton>} />`.
      - DO NOT touch the searchParams error-code parsing (Auth.js passes ?error=AccessDenied etc.) — just restyle the rendering.
      - Sweep for D-125 violations.

    Step 3 — `src/app/page.tsx` (marketing landing, 7.6K):
      - Add `import { BrandLogo, BrandHeading, BrandButton } from '@/components/brand';`.
      - Header / hero section: large `<BrandLogo size="lg" />` + `<BrandHeading as="h1" size="display">{tagline}</BrandHeading>`.
      - Bilingual tagline (TR/EN per Quick Task 260525-2uq) preserved byte-identical via next-intl.
      - Primary CTA "Go to dashboard" / "Panele git" → `<BrandButton variant="primary" size="lg">`.
      - Convert hero / feature / footer section wrappers to BrandCard or BrandCard.Body where bespoke divs currently exist.
      - Sweep for D-125 violations.
      - DO NOT touch the locale toggle wiring or any next-intl `getTranslations` call.
  </action>
  <verify>
    <automated>
      grep -c "BrandLogo" src/app/auth/signin/page.tsx; (>= 1)
      grep -c "BrandButton" src/app/auth/signin/page.tsx; (>= 1)
      grep -c "BrandCard" src/app/auth/signin/page.tsx; (>= 1)
      grep -c "BrandEmpty" src/app/auth/error/page.tsx; (>= 1)
      grep -c "BrandLogo" src/app/page.tsx; (>= 1)
      grep -c "BrandHeading" src/app/page.tsx; (>= 1)
      grep -v "^#" src/app/auth/signin/page.tsx | grep -Ec "shadow-(sm|md|lg|xl|2xl)\\b"; (== 0)
      grep -v "^#" src/app/page.tsx | grep -Ec "shadow-(sm|md|lg|xl|2xl)\\b"; (== 0)
      grep -c "signIn" src/app/auth/signin/page.tsx; (>= 1 — Auth.js call preserved)
      npx vitest run; (full suite green)
      npx next build 2>&1 | tail -5; (exit 0)
    </automated>
  </verify>
  <done>Auth signin + error + marketing landing re-skinned with BrandLogo / BrandCard / BrandButton / BrandEmpty; Auth.js signIn() call preserved; full vitest green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-of-phase Manual UAT — full visual + functional regression sign-off (Manual UAT rows 4 + 5 + 6 + 7)</name>
  <what-built>
    Phase 13 complete: all four waves shipped. Wave 1 token cascade + brand primitives + sidebar wordmark. Wave 2 hakkediş + exports re-skin. Wave 3 analytics + people + records + settings + overview re-skin. Wave 4 projects + auth + marketing re-skin.

    This UAT is the formal acceptance gate for BRAND-02 success criterion 4 ("user accepts each surface visually") + VALIDATION Manual UAT rows 4, 5, 6, 7.
  </what-built>
  <how-to-verify>
    A. **Wave 4 surfaces — visual UAT (VALIDATION row 4):**
       1. `npm run dev`. Visit `/dashboard/projects` — confirm project list cards use BrandCard, no shadows, rounded-md corners, amber primary "Yeni Proje" CTA.
       2. Click any project → confirm project tabbed shell renders with BrandCard sections; click "Edit" → BOQ table is BrandTable (compact text-sm), remaining-balance column visible; "Add BOQ item" button is amber.
       3. Force a 404 by visiting `/dashboard/this-route-does-not-exist` — confirm `app/not-found.tsx` BrandEmpty surface renders correctly (slate icon, BrandHeading title, slate-600 description, outline button linking to /dashboard/overview).
       4. To force a runtime error, temporarily rename DATABASE_URL in `.env.local` to a bad value and visit an analytics page — confirm `app/error.tsx` BrandEmpty renders (red TriangleAlert icon, amber Try-again button). Restore .env.local after.
       5. Visit `/auth/signin` (sign out first if needed): confirm large `<BrandLogo size="lg" />` at top of the sign-in card, amber Submit button.
       6. Visit `/auth/error?error=AccessDenied`: confirm BrandEmpty error surface.
       7. Visit `/` (marketing landing): confirm large BrandLogo, display-size BrandHeading tagline, amber primary CTA.

    B. **TR ↔ EN locale toggle on every wave's surfaces (VALIDATION row 5):**
       8. Toggle locale on at least 3 surfaces touched in each wave (Wave 1 sidebar, Wave 2 hakkediş, Wave 3 overview, Wave 4 projects). Confirm no fallback strings, no broken layout.

    C. **LivePeriodPoller sr-only contract (VALIDATION row 6 — Phase 13 must NOT break Phase 12 sr-only span):**
       9. Open a draft hakkediş period detail in VoiceOver (macOS) or NVDA (Windows). With the screen reader on, navigate to the period detail page. Confirm the "polling indicator" string (TR: "Otomatik yenileme aktif" or whatever the next-intl key resolves to) is announced periodically.
       10. With DevTools Elements panel open, confirm the `<span class="sr-only" role="status" aria-live="polite">` element is visually hidden (sr-only utility — width 1px, position absolute, etc.) and no visible polling element appears.

    D. **Functional regression spot-check on the audit→hakkediş loop (VALIDATION row 7 — highest-stakes flow):**
       11. From a Telegram bot session: have an auditor approve a work-application submission for a project that has an open draft hakkediş period.
       12. Within 30s the draft period detail page (re-skinned in Wave 2) should update the period_qty WITHOUT manual refresh (SDH-01 contract preserved).
       13. Open the 8th column expand-row for the affected BOQ line → confirm the LineSubmissionsPanel renders the new submission (SDH-02 traceability preserved).
       14. Open a finalized period → confirm Excel + PDF download buttons render (D-108 distributed trigger preserved via BrandButton variant outline).
       15. Click Excel → confirm a .xlsx file downloads with the same byte-identical content as before Phase 13 (Phase 11 contract — `tests/exports.test.ts` already covers this; this manual step is just smoke-confirmation).
       16. Click PDF → confirm Turkish glyphs (ğ ş ı ö ü ç) render correctly (D-106 DejaVu font path preserved).

    E. **Full test-suite gate:**
       17. Run `npx vitest run` from repo root in a separate terminal. Confirm exit 0 — full suite green.
       18. Run `npx next build`. Confirm exit 0.

    Reply with "approved" or describe specific surface + issue observed.
  </how-to-verify>
  <files>(verification-only — no files modified by this end-of-phase UAT checkpoint; updates .planning/phases/13-ux-brand-pass/13-VALIDATION.md frontmatter on approval per <output>)</files>
  <action>Execute UAT steps A–E in <how-to-verify> as a human checkpoint covering Wave 4 visual + TR/EN locale toggle + LivePeriodPoller sr-only contract + end-to-end audit→hakkediş loop regression + full vitest/next-build gate. Implementer pauses execution until user replies "approved" or describes a specific blocker. On approval, set `nyquist_compliant: true` in 13-VALIDATION.md frontmatter and write the Phase 13 close-out summary.</action>
  <verify>
    <human-check>User reply contains "approved" AND `npx vitest run` exit 0 AND `npx next build` exit 0 AND SDH-01 live-poll confirmed in UAT step D-12 AND Phase 11 PDF Turkish glyphs confirmed in UAT step D-16</human-check>
  </verify>
  <done>User has replied "approved"; full test suite green; next build green; Phase 13 phase-completion gate satisfied; 13-VALIDATION.md `nyquist_compliant: true`</done>
  <resume-signal>Reply with "approved" or describe blocker</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous user → /auth/signin | Existing — Auth.js handles magic-link issuance; restyle does not touch the form action URL |
| anonymous user → /auth/error | Existing — error rendering only; no action surface |
| anonymous user → / (marketing) | Existing — public landing; no data fetches |
| authenticated user → /dashboard/projects | Existing — auth + tenant scope unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04-AUTH | Spoofing | Auth.js signIn() call on /auth/signin | accept | Restyle does not touch signIn() invocation; grep gate `grep -c "signIn" src/app/auth/signin/page.tsx` >= 1 |
| T-13-04-FILE | Tampering | GeoJSON upload in projects/[id]/edit | accept | Restyle does not touch zod schema or upload server action; Phase 1 contract preserved |
| T-13-04-MAP | Information Disclosure | Mapbox token (NEXT_PUBLIC_MAPBOX_TOKEN) | accept | Token already exposed by design (DASH-04 — domain-restricted at Mapbox dashboard); restyle does not change exposure |
| T-13-04-REG | Tampering (regression) | Phase 1–12 capabilities behind restyled surfaces | mitigate | Task 3 end-of-phase UAT step D explicitly walks the audit→hakkediş loop; Tasks 1–2 grep gates assert Auth.js + boqBalance + colSpan contracts intact |
| T-13-04-SEO | Information Disclosure | OG image at app/opengraph-image.tsx (shipped Wave 1) | accept | Public marketing content; no PII; bilingual tagline matches existing /page.tsx |
</threat_model>

<verification>
- VALIDATION.md Manual UAT rows 4 / 5 / 6 / 7 SATISFIED via Task 3.
- Phase 1 capabilities (sign in, create project, edit BOQ, upload GeoJSON, assign person) confirmed working via UAT spot-checks.
- Phase 11 export bytes + Phase 11 PDF Turkish glyphs confirmed unchanged via UAT steps 15–16.
- Phase 12 SDH-01 live-poll + SDH-02 traceability confirmed unchanged via UAT steps 11–13.
</verification>

<success_criteria>
- BRAND-02: Every Phase 1–12 surface re-skinned across all four waves; no functional regression in the audit→hakkediş loop; user accepts each surface visually via end-of-phase UAT.
- All four ROADMAP §Phase 13 success criteria SATISFIED:
  1. Brand reference checked in (BRAND.md + 13-CONTEXT.md — Wave 1)
  2. Shared brand primitives in src/components/brand/ — Wave 1
  3. Every existing dashboard surface re-skinned — Waves 1–4
  4. User accepts each surface visually — Task 3 end-of-phase UAT
</success_criteria>

<output>
Create `.planning/phases/13-ux-brand-pass/13-04-SUMMARY.md` when done. Record: count of files re-skinned in Wave 4 (target 9), Task 3 UAT verdict, any surface notes the user flagged, and Phase 13 phase-completion status. Also update `.planning/phases/13-ux-brand-pass/13-VALIDATION.md` frontmatter: set `nyquist_compliant: true` after UAT approved.
</output>
