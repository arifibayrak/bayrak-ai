---
phase: 07-data-foundation-canonical-record
plan: 04
type: execute
wave: 4
depends_on: ["07-01", "07-02", "07-03"]
files_modified:
  - src/actions/boq.ts
  - src/actions/projects.ts
  - src/actions/people.ts
  - src/actions/routes.ts
  - src/components/dashboard/BoqItemDialog.tsx
  - tests/analytics.test.ts
autonomous: false
requirements: [COST-01, PERF-03]
nyquist_compliant: true

must_haves:
  truths:
    - "setUnitPrice() persists unit_price + currency_code, auth-guarded, tenant-scoped, validating non-negative numeric"
    - "Every office-engineer mutation (project/BOQ/unit-price/person/route) calls logOfficeActivity() after the primary write succeeds"
    - "logOfficeActivity is never awaited and never fails the primary mutation"
    - "Office engineer can enter unit price + currency in the BOQ edit dialog; unpriced items show a placeholder not zero"
  artifacts:
    - path: "src/actions/boq.ts"
      provides: "setUnitPrice action + logOfficeActivity wiring on all BOQ mutations"
      contains: "setUnitPrice"
    - path: "src/components/dashboard/BoqItemDialog.tsx"
      provides: "unit price + currency form fields"
      contains: "currencyCode"
  key_links:
    - from: "src/components/dashboard/BoqItemDialog.tsx"
      to: "setUnitPrice / updateBoqItem"
      via: "form submit Server Action call"
      pattern: "setUnitPrice|currencyCode"
    - from: "src/actions/projects.ts"
      to: "office_activity_log"
      via: "logOfficeActivity() after primary write"
      pattern: "logOfficeActivity\\("
---

<objective>
Make unit price + currency enterable and persisted (COST-01), and wire logOfficeActivity() into every office-engineer mutation so the activity log captures actor/action/entity/timestamp (PERF-03). Adds the setUnitPrice() Server Action and the BOQ dialog form fields.

Purpose: Close COST-01 (the only UI-facing requirement in this phase) and PERF-03's write path so Phase 9's office-engineer scorecard has real data to read.

Output: setUnitPrice() in boq.ts; logOfficeActivity() calls in boq.ts/projects.ts/people.ts/routes.ts; unit-price + currency fields in BoqItemDialog; COST-01 + PERF-03 wiring tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-data-foundation-canonical-record/07-RESEARCH.md
@.planning/phases/07-data-foundation-canonical-record/07-PATTERNS.md
@.planning/phases/07-data-foundation-canonical-record/07-VALIDATION.md
@.planning/phases/07-03-PLAN.md

<interfaces>
<!-- Contracts the executor wires against. -->

From src/lib/log-office-activity.ts (Plan 03):
  function logOfficeActivity(params: { actorUserId: string; actionType: OfficeActionType;
    entityType: string; entityId?: string; projectId?: string; metadata?: Record<string,unknown> }): void
  // synchronous void — NEVER await it.

Existing boq.ts mutations (auth-guarded; from the read file), to wire + extend:
  addBoqItem({projectId,material,unit,plannedQty}) → returns {ok,id}    → actionType 'boq_item_created'
  updateBoqItem(id,{material?,unit?,plannedQty?}) → returns {ok}; fetches projectId post-update → 'boq_item_updated'
  deleteBoqItem(id) → fetches projectId pre-delete then deletes         → actionType 'boq_item_deleted'
  confirmBoqImport(projectId, rows[]) → returns {ok,count}              → actionType 'boq_imported'
  // All use session = await auth(); getDefaultTenantId(); revalidatePath(`/dashboard/projects/${projectId}`)

projects.ts (from PATTERNS): createProject/updateProject/deleteProject → 'project_created'/'project_updated'/'project_deleted'
people.ts exports: approvePending → 'person_approved'; addManualPerson/assignment → 'person_assigned';
  removeAssignment → 'person_unassigned'
routes.ts: uploadRoute → 'route_uploaded'

session.user.id IS available (database-session strategy via DrizzleAdapter; users.id is text).
BoqItemDialog currently has material/unit/plannedQty fields and calls addBoqItem/updateBoqItem.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: setUnitPrice() action + logOfficeActivity wiring across boq.ts and projects.ts</name>
  <files>src/actions/boq.ts, src/actions/projects.ts, tests/analytics.test.ts</files>
  <read_first>
    - src/actions/boq.ts (the file being modified — updateBoqItem is the template for setUnitPrice; existing auth/tenant/revalidate patterns)
    - src/actions/projects.ts (createProject/updateProject/deleteProject — wiring targets)
    - src/lib/log-office-activity.ts (logOfficeActivity signature — from Plan 03)
    - .planning/phases/07-data-foundation-canonical-record/07-RESEARCH.md (§ New setUnitPrice() Server Action — exact body; § Wiring into existing Server Actions)
    - .planning/phases/07-data-foundation-canonical-record/07-PATTERNS.md (boq.ts + projects.ts wiring pattern assignments)
    - .planning/phases/07-data-foundation-canonical-record/07-VALIDATION.md (COST-01 setUnitPrice persist + unauthorized; Money-Math Test 3)
  </read_first>
  <behavior>
    - setUnitPrice({ boqItemId, unitPrice, currencyCode }) with no session throws 'Unauthorized'.
    - With unitPrice a non-negative numeric string (or null to clear) + currencyCode, it UPDATEs boq_items (tenant-scoped WHERE) and persists; returns { ok: true }.
    - unitPrice that is NaN or negative returns { ok: false, error: ... } and does NOT write.
    - After a successful write it calls logOfficeActivity with actionType 'unit_price_set', entityType 'boq_item', entityId=boqItemId, projectId (fetched before update), metadata { oldPrice, newPrice, currencyCode }.
    - createProject still returns ok even if the logOfficeActivity INSERT throws (Money-Math Test 3 — after() mocked to run immediately, insert mocked to throw) and the project row exists.
  </behavior>
  <action>
    In src/actions/boq.ts: import logOfficeActivity from '@/lib/log-office-activity'. Add `export async function setUnitPrice(params: { boqItemId: string; unitPrice: string | null; currencyCode: string })` modeled on updateBoqItem — auth guard; validate `if (params.unitPrice !== null) { const val = parseFloat(params.unitPrice); if (isNaN(val) || val < 0) return { ok:false as const, error:'Unit price must be a non-negative number' }; }`; fetch `[old]` = `{ unitPrice, projectId }` from boqItems WHERE id=boqItemId AND tenantId=getDefaultTenantId() limit 1; if !old return not-found; UPDATE boqItems SET unitPrice=params.unitPrice, currencyCode=params.currencyCode WHERE id AND tenantId; call logOfficeActivity({ actorUserId: session.user.id, actionType:'unit_price_set', entityType:'boq_item', entityId: params.boqItemId, projectId: old.projectId, metadata: { oldPrice: old.unitPrice, newPrice: params.unitPrice, currencyCode: params.currencyCode } }); revalidatePath(`/dashboard/projects/${old.projectId}`); return { ok:true as const }.

    Also wire logOfficeActivity into the existing boq.ts mutations after their successful write, before revalidatePath: addBoqItem → 'boq_item_created' (entityId inserted.id, projectId, metadata {material,unit,plannedQty}); updateBoqItem → 'boq_item_updated' (entityId id, projectId from the fetched row, metadata of changed fields); deleteBoqItem → 'boq_item_deleted' (entityId id, projectId from the pre-delete fetch); confirmBoqImport → 'boq_imported' (entityType 'project', projectId, metadata { rowCount: rows.length }).

    In src/actions/projects.ts: import logOfficeActivity; add a call after the .returning()/update/delete in createProject ('project_created', entityId project.id, metadata {name}), updateProject ('project_updated'), deleteProject ('project_deleted'), each using session.user.id. Never await any logOfficeActivity call.

    Add the COST-01 setUnitPrice tests (persist + unauthorized) and the PERF-03 Money-Math Test 3 (logOfficeActivity insert throws → createProject still ok + row present) to tests/analytics.test.ts and turn them green. Use the existing auth mock returning { user: { id:'test-user-id', email:... } } and the next/server after() mock.
  </action>
  <verify>
    <automated>npx vitest run tests/analytics.test.ts -t "COST-01" tests/analytics.test.ts -t "non-blocking" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export async function setUnitPrice" src/actions/boq.ts` and it begins with `const session = await auth()`.
    - setUnitPrice UPDATE includes `eq(boqItems.tenantId, getDefaultTenantId())` (tenant-scoped) and validates non-negative numeric before writing.
    - `grep -c "logOfficeActivity(" src/actions/boq.ts` ≥ 5 (addBoqItem, updateBoqItem, deleteBoqItem, confirmBoqImport, setUnitPrice) and `grep -c "logOfficeActivity(" src/actions/projects.ts` ≥ 3.
    - `grep -c "await logOfficeActivity" src/actions/boq.ts src/actions/projects.ts` == 0 (never awaited).
    - COST-01 persist test passes (unit_price + currency_code read back); unauthorized test throws 'Unauthorized'.
    - Money-Math Test 3 passes: createProject returns ok and the project exists even when the log insert throws.
  </acceptance_criteria>
  <done>setUnitPrice persists price+currency with auth+tenant+validation and logs 'unit_price_set'; all boq.ts + projects.ts mutations fire (un-awaited) logOfficeActivity; COST-01 + Money-Math Test 3 pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: logOfficeActivity wiring across people.ts and routes.ts</name>
  <files>src/actions/people.ts, src/actions/routes.ts, tests/analytics.test.ts</files>
  <read_first>
    - src/actions/people.ts (approvePending, addManualPerson, removeAssignment — wiring targets and their return/entity shapes)
    - src/actions/routes.ts (uploadRoute — the route_uploaded wiring target)
    - src/lib/log-office-activity.ts (logOfficeActivity signature)
    - .planning/phases/07-data-foundation-canonical-record/07-PATTERNS.md (people.ts + routes.ts wiring pattern)
    - .planning/phases/07-data-foundation-canonical-record/07-VALIDATION.md (PERF-03 logOfficeActivity inserts row; getOfficeActivityLog filtered)
  </read_first>
  <behavior>
    - Approving a pending person fires logOfficeActivity 'person_approved' (entityType 'person', entityId personId, metadata { role, displayName }); assigning fires 'person_assigned' (metadata { personId, role, projectId }); removeAssignment fires 'person_unassigned'.
    - Uploading a route fires 'route_uploaded' (entityType 'project', projectId, metadata { ... }).
    - A person/route mutation that succeeds still succeeds if the log INSERT throws (non-blocking).
    - After wiring, getOfficeActivityLog (Plan 03) returns the logged rows filtered by actorUserId — end-to-end PERF-03 path.
  </behavior>
  <action>
    In src/actions/people.ts: import logOfficeActivity. After the successful DB write in approvePending add a call with actionType 'person_approved' (entityType 'person', entityId = approved person id, projectId if assigned in the same action, metadata { role, displayName }); in addManualPerson / the assignment path add 'person_assigned' (metadata { personId, role: roleOnProject, projectId }); in removeAssignment add 'person_unassigned' (entityType 'person', entityId personId, projectId, metadata { assignmentId }). Use session.user.id for actorUserId; never await.

    In src/actions/routes.ts: import logOfficeActivity. After a successful uploadRoute write add a call with actionType 'route_uploaded' (entityType 'project', entityId projectId, projectId, metadata { coordinateCount or fileName as available }). Never await.

    Add a PERF-03 end-to-end wiring test to tests/analytics.test.ts: call a wired action (e.g., createProject or approvePending) with after() mocked to run immediately, then assert getOfficeActivityLog({ actorUserId:'test-user-id' }) returns the inserted entry with the expected actionType. Turn the "logOfficeActivity inserts row" stub green.
  </action>
  <verify>
    <automated>npx vitest run tests/analytics.test.ts -t "PERF-03" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "logOfficeActivity(" src/actions/people.ts` ≥ 3 (approved, assigned, unassigned) and `grep -c "logOfficeActivity(" src/actions/routes.ts` ≥ 1.
    - `grep -c "await logOfficeActivity" src/actions/people.ts src/actions/routes.ts` == 0.
    - Each logOfficeActivity call uses session.user.id as actorUserId and a value from OFFICE_ACTION_TYPES as actionType.
    - PERF-03 "logOfficeActivity inserts row" test passes: getOfficeActivityLog returns the entry filtered by actorUserId after a wired mutation.
  </acceptance_criteria>
  <done>people.ts + routes.ts mutations fire (un-awaited) logOfficeActivity with correct action types; the PERF-03 end-to-end insert→read test passes.</done>
</task>

<task type="auto">
  <name>Task 3: Unit price + currency fields in the BOQ edit dialog (COST-01 UI)</name>
  <files>src/components/dashboard/BoqItemDialog.tsx</files>
  <read_first>
    - src/components/dashboard/BoqItemDialog.tsx (the dialog being extended — existing material/unit/plannedQty field + validate() + handleSubmit patterns)
    - src/components/dashboard/BoqTable.tsx (the BoqItem type — confirm/extend with unitPrice + currencyCode so the dialog can prefill in edit mode)
    - src/actions/boq.ts (setUnitPrice signature — from Task 1)
    - .planning/phases/07-data-foundation-canonical-record/07-RESEARCH.md (Open Question 1 — default 'TRY' with a select of ['TRY','USD','EUR'])
    - .planning/phases/07-data-foundation-canonical-record/07-VALIDATION.md (Manual-Only Verifications — COST-01 UI)
  </read_first>
  <action>
    Extend src/components/dashboard/BoqItemDialog.tsx: add two controlled fields — a unit price text Input (inputMode="decimal", value `unitPrice`, comma→period normalization on parse, ALLOWED empty = no price) and a currency select defaulting to 'TRY' with options ['TRY','USD','EUR']. Prefill from `item?.unitPrice` / `item?.currencyCode` in edit mode (treat null/undefined price as empty string so the placeholder shows, NOT '0'). Add an empty/NaN-tolerant validation: price is optional; if present it must parse to a non-negative number (reuse the comma-normalization already used for plannedQty). On submit in edit mode, after updateBoqItem succeeds (or as a combined flow), call setUnitPrice({ boqItemId: item.id, unitPrice: <normalized string or null>, currencyCode }); in add mode, the price may be set in the same save by calling setUnitPrice on the newly-created id, or left empty. Add the i18n label keys for the new fields to the existing translation namespace usage (placeholder text indicating "no price" when empty). Do NOT display '0' for an unpriced item — show the placeholder.

    If BoqTable's BoqItem type lacks unitPrice/currencyCode, extend that type so the dialog receives them for prefill (keep unitPrice as string | null).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "BoqItemDialog|BoqTable" ; test $? -ne 0 && echo "NO_DIALOG_TS_ERRORS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "currencyCode" src/components/dashboard/BoqItemDialog.tsx` and `grep -q "setUnitPrice" src/components/dashboard/BoqItemDialog.tsx`.
    - The currency control defaults to 'TRY' and offers TRY/USD/EUR.
    - An empty unit price field submits as null (clears/keeps no price), not '0'; an unpriced item in edit mode shows an empty field/placeholder.
    - `npx tsc --noEmit` produces no errors in BoqItemDialog.tsx or BoqTable.tsx.
  </acceptance_criteria>
  <done>BOQ edit dialog has unit price (optional, comma-tolerant, non-negative) + currency (TRY default, TRY/USD/EUR) fields wired to setUnitPrice; unpriced items show a placeholder not zero; type-check clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: [CHECKPOINT] Verify BOQ price/currency persistence + placeholder render</name>
  <action>Human verifies the COST-01 UI round-trip in the running dashboard — automated tests cover persistence and the log write path; this checkpoint verifies the render + reload behavior they cannot exercise.</action>
  <what-built>Unit price + currency fields in the BOQ edit dialog wired to setUnitPrice() (COST-01), and activity logging on every office-engineer mutation (PERF-03). The DB-integration tests cover persistence and the log write path; this checkpoint verifies the UI render + persistence round-trip that automated tests cannot exercise.</what-built>
  <how-to-verify>
    1. Start the dev server (`npm run dev` is sufficient for the BOQ form).
    2. Open a project's BOQ tab, edit a line item, set unit price `1000` and currency `TRY`, save.
    3. Reload the page — the price `1000` and currency `TRY` persist on that line item.
    4. Edit a different line item, leave price empty, save, reload — it shows a placeholder (e.g., an em-dash or "—"), NOT `0`.
    5. (Optional) Confirm an office_activity_log row was written for the unit_price_set action via a quick DB query.
  </how-to-verify>
  <resume-signal>Type "approved" if price+currency persist and unpriced items show a placeholder; otherwise describe what rendered.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form → setUnitPrice Server Action | untrusted unit-price/currency input crosses here |
| Server Action → office_activity_log | mutation metadata written to the audit trail |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | Tampering | unauthorized unit-price modification | mitigate | setUnitPrice starts with `await auth()` guard + tenant-scoped UPDATE WHERE (id AND tenantId) |
| T-07-10 | Tampering | malformed/negative unit price | mitigate | parseFloat + isNaN + `< 0` rejection before any write; numeric persisted as string |
| T-07-11 | Information Disclosure | activity-log PII | mitigate | metadata stores entityId + numeric oldPrice/newPrice/currencyCode + role, not emails; person displayName limited and names re-joined at read time |
| T-07-12 | Repudiation | office action without an audit trail | mitigate | logOfficeActivity fires on every mutation in boq/projects/people/routes; actorUserId = session.user.id |
</threat_model>

<verification>
- `npx vitest run tests/analytics.test.ts` green for COST-01 (persist + unauthorized) and PERF-03 (insert row + non-blocking).
- `grep` confirms logOfficeActivity wired (un-awaited) across boq.ts, projects.ts, people.ts, routes.ts.
- `npx tsc --noEmit` clean for the dialog; human-verify checkpoint confirms persistence + placeholder behavior.
- Full suite: `npx vitest run` green before phase gate.
</verification>

<success_criteria>
- setUnitPrice persists unit_price + currency_code, auth-guarded, tenant-scoped, validated.
- Every office-engineer mutation writes an office_activity_log row via un-awaited logOfficeActivity.
- BOQ dialog lets the office engineer set price + currency; unpriced items show a placeholder not zero.
</success_criteria>

<output>
Create `.planning/phases/07-data-foundation-canonical-record/07-04-SUMMARY.md` when done.
</output>
