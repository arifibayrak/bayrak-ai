---
phase: 07-data-foundation-canonical-record
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/db/migrations/0004_v2_data_foundation.sql
  - src/db/migrations/0005_v2_indexes.sql
  - src/db/migrations/meta/_journal.json
  - src/db/migrations/meta/0004_snapshot.json
autonomous: false
requirements: [COST-01, COST-02, COST-03, COST-04, COST-05, PERF-03]
nyquist_compliant: true

must_haves:
  truths:
    - "Migration 0004 adds unit_price + currency_code to boq_items and creates the three new tables"
    - "0004 carries the hand-edited CHECK (cumulative_qty_approved >= previous_cumulative_qty) constraint"
    - "0005 hand-written partial/composite indexes exist (drizzle-kit cannot emit partial WHERE indexes)"
    - "All migrations applied to the live Neon DB via tsx src/db/migrate.ts (NOT drizzle-kit push)"
    - "Existing boq_items rows read back currency_code = 'TRY' after the migration"
    - "INSERT violating the CHECK constraint is rejected by Postgres"
  artifacts:
    - path: "src/db/migrations/0004_v2_data_foundation.sql"
      provides: "Column add + 3 table creates + hand-edited CHECK"
      contains: "hakedis_period_lines_cumulative_check"
    - path: "src/db/migrations/0005_v2_indexes.sql"
      provides: "Hand-written analytics partial + composite indexes"
      contains: "WHERE status = 'pending_audit'"
  key_links:
    - from: "tsx src/db/migrate.ts"
      to: "Neon Postgres"
      via: "Drizzle migrate() applies 0004 then 0005 alphabetically"
      pattern: "migrate"
    - from: "0004 generated SQL"
      to: "hakedis_period_lines CHECK"
      via: "hand-edited ALTER TABLE ... ADD CONSTRAINT"
      pattern: "CHECK \\(cumulative_qty_approved >= previous_cumulative_qty\\)"
---

<objective>
[BLOCKING] Generate, hand-edit, and APPLY the Phase 7 schema to the live Neon database. Without this step, type checks and unit tests pass against config-derived types while the live DB lacks the columns/tables/constraints — a false-positive verification state. This plan closes that trap.

Purpose: Make the schema authored in Plan 01 real in Neon, including the two artifacts drizzle-kit cannot emit (the CHECK constraint and the partial indexes), applied through the project's only valid runner (tsx src/db/migrate.ts — D-49 makes drizzle-kit push permanently unusable).

Output: 0004_v2_data_foundation.sql (generated + hand-edited CHECK), 0005_v2_indexes.sql (hand-written), both applied to live Neon; updated meta journal/snapshot.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-data-foundation-canonical-record/07-RESEARCH.md
@.planning/phases/07-data-foundation-canonical-record/07-PATTERNS.md
@.planning/phases/07-01-PLAN.md

<interfaces>
<!-- Migration facts extracted from the codebase. -->

Existing migrations (next number is 0004), from src/db/migrations/:
  0000_enable_postgis.sql, 0000_lame_silver_sable.sql, 0001_fixed_sunspot.sql,
  0002_normal_mach_iv.sql, 0003_slippery_prowler.sql

0003_slippery_prowler.sql is the hand-edit precedent for partial indexes + non-generatable DDL.
Runner: tsx src/db/migrate.ts (applies all .sql in src/db/migrations/ alphabetically via Drizzle migrate()).
D-49: `npx drizzle-kit push` is PERMANENTLY UNUSABLE on this project (spatial_ref_sys permission error). Never run it.
DATABASE_URL must be present in the environment for migrate.ts to connect.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: [BLOCKING] decimal.js package legitimacy gate</name>
  <action>Human verifies the decimal.js install (added in Plan 01) on npmjs.com before any migration runs. This gate is not auto-approvable (workflow.auto_advance is ignored for legitimacy checkpoints).</action>
  <what-built>Plan 01 added a net-new dependency: decimal.js (10.6.0). RESEARCH pre-verified it (11-yr-old package, author Mike McFadyen, no postinstall script) but slopcheck was unavailable in the research environment, so the install carries an [ASSUMED] slopcheck verdict (T-07-SC).</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/decimal.js and confirm: weekly downloads in the millions, repository github.com/MikeMcl/decimal.js, latest version 10.6.x, no install/postinstall scripts listed.
    2. Confirm `npm ls decimal.js` resolves to 10.6.x with no unexpected transitive additions.
    3. Approve only after the registry page matches.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the migration, or describe the discrepancy.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Generate migration, hand-edit the CHECK constraint, hand-write the index migration</name>
  <files>src/db/migrations/0004_v2_data_foundation.sql, src/db/migrations/0005_v2_indexes.sql, src/db/migrations/meta/_journal.json, src/db/migrations/meta/0004_snapshot.json</files>
  <read_first>
    - src/db/migrations/0003_slippery_prowler.sql (hand-edit precedent: header marker, partial index syntax, non-generatable DDL)
    - src/db/schema/hakedis-period-lines.ts (the table the CHECK constraint protects — from Plan 01)
    - src/db/schema/submissions.ts (column names referenced by 0005 indexes: status, submitted_at, project_id, decided_by, decided_at, person_id, boq_item_id)
    - .planning/phases/07-data-foundation-canonical-record/07-RESEARCH.md (§ Migration Strategy — Exact Sequence, steps 1–5; the exact CHECK + index SQL)
  </read_first>
  <action>
    Run `npx drizzle-kit generate`. It emits one file `src/db/migrations/0004_<random>.sql` covering all four Plan 01 schema changes (boq_items ALTER + 3 CREATE TABLE). Rename that file to `src/db/migrations/0004_v2_data_foundation.sql` and update the corresponding entry in `src/db/migrations/meta/_journal.json` so the journal tag matches the renamed file (Drizzle tracks by journal entry — keep idx/when consistent with the generated snapshot). Confirm the generated SQL contains `ALTER TABLE "boq_items" ADD COLUMN "unit_price" numeric(15, 4);` and `ALTER TABLE "boq_items" ADD COLUMN "currency_code" text NOT NULL DEFAULT 'TRY';` and the three CREATE TABLE statements.

    Hand-edit 0004_v2_data_foundation.sql: add a header comment `-- HAND-VERIFIED (2026-05-26): CHECK constraint added manually — drizzle-kit cannot emit CHECK on numeric columns` and append at the bottom: `ALTER TABLE "hakedis_period_lines" ADD CONSTRAINT "hakedis_period_lines_cumulative_check" CHECK (cumulative_qty_approved >= previous_cumulative_qty);`

    Create src/db/migrations/0005_v2_indexes.sql BY HAND (drizzle-kit cannot emit partial WHERE indexes) with a header marker `-- HAND-WRITTEN: Phase 7 analytics indexes (partial WHERE indexes not emitted by drizzle-kit generate)`. Include exactly these five indexes from RESEARCH § Migration Strategy step 4: submissions_status_submitted_idx on submissions(status, submitted_at DESC); submissions_pending_idx on submissions(project_id, submitted_at DESC) WHERE status = 'pending_audit'; submissions_decided_by_idx on submissions(decided_by, decided_at DESC) WHERE decided_by IS NOT NULL; submissions_person_status_date_idx on submissions(person_id, status, submitted_at DESC); submissions_project_status_boq_idx on submissions(project_id, status, boq_item_id). Verify every column name exists on the submissions table before writing.

    DO NOT run `npx drizzle-kit push` (D-49). DO NOT apply migrations in this task — application happens in Task 2.
  </action>
  <verify>
    <automated>grep -v '^#' src/db/migrations/0004_v2_data_foundation.sql | grep -c "hakedis_period_lines_cumulative_check" && grep -v '^--' src/db/migrations/0005_v2_indexes.sql | grep -c "WHERE status = 'pending_audit'"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/db/migrations/0004_v2_data_foundation.sql` exists; `grep -c "ADD COLUMN \"unit_price\"" 0004_v2_data_foundation.sql` ≥ 1 and `grep -c "CREATE TABLE \"hakedis_period_lines\"" 0004_v2_data_foundation.sql` ≥ 1.
    - `grep -c "CHECK (cumulative_qty_approved >= previous_cumulative_qty)" src/db/migrations/0004_v2_data_foundation.sql` == 1.
    - `src/db/migrations/0005_v2_indexes.sql` exists with all five index names; exactly two carry a `WHERE` clause (pending + decided_by partial indexes).
    - `src/db/migrations/meta/_journal.json` references the renamed 0004 file; no orphaned random-named SQL remains in the migrations dir.
    - No invocation of `drizzle-kit push` anywhere in this task.
  </acceptance_criteria>
  <done>0004_v2_data_foundation.sql carries the column add, three table creates, and the hand-edited CHECK; 0005_v2_indexes.sql carries the five hand-written indexes; journal updated; nothing applied yet.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Apply migrations to live Neon and prove the schema is real</name>
  <files>src/db/migrations/0004_v2_data_foundation.sql, src/db/migrations/0005_v2_indexes.sql</files>
  <read_first>
    - src/db/migrate.ts (the project migration runner — confirm it points at src/db/migrations and uses Drizzle migrate())
    - .planning/STATE.md (D-49 — drizzle-kit push unusable; migrate.ts is the runner; neon-serverless Pool for transactions)
    - .planning/phases/07-data-foundation-canonical-record/07-RESEARCH.md (§ Migration Strategy step 5)
  </read_first>
  <action>
    Run `tsx src/db/migrate.ts` with DATABASE_URL in the environment. The runner applies 0004_v2_data_foundation.sql then 0005_v2_indexes.sql (alphabetical order — correct: data foundation before indexes that reference the same tables). If migrate.ts errors on a missing DATABASE_URL, surface that as a blocker rather than guessing the value.

    After apply, prove the schema is live (not just config-typed) with direct DB assertions via `tsx` one-liners or psql against DATABASE_URL: (a) `\d boq_items` (or information_schema query) shows unit_price and currency_code columns; (b) the three new tables exist; (c) the constraint `hakedis_period_lines_cumulative_check` exists in pg_constraint; (d) the five indexes exist in pg_indexes for the submissions table; (e) an existing boq_items row (or a freshly inserted one without currency) reads back currency_code = 'TRY'.

    If any assertion fails, the migration did not fully apply — fix the SQL and re-run before declaring done. Do NOT mark complete on a green type-check alone (that is the false-positive trap this plan exists to close).
  </action>
  <verify>
    <automated>npx vitest run tests/schema.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `tsx src/db/migrate.ts` exits 0; migration journal in the DB records 0004 and 0005 as applied.
    - information_schema.columns shows boq_items.unit_price (numeric) and boq_items.currency_code (text) — verified by a live query, not by reading the schema file.
    - pg_constraint contains `hakedis_period_lines_cumulative_check`; an INSERT with cumulative_qty_approved < previous_cumulative_qty raises a Postgres constraint violation (Money-Math Test 5 in tests/schema.test.ts now passes against the live DB).
    - pg_indexes for submissions includes all five Plan-02 index names; submissions_pending_idx and submissions_decided_by_idx carry WHERE predicates.
    - tests/schema.test.ts currency-default, CHECK, and actor-FK blocks PASS (no longer it.todo/skip) against live Neon.
  </acceptance_criteria>
  <done>Migrations applied to live Neon via migrate.ts; columns, tables, CHECK constraint, and five indexes confirmed by direct DB queries; tests/schema.test.ts green against the live schema.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration SQL → live Neon DB | DDL crosses into the production-shaped database |
| npm registry → build | decimal.js supply-chain (carried from Plan 01, gated here) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02 | Tampering | hakedis_period_lines retroactive qty manipulation | mitigate | Hand-edited CHECK (cumulative_qty_approved >= previous_cumulative_qty) applied to live DB; proven by a rejected INSERT |
| T-07-03 | Denial of Service | analytics full-table scans | mitigate | Hand-written partial + composite indexes (0005) applied; overview/scorecard queries hit indexes not seq scans |
| T-07-04 | Tampering | wrong migration runner corrupts schema | mitigate | tsx src/db/migrate.ts only; drizzle-kit push explicitly forbidden (D-49) |
| T-07-SC | Tampering | decimal.js install | mitigate | Blocking human-verify legitimacy checkpoint (Task 0) — not auto-approvable |
</threat_model>

<verification>
- `tsx src/db/migrate.ts` applies 0004 then 0005 with exit 0.
- Direct DB queries confirm columns, three tables, CHECK constraint, and five indexes exist in live Neon.
- `npx vitest run tests/schema.test.ts` green (currency default, CHECK violation, actor FK) against the live schema.
- decimal.js legitimacy checkpoint approved before any migration ran.
</verification>

<success_criteria>
- 0004_v2_data_foundation.sql + 0005_v2_indexes.sql exist, hand-edits present, journal consistent.
- Live Neon has unit_price + currency_code, the three new tables, the CHECK constraint, and the five indexes — verified by live query.
- tests/schema.test.ts passes against the live DB.
</success_criteria>

<output>
Create `.planning/phases/07-data-foundation-canonical-record/07-02-SUMMARY.md` when done.
</output>
