---
phase: 07-data-foundation-canonical-record
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - src/lib/log-office-activity.ts
  - src/actions/analytics.ts
  - tests/analytics.test.ts
autonomous: true
requirements: [COST-02, COST-03, COST-04, COST-05, PERF-03]
nyquist_compliant: true

must_haves:
  truths:
    - "logOfficeActivity() is a synchronous void function using next/server after() to defer a non-blocking INSERT that swallows its own errors"
    - "getCanonicalSubmissions(), getProjectMetrics(), getPersonMetrics(), getPortfolioOverview(), getOfficeActivityLog() are exported, auth-guarded, and tenant-scoped"
    - "All value aggregates are grouped by currency_code and returned as Record<string,string> maps — never a single cross-currency sum"
    - "All money multiplication happens in Postgres (SUM(qty::numeric * price::numeric)); no JS float arithmetic"
    - "Postgres SUM matches decimal.js with no kuruş drift"
  artifacts:
    - path: "src/lib/log-office-activity.ts"
      provides: "logOfficeActivity helper (after()-deferred, error-swallowing)"
      contains: "after("
    - path: "src/actions/analytics.ts"
      provides: "5 typed aggregation functions"
      contains: "getProjectMetrics"
  key_links:
    - from: "src/actions/analytics.ts"
      to: "Neon Postgres"
      via: "db.execute(sql`... GROUP BY b.currency_code`)"
      pattern: "GROUP BY b.currency_code"
    - from: "every analytics export"
      to: "auth + tenant scope"
      via: "await auth() guard + WHERE tenant_id"
      pattern: "await auth\\(\\)"
---

<objective>
Build the money-safe aggregation layer that every downstream v2 phase consumes: the logOfficeActivity() helper and the five typed query functions in analytics.ts. All value math runs in Postgres and is grouped by currency; every function is auth-guarded and tenant-scoped.

Purpose: Provide a stable, money-safe data contract (CanonicalSubmission + currency-grouped metric maps) so Phases 8–11 never re-derive aggregation or risk float drift / cross-currency summation.

Output: src/lib/log-office-activity.ts; src/actions/analytics.ts (getCanonicalSubmissions, getProjectMetrics, getPersonMetrics, getPortfolioOverview, getOfficeActivityLog); analytics.test.ts stubs for COST-02..05 + PERF-03 turned green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-data-foundation-canonical-record/07-RESEARCH.md
@.planning/phases/07-data-foundation-canonical-record/07-PATTERNS.md
@.planning/phases/07-data-foundation-canonical-record/07-VALIDATION.md
@.planning/phases/07-01-PLAN.md

<interfaces>
<!-- Contracts the executor builds against. -->

From src/lib/types (Plan 01): import type { CanonicalSubmission }
From src/db/schema/office-activity-log.ts (Plan 01): officeActivityLog table, type OfficeActionType
From src/lib/tenant.ts: getDefaultTenantId(): string
From src/lib/auth.ts: auth (database-session strategy — session.user.id is populated)
From src/db: db (Drizzle instance)
next/server: after (confirmed present in next 15.5.18)

Analytics analog (auth guard + tenant scope + db.execute raw SQL), from src/actions/submissions.ts:
  const session = await auth(); if (!session) throw new Error('Unauthorized');
  eq(submissions.tenantId, getDefaultTenantId())   // tenant scope on every query
  await db.execute(sql`SELECT ...`)                // raw SQL escape hatch; rows on result.rows

Drizzle numeric columns return as STRINGS — never multiply in JS (Pitfall 1).
Submissions columns available: status, submitted_at, decided_at, decided_by (people.id), person_id,
  project_id, boq_item_id, quantity, location_match, location_warning, location_distance_m, photo_url, notes, rejection_reason.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: logOfficeActivity() helper</name>
  <files>src/lib/log-office-activity.ts</files>
  <read_first>
    - src/actions/boq.ts (db.insert().values() pattern, lines ~56–67 — the insert template)
    - src/db/schema/office-activity-log.ts (officeActivityLog columns + OfficeActionType — from Plan 01)
    - src/lib/tenant.ts (getDefaultTenantId)
    - .planning/phases/07-data-foundation-canonical-record/07-RESEARCH.md (§ logOfficeActivity() — Helper and Wiring; Pitfalls 4 & 5)
    - .planning/phases/07-data-foundation-canonical-record/07-PATTERNS.md (log-office-activity.ts pattern assignment; after() mock pattern)
  </read_first>
  <behavior>
    - Calling logOfficeActivity(params) returns void synchronously (does NOT return a Promise; callers never await it).
    - It schedules a single db.insert into officeActivityLog via next/server after(); the INSERT uses tenantId=getDefaultTenantId() and the passed actorUserId/actionType/entityType/entityId/projectId/metadata (entityId/projectId/metadata default to null).
    - If the deferred INSERT throws, the error is swallowed (try/catch inside the after() callback) and never propagates.
  </behavior>
  <action>
    Create src/lib/log-office-activity.ts (no 'use server' — it is a helper imported BY server actions; it is never a route). Define `type LogParams = { actorUserId: string; actionType: OfficeActionType; entityType: string; entityId?: string; projectId?: string; metadata?: Record<string, unknown> }`. Export `function logOfficeActivity(params: LogParams): void` that calls `after(async () => { try { await db.insert(officeActivityLog).values({ tenantId: getDefaultTenantId(), actorUserId, actionType, entityType, entityId: params.entityId ?? null, projectId: params.projectId ?? null, metadata: params.metadata ?? null }); } catch {} })`. Import `after` from 'next/server', `db` from '@/db', `officeActivityLog` + `OfficeActionType` from '@/db/schema/office-activity-log', `getDefaultTenantId` from '@/lib/tenant'. Add a JSDoc note: call AFTER the primary write succeeds; never await; only valid inside a Server Action / Route Handler request scope (Pitfalls 4 & 5).
  </action>
  <verify>
    <automated>npx vitest run tests/analytics.test.ts -t "non-blocking" 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - logOfficeActivity returns void (signature `: void`); `grep -q "): void" src/lib/log-office-activity.ts`.
    - `grep -q "after(" src/lib/log-office-activity.ts` and the insert is inside the after() callback wrapped in try/catch.
    - The PERF-03 non-blocking test (Money-Math Test 3 — after() mocked to run immediately, insert mocked to throw) passes: createProject still returns ok and the project row exists.
    - No `await logOfficeActivity` anywhere; the function does not return a Promise.
  </acceptance_criteria>
  <done>logOfficeActivity is a synchronous void helper deferring an error-swallowing INSERT via after(); the non-blocking test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: analytics.ts — getCanonicalSubmissions, getProjectMetrics, getOfficeActivityLog</name>
  <files>src/actions/analytics.ts, tests/analytics.test.ts</files>
  <read_first>
    - src/actions/submissions.ts (auth guard, tenant scope, db.execute sql``, multi-join, Promise.all — the exact analog)
    - src/lib/types/canonical-submission.ts (the return shape of getCanonicalSubmissions — from Plan 01)
    - src/db/schema/office-activity-log.ts (for getOfficeActivityLog join to users + projects)
    - .planning/phases/07-data-foundation-canonical-record/07-RESEARCH.md (§ Aggregation Functions — getCanonicalSubmissions, getProjectMetrics, getOfficeActivityLog: exact SQL + signatures)
    - .planning/phases/07-data-foundation-canonical-record/07-VALIDATION.md (Money-Math Tests 1 & 2; COST-02/COST-05 rows)
  </read_first>
  <behavior>
    - getProjectMetrics(projectId) runs two queries in Promise.all: counts/SLA (approved/rejected/pending counts, avg audit latency for decided only, location_warning count, rejection rate) and currency-grouped values (evByCurrency, bacByCurrency, reworkValueByCurrency). EV uses approved submissions × unit_price; BAC uses planned_qty × unit_price; rework uses rejected submissions × unit_price — all SUM in Postgres, GROUP BY currency_code, returned as Record<string,string>.
    - Money-Math Test 1: unit_price '1250.0001', three approved submissions of quantity '333.333' → evByCurrency['TRY'] equals the Postgres numeric result; new Decimal(result).minus(expected).abs() < 0.001.
    - Money-Math Test 2: project with one TRY + one USD BOQ item → evByCurrency has exactly keys TRY and USD and NO 'total' key.
    - COST-05: reworkValueByCurrency counts ONLY rejected submissions.
    - getCanonicalSubmissions(filters) returns CanonicalSubmission[] with unit_price/quantity/earned_value kept as numeric strings (no ::float8); earned_value is NULL when unit_price is NULL; audit_latency_hours is NULL when decided_at IS NULL.
    - getOfficeActivityLog filters by actorUserId and returns entries joined to users.email + projects.name.
  </behavior>
  <action>
    Create src/actions/analytics.ts with `'use server'` header. Import sql from 'drizzle-orm', db from '@/db', auth from '@/lib/auth', getDefaultTenantId from '@/lib/tenant', type CanonicalSubmission from '@/lib/types'. Every exported function starts with `const session = await auth(); if (!session) throw new Error('Unauthorized');` and a `const tenantId = getDefaultTenantId();` used in every WHERE.

    Implement getCanonicalSubmissions(filters: { projectIds?: string[]; from?: Date; to?: Date; status?: 'pending_audit'|'approved'|'rejected'; personId?: string } = {}): Promise<CanonicalSubmission[]> using db.execute(sql`...`) with the join from RESEARCH (submissions s JOIN projects p JOIN people w JOIN boq_items b LEFT JOIN people aud ON aud.id = s.decided_by), earned_value computed as CASE WHEN b.unit_price IS NOT NULL THEN (s.quantity::numeric * b.unit_price::numeric)::text ELSE NULL END, audit_latency_hours as EXTRACT(EPOCH FROM (decided_at - submitted_at))/3600.0 only when decided_at IS NOT NULL, WHERE s.tenant_id = tenantId plus dynamically-appended filter clauses. NO ::float8 cast on unit_price/quantity. Map result.rows to CanonicalSubmission (numeric fields stay strings; timestamps to ISO strings).

    Implement getProjectMetrics(projectId, dateRange?): Promise<ProjectMetrics> per RESEARCH — Query 1 counts/SLA/rejection_rate (no currency grouping), Query 2 currency-grouped EV/BAC/rework (GROUP BY b.currency_code), run via Promise.all, merged in TS into evByCurrency/bacByCurrency/reworkValueByCurrency Record<string,string> maps. Define the ProjectMetrics type exactly as in RESEARCH (no single cross-currency total field).

    Implement getOfficeActivityLog(options?: { actorUserId?; projectId?; limit?; from?; to? }): Promise<ActivityLogEntry[]> joining users (actorEmail) and projects (projectName), default limit 50, ordered by occurred_at DESC, tenant-scoped.

    Turn the corresponding stubs in tests/analytics.test.ts green: COST-02 EV+BAC grouped by currency, Money-Math Test 1 (no drift), Money-Math Test 2 (cross-currency guard — assert keys and absence of 'total'), COST-05 rework rejected-only, PERF-03 getOfficeActivityLog filtered by actorUserId. Seed via the fixture insert pattern (onConflictDoNothing; raw SQL for speed where needed).
  </action>
  <verify>
    <automated>npx vitest run tests/analytics.test.ts -t "COST-02" tests/analytics.test.ts -t "COST-05" tests/analytics.test.ts -t "getOfficeActivityLog" 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "GROUP BY b.currency_code" src/actions/analytics.ts` ≥ 2 (project metrics value query + any other value rollup); no value SUM lacks a currency GROUP BY.
    - `grep -c "::float8" src/actions/analytics.ts` == 0 (no float cast on money/quantity).
    - Every exported function in analytics.ts begins with `const session = await auth()` and includes `tenant_id` / `getDefaultTenantId()` in its query — `grep -c "await auth()" src/actions/analytics.ts` equals the number of exported functions.
    - Money-Math Test 1 passes: Decimal diff < 0.001 between getProjectMetrics().evByCurrency['TRY'] and the expected Postgres result.
    - Money-Math Test 2 passes: return has keys TRY and USD, `expect(result).not.toHaveProperty('total')`.
    - COST-05 test: reworkValueByCurrency reflects rejected-only submissions; getOfficeActivityLog test filters by actorUserId.
  </acceptance_criteria>
  <done>getCanonicalSubmissions, getProjectMetrics, getOfficeActivityLog implemented, auth-guarded, tenant-scoped, currency-grouped, no float math; COST-02/COST-05/PERF-03-log + Money-Math Tests 1 & 2 pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: analytics.ts — getPersonMetrics + getPortfolioOverview (COST-04 + dual-role isolation)</name>
  <files>src/actions/analytics.ts, tests/analytics.test.ts</files>
  <read_first>
    - src/actions/analytics.ts (the file extended — reuse the auth/tenant/db.execute pattern from Task 2)
    - src/db/schema/assignments.ts (role_on_project join for auditor pending backlog + worker scoping)
    - .planning/phases/07-data-foundation-canonical-record/07-RESEARCH.md (§ getPersonMetrics + getPortfolioOverview: exact SQL + signatures; NULL decidedAt handling)
    - .planning/phases/07-data-foundation-canonical-record/07-VALIDATION.md (Money-Math Test 4 — dual-role isolation; COST-04 row)
  </read_first>
  <behavior>
    - getPersonMetrics(personId, options?) returns worker metrics (submissionsApproved/Rejected/Pending, locationComplianceRate) and, when asAuditor, auditor metrics (decisionsCount, avgDecisionLatencyHours for decided only, pendingBacklogCount queried separately so NULL decidedAt never poisons the average).
    - valueContributedByCurrency (COST-04): SUM(quantity::numeric * unit_price::numeric) FILTER (WHERE status='approved' AND unit_price IS NOT NULL) GROUP BY currency_code, scoped to personId — Record<string,string>.
    - Money-Math Test 4 (dual-role isolation): person who is worker on project A (3 submissions) and auditor on project B (5 decisions) → worker metrics reflect project A submissions only; project B auditor decisions do not bleed into worker submission counts.
    - getPortfolioOverview() returns ProjectSummary[] (one entry per project) with contractedValueByCurrency + earnedValueByCurrency maps, tenant-scoped, merged from one-row-per-project-per-currency SQL.
  </behavior>
  <action>
    Extend src/actions/analytics.ts with getPersonMetrics(personId: string, options?: { projectIds?: string[]; asAuditor?: boolean }): Promise<PersonMetrics> and getPortfolioOverview(): Promise<ProjectSummary[]>. Define PersonMetrics and ProjectSummary types exactly as in RESEARCH. Both functions: auth guard + tenant scope.

    getPersonMetrics — worker SQL grouped by person (approved/rejected/pending counts, location_compliance_rate = approved-near / NULLIF(approved,0)); a SEPARATE value-contribution query grouped by currency_code; when asAuditor, a SEPARATE auditor query (decisions_count, avg_decision_latency_hours FILTER decided_at IS NOT NULL) and a SEPARATE pending-backlog COUNT joined through assignments WHERE role_on_project='auditor'. Never compute the auditor average and the pending count in the same aggregate (NULL decidedAt poisoning).

    getPortfolioOverview — single query returning project_id, project_name, currency_code, approved_count, pending_count, contracted_value (SUM planned_qty × unit_price FILTER unit_price IS NOT NULL), earned_value (SUM approved_qty × unit_price FILTER unit_price IS NOT NULL), GROUP BY p.id, p.name, b.currency_code, ORDER BY p.name, b.currency_code; merge rows by projectId into contractedValueByCurrency/earnedValueByCurrency maps.

    Turn green the analytics.test.ts stubs: COST-04 value_contributed grouped by currency, Money-Math Test 4 dual-role isolation, and a getPortfolioOverview test asserting per-project currency maps. Seed a dual-role person fixture (worker on A, auditor on B).
  </action>
  <verify>
    <automated>npx vitest run tests/analytics.test.ts -t "COST-04" tests/analytics.test.ts -t "dual-role" tests/analytics.test.ts -t "getPortfolioOverview" 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - getPersonMetrics and getPortfolioOverview are exported, each beginning with `await auth()` and tenant-scoped.
    - valueContributedByCurrency and the portfolio value fields are Record<string,string>; no single cross-currency total exists on either return type.
    - Auditor avg latency and pending backlog are computed in separate queries — `grep -c "pending_backlog" src/actions/analytics.ts` ≥ 1 and the backlog count is not inside the avg-latency aggregate.
    - Money-Math Test 4 passes: worker submissionsApproved == project-A count; no project-B auditor bleed.
    - getPortfolioOverview test passes: one ProjectSummary per project with currency-keyed value maps.
  </acceptance_criteria>
  <done>getPersonMetrics + getPortfolioOverview implemented with currency-grouped value maps and separated NULL-safe auditor aggregates; COST-04 + dual-role isolation + portfolio tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client/Server Component → analytics Server Action | untrusted caller invokes financial aggregation |
| analytics query → multi-project DB rows | cross-tenant data could leak without scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Elevation of Privilege | analytics.ts exports callable without session | mitigate | Every export starts with `const session = await auth(); if (!session) throw` — grep-gated by acceptance criteria |
| T-07-06 | Information Disclosure | cross-tenant data leak in aggregation | mitigate | Every query includes `WHERE tenant_id = ${getDefaultTenantId()}` (T-04 access control) |
| T-07-07 | Information Disclosure | activity-log PII in getOfficeActivityLog | mitigate | metadata stores entity IDs + numeric values; names/emails reconstructed at READ time by joining users/projects, not stored in the log row |
| T-07-08 | Tampering | float drift / cross-currency sum producing wrong financial figures | mitigate | All multiplication in Postgres numeric; all value SUM GROUP BY currency_code; Money-Math Tests 1 & 2 gate it |
</threat_model>

<verification>
- `npx vitest run tests/analytics.test.ts` green for all COST-02..05 + PERF-03 behaviors including Money-Math Tests 1, 2, 4.
- `grep -c "::float8" src/actions/analytics.ts` == 0; every value SUM has GROUP BY currency_code.
- Every analytics export is auth-guarded and tenant-scoped.
</verification>

<success_criteria>
- logOfficeActivity is a synchronous, after()-deferred, error-swallowing void helper.
- Five aggregation functions exported, auth-guarded, tenant-scoped, returning typed currency-grouped maps.
- No JS float math; no cross-currency summation; analytics test suite green.
</success_criteria>

<output>
Create `.planning/phases/07-data-foundation-canonical-record/07-03-SUMMARY.md` when done.
</output>
