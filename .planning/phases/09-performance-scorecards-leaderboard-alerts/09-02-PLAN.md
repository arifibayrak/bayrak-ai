---
phase: 09-performance-scorecards-leaderboard-alerts
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/admin/KpiCard.tsx
autonomous: true
requirements: [PERF-06]
must_haves:
  truths:
    - "KpiCard accepts a 'warning' valueColor that renders the stat number in amber (D-87 caution state)"
    - "KpiCard accepts an optional alertBadge node rendered as a top-right corner badge (D-87/D-88 inline alert)"
    - "Alert colors use semantic destructive/amber only — never --primary (UI-SPEC hard rule)"
  artifacts:
    - path: "src/components/admin/KpiCard.tsx"
      provides: "warning ValueColor + alertBadge prop"
      contains: "alertBadge"
  key_links:
    - from: "src/components/admin/KpiCard.tsx colorClass()"
      to: "amber text token"
      via: "warning → text-amber-600 mapping"
      pattern: "amber"
---

<objective>
Extend the shared `KpiCard` component with a `'warning'` value color (amber) and an optional `alertBadge` slot rendered at the card's top-right corner. This is the only KpiCard code change in Phase 9 (UI-SPEC) and unblocks the Overview alert wiring in Plan 06 and the auditor SLA-breach card in Plan 05. The component change is purely additive — existing call sites (`valueColor` default, no badge) are unaffected.

Purpose: Provide the inline-alert presentation primitive (D-87) so alert state is expressed via color + corner badge, never a separate panel.
Output: An extended KpiCard with backward-compatible new props.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-performance-scorecards-leaderboard-alerts/09-UI-SPEC.md
@.planning/phases/09-performance-scorecards-leaderboard-alerts/09-PATTERNS.md

<interfaces>
<!-- Current KpiCard contract (verified src/components/admin/KpiCard.tsx). -->
Current (line 15): `type ValueColor = 'default' | 'success' | 'destructive';`
Current (lines 17-24): `interface KpiCardProps { label; subLabel; value: number|string; icon: React.ReactNode; drillHref?; valueColor?: ValueColor; }`
Current colorClass(): success → 'text-emerald-700'; destructive → 'text-destructive'; else 'text-foreground'.
Card JSX wraps in `<Card>` (line 49).

Target extension (09-PATTERNS.md "KpiCard.tsx (extend)" + 09-UI-SPEC.md Surface 5):
- ValueColor gains `'warning'`; colorClass gains `if (color === 'warning') return 'text-amber-600';`
- KpiCardProps gains `alertBadge?: React.ReactNode;`
- When alertBadge present: `<Card className="relative">` and render `<span className="absolute top-2 right-2" aria-label="Alert: threshold exceeded">{alertBadge}</span>`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 'warning' ValueColor and alertBadge prop to KpiCard</name>
  <files>src/components/admin/KpiCard.tsx</files>
  <read_first>
    - src/components/admin/KpiCard.tsx (full file — current ValueColor type, colorClass, KpiCardProps, Card JSX)
    - 09-PATTERNS.md section "src/components/admin/KpiCard.tsx (extend...)" (exact line-level diffs)
    - 09-UI-SPEC.md Surface 5 "Inline alert badge placement" + "Alert color semantics (NEW in Phase 9)" (warning=amber, badge=destructive TriangleAlert, never --primary)
    - 09-UI-SPEC.md "Accessibility" (alertBadge wrapped in span with aria-label; color never the only signal)
  </read_first>
  <behavior>
    - `<KpiCard valueColor="warning" .../>` renders the stat number with the amber class `text-amber-600`
    - `<KpiCard alertBadge={<Badge.../>}  .../>` renders the badge node absolutely positioned at the card top-right; the Card gains `relative`
    - `<KpiCard .../>` with no new props renders identically to before (no `relative` class, no badge span) — backward compatible
    - The alert-badge wrapper carries `aria-label="Alert: threshold exceeded"` (D-87 a11y)
  </behavior>
  <action>
    In `src/components/admin/KpiCard.tsx`: (1) extend the `ValueColor` union to add `'warning'`; (2) in `colorClass()`, add `if (color === 'warning') return 'text-amber-600';` before the final `return 'text-foreground'`; (3) add `alertBadge?: React.ReactNode;` to `KpiCardProps` with an inline comment "optional — absolute top-right corner badge (D-87)"; (4) accept `alertBadge` in the destructured props; (5) conditionally apply `className={alertBadge ? 'relative' : undefined}` to the `<Card>` and, when `alertBadge` is truthy, render `<span className="absolute top-2 right-2" aria-label="Alert: threshold exceeded">{alertBadge}</span>` as the first child of the Card. Do NOT introduce `--primary` anywhere — alert visuals are destructive/amber only (UI-SPEC hard rule). No fenced implementation beyond these directives.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v 'analytics.test\|tenant-settings' | grep -c 'error TS' | grep -qx 0 && grep -q "text-amber-600" src/components/admin/KpiCard.tsx && grep -q "alertBadge" src/components/admin/KpiCard.tsx && echo KPICARD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "'warning'" src/components/admin/KpiCard.tsx` (ValueColor union extended)
    - `grep -q "text-amber-600" src/components/admin/KpiCard.tsx` (warning → amber mapping)
    - `grep -q "alertBadge" src/components/admin/KpiCard.tsx` (new prop)
    - `grep -q "absolute top-2 right-2" src/components/admin/KpiCard.tsx` (corner placement)
    - `grep -q 'aria-label="Alert: threshold exceeded"' src/components/admin/KpiCard.tsx` (a11y)
    - `grep -vc "primary" src/components/admin/KpiCard.tsx` confirms no new --primary usage for alerts (existing non-alert usage, if any, unchanged)
    - `npx tsc --noEmit` reports zero new errors in KpiCard
  </acceptance_criteria>
  <done>KpiCard supports a warning (amber) value color and an optional top-right alertBadge; existing usages compile unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC → KpiCard props | alertBadge is a React node supplied by the Overview RSC (Plan 06), not user input; value/label come from auth+tenant-scoped analytics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-XSS | Information Disclosure (XSS) | KpiCard rendered values | mitigate | React JSX auto-escapes all text children; no `dangerouslySetInnerHTML`; alertBadge is a trusted server-rendered icon node (UI-SPEC) |
| T-09-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs — Badge/TriangleAlert already present from Phase 8 (RESEARCH: zero new packages) |
</threat_model>

<verification>
- `npx tsc --noEmit` passes for KpiCard
- Backward compatibility: existing Overview KpiCard usages (Phase 8) compile without modification
</verification>

<success_criteria>
- 'warning' value color renders amber
- alertBadge renders a top-right corner badge with an aria-label
- No --primary used for alert visuals
- Zero new type errors
</success_criteria>

<output>
Create `.planning/phases/09-performance-scorecards-leaderboard-alerts/09-02-SUMMARY.md` when done
</output>
