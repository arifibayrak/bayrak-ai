---
phase: 09-performance-scorecards-leaderboard-alerts
plan: 04
type: execute
wave: 3
depends_on: ["09-01", "09-03"]
files_modified:
  - src/actions/analytics.ts
  - src/actions/settings.ts
autonomous: true
requirements: [PERF-01, PERF-02, PERF-06]
must_haves:
  truths:
    - "getPersonMetrics returns outputQuantitySum (SUM of approved quantities) for workers (PERF-01, D-78)"
    - "getPersonMetrics returns slaBreachRateDecided as a fraction of decided submissions exceeding the configurable threshold, null when no decided submissions (PERF-02, D-79)"
    - "getStalledProjects returns projects with no approved submission in N days, point-in-time from NOW (PERF-06, D-88)"
    - "getTenantSettings returns the seeded Moderate defaults; updateTenantSettings upserts tenant-scoped, zod-validated, office-engineer-only (PERF-06, D-83/D-84/D-89)"
  artifacts:
    - path: "src/actions/analytics.ts"
      provides: "outputQuantitySum + slaBreachRateDecided fields + getStalledProjects"
      contains: "getStalledProjects"
    - path: "src/actions/settings.ts"
      provides: "getTenantSettings + updateTenantSettings"
      contains: "updateTenantSettings"
  key_links:
    - from: "getPersonMetrics auditor branch"
      to: "auditSlaHours bound SQL param"
      via: "FILTER on (decided_at - submitted_at) hours > threshold (CR-03 bound param)"
      pattern: "auditSlaHours"
    - from: "updateTenantSettings"
      to: "tenant_settings table"
      via: "auth() + WHERE tenant_id + zod parse + INSERT ON CONFLICT DO UPDATE (D-89)"
      pattern: "ON CONFLICT"
    - from: "getStalledProjects"
      to: "submissions/projects"
      via: "NOT EXISTS approved within thresholdDate, point-in-time NOT date-filtered (D-66/D-88)"
      pattern: "NOT EXISTS"
---

<objective>
Extend the analytics data layer and create the settings server actions — the parameter-pure aggregation tier that every Phase-9 UI surface consumes. Adds the worker output-quantity sum (PERF-01), the auditor SLA-breach rate (PERF-02), the stalled-projects query (PERF-06), and the tenant-settings read/write actions (PERF-06). All functions follow the established `auth()` + `WHERE tenant_id = ${tenantId}` + bound-`sql\`\`` pattern with money/numeric math in Postgres. This plan turns the Wave-0 test blocks (Plan 01) green.

Purpose: Provide a stable, tenant-scoped, money-safe data contract for the scorecard, leaderboard, and alert surfaces.
Output: Extended analytics.ts and a new settings.ts action file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-performance-scorecards-leaderboard-alerts/09-RESEARCH.md
@.planning/phases/09-performance-scorecards-leaderboard-alerts/09-PATTERNS.md

<interfaces>
<!-- Verified from src/actions/analytics.ts. -->
Current PersonMetrics type (lines 50-64): personId, displayName, submissionsApproved, submissionsRejected,
submissionsPending, locationComplianceRate, valueContributedByCurrency, decisionsCount?, avgDecisionLatencyHours?, pendingBacklogCount?.
ADD optional: `outputQuantitySum?: string | null;`  `slaBreachRateDecided?: number | null;`

Current getPersonMetrics signature (line 661): `(personId: string, options?: { projectIds?: string[]; asAuditor?: boolean; dateRange?: { from: Date; to: Date } })`.
ADD to options: `auditSlaHours?: number`.

Auth+tenant template (lines 665-667): `const session = await auth(); if (!session) throw new Error('Unauthorized'); const tenantId = getDefaultTenantId();`
Existing auditor latency aggregate (lines ~743-755) already uses `FILTER (WHERE s.decided_at IS NOT NULL)` — copy this shape for SLA breach.
getOfficeActivityLog (line 580) already exists — do NOT touch (PERF-03 read path used by Plan 05).

Settings analog: src/actions/projects.ts ('use server', z import, auth guard, getDefaultTenantId, revalidatePath).
Upsert pattern (RESEARCH Code Examples): INSERT ... ON CONFLICT (tenant_id) DO UPDATE SET ... = EXCLUDED... .
Zod schema (RESEARCH Pattern 8): auditSlaHours int 1..720; rejectionRateThreshold 0..1; stalledDays int 1..365.

Neon-http single-element array bug (STATE D-NeonArrayFix): if filters.projectIds has exactly one element, use `= ${projectIds[0]}` not `= ANY(${projectIds})`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend getPersonMetrics with outputQuantitySum + slaBreachRateDecided</name>
  <files>src/actions/analytics.ts</files>
  <read_first>
    - src/actions/analytics.ts lines 50-64 (PersonMetrics type), 661-779 (getPersonMetrics worker Query 1 + auditor Query 3 + result mapping)
    - 09-PATTERNS.md section "src/actions/analytics.ts (extend...)" (exact type + signature + SELECT additions + mapping)
    - 09-RESEARCH.md Pattern 3 (SLA-breach SQL), Pattern 4 (output-quantity sum + throughput), Pitfall 2 (denominator = decided only), Pitfall 7 (auditSlaHours in options object)
    - tests/analytics.test.ts (the PERF-01/02 describe block from Plan 01 — these assertions must pass)
  </read_first>
  <behavior>
    - getPersonMetrics(personId, { asAuditor: false }) returns outputQuantitySum = String(SUM of quantity WHERE status='approved'); null when no approved submissions
    - getPersonMetrics(personId, { asAuditor: true, auditSlaHours: 48 }) returns slaBreachRateDecided = (count of decided submissions whose (decided_at - submitted_at) hours > 48) / (count of decided submissions); a fraction 0..1
    - slaBreachRateDecided is null when auditSlaHours is undefined OR when there are no decided submissions (NULLIF denominator)
    - Existing call sites that omit auditSlaHours still compile and behave as before
  </behavior>
  <action>
    In `src/actions/analytics.ts`: add `outputQuantitySum?: string | null;` and `slaBreachRateDecided?: number | null;` to the `PersonMetrics` type. Add `auditSlaHours?: number` to the `getPersonMetrics` options object (optional — existing call sites unaffected, Pitfall 7). In the worker Query 1 SELECT, add `SUM(s.quantity::numeric) FILTER (WHERE s.status = 'approved') AS output_quantity_sum` and map the result to `metrics.outputQuantitySum = workerRow?.output_quantity_sum != null ? String(...) : null`. In the auditor Query 3 SELECT, add the SLA-breach aggregate: `COUNT(*) FILTER (WHERE s.decided_at IS NOT NULL AND (EXTRACT(EPOCH FROM (s.decided_at - s.submitted_at)) / 3600.0) > ${options?.auditSlaHours ?? null})::float / NULLIF(COUNT(*) FILTER (WHERE s.decided_at IS NOT NULL), 0) AS sla_breach_rate` — `${auditSlaHours}` is a BOUND param (CR-03), never sql.raw(). Map: `metrics.slaBreachRateDecided = (auditorRow?.sla_breach_rate != null && options?.auditSlaHours != null) ? Number(...) : null`. Denominator is decided-only (Pitfall 2). Do not change the existing latency/backlog logic.
  </action>
  <verify>
    <automated>npx vitest run tests/analytics.test.ts -t "getPersonMetrics enrichments" 2>&1 | grep -Eq "passed|✓" && echo PERSONMETRICS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "outputQuantitySum" src/actions/analytics.ts` AND `grep -q "slaBreachRateDecided" src/actions/analytics.ts`
    - `grep -q "output_quantity_sum" src/actions/analytics.ts` (SQL alias)
    - `grep -q "sla_breach_rate" src/actions/analytics.ts` AND the threshold is bound (`grep -q 'auditSlaHours' src/actions/analytics.ts`), with no `sql.raw` used for the threshold
    - `grep -q "auditSlaHours" src/actions/analytics.ts` in the options type
    - `npx vitest run tests/analytics.test.ts -t "getPersonMetrics enrichments"` passes (outputQuantitySum, slaBreachRateDecided fraction, null-safe denominator)
  </acceptance_criteria>
  <done>getPersonMetrics returns the worker output-quantity sum and the auditor SLA-breach rate (null-safe), with the threshold as a bound param.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add getStalledProjects to analytics.ts</name>
  <files>src/actions/analytics.ts</files>
  <read_first>
    - src/actions/analytics.ts lines 580-660 (getOfficeActivityLog — auth+tenant guard + bound-param sql`` + result.rows.map shape to mirror)
    - 09-RESEARCH.md Pattern 5 (stalled-projects NOT EXISTS query), Anti-Patterns ("do NOT date-filter stalled"), Pitfall 6 (neon-http single-element array)
    - 09-PATTERNS.md "New getStalledProjects function" note (point-in-time from NOW, D-66/D-88)
    - tests/analytics.test.ts (the PERF-06: getStalledProjects describe block from Plan 01)
  </read_first>
  <behavior>
    - getStalledProjects(7) returns a project whose most recent approved submission is older than 7 days
    - A project with an approved submission within the last 7 days is NOT returned
    - A project that has never had any submission is NOT returned
    - The query uses NOW-anchored thresholdDate, NOT the date filter (point-in-time per D-66/D-88)
  </behavior>
  <action>
    Add `export async function getStalledProjects(stalledDays: number, filters?: { projectIds?: string[] }): Promise<{ projectId: string; projectName: string }[]>` to `src/actions/analytics.ts`. Begin with the auth + tenant guard template. Compute `thresholdDate = new Date(Date.now() - stalledDays * 24 * 60 * 60 * 1000)`. Run a bound-param `db.execute(sql\`...\`)`: select `p.id AS project_id, p.name AS project_name FROM projects p WHERE p.tenant_id = ${tenantId} AND EXISTS (SELECT 1 FROM submissions s WHERE s.project_id = p.id AND s.tenant_id = ${tenantId}) AND NOT EXISTS (SELECT 1 FROM submissions s WHERE s.project_id = p.id AND s.tenant_id = ${tenantId} AND s.status = 'approved' AND s.decided_at >= ${thresholdDate.toISOString()}) ORDER BY p.name`. If `filters?.projectIds` is provided, append a bound condition; when it has exactly one element use `= ${projectIds[0]}` (Pitfall 6 neon-http), else `= ANY(${projectIds})`. Map rows to `{ projectId: String(r.project_id), projectName: String(r.project_name) }`. Do NOT apply the date-range filter to this query (stalled is always point-in-time from NOW — D-88).
  </action>
  <verify>
    <automated>npx vitest run tests/analytics.test.ts -t "getStalledProjects" 2>&1 | grep -Eq "passed|✓" && echo STALLED_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export async function getStalledProjects" src/actions/analytics.ts`
    - `grep -q "NOT EXISTS" src/actions/analytics.ts` (no recent approval sub-select)
    - `grep -q "thresholdDate" src/actions/analytics.ts` (NOW-anchored, not date-filter)
    - The function does NOT reference the dateRange/from/to filter (point-in-time per D-66/D-88)
    - `npx vitest run tests/analytics.test.ts -t "getStalledProjects"` passes all three cases
  </acceptance_criteria>
  <done>getStalledProjects returns NOW-anchored stalled projects, excludes recent-approval and no-submission projects, and is tenant-scoped + auth-guarded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create settings.ts — getTenantSettings + updateTenantSettings (zod, upsert, tenant-scoped)</name>
  <files>src/actions/settings.ts</files>
  <read_first>
    - src/actions/projects.ts (analog — 'use server', z import, auth guard, getDefaultTenantId, zod schema, revalidatePath, return shape)
    - src/actions/analytics.ts lines 665-667 (auth+tenant guard template)
    - 09-RESEARCH.md Pattern 8 (settings form/action), Code Examples (upsert SQL, zod schema), Security Domain (V4 access control, V5 input validation), Pitfall 1 (defaults must be seeded — read returns the row)
    - 09-PATTERNS.md section "src/actions/settings.ts (service, CRUD)" (full pattern: getTenantSettings + updateTenantSettings)
    - tests/analytics.test.ts (the PERF-06: getTenantSettings/updateTenantSettings describe block from Plan 01)
  </read_first>
  <behavior>
    - getTenantSettings() returns { auditSlaHours: 48, rejectionRateThreshold: '0.3000' (or number), stalledDays: 7 } from the seeded row for the default tenant
    - updateTenantSettings({ auditSlaHours, rejectionRateThreshold, stalledDays }) upserts the row for the current tenant and returns { ok: true }
    - updateTenantSettings throws 'Unauthorized' when auth() is null (D-89 office-engineer-only)
    - zod rejects out-of-range values (auditSlaHours 0 or 9999, rejectionRateThreshold > 1, stalledDays 0)
  </behavior>
  <action>
    Create `src/actions/settings.ts` with `'use server'` following the projects.ts header (imports: zod, revalidatePath from next/cache, db from '@/db', auth from '@/lib/auth', getDefaultTenantId from '@/lib/tenant'). Implement `getTenantSettings()`: auth guard + tenant scope, then `db.execute(sql\`SELECT audit_sla_hours, rejection_rate_threshold, stalled_days FROM tenant_settings WHERE tenant_id = ${tenantId}\`)`, returning a typed object mapped from the row; since Plan 03 seeds the default row, the row is expected to exist (Pitfall 1) — if absent, return the D-84 defaults as a safe fallback. Implement `updateTenantSettings(input)`: auth guard (throws 'Unauthorized' — D-89), parse with the zod schema `{ auditSlaHours: z.number().int().min(1).max(720), rejectionRateThreshold: z.number().min(0).max(1), stalledDays: z.number().int().min(1).max(365) }`, then upsert via `db.execute(sql\`INSERT INTO tenant_settings (tenant_id, audit_sla_hours, rejection_rate_threshold, stalled_days, updated_at) VALUES (${tenantId}, ${parsed.auditSlaHours}, ${parsed.rejectionRateThreshold}, ${parsed.stalledDays}, NOW()) ON CONFLICT (tenant_id) DO UPDATE SET audit_sla_hours = EXCLUDED.audit_sla_hours, rejection_rate_threshold = EXCLUDED.rejection_rate_threshold, stalled_days = EXCLUDED.stalled_days, updated_at = NOW()\`)` — all values bound params (CR-03). Call `revalidatePath('/dashboard/overview')` and `revalidatePath('/dashboard/settings')`. Return `{ ok: true }`. Export a `TenantSettings` type for consumers.
  </action>
  <verify>
    <automated>npx vitest run tests/analytics.test.ts -t "getTenantSettings" 2>&1 | grep -Eq "passed|✓" && echo SETTINGS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export async function getTenantSettings" src/actions/settings.ts`
    - `grep -q "export async function updateTenantSettings" src/actions/settings.ts`
    - `grep -q "ON CONFLICT (tenant_id)" src/actions/settings.ts` (upsert)
    - `grep -q "z.number().int().min(1).max(720)" src/actions/settings.ts` (SLA zod) AND `grep -q "min(0).max(1)" src/actions/settings.ts` (rate zod)
    - `grep -q "WHERE tenant_id = " src/actions/settings.ts` (tenant scope — V4)
    - `grep -q "revalidatePath('/dashboard/overview')" src/actions/settings.ts` AND `grep -q "revalidatePath('/dashboard/settings')" src/actions/settings.ts`
    - `npx vitest run tests/analytics.test.ts -t "getTenantSettings"` passes (defaults, upsert, auth guard, zod rejection)
  </acceptance_criteria>
  <done>settings.ts exposes tenant-scoped, auth-guarded, zod-validated getTenantSettings + updateTenantSettings with an idempotent upsert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client form → updateTenantSettings | Office engineer submits threshold values across the server-action boundary (untrusted input) |
| RSC pages → analytics/settings reads | Auth.js session establishes identity; tenant scope confines data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-EoP | Elevation of Privilege | updateTenantSettings | mitigate | `auth()` guard throws 'Unauthorized' before any DB access (D-89 office-engineer-only) |
| T-09-04-T | Tampering | injected threshold values (negative SLA, >100% rate) | mitigate | Zod: `int().min(1).max(720)` hours, `min(0).max(1)` rate, `int().min(1).max(365)` days — reject out-of-range (V5) |
| T-09-04-ID | Information Disclosure | cross-tenant settings/stalled read | mitigate | `WHERE tenant_id = ${getDefaultTenantId()}` as first condition in every query (V4); all values bound params (CR-03) |
| T-09-04-SQLi | Tampering | analytics/settings sql`` | mitigate | All caller-supplied values (auditSlaHours, projectIds, thresholds) bound via Drizzle `sql\`\`` params; never sql.raw() for user input (CR-03) |
| T-09-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages (RESEARCH Package Legitimacy Audit: zero installs; zod/drizzle already present) |
</threat_model>

<verification>
- `npx vitest run tests/analytics.test.ts` — PERF-01/02 enrichment, getStalledProjects, getTenantSettings/updateTenantSettings blocks all green
- All new queries auth-guarded + tenant-scoped + bound params (CR-03)
- Stalled query is point-in-time (no date filter)
</verification>

<success_criteria>
- outputQuantitySum + slaBreachRateDecided computed correctly (null-safe)
- getStalledProjects NOW-anchored and tenant-scoped
- getTenantSettings/updateTenantSettings tenant-scoped, zod-validated, auth-guarded, idempotent upsert
</success_criteria>

<output>
Create `.planning/phases/09-performance-scorecards-leaderboard-alerts/09-04-SUMMARY.md` when done
</output>
