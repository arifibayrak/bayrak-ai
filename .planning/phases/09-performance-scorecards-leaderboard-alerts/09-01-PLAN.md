---
phase: 09-performance-scorecards-leaderboard-alerts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema/tenant-settings.ts
  - src/db/schema/index.ts
  - tests/fixtures/db.ts
  - tests/analytics.test.ts
autonomous: true
requirements: [PERF-06]
must_haves:
  truths:
    - "A tenant_settings Drizzle table is defined with auditSlaHours, rejectionRateThreshold, stalledDays columns and a unique tenant_id (D-83/D-84)"
    - "The new table is exported from the schema barrel so Drizzle queries can reference it"
    - "tenant_settings is registered in the test truncate helper so integration tests reset cleanly"
    - "Failing Wave-0 test blocks exist for all new Phase-9 behaviors (PERF-01/02/05/06)"
  artifacts:
    - path: "src/db/schema/tenant-settings.ts"
      provides: "tenantSettings table definition (Moderate defaults per D-84)"
      contains: "tenantSettings"
    - path: "tests/analytics.test.ts"
      provides: "Wave-0 failing describe blocks for new analytics behaviors"
      contains: "getStalledProjects"
  key_links:
    - from: "src/db/schema/index.ts"
      to: "src/db/schema/tenant-settings.ts"
      via: "export * re-export"
      pattern: "tenant-settings"
    - from: "tests/fixtures/db.ts"
      to: "tenant_settings table"
      via: "truncate list entry (D-84 settings reset between tests)"
      pattern: "tenant_settings"
---

<objective>
Author the `tenant_settings` schema file (D-83 admin-configurable alert thresholds with D-84 Moderate defaults), register it in the schema barrel and the test truncate helper, and lay down the failing Wave-0 test scaffolds for every new Phase-9 behavior. This is the foundation plan — no migration is applied here (that is the [BLOCKING] Plan 03); this plan only defines the schema source-of-truth and the test contracts that later plans turn green.

Purpose: Establish the configurable-threshold data model and a deterministic test surface so downstream analytics/UI plans build against a fixed contract.
Output: One new schema file, an updated barrel, an updated truncate helper, and failing test blocks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-performance-scorecards-leaderboard-alerts/09-CONTEXT.md
@.planning/phases/09-performance-scorecards-leaderboard-alerts/09-PATTERNS.md
@.planning/phases/09-performance-scorecards-leaderboard-alerts/09-RESEARCH.md

<interfaces>
<!-- Existing schema analog — copy pgTable column shape, add FK + integer + numeric columns. -->
From src/db/schema/tenants.ts:
```
export const tenants = pgTable('tenants', {
  id: uuid('id').primaryKey().defaultRandom(),
  name: text('name').notNull(),
  createdAt: timestamp('created_at', { withTimezone: true }).defaultNow().notNull(),
});
```

Target shape (RESEARCH Pattern 2 / PATTERNS tenant-settings.ts):
columns: id (uuid pk defaultRandom), tenantId (uuid notNull references tenants.id .unique()),
auditSlaHours (integer notNull default 48), rejectionRateThreshold (numeric {precision:5,scale:4} notNull default '0.3000'),
stalledDays (integer notNull default 7), updatedAt (timestamp withTimezone defaultNow notNull).

Existing barrel ordering note (src/db/schema/index.ts): append AFTER hakedis-period-lines; tenant_settings references tenants only.

Test fixtures truncate ordering note (tests/fixtures/db.ts): tenant_settings references tenants → must truncate BEFORE tenants, alongside other tenant-referencing children.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author tenant_settings schema + register in barrel and truncate helper</name>
  <files>src/db/schema/tenant-settings.ts, src/db/schema/index.ts, tests/fixtures/db.ts</files>
  <read_first>
    - src/db/schema/tenants.ts (analog — pgTable column shape to copy)
    - src/db/schema/office-activity-log.ts (analog — tenant-referencing table with FK + index conventions)
    - src/db/schema/index.ts (barrel — append re-export in dependency order)
    - tests/fixtures/db.ts (truncate list + phaseN fallback sets — must add tenant_settings)
    - 09-PATTERNS.md section "src/db/schema/tenant-settings.ts" (exact target column shape)
    - 09-RESEARCH.md Pattern 2 (schema design rationale, D-84 defaults)
  </read_first>
  <behavior>
    - Importing `tenantSettings` from '@/db/schema' resolves to a defined pgTable (no missing-export error)
    - The table has exactly these columns: id, tenantId (unique FK to tenants.id), auditSlaHours (default 48), rejectionRateThreshold (numeric 5,4 default '0.3000'), stalledDays (default 7), updatedAt
    - `truncateAllTables` includes 'tenant_settings' and does not throw "relation does not exist" before migration lands (graceful fallback set membership)
  </behavior>
  <action>
    Create `src/db/schema/tenant-settings.ts` defining `tenantSettings` via `pgTable('tenant_settings', {...})` following the tenants.ts column pattern: `id` uuid primaryKey defaultRandom; `tenantId` uuid notNull references `() => tenants.id` with `.unique()`; `auditSlaHours` integer notNull default 48; `rejectionRateThreshold` numeric with `{ precision: 5, scale: 4 }` notNull default `'0.3000'` (string literal, NOT 0.3 — Pitfall 5); `stalledDays` integer notNull default 7; `updatedAt` timestamp `{ withTimezone: true }` defaultNow notNull. Import `pgTable, uuid, integer, numeric, timestamp` from 'drizzle-orm/pg-core' and `tenants` from './tenants'. Defaults match D-84 Moderate thresholds.
    In `src/db/schema/index.ts`, append `export * from './tenant-settings';` after the hakedis-period-lines line, with a trailing comment noting it references tenants.
    In `tests/fixtures/db.ts`, add `'tenant_settings'` to the `tables` array positioned BEFORE `'tenants'` (tenant-referencing child) and add it to a Phase-9 fallback set (mirror the existing `phase7Tables` graceful-degradation pattern so tests pass before Plan 03 migrates the table) — i.e. register a `phase9Tables = ['tenant_settings']` and include it in the `laterTables` Set.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v 'tenant-settings\|analytics.test' | grep -c 'error TS' | grep -qx 0 && echo TYPECHECK_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "pgTable('tenant_settings'" src/db/schema/tenant-settings.ts` succeeds
    - `grep -q "rejectionRateThreshold" src/db/schema/tenant-settings.ts` AND the default is the string `'0.3000'` not a float literal (`grep -q "default('0.3000')" src/db/schema/tenant-settings.ts`)
    - `grep -q ".unique()" src/db/schema/tenant-settings.ts` (one row per tenant)
    - `grep -q "tenant-settings" src/db/schema/index.ts`
    - `grep -q "tenant_settings" tests/fixtures/db.ts`
  </acceptance_criteria>
  <done>tenantSettings table is defined with D-84 defaults, exported from the barrel, and registered in the truncate helper with a graceful pre-migration fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Lay down failing Wave-0 test blocks for all new Phase-9 behaviors</name>
  <files>tests/analytics.test.ts</files>
  <read_first>
    - tests/analytics.test.ts lines 1-80 (file header mocks: next/cache, @/lib/auth, next/server after(); describeIfDb + getTestDb + truncateAllTables pattern; sql.raw seed pattern)
    - tests/fixtures/db.ts (getTestDb, describeIfDb, truncateAllTables exports)
    - 09-PATTERNS.md section "tests/analytics.test.ts (extend: new test blocks)" (the 4 describe blocks to add + seed/auth-guard patterns)
    - 09-RESEARCH.md "Validation Architecture" → "Wave 0 Gaps" (exact behaviors to assert)
  </read_first>
  <action>
    Extend `tests/analytics.test.ts` with four new test blocks following the existing `describeIfDb` + `vi.mock` conventions already in the file (do NOT re-declare the file-level mocks if present; reuse them). Add:
    1. `describeIfDb('PERF-01/02: getPersonMetrics enrichments')` — assert `getPersonMetrics(personId, { asAuditor: false })` returns `outputQuantitySum` equal to the SUM of approved quantities (seed an approved submission with a known qty); assert auditor branch with `auditSlaHours` returns `slaBreachRateDecided` as a fraction; assert `slaBreachRateDecided` is `null` when no decided submissions exist.
    2. `describeIfDb('PERF-06: getStalledProjects')` — assert a project whose last approved submission is older than N days is returned; a project with a recent approval is NOT returned; a project with no submissions at all is NOT returned.
    3. `describeIfDb('PERF-06: getTenantSettings / updateTenantSettings')` — assert `getTenantSettings()` returns the seeded Moderate defaults (48 / 0.3 / 7); assert `updateTenantSettings` upserts new values and is tenant-scoped; assert it throws 'Unauthorized' when `auth()` is null; assert zod rejects out-of-range input (e.g. auditSlaHours = 0 or 9999).
    4. `describe('PERF-05: leaderboard sort')` (plain describe — pure TS sort, no DB) — assert workers default-sort by `submissionsApproved` DESC, auditors default-sort by `avgDecisionLatencyHours` ASC, ties broken by `displayName`. Import the sort helpers from where Plan 05 will define them; if not yet importable, write the assertions against the documented helper signatures so they fail with a clear "not a function"/import error (RED state). These functions are GREEN in Plans 04/05.
    Use `sql.raw` INSERT seeding for tenants/projects/people/boq_items/submissions exactly as the existing blocks do. Mark these blocks so the suite reports them as failing until the corresponding functions ship (they are the Nyquist scaffold).
  </action>
  <verify>
    <automated>npx vitest run tests/analytics.test.ts 2>&1 | grep -Eq 'getStalledProjects|getTenantSettings|leaderboard sort' && echo SCAFFOLD_PRESENT</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "describeIfDb('PERF-06: getStalledProjects'" tests/analytics.test.ts` returns 1
    - `grep -q "getTenantSettings" tests/analytics.test.ts` AND `grep -q "updateTenantSettings" tests/analytics.test.ts`
    - `grep -q "outputQuantitySum" tests/analytics.test.ts` AND `grep -q "slaBreachRateDecided" tests/analytics.test.ts`
    - `grep -q "leaderboard sort" tests/analytics.test.ts`
    - Running `npx vitest run tests/analytics.test.ts` shows the new blocks present (failing is expected at this wave — they are scaffolds)
  </acceptance_criteria>
  <done>Four Wave-0 test blocks exist covering outputQuantitySum, slaBreachRateDecided (incl. null denominator), getStalledProjects, getTenantSettings/updateTenantSettings (incl. auth + zod), and leaderboard sort defaults/ties.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration/seed → DB | The seed INSERT (added in Plan 03) writes a default tenant_settings row; this plan only defines the schema and unique constraint that prevents duplicate per-tenant rows |
| test fixtures → test DB | Seed inserts in tests use `sql.raw` with literal UUIDs (test-only, not user input) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-T | Tampering | tenant_settings table | mitigate | `.unique()` on tenant_id enforced at schema + migration level (Plan 03) prevents conflicting duplicate threshold rows per tenant (D-83) |
| T-09-01-I | Information Disclosure | cross-tenant settings | mitigate | tenantId is a notNull FK; all reads/writes (Plan 04) filter `WHERE tenant_id = ${tenantId}` — schema makes tenant scoping mandatory |
| T-09-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan or phase (RESEARCH Package Legitimacy Audit: zero new packages) |
</threat_model>

<verification>
- `npx tsc --noEmit` passes (excluding intentionally-failing test scaffolds referencing not-yet-shipped functions)
- `tenantSettings` is importable from '@/db/schema'
- The four Wave-0 test blocks are present and execute (failing is expected until Plans 03/04/05 land)
</verification>

<success_criteria>
- tenant_settings schema file authored with D-84 Moderate defaults and a unique tenant_id constraint
- Barrel re-exports the new table
- Truncate helper resets tenant_settings with a pre-migration graceful fallback
- Failing Wave-0 test blocks scaffold all new Phase-9 behaviors
</success_criteria>

<output>
Create `.planning/phases/09-performance-scorecards-leaderboard-alerts/09-01-SUMMARY.md` when done
</output>
