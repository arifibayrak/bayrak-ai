---
phase: 09-performance-scorecards-leaderboard-alerts
plan: 05
type: execute
wave: 4
depends_on: ["09-02", "09-04"]
files_modified:
  - src/app/dashboard/(admin)/people/[personId]/page.tsx
  - src/app/dashboard/(admin)/people/page.tsx
  - src/app/dashboard/(admin)/analytics/page.tsx
  - src/app/dashboard/(admin)/analytics/office-engineers/[userId]/page.tsx
  - src/components/admin/LeaderboardSortSelect.tsx
  - messages/en.json
  - messages/tr.json
autonomous: true
requirements: [PERF-01, PERF-02, PERF-03, PERF-05]
must_haves:
  truths:
    - "Worker profile shows Output Volume + Approval Rate KPI cards scoped per-project and across all projects (PERF-01, D-77/D-78)"
    - "Auditor profile shows an SLA-Breach-Rate KPI card colored by severity (PERF-02, D-79)"
    - "The People directory ranks Workers and Auditors by a selectable metric with a rank column (PERF-05, D-81/D-82)"
    - "A read-only office-engineer scorecard lists logged actions with timestamps from office_activity_log (PERF-03, D-80 revised)"
  artifacts:
    - path: "src/app/dashboard/(admin)/people/[personId]/page.tsx"
      provides: "enriched worker + auditor scorecard cards"
      contains: "outputQuantitySum"
    - path: "src/app/dashboard/(admin)/people/page.tsx"
      provides: "leaderboard rank column + sort"
      contains: "sortBy"
    - path: "src/app/dashboard/(admin)/analytics/office-engineers/[userId]/page.tsx"
      provides: "read-only OE activity scorecard"
      contains: "getOfficeActivityLog"
  key_links:
    - from: "people/[personId]/page.tsx"
      to: "getTenantSettings().auditSlaHours"
      via: "passed to getPersonMetrics for auditor SLA breach (D-79/D-84)"
      pattern: "auditSlaHours"
    - from: "people/page.tsx"
      to: "getPortfolioPeople result"
      via: "TS sort by sortBy URL param + rank assignment (D-81/D-82, no new SQL)"
      pattern: "sortBy"
    - from: "analytics/office-engineers/[userId]/page.tsx"
      to: "getOfficeActivityLog({ actorUserId })"
      via: "read-only activity table (D-80; reuse Phase-7 data layer, no rebuild)"
      pattern: "actorUserId"
---

<objective>
Deliver the three role scorecards and the leaderboard. Enrich the Phase-8 person profile with the worker Output-Volume + Approval-Rate cards (PERF-01) and the auditor SLA-Breach-Rate card (PERF-02); add a sortable, ranked mode to the People directory (PERF-05); and build a NEW minimal read-only office-engineer scorecard view under the Analytics namespace (PERF-03, D-80 revised — reusing the Phase-7 `office_activity_log` data layer, not rebuilding it). All new strings are keyed in TR/EN.

Purpose: Turn the per-person metrics (Plan 04) into full role scorecards and a comparison leaderboard, completing PERF-01/02/03/05.
Output: Enriched profile page, leaderboard directory, OE scorecard route + Analytics entry point, a sort-select client component, and i18n additions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-performance-scorecards-leaderboard-alerts/09-UI-SPEC.md
@.planning/phases/09-performance-scorecards-leaderboard-alerts/09-PATTERNS.md
@.planning/phases/09-performance-scorecards-leaderboard-alerts/09-RESEARCH.md

<interfaces>
<!-- Verified data-layer contracts (from Plan 04 + existing analytics.ts). -->
getPersonMetrics(personId, { asAuditor, dateRange, projectIds, auditSlaHours }) →
  PersonMetrics incl. outputQuantitySum?: string|null, slaBreachRateDecided?: number|null (Plan 04).
getTenantSettings() → { auditSlaHours, rejectionRateThreshold, stalledDays } (Plan 04 settings.ts).
getPortfolioPeople({ role, dateRange, projectIds }) → PortfolioWorker[] / PortfolioAuditor[] (existing; bulk, no N+1).
  PortfolioWorker has submissionsApproved, submissionsRejected, displayName, value-by-currency.
  PortfolioAuditor has avgDecisionLatencyHours, decisionsCount, pendingBacklogCount, displayName.
getOfficeActivityLog({ actorUserId, projectId, limit, from, to }) → ActivityLogEntry[] (Phase 7; actorEmail, actionType, entityType, projectName, occurredAt). DO NOT rebuild.

Office engineers live in the `users` table (auth.ts: id text PK, name, email) — NOT in `people`.
office_activity_log.action_type ∈ OFFICE_ACTION_TYPES (office-activity-log.ts): project_created, project_updated, boq_imported, unit_price_set, person_approved, person_assigned, hakedis_period_created, hakedis_period_finalized, etc.

KpiCard (Plan 02): valueColor ∈ default|success|destructive|warning; optional alertBadge.
LeaderboardSortSelect: 'use client', useRouter+useSearchParams, wrapped in <Suspense> (FilterBar pattern).

i18n namespaces this plan OWNS (must NOT touch settings.* / new overview.* — those are Plan 06):
  dashboard.admin.scorecard.*  dashboard.admin.leaderboard.*  dashboard.admin.oe_scorecard.*
  + the rank "#" header under dashboard.admin.people.*
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enrich person profile with worker + auditor scorecard cards (PERF-01/02)</name>
  <files>src/app/dashboard/(admin)/people/[personId]/page.tsx, messages/en.json, messages/tr.json</files>
  <read_first>
    - src/app/dashboard/(admin)/people/[personId]/page.tsx (full — current parallel fetch, isWorker/isAuditor branches, worker 4-card grid, auditor grid, Separator dual-role layout)
    - 09-PATTERNS.md section "people/[personId]/page.tsx (extend: scorecard enrichment)" (exact KpiCard usages, grid changes, settings-first fetch, slaBreachColor logic)
    - 09-UI-SPEC.md Surface 1 (worker cards 5/6 Output Volume + Approval Rate, auditor card 5 SLA Breach Rate, color rules, dual-role layout, per-project vs all-projects sub-labels, D-80 parity note)
    - src/components/admin/KpiCard.tsx (warning valueColor from Plan 02)
  </read_first>
  <action>
    Extend `src/app/dashboard/(admin)/people/[personId]/page.tsx`. Fetch `getTenantSettings()` first (its `auditSlaHours` is needed), then pass `auditSlaHours: settings.auditSlaHours` to the auditor `getPersonMetrics` call. For workers, expand the KPI grid (per UI-SPEC: 6-card layout, `grid-cols-2 sm:grid-cols-3`) adding: an Output Volume card (`Package` icon, value = `Intl.NumberFormat('tr-TR').format(Number(outputQuantitySum))` or "—", sub-label shows throughput "X.XX per day" derived in TS as `outputQuantitySum / dateRangeDays` when a date range is active else "—") and an Approval Rate card (`TrendingUp` icon, value = `(approved/(approved+rejected))*100` to 1 decimal or "—", valueColor success ≥80% / default 50-79% / destructive <50%). For auditors, add an SLA-Breach-Rate card (`AlertTriangle` icon, value = `(slaBreachRateDecided*100).toFixed(1)+"%"` or "—", valueColor destructive >20% / warning 10-20% / success <10% / default when null; icon color matches value). Preserve the existing Phase-8 cards in positions 1-4 and the dual-role `<Separator>` layout. Each card sub-label uses the per-project ("in {projectName}") vs all-projects ("across all projects") pattern via the existing `project` FilterBar param. Add the D-80 office-engineer parity note as an informational `<Alert>` rendered only when `!isWorker && !isAuditor` (no-op guard — office engineers are not in `people`). All new strings keyed under `dashboard.admin.scorecard.*`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v 'analytics.test\|tenant-settings' | grep -c 'error TS' | grep -qx 0 && grep -q "outputQuantitySum" "src/app/dashboard/(admin)/people/[personId]/page.tsx" && grep -q "slaBreachRateDecided" "src/app/dashboard/(admin)/people/[personId]/page.tsx" && echo PROFILE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "getTenantSettings" "src/app/dashboard/(admin)/people/[personId]/page.tsx"` AND `grep -q "auditSlaHours" "src/app/dashboard/(admin)/people/[personId]/page.tsx"` (SLA threshold wired)
    - `grep -q "outputQuantitySum" "src/app/dashboard/(admin)/people/[personId]/page.tsx"` (Output Volume card, PERF-01)
    - `grep -q "slaBreachRateDecided" "src/app/dashboard/(admin)/people/[personId]/page.tsx"` (SLA card, PERF-02)
    - `grep -q "scorecard." messages/en.json` AND `grep -q "scorecard." messages/tr.json` (new namespace keyed both locales)
    - `npx tsc --noEmit` reports zero new errors
  </acceptance_criteria>
  <done>Worker profile shows Output Volume + Approval Rate; auditor profile shows SLA Breach Rate colored by severity; SLA threshold sourced from tenant settings; per-project/all-projects scoping preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Add leaderboard rank column + sort to People directory (PERF-05)</name>
  <files>src/app/dashboard/(admin)/people/page.tsx, src/components/admin/LeaderboardSortSelect.tsx, messages/en.json, messages/tr.json</files>
  <read_first>
    - src/app/dashboard/(admin)/people/page.tsx (full — searchParams, parallel getPortfolioPeople fetch, Workers/Auditors tabs, Table structure, Badge usage, FilterBar Suspense)
    - 09-PATTERNS.md section "people/page.tsx (extend: leaderboard sort)" (sortBy destructure, getWorkerSortFn helper, addRanks competition ranking, RankCell badge rules, LeaderboardSortSelect client component, Suspense wrap)
    - 09-UI-SPEC.md Surface 3 (URL param sortBy, worker/auditor rank-by option tables, rank badge colors rank1=primary/2-3=secondary/4+=plain, tie handling, sort indicator, default state)
    - 09-RESEARCH.md Pattern 6 (sort in RSC, competition ranking, no-cross-currency value sort), Pitfall 3 (currency guard)
  </read_first>
  <action>
    Create `src/components/admin/LeaderboardSortSelect.tsx` as a `'use client'` component using `useRouter` + `useSearchParams` and shadcn `<Select>` to update the `sortBy` URL param (mirror FilterBar.tsx); it takes the option list + label per role and must be wrapped in `<Suspense>` by the page. Extend `src/app/dashboard/(admin)/people/page.tsx`: destructure `sortBy` from `searchParams`; define `getWorkerSortFn(sortBy)` (options: approved [default, DESC], rejected, rejection_rate [derived], value [single-currency only — no cross-currency, "—" when unpriced per Pitfall 3]) and `getAuditorSortFn(sortBy)` (options: turnaround [default, ASC lower=better], decisions, backlog, sla_breach [null-last]) with `displayName.localeCompare` as the final tiebreaker; sort copies of the fetched arrays and assign ranks using standard competition ranking (1,1,3). Prepend a Rank `<TableHead scope="col" aria-label="Rank">#</TableHead>` and a rank `<TableCell>` to both tables: rank 1 → `<Badge>` primary bg, rank 2-3 → `<Badge variant="secondary">`, rank 4+ → plain `text-sm font-semibold text-muted-foreground tabular-nums`. Render `<LeaderboardSortSelect>` right-aligned in the filter row (wrapped in `<Suspense>`). Show the lucide sort-direction icon on the active sort column header. No new DB query — `getPortfolioPeople` already returns all needed fields. New strings keyed under `dashboard.admin.leaderboard.*` and the rank header under `dashboard.admin.people.*`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v 'analytics.test\|tenant-settings' | grep -c 'error TS' | grep -qx 0 && grep -q "sortBy" "src/app/dashboard/(admin)/people/page.tsx" && test -f "src/components/admin/LeaderboardSortSelect.tsx" && echo LEADERBOARD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "sortBy" "src/app/dashboard/(admin)/people/page.tsx"` (URL param)
    - `grep -qE "getWorkerSortFn|workerSortFn" "src/app/dashboard/(admin)/people/page.tsx"` AND auditor sort helper present
    - `grep -q "localeCompare" "src/app/dashboard/(admin)/people/page.tsx"` (tiebreaker)
    - `test -f "src/components/admin/LeaderboardSortSelect.tsx"` AND `grep -q "'use client'" src/components/admin/LeaderboardSortSelect.tsx` AND `grep -q "useSearchParams" src/components/admin/LeaderboardSortSelect.tsx`
    - `grep -q "Suspense" "src/app/dashboard/(admin)/people/page.tsx"` (sort select wrapped)
    - `grep -q "leaderboard." messages/en.json` AND `grep -q "leaderboard." messages/tr.json`
    - `npx tsc --noEmit` reports zero new errors
  </acceptance_criteria>
  <done>Both People tabs show a rank column and a "Rank by" selector that re-sorts via the sortBy URL param using competition ranking; value sort respects the no-cross-currency rule; no new SQL.</done>
</task>

<task type="auto">
  <name>Task 3: Build read-only office-engineer scorecard (PERF-03, D-80 revised) + Analytics entry point</name>
  <files>src/app/dashboard/(admin)/analytics/office-engineers/[userId]/page.tsx, src/app/dashboard/(admin)/analytics/page.tsx, messages/en.json, messages/tr.json</files>
  <read_first>
    - src/actions/analytics.ts lines 580-660 (getOfficeActivityLog signature + ActivityLogEntry shape — reuse, do NOT rebuild)
    - src/db/schema/office-activity-log.ts (OFFICE_ACTION_TYPES enum — action label mapping)
    - src/db/schema/auth.ts (users table: id text PK, name, email — office engineers source)
    - src/app/dashboard/(admin)/analytics/page.tsx (current stub — heading + coming-soon Badge to preserve)
    - src/app/dashboard/(admin)/people/[personId]/page.tsx (analog — force-dynamic, params Promise await, notFound() guard, back link, name+role badge, Table layout, empty state)
    - 09-PATTERNS.md sections "analytics/page.tsx (extend)" + "analytics/office-engineers/[userId]/page.tsx (new controller)" (exact patterns)
    - 09-UI-SPEC.md Surface 2 (page layout, activity-log table anatomy, action label copy table, entry point, empty/not-found states)
  </read_first>
  <action>
    Create `src/app/dashboard/(admin)/analytics/office-engineers/[userId]/page.tsx` (`force-dynamic`, `params: Promise<{ userId: string }>`). Auth-guard + tenant-scope; fetch the office engineer's `users` row (tenant-scoped) — call `notFound()` if absent. Fetch the activity via `getOfficeActivityLog({ actorUserId: userId })` (Phase-7 function — reuse, never rebuild the data layer). Render: a back link "← Analytics" → `/dashboard/analytics`; the engineer name (or email) as the page heading; a `<Badge variant="secondary">` "Office Engineer"; an "Activity Log" section with a `<Table>` of Timestamp / Action / Context columns. Map each `action_type` to its localized label per the UI-SPEC action-label copy table. Timestamp via `toLocaleDateString('tr-TR')` + time; Context = project name + BOQ item name where present (JSX auto-escapes — no dangerouslySetInnerHTML). Empty state: `ClipboardList` icon + "No activity recorded yet." If row count exceeds 50, render a "Load more" ghost button (basic — increase limit param). Then extend `src/app/dashboard/(admin)/analytics/page.tsx`: keep the heading + coming-soon badge, but add a tenant-scoped list of office engineers (query the `users` table) rendered as a `<Table>` whose names link to `/dashboard/analytics/office-engineers/[userId]`; empty state "No office engineers found." All new strings keyed under `dashboard.admin.oe_scorecard.*`. Do NOT add a sidebar item (6-item nav locked, D-74/D-86) — this lives within the Analytics route namespace.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v 'analytics.test\|tenant-settings' | grep -c 'error TS' | grep -qx 0 && test -f "src/app/dashboard/(admin)/analytics/office-engineers/[userId]/page.tsx" && grep -q "getOfficeActivityLog" "src/app/dashboard/(admin)/analytics/office-engineers/[userId]/page.tsx" && echo OE_SCORECARD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f "src/app/dashboard/(admin)/analytics/office-engineers/[userId]/page.tsx"`
    - `grep -q "getOfficeActivityLog" "src/app/dashboard/(admin)/analytics/office-engineers/[userId]/page.tsx"` (reuses Phase-7 layer — PERF-03 read query only)
    - `grep -q "notFound" "src/app/dashboard/(admin)/analytics/office-engineers/[userId]/page.tsx"` (invalid userId guard)
    - `grep -q "force-dynamic" "src/app/dashboard/(admin)/analytics/office-engineers/[userId]/page.tsx"`
    - `grep -q "office-engineers" "src/app/dashboard/(admin)/analytics/page.tsx"` (entry point list links)
    - `grep -q "oe_scorecard." messages/en.json` AND `grep -q "oe_scorecard." messages/tr.json`
    - No new sidebar nav item added (`grep` of SidebarNav unchanged — 6-item nav locked)
    - `npx tsc --noEmit` reports zero new errors
  </acceptance_criteria>
  <done>A read-only OE scorecard at /dashboard/analytics/office-engineers/[userId] lists logged actions with timestamps from the existing office_activity_log layer, reachable from a tenant-scoped engineer list on the Analytics page; no nav change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL params (personId, userId, sortBy) → RSC | Route params and query strings are untrusted; resolved against tenant-scoped queries |
| office_activity_log context strings → JSX | Stored action context rendered as text (potential stored-XSS surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05-IDOR | Information Disclosure | OE scorecard [userId], profile [personId] | mitigate | Tenant-scoped user/person lookup + `notFound()` when the id is not in the tenant; getOfficeActivityLog filters `WHERE tenant_id` |
| T-09-05-XSS | Information Disclosure (XSS) | activity-log context, leaderboard displayName | mitigate | React JSX auto-escapes all text; no `dangerouslySetInnerHTML` (UI-SPEC) |
| T-09-05-T | Tampering | sortBy URL param | mitigate | sortBy mapped through a fixed option allowlist (getWorkerSortFn/getAuditorSortFn default fallback); never interpolated into SQL (sort is pure TS) |
| T-09-05-AC | Access Control | OE scorecard + profile pages | mitigate | `auth()` guard on every RSC; data reads tenant-scoped via existing analytics functions (V2/V4) |
| T-09-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages; Select/Badge/Alert already present (RESEARCH: zero installs) |
</threat_model>

<verification>
- `npx tsc --noEmit` passes
- `npx vitest run` — existing + Plan-04 analytics tests remain green (this plan adds UI, not new SQL)
- PERF-01/02 cards, PERF-05 ranking, PERF-03 OE scorecard all render with TR/EN strings
</verification>

<success_criteria>
- Worker + auditor scorecards complete on the profile page (PERF-01/02)
- People directory ranks with a selectable metric (PERF-05)
- Read-only OE scorecard delivered via the existing activity-log layer (PERF-03)
- All new copy localized TR/EN; no sidebar change
</success_criteria>

<output>
Create `.planning/phases/09-performance-scorecards-leaderboard-alerts/09-05-SUMMARY.md` when done
</output>
