---
phase: 09-performance-scorecards-leaderboard-alerts
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/db/migrations/0007_v2_tenant_settings.sql
  - src/db/migrations/meta/_journal.json
autonomous: false
requirements: [PERF-06]
must_haves:
  truths:
    - "A 0007 migration creates the tenant_settings table with the unique tenant_id constraint and correct numeric(5,4) precision (D-83)"
    - "The migration seeds the default tenant's Moderate thresholds (48 / 0.3000 / 7) idempotently (D-84)"
    - "The migration is applied to the live DB via tsx src/db/migrate.ts — NEVER drizzle-kit push (D-49)"
    - "The _journal.json carries the idx-7 entry whose tag matches the migration filename"
  artifacts:
    - path: "src/db/migrations/0007_v2_tenant_settings.sql"
      provides: "CREATE TABLE tenant_settings + UNIQUE + seed INSERT"
      contains: "tenant_settings"
    - path: "src/db/migrations/meta/_journal.json"
      provides: "idx 7 journal entry"
      contains: "0007_v2_tenant_settings"
  key_links:
    - from: "src/db/migrations/0007_v2_tenant_settings.sql"
      to: "live Neon DB"
      via: "tsx src/db/migrate.ts (NOT drizzle-kit push)"
      pattern: "migrate"
    - from: "_journal.json idx 7"
      to: "0007_v2_tenant_settings.sql"
      via: "tag match"
      pattern: "0007_v2_tenant_settings"
---

<objective>
[BLOCKING] Generate, hand-verify, and apply the `tenant_settings` migration. This closes the false-positive verification trap: TypeScript and build pass without it, but the live DB would lack the table, so every Plan 04 settings query would fail at runtime. The migration MUST be applied via the project's real runner (`tsx src/db/migrate.ts`) — `drizzle-kit push` is permanently unusable (D-49, spatial_ref_sys permission error on Neon).

Purpose: Materialize the configurable-threshold table in both the production and test databases with seeded Moderate defaults (D-84) so downstream analytics/UI plans run against a real table.
Output: One verified migration file, an updated journal, and a live-applied schema change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-performance-scorecards-leaderboard-alerts/09-RESEARCH.md
@.planning/phases/09-performance-scorecards-leaderboard-alerts/09-PATTERNS.md

@src/db/schema/tenant-settings.ts
@src/db/migrate.ts

<interfaces>
<!-- Migration precedents (verified). The runner is drizzle-orm/neon-http/migrator migrate(). -->
Existing migrations: 0000..0006 in src/db/migrations/. Hand-edited precedents:
- 0003_slippery_prowler.sql (partial index hand-edit)
- 0005_v2_indexes.sql (hand-written, --> statement-breakpoint between every CREATE INDEX; neon-http cannot run multiple statements per prepared call)
- 0006_v2_period_qty_check.sql (hand-written CHECK constraints; header comment explaining why)

_journal.json: currently 7 entries (idx 0-6). Next idx is 7. The `tag` MUST equal the filename without `.sql` → "0007_v2_tenant_settings".

Default tenant UUID (seed target): '00000000-0000-0000-0000-000000000001' (getDefaultTenantId convention, STATE.md).

Apply commands (RESEARCH Pattern 1):
- Production: `npx tsx src/db/migrate.ts`
- Test branch: `TEST_DATABASE_URL=<url> DATABASE_URL=<url> npx tsx src/db/migrate.ts` (separate Neon branch — 0007 must be applied there too, STATE.md note)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate + hand-verify the 0007 migration (table, UNIQUE, precision, seed)</name>
  <files>src/db/migrations/0007_v2_tenant_settings.sql, src/db/migrations/meta/_journal.json</files>
  <read_first>
    - src/db/schema/tenant-settings.ts (the schema this migration must match — from Plan 01)
    - src/db/migrations/0006_v2_period_qty_check.sql (analog — hand-written header + statement-breakpoint style)
    - src/db/migrations/0005_v2_indexes.sql (analog — statement-breakpoint between every statement)
    - src/db/migrations/meta/_journal.json (append idx 7; tag must match filename)
    - 09-RESEARCH.md Pattern 1 (exact generate→rename→hand-edit→apply recipe; Pitfall 5 numeric default)
    - 09-PATTERNS.md section "0007_v2_tenant_settings.sql" (exact SQL shape, journal entry shape, seed INSERT)
  </read_first>
  <action>
    Run `DATABASE_URL="<neon-url>" npx tsx node_modules/.bin/drizzle-kit generate` to produce a migration template from the Plan-01 schema file. Rename the generated file to `src/db/migrations/0007_v2_tenant_settings.sql`. Hand-verify and correct the SQL so it: creates `tenant_settings` with `id` uuid PK DEFAULT gen_random_uuid(), `tenant_id` uuid NOT NULL REFERENCES "tenants"("id"), `audit_sla_hours` integer NOT NULL DEFAULT 48, `rejection_rate_threshold` numeric(5,4) NOT NULL DEFAULT '0.3000' (string literal — confirm drizzle-kit did NOT emit `DEFAULT 0.3` per Pitfall 5), `stalled_days` integer NOT NULL DEFAULT 7, `updated_at` timestamp with time zone DEFAULT now() NOT NULL. Add a hand-written header comment (mirror 0006) explaining the hand-edits. Add `--> statement-breakpoint` between every statement. Append the UNIQUE constraint: `ALTER TABLE "tenant_settings" ADD CONSTRAINT "tenant_settings_tenant_id_unique" UNIQUE ("tenant_id");`. Append the idempotent seed: `INSERT INTO "tenant_settings" (tenant_id, audit_sla_hours, rejection_rate_threshold, stalled_days) VALUES ('00000000-0000-0000-0000-000000000001', 48, '0.3000', 7) ON CONFLICT (tenant_id) DO NOTHING;` (D-84 Moderate seed; do NOT seed via a separate script — RESEARCH anti-pattern). Append the idx-7 entry to `_journal.json` with `tag: "0007_v2_tenant_settings"` and `breakpoints: true`. Do NOT run `drizzle-kit push` at any point.
  </action>
  <verify>
    <automated>test -f src/db/migrations/0007_v2_tenant_settings.sql && grep -q "numeric(5,4)" src/db/migrations/0007_v2_tenant_settings.sql && grep -q "tenant_settings_tenant_id_unique" src/db/migrations/0007_v2_tenant_settings.sql && grep -q "ON CONFLICT" src/db/migrations/0007_v2_tenant_settings.sql && grep -q "0007_v2_tenant_settings" src/db/migrations/meta/_journal.json && echo MIGRATION_FILE_OK</automated>
  </verify>
  <acceptance_criteria>
    - File `src/db/migrations/0007_v2_tenant_settings.sql` exists
    - `grep -q "numeric(5,4)" 0007_v2_tenant_settings.sql` (precision preserved — Pitfall 5)
    - `grep -q "DEFAULT '0.3000'" src/db/migrations/0007_v2_tenant_settings.sql` (string literal, not `DEFAULT 0.3`)
    - `grep -q "tenant_settings_tenant_id_unique" src/db/migrations/0007_v2_tenant_settings.sql` (UNIQUE)
    - `grep -q "ON CONFLICT (tenant_id) DO NOTHING" src/db/migrations/0007_v2_tenant_settings.sql` (idempotent seed)
    - `grep -c "statement-breakpoint" src/db/migrations/0007_v2_tenant_settings.sql` is ≥ 2
    - `_journal.json` contains an entry with `"idx": 7` and `"tag": "0007_v2_tenant_settings"`
    - The file contains NO occurrence of `drizzle-kit push`
  </acceptance_criteria>
  <done>The 0007 migration is authored with correct precision, the UNIQUE constraint, an idempotent Moderate-defaults seed, and a matching journal entry — verified by hand against the schema.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Apply 0007 migration to production + test DBs via tsx migrate.ts</name>
  <action>Apply the verified 0007 migration to the production Neon DB and the test branch via `npx tsx src/db/migrate.ts` (NEVER drizzle-kit push — D-49), then confirm the seeded Moderate-defaults row exists. This is a human-verified checkpoint because it requires live DB credentials and confirmation that the table + seed materialized (closing the false-positive verification trap).</action>
  <what-built>The 0007_v2_tenant_settings migration file + journal entry, ready to apply to the live Neon production DB and the test branch via `npx tsx src/db/migrate.ts`. drizzle-kit push is NOT used (D-49).</what-built>
  <how-to-verify>
    1. Confirm `DATABASE_URL` (production Neon) and `TEST_DATABASE_URL` (test branch) are available in the environment / `.env`.
    2. Apply to production: run `npx tsx src/db/migrate.ts`. Expect it to apply migration 0007 with no error.
    3. Apply to the test branch: run `TEST_DATABASE_URL=<test-url> DATABASE_URL=<test-url> npx tsx src/db/migrate.ts`. Expect 0007 applied there too (separate Neon branch — STATE.md note).
    4. Verify the table + seed row exist: query `SELECT audit_sla_hours, rejection_rate_threshold, stalled_days FROM tenant_settings WHERE tenant_id = '00000000-0000-0000-0000-000000000001';` — expect one row `48 | 0.3000 | 7`.
    5. Verify the unique constraint: a second INSERT with the same tenant_id must be a no-op (ON CONFLICT DO NOTHING) — re-running migrate.ts must not error or duplicate.
  </how-to-verify>
  <resume-signal>Type "approved" once the migration is applied to both databases and the seeded Moderate-defaults row is confirmed present, or describe the error.</resume-signal>
  <acceptance_criteria>
    - `tenant_settings` table exists in the production Neon DB
    - One seeded row exists with audit_sla_hours=48, rejection_rate_threshold=0.3000, stalled_days=7
    - The migration is also applied to the test branch (integration tests in Plan 04 can read the seeded row)
    - Re-running migrate.ts is idempotent (no duplicate row, no error)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → live DB | DDL + seed run with elevated DB privileges; values are literals authored by the developer, not user input |
| migrate.ts → DB | Uses drizzle-orm/neon-http/migrator; idempotent journal-tracked application |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-T | Tampering | tenant_settings rows | mitigate | UNIQUE(tenant_id) prevents conflicting threshold rows; seed uses ON CONFLICT DO NOTHING (idempotent re-apply) |
| T-09-03-D | Denial of Service | migration apply | mitigate | Journal-tracked migrate() applies each migration once; re-run is a no-op — no risk of repeated table creation |
| T-09-03-FP | Repudiation (false-positive verify) | DB schema vs build | mitigate | This [BLOCKING] plan exists specifically so build-green does not mask a missing live table; checkpoint confirms live apply |
| T-09-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages; drizzle-kit already present (RESEARCH: zero new packages) |
</threat_model>

<verification>
- `0007_v2_tenant_settings.sql` exists with correct precision, UNIQUE, and idempotent seed
- `_journal.json` has the idx-7 entry tagged `0007_v2_tenant_settings`
- Migration applied to production AND test DB via `tsx src/db/migrate.ts` (checkpoint-confirmed)
- `drizzle-kit push` never invoked (D-49)
</verification>

<success_criteria>
- tenant_settings table live in both databases
- Seeded Moderate-defaults row present and unique-constrained
- Idempotent re-apply confirmed
</success_criteria>

<output>
Create `.planning/phases/09-performance-scorecards-leaderboard-alerts/09-03-SUMMARY.md` when done
</output>
