---
phase: 09-performance-scorecards-leaderboard-alerts
plan: 06
type: execute
wave: 5
depends_on: ["09-02", "09-04", "09-05"]
files_modified:
  - src/app/dashboard/(admin)/overview/page.tsx
  - src/app/dashboard/(admin)/settings/page.tsx
  - src/components/admin/ThresholdSettingsForm.tsx
  - src/components/layout/TopNav.tsx
  - messages/en.json
  - messages/tr.json
autonomous: false
requirements: [PERF-06]
must_haves:
  truths:
    - "Overview shows a new Stalled Projects KPI card that badges red when ≥1 stalled project and drills to the projects list (D-88)"
    - "The pending-backlog KPI card shows destructive when pendingBacklog > 0 AND getPortfolioKPIs.avgDecisionLatencyHours > auditSlaHours, warning when pendingBacklog > 0 but latency is within threshold, per UI-SPEC Surface 5 two-condition rule (D-85/D-87)"
    - "The rejection KPI card shows an inline alert badge/color when the absolute rate in the active window exceeds the threshold; the rejection alert is suppressed when no date filter is active (D-85/D-87, research recommendation)"
    - "A /dashboard/settings page (office-engineer-only) edits the 3 thresholds via a zod-validated form, reachable from a TopNav gear icon — no new sidebar item (D-83/D-86/D-89)"
    - "Alert colors use semantic destructive/amber only, never --primary (UI-SPEC hard rule)"
  artifacts:
    - path: "src/app/dashboard/(admin)/overview/page.tsx"
      provides: "alert badges + Stalled Projects KPI card"
      contains: "getStalledProjects"
    - path: "src/app/dashboard/(admin)/settings/page.tsx"
      provides: "threshold settings page"
      contains: "getTenantSettings"
    - path: "src/components/admin/ThresholdSettingsForm.tsx"
      provides: "3-input threshold form"
      contains: "updateTenantSettings"
    - path: "src/components/layout/TopNav.tsx"
      provides: "settings gear entry point"
      contains: "/dashboard/settings"
  key_links:
    - from: "overview/page.tsx pending-backlog badge"
      to: "getPortfolioKPIs().avgDecisionLatencyHours vs getTenantSettings().auditSlaHours"
      via: "destructive when pendingBacklog > 0 AND avgDecisionLatencyHours > auditSlaHours, else warning when pendingBacklog > 0 (two-condition rule, UI-SPEC Surface 5, D-85/D-87)"
      pattern: "avgDecisionLatencyHours"
    - from: "overview/page.tsx"
      to: "getTenantSettings + getStalledProjects + getPortfolioKPIs"
      via: "pure-TS alert state computed from already-fetched data (D-87, no extra round-trip)"
      pattern: "stalledColor"
    - from: "ThresholdSettingsForm"
      to: "updateTenantSettings"
      via: "client form submit, rate %→decimal conversion, zod-validated server action (D-83/D-89)"
      pattern: "updateTenantSettings"
    - from: "TopNav gear icon"
      to: "/dashboard/settings"
      via: "Link (no sidebar item — D-86)"
      pattern: "/dashboard/settings"
---

<objective>
Surface the alerts and ship the configurable-threshold UI. Add the Overview inline alert badges + the new "Stalled Projects" KPI card (PERF-06, D-87/D-88), build the `/dashboard/settings` threshold form (office-engineer-only, D-83/D-86/D-89) reachable via a TopNav gear icon, and key all new copy TR/EN. Alert state is computed in the Overview RSC as pure TypeScript over already-fetched data — no extra DB round-trip. This is the final Phase-9 plan and completes PERF-06.

Purpose: Make SLA / performance alerts visible on the Overview and let the office engineer tune the thresholds that drive them.
Output: Extended Overview, a settings page + client form, a TopNav gear entry point, and i18n additions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-performance-scorecards-leaderboard-alerts/09-UI-SPEC.md
@.planning/phases/09-performance-scorecards-leaderboard-alerts/09-PATTERNS.md
@.planning/phases/09-performance-scorecards-leaderboard-alerts/09-RESEARCH.md

<interfaces>
<!-- Verified contracts. -->
getTenantSettings() → { auditSlaHours, rejectionRateThreshold (numeric string/number 0..1), stalledDays } (Plan 04).
getStalledProjects(stalledDays) → { projectId, projectName }[] (Plan 04; point-in-time, NOT date-filtered).
updateTenantSettings({ auditSlaHours, rejectionRateThreshold (0..1), stalledDays }) → { ok: true } (Plan 04; throws Unauthorized; zod-validated).
getPortfolioKPIs(filters) → { pendingBacklog, approvalsInRange, rejectionsInRange, activeWorkers, avgDecisionLatencyHours (number|null, point-in-time portfolio-wide mean decided turnaround, null when no decided submissions — Plan 04 W5 addition) } (existing; pendingBacklog point-in-time D-66).

KpiCard (Plan 02): valueColor ∈ default|success|destructive|warning; optional alertBadge (top-right corner node).
Overview parallel fetch (overview/page.tsx lines 58-64): Promise.all([getPortfolioKPIs, getPortfolioTrends, getPortfolioOverview, getProjects, getActivePeople]).
  Two-phase: add getTenantSettings() to Promise.all, then `await getStalledProjects(settings.stalledDays)` (stalledDays depends on settings).
  getPortfolioKPIs already returns avgDecisionLatencyHours — no extra fetch needed for the pending-backlog severity rule.

ProjectForm.tsx analog: 'use client', useState, async handleSubmit, validate-on-submit, error <p> with aria-describedby.
TopNav.tsx right-side items (lines 29-52): <LanguageToggle/> then userEmail then sign-out form. Insert gear Link after LanguageToggle.
Established protected-RSC auth pattern (src/app/dashboard/layout.tsx lines 19-20): `const session = await auth(); if (!session) redirect('/auth/signin');` — mirror this exact target.

isDateFiltered = (validatedFrom && validatedTo) present — drives rejection-badge suppression (Pitfall 4 / research recommendation).

i18n namespaces this plan OWNS (must NOT touch scorecard.*/leaderboard.*/oe_scorecard.* — those are Plan 05):
  dashboard.admin.settings.*  + additions to dashboard.admin.overview.* (kpi_stalled_*, settings_aria_label under nav.*)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Overview alert badges + Stalled Projects KPI card (PERF-06, D-87/D-88)</name>
  <files>src/app/dashboard/(admin)/overview/page.tsx, messages/en.json, messages/tr.json</files>
  <read_first>
    - src/app/dashboard/(admin)/overview/page.tsx (full — parallel fetch, existing pendingColor pattern, KPI grid grid-cols-4, KpiCard usages, isDateFiltered derivation)
    - 09-PATTERNS.md section "overview/page.tsx (extend: alert badges + Stalled KpiCard)" (two-phase fetch, alert color computation, grid-cols-5, alertBadge element, Stalled KpiCard)
    - 09-UI-SPEC.md Surface 5 (5-card grid, Stalled card props, inline alert wiring table — note the pending-backlog two-condition rule, warning state, healthy state, rejection suppression when no date filter)
    - 09-RESEARCH.md Pattern 7 (pure-TS alert state, no extra round-trip), Pitfall 4 (suppress rejection badge with no date filter)
    - src/components/admin/KpiCard.tsx (alertBadge + warning from Plan 02)
  </read_first>
  <action>
    Extend `src/app/dashboard/(admin)/overview/page.tsx`. Add `getTenantSettings()` to the existing `Promise.all`, then `const stalledProjects = await getStalledProjects(settings.stalledDays)` (two-phase — stalledDays depends on settings). Compute alert state as pure TS (no new DB call — `getPortfolioKPIs` already returns `avgDecisionLatencyHours`): `stalledColor` = destructive when `stalledProjects.length >= 1` else default; `rejectionAlertFires` = `isDateFiltered && (rejectionsInRange / max(approvalsInRange + rejectionsInRange, 1)) > Number(settings.rejectionRateThreshold)` (suppress entirely when no date filter — Pitfall 4); `pendingColor` per the UI-SPEC Surface 5 two-condition rule — `destructive` when `pendingBacklog > 0 && kpis.avgDecisionLatencyHours != null && kpis.avgDecisionLatencyHours > settings.auditSlaHours`, else `warning` when `pendingBacklog > 0` (latency null or within threshold), else `default`; `pendingAlertFires` = the destructive branch only (the alertBadge fires on SLA breach, not on the amber caution state). Define a destructive `<Badge>` with a 12px `<TriangleAlert>` and `aria-label` as the shared `alertBadgeEl`. Pass `valueColor` + `alertBadge` (only when its alert is firing — destructive pending or firing rejection) to the Pending and Rejections cards. Expand the KPI grid to `grid-cols-2 md:grid-cols-3 lg:grid-cols-5` and add a 5th "Stalled Projects" KpiCard (`PauseCircle` icon, value = `stalledProjects.length`, valueColor stalledColor, alertBadge when ≥1, `drillHref="/dashboard/projects?stalled=true"` only when ≥1, sub-label alert/healthy variants). Alert visuals are destructive/amber only — never `--primary` (UI-SPEC hard rule). New strings keyed under `dashboard.admin.overview.*` (kpi_stalled_label/sub variants).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v 'analytics.test\|tenant-settings' | grep -c 'error TS' | grep -qx 0 && grep -q "getStalledProjects" "src/app/dashboard/(admin)/overview/page.tsx" && grep -q "lg:grid-cols-5" "src/app/dashboard/(admin)/overview/page.tsx" && grep -q "avgDecisionLatencyHours" "src/app/dashboard/(admin)/overview/page.tsx" && echo OVERVIEW_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "getStalledProjects" "src/app/dashboard/(admin)/overview/page.tsx"` AND `grep -q "getTenantSettings" "src/app/dashboard/(admin)/overview/page.tsx"`
    - `grep -q "avgDecisionLatencyHours" "src/app/dashboard/(admin)/overview/page.tsx"` AND `grep -q "auditSlaHours" "src/app/dashboard/(admin)/overview/page.tsx"` (pending-backlog two-condition severity: destructive when latency > SLA, warning otherwise)
    - `grep -q "lg:grid-cols-5" "src/app/dashboard/(admin)/overview/page.tsx"` (5-card grid)
    - `grep -q "TriangleAlert" "src/app/dashboard/(admin)/overview/page.tsx"` (alert badge) AND `grep -q "alertBadge" "src/app/dashboard/(admin)/overview/page.tsx"`
    - `grep -q "isDateFiltered" "src/app/dashboard/(admin)/overview/page.tsx"` used to gate the rejection badge (Pitfall 4)
    - `grep -q "/dashboard/projects?stalled=true" "src/app/dashboard/(admin)/overview/page.tsx"` (Stalled drill)
    - `grep -q "kpi_stalled" messages/en.json` AND `grep -q "kpi_stalled" messages/tr.json`
    - `npx tsc --noEmit` reports zero new errors
  </acceptance_criteria>
  <done>Overview renders a 5-card row with a Stalled Projects card and inline alert badges/colors driven by configured thresholds; the pending-backlog card is destructive only when avgDecisionLatencyHours > auditSlaHours (warning otherwise); rejection badge is suppressed without a date filter; alert visuals are destructive/amber only.</done>
</task>

<task type="auto">
  <name>Task 2: Build /dashboard/settings page + ThresholdSettingsForm (PERF-06, D-83/D-89)</name>
  <files>src/app/dashboard/(admin)/settings/page.tsx, src/components/admin/ThresholdSettingsForm.tsx, messages/en.json, messages/tr.json</files>
  <read_first>
    - src/components/dashboard/ProjectForm.tsx (full — 'use client', useState, handleSubmit, validate-on-submit, error <p> + aria-describedby, disabled while submitting)
    - src/actions/settings.ts (Plan 04 — updateTenantSettings input shape: rejectionRateThreshold is 0..1 decimal)
    - src/app/dashboard/layout.tsx lines 19-20 (the established `const session = await auth(); if (!session) redirect('/auth/signin');` guard to mirror)
    - src/app/dashboard/(admin)/overview/page.tsx (analog — force-dynamic, getTranslations, page heading/subtitle, Card layout)
    - 09-PATTERNS.md sections "settings/page.tsx (controller)" + "ThresholdSettingsForm.tsx (component)" (full patterns: props, %→decimal conversion, transient success Alert)
    - 09-UI-SPEC.md Surface 4 (page layout, form anatomy 3 rows, field/label/desc/unit/min/max/default table, validation error copy, success feedback, auth guard behavior)
  </read_first>
  <action>
    Create `src/components/admin/ThresholdSettingsForm.tsx` as `'use client'` following ProjectForm.tsx: props `{ defaultAuditSlaHours: number; defaultRejectionRatePercent: number; defaultStalledDays: number }`. Three numeric `<Input type="number">` rows (Label + muted description + input + inline unit annotation `<span aria-label>`): Audit SLA (hours, 1-720), Rejection Rate (%, 0-100), Stalled days (1-365). On submit: validate on submit (client), then call `updateTenantSettings({ auditSlaHours, rejectionRateThreshold: rejectionRatePercent / 100, stalledDays })` (convert % → 0..1 decimal). Per-field error `<p className="text-sm text-destructive">` wired via `aria-describedby`. On success show a transient `<Alert>` ("Settings saved.") for 3s via `useState` + `setTimeout`; fields retain saved values (no reset). On failure show the error-state copy. All strings keyed under `dashboard.admin.settings.*`.
    Create `src/app/dashboard/(admin)/settings/page.tsx` (`export const dynamic = 'force-dynamic'`). Auth-guard at the top of the RSC mirroring the established protected-page pattern in src/app/dashboard/layout.tsx: import `auth` from '@/lib/auth' and `redirect` from 'next/navigation', then `const session = await auth(); if (!session) redirect('/auth/signin');` (office-engineer-only + tenant-scoped — D-89; the named redirect target `/auth/signin` matches every other protected RSC). After the guard, call `getTenantSettings()` (itself auth-guarded + tenant-scoped), render heading "Settings" + subtitle, and a `<Card>` wrapping `<ThresholdSettingsForm>` with defaults derived from settings (`defaultRejectionRatePercent = Math.round(Number(settings.rejectionRateThreshold) * 100)`). No new sidebar item (D-86).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v 'analytics.test\|tenant-settings' | grep -c 'error TS' | grep -qx 0 && test -f "src/app/dashboard/(admin)/settings/page.tsx" && test -f src/components/admin/ThresholdSettingsForm.tsx && grep -q "updateTenantSettings" src/components/admin/ThresholdSettingsForm.tsx && echo SETTINGS_UI_OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f "src/app/dashboard/(admin)/settings/page.tsx"` AND `grep -q "force-dynamic" "src/app/dashboard/(admin)/settings/page.tsx"` AND `grep -q "getTenantSettings" "src/app/dashboard/(admin)/settings/page.tsx"`
    - `grep -q "const session = await auth()" "src/app/dashboard/(admin)/settings/page.tsx"` AND `grep -q "redirect('/auth/signin')" "src/app/dashboard/(admin)/settings/page.tsx"` (office-engineer guard mirroring layout.tsx — D-89)
    - `test -f src/components/admin/ThresholdSettingsForm.tsx` AND `grep -q "'use client'" src/components/admin/ThresholdSettingsForm.tsx`
    - `grep -q "updateTenantSettings" src/components/admin/ThresholdSettingsForm.tsx` AND `grep -q "/ 100" src/components/admin/ThresholdSettingsForm.tsx` (%→decimal conversion)
    - `grep -q "aria-describedby" src/components/admin/ThresholdSettingsForm.tsx` (error a11y)
    - `grep -q "settings." messages/en.json` AND `grep -q "settings." messages/tr.json`
    - `npx tsc --noEmit` reports zero new errors
  </acceptance_criteria>
  <done>An office-engineer-only /dashboard/settings page (guarded by `const session = await auth(); if (!session) redirect('/auth/signin');` mirroring layout.tsx) edits the 3 thresholds via a zod-validated form with %→decimal conversion, per-field validation, and a transient success message; no sidebar item added.</done>
</task>

<task type="auto">
  <name>Task 3: Add settings gear entry point to TopNav (PERF-06, D-86)</name>
  <files>src/components/layout/TopNav.tsx, messages/en.json, messages/tr.json</files>
  <read_first>
    - src/components/layout/TopNav.tsx (full — right-side item group, LanguageToggle placement, translation hook usage)
    - 09-PATTERNS.md section "TopNav.tsx (extend: settings gear icon)" (exact insert position, Link + Settings icon, aria-label key)
    - 09-UI-SPEC.md Surface 4 "Entry point" (gear icon 20px, after LanguageToggle, 8px left margin, text-muted-foreground rest / text-foreground hover, aria-label) + Accessibility (aria-label mandatory)
  </read_first>
  <action>
    Extend `src/components/layout/TopNav.tsx`: insert a `<Link href="/dashboard/settings">` containing a 20px lucide `<Settings>` icon, placed after `<LanguageToggle>` and before the user email, with `className="text-muted-foreground hover:text-foreground ml-2"`, `aria-hidden="true"` on the icon, and `aria-label` from a new key `dashboard.admin.nav.settings_aria_label` ("Open settings" / "Ayarları aç"). Icon-only — no visible text. This is the sole settings entry point (no sidebar item — D-86). Add the `settings_aria_label` key to both `messages/en.json` and `messages/tr.json` under the existing `dashboard.admin.nav` namespace.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v 'analytics.test\|tenant-settings' | grep -c 'error TS' | grep -qx 0 && grep -q "/dashboard/settings" src/components/layout/TopNav.tsx && grep -q "settings_aria_label" messages/en.json && echo TOPNAV_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "/dashboard/settings" src/components/layout/TopNav.tsx` (gear Link)
    - `grep -q "Settings" src/components/layout/TopNav.tsx` (lucide icon import + use)
    - `grep -q "settings_aria_label" messages/en.json` AND `grep -q "settings_aria_label" messages/tr.json` (a11y label both locales)
    - SidebarNav is NOT modified (6-item nav locked — D-86)
    - `npx tsc --noEmit` reports zero new errors
  </acceptance_criteria>
  <done>TopNav has a gear icon linking to /dashboard/settings with a localized aria-label; no sidebar item was added.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify alerts, settings persistence, gear nav, and TR/EN localization</name>
  <action>Human-verify the end-to-end PERF-06 experience in the running app: the Stalled Projects KPI card, inline alert badges with correct date-filter suppression and the pending-backlog destructive-vs-warning distinction, settings persistence via the TopNav gear, and TR/EN localization. This checkpoint confirms the full alert + settings loop works against live data after autonomous Tasks 1-3.</action>
  <what-built>The full PERF-06 alert + settings experience: Overview now shows a Stalled Projects KPI card and inline alert badges on the pending/rejection cards; /dashboard/settings (reached via the TopNav gear) edits the 3 thresholds and persists them; changing a threshold re-scopes the Overview alerts.</what-built>
  <how-to-verify>
    1. Run the dev server and sign in. Visit `/dashboard/overview` — confirm 5 KPI cards including "Stalled Projects" / "Durmuş Projeler".
    2. Click the gear icon in the top-right of the TopNav — confirm it navigates to `/dashboard/settings`.
    3. On the settings page, lower the "Audit SLA Threshold" (e.g. to 1 hour) and "Rejection Rate Alert" (e.g. to 0%), save — confirm the "Settings saved." transient message appears.
    4. Back on the Overview, confirm the Pending Audits card now shows a red destructive badge (avg decision latency now exceeds the 1-hour SLA) rather than the amber caution state.
    5. Apply a date-range filter on the Overview — confirm the Rejections card now shows a red alert badge (and confirm it does NOT show one when no date filter is active — Pitfall 4).
    6. If any project has no approval within the stalled threshold, confirm the Stalled Projects card badges red and links to `/dashboard/projects?stalled=true`.
    7. Toggle the dashboard locale TR↔EN — confirm all new labels (Stalled Projects, Settings, form fields, units) render in both languages.
  </how-to-verify>
  <resume-signal>Type "approved" once the Stalled card, inline alert badges (with correct date-filter suppression AND the pending destructive-vs-warning distinction), settings persistence, gear navigation, and TR/EN localization are all confirmed, or describe the issues.</resume-signal>
  <acceptance_criteria>
    - Overview shows 5 KPI cards with the Stalled Projects card
    - Lowering thresholds and saving persists (transient success shown); Overview reflects new thresholds on reload
    - Pending Audits card shows destructive when avg latency exceeds the SLA, warning (amber) otherwise
    - Rejection alert badge appears only with an active date filter (suppressed otherwise)
    - Gear icon navigates to /dashboard/settings; no sidebar item exists for settings
    - All new copy renders correctly in TR and EN
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client form → updateTenantSettings | Office engineer threshold input crosses the server-action boundary (untrusted) |
| URL params (date filter) → Overview RSC | Query strings drive alert computation; validated before use |
| TopNav → settings route | Navigation link; the route itself enforces auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06-EoP | Elevation of Privilege | /dashboard/settings page + form | mitigate | RSC `const session = await auth(); if (!session) redirect('/auth/signin');` guard (mirrors layout.tsx) + server-action `auth()` guard (both); non-office-engineer access redirects/throws (D-89) |
| T-09-06-T | Tampering | threshold form values | mitigate | Client validate-on-submit + server zod (Plan 04) reject out-of-range; % is bounded 0-100 and converted to 0..1 decimal |
| T-09-06-ID | Information Disclosure | Overview alert data, stalled list, avg latency | mitigate | All source functions (getPortfolioKPIs/getStalledProjects/getTenantSettings) are auth-guarded + tenant-scoped (V4); alert state is pure-TS over already-fetched tenant data |
| T-09-06-XSS | Information Disclosure (XSS) | project names in Stalled drill / KPI labels | mitigate | React JSX auto-escapes; no `dangerouslySetInnerHTML` |
| T-09-06-SC | Tampering | npm/pip/cargo installs | accept | No new packages; Settings/TriangleAlert/PauseCircle from lucide + Badge/Alert/Input/Label already present (RESEARCH: zero installs) |
</threat_model>

<verification>
- `npx tsc --noEmit` passes
- `npx vitest run` — full suite green (Plan-04 settings/analytics tests + existing)
- Human-verify checkpoint confirms Stalled card, alert badges (incl. pending destructive-vs-warning), settings persistence, gear nav, TR/EN
</verification>

<success_criteria>
- Overview shows the Stalled Projects card + inline threshold-driven alert badges; pending-backlog is destructive only when avgDecisionLatencyHours > auditSlaHours (warning otherwise); rejection suppressed without date filter
- /dashboard/settings edits + persists the 3 thresholds, office-engineer-only (auth() + redirect('/auth/signin') guard), via the TopNav gear (no sidebar item)
- Alert visuals destructive/amber only; all new copy TR/EN
</success_criteria>

<output>
Create `.planning/phases/09-performance-scorecards-leaderboard-alerts/09-06-SUMMARY.md` when done
</output>
</content>
</invoke>
