---
phase: 05-dashboard-map
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - next.config.ts
  - messages/tr.json
  - messages/en.json
  - tests/submissions.test.ts
  - tests/boq.test.ts
autonomous: false
requirements: [DASH-01, DASH-02, DASH-03, DASH-04, DASH-05]
user_setup:
  - service: mapbox
    why: "Public token for client-side map tile/style loading (DASH-01) + domain restriction (SC4/D-62)"
    env_vars:
      - name: NEXT_PUBLIC_MAPBOX_TOKEN
        source: "account.mapbox.com -> Tokens -> default public token (scopes: styles:read, fonts:read)"
    dashboard_config:
      - task: "Restrict the public token to URL https://bayrak.ai/* (and https://www.bayrak.ai/*) BEFORE sharing any dashboard URL externally (D-62 / SC4). Token Referrer-Policy must NOT be no-referrer."
        location: "account.mapbox.com -> Tokens -> (token) -> URL restrictions"

must_haves:
  truths:
    - "mapbox-gl@3.24.x, react-map-gl@8.1.x, @types/mapbox-gl are installed and resolvable"
    - "next/image can load photos from *.public.blob.vercel-storage.com (DASH-03 thumbnails)"
    - "Every new dashboard surface string has a TR and EN message key (I18N-02 / D-63)"
    - "tests/submissions.test.ts exists and defines RED stubs for getRouteGeoJSON, getApprovedPoints, getSubmissions"
    - "tests/boq.test.ts has progress-% edge-case tests (0%, ≥100% cap, null/0 planned)"
    - "The public Mapbox token's domain-restriction obligation is surfaced as a blocking checkpoint + user_setup: URL-restrict to bayrak.ai before any external dashboard share (D-62 / SC4)"
  artifacts:
    - path: "next.config.ts"
      provides: "images.remotePatterns for Vercel Blob host"
      contains: "public.blob.vercel-storage.com"
    - path: "messages/tr.json"
      provides: "dashboard.submissions + dashboard.map namespaces + dashboard.boq.col_completion_pct"
      contains: "submissions"
    - path: "messages/en.json"
      provides: "EN parity of the TR keys"
      contains: "submissions"
    - path: "tests/submissions.test.ts"
      provides: "DASH-01/02/03 test scaffold (describeIfDb + seedSpatialFixture)"
      contains: "getApprovedPoints"
    - path: "tests/boq.test.ts"
      provides: "DASH-04 progress-% edge-case tests"
      contains: "100"
  key_links:
    - from: "next.config.ts images.remotePatterns"
      to: "next/image photo thumbnails (Plan 04 / Plan 03)"
      via: "remotePatterns allowlist"
      pattern: "public\\.blob\\.vercel-storage\\.com"
    - from: "messages/{tr,en}.json dashboard.submissions"
      to: "KayitlarTabClient useTranslations('dashboard.submissions')"
      via: "next-intl message lookup"
      pattern: "dashboard"
---

<objective>
Lay the Phase 5 foundation: install the locked map stack, configure next/image for Vercel Blob photos, add the complete TR/EN message catalog for every new surface, and create the Wave 0 test scaffolds that subsequent plans turn GREEN.

Purpose: All downstream plans (data layer, map, Kayıtlar, BOQ progress) depend on these dependencies, message keys, and config existing first. The test scaffolds satisfy the VALIDATION.md Nyquist contract — they must exist (RED) before the data layer is implemented.

Output: Installed map deps, extended next.config.ts, complete messages/tr.json + messages/en.json, tests/submissions.test.ts (new, RED), extended tests/boq.test.ts (progress-% edge cases).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-dashboard-map/05-CONTEXT.md
@.planning/phases/05-dashboard-map/05-RESEARCH.md
@.planning/phases/05-dashboard-map/05-VALIDATION.md
@.planning/phases/05-dashboard-map/05-UI-SPEC.md
@CLAUDE.md

<interfaces>
<!-- Concrete contracts the executor needs. Do NOT re-explore the codebase. -->

next.config.ts (CURRENT — extend, do not replace):
```
const nextConfig: NextConfig = {
  serverExternalPackages: ["grammy", "pg", "ws", "@neondatabase/serverless"],
};
export default withNextIntl(nextConfig);
```

i18n is next-intl with messages at `messages/tr.json` and `messages/en.json` (project root), loaded by src/i18n/request.ts (`../../messages/${locale}.json`). Default locale = 'tr'.

Existing dashboard namespace shape in BOTH files:
- dashboard.projects.*, dashboard.boq.*, dashboard.route.*, dashboard.people.*, common.*
- dashboard.boq keys today: title, add_item, import_excel, download_template, confirm_import, empty_state, col_number, col_material, col_unit, col_contracted_qty, col_approved_qty, col_remaining, col_actions, remove_item, remove_item_confirm, remove_item_cta, validating, import_count, excel_not_xlsx, row_parse_error

Test fixtures (tests/fixtures/db.ts — already present, import from there):
- describeIfDb, getTestDb, truncateAllTables, seedSpatialFixture, SPATIAL_FIXTURE_IDS
- SPATIAL_FIXTURE_IDS = { tenantId '..0001', projectId '..0002', routeId '..0003', boqItemId '..0010', personId '..0011' }
- seedSpatialFixture inserts tenant+project+boqItem+person+route (Istanbul LineString [[28.9,41.0],[28.95,41.05]]) but NOT a submissions row.
- truncateAllTables already lists submissions + audit_notifications in correct FK order.

submissions schema columns (src/db/schema/submissions.ts): id, tenantId, flowId, personId, projectId, boqItemId, photoUrl, location, locationLat, locationLon, snappedPoint, segmentFraction, locationMatch ('near'|'far'|'no_route'), locationWarning, locationDistanceM, quantity, notes, status ('pending_audit'|'approved'|'rejected'), submittedAt, decidedBy, decidedAt, rejectionReason.

Existing boq.test.ts structure (extend, do not rewrite): pure `describe('remainingBalance helper', ...)` block at top; `describeIfDb('BOQ Server Actions (DB)', ...)` below. Mocks next/cache + @/lib/auth already declared at top.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install map stack and configure next/image for Vercel Blob</name>
  <files>package.json, package-lock.json, next.config.ts</files>
  <read_first>
    - package.json (current deps/devDeps + dev script "next dev --turbopack")
    - next.config.ts (current config — extend, do not replace)
    - .planning/phases/05-dashboard-map/05-RESEARCH.md (Standard Stack versions; Pattern 6 remotePatterns; Pitfall 1 Turbopack)
  </read_first>
  <action>
    Verify exact versions first: run `npm view mapbox-gl version`, `npm view react-map-gl version`, `npm view @types/mapbox-gl version` (expect 3.24.0 / 8.1.1 / 3.5.0; these are Approved in RESEARCH Package Legitimacy Audit). Then install `mapbox-gl@3.24.x react-map-gl@8.1.x` as dependencies and `@types/mapbox-gl` as a devDependency.
    Extend next.config.ts: add an `images.remotePatterns` array containing exactly `new URL('https://*.public.blob.vercel-storage.com/**')` — the single Vercel Blob host pattern (V5 SSRF mitigation T-05-01: scope to this host only, never a bare wildcard). Keep the existing `serverExternalPackages` array unchanged. Do NOT add mapbox-gl to `transpilePackages` or `serverExternalPackages` (RESEARCH anti-pattern — conflicts). Preserve the `export default withNextIntl(nextConfig)` wrapper.
    Record the Turbopack dev workaround in the plan SUMMARY: development requires `npm run dev -- --no-turbopack` (or remove `--turbopack` from the dev script) because Turbopack tears down mapbox-gl's web worker (RESEARCH Pitfall 1 / vercel/next.js#86495); production builds are unaffected.
  </action>
  <verify>
    <automated>node -e "require.resolve('mapbox-gl'); require.resolve('react-map-gl/mapbox'); console.log('deps ok')" && grep -q "public.blob.vercel-storage.com" next.config.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "require.resolve('react-map-gl/mapbox')"` resolves without error (v8 subpath export present)
    - package.json lists mapbox-gl ^3.24, react-map-gl ^8.1 in dependencies and @types/mapbox-gl in devDependencies
    - next.config.ts contains `images.remotePatterns` with `*.public.blob.vercel-storage.com` and retains the original `serverExternalPackages` entries
    - mapbox-gl appears in NEITHER transpilePackages NOR serverExternalPackages
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Map deps installed, next.config images.remotePatterns scoped to the Vercel Blob host, typecheck clean, Turbopack dev note recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Add full TR/EN message catalog for all new Phase 5 surfaces (D-63 / I18N-02)</name>
  <files>messages/tr.json, messages/en.json</files>
  <read_first>
    - messages/tr.json (full file — current dashboard.* and common.* structure)
    - messages/en.json (full file — must stay key-for-key in parity with tr.json)
    - .planning/phases/05-dashboard-map/05-UI-SPEC.md (Copywriting Contract + Empty States tables — these are the LOCKED strings)
  </read_first>
  <action>
    Add a new `dashboard.submissions` namespace and a new `dashboard.map` namespace to BOTH messages/tr.json and messages/en.json, and add `col_completion_pct` to the existing `dashboard.boq` namespace in both files. Use the LOCKED copy from UI-SPEC Copywriting Contract + Empty States.
    dashboard.submissions keys (TR / EN): tab_label ("Kayıtlar" / "Submissions"), filter_all ("Tümü" / "All"), filter_pending ("Bekliyor" / "Pending"), filter_approved ("Onaylandı" / "Approved"), filter_rejected ("Reddedildi" / "Rejected"), col_photo ("Fotoğraf" / "Photo"), col_boq ("BOQ Kalemi" / "BOQ Item"), col_quantity ("Miktar" / "Quantity"), col_status ("Durum" / "Status"), col_date ("Tarih" / "Date"), col_location ("Konum" / "Location"), col_notes ("Notlar" / "Notes"), status_pending ("Beklemede" / "Pending"), status_approved ("Onaylandı" / "Approved"), status_rejected ("Reddedildi" / "Rejected"), empty_all ("Henüz kayıt yok." / "No submissions yet."), empty_filtered ("Bu filtre için kayıt bulunamadı." / "No submissions found for this filter."), pagination ("Sayfa {page} / {pages}" / "Page {page} of {pages}"), photo_alt ("{material} fotoğrafı" / "{material} photo"), prev ("Önceki" / "Previous"), next ("Sonraki" / "Next").
    dashboard.map keys (TR / EN): empty_no_route ("Rotayı görmek için GeoJSON dosyası yükleyin." / "Upload a GeoJSON file to see the route."), empty_no_points ("Henüz onaylı iş noktası yok." / "No approved work points yet."), popup_quantity ("Miktar" / "Quantity"), popup_status ("Durum" / "Status"), popup_date ("Tarih" / "Date"), popup_auditor ("Onaylayan" / "Approved by"), popup_distance ("Güzergahtan uzak — {meters} m" / "Far from route — {meters} m"), legend_title ("Lejant" / "Legend"), load_error ("Veriler yüklenemedi. Sayfayı yenileyiniz." / "Failed to load data. Please refresh the page.").
    dashboard.boq add (TR / EN): col_completion_pct ("% Tamamlanan" / "% Complete").
    Use next-intl ICU placeholder syntax `{name}` exactly as shown (page, pages, material, meters). No hardcoded strings may remain in any Phase 5 component — these keys are the contract Plans 03/04/05 consume.
  </action>
  <verify>
    <automated>node -e "const tr=require('./messages/tr.json'),en=require('./messages/en.json'); const need={'dashboard.submissions':['tab_label','filter_all','col_photo','status_approved','empty_all','pagination'],'dashboard.map':['empty_no_route','popup_distance','legend_title'],'dashboard.boq':['col_completion_pct']}; for(const [ns,keys] of Object.entries(need)){const [a,b]=ns.split('.'); for(const k of keys){if(!tr[a]?.[b]?.[k]) throw new Error('TR missing '+ns+'.'+k); if(!en[a]?.[b]?.[k]) throw new Error('EN missing '+ns+'.'+k);}} console.log('i18n parity ok')"</automated>
  </verify>
  <acceptance_criteria>
    - messages/tr.json and messages/en.json both contain dashboard.submissions, dashboard.map, and dashboard.boq.col_completion_pct
    - Every key present in tr.json dashboard.submissions/dashboard.map exists in en.json (key-for-key parity)
    - dashboard.submissions.pagination contains the ICU placeholders `{page}` and `{pages}`; dashboard.map.popup_distance contains `{meters}`; dashboard.submissions.photo_alt contains `{material}`
    - TR strings match UI-SPEC Copywriting Contract exactly (e.g. dashboard.submissions.empty_all === "Henüz kayıt yok.")
    - Both files remain valid JSON (the verify node script parses them)
  </acceptance_criteria>
  <done>Both locale files carry the complete new namespaces with ICU placeholders and exact UI-SPEC copy; EN is in full parity with TR.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wave 0 test scaffolds — tests/submissions.test.ts (new, RED) + tests/boq.test.ts progress-% extension</name>
  <files>tests/submissions.test.ts, tests/boq.test.ts</files>
  <read_first>
    - tests/boq.test.ts (full — copy its describeIfDb + mock + beforeEach/afterEach structure; extend the pure top block)
    - tests/fixtures/db.ts (describeIfDb, getTestDb, truncateAllTables, seedSpatialFixture, SPATIAL_FIXTURE_IDS)
    - tests/postgis.test.ts (ST_AsGeoJSON read-back assertion pattern; coordinate-order read-back)
    - .planning/phases/05-dashboard-map/05-VALIDATION.md (Per-Task Verification Map — the exact behaviors DASH-01/02/03/04 must assert)
    - .planning/phases/05-dashboard-map/05-PATTERNS.md (tests/submissions.test.ts section — exact test stubs)
  </read_first>
  <behavior>
    tests/submissions.test.ts (covers DASH-01, DASH-02, DASH-03) — gated behind describeIfDb, mocks next/cache + @/lib/auth like boq.test.ts:
    - DASH-01: getRouteGeoJSON(projectId) returns an object whose `geojson` is `{ type: 'LineString', coordinates: [...] }` with coordinates in [lng, lat] order (28.9 before 41.0); returns null when the project has no route.
    - DASH-02: getApprovedPoints(projectId) returns a GeoJSON FeatureCollection containing ONLY submissions with status='approved' AND snapped_point IS NOT NULL (D-46). Seed three submissions (approved+snapped, approved+no-snap, pending+snapped); assert exactly one feature, geometry.type === 'Point', and its coordinates are [lng, lat].
    - DASH-03: getSubmissions(projectId, {status}) filters to only matching rows; getSubmissions paginates (insert pageSize+1 rows, page 2 returns the overflow row, pageCount correct); getSubmissions rejects an invalid status string (rejects/throws).
    tests/boq.test.ts extension (covers DASH-04 progress %), added to the existing pure top-level describe block (no DB needed — pure math):
    - completion % capped at 100 when over-approved (planned 100, approved 150 -> 100).
    - completion % is 0 when planned is 0 (division guard).
    - completion % is exact for partial (planned 1000, approved 250 -> 25).
  </behavior>
  <action>
    Create tests/submissions.test.ts following the boq.test.ts scaffold: same `vi.mock('next/cache', ...)` and `vi.mock('@/lib/auth', { auth: vi.fn().mockResolvedValue({ user: { email: 'test@example.com' } }) })` at top; `describeIfDb('submissions Server Actions (DB)', ...)` with beforeEach calling `getTestDb()`, `truncateAllTables(db)`, then `seedSpatialFixture(db)` (gives tenant/project/route/boqItem/person via SPATIAL_FIXTURE_IDS); afterEach truncates. Insert submissions rows directly via the submissions schema using SPATIAL_FIXTURE_IDS for FKs and `ST_GeomFromGeoJSON(${pointJson})` for snapped_point (parameterized sql`` — never string-interpolate geometry). Import the (not-yet-implemented) functions via dynamic `await import('@/actions/submissions')` inside each test so the file compiles before Plan 02 lands. These tests SHOULD FAIL now (RED) because src/actions/submissions.ts does not exist yet — that is expected and correct for Wave 0.
    Extend tests/boq.test.ts: add the three progress-% edge-case `it(...)` cases to the EXISTING top-level pure `describe('remainingBalance helper', ...)` block (or a new sibling pure describe `describe('BOQ completion percentage', ...)`). Use the formula `planned > 0 ? Math.min((approved / planned) * 100, 100) : 0`. Do not touch the describeIfDb DB block. These are pure and pass immediately.
    Confirm both files run: `npx vitest run tests/submissions.test.ts tests/boq.test.ts` must execute (DB tests skip cleanly via describeIfDb when TEST_DATABASE_URL is unset; the boq progress-% tests pass; submissions DB tests are RED only when a DB is present, which is acceptable Wave 0 state).
  </action>
  <verify>
    <automated>npx vitest run tests/boq.test.ts && test -f tests/submissions.test.ts && grep -q "getApprovedPoints" tests/submissions.test.ts && grep -q "Math.min" tests/boq.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/submissions.test.ts exists, imports describeIfDb + seedSpatialFixture + SPATIAL_FIXTURE_IDS from ./fixtures/db, and references getRouteGeoJSON, getApprovedPoints, getSubmissions
    - tests/submissions.test.ts uses `await import('@/actions/submissions')` (dynamic) so the file compiles even though the module does not exist yet
    - `npx vitest run tests/boq.test.ts` exits 0 with the three new progress-% cases passing (0%, 100% cap, 25% partial)
    - tests/boq.test.ts contains the `Math.min((approved / planned) * 100, 100)` capped-percentage formula
    - No watch-mode flag used (vitest run, not vitest)
  </acceptance_criteria>
  <done>Submissions test scaffold exists and is RED-ready for Plan 02; boq progress-% edge cases pass; both files invoke under one vitest run command per VALIDATION sampling rate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Confirm Mapbox token + D-62 domain-restriction obligation</name>
  <action>Pause for the developer to confirm NEXT_PUBLIC_MAPBOX_TOKEN is present and to acknowledge the D-62/SC4 token domain-restriction obligation. Verify via the steps below; do not auto-advance.</action>
  <what-built>Map stack installed (mapbox-gl 3.24, react-map-gl 8.1, @types/mapbox-gl), next.config images.remotePatterns scoped to the Vercel Blob host, full TR/EN message catalog, Wave 0 test scaffolds.</what-built>
  <how-to-verify>
    1. Confirm `NEXT_PUBLIC_MAPBOX_TOKEN` is set in `.env.local` (a free Mapbox public token works for development). Without it the map renders the empty state.
    2. SC4 / D-62 ops gate (record intent now; enforce before any external share): in account.mapbox.com, the public token MUST be URL-restricted to `https://bayrak.ai/*` (and www variant) before any dashboard URL is shared externally. Confirm you will apply this restriction prior to sharing. (This is account config, not code — it is the only manual step in Phase 5.)
    3. Confirm the dev workaround is understood: run the dev server with `npm run dev -- --no-turbopack` while working on the map (Turbopack breaks mapbox-gl's worker; production unaffected).
  </how-to-verify>
  <resume-signal>Type "approved" once the Mapbox token is present in .env.local and you have acknowledged the D-62 domain-restriction obligation, or describe what is missing.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → next/image optimizer | Photo URLs from feature/table data flow into the next/image optimizer; an unconstrained host list is an SSRF vector |
| build/runtime → npm registry | New package installs (mapbox-gl, react-map-gl) introduce third-party code |
| public token in page source → Mapbox API | NEXT_PUBLIC_MAPBOX_TOKEN is readable by anyone; an unrestricted token is billable from any origin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering (SSRF) | next/image remotePatterns | mitigate | Restrict remotePatterns to exactly `*.public.blob.vercel-storage.com` — never a bare-domain wildcard (Task 1) |
| T-05-EP | Elevation of Privilege | NEXT_PUBLIC_MAPBOX_TOKEN | mitigate | D-62: URL-restrict token to bayrak.ai in Mapbox dashboard before external share; surfaced as blocking-human checkpoint + user_setup |
| T-05-SC | Tampering | npm installs (mapbox-gl, react-map-gl, @types/mapbox-gl) | accept | RESEARCH Package Legitimacy Audit marks all three Approved (official Mapbox/vis.gl/DefinitelyTyped, no postinstall, 10+ yr history). No [ASSUMED]/[SUS]/[SLOP] packages → no blocking install gate required |
</threat_model>

<verification>
- `node -e "require.resolve('react-map-gl/mapbox')"` resolves
- `npx tsc --noEmit` exits 0
- `grep public.blob.vercel-storage.com next.config.ts` matches
- i18n parity script passes (TR/EN key-for-key)
- `npx vitest run tests/boq.test.ts` green; tests/submissions.test.ts present and RED-ready
</verification>

<success_criteria>
- Locked map stack installed at pinned major versions
- next/image SSRF surface scoped to the single Blob host (T-05-01)
- Complete, parity-checked TR/EN catalog for every new surface (I18N-02 / D-63)
- Wave 0 test scaffolds exist per VALIDATION.md (DASH-01/02/03 RED stubs, DASH-04 progress-% GREEN)
- Mapbox token present + D-62 domain-restriction obligation acknowledged
</success_criteria>

<output>
Create `.planning/phases/05-dashboard-map/05-01-SUMMARY.md` when done. Record: exact installed versions, the Turbopack dev workaround, the list of new i18n keys, and the D-62 token-restriction obligation as a carry-forward item.
</output>
