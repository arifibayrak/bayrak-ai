---
phase: 05-dashboard-map
plan: 03
type: execute
wave: 3
depends_on: [05-01, 05-02]
files_modified:
  - src/components/dashboard/MapView.tsx
  - src/components/dashboard/RouteTab.tsx
  - src/components/dashboard/RouteTabClient.tsx
autonomous: false
requirements: [DASH-01, DASH-02]

must_haves:
  truths:
    - "The Rota tab renders the pipeline route as a Mapbox line layer (DASH-01)"
    - "Approved submissions render as circle markers color-coded by BOQ item, snapped to the route (DASH-02)"
    - "location_match='far' points get a distinct amber warning ring layered over the BOQ color (D-52, shape not hue alone)"
    - "Clicking a point opens a popup with photo thumbnail, BOQ item, quantity, status, date, auditor (and distance if far) (D-51)"
    - "On load the map fitBounds to the route bounding box; no route falls through to the upload empty state (D-56/D-57)"
    - "A legend maps BOQ color → material (D-58); hidden when no approved points"
    - "The map is genuinely responsive: fills available width at h-[600px] desktop / h-[400px] mobile with touch-friendly controls (D-60)"
    - "Practical accessibility: color is never the only signal — legend + text labels plus shape-coded anomaly ring (D-52), with sufficient route/marker contrast (D-59)"
  artifacts:
    - path: "src/components/dashboard/MapView.tsx"
      provides: "'use client' react-map-gl map: route line + approved-points + anomaly-ring layers + popup + legend + empty states"
      contains: "react-map-gl/mapbox"
      min_lines: 90
    - path: "src/components/dashboard/RouteTab.tsx"
      provides: "Server Component fetching route GeoJSON + approved points + legend, passing to client"
      contains: "getApprovedPoints"
    - path: "src/components/dashboard/RouteTabClient.tsx"
      provides: "Mounts MapView when a route exists; keeps the upload/replace control"
      contains: "MapView"
  key_links:
    - from: "src/components/dashboard/MapView.tsx"
      to: "react-map-gl/mapbox Source/Layer/Popup"
      via: "import Map, { Source, Layer, Popup } from 'react-map-gl/mapbox'"
      pattern: "react-map-gl/mapbox"
    - from: "src/components/dashboard/RouteTab.tsx"
      to: "getRouteGeoJSON + getApprovedPoints + getBoqLegend"
      via: "Promise.all server fetch"
      pattern: "getApprovedPoints"
    - from: "src/components/dashboard/MapView.tsx"
      to: "mapbox-gl CSS"
      via: "import 'mapbox-gl/dist/mapbox-gl.css'"
      pattern: "mapbox-gl/dist/mapbox-gl.css"
---

<objective>
Upgrade the Rota tab from a static upload widget into a live react-map-gl map: the route LineString, approved submission points color-coded by BOQ item, an amber anomaly ring for far points, a click popup, a legend, and graceful empty states.

Purpose: This is the visual core of DASH-01 and DASH-02. All data is pre-fetched and serialized by the Plan 02 server actions; this plan is the client rendering layer plus its server wrapper.

Output: New MapView.tsx ('use client'), extended RouteTab.tsx (Server) and RouteTabClient.tsx (client mount), preserving the existing route upload/replace flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-dashboard-map/05-RESEARCH.md
@.planning/phases/05-dashboard-map/05-PATTERNS.md
@.planning/phases/05-dashboard-map/05-UI-SPEC.md
@.planning/phases/05-dashboard-map/05-CONTEXT.md
@.planning/phases/05-dashboard-map/05-02-SUMMARY.md

<interfaces>
<!-- Locked stack + exact contracts. CLAUDE.md locks react-map-gl@8.1 → import from 'react-map-gl/mapbox'. -->

react-map-gl v8 imports (mapbox-gl >= 3.5 → 'react-map-gl/mapbox', NOT 'react-map-gl' — RESEARCH Pitfall 2):
```
'use client';
import Map, { Source, Layer, Popup } from 'react-map-gl/mapbox';
import type { MapRef, LayerProps, MapLayerMouseEvent } from 'react-map-gl/mapbox';
import 'mapbox-gl/dist/mapbox-gl.css';   // MANDATORY — map invisible/controls broken without it (Pitfall 3)
```
Token: `process.env.NEXT_PUBLIC_MAPBOX_TOKEN` passed as `mapboxAccessToken`. fitBounds via `mapRef.current.getMap().fitBounds(bounds, { padding: 48, animate: false })` (NOT mapRef.current.fitBounds — Pitfall/State-of-the-art).

Data from Plan 02 actions (see 05-02-SUMMARY.md for exact shapes):
- getRouteGeoJSON(projectId) → { id, coordinateCount, uploadedAt: string, geojson: { type:'LineString', coordinates:[[lng,lat],...] } } | null
- getApprovedPoints(projectId) → GeoJSON FeatureCollection; feature.properties: { id, boqItemId, boqPaletteSlot:0-5, boqMaterial, locationWarning:boolean, locationDistanceM:number|null, quantity:number, unit, photoUrl, status, decidedAt:string|null, auditorName:string|null }
- getBoqLegend(projectId) → [{ id, material, paletteSlot }]

UI-SPEC visual contract (LOCKED):
- Route line layer: type 'line', color '#64748B', width 3, opacity 0.8
- Approved points circle layer id 'approved-points': data-driven `['match', ['get','boqPaletteSlot'], 0,'#2563EB', 1,'#D97706', 2,'#7C3AED', 3,'#059669', 4,'#DB2777', 5,'#0891B2', '#94A3B8']`, radius 8, white 2px stroke
- Anomaly ring layer (declare BEFORE the circle layer so it renders behind): filter `['==', ['get','locationWarning'], true]`, radius 14, transparent fill, stroke 3px `hsl(38 92% 50%)`
- Map height: desktop h-[600px], mobile h-[400px]; mapStyle "mapbox://styles/mapbox/streets-v12"; interactiveLayerIds={['approved-points']}
- Popup: next/image 80×80 object-cover; BOQ item (Label), quantity+unit (Body), status badge, date toLocaleDateString('tr-TR'), auditor (muted); if locationWarning show dashboard.map.popup_distance with {meters}
- Legend: bottom-left overlay Card bg-background/90; hidden if FeatureCollection empty
- Empty states (next-intl, keys from Plan 01): dashboard.map.empty_no_route (no route → upload CTA), dashboard.map.empty_no_points (route but no approved points → subtle note)

next-intl client usage: `import { useTranslations } from 'next-intl'; const t = useTranslations('dashboard.map');` Photo alt uses dashboard.submissions.photo_alt with {material}.

RouteTab.tsx CURRENT (Server Component — extend, keep heading + RouteTabClient):
```
export async function RouteTab({ projectId }) {
  const t = await getTranslations('dashboard.route');
  const existingRoute = await getRoute(projectId);
  return (<div className="space-y-6"><h2 ...>{t('title')}</h2>
    <RouteTabClient projectId={projectId} existingRoute={existingRoute} /></div>);
}
```

RouteTabClient.tsx CURRENT: 'use client'; ExistingRoute = { id, coordinateCount, uploadedAt: Date }; renders saved-route metadata card + Replace button when savedRoute && !isReplacing, else RouteUpload. NOTE: it currently receives uploadedAt as a Date — when adding map props, serialize Date to string upstream (RESEARCH Pitfall 5 / PATTERNS date-serialization rule).
</interfaces>

<react_guidance>
The map is a 'use client' leaf. Keep all data fetching in the Server Component (RouteTab); pass only serialized GeoJSON + plain objects. Use `useRef<MapRef>`, `useState` for popupInfo, `useCallback` for onLoad/handleMapClick. Do NOT use individual `<Marker>` components for the points (re-render cost) — use a `<Source>` + circle `<Layer>` (RESEARCH anti-pattern).
</react_guidance>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MapView.tsx — react-map-gl map with route line, point layers, anomaly ring, popup, legend, empty states</name>
  <files>src/components/dashboard/MapView.tsx</files>
  <read_first>
    - src/components/dashboard/RouteTabClient.tsx (client-boundary + props-interface + early-return empty-state idiom to mirror)
    - src/components/dashboard/BoqTable.tsx (Intl.NumberFormat('tr-TR') + tabular-nums + Badge usage; status-badge styling reference)
    - .planning/phases/05-dashboard-map/05-RESEARCH.md (Patterns 1,2,3; Pitfalls 2,3; Code Examples — layer styles, handleMapClick, onLoad fitBounds)
    - .planning/phases/05-dashboard-map/05-UI-SPEC.md (Rota tab map layout, popup content, legend, color hex palette, anomaly ring spec)
    - .planning/phases/05-dashboard-map/05-02-SUMMARY.md (exact feature.properties + legend shape from Plan 02)
  </read_first>
  <action>
    Create src/components/dashboard/MapView.tsx as a `'use client'` component. Import Map, Source, Layer, Popup and types MapRef/LayerProps/MapLayerMouseEvent from `'react-map-gl/mapbox'` (NOT 'react-map-gl' — Pitfall 2) and import `'mapbox-gl/dist/mapbox-gl.css'` (Pitfall 3). Props interface: `{ routeGeoJSON: GeoJSON.LineString | null; approvedPoints: GeoJSON.FeatureCollection; boqLegend: Array<{ id: string; material: string; paletteSlot: number }> }`.
    Empty state: if routeGeoJSON is null, return the upload empty state using `useTranslations('dashboard.map')('empty_no_route')` — do NOT render a blank map (D-56/D-57). (The upload control itself stays in RouteTabClient; MapView's null branch shows the guided text.)
    Define the three layer styles exactly per UI-SPEC: routeLayerStyle (line #64748B width 3 opacity 0.8), circleLayerStyle id 'approved-points' with the `['match', ['get','boqPaletteSlot'], 0,'#2563EB',1,'#D97706',2,'#7C3AED',3,'#059669',4,'#DB2777',5,'#0891B2','#94A3B8']` color expression + radius 8 + white 2px stroke, and anomalyRingStyle with filter `['==',['get','locationWarning'],true]` radius 14 transparent fill amber stroke `hsl(38,92%,50%)`. Render `<Source id="route" type="geojson" data={routeGeoJSON}>` with the line layer, and `<Source id="approved" ...>` with the anomaly-ring layer declared BEFORE the circle layer (behind). Set `mapboxAccessToken={process.env.NEXT_PUBLIC_MAPBOX_TOKEN}`, `mapStyle="mapbox://styles/mapbox/streets-v12"`, `interactiveLayerIds={['approved-points']}`, responsive height via className (desktop h-[600px], mobile h-[400px]) per D-60.
    onLoad (useCallback): compute bounds from routeGeoJSON.coordinates min/max and call `mapRef.current.getMap().fitBounds(bounds, { padding: 48, animate: false })` (D-56; getMap() required). handleMapClick (useCallback): read `event.features?.[0]`, guard Point geometry, set popupInfo from coordinates + feature.properties. Render `<Popup>` conditionally with photo (`next/image` 80×80 object-cover, alt from dashboard.submissions.photo_alt with {material}), BOQ material (Label), quantity+unit via Intl.NumberFormat('tr-TR'), a status badge matching the UI-SPEC status spec, date via toLocaleDateString('tr-TR'), auditor name (muted), and — only when `locationWarning` — the amber `dashboard.map.popup_distance` line with {meters} = locationDistanceM.
    Legend: render a bottom-left overlay Card (`bg-background/90`) listing each boqLegend entry as material + a colored swatch from the same palette index; hide the legend entirely when approvedPoints.features.length === 0, and in that case render the `dashboard.map.empty_no_points` subtle note instead. Every visible string MUST come from next-intl — no hardcoded copy (D-63).
  </action>
  <verify>
    <human-check>Per VALIDATION.md, react-map-gl canvas rendering is manual: load a project with a route + approved submissions; confirm the route renders as a line, approved points render as palette-colored circles snapped to the route, a far point shows the amber ring, clicking a point opens the popup with photo/BOQ/qty/status/date/auditor, and the legend maps color→material.</human-check>
  </verify>
  <acceptance_criteria>
    - MapView.tsx imports from 'react-map-gl/mapbox' (NOT bare 'react-map-gl') and imports 'mapbox-gl/dist/mapbox-gl.css' (grep both)
    - The circle-color paint uses the exact 6-slot match expression hex values from UI-SPEC
    - The anomaly-ring `<Layer>` is declared before the circle `<Layer>` in the approved Source (renders behind)
    - fitBounds is called via `mapRef.current.getMap().fitBounds(...)` with padding 48
    - No individual `<Marker>` is used for the points (uses Source+Layer)
    - Null routeGeoJSON returns the empty-state branch, never a blank `<Map>`
    - No hardcoded user-facing string — all via useTranslations; `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>MapView renders the route, color-coded snapped points, anomaly ring, click popup, and legend with empty states, all i18n-driven and type-clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire RouteTab (server fetch) + RouteTabClient (mount MapView, keep upload)</name>
  <files>src/components/dashboard/RouteTab.tsx, src/components/dashboard/RouteTabClient.tsx</files>
  <read_first>
    - src/components/dashboard/RouteTab.tsx (full — extend the server fetch)
    - src/components/dashboard/RouteTabClient.tsx (full — extend the conditional render; keep upload/replace)
    - src/components/dashboard/MapView.tsx (the component Task 1 created — its props contract)
    - src/actions/submissions.ts + src/actions/routes.ts (getRouteGeoJSON, getApprovedPoints, getBoqLegend signatures)
    - src/app/dashboard/projects/[id]/page.tsx (the Promise.all parallel-fetch idiom to mirror)
  </read_first>
  <action>
    Extend RouteTab.tsx (Server Component): replace the single `getRoute` call with a `Promise.all` fetching `getRouteGeoJSON(projectId)`, `getApprovedPoints(projectId)`, and `getBoqLegend(projectId)` (import getApprovedPoints + getBoqLegend from '@/actions/submissions', getRouteGeoJSON from '@/actions/routes'). Keep the `<h2>{t('title')}</h2>` heading. Pass to RouteTabClient: `routeGeoJSON` (the `.geojson` field or null), `approvedPoints`, `boqLegend`, and an `existingRoute` summary `{ id, coordinateCount, uploadedAt: <ISO string> }` for the replace card — serialize uploadedAt to a string here (RESEARCH Pitfall 5; the current code passes a Date, which must be fixed).
    Extend RouteTabClient.tsx: update the ExistingRoute interface so `uploadedAt` is `string` (not Date) and adjust the metadata-card formatting to parse the ISO string with `new Date(savedRoute.uploadedAt).toLocaleDateString('tr-TR', ...)`. Add the new props (routeGeoJSON, approvedPoints, boqLegend). When a route exists and not replacing, render `<MapView routeGeoJSON={routeGeoJSON} approvedPoints={approvedPoints} boqLegend={boqLegend} />` ABOVE the existing upload/replace control (D-49: the Rota tab becomes the map but keeps the upload control as a secondary action below). Preserve the RouteUpload flow and the Replace button behavior exactly.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint src/components/dashboard/RouteTab.tsx src/components/dashboard/RouteTabClient.tsx src/components/dashboard/MapView.tsx</automated>
  </verify>
  <acceptance_criteria>
    - RouteTab.tsx fetches getRouteGeoJSON + getApprovedPoints + getBoqLegend via Promise.all and passes them to RouteTabClient
    - RouteTabClient ExistingRoute.uploadedAt is typed `string` and the card uses `new Date(...).toLocaleDateString('tr-TR', ...)`
    - RouteTabClient renders `<MapView .../>` when a route exists, with the upload/replace control retained below (D-49)
    - No Date instance is passed RSC→client (uploadedAt serialized to ISO in RouteTab)
    - `npx tsc --noEmit` exits 0 and eslint passes on the three files
  </acceptance_criteria>
  <done>The Rota tab server-fetches map data and mounts MapView while keeping the route upload/replace flow; no serialization warnings.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify the live Mapbox map renders (route, points, ring, popup, legend)</name>
  <action>Pause for the developer to visually verify the map per VALIDATION.md (canvas rendering is manual). Run the verification steps below in a non-Turbopack dev server.</action>
  <what-built>The Rota tab now renders a live Mapbox map: route line, color-coded approved-point markers snapped to the route, amber anomaly ring on far points, click popup, and a color→material legend, with guided empty states.</what-built>
  <how-to-verify>
    1. Start the dev server with `npm run dev -- --no-turbopack` (Turbopack breaks mapbox-gl's worker — RESEARCH Pitfall 1).
    2. Open a project that has a route + at least one approved submission. On the Rota tab confirm: the route renders as a slate line; approved submissions render as circles color-coded by BOQ item, sitting on the route; the legend (bottom-left) maps each color to its material.
    3. If any submission has location_match='far', confirm it shows the amber warning ring AND the popup shows "Güzergahtan uzak — {N} m".
    4. Click a marker — confirm the popup shows the photo thumbnail, BOQ item, quantity, status badge, date, and deciding auditor.
    5. Open a project with NO route — confirm the upload empty state appears (no blank/broken map). Open a project with a route but no approved points — confirm the route line shows with the "Henüz onaylı iş noktası yok." note and no legend.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any rendering issue (blank tiles → check token + non-turbopack dev; missing markers → check ST_AsGeoJSON output).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server action → client map props | GeoJSON + properties (incl. photoUrl) cross into a 'use client' component |
| photoUrl → next/image | Popup photo thumbnail loads a Blob URL via next/image |
| public token in client bundle → Mapbox | NEXT_PUBLIC_MAPBOX_TOKEN ships to the browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-XSS | Tampering (XSS via photo URL) | MapView Popup photo | mitigate | Render photo only through next/image src={photoUrl}; Next validates against remotePatterns (Plan 01 T-05-01) — off-host URLs are blocked, not rendered raw |
| T-05-AC | Information Disclosure | approvedPoints/route data in client | accept | Data is already auth+tenant-scoped server-side (Plan 02 T-05-AC); only the owning tenant's authorized engineer reaches this page |
| T-05-EP | Elevation of Privilege | NEXT_PUBLIC_MAPBOX_TOKEN in bundle | mitigate | D-62 domain restriction (Plan 01 checkpoint + user_setup) — token usable only from bayrak.ai before external sharing |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `grep "react-map-gl/mapbox" src/components/dashboard/MapView.tsx` matches; bare `from 'react-map-gl'` absent
- `grep "mapbox-gl/dist/mapbox-gl.css" src/components/dashboard/MapView.tsx` matches
- Manual map render checkpoint approved (canvas rendering is manual per VALIDATION.md)
</verification>

<success_criteria>
- DASH-01: route renders as a Mapbox line, auto-framed via fitBounds
- DASH-02: approved points render as palette-colored circles snapped to the route, far points carry the amber ring, popup is rich (D-51), legend maps color→material (D-58)
- Empty states are guided, never a broken map (D-57)
- Map fills width and is touch-usable on mobile (D-60)
</success_criteria>

<output>
Create `.planning/phases/05-dashboard-map/05-03-SUMMARY.md` when done. Record the MapView props contract, the layer ids, and confirm the Turbopack-free dev requirement for any future map work.
</output>
