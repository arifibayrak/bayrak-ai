---
phase: 05-dashboard-map
plan: 05
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/components/dashboard/BoqTable.tsx
autonomous: true
requirements: [DASH-04]

must_haves:
  truths:
    - "Each BOQ row shows a % Tamamlanan column = approvedQty/plannedQty*100, capped at 100 (DASH-04 / D-50)"
    - "Each BOQ row shows a shadcn Progress bar reflecting the same completion percentage"
    - "A 0% item renders an empty progress bar (no special state); planned=0 shows 0% without dividing by zero (D-57)"
    - "Existing columns (number, material, unit, contracted, approved, remaining, actions) are preserved"
  artifacts:
    - path: "src/components/dashboard/BoqTable.tsx"
      provides: "% Tamamlanan column + per-row Progress bar inline in the existing table"
      contains: "Progress"
  key_links:
    - from: "src/components/dashboard/BoqTable.tsx"
      to: "shadcn Progress component"
      via: "import { Progress } from '@/components/ui/progress'"
      pattern: "@/components/ui/progress"
    - from: "src/components/dashboard/BoqTable.tsx % column"
      to: "dashboard.boq.col_completion_pct"
      via: "useTranslations('dashboard.boq')"
      pattern: "col_completion_pct"
---

<objective>
Add live BOQ progress to the existing BOQ tab: a "% Tamamlanan" column and a per-row Progress bar, computed from approved vs planned quantity — inline in the existing BoqTable, not a separate card (D-50).

Purpose: DASH-04 — the office engineer sees contracted vs approved completion per line item, refreshing on load/focus (force-dynamic + RefreshOnFocus land in Plan 06; the math/UI is here).

Output: Extended src/components/dashboard/BoqTable.tsx (two new columns, no rebuild). The progress-% math is already test-covered by the Plan 01 tests/boq.test.ts extension.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-dashboard-map/05-UI-SPEC.md
@.planning/phases/05-dashboard-map/05-PATTERNS.md
@.planning/phases/05-dashboard-map/05-CONTEXT.md

<interfaces>
<!-- BoqTable.tsx is extended in place. Exact current contract: -->

BoqItem (existing interface in BoqTable.tsx — already includes approvedQty):
```
export interface BoqItem { id; material; unit; plannedQty: string; approvedQty: string; sortOrder: number; }
```
plannedQty/approvedQty are numeric strings (Drizzle). The table already does `parseFloat(item.plannedQty)` / `parseFloat(item.approvedQty)`.

Existing helpers/imports in BoqTable.tsx:
- `import { remainingBalance } from '@/lib/boq-balance';`
- `const trFmt = new Intl.NumberFormat('tr-TR', { maximumFractionDigits: 3 });` + `formatQty()`
- `const t = useTranslations('dashboard.boq'); const tc = useTranslations('common');`
- `balanceColorClass(balance, planned)` returns warning/success/destructive text classes

Existing TableHead order (insert AFTER col_approved_qty, BEFORE col_remaining):
```
<TableHead ... >{t('col_contracted_qty')}</TableHead>
<TableHead ... >{t('col_approved_qty')}</TableHead>      <-- insert new columns after this
<TableHead ... >{t('col_remaining')}</TableHead>
<TableHead ... >{t('col_actions')}</TableHead>
```
Each TableBody row must add matching TableCells in the SAME position.

New i18n key (added in Plan 01): dashboard.boq.col_completion_pct → "% Tamamlanan" / "% Complete".

shadcn Progress already installed (UI-SPEC Component Inventory): `import { Progress } from '@/components/ui/progress';` usage `<Progress value={pct} className="min-w-[80px] h-2" />` (value is 0–100).

UI-SPEC progress contract (LOCKED):
- % column: `(approved / planned) * 100` formatted `Intl.NumberFormat('tr-TR', { maximumFractionDigits: 1 })` + '%'
- Progress bar: shadcn <Progress value={0-100}> width min-w-[80px] h-2
- Completion % capped at 100; planned=0 → 0% (no divide-by-zero)
- progressColorClass: >=90% success hsl(142 76% 36%); >0 && <=10% warning hsl(38 92% 50%); else default
</interfaces>

<react_guidance>
BoqTable is already a 'use client' component — extend it, do not introduce a new component. Add a pure `progressColorClass`/completion computation inside the existing `.map` row render. No new state, no new effects.
</react_guidance>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add % Tamamlanan column + per-row Progress bar to BoqTable (DASH-04 / D-50)</name>
  <files>src/components/dashboard/BoqTable.tsx</files>
  <read_first>
    - src/components/dashboard/BoqTable.tsx (full — extend the header + body row map in place; reuse trFmt, formatQty, balanceColorClass)
    - src/lib/boq-balance.ts (remainingBalance — already imported)
    - .planning/phases/05-dashboard-map/05-UI-SPEC.md (BOQ tab progress columns spec: format, Progress props, color thresholds, zero-progress)
    - .planning/phases/05-dashboard-map/05-PATTERNS.md (BoqTable.tsx extension section — insertion points, progressColorClass)
    - tests/boq.test.ts (the progress-% edge-case tests from Plan 01 — match the formula exactly)
  </read_first>
  <action>
    Extend src/components/dashboard/BoqTable.tsx in place (do NOT rebuild). Import `{ Progress } from '@/components/ui/progress'`. Add a pure helper near balanceColorClass: `function progressColorClass(pct: number): string` returning `'text-[hsl(142_76%_36%)]'` when pct>=90, `'text-[hsl(38_92%_50%)]'` when pct>0 && pct<=10, else `''` (matches UI-SPEC + the existing balanceColorClass class idiom).
    In the TableHeader, insert two `<TableHead>` AFTER the `col_approved_qty` head and BEFORE the `col_remaining` head: one `scope="col" className="w-20 text-right"` with `{t('col_completion_pct')}`, and one `scope="col" className="min-w-[80px]"` with no header text (the progress-bar column).
    In the TableBody row map, compute per row: `const planned = parseFloat(item.plannedQty); const approved = parseFloat(item.approvedQty); const completionPct = planned > 0 ? Math.min((approved / planned) * 100, 100) : 0;` — this is the SAME capped formula the Plan 01 tests/boq.test.ts asserts (planned 100/approved 150 → 100; planned 0 → 0). Add two `<TableCell>` in the matching position: (1) the % cell `className={\`text-right tabular-nums \${progressColorClass(completionPct)}\`}` rendering `new Intl.NumberFormat('tr-TR', { maximumFractionDigits: 1 }).format(completionPct) + '%'`, and (2) the bar cell rendering `<Progress value={completionPct} className="min-w-[80px] h-2" />`. A 0% row renders `<Progress value={0} />` (empty bar — no special-case needed, D-57). Preserve every existing column, the dropdown actions, and both dialogs unchanged.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/boq.test.ts && npx eslint src/components/dashboard/BoqTable.tsx && grep -q "@/components/ui/progress" src/components/dashboard/BoqTable.tsx && grep -q "col_completion_pct" src/components/dashboard/BoqTable.tsx</automated>
  </verify>
  <acceptance_criteria>
    - BoqTable imports shadcn Progress and renders `<Progress value={completionPct} className="min-w-[80px] h-2" />` per row
    - The % column uses `Math.min((approved / planned) * 100, 100)` with the planned>0 guard (matches Plan 01 boq.test.ts) and formats with Intl.NumberFormat('tr-TR',{maximumFractionDigits:1}) + '%'
    - The two new columns are inserted between col_approved_qty and col_remaining in BOTH the header and every body row (column counts match)
    - The header label comes from t('col_completion_pct') — no hardcoded "% Tamamlanan"
    - Existing columns, dropdown actions, edit + delete dialogs are unchanged
    - `npx vitest run tests/boq.test.ts` green; `npx tsc --noEmit` exits 0; eslint passes
  </acceptance_criteria>
  <done>BoqTable shows a capped completion % and a Progress bar per line item, header is i18n-driven, the progress-% tests pass, and all prior behavior is intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BOQ items prop → client render | approvedQty/plannedQty already auth+tenant-scoped server-side (getBoqItems) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-AC | Information Disclosure | BOQ progress data | accept | getBoqItems is auth-guarded + tenant-scoped (established Phase 1 pattern); this plan adds no new data path, only a derived % display |
| T-05-DIV | Denial of Service (NaN/Infinity render) | completion % math | mitigate | planned>0 guard prevents divide-by-zero; Math.min caps at 100 — no NaN/Infinity reaches the Progress value (Task 1, test-covered) |
</threat_model>

<verification>
- `npx vitest run tests/boq.test.ts` green (progress-% edge cases)
- `npx tsc --noEmit` exits 0; eslint passes
- `grep col_completion_pct src/components/dashboard/BoqTable.tsx` matches
</verification>

<success_criteria>
- DASH-04: per-line-item contracted-vs-approved completion % + Progress bar, capped at 100, divide-by-zero safe
- Inline in the existing BoqTable (D-50), no separate card, no rollup
- i18n header (D-63); no regression to existing BOQ CRUD
</success_criteria>

<output>
Create `.planning/phases/05-dashboard-map/05-05-SUMMARY.md` when done. Note that liveness (force-dynamic + RefreshOnFocus) is wired in Plan 06 — the % reflects whatever approvedQty the server fetch returns.
</output>
