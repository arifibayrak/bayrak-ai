---
phase: 04-spatial-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema/submissions.ts
  - tests/spatial.test.ts
  - tests/fixtures/db.ts
autonomous: true
requirements: [GEO-01, GEO-02]

must_haves:
  truths:
    - "The submissions schema declares snapped_point, segment_fraction, location_match, location_warning, location_distance_m"
    - "A second GiST index on snapped_point is declared for Phase 5 map queries"
    - "tests/spatial.test.ts exists with the failing Wave 0 test scaffold (D-48 coordinate-order + GEO-01/GEO-02 stubs)"
    - "A spatial seed helper exists so DB-gated tests can insert a tenant + project + route + submission fixture"
  artifacts:
    - path: "src/db/schema/submissions.ts"
      provides: "Five new Phase 4 columns + snapped_point GiST index"
      contains: "snapped_point"
    - path: "tests/spatial.test.ts"
      provides: "Wave 0 test scaffold for GEO-01, GEO-02, D-48"
      contains: "describeIfDb"
    - path: "tests/fixtures/db.ts"
      provides: "seedSpatialFixture helper (tenant + project + route + submission)"
      contains: "seedSpatialFixture"
  key_links:
    - from: "tests/spatial.test.ts"
      to: "tests/fixtures/db.ts"
      via: "import { describeIfDb, getTestDb, truncateAllTables, seedSpatialFixture }"
      pattern: "from './fixtures/db'"
---

<objective>
Extend the `submissions` Drizzle schema with the five Phase 4 spatial columns and a GiST index, and create the Wave 0 test scaffold (`tests/spatial.test.ts`) plus the spatial seed fixture that every later Phase 4 test depends on.

Purpose: This is the interface-and-contract plan. It defines the column names and types that the migration (Plan 02), the spatial helper (Plan 03), and the auditor caption (Plan 04) all build against, and it creates the failing test file the Nyquist validation contract requires before any spatial logic is written.

Output: Updated `src/db/schema/submissions.ts`, new `tests/spatial.test.ts` (failing stubs), and a `seedSpatialFixture` helper in `tests/fixtures/db.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-spatial-layer/04-CONTEXT.md
@.planning/phases/04-spatial-layer/04-RESEARCH.md
@.planning/phases/04-spatial-layer/04-PATTERNS.md
@.planning/phases/04-spatial-layer/04-VALIDATION.md

<interfaces>
<!-- Existing submissions table (the file being extended). Add the five new columns
     after the locationLon column (line 24), before quantity (line 25), and the new
     index inside the table options array after submissions_location_gist (line 44). -->

From src/db/schema/submissions.ts (current import line 5 — `boolean` is NOT yet imported):
```
import { pgTable, uuid, text, numeric, timestamp, index, unique, geometry } from 'drizzle-orm/pg-core';
```

Existing analog column conventions in the same file:
- Existing point geometry (line 22): `location: geometry('location', { type: 'point', mode: 'xy', srid: 4326 })`
- Existing text-enum column (lines 28-31): `status: text('status', { enum: ['pending_audit','approved','rejected'] }).notNull().default('pending_audit')`
- Existing GiST index (line 44): `index('submissions_location_gist').using('gist', t.location)`

Sibling boolean convention — src/db/schema/audit-notifications.ts: `sendFailed: boolean('send_failed').notNull().default(false)`

Test harness contract — tests/fixtures/db.ts already exports:
```
export const describeIfDb;                 // describe or describe.skip based on TEST_DATABASE_URL
export async function getTestDb();          // drizzle-orm/neon-http client
export async function truncateAllTables(db); // truncates submissions, routes, projects, tenants, etc.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the five Phase 4 spatial columns + GiST index to the submissions schema</name>
  <files>src/db/schema/submissions.ts</files>
  <read_first>
    - src/db/schema/submissions.ts (the file being modified — read fully)
    - src/db/schema/audit-notifications.ts (boolean column convention)
    - .planning/phases/04-spatial-layer/04-PATTERNS.md (section "src/db/schema/submissions.ts (MODIFY — add 5 columns + 1 GiST index)")
    - .planning/phases/04-spatial-layer/04-RESEARCH.md (section "Drizzle Schema Additions")
  </read_first>
  <action>
    In src/db/schema/submissions.ts, add `boolean` to the existing import from 'drizzle-orm/pg-core' (it is not yet imported).
    Insert five new columns immediately after the `locationLon` column (line 24) and before `quantity` (line 25), matching the existing column style:
    - `snappedPoint: geometry('snapped_point', { type: 'point', mode: 'xy', srid: 4326 })` — the closest point on routes.geom; null when location_match='no_route'.
    - `segmentFraction: numeric('segment_fraction', { precision: 10, scale: 8 })` — ST_LineLocatePoint result in [0.0,1.0]; null when no_route.
    - `locationMatch: text('location_match', { enum: ['near', 'far', 'no_route'] })` — three-state source of truth per D-43/D-44.
    - `locationWarning: boolean('location_warning').default(false)` — true ONLY when location_match='far' per D-44; kept for SC2 compatibility and cheap filtering.
    - `locationDistanceM: numeric('location_distance_m', { precision: 12, scale: 2 })` — metre distance to route; stored so fanOutToAuditors formats the D-47 caption without re-querying PostGIS; null when no_route.
    Append a second GiST index inside the table options array after the existing `submissions_location_gist` entry (line 44): `index('submissions_snapped_point_gist').using('gist', t.snappedPoint)`. This front-loads the index Phase 5 map queries need (D-46 query is `WHERE status='approved' AND snapped_point IS NOT NULL`).
    Do NOT add `.notNull()` to any of the five columns — pre-Phase-4 rows have no spatial data and must remain valid (backfill is explicitly out of scope per CONTEXT Deferred Ideas). No insert in this phase requires these columns to be non-null.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "submissions" | grep -v "^#" | grep -c error; test "$(grep -c "snapped_point\|segment_fraction\|location_match\|location_warning\|location_distance_m" src/db/schema/submissions.ts)" -ge 5</automated>
  </verify>
  <acceptance_criteria>
    - src/db/schema/submissions.ts contains `snapped_point`, `segment_fraction`, `location_match`, `location_warning`, and `location_distance_m`
    - src/db/schema/submissions.ts contains `submissions_snapped_point_gist`
    - `boolean` is present in the import from 'drizzle-orm/pg-core'
    - `npx tsc --noEmit` reports no new errors in submissions.ts
    - None of the five new columns declares `.notNull()`
  </acceptance_criteria>
  <done>The submissions table declares all five Phase 4 columns with the exact names/types from RESEARCH §Drizzle Schema Additions, plus the snapped_point GiST index, and the project type-checks.</done>
</task>

<task type="auto">
  <name>Task 2: Add seedSpatialFixture helper to tests/fixtures/db.ts</name>
  <files>tests/fixtures/db.ts</files>
  <read_first>
    - tests/fixtures/db.ts (the file being modified — read fully)
    - tests/postgis.test.ts (lines 22-84 — the tenant/project/route seed pattern via sql.raw + ST_GeomFromGeoJSON)
    - .planning/phases/04-spatial-layer/04-RESEARCH.md (section "Test Data Requirements (for DB-gated tests)")
  </read_first>
  <action>
    Add an exported async helper `seedSpatialFixture(db)` to tests/fixtures/db.ts that inserts the minimal fixture rows needed by the snap tests, all with deterministic UUIDs and `ON CONFLICT DO NOTHING`:
    - tenant id `00000000-0000-0000-0000-000000000001`
    - project id `00000000-0000-0000-0000-000000000002` (tenant_id = above)
    - one BOQ item and one person referencing that project/tenant (needed because submissions has notNull FKs to boq_item_id and person_id) — use deterministic UUIDs.
    - a `routes` row for the project with a known Istanbul LineString inserted via `ST_GeomFromGeoJSON(...)` with coordinates `[[28.9, 41.0], [28.95, 41.05]]` and `coordinate_count = 2`. Use the parameterized `sql\`...\`` form (NOT string interpolation) for the GeoJSON string, mirroring tests/postgis.test.ts line 73.
    Return the deterministic UUIDs (tenantId, projectId, boqItemId, personId) as an object so test cases can build submission rows and assert against them. Do NOT seed a submissions row here — individual tests insert their own submission with the flow_id under test, so near/far/no_route cases can vary the location. Use lazy `import { sql } from 'drizzle-orm'` consistent with the existing truncateAllTables helper.
    Do NOT modify the `truncateAllTables` table list — no new tables are added in Phase 4 (the five columns live on the already-listed `submissions` table).
  </action>
  <verify>
    <automated>grep -c "export async function seedSpatialFixture" tests/fixtures/db.ts; npx tsc --noEmit 2>&1 | grep -i "fixtures/db" | grep -v "^#" | grep -c error</automated>
  </verify>
  <acceptance_criteria>
    - tests/fixtures/db.ts exports `seedSpatialFixture`
    - The route seed uses `ST_GeomFromGeoJSON` with the Istanbul LineString `[[28.9, 41.0], [28.95, 41.05]]`
    - The route seed uses a parameterized `sql\`...\`` template, not string concatenation of the GeoJSON
    - `npx tsc --noEmit` reports no new errors in tests/fixtures/db.ts
  </acceptance_criteria>
  <done>`seedSpatialFixture` inserts tenant + project + BOQ item + person + Istanbul route and returns the deterministic IDs; truncateAllTables is unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Create tests/spatial.test.ts Wave 0 scaffold (D-48 + GEO-01/GEO-02 failing stubs)</name>
  <files>tests/spatial.test.ts</files>
  <read_first>
    - tests/postgis.test.ts (the structural analog — describeIfDb, getTestDb, truncateAllTables, Istanbul fixture, sql vs sql.raw distinction)
    - tests/fixtures/db.ts (after Task 2 — the seedSpatialFixture helper)
    - .planning/phases/04-spatial-layer/04-RESEARCH.md (section "Validation Architecture" → "Phase Requirements → Test Map" and "D-48 Mandatory Coordinate-Order Unit Test")
    - .planning/phases/04-spatial-layer/04-PATTERNS.md (section "tests/spatial.test.ts (CREATE)")
  </read_first>
  <action>
    Create tests/spatial.test.ts copying the harness structure of tests/postgis.test.ts: import `{ beforeEach, afterEach, it, expect }` from 'vitest', `{ sql }` from 'drizzle-orm', and `{ describeIfDb, getTestDb, truncateAllTables, seedSpatialFixture }` from './fixtures/db'. Wrap all tests in `describeIfDb('Phase 4 spatial snap (GEO-01, GEO-02)', () => { ... })` with beforeEach (getTestDb + truncateAllTables + seedSpatialFixture) and afterEach (truncateAllTables).
    Implement the ONE test that can pass with no spatial logic yet — the D-48 coordinate-order guard (it does not depend on the snap helper, only on PostGIS):
    - `it('(D-48) Istanbul point (lng 28.9, lat 41.0) reads back longitude-first in GeoJSON', ...)` — `SELECT ST_AsGeoJSON(ST_SetSRID(ST_MakePoint(28.9, 41.0), 4326))::json AS geojson`; assert `coordinates[0]` toBeCloseTo 28.9 (longitude first) and `coordinates[1]` toBeCloseTo 41.0 (latitude second). Note: ST_MakePoint takes longitude FIRST.
    Add `it.todo(...)` (or `it.skip` with a clear TODO comment) placeholders — NOT passing assertions — for the snap behaviors that Plan 03 and Plan 04 will implement, so the Nyquist test map has named slots:
    - `(GEO-01) submission within 500m stores non-null snapped_point and segment_fraction`
    - `(GEO-01) segment_fraction is within [0.0, 1.0]`
    - `(GEO-02) far submission → location_match='far' and location_warning=true`
    - `(GEO-02) near submission → location_match='near' and location_warning=false`
    - `(GEO-02) no-route project → location_match='no_route', null snapped_point/segment_fraction, submission still persists`
    - `(D-47) caption includes distance line when far; neutral line when no_route; silent when near`
    Use `it.todo` for the not-yet-implemented cases so the suite is green now (the scaffold must not block Wave 1→2 by failing). Plan 03 and Plan 04 convert the relevant `it.todo` entries into real assertions.
  </action>
  <verify>
    <automated>npx vitest run tests/spatial.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/spatial.test.ts exists and uses `describeIfDb` (not bare `describe`)
    - The D-48 coordinate-order test asserts `coordinates[0]` ≈ 28.9 and `coordinates[1]` ≈ 41.0
    - The file contains `it.todo` named slots for all GEO-01, GEO-02, and D-47 behaviors listed in the action
    - `npx vitest run tests/spatial.test.ts` exits 0 (D-48 passes when a test DB is configured; todo slots are pending, not failing)
  </acceptance_criteria>
  <done>tests/spatial.test.ts exists with a passing D-48 coordinate-order test and named `it.todo` slots for every GEO-01/GEO-02/D-47 behavior; the suite runs green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram worker → bot handler | Worker-supplied lat/lon enters the system; consumed later in Plan 03's PostGIS SQL. This plan only declares the columns that store the results. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | location_match column (three-state integrity) | mitigate | Declare `location_match` as a `text` enum `{'near','far','no_route'}` so a CHECK constraint in the migration (Plan 02) prevents invalid states being stored. Schema-level enum is the structural guard against silently dropping the location_warning (security_requirement). |
| T-04-02 | Information Disclosure | snapped_point geometry | accept | Snapped coordinates are project-internal field data, no PII; readable only by authenticated office users in Phase 5. Low-value target. |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase — RESEARCH §Package Legitimacy Audit confirms zero new packages. No checkpoint needed. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes (schema + fixture type-check).
- `npx vitest run tests/spatial.test.ts` exits 0 (D-48 passes with a test DB; todo slots pending).
- `grep -c "snapped_point\|segment_fraction\|location_match\|location_warning\|location_distance_m" src/db/schema/submissions.ts` ≥ 5.
</verification>

<success_criteria>
- Five Phase 4 columns + snapped_point GiST index declared in the schema (no `.notNull()`).
- `seedSpatialFixture` helper available to all Phase 4 tests.
- `tests/spatial.test.ts` exists with a passing D-48 test and named todo slots; suite green.
</success_criteria>

<output>
Create `.planning/phases/04-spatial-layer/04-01-SUMMARY.md` when done.
</output>
