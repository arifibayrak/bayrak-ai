---
phase: 04-spatial-layer
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - src/lib/bot-audit.ts
  - tests/spatial.test.ts
autonomous: false
requirements: [GEO-02]

must_haves:
  truths:
    - "The auditor notification caption includes a distance line when location_match='far'"
    - "The auditor notification caption includes a neutral note when location_match='no_route'"
    - "The auditor notification caption shows no location line when location_match='near' or null"
    - "The existing Google Maps link is kept in the caption in all cases"
    - "The shown distance is human-formatted (km with one decimal ≥1000m, else metres)"
  artifacts:
    - path: "src/lib/bot-audit.ts"
      provides: "fanOutToAuditors caption includes the D-47 location anomaly line"
      contains: "Konum rotadan uzak"
  key_links:
    - from: "src/lib/bot-audit.ts"
      to: "src/lib/spatial.ts"
      via: "await import('@/lib/spatial') for formatDistance"
      pattern: "formatDistance"
    - from: "src/lib/bot-audit.ts"
      to: "submissions.location_match / location_distance_m"
      via: "submission.locationMatch / submission.locationDistanceM on the loaded row"
      pattern: "locationMatch"
---

<objective>
Extend `fanOutToAuditors` to add the D-47 location-anomaly caption line based on the `location_match` and `location_distance_m` columns populated by Plan 03, and add the caption unit tests to `tests/spatial.test.ts`.

Purpose: This delivers the user-facing half of GEO-02 (ROADMAP SC2): "the auditor's Telegram notification includes a distance-anomaly flag." It mirrors the D-28 over-delivery pattern (show the number, not just a flag) and respects the D-26 Turkish tone (neutral, not alarmist, for the no-route case).

Output: Modified `fanOutToAuditors` in `src/lib/bot-audit.ts` and the D-47 caption unit tests in `tests/spatial.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-spatial-layer/04-CONTEXT.md
@.planning/phases/04-spatial-layer/04-RESEARCH.md
@.planning/phases/04-spatial-layer/04-PATTERNS.md

<interfaces>
<!-- The loaded submission row in fanOutToAuditors exposes the new columns camelCased:
     submission.locationMatch ('near'|'far'|'no_route'|null), submission.locationDistanceM (string|null). -->

fanOutToAuditors caption assembly (src/lib/bot-audit.ts):
- Line ~99: the full submission row is loaded via `db.select().from(submissions).where(...)`.
- Line ~155: `const captionLines: string[] = [ ... ];`
- Lines ~164-165: existing Google Maps link line:
  `captionLines.push(\`📍 https://maps.google.com/?q=${submission.locationLat},${submission.locationLon}\`)`
- Lines ~169-172: existing D-28 over-delivery block (the analog to copy):
  `if (newTotal > plannedQty) { captionLines.push(MESSAGES.auditOverDelivery(newTotal, plannedQty, boqItem.unit)); }`
- Line ~174: `const captionText = captionLines.join('\n');`

Lazy import discipline: fanOutToAuditors uses only `await import(...)` inside the function body (pattern at lines ~81-91). `formatDistance` must be lazy-imported the same way.

From src/lib/spatial.ts (Plan 03): `export function formatDistance(distanceM: number): string` — ≥1000m → "~X.X km", else "~N m".

D-47 microcopy (final, from CONTEXT §Specific Ideas):
- far  → `⚠ Konum rotadan uzak (${formatDistance(distanceM)})`  e.g. "⚠ Konum rotadan uzak (~1.2 km)"
- no_route → `ℹ Rota yüklenmemiş — konum doğrulanamadı`  (neutral, NOT an alarm)
- near or null → no line added (silent)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the D-47 location-anomaly caption line to fanOutToAuditors</name>
  <files>src/lib/bot-audit.ts</files>
  <read_first>
    - src/lib/bot-audit.ts (lines ~80-175 — fanOutToAuditors: the submission load, captionLines array, Google Maps link, and the D-28 over-delivery block analog)
    - src/lib/spatial.ts (the formatDistance export from Plan 03)
    - .planning/phases/04-spatial-layer/04-PATTERNS.md (section "src/lib/bot-audit.ts (MODIFY fanOutToAuditors — add D-47 caption line)")
    - .planning/phases/04-spatial-layer/04-RESEARCH.md (section "Pattern 4: Auditor Caption Extension (D-47)" + "Code Examples → Auditor Caption Distance Formatting")
  </read_first>
  <action>
    In fanOutToAuditors, immediately AFTER the existing D-28 over-delivery block (around line 172) and using the same `if (...) { captionLines.push(...) }` style, add the D-47 location-anomaly line. Read the new columns off the already-loaded submission row:
      `const locationMatch = submission.locationMatch as 'near' | 'far' | 'no_route' | null;`
      `const distanceM = submission.locationDistanceM != null ? parseFloat(String(submission.locationDistanceM)) : null;`
    Then:
      • If `locationMatch === 'far' && distanceM !== null`: lazy-import formatDistance (`const { formatDistance } = await import('@/lib/spatial');`) and `captionLines.push(\`⚠ Konum rotadan uzak (${formatDistance(distanceM)})\`)`.
      • Else if `locationMatch === 'no_route'`: `captionLines.push('ℹ Rota yüklenmemiş — konum doğrulanamadı')` — neutral note, NOT an alarm (D-43/D-47).
      • Else (`'near'` or `null` for pre-Phase-4 rows): add NOTHING (silent).
    Do NOT remove or move the existing Google Maps link line — it stays in all cases (D-47). Do NOT add a top-level import of @/lib/spatial — formatDistance is lazy-imported per the file's discipline. Place the block before `const captionText = captionLines.join('\n')` so the new line is included in the sent caption.
    Final microcopy and m-vs-km rounding are already fixed in formatDistance (Plan 03); use the exact Turkish strings above (CONTEXT D-47).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "bot-audit.ts" | grep -v "^#" | grep -c error; grep -q "Konum rotadan uzak" src/lib/bot-audit.ts && grep -q "Rota yüklenmemiş" src/lib/bot-audit.ts && grep -q "import('@/lib/spatial')" src/lib/bot-audit.ts && echo CAPTION_OK</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/bot-audit.ts contains the far-case string `⚠ Konum rotadan uzak (` with `formatDistance` interpolated
    - src/lib/bot-audit.ts contains the no_route string `ℹ Rota yüklenmemiş — konum doğrulanamadı`
    - The 'near'/null case adds no caption line
    - The existing Google Maps link line is unchanged and still present
    - formatDistance is imported via `await import('@/lib/spatial')` (no top-level import)
    - `npx tsc --noEmit` reports no new errors in bot-audit.ts
  </acceptance_criteria>
  <done>fanOutToAuditors appends the distance-anomaly line for 'far', the neutral note for 'no_route', stays silent for 'near'/null, and keeps the Google Maps link in all cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add D-47 caption unit tests to tests/spatial.test.ts</name>
  <files>tests/spatial.test.ts</files>
  <read_first>
    - tests/spatial.test.ts (the D-47 it.todo slot from Plan 04-01)
    - src/lib/spatial.ts (formatDistance — the pure helper under unit test)
    - src/lib/bot-audit.ts (the caption logic added in Task 1)
    - .planning/phases/04-spatial-layer/04-RESEARCH.md (section "Validation Architecture → Phase Requirements → Test Map" D-47 rows)
  </read_first>
  <behavior>
    - formatDistance(1234) === "~1.2 km"; formatDistance(420) === "~420 m"; formatDistance(1000) === "~1.0 km".
    - Caption decision (pure, no DB): for a row with locationMatch='far' and locationDistanceM='1234.00' the assembled location line is "⚠ Konum rotadan uzak (~1.2 km)".
    - For locationMatch='no_route' the location line is "ℹ Rota yüklenmemiş — konum doğrulanamadı".
    - For locationMatch='near' or null no location line is produced.
  </behavior>
  <action>
    Convert the D-47 `it.todo` slot from Plan 04-01 into real tests. These are PURE unit tests (no DB) — they must NOT require describeIfDb, so place them in a plain `describe('D-47 auditor caption (GEO-02)', () => { ... })` block within the same file (a sibling of the describeIfDb block), so they run even without TEST_DATABASE_URL.
    - Test formatDistance directly (imported from '@/lib/spatial'): assert "~1.2 km" for 1234, "~420 m" for 420, and a ≥1000 boundary case.
    - Test the caption-line decision. Extract the pure decision into a small testable form: either (a) export a tiny pure helper `buildLocationCaptionLine(locationMatch, distanceM): string | null` from src/lib/spatial.ts and unit-test it directly (preferred — keeps bot-audit.ts free of new exports and avoids mocking grammY), or (b) if you keep the logic inline in bot-audit.ts, assert via a focused string-builder test. Prefer (a): add `buildLocationCaptionLine` to spatial.ts in this task and have bot-audit.ts call it (refactor Task 1's inline block to call the helper). Assert: 'far' + 1234 → "⚠ Konum rotadan uzak (~1.2 km)"; 'no_route' → "ℹ Rota yüklenmemiş — konum doğrulanamadı"; 'near' → null; null → null.
    Keep all existing tests (D-48, GEO-01, GEO-02) passing.
  </action>
  <verify>
    <automated>npx vitest run tests/spatial.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `formatDistance(1234)` returns "~1.2 km" and `formatDistance(420)` returns "~420 m"
    - The caption decision returns "⚠ Konum rotadan uzak (~1.2 km)" for far+1234, "ℹ Rota yüklenmemiş — konum doğrulanamadı" for no_route, and null/empty for near and null
    - The D-47 tests run WITHOUT a test DB (plain describe, not describeIfDb)
    - `npx vitest run tests/spatial.test.ts` exits 0 with all prior cases still green
  </acceptance_criteria>
  <done>tests/spatial.test.ts proves formatDistance formatting and the three-state caption decision as pure unit tests that run without a DB, alongside the still-green GEO-01/GEO-02/D-48 cases.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the live auditor anomaly notification (GEO-02 SC2)</name>
  <action>After the automated tests are green, verify the D-47 caption line renders correctly in a real Telegram auditor notification (live delivery is the one manual-only behavior per VALIDATION). See how-to-verify for steps.</action>
  <what-built>
    The full GEO-02 path is wired: handleConfirmSubmit snaps the location inside its transaction (Plan 03), and fanOutToAuditors now appends the D-47 anomaly line — "⚠ Konum rotadan uzak (~X km)" for beyond-threshold submissions and "ℹ Rota yüklenmemiş — konum doğrulanamadı" for route-less projects — while keeping the Google Maps link. All automated tests (D-48, GEO-01, GEO-02, D-47 caption) pass.
  </what-built>
  <how-to-verify>
    Live Telegram delivery is the one behavior outside unit-test scope (VALIDATION §Manual-Only Verifications). Verify end-to-end:
    1. Ensure PROXIMITY_THRESHOLD_M is set (or defaulting to 500) in the bot's environment.
    2. From the worker bot, complete a submission whose shared location is clearly MORE than 500m from the project's uploaded route.
    3. Confirm the auditor receives a Telegram notification whose caption includes a line like "⚠ Konum rotadan uzak (~X km)" with a sensible distance, AND still shows the 📍 Google Maps link, BOQ item, quantity, and notes.
    4. (Optional) Submit on a project that has NO route uploaded and confirm the auditor caption shows "ℹ Rota yüklenmemiş — konum doğrulanamadı" (neutral, not an alarm) and the submission still arrives as pending_audit.
    5. (Optional) Submit a within-500m location and confirm NO location-anomaly line appears (silent).
  </how-to-verify>
  <resume-signal>Type "approved" if the auditor notification shows the correct anomaly/neutral line and keeps the map link, or describe what was wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| submissions row → auditor Telegram caption | Stored location_match/distance becomes auditor-facing text. The auditor is the human guard for likely-wrong locations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Repudiation (silent warning drop) | D-47 caption omitting a real 'far' anomaly | mitigate | The caption is driven directly off the committed `location_match` column (the D-44 source of truth). 'far' always emits the warning line; 'no_route' always emits the neutral note; only 'near'/null are silent. The D-47 unit tests assert each branch, so a beyond-threshold submission can never reach an auditor without its anomaly line (security_requirement: best-effort snap cannot silently drop the warning). |
| T-04-08 | Spoofing (GPS fraud) | worker-reported location | accept | Phase 4 cannot detect GPS spoofing — it is a physical-security concern. location_warning surfaces likely-wrong locations to the auditor (the human guard); Phase 6 AI vision is the technical follow-up. Out of scope for this phase per RESEARCH §Security Domain. |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (RESEARCH §Package Legitimacy Audit). No checkpoint needed. |
</threat_model>

<verification>
- `npx vitest run tests/spatial.test.ts` green (D-48, GEO-01, GEO-02, D-47 caption + formatDistance).
- `npx vitest run` full suite green (no regression in telegram-audit tests from the fanOutToAuditors edit).
- `npx tsc --noEmit` clean.
- Human checkpoint confirms the live auditor Telegram caption renders the anomaly/neutral line with the map link kept.
</verification>

<success_criteria>
- GEO-02 SC2 fully delivered: beyond-threshold submission → location_warning=true (Plan 03) AND auditor notification carries the distance-anomaly flag (this plan).
- no_route renders a neutral note, not an alarm (D-43/D-47).
- Google Maps link preserved in all cases; near/null submissions are silent.
</success_criteria>

<output>
Create `.planning/phases/04-spatial-layer/04-04-SUMMARY.md` when done.
</output>
