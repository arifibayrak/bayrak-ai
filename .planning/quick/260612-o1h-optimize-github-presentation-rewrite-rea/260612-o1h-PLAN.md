---
phase: quick-260612-o1h
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - AGENTS.md
autonomous: false
requirements: [QUICK-260612-o1h]
must_haves:
  truths:
    - "README.md presents bayrak.ai professionally — no create-next-app template remains"
    - "AGENTS.md exists at repo root as a thin pointer-based agent context file"
    - "GitHub repo has a description, homepage URL, and relevant topics"
  artifacts:
    - path: README.md
      provides: "Professional GitHub-facing project README, AI-agent-friendly structure"
      contains: "bayrak.ai"
    - path: AGENTS.md
      provides: "Cross-tool agent context pointers (Codex/Cursor/Copilot)"
      contains: "bayrak.ai"
  key_links:
    - from: README.md
      to: CLAUDE.md, BRAND.md, AGENTS.md, .planning/ROADMAP.md
      via: "documentation-for-AI-agents pointers"
      pattern: "CLAUDE\\.md|BRAND\\.md|AGENTS\\.md"
---

<objective>
Optimize the GitHub presentation of bayrak.ai so the repo reads as a serious commercial product to both human visitors and AI coding agents.

Purpose: The repo currently ships the default create-next-app README and has no GitHub description, homepage, or topics. This makes a polished, real product look like an empty scaffold.

Output:
- Rewritten `README.md` (full replacement of the create-next-app template)
- New `AGENTS.md` at repo root (cross-tool agent context, pointer-based)
- GitHub repo metadata set via `gh` CLI (description, homepage, topics)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@BRAND.md
@.env.example
@package.json

# Verified repo facts (do not re-verify):
# - GitHub repo: https://github.com/arifibayrak/bayrak-ai — PUBLIC, gh CLI authenticated.
# - Production site: https://www.bayrak.ai (Telegram webhook at /api/telegram/webhook).
# - Deploys via `vercel --prod --yes` CLI, NOT git push.
# - Tagline (BRAND.md OG): "Saha sahipleniyor / Field accountability for utility-network contractors".
# - Tests: vitest, 416 passing. Run with `npx vitest run` (no test script in package.json).
#
# Verified repository layout (use these REAL paths in the repo map — do not invent):
#   src/app/        Next.js App Router (api/, auth/, dashboard/, layout.tsx, page.tsx, opengraph-image.tsx, icon/apple-icon)
#   src/actions/    Server actions (analytics.ts, boq.ts, hakedis.ts, routes.ts, chainage.ts, projects.ts, people.ts, ai-flags.ts, settings.ts, dxf-preview.ts)
#   src/components/ React components (incl. src/components/brand — the 7 brand primitives)
#   src/db/         Drizzle schema + migrate.ts
#   src/lib/        Shared utilities
#   src/i18n/       next-intl config (request/routing)
#   src/hooks/      React hooks
#   src/types/      Shared TypeScript types
#   messages/       i18n message catalogs (en.json, tr.json) — Turkish-first
#   tests/          vitest tests
#   scripts/        one-off scripts (seed, CDP console-capture, etc.)
#   .planning/      GSD planning artifacts (ROADMAP.md, STATE.md, REQUIREMENTS.md, phases/)
#
# package.json scripts: dev, build, start, lint, migrate (tsx src/db/migrate.ts), migrate:test.
# NOTE: there is NO "test" script — agents must use `npx vitest run`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md (full replacement)</name>
  <files>README.md</files>
  <action>
  Fully replace the create-next-app template in README.md with a professional, AI-agent-friendly README. Plain GitHub-flavored markdown only — NO badges that require CI/external services (no CI exists). Use real repo paths from the context block; do not invent files.

  Required sections, in order:

  1. Title + one-line value prop. Title `# bayrak.ai`. One-line: field-to-office operations platform for pipeline & utility-network subcontractors that replaces WhatsApp/phone field comms with a structured, geolocated, AI-assisted loop. Include the bilingual tagline "Saha sahipleniyor — Field accountability for utility-network contractors".

  2. `## What is bayrak.ai` — short prose, then the core loop as a text diagram (no images), e.g. `Worker submits (Telegram) → Auditor approves/rejects on-site (Telegram) → BOQ deducts + live map + hakkediş update automatically`.

  3. `## Features` — grouped subsections (use `###` or bold-label bullet groups): Field loop (Telegram worker bot, conversational work logging, photo + GPS location), Office dashboard (live map, project overview, performance analytics/scorecards, admin shell), Geospatial (PostGIS nearest-segment matching, DXF route import, LandXML alignment import with parabolic vertical curves + clothoid spirals, chainage as-built view, elevation profile), Billing & exports (auto-deducting BOQ, submission-driven hakkediş, bilingual Excel + PDF exports), AI assist (AI Vision photo anomaly flagging via AI SDK + Vercel AI Gateway — note it is eval-gated/dormant until the precision eval runs).

  4. `## Tech stack` — a markdown table (Layer | Technology) sourced from CLAUDE.md + package.json: Framework Next.js 15 (App Router) + TypeScript; Styling Tailwind CSS v4 + shadcn/ui; Database Neon Postgres 16 + PostGIS; ORM Drizzle; Telegram grammY (+ conversations); Mapping Mapbox GL JS via react-map-gl; Auth Auth.js v5 magic-link (Resend); AI AI SDK v6 via Vercel AI Gateway (Claude vision); i18n next-intl (TR/EN); Storage Vercel Blob; Exports ExcelJS + @react-pdf/renderer; Testing vitest.

  5. `## Repository map` — a list/table mapping the REAL directories from the context block (src/app, src/actions, src/components incl. brand primitives, src/db, src/lib, src/i18n, src/hooks, src/types, messages/, tests/, scripts/, .planning/) each with a one-line description.

  6. `## Getting started` — prereqs (Node, a Neon Postgres+PostGIS database); then numbered steps: copy `.env.example` to `.env.local` and fill values, `npm install`, `npm run migrate`, `npm run dev` (opens http://localhost:3000), and `npx vitest run` for tests. Explicitly state there is no `test` npm script — use `npx vitest run`.

  7. `## Documentation for AI agents` — pointers (with relative links) to `CLAUDE.md` (stack rules + integration patterns), `BRAND.md` (UI tokens + the 7 brand primitives in `src/components/brand`), `AGENTS.md` (cross-tool agent entry point), and `.planning/ROADMAP.md` + `.planning/STATE.md` (roadmap + current state).

  8. `## Localization` — Turkish-first worker bot; TR/EN switchable dashboard via next-intl; message catalogs in `messages/`.

  9. `## Deployment` — Vercel, deployed via `vercel --prod --yes` CLI (not git push). Production at https://www.bayrak.ai.

  10. `## Status` — closing line: commercial product, single-tenant MVP; all rights reserved (no open-source LICENSE).

  Do NOT add a LICENSE file. Do NOT touch any file other than README.md in this task.
  </action>
  <verify>
    <automated>test -f README.md && ! grep -qi "create-next-app" README.md && grep -q "bayrak.ai" README.md && grep -q "vitest run" README.md && grep -q "src/components/brand" README.md && grep -qi "PostGIS" README.md && grep -q "AGENTS.md" README.md</automated>
  </verify>
  <done>README.md contains all 10 sections, references real repo paths, no create-next-app template text remains, and points to CLAUDE.md/BRAND.md/AGENTS.md/.planning.</done>
</task>

<task type="auto">
  <name>Task 2: Create AGENTS.md (thin, pointer-based)</name>
  <files>AGENTS.md</files>
  <action>
  Create `AGENTS.md` at the repo root. It is the cross-tool agent standard file (Codex/Cursor/Copilot/etc.). Keep it THIN and pointer-based — do NOT duplicate the stack tables or integration patterns from CLAUDE.md; point to them instead.

  Required content:

  1. Project one-liner: bayrak.ai — single-tenant B2B field-to-office operations platform for pipeline & utility-network subcontractors (Telegram field logging → on-site audit → auto-updating BOQ + map + hakkediş).

  2. `## Commands` — dev (`npm run dev`), build (`npm run build`), lint (`npm run lint`), migrate (`npm run migrate`), tests (`npx vitest run` — note: NO `test` npm script exists).

  3. `## Where canonical context lives` — bullets: `CLAUDE.md` = tech-stack rules + integration patterns + constraints (authoritative); `BRAND.md` = UI design tokens + the 7 brand primitives in `src/components/brand` (use these, NOT raw shadcn, from feature surfaces); `.planning/` = ROADMAP.md, STATE.md, REQUIREMENTS.md, phases/ (roadmap + state + requirements).

  4. `## Conventions` — TypeScript everywhere; Drizzle schema in `src/db`; server actions in `src/actions`; i18n message catalogs in `messages/` (en.json, tr.json), Turkish-first strings; brand primitives over raw shadcn.

  5. `## Safety notes` — CRITICAL: local `.env.local` `DATABASE_URL` points at the PRODUCTION Neon database — be extremely careful running seeds/migrations locally, they hit prod. Never commit `.env.local`. Deploys go through `vercel --prod --yes` CLI, not git push.

  Plain markdown. Do NOT touch any other file.
  </action>
  <verify>
    <automated>test -f AGENTS.md && grep -qi "bayrak.ai" AGENTS.md && grep -q "vitest run" AGENTS.md && grep -q "CLAUDE.md" AGENTS.md && grep -q "BRAND.md" AGENTS.md && grep -qi "PRODUCTION" AGENTS.md && grep -q ".env.local" AGENTS.md</automated>
  </verify>
  <done>AGENTS.md exists at repo root with project one-liner, commands, canonical-context pointers, conventions, and the production-DB safety note. It does not duplicate CLAUDE.md stack tables.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  README.md and AGENTS.md are written. This task sets GitHub repo metadata (description, homepage, topics) via the authenticated `gh` CLI. A blocking checkpoint follows so you can eyeball the rendered README and the repo sidebar on github.com before considering the quick task done.

  Run these commands (gh is already authenticated):

  Set description + homepage:
  `gh repo edit arifibayrak/bayrak-ai --description "Field-to-office operations platform for pipeline & utility-network subcontractors: Telegram work logging, on-site audit approval, and auto-updating Bill of Quantities, live PostGIS map, and hakkediş billing." --homepage "https://www.bayrak.ai"`

  Add topics (single command, repeated --add-topic flags):
  `gh repo edit arifibayrak/bayrak-ai --add-topic nextjs --add-topic typescript --add-topic postgis --add-topic telegram-bot --add-topic grammy --add-topic drizzle-orm --add-topic mapbox --add-topic neon --add-topic construction-management --add-topic geospatial --add-topic ai-sdk --add-topic vercel`

  Then confirm with:
  `gh repo view arifibayrak/bayrak-ai --json description,homepageUrl,repositoryTopics`
  </what-built>
  <how-to-verify>
  1. Run the three `gh repo edit` / `gh repo view` commands above. The view command should show the description, homepageUrl https://www.bayrak.ai, and all 12 topics.
  2. Open https://github.com/arifibayrak/bayrak-ai in a browser. Confirm: the About sidebar shows the description, the homepage link, and the topic chips; the README renders the new content (no create-next-app text) with correct headings, the tech-stack table, and the repo map.
  </how-to-verify>
  <resume-signal>Type "approved" if the README renders well and the GitHub sidebar shows description + homepage + topics, or describe what looks off.</resume-signal>
</task>

</tasks>

<verification>
- README.md fully replaces the create-next-app template and contains all 10 required sections.
- AGENTS.md exists at repo root, thin and pointer-based.
- `gh repo view arifibayrak/bayrak-ai --json description,homepageUrl,repositoryTopics` shows non-empty description, homepage https://www.bayrak.ai, and 12 topics.
- No files other than README.md and AGENTS.md were modified. No LICENSE added. No CI badges.
</verification>

<success_criteria>
- A visitor to https://github.com/arifibayrak/bayrak-ai sees a professional product README, an About sidebar with description/homepage/topics, and an AGENTS.md entry point — no scaffold remnants.
- An AI agent landing in the repo can orient from README.md → AGENTS.md → CLAUDE.md/BRAND.md/.planning with real paths and accurate commands.
</success_criteria>

<output>
Create `.planning/quick/260612-o1h-optimize-github-presentation-rewrite-rea/260612-o1h-SUMMARY.md` when done.
</output>
