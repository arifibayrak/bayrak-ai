---
phase: quick-260525-2uq
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - messages/tr.json
  - messages/en.json
  - src/app/page.tsx
  - src/components/landing/LandingHeader.tsx
  - src/components/landing/Section.tsx
autonomous: true
requirements: [LANDING-01, LANDING-02, LANDING-03, LANDING-04, LANDING-05]
must_haves:
  truths:
    - "Visiting / renders a bilingual marketing landing page, not the create-next-app boilerplate"
    - "A TR/EN toggle is present and switches all landing copy via the existing cookie+reload mechanism"
    - "Primary CTA 'Giriş Yap'/'Sign in' links to /auth/signin"
    - "Secondary CTA 'İletişim'/'Request access' is a mailto:burakkbayrak@gmail.com link"
    - "Every landing string exists in BOTH messages/tr.json and messages/en.json with no hardcoded copy in JSX"
    - "npx tsc --noEmit is clean and npm run build succeeds"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Bilingual landing page root route (RSC) with hero, problem/solution, how-it-works, features, footer"
      contains: "getTranslations"
    - path: "src/components/landing/LandingHeader.tsx"
      provides: "Landing header with wordmark + LanguageToggle + primary Sign in CTA"
    - path: "src/components/landing/Section.tsx"
      provides: "Reusable landing section wrapper enforcing 8pt spacing + max-width container"
    - path: "messages/tr.json"
      provides: "landing namespace (Turkish)"
      contains: "landing"
    - path: "messages/en.json"
      provides: "landing namespace (English)"
      contains: "landing"
  key_links:
    - from: "src/app/page.tsx"
      to: "messages/{tr,en}.json landing namespace"
      via: "getTranslations('landing')"
      pattern: "getTranslations\\(['\\\"]landing"
    - from: "src/components/landing/LandingHeader.tsx"
      to: "src/components/layout/LanguageToggle.tsx"
      via: "import of existing toggle (cookie + reload)"
      pattern: "LanguageToggle"
    - from: "src/app/page.tsx"
      to: "/auth/signin"
      via: "primary CTA link"
      pattern: "/auth/signin"
---

<objective>
Replace the create-next-app boilerplate at `/` (src/app/page.tsx) with a concise, marketing-oriented, fully bilingual (TR/EN) landing page for bayrak.ai, built on the EXISTING next-intl setup and the locked Phase 5 design system.

Purpose: bayrak.ai currently shows the Next.js starter at its root. A real landing page communicates the product (a structured field-ops loop replacing WhatsApp/phone chaos) to Turkish subcontractor firms, routes engineers to sign-in, and routes prospects to a gated access request — without implying open self-signup.

Output: A bilingual landing page composed of small landing-only components, a new `landing` namespace in both message files (full TR/EN parity), and a TR/EN toggle that reuses the dashboard's exact cookie+reload mechanism. No dashboard/auth/bot/Phase-5 code is touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@.planning/phases/05-dashboard-map/05-UI-SPEC.md

<!-- i18n wiring — cookie-based, NO [locale] path segments. Replicate exactly. -->
@src/i18n/request.ts
@src/app/layout.tsx
@src/components/layout/LanguageToggle.tsx
@src/components/layout/TopNav.tsx

<!-- design tokens + button variants the landing must reuse -->
@src/app/globals.css
@src/components/ui/button.tsx

<!-- file being replaced + message files to extend -->
@src/app/page.tsx
@messages/tr.json
@messages/en.json

<interfaces>
<!-- Established patterns extracted from codebase. Use directly — no exploration needed. -->

i18n mechanism (CRITICAL — cookie-based, no routing locale):
- src/i18n/request.ts: getRequestConfig reads cookie `locale` (default 'tr'), imports `messages/${locale}.json`.
- src/app/layout.tsx: RootLayout is async; calls getLocale() + getMessages(), wraps children in NextIntlClientProvider (messages={messages}). <html lang={locale}> already set. DO NOT modify layout.tsx.
- Static text in a Server Component: import { getTranslations } from 'next-intl/server' then const t = await getTranslations('landing'). page.tsx is an RSC (async function) — use this pattern.
- Current locale in a Server Component: import { getLocale } from 'next-intl/server' then const locale = await getLocale().

LanguageToggle (REUSE — do not reimplement):
- src/components/layout/LanguageToggle.tsx exports LanguageToggle ('use client').
- Signature: LanguageToggle({ currentLocale }: { currentLocale: string }).
- It sets document.cookie = "locale=...; path=/; max-age=31536000; SameSite=Lax" then window.location.reload(). This is the locale-switch mechanism — import and pass the server-resolved locale.

Button (shadcn base-nova, @base-ui/react):
- import { Button, buttonVariants } from '@/components/ui/button'.
- variants: default (bg-primary = accent — use for PRIMARY CTA only), outline, secondary, ghost, link.
- sizes: default (h-8), sm (h-7), lg (h-9), icon. For a prominent landing CTA use size="lg".
- Button is NOT a Next.js Link. For CTAs that navigate, prefer applying buttonVariants({ variant, size }) as className on a next/link <Link> or an <a>. buttonVariants is exported from button.tsx.

Card (available, optional for feature/problem cards):
- import { Card, CardHeader, CardTitle, CardDescription, CardContent } from '@/components/ui/card'.

Icons: lucide-react ^1.16 is installed. Optional for how-it-works/feature glyphs (e.g. MessageSquare, CheckCircle2, MapPin). Icons must NEVER be the only signal — always pair with a text label.

messages structure: top-level namespaces are auth, nav, dashboard, common (TR/EN at parity). ADD a new sibling top-level key `landing` to BOTH files. Do not nest under existing namespaces.

Design tokens (globals.css, OKLCH neutral):
- 60% dominant: bg-background / text-foreground.
- 30% secondary: bg-card / bg-muted + text-muted-foreground (section alternation, feature cards).
- 10% accent: --primary via bg-primary text-primary-foreground (default Button) — RESERVED for the primary "Sign in" CTA only. Secondary CTA uses outline variant.
- Weights: only 400 (normal) and 600 (font-semibold). For landing headings use font-semibold. No 700/font-bold.
- Spacing: 8pt grid. Section vertical padding py-16 (64px) or py-12; container max-w-5xl mx-auto px-6 (matches TopNav). Element gaps from {gap-2, gap-4, gap-6, gap-8}.
- Typography: hero display may scale up (text-3xl sm:text-4xl font-semibold is acceptable for a marketing hero, still weight 600). Body = text-sm/text-base text-muted-foreground. Section headings = text-xl font-semibold or larger.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the landing namespace to both message files (TR + EN parity)</name>
  <files>messages/tr.json, messages/en.json</files>
  <read_first>
    @messages/tr.json
    @messages/en.json
    @.planning/phases/05-dashboard-map/05-UI-SPEC.md
  </read_first>
  <action>
    Add a new top-level `landing` namespace key (sibling of auth, nav, dashboard, common) to BOTH messages/tr.json and messages/en.json. The two files MUST have identical key structure (full parity) — only the string values differ by language. Turkish is the primary/reference language; write natural, professional Turkish for subcontractor-firm decision-makers, then provide accurate English equivalents.

    Define keys covering every landing string so NO copy is hardcoded in JSX. Use this key tree (refine values for quality, but keep both files in lockstep):

    landing:
      hero: { title (bayrak.ai), tagline (one-line value prop: a structured, trackable, geolocated, AI-assisted field-ops loop that replaces WhatsApp/phone chaos), cta_primary (Giriş Yap / Sign in), cta_secondary (İletişim / Request access) }
      problem: { heading, before_title (e.g. Dağınık WhatsApp ve telefon / Scattered WhatsApp and calls), before_body, after_title (e.g. Tek güvenilir akış / One trustworthy loop), after_body }
      how: { heading, step1_title, step1_body (worker logs work via Turkish Telegram bot: photo, location, quantity, notes), step2_title, step2_body (on-site auditor approves or rejects from Telegram), step3_title, step3_body (office sees live Mapbox map and BOQ auto-deducts as work is approved) }
      features: { heading, telegram_title, telegram_body, audit_title, audit_body, map_title, map_body, boq_title, boq_body, geo_title, geo_body (geolocation / anomaly flagging), bilingual_title, bilingual_body (TR/EN) }
      footer: { tagline (short), copyright (e.g. © 2026 bayrak.ai) }
      a11y: { skip_to_content, lang_region_label }

    Copy constraints (scope fence on messaging):
    - The secondary CTA must read as "request access" / contact — NOT "sign up" / "create account". Signup is gated by an allowlist; do not imply open self-signup anywhere in the copy.
    - Keep all copy concise and marketing-oriented (short sentences, no walls of text).
    - Do not invent unbuilt features. Stick to: Telegram work-logging, on-site audit, live Mapbox progress map, auto-deducting BOQ, geolocation/anomaly flagging, TR/EN bilingual.

    Keep both files valid JSON (no trailing commas). Preserve the existing namespaces untouched.
  </action>
  <verify>
    <automated>node -e "const tr=require('/Users/arifismailbayrak/bayrak-ai/messages/tr.json'); const en=require('/Users/arifismailbayrak/bayrak-ai/messages/en.json'); const flat=(o,p='')=>Object.entries(o).flatMap(([k,v])=>typeof v==='object'&&v?flat(v,p+k+'.'):[p+k]); const tk=flat(tr.landing).sort(), ek=flat(en.landing).sort(); if(JSON.stringify(tk)!==JSON.stringify(ek)){console.error('PARITY FAIL'); console.error('tr-only:',tk.filter(k=>!ek.includes(k))); console.error('en-only:',ek.filter(k=>!tk.includes(k))); process.exit(1);} if(tk.length<15){console.error('too few keys:',tk.length);process.exit(1);} console.log('landing parity OK,',tk.length,'keys');"</automated>
  </verify>
  <acceptance_criteria>
    - Both files parse as valid JSON (the node require above does not throw).
    - landing is a top-level key in BOTH files; existing namespaces (auth, nav, dashboard, common) are unchanged.
    - The flattened landing key sets of tr.json and en.json are IDENTICAL (parity script prints "landing parity OK" with >=15 keys).
    - Secondary CTA value is contact/request-access wording, never signup-implying. Run: grep -iE "sign ?up|create account|kayit ol|hesap olustur" messages/tr.json messages/en.json — must return no match for the landing copy.
  </acceptance_criteria>
  <done>Both message files contain a complete, parity-matched landing namespace with marketing copy; no signup-implying strings.</done>
</task>

<task type="auto">
  <name>Task 2: Build landing-only components (Section wrapper + LandingHeader)</name>
  <files>src/components/landing/Section.tsx, src/components/landing/LandingHeader.tsx</files>
  <read_first>
    @src/components/layout/LanguageToggle.tsx
    @src/components/layout/TopNav.tsx
    @src/components/ui/button.tsx
    @src/app/globals.css
    @.planning/phases/05-dashboard-map/05-UI-SPEC.md
  </read_first>
  <action>
    Create two small landing-only components under src/components/landing/. Do NOT modify anything in src/components/layout/ or src/components/dashboard/.

    Section.tsx (Server Component, no 'use client'): a presentational wrapper enforcing the design system container + spacing. Props: { id?: string; children: React.ReactNode; muted?: boolean; className?: string }. Render a <section> with py-16 vertical rhythm and an inner <div className="max-w-5xl mx-auto px-6"> (matches TopNav container). When muted is true apply bg-muted for 30%-band section alternation; otherwise bg-background. Merge any extra className via the existing cn helper from '@/lib/utils'. Keep it tiny.

    LandingHeader.tsx (Server Component): the top bar for the landing page, structurally mirroring TopNav but landing-scoped (do NOT import or reuse TopNav, which is auth/dashboard-bound). Use getLocale and getTranslations from 'next-intl/server'. Layout: sticky top-0 z-40 h-14 bg-card border-b border-border with inner max-w-5xl mx-auto px-6 h-full flex items-center justify-between. Left: wordmark <span className="text-xl font-semibold">bayrak.ai</span> (literal product name is acceptable as the wordmark, or pull from landing.hero.title). Right: a flex row (gap-3) containing the IMPORTED existing LanguageToggle from '@/components/layout/LanguageToggle' (pass currentLocale={locale}) followed by the primary "Sign in" CTA.

    Primary CTA: render as a next/link <Link href="/auth/signin"> styled with buttonVariants({ size: 'lg' }) (default variant = accent/bg-primary). Import buttonVariants from '@/components/ui/button' and Link from 'next/link'. Text from t('hero.cta_primary') via getTranslations('landing'). This is the ONLY accent-colored element in the header.

    Accessibility: header is a <header>; the CTA is a real link (keyboard-focusable, gets the Button focus ring via buttonVariants). Reuse LanguageToggle's built-in aria. No color-only signals.
  </action>
  <verify>
    <automated>test -f /Users/arifismailbayrak/bayrak-ai/src/components/landing/Section.tsx && test -f /Users/arifismailbayrak/bayrak-ai/src/components/landing/LandingHeader.tsx && grep -q "LanguageToggle" /Users/arifismailbayrak/bayrak-ai/src/components/landing/LandingHeader.tsx && grep -q "/auth/signin" /Users/arifismailbayrak/bayrak-ai/src/components/landing/LandingHeader.tsx && grep -q "getTranslations" /Users/arifismailbayrak/bayrak-ai/src/components/landing/LandingHeader.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Both component files exist under src/components/landing/.
    - LandingHeader imports the EXISTING LanguageToggle from '@/components/layout/LanguageToggle' (grep: LanguageToggle present) — toggle is not reimplemented.
    - LandingHeader links to /auth/signin and uses getTranslations for all visible text (no hardcoded copy except the bayrak.ai wordmark).
    - No file under src/components/layout/ or src/components/dashboard/ was modified (only files under src/components/landing/ added).
    - Primary CTA uses the default (accent) button styling; LanguageToggle is the only other interactive control in the header.
  </acceptance_criteria>
  <done>Section and LandingHeader exist; header reuses the existing locale toggle and routes the primary CTA to /auth/signin, all copy via i18n.</done>
</task>

<task type="auto">
  <name>Task 3: Replace page.tsx with the bilingual landing page (hero, problem to solution, how-it-works, features, footer)</name>
  <files>src/app/page.tsx</files>
  <read_first>
    @src/app/page.tsx
    @src/app/layout.tsx
    @src/i18n/request.ts
    @src/components/landing/LandingHeader.tsx
    @src/components/landing/Section.tsx
    @src/components/ui/button.tsx
    @src/components/ui/card.tsx
    @.planning/phases/05-dashboard-map/05-UI-SPEC.md
    @src/app/globals.css
  </read_first>
  <action>
    Completely replace the create-next-app boilerplate in src/app/page.tsx. Remove ALL boilerplate: the next/image import, next.svg/vercel.svg/file.svg/window.svg/globe.svg usage, the "Get started by editing" / "Save and see your changes" / "Deploy now" / "Read our docs" content, and the grid-rows boilerplate layout.

    Make Home an async Server Component. Use getTranslations('landing') for ALL copy. Render <LandingHeader /> (from '@/components/landing/LandingHeader') as the top bar, then a <main> containing these sections built with the Section wrapper (from '@/components/landing/Section'; alternate the muted prop for 30%-band rhythm):

    1. HERO: <h1> heading t('hero.title') (text-3xl sm:text-4xl font-semibold), t('hero.tagline') as a text-base sm:text-lg text-muted-foreground lead paragraph, then a CTA row (flex flex-col sm:flex-row gap-4):
       - Primary: next/link to /auth/signin styled buttonVariants({ size: 'lg' }) (default/accent) — text t('hero.cta_primary').
       - Secondary: an <a href="mailto:burakkbayrak@gmail.com"> styled buttonVariants({ variant: 'outline', size: 'lg' }) — text t('hero.cta_secondary'). This is "Request access"/"İletişim"; it MUST be a mailto link, NOT a signup form.
    2. PROBLEM to SOLUTION: <h2> t('problem.heading'); two side-by-side cards/columns (grid sm:grid-cols-2 gap-6): before (WhatsApp/calls chaos: before_title/before_body) and after (one trustworthy loop: after_title/after_body). Use Card or plain bordered divs; differentiate with layout/weight/text, NEVER color alone, and DO NOT use accent color here (accent reserved for the primary CTA).
    3. HOW IT WORKS: <h2> t('how.heading'); 3 numbered steps in a grid sm:grid-cols-3 gap-6. Each step: a visible number or lucide icon (optional) PAIRED with stepN_title (font-semibold) and stepN_body. Steps: (1) worker logs via Telegram, (2) auditor approves on-site, (3) office sees live map + BOQ auto-updates.
    4. KEY FEATURES: <h2> t('features.heading'); a responsive grid (grid sm:grid-cols-2 lg:grid-cols-3 gap-6) of feature cards covering: Telegram work-logging, on-site audit, live Mapbox progress map, auto-deducting BOQ, geolocation/anomaly flagging, TR/EN bilingual. Each card: title (font-semibold) + body. Optional lucide icon as secondary cue only.
    5. FOOTER: a <footer> element with t('footer.tagline') and t('footer.copyright'), text-sm text-muted-foreground, centered, border-t border-border.

    Rules: semantic heading hierarchy (one <h1> = hero title; section headings <h2>; card titles <h3> or font-semibold paragraphs). Only weights 400/600. Accent (bg-primary) appears ONLY on the primary Sign-in CTA. Genuinely mobile-usable: single-column by default, sm:/lg: grids for larger viewports. No hardcoded copy — every visible string from the landing namespace. Do not modify layout.tsx; the existing <title>bayrak.ai</title> is fine (optionally export a landing-scoped metadata const from page.tsx).
  </action>
  <verify>
    <automated>cd /Users/arifismailbayrak/bayrak-ai && npx tsc --noEmit && grep -Eq "getTranslations\\((['\"])landing" src/app/page.tsx && grep -q "/auth/signin" src/app/page.tsx && grep -q "mailto:burakkbayrak@gmail.com" src/app/page.tsx && ! grep -Eq "next\\.svg|Get started by editing|Deploy now|Read our docs" src/app/page.tsx && echo "PAGE OK"</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit exits 0 (clean).
    - npm run build succeeds (run: cd /Users/arifismailbayrak/bayrak-ai && npm run build — exit 0).
    - page.tsx uses getTranslations('landing') and contains NO boilerplate: grep -Eq "next\\.svg|Get started by editing|Deploy now|Read our docs" src/app/page.tsx returns NO match (the boilerplate is gone).
    - Primary CTA links to /auth/signin (grep match); secondary CTA is mailto:burakkbayrak@gmail.com (grep match) and contains no signup form.
    - <LandingHeader /> and <Section> from src/components/landing/ are used; only src/app/page.tsx was modified in this task.
    - Page renders all five sections (hero, problem/solution, 3-step how-it-works, features grid, footer) with one <h1> and <h2> section headings.
  </acceptance_criteria>
  <done>page.tsx is a bilingual Server-Component landing page with all five sections, both CTAs wired, zero boilerplate, tsc clean, build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor browser to / (public, unauthenticated) | The landing page is fully public; no auth, no user input, no data fetching. The only outbound links are /auth/signin (internal) and a mailto: (external). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2uq-01 | Information disclosure | landing copy | mitigate | Copy must not reveal gated-signup internals beyond "request access"; no env vars, no internal URLs, no allowlist details in rendered text. |
| T-2uq-02 | Tampering | secondary CTA target | mitigate | mailto: is a static literal (burakkbayrak@gmail.com); no user-controlled href interpolation, no open-redirect surface. |
| T-2uq-03 | Tampering | npm/pip/cargo installs | accept | No new packages installed (lucide-react, next-intl, shadcn already present). No package legitimacy gate required. |
</threat_model>

<verification>
Phase-level checks (run from /Users/arifismailbayrak/bayrak-ai):
1. `npx tsc --noEmit` — clean, no type errors.
2. `npm run build` — succeeds (exit 0).
3. Message parity: the Task 1 node parity script prints "landing parity OK".
4. Boilerplate removed: `grep -Eq "next\.svg|Get started by editing|Deploy now|Read our docs" src/app/page.tsx` returns no match.
5. CTA wiring: `grep -q "/auth/signin" src/app/page.tsx` and `grep -q "mailto:burakkbayrak@gmail.com" src/app/page.tsx` both match.
6. Toggle reuse: `grep -rq "LanguageToggle" src/components/landing/` matches (no reimplementation).
7. Scope fence: only messages/tr.json, messages/en.json, src/app/page.tsx, and src/components/landing/* changed; nothing under src/components/layout, src/components/dashboard, src/app/auth, src/app/api, or src/app/dashboard.
</verification>

<success_criteria>
- `/` renders a bilingual marketing landing page (hero, problem to solution, 3-step how-it-works, key features, footer), NOT the Next.js starter.
- TR/EN toggle present in the header, reusing the existing cookie+reload LanguageToggle; switching changes all landing copy.
- Primary CTA "Giriş Yap"/"Sign in" links to /auth/signin; secondary "İletişim"/"Request access" opens mailto:burakkbayrak@gmail.com (no implied open signup).
- landing namespace exists in both messages/tr.json and messages/en.json at full key parity; no hardcoded strings in JSX.
- Design system respected: OKLCH tokens, weights 400/600 only, accent reserved for the primary CTA, 60/30/10 split via background/muted bands, mobile-usable single-column layout that expands to grids on larger viewports.
- `npx tsc --noEmit` clean and `npm run build` green.
- Scope fence honored: only the 5 declared files (+ src/components/landing/*) touched.
</success_criteria>

<output>
Create `.planning/quick/260525-2uq-bilingual-tr-en-marketing-landing-page-a/260525-2uq-SUMMARY.md` when done.
</output>
