---
quick_id: 260601-kj4
description: Field-Industrial premium UI foundation — tokens + Space Grotesk display + brand primitives + dashboard shell
mode: quick-full
created: 2026-06-01
---

# Quick Task 260601-kj4: Field-Industrial UI Foundation

Foundation increment of the premium UI/brand pass. Direction chosen by user: **Field-Industrial, light-canvas** (graphite ink + cool steel neutrals + amber as a restrained hi-vis safety accent; technical type; flat hairline-bordered panels; monospaced figures). Per-screen polish is a follow-up. Preserve ALL functionality, i18n keys, and component prop APIs — visual/token changes only.

## Design contract (concrete — implement exactly)

### Tokens — `src/app/globals.css` `:root` (light). Replace the amber-on-slate values:
- `--background`: `oklch(0.985 0.0015 250)` (near-white cool steel canvas)
- `--foreground`: `oklch(0.22 0.012 255)` (graphite ink)
- `--card`: `oklch(1 0 0)`; `--card-foreground`: graphite
- `--popover`/`--popover-foreground`: white / graphite
- `--primary`: `oklch(0.769 0.188 70)` (amber — KEEP); `--primary-foreground`: `oklch(0.20 0.015 60)` (graphite reads on amber)
- `--secondary`: `oklch(0.965 0.004 255)` (steel-100); `--secondary-foreground`: graphite
- `--muted`: `oklch(0.965 0.004 255)`; `--muted-foreground`: `oklch(0.50 0.014 256)` (steel)
- `--accent`: `oklch(0.97 0.03 88)` (faint amber wash); `--accent-foreground`: graphite
- `--border`: `oklch(0.90 0.005 256)` (hairline steel); `--input`: same
- `--ring`: amber (`oklch(0.769 0.188 70)`)
- charts: amber / sky / emerald / amber-700 / steel-400 (keep current mapping, retune to cool greys)
- `--radius`: `0.25rem` (engineered)
- sidebar = **graphite panel** (signature industrial move; this is sidebar chrome, NOT app-wide dark mode):
  - `--sidebar`: `oklch(0.24 0.012 258)` (graphite); `--sidebar-foreground`: `oklch(0.92 0.004 250)` (light steel)
  - `--sidebar-primary`: amber; `--sidebar-primary-foreground`: graphite
  - `--sidebar-accent`: `oklch(0.30 0.014 258)` (raised graphite for hover/active row); `--sidebar-accent-foreground`: `oklch(0.97 0.003 250)`
  - `--sidebar-border`: `oklch(0.32 0.012 258)`; `--sidebar-ring`: amber
- semantic (keep): `--success` emerald, `--info` sky, `--warning` orange (NOT amber), `--destructive` red
- Leave the `.dark { }` block intact but unused (D-121).

### Fonts — `src/app/layout.tsx` + globals `@theme`:
- Add `Space_Grotesk` from `next/font/google` (`subsets: ['latin']`, `variable: '--font-space-grotesk'`, `display: 'swap'`). Add its `.variable` to the `<html>` className alongside Geist.
- In globals `@theme inline`: set `--font-heading: var(--font-space-grotesk), var(--font-geist-sans), ui-sans-serif, system-ui, sans-serif;` Keep `--font-sans` (Geist Sans body) and `--font-mono` (Geist Mono).
- Add a `.font-data` / `.tabular` utility (or reuse `font-mono` + `tabular-nums`) and document the rule: ALL numerics (qty, chainage, BOQ codes, IDs, currency, %, dates) use `font-mono tabular-nums`. (Apply broadly in this task where primitives render figures; per-screen sweep is follow-up.)
- Headings already get negative tracking in `@layer base`; ensure h1–h4 use `font-heading` (Space Grotesk).

### Surface language (globals utilities):
- Prefer **1px hairline borders + flat panels** over heavy shadows. Define a `.panel` utility (bg-card, 1px border-border, radius-md) and a subtle `.elevated` (border + a single soft shadow `0 1px 2px oklch(0 0 0 / 0.04)`).
- Add a `.rule-tick` / blueprint divider utility: a thin border-top with a small amber tick (e.g. `::before` 2px×8px amber block) for section dividers. Use sparingly.

### Brand primitives — `src/components/brand/` (visual only; keep props/exports):
- **BrandButton**: crisp `transition-colors duration-[120ms]` (no scale/bounce). `primary` = amber bg + graphite text + amber-600 hover; `secondary` = white panel + hairline border + graphite text; `ghost` = transparent + steel hover; `destructive` = red. `radius-sm`. Strong `focus-visible:ring-2 ring-ring`.
- **BrandCard**: flat `.panel` — `bg-card border border-border radius-md`, drop heavy shadow; Header `p-3` / Body `p-4` tighter rhythm; hairline border between header and body.
- **BrandHeading**: `font-heading` (Space Grotesk), tight tracking, weight per size; display size uses existing `.text-display`.
- **BrandBadge**: squared-off industrial chips (`radius-sm`, uppercase `text-[11px] tracking-wide font-semibold`, 1px border, tonal bg) — variants: neutral (steel), success, info, warning, destructive, + amber/primary. Keep current variant names; restyle only.
- **BrandTable**: dense (`text-sm`, tight row padding `py-2 px-3`), zebra-free, sticky `thead` (graphite-tinted or steel-100 bg, uppercase steel labels), hairline row separators (`divide-y divide-border`), right-aligned + `font-mono tabular-nums` numeric cells (provide an `align="right"`/numeric cell affordance or document usage).
- **BrandEmpty**, **BrandLogo**: align to steel/graphite + amber; keep logo mark, ensure amber accent.

### Dashboard shell:
- **AppSidebar.tsx / SidebarNav.tsx**: graphite panel (uses `--sidebar*` tokens), light steel labels, **amber left-accent bar + amber text/icon on active item**, raised-graphite hover, hairline section separators, tighter density. Keep all nav items + i18n + active-route logic.
- **TopNav.tsx**: white/steel bar, hairline bottom border, graphite text, amber focus rings; keep locale switch, user menu, all behavior.

## Tasks

1. **Tokens + fonts** — globals.css `:root` token swap + radius + surface/utility additions; layout.tsx Space Grotesk wiring; `@theme` `--font-heading` mapping. Verify: `grep` for new oklch graphite/steel values + `--font-space-grotesk`; `npx tsc --noEmit`.
2. **Brand primitives** — restyle the 7 brand components per contract; keep all exports/props/test compatibility (BrandButton.test.tsx, BrandLogo.test.tsx must still pass). Verify: `npx vitest run src/components/brand` (or full) green; visual classes reflect new system.
3. **Dashboard shell** — restyle AppSidebar, SidebarNav, TopNav. Verify: `npx tsc --noEmit`; `npm run build` green; `npx vitest run` all pass.

## Acceptance / verify
- `npx tsc --noEmit` clean.
- `npm run build` green (all routes).
- `npx vitest run` — all existing tests pass (esp. BrandButton/BrandLogo/i18n).
- `grep -q "font-space-grotesk" src/app/layout.tsx src/app/globals.css`.
- No component renamed; no prop API changed; no i18n key removed; no business logic/DB touched.
- Amber appears only as accent (CTAs, active nav, focus, status) — not as fill across surfaces.

## Notes
- Light-canvas; dark mode stays deferred (D-121). The graphite SIDEBAR is chrome, not app-wide dark mode.
- This is the foundation; follow-up tasks polish overview, records, hakkedis, exports, and the landing per-screen.
