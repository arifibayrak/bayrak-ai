---
quick_id: 260525-3fc
slug: polish-ui-ux-typography-fonts-spacing-vi
title: Polish UI/UX — typography, fonts, spacing, visual hierarchy
created: 2026-05-25
mode: quick
---

# Quick Task 260525-3fc — Plan

Visual/typographic polish across landing, auth, and dashboard. Decisions locked in
260525-3fc-CONTEXT.md: Inter (latin+latin-ext), Industrial Blue accent on slate
neutrals, richer light theme, bold+tight headings. No feature/route/data changes.
Token-driven: fixing globals.css propagates accent + neutrals through every shadcn primitive.

## Task 1 — Typography & color foundation (root-cause fix)
- **files:** `src/app/layout.tsx`, `src/app/globals.css`, `src/components/ui/card.tsx`,
  `.planning/phases/05-dashboard-map/05-UI-SPEC.md`
- **action:**
  - `layout.tsx`: replace Geist/Geist_Mono with `Inter({ subsets:["latin","latin-ext"], variable:"--font-inter", display:"swap" })`; body uses `${inter.variable} antialiased`. (`latin-ext` is mandatory — İ ı Ş ş Ğ ğ live there.)
  - `globals.css`: fix the self-referential `--font-sans`; map `--font-sans`/`--font-heading` → `var(--font-inter), …fallbacks`, `--font-mono` → ui-monospace stack. Replace pure-gray OKLCH tokens with slate-tinted neutrals + Industrial Blue `--primary`/`--ring` (light + dark). Add display type-scale + heading tracking in base layer. Keep destructive(red)/amber/emerald + BOQ hex.
  - `card.tsx`: swap near-invisible `ring-1 ring-foreground/10` for a real `border` + subtle `shadow-sm` for depth.
  - `05-UI-SPEC.md`: annotate Typography + Color sections — superseded by this task (Inter, blue accent, 4 weights, bold/tight display).
- **verify:** `npx tsc --noEmit`; grep confirms no `var(--font-sans): var(--font-sans)` self-ref; Inter referenced.
- **done:** App renders in Inter with Turkish glyphs; blue accent + slate neutrals live globally; cards have depth.

## Task 2 — Landing + auth polish
- **files:** `src/app/page.tsx`, `src/components/landing/LandingHeader.tsx`,
  `src/components/landing/Section.tsx`, `src/app/auth/signin/page.tsx`,
  `messages/tr.json`, `messages/en.json` (only if new strings)
- **action:** Fluid display hero (`clamp` ~40→60px / 700 / tracking-tight), relaxed tagline, larger CTAs (~h-12); vary section rhythm; polish problem/how/features cards + step badges with accent; refine header wordmark; scale signin heading + card. New visible strings → tr.json AND en.json.
- **verify:** `npx tsc --noEmit`; both locales have any new keys; toggle untouched.
- **done:** Landing reads polished with real hierarchy; auth matches.

## Task 3 — Dashboard polish
- **files:** `src/app/dashboard/layout.tsx`, `src/app/dashboard/projects/page.tsx`,
  `src/components/dashboard/ProjectCard.tsx`, `src/app/dashboard/projects/[id]/page.tsx`,
  `src/components/ui/tabs.tsx` (accent active underline), `messages/tr.json`, `messages/en.json` (if new strings)
- **action:** Stronger page titles (28px/700) + optional muted subtitle; richer ProjectCard hierarchy/meta; accent active-tab underline (`after:bg-primary`); tighten content rhythm. New strings → tr+en.
- **verify:** `npx tsc --noEmit`; `npm run build` passes.
- **done:** Dashboard chrome cohesive with new system; build green.

## Post
- `npx tsc --noEmit` + `npm run build` both pass → tell user to redeploy via `vercel --prod --yes` (manual; no git remote).
